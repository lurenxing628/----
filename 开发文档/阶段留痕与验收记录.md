# 阶段留痕与验收记录（必做）

> 目的：确保“每个阶段搞定”都能**复盘、回滚、排障、审计**。  
> 本文档用于记录 **Phase/步骤级** 的开发验收留痕；同时对接系统内置的 **业务留痕（OperationLogs/ScheduleHistory）** 与 **文件日志（logs）**。

---

## 1. 留痕分层（你需要留下哪些“证据”）

### 1.1 开发阶段留痕（本文件记录的核心）
- **提交记录（必须）**：每个步骤完成后至少 1 次提交，commit message 建议包含步骤 ID（如 `P1-03`）。  
- **验收记录（必须）**：在本文档里记录验收时间、负责人、版本号（commit hash）、验收结果、证据位置。
- **证据附件（推荐）**：截图/导出文件/关键日志片段，统一放到 `evidence/` 目录（建议按日期或步骤号分目录）。

### 1.1.1 你提出的“每完成功能就要测试+打点+留痕”（强烈建议按此执行）
- **最小测试（必须）**：每实现一个功能，至少做一次“冒烟测试”（主路径 + 1~2 个常见异常路径）。
- **打点日志（必须）**：关键链路要有清晰的调试日志（开始/结束/耗时/关键参数/异常原因），方便用户排障。
- **留痕核对（必须）**：涉及导入/导出/排产等关键操作时，核对 DB `OperationLogs` / `ScheduleHistory` 是否写入正确。
- **证据归档（必须）**：把本次测试结果与证据放进 `evidence/`，并在本文件的“阶段验收记录”里引用对应路径。

### 1.2 业务操作留痕（系统内置，必须打通）
按《开发文档.md》要求，关键操作必须写入：
- **数据库 `OperationLogs`**：导入/导出/排产/报表（未来）统一留痕（detail 为 JSON 字符串）。  
  - 注意：detail 的**键名必须按文档固定**（例如 `filename/mode/time_cost_ms` 等），便于统一报表与审计。
- **数据库 `ScheduleHistory`**：排产历史（Phase7 才会用到，但从 Phase0 就要保留表结构）。

### 1.3 运行诊断留痕（用户排障必须）
- **文件日志**：`logs/aps.log` + `logs/aps_error.log`（滚动日志）。  
- **用户可见信息中文化（硬约束）**：页面提示、接口错误 message、预览行提示、系统日志页展示等必须中文，方便排查。

---

## 2. 命名与存放规范（建议）

### 2.1 证据目录（推荐）
在项目根目录创建 `evidence/`（可提交空目录占位），建议结构：
- `evidence/Phase0_M0/`
- `evidence/Phase1_Excel/`
- `evidence/PhaseX_.../`
也可以进一步细分为：
- `evidence/Phase1_Excel/2026-01-20/`（按日期）
- `evidence/Phase1_Excel/P1-03/`（按步骤ID）

### 2.2 文件命名（推荐）
- 截图：`YYYY-MM-DD_Px-yy_说明.png`
- 导出文件：`YYYY-MM-DD_export_类型.xlsx`
- 日志摘录：`YYYY-MM-DD_Px-yy_log_excerpt.txt`

---

## 3. 通用验收记录模板（复制后填）

> 建议：每个步骤完成就追加一条记录，避免堆到最后补。

### 3.X（填写步骤ID，如 P1-03）
- **步骤ID**：P?-??
- **阶段/里程碑**：Phase ? / M?
- **完成日期**：YYYY-MM-DD
- **负责人**：xxx
- **版本号（commit）**：`<git commit hash>`
- **变更范围**：列出关键文件（如 `app.py` / `core/...`）
- **验收项**：
  - [ ] 可运行（启动无报错）
  - [ ] 关键功能可用（按本步骤定义）
  - [ ] 留痕齐全（OperationLogs / 文件日志）
  - [ ] 用户可见报错/提示为中文
- **证据**：
  - 日志文件：`logs/aps.log` / `logs/aps_error.log`（可附摘录）
  - DB 证据：SQL 查询结果（粘贴输出）
  - 截图/附件：`evidence/...`
- **结论**：通过 / 不通过（原因）

---

## 4. Phase 0（M0）验收留痕要点

### 4.1 必须验证项（M0 阻断）
- Flask 可启动并打开首页  
- 启动时自动创建/打开数据库：`db/aps.db`  
- 表结构齐全：至少包含文档列出的所有 V1 表（含预留表）  
- 日志正常输出：`logs/aps.log` 与 `logs/aps_error.log`  
- 退出自动备份有效：退出进程后 `backups/` 出现 `*_auto.db`  
- **证明“并发锁未实现”**：
  - 项目中不存在 `core/infrastructure/locking.py`
  - DB 中不存在 `ResourceLocks` 表

### 4.2 建议证据（可直接粘贴到验收记录）
#### A) 表存在性检查（SQLite）
（任选其一方式执行并粘贴结果）：
- 用 SQLite CLI（如有）：查询表清单
- 或用 DB Browser for SQLite（推荐，非强制）

SQL 示例：
```sql
-- 1) 列出所有表
SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;

-- 2) 证明不存在 ResourceLocks
SELECT name FROM sqlite_master WHERE type='table' AND name='ResourceLocks';
```

#### B) 自动备份证据
- 退出应用后，列出 `backups/` 目录最新文件名（截图或文字）

---

## 5. Phase 1（通用 Excel 能力）验收留痕要点

### 5.1 必须验证项
- `OpenpyxlBackend.read()` 能读取 `.xlsx` 并返回 `List[Dict]`（无 DataFrame）
- `ExcelService.preview_import()`：
  - 行号：`row_num = idx + 2`（标题行+1）
  - 状态正确：NEW/UPDATE/UNCHANGED/SKIP/ERROR
  - diff 正确：`changes[field] = (old, new)`
  - message 为中文（便于用户定位问题）
- 导入/导出留痕写入 `OperationLogs`：
  - import：detail 至少包含 `filename/mode/total_rows/.../errors_sample/time_cost_ms`
  - export：detail 至少包含 `template_or_export_type/filters/row_count/time_range`

### 5.2 推荐验收路径（本项目已提供 demo）
- 打开：`/excel-demo/`
- 下载模板并导入一次（产生 NEW）
- 再用同一文件导入一次（产生 UPDATE/UNCHANGED 或 SKIP，取决于 mode）
- 导入一个错误文件（如缺“工号/姓名/状态”）观察 ERROR 行提示是否中文清晰

### 5.3 建议证据（SQL）
```sql
-- 查看最近 20 条操作日志（导入/导出）
SELECT id, log_time, log_level, module, action, target_type, target_id, error_code, error_message, detail
FROM OperationLogs
ORDER BY id DESC
LIMIT 20;
```

---

## 6. 阶段验收记录（在这里持续追加）

> 你可以从 M0 与 Phase1 开始填写；后续 Phase2~Phase10 继续在此追加。

### 6.1 Phase 0 / M0
- （填写你的实际验收记录）

### 6.2 Phase 1 / 通用 Excel 能力
- （填写你的实际验收记录）

### 6.3 Phase 2 / 数据访问层（Repositories）与模型（Models）
- **步骤ID**：P2-01 / P2-02 / P2-03
- **阶段/里程碑**：Phase 2（为后续各业务模块服务层做准备）
- **完成日期**：2026-01-20
- **负责人**：待填
- **版本号（commit）**：N/A（当前目录未启用 git）
- **变更范围（关键文件）**：
  - `data/repositories/base_repo.py`（基础仓库类：SQL 执行 + 异常翻译）
  - `data/repositories/*_repo.py`（各表仓库：最小 CRUD + 常用查询）
  - `core/models/*.py`（最小领域模型：字段与 `schema.sql` 对齐）
  - `tests/smoke_phase2.py`（Phase2 冒烟测试）
- **验收项**：
  - [x] Repo 覆盖到 Phase2 清单关键表（人员/设备/工艺/批次/排程/日历/配置/留痕）
  - [x] UNIQUE/FK 等数据库完整性异常可被捕获并翻译为**中文** `AppError`（避免用户看到英文报错）
  - [x] 冒烟测试通过（CRUD、UNIQUE/FK、日历 upsert、配置 set/get、留痕表写入与查询）
  - [x] 用户可见提示尽量中文（错误 message、报告内容）
- **证据**：
  - 冒烟测试报告：`evidence/Phase2/smoke_phase2_report.md`
  - 报告内包含关键 SQL 输出：Operators/Machines/OperatorMachine 行数等（可用于复核）
- **结论**：通过

