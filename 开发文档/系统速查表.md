# 回转壳体单元智能排产系统（APS）— 系统速查表（V1 / Phase0~9）

> 目标：把**术语/枚举/HTTP接口/数据库表字段/留痕规范/Excel模板**一次性收敛到一份文档，后续开发无需反复研读长文档。
>
> 代码基线：以当前仓库实现为准（见 `app.py`、`schema.sql`、`web/routes/*.py`、`core/*`）。

---

## 全局硬约束（必须遵守）

- **目标环境**：Win7 单机单用户（目标机不装 Python）
- **Python**：3.8.x（打包机/开发机）
- **依赖锁定**：`requirements.txt`（openpyxl-only；不引入 pandas/numpy/schedule）
- **Excel 导入**：确认导入阶段若存在任意 `ERROR` 行，**整批拒绝导入**（不做部分导入）；错误提示必须中文且可定位到行号/字段/期望格式
- **日期/时间输入**：日期允许 `YYYY-MM-DD` / `YYYY/MM/DD`（落库统一 `YYYY-MM-DD`）；时间允许 `8:00/08:00` 且支持中文冒号 `：`（内部统一规范化）
- **并发/资源锁**：V1 **不实现**（无 `ResourceLocks` 表、无 `core/infrastructure/locking.py`）
- **备份**：退出自动备份（`atexit.register`）+ 手动备份/恢复/清理（不启后台定时线程）
- **留痕**：导入/导出/排产/报表（预留）必须可追溯（文件日志 + DB 表 `OperationLogs` / `ScheduleHistory`）
- **甘特图资源**：Frappe Gantt **0.6.1** 静态资源本地化（`static/js` + `static/css`）

---

## 目录与关键文件（开发最常用）

- **应用入口**：`app.py`
- **配置**：`config.py`
- **数据库结构**：`schema.sql`
- **基础设施**：
  - `core/infrastructure/database.py`（连接 + ensure_schema）
  - `core/infrastructure/transaction.py`（事务管理）
  - `core/infrastructure/errors.py`（错误码/异常/response helper；Flask error handlers 在 `web/error_handlers.py`）
  - `core/infrastructure/logging.py`（滚动文件日志 + DB 操作日志）
  - `core/infrastructure/backup.py`（备份管理）
- **Excel 通用**：
  - `core/services/common/openpyxl_backend.py`（openpyxl-only）
  - `core/services/common/excel_service.py`（预览/差异对比）
  - `core/services/common/excel_audit.py`（导入/导出留痕 detail 规范）
  - `core/services/common/excel_templates.py`（交付模板生成/补齐）
- **排产核心**：
  - `core/services/scheduler/config_service.py`（排产策略配置：策略/权重/派工/增强开关）
  - `core/services/scheduler/schedule_service.py`（排产执行对外入口，内部逻辑已拆分到：`operation_edit_service.py`、`schedule_input_builder.py`、`freeze_window.py`、`resource_pool_builder.py`、`schedule_optimizer.py`、`schedule_summary.py`、`schedule_persistence.py`）
  - `core/algorithms/sort_strategies.py`（排序策略：priority/due/weighted/fifo）
  - `core/algorithms/dispatch_rules.py`（SGS 派工规则：slack/cr/atc）
  - `core/algorithms/evaluation.py`（评估指标 + objective 评分）
  - `core/algorithms/ortools_bottleneck.py`（可选 OR-Tools：瓶颈工种 warm-start）
  - `core/algorithms/greedy/`（贪心排产包；入口仍为 `core.algorithms.greedy`；公共类型在 `core/algorithms/types.py`：`ScheduleResult`/`ScheduleSummary`）
- **可视化/导出**：
  - `core/services/scheduler/gantt_service.py`（甘特 tasks + 周计划行）
- **Web 路由**：`web/routes/*.py`（排产路由已拆分：`scheduler_batches.py`、`scheduler_batch_detail.py`、`scheduler_ops.py`、`scheduler_run.py`、`scheduler_config.py`、`scheduler_analysis.py`、`scheduler_calendar_pages.py`；`scheduler_pages.py` 为兼容入口）

---

## 术语表（Glossary）

- **Operator（人员）**：操机人员；表 `Operators`
- **Machine（设备）**：设备资源；表 `Machines`
- **OperatorMachine（人员-设备关联）**：人员可操作设备的共享关联表；表 `OperatorMachine`
- **OpType（工种）**：工序类型/工种；用于区分 internal/external；表 `OpTypes`
- **Supplier（供应商）**：外协供应商；可绑定工种并提供默认周期；表 `Suppliers`
- **Part（零件/图号）**：图号为主键；表 `Parts`
- **PartOperations（零件工序模板）**：从工艺路线解析生成的模板工序；表 `PartOperations`
- **ExternalGroup（连续外部工序组）**：连续 external 工序聚合；表 `ExternalGroups`
- **Batch（批次）**：排产对象（后续阶段使用）；表 `Batches`
- **BatchOperations（批次工序）**：从零件模板复制到批次；表 `BatchOperations`
- **Schedule（排程结果）**：排产落库结果；表 `Schedule`
- **WorkCalendar（工作日历）**：日历配置（后续阶段）；表 `WorkCalendar`
- **OperatorCalendar（人员专属工作日历）**：可覆盖全局 WorkCalendar；表 `OperatorCalendar`
- **ScheduleConfig（排产配置）**：排序策略/权重等（后续阶段）；表 `ScheduleConfig`
- **OperationLogs（操作日志）**：关键操作留痕（导入/导出/排产…）；表 `OperationLogs`
- **ScheduleHistory（排产历史）**：排产结果摘要与版本（后续阶段）；表 `ScheduleHistory`

---

## 枚举值（以 DB 字段/`core/models/enums.py` 为准）

### 通用 Yes/No
- `yes` / `no`

### 人员状态（Operators.status）
- `active`：在岗
- `inactive`：停用/休假

### 设备状态（Machines.status）
- `active`：可用
- `maintain`：维修
- `inactive`：停用

### 工序归属（*.source）
- `internal`：内部工序
- `external`：外部工序

### 批次状态（Batches.status，后续阶段）
- `pending` / `scheduled` / `processing` / `completed` / `cancelled`

### 优先级（Batches.priority，后续阶段）
- `normal` / `urgent` / `critical`

### 齐套状态（Batches.ready_status，后续阶段）
- `yes` / `no` / `partial`

### 日历类型（WorkCalendar.day_type，后续阶段）
- `workday` / `holiday`（假期=节假日/周末；兼容值：`weekend`，统一视为 `holiday`）

### 外部组周期模式（ExternalGroups.merge_mode）
- `separate`：分别设置（每道外部工序 `PartOperations.ext_days` 各自存）
- `merged`：合并设置（组表 `ExternalGroups.total_days` 存；组内 `PartOperations.ext_days` 为 NULL）

---

## HTTP 接口清单（当前实现：Flask + 模板渲染；无 /api 前缀）

> 基础前缀：`app.py` 注册蓝图：`/`、`/excel-demo`、`/personnel`、`/equipment`、`/process`、`/scheduler`、`/reports`、`/material`、`/system`

### 首页
- `GET /`：首页（`templates/dashboard.html`）

### Excel 演示（/excel-demo）
- `GET /excel-demo/`：演示页
- `POST /excel-demo/preview`：上传→预览
- `POST /excel-demo/confirm`：确认导入（落库）
- `GET /excel-demo/template`：下载模板（优先读取 `templates_excel/人员基本信息.xlsx`，缺失兜底动态生成）

### 人员管理（/personnel）
- `GET /personnel/`：人员列表
- `POST /personnel/create`：创建人员
- `GET /personnel/<operator_id>`：人员详情
- `POST /personnel/<operator_id>/update`：更新人员
- `POST /personnel/<operator_id>/status`：设置状态
- `POST /personnel/<operator_id>/delete`：删除人员（引用保护：被批次工序/排程引用则禁止删除，建议改为停用）
- `POST /personnel/bulk/status`：批量设置状态（active/inactive）
- `POST /personnel/bulk/delete`：批量删除（引用保护）
- `POST /personnel/<operator_id>/link/add`：新增人员-设备关联
- `POST /personnel/<operator_id>/link/remove`：移除人员-设备关联
- `POST /personnel/<operator_id>/link/update`：更新人员-设备关联（用于修改关联属性）
- `GET /personnel/<operator_id>/calendar`：个人工作日历页面（人员专属日历，可覆盖全局 WorkCalendar）
- `POST /personnel/<operator_id>/calendar/upsert`：保存个人工作日历配置

#### 人员 Excel（/personnel/excel/*）
- `GET /personnel/excel/operators`：人员 Excel 页面
- `POST /personnel/excel/operators/preview`：预览
- `POST /personnel/excel/operators/confirm`：确认导入（事务；REPLACE 会清空 `Operators`，带保护）
- `GET /personnel/excel/operators/template`：下载模板（优先读 `templates_excel/人员基本信息.xlsx`）
- `GET /personnel/excel/operators/export`：导出

- `GET /personnel/excel/links`：人员设备关联 Excel 页面
- `POST /personnel/excel/links/preview`：预览（复合键：工号+设备编号）
- `POST /personnel/excel/links/confirm`：确认导入（可 REPLACE 清空 `OperatorMachine`）
- `GET /personnel/excel/links/template`：下载模板（优先读 `templates_excel/人员设备关联.xlsx`）
- `GET /personnel/excel/links/export`：导出

- `GET /personnel/excel/operator_calendar`：人员专属工作日历 Excel 页面
- `POST /personnel/excel/operator_calendar/preview`：预览
- `POST /personnel/excel/operator_calendar/confirm`：确认导入
- `GET /personnel/excel/operator_calendar/template`：下载模板（优先读 `templates_excel/人员专属工作日历.xlsx`）
- `GET /personnel/excel/operator_calendar/export`：导出

### 设备管理（/equipment）
- `GET /equipment/`：设备列表
- `POST /equipment/create`：创建设备
- `GET /equipment/<machine_id>`：设备详情
- `POST /equipment/<machine_id>/update`：更新设备
- `POST /equipment/<machine_id>/status`：设置状态
- `POST /equipment/<machine_id>/delete`：删除设备（引用保护：被批次工序/排程引用则禁止删除，建议改为停用/维修）
- `POST /equipment/bulk/status`：批量设置状态（active/maintain/inactive）
- `POST /equipment/bulk/delete`：批量删除（引用保护）
- `POST /equipment/<machine_id>/link/add`：新增设备-人员关联（写 `OperatorMachine`）
- `POST /equipment/<machine_id>/link/remove`：移除设备-人员关联
- `POST /equipment/<machine_id>/link/update`：更新设备-人员关联（用于修改关联属性）
- `POST /equipment/<machine_id>/downtimes/create`：新增停机计划（时间段）
- `POST /equipment/<machine_id>/downtimes/<int:downtime_id>/cancel`：取消停机计划
- `GET /equipment/downtimes/batch`：批量停机计划页面（按设备/类别/全部）
- `POST /equipment/downtimes/batch/create`：按范围批量创建停机计划（会展开为“按设备逐条写入”）

#### 设备 Excel（/equipment/excel/*）
- `GET /equipment/excel/machines`：设备 Excel 页面
- `POST /equipment/excel/machines/preview`：预览（状态中英兼容；工种支持 ID/名称）
- `POST /equipment/excel/machines/confirm`：确认导入（事务；REPLACE 带引用保护）
- `GET /equipment/excel/machines/template`：下载模板（优先读 `templates_excel/设备信息.xlsx`）
- `GET /equipment/excel/machines/export`：导出

- `GET /equipment/excel/links`：设备人员关联 Excel 页面
- `POST /equipment/excel/links/preview`：预览（字段兼容：支持别名表头）
- `POST /equipment/excel/links/confirm`：确认导入
- `GET /equipment/excel/links/template`：下载模板（优先读 `templates_excel/设备人员关联.xlsx`）
- `GET /equipment/excel/links/export`：导出

### 工艺管理（/process）
- `GET /process/`：零件列表
- `POST /process/parts/create`：新增零件
- `GET /process/parts/<part_no>`：零件详情（工序模板/外部组/高亮/可删控制）
- `POST /process/parts/<part_no>/update`：更新零件基本信息
- `POST /process/parts/<part_no>/delete`：删除零件（引用保护：已被批次引用则禁止删除）
- `POST /process/parts/bulk/delete`：批量删除零件（引用保护）
- `POST /process/parts/<part_no>/reparse`：重新解析并覆盖模板（事务边界）
- `POST /process/parts/<part_no>/ops/<int:seq>/hours`：更新内部工序工时（setup/unit）
- `POST /process/parts/<part_no>/groups/<group_id>/mode`：设置外部组周期模式（separate/merged）
- `POST /process/parts/<part_no>/groups/<group_id>/delete`：删除外部工序组（严格规则校验）

#### 工种配置（/process/op-types）
- `GET /process/op-types`：工种列表
- `POST /process/op-types/create`：新增工种
- `GET /process/op-types/<op_type_id>`：工种详情
- `POST /process/op-types/<op_type_id>/update`：更新工种
- `POST /process/op-types/<op_type_id>/delete`：删除工种（带引用保护）

#### 供应商配置（/process/suppliers）
- `GET /process/suppliers`：供应商列表
- `POST /process/suppliers/create`：新增供应商
- `GET /process/suppliers/<supplier_id>`：供应商详情
- `POST /process/suppliers/<supplier_id>/update`：更新供应商
- `POST /process/suppliers/<supplier_id>/delete`：删除供应商（带引用保护）

#### 工艺 Excel（/process/excel/*）
- `GET /process/excel/op-types`
- `POST /process/excel/op-types/preview`
- `POST /process/excel/op-types/confirm`
- `GET /process/excel/op-types/template`
- `GET /process/excel/op-types/export`
- `GET /process/excel/suppliers`
- `POST /process/excel/suppliers/preview`
- `POST /process/excel/suppliers/confirm`
- `GET /process/excel/suppliers/template`
- `GET /process/excel/suppliers/export`
- `GET /process/excel/routes`
- `POST /process/excel/routes/preview`
- `POST /process/excel/routes/confirm`
- `GET /process/excel/routes/template`
- `GET /process/excel/routes/export`
- `GET /process/excel/part-operations`：导出页
- `GET /process/excel/part-operations/export`：导出零件工序模板

### 排产调度（/scheduler）
- `GET /scheduler/`：执行排产主页（待排批次列表 + 执行/模拟排产 + 常用方案）
- `GET /scheduler/batches`：批次管理（二级页：新增批次 + 批量复制/删除/修改；进入批次详情）
- `GET /scheduler/config`：排产高级设置（二级页：参数说明 + 模板/方案管理）
- `GET /scheduler/config/manual`：排产调度说明书（读取 Markdown 原文；前端轻量渲染目录与标题；不引入第三方渲染依赖；禁用 JS 时回退显示原文 `<pre>`）
- `GET /scheduler/config/manual/download`：下载说明书 Markdown（文件名：`排产调度说明书.md`）
- `GET /scheduler/analysis`：排产分析页（统计/诊断类视图）
- `POST /scheduler/batches/create`：创建批次（默认/强制从零件模板生成工序；缺模板时会尝试自动解析 `Parts.route_raw`）
- `GET /scheduler/batches/<batch_id>`：批次详情（批次工序补充编辑；支持 query `lazy_select=1/0` 强制开关，默认 auto 按阈值启用，用于 Win7 性能）
- `POST /scheduler/batches/<batch_id>/delete`：删除批次（级联删除批次工序与排程记录）
- `POST /scheduler/batches/bulk/delete`：批量删除批次
- `POST /scheduler/batches/bulk/copy`：批量复制批次（批次号末尾数字自动 +1；复制批次工序）
- `POST /scheduler/batches/bulk/update`：批量修改批次（优先级/交期/备注）
- `POST /scheduler/batches/<batch_id>/generate-ops`：从零件模板重建批次工序（会覆盖已补充信息）
- `POST /scheduler/ops/<int:op_id>/update`：保存单条批次工序补充信息（内部/外部分支）
- **内部工序人机约束**（重要）：当内部工序同时选择 `machine_id` 与 `operator_id` 时，必须满足 `OperatorMachine` 已维护的可操作关系；页面“设备/人员”下拉**双向联动**并在保存时做**后端强校验**，不匹配会报中文错误并带工序信息（便于定位）。
- `POST /scheduler/run`：执行排产（落库 Schedule + 留痕 ScheduleHistory/OperationLogs；支持 `start_dt` 默认明天08:00 与可选 `end_date` 截止窗口；支持 `enforce_ready` 启用齐套约束：未齐套禁止排产）
- `POST /scheduler/config`：保存排产策略与权重（Phase7 算法会复用）
- `POST /scheduler/config/default`：恢复默认策略与权重
- `POST /scheduler/config/preset/apply`：应用模板/方案（支持 `next` 回跳；也可从主页面“常用方案”触发）
- `POST /scheduler/config/preset/save`：将当前配置保存为新模板（不覆盖内置模板）
- `POST /scheduler/config/preset/delete`：删除模板（内置模板不可删）
- `GET /scheduler/calendar`：工作日历配置页
- `POST /scheduler/calendar/upsert`：新增/更新某一天日历配置

#### 甘特图与周计划（/scheduler/gantt / /scheduler/week-plan）
- `GET /scheduler/gantt`：甘特图页面（设备/人员视图切换；支持 `start_date/end_date` 区间与 `week_start` 指定周，区间优先）
- `GET /scheduler/gantt/data`：甘特图数据接口（返回 Frappe Gantt tasks）
- `GET /scheduler/week-plan`：周计划预览页（支持 `start_date/end_date` 区间与 `week_start` 指定周，区间优先）
- `GET /scheduler/week-plan/export`：导出周计划表.xlsx（写 export 留痕）
- `POST /scheduler/simulate`：插单模拟（simulate=True：落库到新版本但不更新批次/工序状态；支持 `enforce_ready` 启用齐套约束：未齐套禁止排产）

#### 排产调度 Excel（/scheduler/excel/*）
- `GET /scheduler/excel/batches`：批次信息 Excel 页面
- `POST /scheduler/excel/batches/preview`：预览
- `POST /scheduler/excel/batches/confirm`：确认导入（单事务；可选“导入后自动重建批次工序”；**若预览存在 ERROR 行则整批拒绝**；交期/齐套日期仅允许日期：`YYYY-MM-DD`/`YYYY/MM/DD` 或 Excel 日期单元格）
- `GET /scheduler/excel/batches/template`：下载模板（优先读 `templates_excel/批次信息.xlsx`）
- `GET /scheduler/excel/batches/export`：导出批次信息

- `GET /scheduler/excel/calendar`：工作日历 Excel 页面
- `POST /scheduler/excel/calendar/preview`：预览
- `POST /scheduler/excel/calendar/confirm`：确认导入（单事务，upsert；**若预览存在 ERROR 行则整批拒绝**；日期仅允许 `YYYY-MM-DD`/`YYYY/MM/DD` 或 Excel 日期单元格）
- `GET /scheduler/excel/calendar/template`：下载模板（优先读 `templates_excel/工作日历.xlsx`）
- `GET /scheduler/excel/calendar/export`：导出工作日历

### 报表中心（/reports）
- `GET /reports/`：报表入口页
- `GET /reports/overdue`：超期报表（按版本/时间范围筛选）
- `GET /reports/overdue/export`：导出超期报表（xlsx）
- `GET /reports/utilization`：设备/人员利用率报表（按版本/时间范围筛选）
- `GET /reports/utilization/export`：导出利用率报表（xlsx）
- `GET /reports/downtime`：设备停机统计报表（按版本/时间范围筛选）
- `GET /reports/downtime/export`：导出停机统计报表（xlsx）

### 物料管理（/material）
- `GET /material/`：物料管理入口（通常跳转到物料列表）
- `GET /material/materials`：物料列表页
- `POST /material/materials/create`：新增物料
- `POST /material/materials/<material_id>/update`：更新物料
- `POST /material/materials/<material_id>/delete`：删除物料
- `GET /material/batches`：批次物料需求总览页
- `POST /material/batches/<batch_id>/requirements/add`：新增批次物料需求（写入 `BatchMaterials` 并回写齐套状态）
- `POST /material/requirements/<int:bm_id>/update`：更新批次物料需求（回写齐套状态/齐套日期）
- `POST /material/requirements/<int:bm_id>/delete`：删除批次物料需求（回写齐套状态）

### 系统管理（/system）
- `GET /system/`：系统管理入口（默认跳转到备份页）
- `GET /system/backup`：备份/恢复页（手动备份、备份列表、恢复、清理过期）
- `POST /system/backup/create`：手动备份（生成 `aps_backup_*.db`）
- `POST /system/backup/settings`：保存备份页自动任务设置（自动备份/自动清理备份；按请求触发，分钟级）
- `POST /system/backup/delete`：删除单个备份文件（参数：`filename`）
- `POST /system/backup/delete-batch`：批量删除备份文件（参数：`filenames[]`）
- `POST /system/backup/restore`：恢复（参数：`filename`；恢复前会自动生成 `*_before_restore.db`）
- `POST /system/backup/cleanup`：清理过期备份（按“备份保留天数”设置；未设置时默认 `BACKUP_KEEP_DAYS`）
- `GET /system/logs`：操作日志页（按 `start_time/end_time/module/action/log_level/limit` 筛选）
- `POST /system/logs/settings`：保存日志自动清理设置（保留天数/分钟间隔；按请求触发）
- `POST /system/logs/delete`：删除单条操作日志（参数：`log_id`）
- `POST /system/logs/delete-batch`：批量删除操作日志（参数：`log_ids[]`）
- `GET /system/history`：排产历史页（按 `version/limit` 选择回看）
- `POST /system/plugins/toggle`：启用/停用运行时插件（插件管理）
- `POST /system/ui-mode`：切换 UI 模式（经典/现代；写 Cookie + SystemConfig.ui_mode；用于模板切换与渐进发布）

---

## Excel 模板清单（交付物：`templates_excel/`）

> 模板文件由 `core/services/common/excel_templates.py` 维护；应用启动会自动补齐缺失文件。

- `人员基本信息.xlsx`：`工号/姓名/状态/备注`
- `人员设备关联.xlsx`：`工号/设备编号/技能等级/主操设备`（后两列可选；留空表示不修改/按默认）
- `人员专属工作日历.xlsx`：`工号/日期/类型/班次开始/班次结束/可用工时/效率/允许普通件/允许急件/说明`
- `设备信息.xlsx`：`设备编号/设备名称/工种/状态`
- `设备人员关联.xlsx`：`设备编号/工号/技能等级/主操设备`（后两列可选；留空表示不修改/按默认）
- `工种配置.xlsx`：`工种ID/工种名称/归属`
- `供应商配置.xlsx`：`供应商ID/名称/对应工种/默认周期`
- `零件工艺路线.xlsx`：`图号/名称/工艺路线字符串`
- `批次信息.xlsx`（Phase 6 已接入）：`批次号/图号/数量/交期/优先级/齐套/齐套日期/备注`（齐套日期可选：影响最早可开工日）
- `工作日历.xlsx`（Phase 6 已接入）：`日期/类型/可用工时/效率/允许普通件/允许急件/说明`

---

## 留痕（OperationLogs / ScheduleHistory）

### OperationLogs（DB 表）字段含义
- `module`：模块名（示例：personnel/equipment/process/excel_demo）
- `action`：动作（当前已落地：`import` / `export` / `schedule` / `simulate` / `backup` / `restore` / `cleanup` / `backup_delete` / `logs_delete` / `logs_cleanup`；预留：`report`）
- `target_type`：目标类型（示例：operator/machine/op_type/supplier/part_route/operator_machine/part_operation）
- `detail`：JSON 字符串（键名固定，便于审计/报表）

### Excel 导入 detail 固定键（`action=import`）
由 `core/services/common/excel_audit.py::log_excel_import()` 生成：
- `filename`
- `mode`：append/overwrite/replace
- `time_cost_ms`
- `file_hash`（可选，当前未启用）
- `total_rows/new_count/update_count/skip_count/error_count`
- `errors_sample`：最多 10 条，元素格式 `{"row": <Excel行号>, "message": "..."}`（仅示意）

### Excel 导出 detail 固定键（`action=export`）
由 `core/services/common/excel_audit.py::log_excel_export()` 生成：
- `template_or_export_type`
- `filters`（dict）
- `row_count`
- `time_range`（dict）
- `time_cost_ms`

### ScheduleHistory（排产历史，已落地使用）
表存在于 `schema.sql`，字段 `result_summary` 为 JSON 字符串；排产执行会写入：strategy/version/批次数/工序数/结果摘要（含超期、错误样本、耗时等）。

---

## 排产策略配置（ScheduleConfig，Phase 6 已接入）

> 说明：配置存于表 `ScheduleConfig`（key/value），供 Phase 7 算法读取。

### 配置键（config_key）
- `sort_strategy`：当前排序策略（允许：`priority_first` / `due_date_first` / `weighted` / `fifo`）
- `priority_weight`：权重模式-优先级权重（默认 `0.4`）
- `due_weight`：权重模式-交期权重（默认 `0.5`）
- `ready_weight`：权重模式-齐套权重（默认 `0.1`，**V1.1 起预留字段：前端不暴露、算法不使用**；齐套约束请使用 `enforce_ready(_default)`）
- `holiday_default_efficiency`：工作日历：假期默认效率（默认 `0.8`；>0；用于页面/Excel 默认填充）
- `enforce_ready_default`：执行排产：默认启用齐套约束（默认 `no`；yes=未齐套禁止排产；运行时可由表单 `enforce_ready` 覆盖）
- `prefer_primary_skill`：工序补充页偏好（默认 `no`；yes=优先推荐主操/高技能人员）
- `dispatch_mode`：派工方式（默认 `batch_order`；允许：`batch_order` / `sgs`）
- `dispatch_rule`：SGS 派工规则（默认 `slack`；允许：`slack` / `cr` / `atc`；仅 `dispatch_mode=sgs` 生效）
- `auto_assign_enabled`：自动资源分配（默认 `no`；yes=内部工序资源缺省时自动选人/选机）
- `algo_mode`：算法模式（默认 `greedy`；允许：`greedy` / `improve`）
- `time_budget_seconds`：时间预算（默认 `20` 秒；仅 `algo_mode=improve` 生效）
- `objective`：目标函数（默认 `min_overdue`；允许：`min_overdue` / `min_tardiness` / `min_changeover`）
- `ortools_enabled`：可选 OR-Tools 高质量起点（默认 `no`；未安装 ortools 时会自动降级不影响主流程）
- `ortools_time_limit_seconds`：OR-Tools 单次求解上限（默认 `5` 秒；仅 `ortools_enabled=yes` 生效）
- `freeze_window_enabled`：冻结窗口开关（默认 `no`）
- `freeze_window_days`：冻结窗口天数（默认 `0`；仅 `freeze_window_enabled=yes` 生效）

---

## 数据库表字段字典（SQLite，来自 `schema.sql`）

> 说明：字段类型以 SQLite 声明为准；**状态/布尔一律字符串枚举**；强依赖外键与唯一性约束（请勿随意改表结构）。

### SchemaVersion（Schema 版本）
- **PK**：`id`（固定为 1）
- 字段：`id/version/updated_at`
- 用途：用于轻量迁移/版本标定；启动时由 `core/infrastructure/database.py` 读写

### Operators（人员）
- **PK**：`operator_id`
- 字段：`operator_id/name/status/remark/created_at/updated_at`
- 索引：`idx_operators_status`

### Machines（设备）
- **PK**：`machine_id`
- 字段：`machine_id/name/op_type_id/category/status/remark/created_at/updated_at`
- 外键：`op_type_id -> OpTypes(op_type_id)`
- 索引：`idx_machines_status/idx_machines_op_type`

### MachineDowntimes（设备停机时间段）
- **PK**：`id`（自增）
- 字段：`id/machine_id/scope_type/scope_value/start_time/end_time/reason_code/reason_detail/status/created_at/updated_at`
- 外键：`machine_id -> Machines(machine_id)`（级联删除）
- 索引：`idx_machine_downtimes_machine/idx_machine_downtimes_time`

### OperatorMachine（人员-设备关联，共享表）
- **PK**：`id`（自增）
- **UNIQUE**：`(operator_id, machine_id)`
- 字段：`id/operator_id/machine_id/skill_level/is_primary/created_at`
- 外键：`operator_id -> Operators`（级联删除），`machine_id -> Machines`（级联删除）
- 索引：`idx_operator_machine_operator/idx_operator_machine_machine`

### OperatorSkill（预留：人员-工种能力）
- **PK**：`id`（自增）
- **UNIQUE**：`(operator_id, op_type_id)`
- 字段：`id/operator_id/op_type_id/skill_level/is_primary/created_at`

### OpTypes（工种）
- **PK**：`op_type_id`
- **UNIQUE**：`name`
- 字段：`op_type_id/name/category/default_hours/remark/created_at`

### Suppliers（供应商）
- **PK**：`supplier_id`
- 字段：`supplier_id/name/op_type_id/default_days/status/remark/created_at`
- 外键：`op_type_id -> OpTypes`

### Parts（零件/图号）
- **PK**：`part_no`
- 字段：`part_no/part_name/route_raw/route_parsed/remark/created_at/updated_at`

### PartOperations（零件工序模板）
- **PK**：`id`（自增）
- **UNIQUE**：`(part_no, seq)`
- 字段：
  - 基本：`id/part_no/seq/op_type_id/op_type_name/source/status/created_at`
  - 外部：`supplier_id/ext_days/ext_group_id`
  - 内部：`setup_hours/unit_hours`
- 外键：`part_no -> Parts`（级联删除），`op_type_id -> OpTypes`，`supplier_id -> Suppliers`
- 索引：`idx_part_operations_part/idx_part_operations_source`

### ExternalGroups（连续外部工序组）
- **PK**：`group_id`
- 字段：`group_id/part_no/start_seq/end_seq/merge_mode/total_days/supplier_id/remark/created_at`
- 外键：`part_no -> Parts`（级联删除），`supplier_id -> Suppliers`
- 索引：`idx_external_groups_part`

### Batches（后续排产阶段）
- **PK**：`batch_id`
- 字段：`batch_id/part_no/part_name/quantity/due_date/priority/ready_status/ready_date/status/remark/created_at/updated_at`
- 外键：`part_no -> Parts`
- 索引：`idx_batches_status/idx_batches_priority/idx_batches_due_date`

### BatchOperations（后续排产阶段）
- **PK**：`id`（自增）
- **UNIQUE**：`op_code`，以及 `(batch_id, seq, piece_id)`
- 字段：`id/op_code/batch_id/piece_id/seq/op_type_id/op_type_name/source/machine_id/operator_id/supplier_id/setup_hours/unit_hours/ext_days/status/created_at`
- 外键：`batch_id -> Batches`（级联删除），`machine_id -> Machines`，`operator_id -> Operators`，`supplier_id -> Suppliers`
- 索引：`idx_batch_operations_batch/idx_batch_operations_status`

### Schedule（后续排产阶段）
- **PK**：`id`（自增）
- 字段：`id/op_id/machine_id/operator_id/start_time/end_time/lock_status/version/created_at`
- 外键：`op_id -> BatchOperations(id)`（级联删除）
- 索引：`idx_schedule_op/idx_schedule_machine/idx_schedule_operator/idx_schedule_time`

### WorkCalendar / OperatorCalendar / ScheduleConfig（后续排产阶段）
- WorkCalendar：`date` 主键；字段 `day_type/shift_start/shift_end/shift_hours/efficiency/allow_normal/allow_urgent/remark`（day_type 推荐值域：workday/holiday；weekend 为兼容值，统一视为 holiday）
- OperatorCalendar：复合主键 `(operator_id, date)`；字段与 WorkCalendar 对齐；索引 `idx_operator_calendar_operator_date/idx_operator_calendar_date`
- ScheduleConfig：自增 `id`；`config_key` 唯一；字段 `config_key/config_value/description/updated_at`

### SystemConfig / SystemJobState（系统管理）
- SystemConfig：自增 `id`；`config_key` 唯一；字段 `config_key/config_value/description/updated_at`
- SystemJobState：自增 `id`；`job_key` 唯一；字段 `job_key/last_run_time/last_run_detail/updated_at`；索引 `idx_system_job_state_time`

### OperationLogs / ScheduleHistory（留痕）
- OperationLogs：`id` 自增；字段 `log_time/log_level/module/action/target_type/target_id/operator/detail/error_code/error_message`
- ScheduleHistory：`id` 自增；字段 `schedule_time/version/strategy/batch_count/op_count/result_status/result_summary/created_by`

### Materials / BatchMaterials（预留）
- Materials：`material_id` 主键；字段 `name/spec/unit/stock_qty/status/remark/created_at`
- BatchMaterials：`id` 自增；字段 `batch_id/material_id/required_qty/available_qty/ready_status`

---

## 错误码与响应结构（`core/infrastructure/errors.py` + `web/error_handlers.py`）

### JSON 响应（当请求倾向 JSON 或路径以 `/api/` 开头时）
- 成功：`{"success": true, "data": "...", "meta": {"..." : "..."}}`（示意）
- 失败：`{"success": false, "error": {"code": "xxxx", "message": "中文提示", "details": {"...": "..."}}}`（示意）

### HTML 错误页
当请求为页面访问时，渲染 `templates/error.html`。

---

## 打包/交付关键点（与 Phase0~5 直接相关）

- 交付目录（必须包含）：`db/ logs/ backups/ templates_excel/ static/ templates/`
- 启动端口与排障（Win7 现场高频）：
  - **端口文件**：`logs/aps_port.txt`（应用启动后写入，仅数字 + 换行；当端口 5000 被占用/受限时，以该文件为准）
  - **Host 文件**：`logs/aps_host.txt`（应用启动后写入，一行 host + 换行；当 `APS_HOST` 不可绑定或监听 `0.0.0.0` 时，以该文件为准）
  - **环境变量（可选）**：
    - `APS_HOST`：默认 `127.0.0.1`
    - `APS_PORT`：首选端口（若不可用会自动回退到可用端口，并写入 `logs/aps_port.txt`）
    - `APS_ENV`：环境（开发/生产；打包与源码运行默认行为见 `app.py`）
  - **启动脚本/启动器**：
    - `start.bat` / `start_new_ui.bat`（开发/调试用；端口被占用时不再强制退出，提示“以端口文件为准”）
    - `assets/启动_排产系统_Chrome.bat`（交付目录启动器：先启动 exe，再用离线 Chrome109 打开 `http://{HOST}:{PORT}/`；优先读取 `logs/aps_host.txt` + `logs/aps_port.txt` 避免打开错 host/端口）
- 本地化资源：
  - `static/js/frappe-gantt.min.js`（Frappe Gantt 0.6.1）
  - `static/css/frappe-gantt.css`（Frappe Gantt 0.6.1）
- 日历日期选择器（WorkCalendar/OperatorCalendar，共用组件）：
  - `static/css/calendar_picker.css`
  - `static/js/calendar_picker.js`
- 排产调度说明书（离线原文）：`static/docs/scheduler_manual.md`（以及 v2 候选路径：`web_new_test/static/docs/scheduler_manual.md`）
- 说明书页面模板：`templates/scheduler/config_manual.html`（v1），以及 `web_new_test/templates/scheduler/config_manual.html`（v2）
- Excel 模板目录：`templates_excel/`（本仓库已补齐模板文件）
- 安装包（Inno Setup）：`installer/aps_win7.iss` 使用 `x64compatible`（适配更多 x64 环境）
- 自测证据：`evidence/FullSelfTest/full_selftest_report.md`（runner 日志目录通常不提交：`evidence/FullSelfTest/logs/`）

