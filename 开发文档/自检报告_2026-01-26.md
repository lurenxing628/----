# 自检报告（实现 vs 开发文档）- 2026-01-26

## 1. 范围与方法

### 1.1 对照范围
- **对照文档（@开发文档）**：
  - `开发文档/开发文档.md`
  - `开发文档/系统速查表.md`
  - `开发文档/实现计划表.md`
  - `开发文档/阶段留痕与验收记录.md`
- **对照实现**：
  - 路由：`web/routes/*.py` + `app.py`
  - 数据模型：`schema.sql` + `core/infrastructure/database.py`
  - 排产算法与服务：`core/algorithms/*`、`core/services/scheduler/*`
  - Excel 导入导出：`core/services/common/*` + 各模块 `/excel/*` 路由
  - 系统管理：`core/infrastructure/backup.py`、`core/services/system/*`、`web/routes/system.py`

### 1.2 检查方法（核心）
- **接口一致性**：枚举代码路由（method+path）与 `系统速查表.md` 接口清单逐条比对
- **Schema 一致性**：逐表比对 `schema.sql` 与文档“数据字典/数据模型”描述
- **算法一致性**：按文档约束点逐项核对（策略、双资源、日历、外协、留痕、窗口、齐套、停机、版本等），同时识别“实现新增扩展能力”
- **Excel 一致性**：对照 openpyxl-only/预览行号/ERROR 整批拒绝/ImportMode/留痕 detail 键名
- **系统管理一致性**：对照退出自动备份/手动备份恢复/按请求触发自动维护/日志筛选与删除留痕

## 2. 结论摘要
- **接口层**：一致（文档列出但实现缺失 0；实现存在但文档未列出 0）。证据见：`evidence/Conformance/quickref_vs_routes.md`
- **数据库模型**：文档缺少/分散的表说明 3 处：`SchemaVersion`、`SystemConfig`、`SystemJobState`（已回填）
- **排产算法/配置**：实现已新增多项“可选增强能力”（派工模式/自动分配/改进搜索/冻结窗口/OR-Tools warm-start/评估指标），原 `开发文档.md` 未覆盖（已回填）
- **留痕**：`ScheduleHistory.result_summary` 实际字段已扩展（含 `algo.metrics` 等），原文档仅列最少字段（已回填）
- **Excel**：规则一致；仅有导入预览 SKIP 文案示例差异（已回填）

## 3. 差异清单与回填结果（按模块）

### 3.1 数据模型（SQLite）

#### 差异 1：SchemaVersion 未在文档数据模型中描述
- **代码/Schema 位置**：
  - `schema.sql`：`CREATE TABLE IF NOT EXISTS SchemaVersion ...`
  - `core/infrastructure/database.py`：`CURRENT_SCHEMA_VERSION` / `_ensure_schema_version()` / `_migrate_with_backup()`
- **回填位置**：
  - `开发文档/开发文档.md`：新增 `### 6.2.1 SchemaVersion（用于轻量迁移/回滚）`
  - `开发文档/系统速查表.md`：数据库字段字典新增 `### SchemaVersion（Schema 版本）`

#### 差异 2：SystemConfig / SystemJobState 在备份章节提及，但数据模型与速查表缺失
- **Schema 位置**：`schema.sql`（System Settings / Jobs 区块）
- **回填位置**：
  - `开发文档/开发文档.md`：新增 `### 6.9 系统管理配置与作业状态（V1.1）`
  - `开发文档/系统速查表.md`：数据库字段字典新增 `### SystemConfig / SystemJobState（系统管理）`

### 3.2 排产算法与配置（Scheduler）

#### 差异 3：ScheduleConfig 配置键扩展未在文档体现
- **代码位置**：`core/services/scheduler/config_service.py`（`ScheduleConfigSnapshot` 与默认值）
- **新增键（文档已回填）**：
  - `prefer_primary_skill`
  - `dispatch_mode` / `dispatch_rule`
  - `auto_assign_enabled`
  - `algo_mode` / `time_budget_seconds` / `objective`
  - `ortools_enabled` / `ortools_time_limit_seconds`
  - `freeze_window_enabled` / `freeze_window_days`
- **回填位置**：
  - `开发文档/开发文档.md`：新增 `### 10.5（V1.1+/V1.2 已落地）排产增强能力与开关`
  - `开发文档/系统速查表.md`：扩展“排产策略配置（ScheduleConfig）”的配置键清单

#### 差异 4：SGS 派工模式与派工规则（slack/cr/atc）未在开发文档说明
- **代码位置**：
  - `core/algorithms/dispatch_rules.py`
  - `core/algorithms/greedy.py`（dispatch_mode/dispatch_rule 相关路径）
- **回填位置**：`开发文档/开发文档.md`：`10.5.2 派工模式（dispatch_mode=sgs）`

#### 差异 5：自动资源分配（auto_assign）未在开发文档说明
- **代码位置**：
  - `core/algorithms/greedy.py`（内部工序自动选人/选机）
  - `core/services/scheduler/schedule_service.py`（排产后对“原本缺省资源”的工序回写 machine/operator）
- **回填位置**：`开发文档/开发文档.md`：`10.5.3 自动资源分配（auto_assign_enabled=yes）`

#### 差异 6：改进模式（improve）与目标函数/时间预算未在开发文档说明
- **代码位置**：
  - `core/services/scheduler/schedule_service.py`（multi-start + 局部搜索 + ILS 重启）
  - `core/algorithms/evaluation.py`（metrics + objective_score）
- **回填位置**：
  - `开发文档/开发文档.md`：`10.5.4 算法改进模式（algo_mode=improve）`
  - `开发文档/开发文档.md`：`12.2.3 排产留痕` 中补充 `result_summary.algo.metrics/best_score` 等字段

#### 差异 7：OR-Tools 瓶颈工种 warm-start 未在开发文档说明
- **代码位置**：`core/algorithms/ortools_bottleneck.py`
- **回填位置**：`开发文档/开发文档.md`：`10.5.5 OR-Tools 瓶颈工种 warm-start`

#### 差异 8：冻结窗口 freeze_window 未在开发文档说明
- **代码位置**：
  - `core/services/scheduler/schedule_service.py`（冻结窗口与种子排程 seed_results）
  - `core/algorithms/greedy.py`（seed_results 去重与窗口约束相关）
- **回填位置**：`开发文档/开发文档.md`：`10.5.6 冻结窗口（freeze_window_enabled=yes）`

### 3.3 留痕（OperationLogs / ScheduleHistory）

#### 差异 9：result_summary 字段集合已扩展，但文档只列最低要求
- **代码位置**：`core/services/scheduler/schedule_service.py`（`result_summary_obj` 构建；包含 `algo.metrics/best_score/...` 等）
- **回填位置**：`开发文档/开发文档.md`：更新 `12.2.3 排产留痕` 的字段说明

### 3.4 Excel 导入导出

#### 差异 10：导入预览 APPEND 的 SKIP 文案示例不一致
- **实现现状**：SKIP message 为“已存在，按‘追加’模式将跳过”（更明确）
- **回填位置**：`开发文档/开发文档.md`：第 11 章 ExcelService 代码段中更新 message 示例文本

### 3.5 Web 接口清单
- **差异**：无
- **证据**：`evidence/Conformance/quickref_vs_routes.md`

## 4. 本次回填改动点（便于复核）

### 4.1 `开发文档/开发文档.md`
- 新增：`6.2.1 SchemaVersion`
- 新增：`6.9 SystemConfig/SystemJobState`
- 新增：`10.5 排产增强能力与开关`
- 更新：`12.2.3 排产留痕 result_summary 字段`
- 更新：第 11 章 ExcelService 示例中 APPEND-SKIP message

### 4.2 `开发文档/系统速查表.md`
- 关键文件：补充 `dispatch_rules.py` / `evaluation.py` / `ortools_bottleneck.py` / `config_service.py`
- ScheduleConfig：补充新增配置键清单
- 数据字典：补充 `SchemaVersion` / `SystemConfig` / `SystemJobState`

## 5. 复核指令（你本地执行）

### 5.1 路由一致性（文档接口 vs 实现）
```bash
python tests/check_quickref_vs_routes.py
```

### 5.2（可选）冒烟验证
```bash
run_smoke_phase7.bat
run_smoke_phase8.bat
run_smoke_phase9.bat
```

> 说明：我本次仅做文档回填与自检报告，不负责编译/打包；如需 Win7 打包请按 `开发文档/开发文档.md` 第 1/2/5 章的 PyInstaller 指令在 Win7 环境执行。

