from __future__ import annotations

from typing import Any, Dict

from .page_manuals_common import _card, _section, _topic
from .page_manuals_scheduler_outputs import SCHEDULER_OUTPUT_TOPICS

SCHEDULER_TOPICS: Dict[str, Dict[str, Any]] = {
    "excel_batches": _topic(
        title="批量维护批次",
        summary="通过“批量维护”整批新增或更新排产批次，重点看图号、数量、交期、优先级和齐套信息是否填对。",
        full_manual_anchor="#1-610批次信息排产核心",
        help_card=_card(
            "批次信息——填写速查",
            "列：批次号、图号、数量、交期、优先级、齐套、齐套日期、备注。",
            "常见示例：B001 / P1001 / 50 / 2026-05-20 / 急件 / 齐套。",
            "其中批次号、图号、数量必填，交期强烈建议填写（不填会排到最后且不计入超期报表）；数量必须是正整数。",
            "图号必须是工艺管理里已有的零件图号，否则报错。",
            "交期和齐套日期格式：2026-04-15 或 2026/04/15，Excel日期单元格也行；只填日期，不要写 08:00 这类时间。",
            "优先级模板下拉推荐填：普通 / 急件 / 特急；以前文件里写过 急 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取；不填默认普通。",
            "齐套模板下拉推荐填：齐套 / 未齐套 / 部分齐套；以前文件里写过 是 / 否 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取；不填默认齐套。",
            "更新已有批次时，齐套日期留空会写成空值，也就是清掉原来的齐套日期；如果想保留旧日期，不要用空白单元格覆盖这一列。",
            "备注只是人工备注，会保存为备注信息，不参与排产计算。",
            "齐套和齐套日期默认只是显示信息；只有执行排产时启用“齐套检查”，系统才会检查批次是否齐套。只要发现未齐套或部分齐套批次，本次排产会报错并停止；系统不会自动跳过这些批次继续排其它批次，齐套日期只对齐套批次作为最早开工日。",
            "如果勾选“导入后自动生成批次工序”，系统只会处理本次真正新增或更新成功的批次；无变化、跳过、出错的行不会顺手刷新工序。",
            "选择“清空本类数据后重导”会清空全部批次和排程记录（不可恢复！），用之前务必先手动备份。",
        ),
        sections=[
            _section("批量维护前准备", "{{excel_common_flow}}\n\n{{excel_import_modes}}\n\n{{excel_date_format}}\n\n{{excel_format_warning}}"),
            _section(
                "关键列怎么填",
                "\n".join(
                    [
                        "- `批次号`：主键字段，必须唯一且保持稳定。",
                        "- `图号`：必须是工艺管理里已有的零件图号。",
                        "- `数量`：正整数，直接影响计算时长和产能占用。",
                        "- `交期`：建议填写日期；留空可以导入，但会排到最后，且不进入超期报表。只填日期，不要带 08:00 这类时间。",
                        "- `优先级`：新表建议只填 `普通/急件/特急`；旧文件里写过 `急` 的，系统会尽量兼容读取；留空默认普通。",
                        "- `齐套`：新表建议只填 `齐套/未齐套/部分齐套`；旧文件里写过 `是/否` 的，系统会尽量兼容读取；留空默认齐套。",
                        "- `齐套日期`：默认只是记录“最早能开工的日期”。这里只填日期，不要带 08:00 这类时间；只有本次排产启用“齐套检查”时，它才会真正限制开工日期。更新已有批次时，空白会清掉原齐套日期。",
                        "- `备注`：只是人工备注，会保存为备注信息，不参与排产计算。",
                    ]
                ),
            ),
            _section(
                "照着填一行",
                "\n".join(
                    [
                        "- 示例：`B001 / P1001 / 50 / 2026-05-20 / 急件 / 齐套`。",
                        "- 这句话的意思是：批次 `B001`，图号 `P1001`，数量 `50`，交期 `2026-05-20`，优先排急件，目前判断为齐套。",
                        "- 如果 `P1001` 还没在工艺管理里维护，批量维护会报错；要先补零件和工艺，再回来维护批次。",
                    ]
                ),
            ),
            _section(
                "导入后自动生成工序会处理哪些批次",
                "\n".join(
                    [
                        "- 勾选“导入后自动生成批次工序”时，系统只处理这次导入里真正新增或更新成功的批次。",
                        "- `无变化` 的行表示系统判断这行和库里已有内容一样，不会写入，也不会刷新这批下面的工序。",
                        "- `跳过` 的行也不会刷新工序，常见原因是导入模式要求只导新编号、这一行不需要改，或这一行没有通过前置检查。",
                        "- 如果你只是想把某些老批次的工序按最新模板重建，不要靠“原样再导一遍”触发；应去批次管理或批次详情里明确点“自动生成或刷新工序”。",
                        "- 这样做是为了避免一份没有变化的 Excel 把已经手工修过的批次工序意外刷新掉。",
                    ]
                ),
            ),
            _section(
                "齐套相关怎么理解",
                "\n".join(
                    [
                        "- `齐套` 是当前主观判断或系统状态，`齐套日期` 是你希望控制的最早开工日期。",
                        "- 不启用齐套检查时，齐套和齐套日期只用于查看，不会挡住排产。",
                        "- 启用齐套检查时，只要发现未齐套或部分齐套批次，系统会报错并停止本次排产；不会自动跳过这些批次继续排其它批次。有齐套日期的齐套批次，不能早于这个日期开工。",
                        "- 若批次还会挂物料需求，后续齐套状态可能被物料模块自动接管。",
                    ]
                ),
            ),
            _section("错误与联动", "{{excel_common_errors}}"),
        ],
        related_manual_ids=["scheduler_batches", "scheduler_batch_detail", "material_batch", "process_parts"],
    ),
    "excel_calendar": _topic(
        title="批量维护工作日历",
        summary="通过“批量维护”整批维护全局工作日历，适合处理节假日、周末加班和临时效率调整。",
        full_manual_anchor="#1-68工作日历",
        help_card=_card(
            "工作日历——填写速查",
            "列：日期(必填)、类型(建议填写)、可用工时、效率、允许普通件、允许急件、说明。",
            "日期格式：2026-04-15 或 2026/04/15，Excel日期单元格也行；只填日期，不要写 08:00 这类时间；同一天不能重复。",
            "类型模板下拉推荐填：工作日 / 假期；以前文件里写过 周末 / 节假日 的，系统会按假期理解。新表按下拉推荐写，留空按工作日处理。",
            "可用工时填非负数(小时)，工作日不填默认8，假期不填默认0；效率填正数，不填工作日默认1.0、假期默认取高级设置中的“假期工作效率”（也就是假期没单独填效率时用的默认值，初始默认0.8）。注意：可用工时和效率只接受有限数字，TRUE/FALSE、NaN、Inf、Infinity 都会报错。",
            "允许普通件/急件填 是/否（以前文件里写过 1/0 的，系统会尽量读懂）；新文件请填 是/否；不填默认是。模板没有班次开始/结束两列，班次时间只在手工日历页维护。",
            "说明只是人工备注，会保存为备注信息，不参与排产计算。",
            "只需配置和默认规则不一样的日期（如周末加班、节假日调休），未配置时周一到周五默认8小时、周六周日默认不排；法定假期和调休要自己维护。",
            "当前手工页面没有单条删除按钮；手工保存只是新增或覆盖某一天。要真正清掉某天特殊配置，请先备份，再进“批量维护工作日历”，下载模板，整理好要保留的日期，谨慎选择“清空本类数据后重导”。没有把握先找维护人员。",
        ),
        sections=[
            _section("批量维护前准备", "{{excel_common_flow}}\n\n{{excel_date_format}}\n\n{{excel_format_warning}}"),
            _section(
                "关键列怎么填",
                "\n".join(
                    [
                        "- `日期` 必填，同一天只能维护一条；只填日期，不要带 08:00 这类时间。`类型` 新表优先按模板下拉填 `工作日/假期`；旧文件里写过 `周末/节假日` 的，系统会尽量按 `假期` 理解；留空按 `工作日` 处理。",
                        "- `可用工时` 与 `效率` 决定当天资源供给水平：工作日留空默认 `8h/1.0`，假期留空默认 `0h/高级设置中的假期工作效率`。",
                        "- `可用工时` 和 `效率` 必须是真正的有限数字；`TRUE/FALSE`、`NaN`、`Inf`、`Infinity` 都会被拒绝。",
                        "- `允许普通件/急件` 填是/否；以前文件里写过 1/0 的，系统会尽量读懂。它用于控制当天允许排哪些批次类型。",
                        "- `说明` 只是人工备注，会保存为备注信息，不参与排产计算。",
                        "- 这张批量模板没有 `班次开始/班次结束` 两列；如果你曾在手工日历页给某天填过班次时间，用“清空本类数据后重导”重建时要先确认这些班次时间是否还需要保留。",
                        "- 如果只是想让某天回到默认规则，当前手工页没有单条删除按钮；手工页只能把这天改成你填写的规则。要真正移除这天的特殊配置，请先备份，再进“批量维护工作日历”，下载模板，整理好要保留的日期，谨慎选择“清空本类数据后重导”。",
                    ]
                ),
            ),
            _section(
                "哪些日子值得单独维护",
                "\n".join(
                    [
                        "- 节假日、调休、周末加班、特殊生产日都适合在这里维护。",
                        "- 普通工作日如果完全按默认规则执行，可以不单独配置。",
                        "- 如果你把某个周末或节假日手填到表里，系统会按假期规则处理，除非你明确把它设成工作日并填可用工时。",
                    ]
                ),
            ),
            _section("错误与联动", "{{excel_common_errors}}"),
        ],
        related_manual_ids=["scheduler_calendar", "personnel_calendar", "scheduler_batches"],
    ),
    "scheduler_batches": _topic(
        title="排产调度",
        summary="这里是排产调度入口，用来筛选批次、查看当前配置，并在页面里的“排产操作”区域启动执行排产或模拟排产。",
        full_manual_anchor="#6-3执行排产与模拟排产",
        help_card=_card(
            "排产调度页先看这 5 点",
            "先勾选待排批次，再设开始时间、截止日期和“本次启用齐套检查”。",
            "高级设置里的“默认启用齐套检查”只决定进页面时默认勾不勾；真正跑这一轮时，以这里的“本次启用齐套检查”为准。",
            "执行排产会生成新版本、保存历史，并更新批次/工序状态。",
            "模拟排产也会生成新版本和历史，方便对比，但不会更新批次/工序状态。",
            "排完后看结果，先确认版本号，再去甘特图、资源排班、优化分析或周计划查看。",
            "如果列表里没有复选框，先确认你筛的是“待排”状态。",
            "“发现参数问题就停止排产”主要管排产配置问题；没选批次、日期格式不对这些硬错误，不管勾不勾都会停。",
            "资源池资料不完整通常会先给黄色提醒，不一定直接拦停；正式排产前仍要回设备、人员和关系资料补齐。",
            "排产完成后如果有提醒（如资源池临时不可用、有些设置需要复核），当前页只展示前 5 条，更多提醒只显示剩余数量。",
            "常用方案下面的当前配置状态卡片，会告诉你当前按哪个方案、哪些设置需要复核。",
        ),
        sections=[
            _section(
                "进入前先准备什么",
                "\n".join(
                    [
                        "- 批次主数据、工序清单、设备、人员、日历和必要的物料约束都应准备好。",
                        "- 若要比较不同参数方案，先确认高级设置中的方案是否已调整完毕。",
                    ]
                ),
            ),
            _section(
                "最常见的操作步骤",
                "\n".join(
                    [
                        "- 先按状态、齐套显示或批次号筛出待排对象；只有“待排”列表里才会出现可勾选框。",
                        "- 勾选批次后设置排产开始时间、排产截止日期和“本次启用齐套检查”。",
                        "- 开始时间不填会用明天 08:00；截止日期不确定就先不填，填了就是硬限制，不是备注。",
                        "- 高级设置里的默认齐套检查只负责页面打开时的初始勾选；你在当前页面手动改勾选，只影响本次执行或模拟，不会反向改高级设置。",
                        "- 如果列表里全是已排、完成或已取消批次，就不能直接执行；先把筛选切回待排，或者回批次管理确认状态。",
                        "- 如果只是试算、插单比较或怕影响正式状态，点“模拟排产（插单模拟）”。",
                        "- 如果这版要作为正式排产结果，点“执行排产”。",
                        "- 模拟成功后通常会跳到甘特图；正式成功后通常回到排产调度页看最近一次排产快照。",
                        "- 排完后看页面提示里的版本号，再进入甘特图、资源排班、排产优化分析或周计划查看结果。",
                    ]
                ),
            ),
            _section(
                "几个按钮怎么理解",
                "\n".join(
                    [
                        "- `执行排产`：正式生成排程，保存一个历史版本，并把排到的批次/工序状态往前推进。",
                        "- `模拟排产（插单模拟）`：也保存一个历史版本，便于和正式版本对比，但不改批次和工序状态。",
                        "- `本次启用齐套检查`：打开后才会按齐套状态和齐套日期限制本轮排产；关闭时，批次上的齐套状态和齐套日期只用于查看。",
                        "- `发现参数问题就停止排产`：勾上后，派工方式、智能派工策略、自动分配设备人员这几项如果配置不合法，会直接停下并提示原因。不勾选时，系统会按页面提示的安全取值继续排产，并告诉你哪些设置需要重新保存。",
                        "- `保存历史`：不是单独按钮；只要执行排产或模拟排产成功，系统都会自动保存一个版本。",
                        "- `查看甘特图`：不是重新排产，只是打开某个版本的图形结果。看图前先确认版本，避免拿错版本判断。",
                    ]
                ),
            ),
            _section(
                "什么情况会报错或停下",
                "\n".join(
                    [
                        "- 没勾选待排批次、开始时间不合法、截止日期早于开始时间，会直接报错。",
                        "- 图号没有工艺、外协工序缺供应商或周期时，系统会提示你回批次详情、工艺模板或供应商配置补资料。",
                        "- 工时空着时可能按 0 小时继续排，结果会偏短；正式排产前请补真实工时。填了负数、文字、NaN、Inf 这类异常值时会报错。",
                        "- 启用齐套检查后，只要选中的批次里有未齐套或部分齐套，本次排产会停止；系统不会自动跳过这些批次继续排其它批次。",
                        "- 如果排产成功但有运行提醒，页面只列前 5 条，更多内容用“还有几条提醒”的数量提示；这表示结果已经生成，但建议排产员按提醒逐项复核。",
                    ]
                ),
            ),
            _section("版本与结果怎么看", "{{scheduler_version_basics}}"),
            _section(
                "容易误解的地方",
                "\n".join(
                    [
                        "- 执行排产和模拟排产的最大区别不是速度，而是是否推进批次状态。",
                        "- 列表里没有复选框，往往不是权限问题，而是当前筛选状态不对。",
                        "- 齐套日期不是一直生效；只有本次启用齐套检查时，它才会影响排产开工日期。启用后如果发现未齐套或部分齐套批次，本次排产会报错并停止，不会自动跳过这些批次继续排其它批次。",
                        "- 开始时间、截止日期、没选批次这类必须先改好的问题，不管是否勾选“发现参数问题就停止排产”，都会直接报错。",
                    ]
                ),
            ),
        ],
        related_manual_ids=["scheduler_batch_detail", "scheduler_gantt", "scheduler_config", "excel_batches"],
    ),
    "scheduler_batches_manage": _topic(
        title="批次管理",
        summary="批次管理负责手工新增、批量维护、批量修改、复制和删除批次，是排产前后都常用的整理页。",
        full_manual_anchor="#6-8批次管理",
        help_card=_card(
            "批次管理最容易误解的地方",
            "手工新增可照着填：B001 / P1001 / 50 / 2026-05-20 / 急件 / 齐套。",
            "批量维护入口适合整批新增或更新批次，不是排产按钮；维护完还要回执行排产页生成版本。",
            "批量修改只会改你实际填写的字段，留空的不动。",
            "自动生成或刷新工序会按当前零件工艺模板给批次补工序；刷新时会先删掉这批旧工序再重建，可能影响后续排程明细。",
            "复制批次依赖批次号末尾数字自增；没有数字的编号复制会失败。",
            "删除会同时删除这些批次下面的工序和排程记录（如果有），操作前先确认影响范围。",
        ),
        sections=[
            _section("这个页面主要做什么", "{{list_page_basics}}"),
            _section(
                "常见操作",
                "\n".join(
                    [
                        "- 手工新增：适合临时补一条批次，图号必须已经在工艺管理里存在。",
                        "- 批量维护：适合从计划表整批新增或更新批次，维护后可自动生成批次工序。",
                        "- 自动生成或刷新工序：按零件详情里的工艺模板给批次建立或重建工序。刷新不是只补缺口，而是先删除这批旧工序，再按模板重建；模板没维护好时不要急着点。",
                        "- 批量修改：只填你确认要改的字段，空着的不会被清空。",
                        "- 复制批次：适合相似订单复用，但批次号必须能自动续号。",
                        "- 删除批次：会连带删除工序和排程记录，属于高风险操作。",
                    ]
                ),
            ),
            _section(
                "手工新增怎么填",
                "\n".join(
                    [
                        "- 示例：批次号填 `B001`，图号选 `P1001`，数量填 `50`，交期选 `2026-05-20`，优先级选 `急件`，齐套显示选 `齐套`。",
                        "- `齐套日期` 默认留空即可；只有你想限制最早开工日期，并且排产时会启用齐套检查，才需要填写。",
                        "- 创建后系统会按零件工艺模板生成批次工序；如果缺工种、缺供应商或外协周期不正确，当前页只展示前 3 条模板提醒，更多提醒只显示剩余数量。",
                    ]
                ),
            ),
            _section(
                "自动生成或刷新工序怎么用",
                "\n".join(
                    [
                        "- 自动生成批次工序只处理新增和更新行；无变化或跳过的批次不会刷新工序。",
                        "- 这个按钮不是排产按钮，它只负责把批次下面的工序准备好，让后面的排产有东西可算。",
                        "- 适合三种情况：刚批量导入批次、工艺模板刚修过、发现某个批次工序漏了或顺序不对。",
                        "- 如果这批已经有工序，刷新会先删除原来的批次工序，再按当前模板新建一套；原来手工改过的工时、设备、人员、外协供应商等批次级特例可能被覆盖。",
                        "- 点之前先确认零件详情里的工序清单、工时、工种归属、供应商和外协周期是最新的；系统会按这些资料生成批次工序。",
                        "- 如果模板里缺工序、工序号重复、图号不存在、外协供应商匹配不到或周期不合法，页面会报错或给提醒。遇到这些提示，先修工艺模板或供应商配置，再回来重新生成。",
                        "- 如果批次已经排过产，刷新工序会影响后续重算和页面里看到的排程明细；正式操作前先确认是否需要保留已有历史版本或先做备份。",
                    ]
                ),
            ),
            _section(
                "做删除或批改前先确认什么",
                "\n".join(
                    [
                        "- 先看当前批次是否已经参与排产、导出周计划或进入历史版本。",
                        "- 如果只是想重新算版本，优先考虑模拟排产或局部调整，而不是直接删批次。",
                    ]
                ),
            ),
            _section("版本与结果怎么看", "{{scheduler_version_basics}}"),
        ],
        related_manual_ids=["scheduler_batches", "scheduler_gantt", "scheduler_analysis", "scheduler_week_plan"],
    ),
    "scheduler_batch_detail": _topic(
        title="批次详情/批次工序",
        summary="这里查看单个批次的基础信息，并补充或修正这个批次下面的工序、设备、人员、工时和外协信息。",
        full_manual_anchor="#6-2补充工序信息",
        help_card=_card(
            "批次工序不完整就排不出正确结果",
            "批量维护批次后，如果工序工时或外协信息不全，来这里补。",
            "批次级修改会优先使用本批次自己的设置，先想清楚这是个例还是模板该改。",
            "自动生成或刷新工序会重新按零件模板补齐本批次工序；刷新会先删除旧工序再重建，模板本身错了要先回零件详情修模板。",
            "工时空着会按 0 小时理解，正式排产前请补真实工时；负数、非数字、NaN、Inf、Infinity 这类才是错误值。",
            "补完后回到执行排产页重新算版本验证效果。",
        ),
        sections=[
            _section(
                "什么时候需要来这里",
                "\n".join(
                    [
                        "- 批量维护批次后需要补充工序工时、外协信息或局部修正模板时。",
                        "- 模板与实际订单不完全一致，需要针对单个批次做差异化处理时。",
                        "- 执行排产报“找不到工序”“工时缺失”“供应商或周期不完整”时，先来这里看这批自己的工序是否完整。",
                    ]
                ),
            ),
            _section(
                "维护时先看什么",
                "\n".join(
                    [
                        "- 先确认该批次对应的零件模板是否正确、工时是否完整。",
                        "- 批次级改动会优先使用本批次自己的设置，所以修改前要明确这是“特例”还是“通用规则”问题。",
                        "- 表格里的工序号要按普通整数维护，工种、设备、人员、换型时间、单件工时、外协供应商和周期要和真实生产一致。",
                        "- 自制工序重点看设备、人员、换型时间和单件工时；外协工序重点看供应商、周期模式和周期天数。",
                        "- 工时空着时系统按 0 小时处理，不会因为“没填”本身直接报错；但正式排产前要补真实工时，否则排出来的时间会偏短。",
                        "- 负数工时、不是数字的工时、NaN、Inf、Infinity、找不到工种、找不到供应商、外协周期不合法，都会影响排产，严重时会直接报错。",
                    ]
                ),
            ),
            _section(
                "自动生成或刷新工序怎么判断要不要点",
                "\n".join(
                    [
                        "- 如果这批刚导入，还没有批次工序，可以点自动生成，让系统按零件模板建一套。",
                        "- 如果零件模板刚修过，并且你希望这批也跟着模板走，可以点刷新工序。",
                        "- 刷新工序会先删除本批次旧工序，再按模板重建；这不是“只补一两道缺的工序”。",
                        "- 如果这批是特殊订单，已经手工改过工时、人员、设备或外协周期，刷新前要先确认这些特例是否还能保留；不确定时先不要点。",
                        "- 旧工序被删后，后续重算、甘特图和资源排班里看到的排程明细可能会变化；已经排过产的批次更要谨慎。",
                        "- 生成或刷新后如果看到前 3 条提醒，先处理这些提醒；更多提醒会用数量提示，表示还有别的问题没有完全展开。",
                    ]
                ),
            ),
            _section(
                "改完后下一步做什么",
                "\n".join(
                    [
                        "- 回到执行排产页重新算版本，或直接去甘特图验证工序顺序和时长是否符合预期。",
                        "- 如果问题根源其实是模板错了，应回到零件工艺模板或批量维护工序工时页面把基础资料改正确。",
                    ]
                ),
            ),
        ],
        related_manual_ids=["scheduler_batches", "scheduler_config", "process_parts", "excel_batches"],
    ),
    "scheduler_config": _topic(
        title="高级设置",
        summary="这里切换排产方案、调策略和计算模式，所有影响排产行为的开关都在这一页。",
        full_manual_anchor="#7-高级设置每个开关的作用",
        help_card=_card(
            "高级设置先抓住这几个原则",
            "日常先切“常用方案”，再改详细参数。",
            "只有“综合”策略时才需要调优先级/交期权重。",
            "优先级权重和交期权重可填 0 到 1 的小数，也可填 40、50 这类百分比；两项合计不能超过 1 或 100%。",
            "只有“精细计算”模式时，优化目标和计算时间上限才真正参与择优；计算时间上限填秒数，例如 20 表示最多算 20 秒。",
            "假期工作效率只在假期被安排工作、且当天效率没单独填时使用；必须填大于 0 的有限数字。",
            "冻结窗口用来保护近期已安排的任务，窗口内的任务不要轻易被新排程打乱；锁定天数要填 0 或正整数。",
            "自动分配只在工序没有指定设备或人员时尝试补齐，前提是设备、人员、技能和人机关系资料都够用。",
            "页面顶部的“当前配置状态”才是排产实际读取到的配置，会标明基线方案、需要复核的设置，以及是否保存系统补齐的设备和人员。",
            "设置改完要保存，再重新执行排产或模拟排产才会生成按新设置计算的新版本；已经生成的历史版本不会自动改变。",
            "填错时页面会提示你哪一项不对；旧设置读取异常时可能先按默认值展示，并用黄色提示提醒你复核后重新保存。",
        ),
        sections=[
            _section(
                "最先看哪几项",
                "\n".join(
                    [
                        "- 先选方案，再看排序策略、计算模式和派工方式。",
                        "- 不确定时先从内置方案开始，而不是一上来逐项改细参。",
                        "- 如果要临时试方案，可以先保存成一个容易识别的方案名，再回执行排产页跑模拟排产。",
                        "- 注意“基线方案”和“当前配置状态”的区别：基线说明来源，当前配置状态说明实际生效值。",
                        "- 改完高级设置后，旧历史版本不会自动变；要重新执行排产或模拟排产，才会看到新设置带来的结果。",
                    ]
                ),
            ),
            _section(
                "平时不直接显示的设置提示",
                "\n".join(
                    [
                        "- “保存系统补齐的设备和人员”控制系统补上的设备/人员是否保存回工序，下次排产继续用；它只有在自动分配真的补到了设备或人员时才有意义，模拟排产只生成版本，不会改工序资料。",
                        "- “默认启用齐套检查”只是执行排产页的默认勾选值；到执行排产页仍可以临时打开或关闭，本次实际勾选才决定这一轮是否启用齐套检查。",
                        "- 齐套检查关闭时，批次上的齐套状态和齐套日期只作为显示信息；打开后，如果发现未齐套或部分齐套批次，本次排产会报错并停止，不会自动跳过这些批次继续排其它批次；齐套日期只对齐套批次作为最早开工日。",
                        "- 页面只展示中文字段名和公开说明，不显示系统保存用的编号。",
                        "- 如果这些平时不直接显示的设置无法自动修复，页面会提示你需要处理；可见字段是否保存，以页面提示为准。",
                    ]
                ),
            ),
            _section(
                "什么时候需要细调权重",
                "\n".join(
                    [
                        "- 只有综合策略下，优先级和交期权重才会真正影响排序结果。",
                        "- 两个权重可以填 `0.4`、`0.5` 这样的 0 到 1 小数，也可以填 `40`、`50` 这样的百分比；`0.4` 和 `40` 都表示约 40%。",
                        "- 优先级权重调高，系统会更偏向急件、特急件先排；交期权重调高，系统会更偏向快到期或已经超期的批次。",
                        "- 举例：优先级权重 `0.7`、交期权重 `0.2`，意思是更照顾急件；优先级权重 `0.3`、交期权重 `0.6`，意思是更照顾快到期的批次。",
                        "- 两项合计不能超过 `1`（或百分比写法里的 `100`）。比如 `70` 加 `50` 会被拒绝，因为已经超过 100%。",
                        "- 权重留空、填负数、填文字、填 NaN 或 Infinity 这类无穷大/无效数字，保存时会提示错误或要求重新确认。",
                        "- 权重不是越大越好。调得太偏，可能让普通批次长期靠后，或者让资源换型次数变多。",
                        "- 调完权重后，建议先做一次模拟排产，再去排产优化分析看超期批次数、总拖期、换型次数有没有变好。",
                        "- 如果你只想快速验证大体可行性，先用默认或常用方案更稳。",
                    ]
                ),
            ),
            _section(
                "假期效率怎么填",
                "\n".join(
                    [
                        "- “假期工作效率”是给工作日历兜底用的：某天是周末、节假日或假期，但你又安排它生产，并且那天没有单独填效率时，系统就用这里的值。",
                        "- 可以填 `0.8`，意思是同样的班次大约按 80% 效率算；也可以填 `1`，意思是和普通工作日一样。",
                        "- 它必须是大于 0 的有限数字；空着、0、负数、文字、NaN、Inf、Infinity 都会被拒绝或提示你修正。",
                        "- 普通工作日且当天日历自己填了效率时，不靠这个默认值；改完后也要重新排产，新版本才会按新效率计算。",
                    ]
                ),
            ),
            _section(
                "冻结窗口怎么用",
                "\n".join(
                    [
                        "- 冻结窗口可以理解成“近期计划先别乱动”。比如窗口设 2 天，系统会尽量保护从开始时间往后 2 天内已经排好的任务。",
                        "- 页面上先打开“锁定近期排程”，再填“锁定天数”。锁定天数填 `0` 表示不实际锁定，填 `2` 表示从本次排产开始时间往后 2 天尽量沿用上一版安排。",
                        "- 适合现场已经领料、已经通知班组、设备已经准备好的短期计划。",
                        "- 冻结窗口太长，系统可调整空间会变小，可能导致后面的批次更难排；太短，又可能让近期安排频繁变化。",
                        "- 锁定天数要填 0 或正整数；填小数、负数、文字、NaN 或 Infinity 会保存失败或提示你改正。",
                        "- 修改冻结窗口后，旧历史版本不会自动变；要重新执行排产或模拟排产，才会生成按新窗口计算的新版本。",
                    ]
                ),
            ),
            _section(
                "精细计算模式要注意什么",
                "\n".join(
                    [
                        "- 精细计算会考虑优化目标和计算时间上限，适合正式比较方案；快速模式适合先看方向，不一定会生成完整优化过程曲线。",
                        "- “计算时间上限”填秒数，例如 `20` 表示最多尝试 20 秒；填得越大，系统有更多时间找更好方案，但页面等待也会更久。",
                        "- “深度优化”和“深度优化尝试时间”只在精细计算时才有意义；如果本机没有相关组件，页面或结果会提示，不要把它当成必开项。",
                        "- 计算时间上限和深度优化尝试时间都要填正整数；空着、0、负数、小数、文字或无穷大都会被拒绝或要求修正。",
                        "- 优化目标决定系统优先追求什么，例如最少超期批次数、最少拖期小时或最少换型次数；换目标后要重新排产，新版本才会体现变化。",
                    ]
                ),
            ),
            _section(
                "自动分配设备和人员怎么用",
                "\n".join(
                    [
                        "- “未指定设备或人员时，系统自动分配”适合工序里没有写死设备或人员，但系统里已经维护了设备、人员、工种、技能和人机关系的情况。",
                        "- 举例：某道车削工序只写了工种，没有指定 `CNC-01` 或 `OP001`，打开自动分配后，系统会在能做这类工序的设备和人员里挑一个合适组合。",
                        "- 自动分配不会凭空创造资源，也不会绕过设备停用、人员停用、日历休息、技能关系缺失这些限制；资料不全时，系统会提示本次先不自动补设备和人员。",
                        "- 如果“保存系统补齐的设备和人员”也生效，正式排产中系统补到的设备/人员可以保存回工序，方便下次继续用；模拟排产不会保存回工序。",
                        "- 打开或关闭自动分配后，旧历史版本不会变；要重新执行排产或模拟排产，才会看到新设置效果。",
                    ]
                ),
            ),
            _section("版本与结果怎么看", "{{scheduler_version_basics}}"),
        ],
        related_manual_ids=["scheduler_batches", "scheduler_analysis", "scheduler_dispatch", "scheduler_gantt"],
    ),
    "scheduler_calendar": _topic(
        title="工作日历配置",
        summary="这里设置全厂统一的工作日历，哪天上班、效率多少、能不能排急件，都在这里定。",
        full_manual_anchor="#5-5工作日历配置",
        help_card=_card(
            "工作日历决定哪天能排、效率多少",
            "只需要配和默认规则不一样的日子，比如周末加班、节假日调休。",
            "系统默认只知道周一到周五、周六周日；法定假期、调休和周末加班要自己维护。",
            "页面手工维护只有“工作日”和“假期”两类；旧批量文件里填过“周末”或“节假日”的，系统会按假期兼容处理。新模板仍建议填工作日或假期。",
            "个人工作日历优先级更高，偏差时先查个人日历有没有特殊配置。",
            "当前手工页面是按日期新增或覆盖保存，没有单条删除按钮；手工保存不能真正删除某天特殊配置。要取消某天特殊配置，请先备份，再进“批量维护工作日历”，下载模板，整理好要保留的日期，谨慎选择“清空本类数据后重导”。注意：批量模板不包含班次开始/结束，手工页维护过的班次时间不能靠这张模板原样保留。",
            "改完后建议去甘特图验证当天是否按预期生效。",
        ),
        sections=[
            _section(
                "这个页面和批量维护工作日历怎么分工",
                "\n".join(
                    [
                        "- 页面维护适合零散修改和即时核对。",
                        "- 批量维护更适合一次性处理大量节假日、调休、周末加班或临时效率调整。",
                    ]
                ),
            ),
            _section(
                "修改时先确认什么",
                "\n".join(
                    [
                        "- 确认你要改的是全局规则还是某个人员的个体差异。",
                        "- 页面下拉框只有 `工作日/假期`：正常上班选工作日，休息或停工选假期。",
                        "- 如果是周末加班，要把这一天单独设成工作日，并填写可用工时、效率、班次开始/结束、是否允许普通件、是否允许急件。",
                        "- 当前手工页面没有单条删除按钮；同一天保存一次就是新增或覆盖这一天的配置，不是删除这一天的特殊配置。",
                        "- 改完后建议重新执行排产或模拟排产生成新版本，再去甘特图验证当天是否按预期生效。",
                        "- 已经生成过的历史版本不会因为你改了日历就自动重算。",
                    ]
                ),
            ),
            _section(
                "容易忽略的地方",
                "\n".join(
                    [
                        "- 全局日历会被个人工作日历覆盖，所以出现偏差时先查个人日历是否有特殊配置。",
                        "- 未配置的日期并不是异常，而是按系统默认规则处理。",
                        "- 旧批量文件里把类型填成 `周末` 或 `节假日`，系统会当成 `假期` 兼容保存；新模板不要故意写旧叫法。如果想让周末生产，类型要填工作日并设置工时。",
                        '- 页面出现黄色提醒提示"假期工作效率"异常时，说明高级设置中的假期效率配置有问题，建议去高级设置页检查。',
                    ]
                ),
            ),
        ],
        related_manual_ids=["excel_calendar", "personnel_calendar", "scheduler_batches"],
    ),
}

SCHEDULER_TOPICS.update(SCHEDULER_OUTPUT_TOPICS)
