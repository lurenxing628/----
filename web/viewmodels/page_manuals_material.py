from __future__ import annotations

from typing import Any, Dict

from .page_manuals_common import _card, _section, _topic

MATERIAL_TOPICS: Dict[str, Dict[str, Any]] = {
    "material_master": _topic(
        title="物料主数据",
        summary="物料主数据用于登记系统认识的物料。先把物料建好，后面给批次挂物料需求时才选得到。",
        full_manual_anchor="#物料主数据",
        help_card=_card(
            "先把物料建好，再判断批次缺不缺料",
            "这里登记的是物料本身，例如物料编号、名称、规格、单位和当前库存。",
            "新增成功且状态为“可用/启用”后，这个物料才会出现在“批次物料需求（齐套判定）”的物料下拉框里。",
            "库存数量只能填数字，允许填 0，不能填负数。",
            "停用物料不会出现在新增需求的下拉框里，但已经挂到批次上的旧需求不会自动删除。",
        ),
        sections=[
            _section(
                "这个页面主要做什么",
                "\n".join(
                    [
                        "- 这个页面维护“物料字典”，意思是先告诉系统有哪些物料可以被批次使用。",
                        "- 物料主数据不是齐套结果。它只负责让后面的批次物料需求能选到正确物料。",
                        "- 如果没有先建物料，批次物料需求页就没法把需求挂到具体物料上。",
                        "",
                        "{{list_page_basics}}",
                    ]
                ),
            ),
            _section(
                "字段怎么填",
                "\n".join(
                    [
                        "- 物料编号：建议填稳定不重复的编号，例如 `MAT001`。保存后编号就是识别这条物料的主键，页面上不能直接改编号。",
                        "- 名称：填别人能看懂的物料名称，例如 `圆钢`、`轴承`。",
                        "- 规格、单位、备注：可以先不填，后面再补。",
                        "- 库存数量：填数字，允许 `0`，不能小于 `0`。",
                        "- 状态：选“可用”表示新增批次需求时能选到；选“停用”表示暂时不再给新需求选择。",
                    ]
                ),
            ),
            _section(
                "按钮点完会发生什么",
                "\n".join(
                    [
                        "- 点“创建”：新增一条物料。成功后页面会提示“已创建物料”，失败时会提示具体原因。",
                        "- 点“保存”：保存这一行的名称、规格、单位、库存、状态和备注。",
                        "- 点“删除”：删除这条物料。如果物料已经被批次物料需求引用，删除可能失败；这时先去处理引用它的批次物料需求。",
                        "- 保存后如果没有看到变化，先看页面顶部提示。绿色提示通常表示成功，红色提示通常表示没保存成功。",
                    ]
                ),
            ),
            _section(
                "常见报错怎么看",
                "\n".join(
                    [
                        "- 提示“物料编号不能为空”：新增时没有填物料编号。",
                        "- 提示“物料名称不能为空”：新增时没有填名称。",
                        "- 提示“库存数量必须是数字”：库存里填了文字、特殊符号或系统读不懂的内容。",
                        "- 提示“库存数量不能小于 0”：库存填了负数。",
                        "- 提示“物料已存在”：这个物料编号已经用过，需要换一个编号，或者直接编辑原来的物料。",
                    ]
                ),
            ),
            _section(
                "和批量维护怎么配合",
                "\n".join(
                    [
                        "- 页面里提到 Excel 入口时，统一理解成“批量维护”：一次导入或导出多条基础资料。",
                        "- 批量维护适合一次性整理很多物料或批次；少量修改直接在当前页面保存更直观。",
                        "- 批量维护前先确认编号列没有被 Excel 自动改坏，例如 `001` 不要变成 `1`。",
                        "",
                        "{{excel_common_flow}}",
                        "",
                        "{{excel_common_errors}}",
                    ]
                ),
            ),
        ],
        related_manual_ids=["material_batch", "scheduler_batches", "excel_batches"],
    ),
    "material_batch": _topic(
        title="批次物料需求（齐套判定）",
        summary="这里给批次挂上需要的物料和到料数量，系统据此判断这批是齐套、部分齐套，还是未齐套。",
        full_manual_anchor="#批次物料需求齐套判定",
        help_card=_card(
            "齐套资料就是这批要什么、到了多少",
            "先选批次，再新增物料需求；保存后系统会自动更新批次的齐套显示。",
            "需求数量必须大于 0；到料数量不填会按已到齐处理，明确填 0 或不足数量才会显示未齐套。",
            "页面显示“未齐套”时，也可以按“缺料”理解：这批还有物料没到够。",
            "只有启用齐套检查时，未齐套和部分齐套批次才会影响本次排产；只要所选批次里有这两种状态，本次排产会报错并停止，不会自动跳过它们继续排其它批次。",
            "物料必须先在“物料主数据”里存在。",
        ),
        sections=[
            _section(
                "齐套资料是什么",
                "\n".join(
                    [
                        "- 齐套资料不是一段文字说明，而是一批物料需求行。",
                        "- 每一行都表示：这个批次需要哪一种物料、需要多少、现在到了多少。",
                        "- 系统会拿“到料数量”和“需求数量”比较：到料数量够了，这一行就是齐套；不够，就是未齐套。",
                        "- 这个页面不填写预计到料日期。如果要控制这批最早哪天能开工，请回到批次信息里填写“齐套日期”。",
                    ]
                ),
            ),
            _section(
                "齐套状态怎么理解",
                "\n".join(
                    [
                        "- 齐套：这批挂上的所有物料需求都到够了。",
                        "- 部分齐套：有的物料到够了，有的还没到够。",
                        "- 未齐套：所有物料需求都没到够；日常也可以理解成“缺料”。",
                        "- 没有物料需求行：页面展示的是批次当前齐套状态，不要简单理解成一定齐套；如果批次当前状态不是齐套，启用齐套检查时本次排产会报错并停止。",
                        "- 齐套日期：它不是到料数量。只有启用“齐套检查”时，它才会影响排产，并作为这批最早可以开工的日期。",
                    ]
                ),
            ),
            _section(
                "怎么填最稳",
                "\n".join(
                    [
                        "- 第一步：先在“物料主数据”里创建物料，确认物料状态是“可用/启用”；停用物料不会出现在新增需求的物料下拉框里。",
                        "- 第二步：回到本页，先选择批次，再点“查看”。",
                        "- 第三步：新增物料需求，选择物料，填写需求数量和到料数量。",
                        "- 填写示例：批次 `B001` 需要物料 `MAT001` 共 `10` 件，已经到了 `6` 件，就填需求数量 `10`、到料数量 `6`，结果会显示未齐套。",
                        "- 如果 `10` 件全到了，就填需求数量 `10`、到料数量 `10`，结果会显示齐套。",
                        "- 如果新增时到料数量留空，系统会按“已经到齐”处理；如果明确填 `0` 或小于需求数量，才会显示未齐套。",
                        "- 更新已有行时，到料数量留空表示不改原来的到料数量；想改成 0，需要明确填 `0`。",
                    ]
                ),
            ),
            _section(
                "按钮点完会发生什么",
                "\n".join(
                    [
                        "- 点“查看”：只切换当前选中的批次，不会改数据。",
                        "- 点“新增”：给当前批次加一条物料需求，并立刻重新计算这个批次的齐套状态。",
                        "- 点“保存”：保存这一行新的需求数量和到料数量，并立刻重新计算这个批次的齐套状态。",
                        "- 点“删除”：删除这一行物料需求，并重新计算这个批次的齐套状态。删除最后一条需求后，页面仍以批次当前齐套状态为准；如果批次状态不是齐套，启用齐套检查仍会影响排产。",
                        "- 点“打开批次详情”：跳到批次详情页，方便查看这批的工序、状态和齐套日期。",
                    ]
                ),
            ),
            _section(
                "常见报错和跳过怎么看",
                "\n".join(
                    [
                        "- 提示“批次不存在”：选中的批次已经不存在，先回批次管理确认批次号。",
                        "- 提示“物料不存在”：物料主数据里没有这个物料，或物料编号填错了。",
                        "- 提示“该批次已存在该物料需求”：同一批次不能重复挂同一个物料；直接修改已有那一行即可。",
                        "- 提示“需求数量不能为空”或“需求数量不能小于 0.000001”：需求数量必须填大于 0 的数字。",
                        "- 提示“需求数量必须是数字”：需求数量里填了文字、特殊符号或系统读不懂的内容。",
                        "- 提示“到料数量必须是数字”：到料数量里填了文字、特殊符号或系统读不懂的内容。",
                        "- 提示“到料数量不能小于 0”：到料数量不能填负数。",
                        "- 批量维护页面如果提示跳过，通常表示这行按当前规则没有写入；先看检查结果里的原因，再决定是否修改后重导。",
                        "",
                        "{{excel_common_flow}}",
                        "",
                        "{{excel_format_warning}}",
                        "",
                        "{{excel_common_errors}}",
                    ]
                ),
            ),
            _section(
                "怎么影响排产",
                "\n".join(
                    [
                        "- 默认情况下，齐套状态和齐套日期主要用于页面显示，不会自动挡住排产。",
                        "- 只有在执行排产时启用了“本次启用齐套检查”，或系统配置里默认启用了齐套检查，未齐套和部分齐套批次才会真正影响本次排产。",
                        "- 速览卡里看到未齐套或部分齐套，都要当成排产前的提醒；只要启用齐套检查并且本次选中了这些批次，本次排产会报错并停止。",
                        "- 系统不会自动跳过未齐套或部分齐套批次继续排其它批次。要么先补齐物料，要么本次不要选这些批次，要么关闭齐套检查后再排。",
                        "- 启用齐套检查后，如果批次是齐套，并且批次信息里填了齐套日期，排产会把这个日期当成最早开工日。",
                        "- 如果没有启用齐套检查，哪怕页面显示未齐套，系统也可能继续排这个批次；这不是齐套资料失效，而是本次排产没有打开这个限制。",
                        "- 想验证齐套约束是否生效，可以维护完物料需求后，到执行排产页勾选“本次启用齐套检查”再试跑。",
                    ]
                ),
            ),
        ],
        related_manual_ids=["material_master", "scheduler_batches", "excel_batches"],
    ),
}
