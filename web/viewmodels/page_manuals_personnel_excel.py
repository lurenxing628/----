from __future__ import annotations

from typing import Any, Dict

from .page_manuals_common import _card, _section, _topic

_PERSONNEL_HARD_FAIL_EXCEL_FLOW = "\n".join(
    [
        "- 先下载模板，保持第一个工作表和表头不变；系统只按模板里的列名认数据，不靠人猜。",
        "- 单次上传文件大小不超过 16MB。",
        "- 上传 Excel 后先检查。检查和预览只是在页面上把文件读一遍，不会写入数据库，也不会改掉原有资料。",
        "- 检查结果里只要还有错误，系统会拒绝确认写入；先按提示改 Excel，再重新上传检查。",
        "- 如果检查后又改了 Excel 文件，或页面提示文件已过期，就要重新上传并重新检查，避免“预览的是旧文件，写入的是另一个文件”。",
        "- 只有点了确认写入，系统才会真正改数据库；确认写入时如果遇到硬错误，本次写入会整体失败或回滚，不会让你按行保留一部分成功数据。",
        "- 导入完成后建议再导出一次当前数据做复核。",
    ]
)

PERSONNEL_EXCEL_TOPICS: Dict[str, Dict[str, Any]] = {
    "excel_personnel": _topic(
        title="批量维护人员",
        summary="用 Excel 批量维护人员基本信息，适合一次性新增或更新很多人的工号、姓名、状态、班组和备注。",
        full_manual_anchor="#1-65人员基本信息",
        help_card=_card(
            "批量维护人员——填写速查",
            "列：工号、姓名、状态、班组、备注。",
            "其中工号、姓名、状态都必填；状态不能为空，不是“不填默认在岗”。",
            "状态模板下拉优先选：在岗 / 停用；以前文件里写过 启用 / 可用 / 正常 / 休假 / 离岗 / 停用/休假 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取。",
            "旧状态兼容口径：在岗/启用/可用/正常 会尽量按可参与排产理解；停用 / 休假 / 停用/休假 / 离岗 会尽量按不可参与或需暂停排产理解。",
            "班组填班组编号或名称，必须系统里已存在；留空则不分组。班组主要用于筛选、资源排班页面查看、报表查看和现场分组管理。",
            "工号会被人员设备关系、个人日历和排产结果引用，确认写入后尽量别频繁改。",
            "删除人员或清空本类数据后重导，会连带清掉对应人员设备关系和个人日历；执行前先导出或备份。",
            "建议维护顺序：先批量维护人员和设备 → 再批量维护人员设备关系 → 最后批量维护个人日历。",
            "编号类字段（工号）建议在 Excel 中设为文本格式填写，避免前导零丢失。",
            "“清空本类数据后重导”模式下如有批次工序或排程结果引用人员，系统会拒绝清空重导，改用“更新已有，新增缺少”即可。",
            "页面上有“导出当前数据”按钮，可以把系统里现有的人员信息导出成 Excel 文件，方便核对、备份或在现有数据基础上修改后重新导入。",
        ),
        sections=[
            _section("批量维护前准备", _PERSONNEL_HARD_FAIL_EXCEL_FLOW + "\n\n{{excel_import_modes}}\n\n{{excel_format_warning}}"),
            _section(
                "一步一步完成批量导入",
                "\n".join(
                    [
                        "1. 点击“下载模板”按钮，保存模板 Excel 文件到本地。",
                        "2. 用 Excel 打开模板，保持第一个工作表和表头不变，只在数据行里填写人员信息。",
                        "3. 填好后保存 Excel 文件（注意文件大小不超过 16MB）。",
                        "4. 回到页面，选择导入模式（默认“更新已有，新增缺少”，不确定就用这个）。",
                        "5. 点击“上传”按钮，选择刚才填好的 Excel 文件。",
                        "6. 系统会自动检查文件内容，检查结果会在页面上显示：哪些行准备新增、更新、跳过、有错误。",
                        "7. 如果有错误行，按提示修改 Excel 后重新上传检查。",
                        "8. 检查结果没有错误后，点击“确认写入”按钮，系统才会真正把数据写入数据库。",
                        "9. 导入完成后，建议点击“导出当前数据”按钮导出一份做复核。",
                    ]
                ),
            ),
            _section(
                "导出当前数据",
                "\n".join(
                    [
                        "- 页面上有“导出当前数据”按钮，点击后系统会把当前数据库里的所有人员信息导出为 Excel 文件。",
                        "- 导出的文件格式和导入模板一致，可以直接在上面修改后重新导入。",
                        "- 建议在以下场景使用导出：导入前备份现有数据、导入后复核结果、需要在现有数据上批量修改时作为底稿。",
                    ]
                ),
            ),
            _section(
                "关键列怎么填",
                "\n".join(
                    [
                        "- `工号`：必填，人员的唯一编号，必须唯一且保持稳定，后续会被关联关系和日历引用。建议在 Excel 中将这列设为文本格式，避免前导零丢失（比如 `001` 变成 `1`）。",
                        "- `姓名`：必填，建议与现场常用名称保持一致，方便排班确认。",
                        "- `状态`：必填，不能留空。新表优先按模板下拉填 `在岗/停用`；旧文件里写过 `启用/可用/正常/休假/离岗/停用/休假` 的，系统会尽量兼容读取。状态会影响这个人后续能不能参与排产。",
                        "- `班组`：选填，可填班组编号或名称，但前提是班组已经存在；留空表示这个人暂不归到某个班组。班组主要用于筛选、资源排班页面查看、报表查看和现场分组管理，不会直接代表加工能力。",
                        "- `备注`：选填，自由文本，适合记录临时说明，不参与排产计算。",
                    ]
                ),
            ),
            _section(
                "检查结果怎么看",
                "\n".join(
                    [
                        "{{excel_common_errors}}",
                        "- `错误`：这一行有问题，例如工号空了、姓名空了、状态不认识、班组不存在。检查结果里还有错误时，系统会拒绝确认写入。",
                        "- `跳过`：这一行不会写入，常见于“只导入新编号”模式下工号已经存在，或者内容没有变化。",
                        "- `写入失败`：如果确认写入阶段遇到硬错误，本次导入会整体失败或回滚，不会按行保留一部分成功数据；修好原因后重新上传检查。",
                    ]
                ),
            ),
            _section(
                "联动影响",
                "\n".join(
                    [
                        "- 工号一旦变更，会影响人员设备关联、个人工作日历和历史排程识别。",
                        "- 删除人员或清空人员后重导，会连带清掉这些人员的人员设备关系和个人日历；执行前先导出或备份，避免关系表和个人日历找不回来。",
                        "- 状态会影响人员是否可被排产使用；备注主要给人看，不参与排产。",
                        "- 班组主要影响筛选、资源排班页面查看和报表查看；人员是否真的能上某台设备，还要看人员设备关系。",
                        "- “清空本类数据后重导”模式风险较高，真正会挡住清空的是已有批次工序或排程结果引用人员，通常应优先选择“更新已有，新增缺少”。",
                    ]
                ),
            ),
        ],
        related_manual_ids=["personnel_management", "excel_personnel_link", "excel_personnel_calendar"],
    ),
    "excel_personnel_link": _topic(
        title="批量维护人员设备关系",
        summary="用 Excel 批量维护谁能操作哪台设备，并标清技能等级和主操设备；这张表直接影响批次详情里人机能不能配对。",
        full_manual_anchor="#1-67人员设备关联",
        help_card=_card(
            "批量维护人员设备关系——填写速查",
            "列：工号(必填)、设备编号(必填)、技能等级、主操设备。",
            "工号和设备编号必须是系统里已存在的，否则报错。",
            "同一个 Excel 文件里，同一组“工号 + 设备编号”只能出现一次，重复会被拒绝。",
            "技能等级模板下拉优先选：初级 / 普通 / 熟练；以前文件里写过 新手 / 一般 / 中级 / 高级 / 专家 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取；不填默认普通。",
            "主操设备模板下拉优先选：是 / 否；以前文件里写过 主操 / 非主操 / 主 / 非主 / 1 / 0 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取。同一个 Excel 文件里，同一人多条主操=是会报错。",
            "技能等级和主操设备影响自动分配顺序，不等于加工更快，也不会让停用人员或休息日人员绕过限制。",
            "正常把某台设备设为主操时，系统会先清掉这个人的旧主操，再写入新的主操设备。",
            "设备侧模板列顺序是 设备编号 / 工号；如果以前文件里用过“机器编号”“操作工号”等列名，系统会尽量读懂。",
            "批量维护前先确认人员和设备都已创建，否则关联不上。",
            "页面上有“导出当前数据”按钮，可以把系统里现有的人员设备关系导出成 Excel 文件，方便核对、备份或在现有数据基础上修改后重新导入。",
        ),
        sections=[
            _section("批量维护前准备", "{{excel_common_flow}}\n\n{{excel_import_modes}}\n\n{{excel_format_warning}}"),
            _section(
                "一步一步完成批量导入",
                "\n".join(
                    [
                        "1. 点击“下载模板”按钮，保存模板 Excel 文件到本地。",
                        "2. 用 Excel 打开模板，保持第一个工作表和表头不变，只在数据行里填写人员设备关系。",
                        "3. 填好后保存 Excel 文件（注意文件大小不超过 16MB）。",
                        "4. 回到页面，选择导入模式（默认“更新已有，新增缺少”，不确定就用这个）。",
                        "5. 点击“上传”按钮，选择刚才填好的 Excel 文件。",
                        "6. 系统会自动检查文件内容，页面上显示检查结果。",
                        "7. 如果有错误行（工号不存在、设备编号不存在、重复组合等），按提示修改 Excel 后重新上传检查。",
                        "8. 检查结果没有错误后，点击“确认写入”按钮。",
                        "9. 导入完成后，建议点击“导出当前数据”按钮导出一份做复核。",
                    ]
                ),
            ),
            _section(
                "导出当前数据",
                "\n".join(
                    [
                        "- 页面上有“导出当前数据”按钮，点击后系统会把当前数据库里的所有人员设备关系导出为 Excel 文件。",
                        "- 导出的文件格式和导入模板一致，可以直接在上面修改后重新导入。",
                        "- 建议在以下场景使用导出：导入前备份现有关系、导入后复核结果、需要在现有关系上批量修改时作为底稿。",
                    ]
                ),
            ),
            _section(
                "关键列怎么填",
                "\n".join(
                    [
                        "- `工号` 与 `设备编号` 都必填，且必须已经存在于系统中。",
                        "- `工号 + 设备编号` 合起来确定一条关系，同一个 Excel 文件里不能重复写同一组，重复会被拒绝。",
                        "- `技能等级` 模板下拉推荐填 `初级/普通/熟练`；以前文件里写过 `新手/一般/中级/高级/专家` 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取；留空默认普通。",
                        "- `主操设备` 模板下拉推荐填 `是/否`；以前文件里写过 `主操/非主操/主/非主/1/0` 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取；同一个 Excel 文件里，同一人多条主操=是会报错。",
                        "- `技能等级` 和 `主操设备` 主要影响自动分配时的优先顺序：主操、熟练人员更容易被优先选中，但不表示加工时间自动变短。",
                        "- 如果这次只给某个人一台设备填主操=是，系统会清掉这个人的旧主操，再把这台写成新的主操设备。",
                        "- 如果某个人某台设备这一行已经存在，再导入同一组合时会按导入模式决定更新、跳过或保持不变。",
                    ]
                ),
            ),
            _section(
                "具体例子",
                "\n".join(
                    [
                        "- `OP001 / CNC-01 / 熟练 / 是`：表示 OP001 可以操作 CNC-01，而且这是他的主操设备。",
                        "- `OP001 / CNC-02 / 普通 / 否`：表示 OP001 也能操作 CNC-02，但不是主操设备。",
                        "- `OP002 / CNC-01 / 初级 / 否`：表示 OP002 可以操作 CNC-01，但技能等级较低，自动分配排序时通常不会优先于熟练主操；这不是说 OP002 一定加工更慢。",
                        "- 同一个人如果在同一个 Excel 文件里同时给两台设备填“主操设备=是”，检查会报错；正常只设置一台主操时，系统会清掉这个人的旧主操再写新的。",
                    ]
                ),
            ),
            _section(
                "和设备侧导入有什么区别",
                "\n".join(
                    [
                        "- 人员侧是 `工号 / 设备编号 / 技能等级 / 主操设备`；设备侧是 `设备编号 / 工号 / 技能等级 / 主操设备`。",
                        "- 两个入口写的是同一张关系表，业务含义相同；一个周期内建议固定选一侧维护，避免双方都改导致理解混乱。",
                    ]
                ),
            ),
            _section(
                "错误、跳过和确认写入怎么看",
                "\n".join(
                    [
                        "{{excel_common_errors}}",
                        "- `错误`：工号不存在、设备编号不存在、同一组工号+设备编号重复、同一人多台主操、技能等级或主操设备填了系统不认识的词，都会在检查结果里标成错误。",
                        "- `跳过`：选择“只导入新编号”时，已存在的工号+设备编号组合会跳过；没有变化的行也可能显示为跳过或无变化。",
                        "- 检查结果里只要还有错误，就不能确认写入。先补人员、设备或改重复关系，再重新上传检查。",
                        "- 如果确认写入阶段又遇到硬错误，系统会拒绝或回滚本次写入；不要理解成会自动保留一部分正确行。看页面最终提示后，修好原因再重新导入关系。",
                    ]
                ),
            ),
        ],
        related_manual_ids=["excel_personnel", "excel_equipment", "equipment_management"],
    ),
    "excel_personnel_calendar": _topic(
        title="批量维护个人日历",
        summary="用 Excel 批量维护个人级别的日历差异，适合把请假、调休、加班、夜班和临时轮班一次性导入。",
        full_manual_anchor="#1-69个人工作日历",
        help_card=_card(
            "批量维护个人日历——填写速查",
            "列：工号(必填)、日期(必填)、类型（建议填写）、班次开始、班次结束、可用工时、效率、允许普通件、允许急件、说明。",
            "日期格式：2026-04-15 或 2026/04/15，Excel日期单元格也能识别；只填日期，不要写 08:00 这类时间；同一人同一天不能重复。",
            "类型建议填：工作日 / 假期。以前写过周末 / 节假日的，系统会按假期理解；留空会按工作日处理。",
            "班次开始写 08:00 这种格式，留空按 08:00；班次结束可填可不填，常规 0<可用工时<=24 时系统会根据工时推导结束时间。",
            "班次结束和可用工时同时填写时，以班次开始/结束反算出来的工时为准。",
            "可用工时超过 24 小时时，不要理解成系统一定会自动回填班次结束；这类特殊安排要人工复核。",
            "如果填写班次结束，且结束时间早于或等于开始时间，系统会按跨午夜班次理解。",
            "可用工时填非负数(小时)，效率填正数；效率留空时，工作日默认1.0，假期默认取排产参数里的“假期工作效率”。可用工时和效率只接受有限数字，TRUE/FALSE、NaN、Inf、Infinity 都会报错。",
            "如果排产参数里的“假期工作效率”缺失或异常，个人日历 Excel 会拒绝检查或确认导入，先去排产参数修好。",
            "允许普通件/急件模板下拉优先填 是/否；以前文件里写过 1/0 的，系统会尽量读懂。新表按下拉推荐写，旧写法只是兼容读取；不填默认是。",
            "说明只是人工备注，会保存为备注信息，不参与排产计算。",
            "预览和当前表会优先显示中文，不会把系统内部代码值当成日常展示值。",
            "工号必须已存在；个人日历优先级高于全局工作日历。",
            "有“预览保护”：预览后数据被改了，系统会要求重新预览。",
            "当前手工个人日历没有单条删除按钮；批量维护适合整批更新。需要清掉一批个人覆盖时，先下载模板整理要保留的人员日期，再谨慎选择“清空本类数据后重导”，确认前看清新增、更新、跳过和清空范围。",
            "页面上有“导出当前数据”按钮，可以把系统里现有的个人日历数据导出成 Excel 文件，方便核对、备份或在现有数据基础上修改后重新导入。",
        ),
        sections=[
            _section("批量维护前准备", _PERSONNEL_HARD_FAIL_EXCEL_FLOW + "\n\n{{excel_date_format}}\n\n{{excel_format_warning}}"),
            _section(
                "一步一步完成批量导入",
                "\n".join(
                    [
                        "1. 点击“下载模板”按钮，保存模板 Excel 文件到本地。",
                        "2. 用 Excel 打开模板，保持第一个工作表和表头不变，只在数据行里填写个人日历数据。",
                        "3. 填好后保存 Excel 文件（注意文件大小不超过 16MB）。",
                        "4. 回到页面，选择导入模式（默认“更新已有，新增缺少”，不确定就用这个）。",
                        "5. 点击“上传”按钮，选择刚才填好的 Excel 文件。",
                        "6. 系统会自动检查文件内容，页面上显示检查结果。",
                        "7. 如果有错误行（工号不存在、日期格式不对、同一人同一天重复等），按提示修改 Excel 后重新上传检查。",
                        "8. 检查结果没有错误后，点击“确认写入”按钮。",
                        "9. 导入完成后，建议点击“导出当前数据”按钮导出一份做复核。",
                    ]
                ),
            ),
            _section(
                "导出当前数据",
                "\n".join(
                    [
                        "- 页面上有“导出当前数据”按钮，点击后系统会把当前数据库里的所有个人日历数据导出为 Excel 文件。",
                        "- 导出的文件格式和导入模板一致，可以直接在上面修改后重新导入。",
                        "- 建议在以下场景使用导出：导入前备份现有日历、导入后复核结果、需要清空重导前先留一份完整底稿。",
                    ]
                ),
            ),
            _section(
                "关键列怎么填",
                "\n".join(
                    [
                        "- `工号` 与 `日期` 必填，其中 `工号 + 日期` 不能重复；日期只填日期，不要带 08:00 这类时间；`类型` 建议填写，留空会按 `工作日` 处理。",
                        "- `类型` 填 `工作日` 表示这个人当天可排产，填 `假期` 表示默认不可排产；如果假期还要加班，就把可用工时填成大于 0。",
                        "- `班次开始` 建议统一写成 `08:00` 格式；不填时系统按 `08:00` 处理。",
                        "- `班次结束` 如果不填，系统会看可用工时；常规 `0 < 可用工时 <= 24` 时，会用班次开始加可用工时推导结束时间。",
                        "- `班次结束` 和 `可用工时` 同时填写时，以班次开始/结束反算出来的工时为准，不按你手填的可用工时为准。",
                        "- `可用工时` 超过 24 小时时，不要按一定会回填班次结束理解；请人工复核这种特殊班次。",
                        "- `可用工时` 和 `效率` 必须是真正的有限数字；`TRUE/FALSE`、`NaN`、`Inf`、`Infinity` 都会被拒绝。",
                        "- `效率` 留空时，工作日默认 `1.0`；假期默认取排产参数里的“假期工作效率”。如果这个参数缺失或异常，检查和确认导入都会被拒绝。",
                        "- `可用工时` 填小时数，`效率` 填正数倍率，`允许普通件/急件` 模板下拉推荐填是/否。以前文件里写过 1/0 的，系统会尽量读懂；新表按下拉推荐写，旧写法只是兼容读取。",
                        "- `说明` 只是人工备注，会保存为备注信息，不参与排产计算。",
                    ]
                ),
            ),
            _section(
                "班次时间怎么填才不误解",
                "\n".join(
                    [
                        "- 白班例子：`班次开始=08:00`、`班次结束=16:00`，系统会算成 8 小时。",
                        "- 只填工时也可以：`班次开始=08:00`、`可用工时=8`、`班次结束`留空，系统会推导结束为 `16:00`。",
                        "- 如果同时填 `班次结束=16:00` 和 `可用工时=10`，系统仍按 08:00 到 16:00 反算为 8 小时。",
                        "- 夜班例子：`班次开始=20:00`、`可用工时=8`、`班次结束`留空，系统会推导到次日 `04:00`。",
                        "- 上面这些自动推导只适合常规 `0 < 可用工时 <= 24`；可用工时超过 24 小时时，不要写成系统一定会回填结束时间。",
                        "- 跨午夜例子：`班次开始=22:00`、`班次结束=06:00`，系统会理解为从当天 22:00 上到第二天 06:00。",
                        "- 如果把开始和结束都填成 `08:00`，系统会按跨到第二天的 24 小时理解，一般不要这样填。",
                    ]
                ),
            ),
            _section(
                "错误和跳过怎么看",
                "\n".join(
                    [
                        "{{excel_common_errors}}",
                        "- `错误`：工号不存在、日期格式不对、同一人同一天重复、可用工时不是有限数字、效率小于等于 0，或填了 `TRUE/FALSE`、`NaN`、`Inf`、`Infinity`，都会在检查结果里标成错误。排产参数里的“假期工作效率”异常时，检查和确认导入也会被拒绝。",
                        "- `跳过`：选择“只导入新编号”时，已存在的工号+日期会跳过；没有变化的行也可能显示为跳过或无变化。",
                        "- `写入失败`：如果确认写入阶段遇到硬错误，本次导入会整体失败或回滚，不会按行保留一部分成功数据；修好原因后重新上传检查。",
                    ]
                ),
            ),
            _section(
                "为什么要先预览再确认",
                "\n".join(
                    [
                        "- 个人日历优先级高于全局日历，错一行就可能让这个人的整天排程失真。",
                        "- 预览保护能帮助你避免“检查后别人又改了数据，你还按旧结果确认写入”的问题。",
                    ]
                ),
            ),
        ],
        related_manual_ids=["personnel_calendar", "excel_personnel", "scheduler_calendar"],
    ),
}
