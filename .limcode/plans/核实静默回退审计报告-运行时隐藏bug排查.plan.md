## TODO LIST

<!-- LIMCODE_TODO_LIST_START -->
- [ ] 在 MD 文档末尾追加第 11 节：运行时隐藏问题核实报告（含致命/高危/中危三级分类）  `#t1`
<!-- LIMCODE_TODO_LIST_END -->


## 目标

对审计报告中列出的 68 个方法/177 个实现点进行逐一源码核实，重点识别：
1. **真正会在运行时隐藏 BUG** 的回退（错误被吞掉，用户/开发者完全无法感知）
2. **导致异常无法抛出/显现** 的回退（异常被 catch 后替换为默认值，调用方无法区分"正常默认"和"异常降级"）
3. **会导致错误结果无声传播** 的回退（回退后产出的结果看似正常但语义已变）

## 核实结论摘要

经逐一比对源码，确认：

### 致命级（CRITICAL）—— 完全静默，会导致错误数据持久化且无任何提示

| # | 文件 | 位置 | 问题 |
|---|---|---|---|
| 1 | `external_group_service.py:129-131` | `_apply_separate_mode` | `per_op_days` 非法/<=0 时静默写 1.0 天到数据库，无 warning、无 log、无返回标记 |
| 2 | `resource_pool_builder.py:142-143` | `load_machine_downtimes` | 单条坏停机记录导致**所有**停机约束被清空（不是只跳过坏记录） |
| 3 | `sgs.py:396-408` | `dispatch_sgs` 结果写回 | 设备/人员负荷统计更新失败 `except: pass`，后续自动分配基于错误负荷数据 |

### 高危级（HIGH）—— 有 warning 但极易被忽略，会改变排产结果

| # | 文件 | 位置 | 问题 |
|---|---|---|---|
| 4-8 | `schedule_params.py` 多处 | `resolve_schedule_params` | 策略/模式/规则全部静默回退默认值，warning 埋在 summary 中 |
| 9 | `sgs.py:302-356` | 评分异常兜底 | 评分完全失败时生成 `(inf, 1.0, 99.0, inf, ...)` 的 key，但候选仍参与排序 |
| 10 | `resource_pool_builder.py:208-216` | `build_resource_pool` | 任何 DB 异常直接关闭 auto-assign |
| 11 | `route_parser.py:191-195` | 未知工种 | 工种配置拼写错误导致所有该工种工序变成外部工序 |
| 12 | `route_parser.py:214-219` | 无供应商匹配 | 硬编码 1.0 天周期写入 |
| 13 | `freeze_window.py:52-54` | 冻结窗口加载失败 | 降级为"不冻结"，已排好的工序被重排 |
| 14 | `auto_assign.py:97-100` | 工时计算失败 | 直接返回 None 放弃分配，完全静默 |

### 中危级（MEDIUM）—— 影响数据质量或可观测性

| # | 文件 | 位置 | 问题 |
|---|---|---|---|
| 15 | `gantt_tasks.py:106-107` | `build_calendar_days` | 单条坏日历致整个日历标注消失 |
| 16 | `gantt_service.py:56-59` | 超期解析 | 坏 JSON 致超期批次不高亮 |
| 17 | `schedule_service.py:427-443` | warnings 合并 | 极端情况下排产 warnings 全部丢失 |
| 18 | `enum_normalizers.py:98-99` | 工种分类空值 | 空值默认 internal，可能应为 external |
| 19 | `calendar.py:26-34` | 日历效率/工时 | 坏数据静默回默认，影响排产时间 |
| 20 | `part_service.py:120-124` | 路线解析失败 | 零件创建成功但路线未解析，API 场景不可见 |

## 更新内容

在原 MD 文档末尾追加 **"第 11 节：运行时隐藏问题核实报告"**，包含：

1. **核实方法说明**：逐文件比对源码，标注行号和上下文
2. **致命级隐藏 BUG 清单**：完全静默、会持久化错误数据
3. **高危级隐藏 BUG 清单**：有 warning 但极易被忽略
4. **中危级隐藏 BUG 清单**：影响数据质量或可观测性
5. **已核实为"合理保护"的回退**：不构成 BUG 隐藏
6. **与原报告的差异说明**：行号修正、实现点数微调
