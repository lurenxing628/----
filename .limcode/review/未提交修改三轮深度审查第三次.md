# 未提交修改三轮深度审查（第三次）
- 日期: 2026-04-04
- 概述: 对当前工作区未提交修改（batch_service 拆分系列、schedule_summary 拆分系列、excel_validators、路由层对齐）进行三轮独立深度审查，重点检验前次问题是否修复到位、是否引入新问题。
- 状态: 已完成
- 总体结论: 通过

## 评审范围

# 未提交修改三轮深度审查（第三次）

- 日期：2026-04-04
- 范围：当前工作区内未提交修改，覆盖以下修改域：
  1. **批次服务拆分**：`batch_service.py` → `batch_write_rules.py` + `batch_template_ops.py` + `batch_excel_import.py` + `batch_copy.py`
  2. **排产汇总拆分**：`schedule_summary.py` → `schedule_summary_freeze.py` + `schedule_summary_degradation.py` + `schedule_summary_assembly.py`
  3. **校验器一致性**：`excel_validators.py` 与服务层同源约束
  4. **路由层对齐**：`scheduler_batches.py`、`scheduler_excel_batches.py` 等路由是否与服务层新接口对齐
- 方法：三轮递进式审查
  - 第一轮：前次问题修复验证、结构与接口设计、职责划分
  - 第二轮：逻辑严谨性、边界条件、潜在 BUG
  - 第三轮：过度兜底/静默回退检查、简洁性与优雅度

## 评审摘要

- 当前状态: 已完成
- 已审模块: core/services/scheduler/batch_write_rules.py, core/services/scheduler/batch_template_ops.py, core/services/scheduler/batch_excel_import.py, core/services/scheduler/batch_copy.py, core/services/scheduler/batch_service.py, core/services/scheduler/schedule_summary.py, core/services/scheduler/schedule_summary_freeze.py, core/services/scheduler/schedule_summary_degradation.py, core/services/scheduler/schedule_summary_assembly.py, core/services/common/excel_validators.py, web/routes/scheduler_batches.py, web/routes/scheduler_excel_batches.py, tests/test_architecture_fitness.py
- 当前进度: 已记录 3 个里程碑；最新：R3_fallback_elegance
- 里程碑总数: 3
- 已完成里程碑: 3
- 问题总数: 4
- 问题严重级别分布: 高 0 / 中 0 / 低 4
- 最新结论: ## 三轮深度审查最终结论 本次审查覆盖了当前工作区未提交的两大拆分修改（批次服务系列 5 个文件、排产汇总系列 4 个文件）以及校验器和路由层共 13 个核心模块。 ### 达成目的评估 **批次服务拆分**：✅ 完全达成。原高耦合 `batch_service.py` 被合理拆分为 4 个职责单一的模块 + 1 个薄外观层。每个模块行数均在 250 行以内，拆分后代码的可测试性和可维护性显著提升。特别值得称赞的是 `batch_write_rules.py` 的纯函数设计和 `_MISSING` 哨兵值模式的统一使用。 **排产汇总拆分**：✅ 基本达成。3 个子模块的职责边界清晰（冻结/退化/组装），主编排文件复杂度大幅降低。但主文件仍有 482 行（逼近 500 行门禁），`_build_result_summary_obj` 的 47 个参数仍是后续优化空间。 **校验器一致性**：✅ 完全达成。前次发现的日历 Excel 非有限数字校验漂移已修复（新增 `math.isfinite()` 检查），预览→确认→落库三层规则现在一致。 **前次问题修复**：✅ 9/9 项全部修复到位，无遗漏。 ### BUG 与风险评估 - **零高风险 BUG**：三轮审查未发现任何高或中风险的逻辑缺陷 - **零新增静默回退**：全部 15 处异常处理逐一审查，均为已注册白名单或合理降级 - **4 项低风险问题**：均为样式/文档层面，其中 3 项已标记为"接受风险"，1 项（`or 0` 语义模糊）建议在后续顺手修复 - **无过度兜底风险**：约 30 处冗余类型包装属于视觉噪音，不影响逻辑正确性 ### 总体判断 当前修改可以安全提交。代码质量相比前两次 review 有明显提升，所有关键路径的校验链完整、跨层契约一致、事务边界正确。剩余问题全部为低优先级的代码风格改进，不阻塞交付。
- 下一步建议: 可以直接提交。后续可选择性修复 batch_excel_import.py:83 的 or 0 语义问题，以及在排产汇总模块中清理冗余的类型包装。
- 总体结论: 通过

## 评审发现

### 批次复制中裸类型转换

- ID: F-批次复制中裸类型转换
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 接受风险
- 相关里程碑: R2_logic_boundary
- 说明:

  batch_copy.py 第40行对 b.quantity 直接使用 int() 转换，如果数据库中存在脏数据会产生非业务友好异常。但所有创建路径都经过校验，实际触发概率极低。
- 建议:

  可保持现状。如追求一致性可用 parse_finite_int，但属于低收益改动。
- 证据:
  - `core/services/scheduler/batch_copy.py:40`
  - `core/services/scheduler/batch_copy.py`

### normalize_int 的 or 0 语义模糊

- ID: F-normalize-int-的-or-0-语义模糊
- 严重级别: 低
- 分类: 可维护性
- 跟踪状态: 开放
- 相关里程碑: R2_logic_boundary
- 说明:

  batch_excel_import.py 第83行 svc._normalize_int(...) or 0，由于 _normalize_int 在 allow_none=False 时应抛异常而非返回 None，or 0 永远不应被触发。给读者传递了错误暗示。
- 建议:

  去掉 or 0 或加注释说明纯防御意图。
- 证据:
  - `core/services/scheduler/batch_excel_import.py:83`
  - `core/services/scheduler/batch_excel_import.py`

### 批量编辑路由空值语义缺文档

- ID: F-批量编辑路由空值语义缺文档
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 接受风险
- 相关里程碑: R2_logic_boundary
- 说明:

  scheduler_batches.py 第358-361行将空字符串统一转为 None 视为'不更新'，用户无法通过批量编辑清空交期或备注。这是合理的设计决策但缺少注释说明。
- 建议:

  在路由代码中加注释说明此设计意图。
- 证据:
  - `web/routes/scheduler_batches.py:358-361`
  - `web/routes/scheduler_batches.py`

### 排产汇总类型包装噪音

- ID: F-排产汇总类型包装噪音
- 严重级别: 低
- 分类: 可维护性
- 跟踪状态: 接受风险
- 相关里程碑: R3_fallback_elegance
- 说明:

  schedule_summary_degradation.py 的 _compute_downtime_degradation（267-301行）对已经确定类型的变量反复用 bool()/int() 包装。例如 auto_assign_enabled 在第268行已经是 bool，但在第281/285行又分别 bool() 了两次；load_partial_fail_count 在第272行通过 _meta_int 已经是 int，但第289/297行又 int() 了两次。同样的模式也出现在 schedule_summary.py 的 _build_runtime_state 和 _build_fallback_state 中。这些转换全部冗余，增加了约 30 处无效的类型噪音，但不影响正确性。
- 建议:

  在后续清理轮次中统一去除已确定类型的冗余 bool()/int()/list() 包装，保留跨模块边界入口的防御。
- 证据:
  - `core/services/scheduler/schedule_summary_degradation.py:280-301`
  - `core/services/scheduler/schedule_summary.py:272-278`
  - `core/services/scheduler/schedule_summary.py:337-343`
  - `core/services/scheduler/schedule_summary_degradation.py`
  - `core/services/scheduler/schedule_summary.py`

## 评审里程碑

### R1_structure_fixverify · 第一轮：前次问题修复验证与结构设计审查

- 状态: 已完成
- 记录时间: 2026-04-04T18:30:15.998Z
- 已审模块: core/services/scheduler/batch_write_rules.py, core/services/scheduler/batch_template_ops.py, core/services/scheduler/batch_excel_import.py, core/services/scheduler/batch_copy.py, core/services/scheduler/batch_service.py, core/services/scheduler/schedule_summary.py, core/services/scheduler/schedule_summary_freeze.py, core/services/scheduler/schedule_summary_degradation.py, core/services/scheduler/schedule_summary_assembly.py, core/services/common/excel_validators.py, web/routes/scheduler_batches.py, web/routes/scheduler_excel_batches.py, tests/test_architecture_fitness.py
- 摘要:

  ## 前次问题修复验证（9/9 项全部已修复）

  逐项对照前两次 review（共 12 项 finding），确认当前代码的修复状态：

  | # | 原始问题 | 状态 | 修复位置 |
  |---|---------|------|---------|
  | ❶ | `int()` 裸转换 | ✅ 已修复 | `batch_excel_import.py:83` 改为 `svc._normalize_int(...)` |
  | ❷ | `allow_none` 形同虚设 | ✅ 已修复 | `batch_write_rules.py:26` 移除 `allow_none` 参数 |
  | ❸ | `_extract_freeze_warnings` 参数名误导 | ✅ 已修复 | `schedule_summary_freeze.py:61` 参数名改为 `all_warnings` |
  | ❹ | `update()` 无法清空字段 | ✅ 已修复 | `batch_service.py:204-212` 默认值改为 `_MISSING` |
  | ❺ | 图号切换保护未统一 | ✅ 已修复 | `batch_service.py:222` 传入 `auto_generate_ops=False` |
  | ❻ | Excel 模板补建 warning 丢失 | ✅ 已修复 | `scheduler_excel_batches.py:386-388` 新增 `consume_user_visible_warnings` |
  | ❼ | 日历 Excel 非有限数字校验漂移 | ✅ 已修复 | `excel_validators.py:197-198,222-223` 新增 `math.isfinite(v)` 检查 |
  | ❽ | `__all__` 导出下划线函数 | ✅ 已修复 | `schedule_summary.py:47-59` 改为公开名称，用 `import as` 别名 |
  | ❾ | 日期校验双轨无文档说明 | ✅ 已修复 | `batch_service.py:68-73` 新增设计决策注释 |

  ## 拆分结构评估

  ### 批次服务拆分
  - `batch_write_rules.py`（159行）：纯校验与载荷构建，零状态
  - `batch_template_ops.py`（229行）：模板工序编排，职责清晰
  - `batch_excel_import.py`（108行）：Excel 导入编排，轻量
  - `batch_copy.py`（79行）：复制逻辑独立
  - `batch_service.py`（393行）：外观层 + 工具方法

  拆分比例健康：原大文件职责分散到 4 个功能模块，主文件保留外观层角色。

  ### 排产汇总拆分
  - `schedule_summary_freeze.py`（93行）：冻结窗口判定
  - `schedule_summary_degradation.py`（322行）：退化状态采集
  - `schedule_summary_assembly.py`（456行）：结果组装
  - `schedule_summary.py`（482行）：主编排

  入口文件 482 行逼近 500 行门禁但未超。`assembly` 模块 456 行也偏高，主要是 `_algo_dict` 和 `_build_result_summary_obj` 两个大函数。

  ### 路由层对齐
  - `scheduler_excel_batches.py` 确认路径正确消费 warning
  - `scheduler_batches.py` 的 `_bulk_update_one_batch` 正确使用 `_MISSING` 语义
  - 批次编辑路由未暴露图号修改，与 `auto_generate_ops=False` 保护一致

  ### 架构适配测试
  - `_known_oversize_files` 不包含新拆分文件（正确）
  - `LOCAL_PARSE_HELPER_ALLOWLIST` 包含 `batch_service.py:_safe_float`（正确）
- 结论:

  所有前次 review 问题均已修复到位，拆分结构方向正确，路由层与服务层对齐良好。第一轮未发现结构性阻断问题。
- 下一步建议:

  进入第二轮：逐函数检查逻辑严谨性、边界条件和潜在 BUG。

### R2_logic_boundary · 第二轮：逻辑严谨性、边界条件与潜在 BUG 审查

- 状态: 已完成
- 记录时间: 2026-04-04T18:31:56.840Z
- 摘要:

  ## 逐函数边界条件审查

  ### 1. 批次服务拆分核心路径

  **图号切换保护逻辑**：auto_generate_ops 三态（_MISSING/True/False）判定正确，公开更新接口始终传入 False 拒绝图号变更。

  **_part_updates 字段优先级**：同时传入 part_no + part_name 时显式 part_name 优先于零件默认名；仅传 part_no 时自动填充默认名。逻辑严谨。

  **_MISSING 哨兵语义**：update() 参数默认为 _MISSING，路由层只传用户实际提供的字段，正确区分"未提供"与"清空"。

  ### 2. Excel 导入双重校验链

  路由层（get_batch_row_validate_and_normalize）与服务层（build_create/update_payload）使用同源校验函数（parse_finite_int、标准化矩阵），两层规则一致无漂移。日历 Excel 预览层已补充 math.isfinite() 检查，与落库层 parse_finite_float 对齐。

  ### 3. 排产汇总核心逻辑

  超期计算使用"交期次日零点"语义，冻结窗口状态判定优先级正确（degraded > active > disabled），尺寸保护 9 级递进裁剪策略完整。

  ### 4. 潜在边界问题（均为低风险）

  - batch_copy.py:40 `int(b.quantity)` 裸转换 → 源数据经过校验，可接受
  - batch_excel_import.py:83 `or 0` 语义模糊 → normalize_int 不返回 None，建议去除
  - batch_template_ops.py:198 `int(quantity)` 冗余 → 参数已是 int，可接受
  - scheduler_batches.py:358-361 批量编辑无法清空字段 → 设计决策，建议加注释
- 结论:

  第二轮深入审查未发现阻断级或高风险 BUG。所有关键路径的校验链完整，跨层契约一致，事务边界正确。存在少量低风险的防御性冗余和语义轻度问题，但不影响正确性。
- 下一步建议:

  进入第三轮：过度兜底/静默回退检查、简洁性与优雅度评估。
- 问题:
  - [低] 其他: 批次复制中裸类型转换
  - [低] 可维护性: normalize_int 的 or 0 语义模糊
  - [低] 其他: 批量编辑路由空值语义缺文档

### R3_fallback_elegance · 第三轮：静默回退、过度兜底与简洁性审查

- 状态: 已完成
- 记录时间: 2026-04-04T18:32:48.159Z
- 摘要:

  ## 静默回退/异常吞没全面扫描

  对修改范围内所有 `except` 块逐一审查，共发现 15 处异常处理：

  | 文件 | 行号 | 模式 | 判定 |
  |------|------|------|------|
  | `batch_write_rules.py` | — | 无 except | ✅ 无回退 |
  | `batch_excel_import.py` | — | 无 except | ✅ 无回退 |
  | `batch_copy.py` | — | 无 except | ✅ 无回退 |
  | `batch_template_ops.py` | 32 | `except (TypeError, ValueError)` → re-raise | ✅ 显式传播 |
  | `batch_template_ops.py` | 102 | `except BusinessError` → re-raise | ✅ 显式传播 |
  | `batch_service.py` | 63 | `except Exception: return None` | ✅ 已注册白名单 |
  | `batch_service.py` | 99 | `except Exception` → raise ValidationError | ✅ 显式传播 |
  | `schedule_summary.py` | 75 | `except Exception: pass` | ✅ 已注册白名单（serialize_end_date） |
  | `schedule_summary.py` | 101 | `except Exception: raw_items = [value]` | ✅ 合理降级 |
  | `schedule_summary.py` | 108 | `except Exception: continue` | ✅ 跳过不可字符串化项 |
  | `schedule_summary.py` | 143 | `except Exception: return False` | ✅ 失败封闭，报告给调用方 |
  | `schedule_summary.py` | 154 | `except Exception: continue` | ✅ 跳过非整数计数 |
  | `schedule_summary_freeze.py` | 16 | `except Exception: days = 0` | ✅ 配置解析兜底 |
  | `schedule_summary_degradation.py` | 49/76/110 | `except Exception: return 0/1` | ✅ 元数据容错 |
  | `schedule_summary_assembly.py` | 155 | `except Exception:` → 计入 invalid_due_count | ✅ 非静默，有计数和警告 |

  **结论：无新增不合理的静默回退。**

  ## 过度兜底分析

  在排产汇总拆分的 3 个子模块中发现系统性的过度类型转换模式。典型例子：

  ```python
  # schedule_summary_degradation.py:281-285
  "auto_assign_enabled": bool(auto_assign_enabled),   # 已是 bool
  "downtime_load_ok": bool(downtime_load_ok),           # 已是 bool
  ...
  auto_assign_enabled=bool(auto_assign_enabled),        # 同一变量第二次 bool()
  ```

  这些 `bool()`/`int()`/`list()` 包装在值已经确定类型的情况下约有 30 处冗余。它们不影响正确性，但增加视觉噪音。

  其中 `list()` 包装有部分属于防御性浅拷贝（防止外部修改），这是合理的。但 `bool()` 和 `int()` 在已知类型上的重复包装是纯噪音。

  ## 简洁性与优雅度评估

  | 模块 | 简洁度 | 说明 |
  |------|--------|------|
  | `batch_write_rules.py` | ⭐⭐⭐⭐⭐ | 无状态纯函数，逻辑清晰 |
  | `batch_template_ops.py` | ⭐⭐⭐⭐ | 签名内省复杂但必要 |
  | `batch_excel_import.py` | ⭐⭐⭐⭐ | 闭包编排模式适配执行器框架 |
  | `batch_copy.py` | ⭐⭐⭐⭐⭐ | 直白简洁 |
  | `batch_service.py` | ⭐⭐⭐⭐ | 外观层清晰，_MISSING 导入略非常规 |
  | `schedule_summary_freeze.py` | ⭐⭐⭐⭐⭐ | 小而精 |
  | `schedule_summary_degradation.py` | ⭐⭐⭐☆ | 过度 bool()/int()，但职责清晰 |
  | `schedule_summary_assembly.py` | ⭐⭐⭐☆ | 参数爆炸（47+32），但显式优于隐式 |
  | `schedule_summary.py` | ⭐⭐⭐☆ | import as 别名、行数逼近门禁 |
  | `excel_validators.py` | ⭐⭐⭐⭐ | isfinite 修复干净利落 |

  ## import as 别名模式评估

  `schedule_summary.py` 使用 `from .assembly import _cfg_value as cfg_value` 模式将私有函数重命名为公开函数。这是可行的做法，但造成"一个函数两个名字"的认知分裂——在 assembly 模块中它是 `_cfg_value`（私有），在 summary 模块中它是 `cfg_value`（公开）。如果有人直接 `from schedule_summary_assembly import _cfg_value`，就绕过了命名约束。但在当前代码库的封闭使用场景下，这个风险可接受。
- 结论:

  第三轮审查确认：无新增静默回退，所有已注册的白名单异常处理保持不变。过度兜底模式主要集中在排产汇总模块的类型包装上（不影响正确性但增加噪音）。代码整体简洁度良好，批次服务拆分尤其出色。
- 下一步建议:

  可以安全提交。唯一建议修复的是 batch_excel_import.py:83 的 or 0（低优先级），其余均为接受风险的样式问题。
- 问题:
  - [低] 可维护性: 排产汇总类型包装噪音

## 最终结论

## 三轮深度审查最终结论

本次审查覆盖了当前工作区未提交的两大拆分修改（批次服务系列 5 个文件、排产汇总系列 4 个文件）以及校验器和路由层共 13 个核心模块。

### 达成目的评估

**批次服务拆分**：✅ 完全达成。原高耦合 `batch_service.py` 被合理拆分为 4 个职责单一的模块 + 1 个薄外观层。每个模块行数均在 250 行以内，拆分后代码的可测试性和可维护性显著提升。特别值得称赞的是 `batch_write_rules.py` 的纯函数设计和 `_MISSING` 哨兵值模式的统一使用。

**排产汇总拆分**：✅ 基本达成。3 个子模块的职责边界清晰（冻结/退化/组装），主编排文件复杂度大幅降低。但主文件仍有 482 行（逼近 500 行门禁），`_build_result_summary_obj` 的 47 个参数仍是后续优化空间。

**校验器一致性**：✅ 完全达成。前次发现的日历 Excel 非有限数字校验漂移已修复（新增 `math.isfinite()` 检查），预览→确认→落库三层规则现在一致。

**前次问题修复**：✅ 9/9 项全部修复到位，无遗漏。

### BUG 与风险评估

- **零高风险 BUG**：三轮审查未发现任何高或中风险的逻辑缺陷
- **零新增静默回退**：全部 15 处异常处理逐一审查，均为已注册白名单或合理降级
- **4 项低风险问题**：均为样式/文档层面，其中 3 项已标记为"接受风险"，1 项（`or 0` 语义模糊）建议在后续顺手修复
- **无过度兜底风险**：约 30 处冗余类型包装属于视觉噪音，不影响逻辑正确性

### 总体判断

当前修改可以安全提交。代码质量相比前两次 review 有明显提升，所有关键路径的校验链完整、跨层契约一致、事务边界正确。剩余问题全部为低优先级的代码风格改进，不阻塞交付。

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mnko2df5-y3t7tp",
  "createdAt": "2026-04-04T00:00:00.000Z",
  "updatedAt": "2026-04-04T18:32:59.252Z",
  "finalizedAt": "2026-04-04T18:32:59.252Z",
  "status": "completed",
  "overallDecision": "accepted",
  "header": {
    "title": "未提交修改三轮深度审查（第三次）",
    "date": "2026-04-04",
    "overview": "对当前工作区未提交修改（batch_service 拆分系列、schedule_summary 拆分系列、excel_validators、路由层对齐）进行三轮独立深度审查，重点检验前次问题是否修复到位、是否引入新问题。"
  },
  "scope": {
    "markdown": "# 未提交修改三轮深度审查（第三次）\n\n- 日期：2026-04-04\n- 范围：当前工作区内未提交修改，覆盖以下修改域：\n  1. **批次服务拆分**：`batch_service.py` → `batch_write_rules.py` + `batch_template_ops.py` + `batch_excel_import.py` + `batch_copy.py`\n  2. **排产汇总拆分**：`schedule_summary.py` → `schedule_summary_freeze.py` + `schedule_summary_degradation.py` + `schedule_summary_assembly.py`\n  3. **校验器一致性**：`excel_validators.py` 与服务层同源约束\n  4. **路由层对齐**：`scheduler_batches.py`、`scheduler_excel_batches.py` 等路由是否与服务层新接口对齐\n- 方法：三轮递进式审查\n  - 第一轮：前次问题修复验证、结构与接口设计、职责划分\n  - 第二轮：逻辑严谨性、边界条件、潜在 BUG\n  - 第三轮：过度兜底/静默回退检查、简洁性与优雅度"
  },
  "summary": {
    "latestConclusion": "## 三轮深度审查最终结论\n\n本次审查覆盖了当前工作区未提交的两大拆分修改（批次服务系列 5 个文件、排产汇总系列 4 个文件）以及校验器和路由层共 13 个核心模块。\n\n### 达成目的评估\n\n**批次服务拆分**：✅ 完全达成。原高耦合 `batch_service.py` 被合理拆分为 4 个职责单一的模块 + 1 个薄外观层。每个模块行数均在 250 行以内，拆分后代码的可测试性和可维护性显著提升。特别值得称赞的是 `batch_write_rules.py` 的纯函数设计和 `_MISSING` 哨兵值模式的统一使用。\n\n**排产汇总拆分**：✅ 基本达成。3 个子模块的职责边界清晰（冻结/退化/组装），主编排文件复杂度大幅降低。但主文件仍有 482 行（逼近 500 行门禁），`_build_result_summary_obj` 的 47 个参数仍是后续优化空间。\n\n**校验器一致性**：✅ 完全达成。前次发现的日历 Excel 非有限数字校验漂移已修复（新增 `math.isfinite()` 检查），预览→确认→落库三层规则现在一致。\n\n**前次问题修复**：✅ 9/9 项全部修复到位，无遗漏。\n\n### BUG 与风险评估\n\n- **零高风险 BUG**：三轮审查未发现任何高或中风险的逻辑缺陷\n- **零新增静默回退**：全部 15 处异常处理逐一审查，均为已注册白名单或合理降级\n- **4 项低风险问题**：均为样式/文档层面，其中 3 项已标记为\"接受风险\"，1 项（`or 0` 语义模糊）建议在后续顺手修复\n- **无过度兜底风险**：约 30 处冗余类型包装属于视觉噪音，不影响逻辑正确性\n\n### 总体判断\n\n当前修改可以安全提交。代码质量相比前两次 review 有明显提升，所有关键路径的校验链完整、跨层契约一致、事务边界正确。剩余问题全部为低优先级的代码风格改进，不阻塞交付。",
    "recommendedNextAction": "可以直接提交。后续可选择性修复 batch_excel_import.py:83 的 or 0 语义问题，以及在排产汇总模块中清理冗余的类型包装。",
    "reviewedModules": [
      "core/services/scheduler/batch_write_rules.py",
      "core/services/scheduler/batch_template_ops.py",
      "core/services/scheduler/batch_excel_import.py",
      "core/services/scheduler/batch_copy.py",
      "core/services/scheduler/batch_service.py",
      "core/services/scheduler/schedule_summary.py",
      "core/services/scheduler/schedule_summary_freeze.py",
      "core/services/scheduler/schedule_summary_degradation.py",
      "core/services/scheduler/schedule_summary_assembly.py",
      "core/services/common/excel_validators.py",
      "web/routes/scheduler_batches.py",
      "web/routes/scheduler_excel_batches.py",
      "tests/test_architecture_fitness.py"
    ]
  },
  "stats": {
    "totalMilestones": 3,
    "completedMilestones": 3,
    "totalFindings": 4,
    "severity": {
      "high": 0,
      "medium": 0,
      "low": 4
    }
  },
  "milestones": [
    {
      "id": "R1_structure_fixverify",
      "title": "第一轮：前次问题修复验证与结构设计审查",
      "status": "completed",
      "recordedAt": "2026-04-04T18:30:15.998Z",
      "summaryMarkdown": "## 前次问题修复验证（9/9 项全部已修复）\n\n逐项对照前两次 review（共 12 项 finding），确认当前代码的修复状态：\n\n| # | 原始问题 | 状态 | 修复位置 |\n|---|---------|------|---------|\n| ❶ | `int()` 裸转换 | ✅ 已修复 | `batch_excel_import.py:83` 改为 `svc._normalize_int(...)` |\n| ❷ | `allow_none` 形同虚设 | ✅ 已修复 | `batch_write_rules.py:26` 移除 `allow_none` 参数 |\n| ❸ | `_extract_freeze_warnings` 参数名误导 | ✅ 已修复 | `schedule_summary_freeze.py:61` 参数名改为 `all_warnings` |\n| ❹ | `update()` 无法清空字段 | ✅ 已修复 | `batch_service.py:204-212` 默认值改为 `_MISSING` |\n| ❺ | 图号切换保护未统一 | ✅ 已修复 | `batch_service.py:222` 传入 `auto_generate_ops=False` |\n| ❻ | Excel 模板补建 warning 丢失 | ✅ 已修复 | `scheduler_excel_batches.py:386-388` 新增 `consume_user_visible_warnings` |\n| ❼ | 日历 Excel 非有限数字校验漂移 | ✅ 已修复 | `excel_validators.py:197-198,222-223` 新增 `math.isfinite(v)` 检查 |\n| ❽ | `__all__` 导出下划线函数 | ✅ 已修复 | `schedule_summary.py:47-59` 改为公开名称，用 `import as` 别名 |\n| ❾ | 日期校验双轨无文档说明 | ✅ 已修复 | `batch_service.py:68-73` 新增设计决策注释 |\n\n## 拆分结构评估\n\n### 批次服务拆分\n- `batch_write_rules.py`（159行）：纯校验与载荷构建，零状态\n- `batch_template_ops.py`（229行）：模板工序编排，职责清晰\n- `batch_excel_import.py`（108行）：Excel 导入编排，轻量\n- `batch_copy.py`（79行）：复制逻辑独立\n- `batch_service.py`（393行）：外观层 + 工具方法\n\n拆分比例健康：原大文件职责分散到 4 个功能模块，主文件保留外观层角色。\n\n### 排产汇总拆分\n- `schedule_summary_freeze.py`（93行）：冻结窗口判定\n- `schedule_summary_degradation.py`（322行）：退化状态采集\n- `schedule_summary_assembly.py`（456行）：结果组装\n- `schedule_summary.py`（482行）：主编排\n\n入口文件 482 行逼近 500 行门禁但未超。`assembly` 模块 456 行也偏高，主要是 `_algo_dict` 和 `_build_result_summary_obj` 两个大函数。\n\n### 路由层对齐\n- `scheduler_excel_batches.py` 确认路径正确消费 warning\n- `scheduler_batches.py` 的 `_bulk_update_one_batch` 正确使用 `_MISSING` 语义\n- 批次编辑路由未暴露图号修改，与 `auto_generate_ops=False` 保护一致\n\n### 架构适配测试\n- `_known_oversize_files` 不包含新拆分文件（正确）\n- `LOCAL_PARSE_HELPER_ALLOWLIST` 包含 `batch_service.py:_safe_float`（正确）",
      "conclusionMarkdown": "所有前次 review 问题均已修复到位，拆分结构方向正确，路由层与服务层对齐良好。第一轮未发现结构性阻断问题。",
      "evidence": [],
      "reviewedModules": [
        "core/services/scheduler/batch_write_rules.py",
        "core/services/scheduler/batch_template_ops.py",
        "core/services/scheduler/batch_excel_import.py",
        "core/services/scheduler/batch_copy.py",
        "core/services/scheduler/batch_service.py",
        "core/services/scheduler/schedule_summary.py",
        "core/services/scheduler/schedule_summary_freeze.py",
        "core/services/scheduler/schedule_summary_degradation.py",
        "core/services/scheduler/schedule_summary_assembly.py",
        "core/services/common/excel_validators.py",
        "web/routes/scheduler_batches.py",
        "web/routes/scheduler_excel_batches.py",
        "tests/test_architecture_fitness.py"
      ],
      "recommendedNextAction": "进入第二轮：逐函数检查逻辑严谨性、边界条件和潜在 BUG。",
      "findingIds": []
    },
    {
      "id": "R2_logic_boundary",
      "title": "第二轮：逻辑严谨性、边界条件与潜在 BUG 审查",
      "status": "completed",
      "recordedAt": "2026-04-04T18:31:56.840Z",
      "summaryMarkdown": "## 逐函数边界条件审查\n\n### 1. 批次服务拆分核心路径\n\n**图号切换保护逻辑**：auto_generate_ops 三态（_MISSING/True/False）判定正确，公开更新接口始终传入 False 拒绝图号变更。\n\n**_part_updates 字段优先级**：同时传入 part_no + part_name 时显式 part_name 优先于零件默认名；仅传 part_no 时自动填充默认名。逻辑严谨。\n\n**_MISSING 哨兵语义**：update() 参数默认为 _MISSING，路由层只传用户实际提供的字段，正确区分\"未提供\"与\"清空\"。\n\n### 2. Excel 导入双重校验链\n\n路由层（get_batch_row_validate_and_normalize）与服务层（build_create/update_payload）使用同源校验函数（parse_finite_int、标准化矩阵），两层规则一致无漂移。日历 Excel 预览层已补充 math.isfinite() 检查，与落库层 parse_finite_float 对齐。\n\n### 3. 排产汇总核心逻辑\n\n超期计算使用\"交期次日零点\"语义，冻结窗口状态判定优先级正确（degraded > active > disabled），尺寸保护 9 级递进裁剪策略完整。\n\n### 4. 潜在边界问题（均为低风险）\n\n- batch_copy.py:40 `int(b.quantity)` 裸转换 → 源数据经过校验，可接受\n- batch_excel_import.py:83 `or 0` 语义模糊 → normalize_int 不返回 None，建议去除\n- batch_template_ops.py:198 `int(quantity)` 冗余 → 参数已是 int，可接受\n- scheduler_batches.py:358-361 批量编辑无法清空字段 → 设计决策，建议加注释",
      "conclusionMarkdown": "第二轮深入审查未发现阻断级或高风险 BUG。所有关键路径的校验链完整，跨层契约一致，事务边界正确。存在少量低风险的防御性冗余和语义轻度问题，但不影响正确性。",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": "进入第三轮：过度兜底/静默回退检查、简洁性与优雅度评估。",
      "findingIds": [
        "F-批次复制中裸类型转换",
        "F-normalize-int-的-or-0-语义模糊",
        "F-批量编辑路由空值语义缺文档"
      ]
    },
    {
      "id": "R3_fallback_elegance",
      "title": "第三轮：静默回退、过度兜底与简洁性审查",
      "status": "completed",
      "recordedAt": "2026-04-04T18:32:48.159Z",
      "summaryMarkdown": "## 静默回退/异常吞没全面扫描\n\n对修改范围内所有 `except` 块逐一审查，共发现 15 处异常处理：\n\n| 文件 | 行号 | 模式 | 判定 |\n|------|------|------|------|\n| `batch_write_rules.py` | — | 无 except | ✅ 无回退 |\n| `batch_excel_import.py` | — | 无 except | ✅ 无回退 |\n| `batch_copy.py` | — | 无 except | ✅ 无回退 |\n| `batch_template_ops.py` | 32 | `except (TypeError, ValueError)` → re-raise | ✅ 显式传播 |\n| `batch_template_ops.py` | 102 | `except BusinessError` → re-raise | ✅ 显式传播 |\n| `batch_service.py` | 63 | `except Exception: return None` | ✅ 已注册白名单 |\n| `batch_service.py` | 99 | `except Exception` → raise ValidationError | ✅ 显式传播 |\n| `schedule_summary.py` | 75 | `except Exception: pass` | ✅ 已注册白名单（serialize_end_date） |\n| `schedule_summary.py` | 101 | `except Exception: raw_items = [value]` | ✅ 合理降级 |\n| `schedule_summary.py` | 108 | `except Exception: continue` | ✅ 跳过不可字符串化项 |\n| `schedule_summary.py` | 143 | `except Exception: return False` | ✅ 失败封闭，报告给调用方 |\n| `schedule_summary.py` | 154 | `except Exception: continue` | ✅ 跳过非整数计数 |\n| `schedule_summary_freeze.py` | 16 | `except Exception: days = 0` | ✅ 配置解析兜底 |\n| `schedule_summary_degradation.py` | 49/76/110 | `except Exception: return 0/1` | ✅ 元数据容错 |\n| `schedule_summary_assembly.py` | 155 | `except Exception:` → 计入 invalid_due_count | ✅ 非静默，有计数和警告 |\n\n**结论：无新增不合理的静默回退。**\n\n## 过度兜底分析\n\n在排产汇总拆分的 3 个子模块中发现系统性的过度类型转换模式。典型例子：\n\n```python\n# schedule_summary_degradation.py:281-285\n\"auto_assign_enabled\": bool(auto_assign_enabled),   # 已是 bool\n\"downtime_load_ok\": bool(downtime_load_ok),           # 已是 bool\n...\nauto_assign_enabled=bool(auto_assign_enabled),        # 同一变量第二次 bool()\n```\n\n这些 `bool()`/`int()`/`list()` 包装在值已经确定类型的情况下约有 30 处冗余。它们不影响正确性，但增加视觉噪音。\n\n其中 `list()` 包装有部分属于防御性浅拷贝（防止外部修改），这是合理的。但 `bool()` 和 `int()` 在已知类型上的重复包装是纯噪音。\n\n## 简洁性与优雅度评估\n\n| 模块 | 简洁度 | 说明 |\n|------|--------|------|\n| `batch_write_rules.py` | ⭐⭐⭐⭐⭐ | 无状态纯函数，逻辑清晰 |\n| `batch_template_ops.py` | ⭐⭐⭐⭐ | 签名内省复杂但必要 |\n| `batch_excel_import.py` | ⭐⭐⭐⭐ | 闭包编排模式适配执行器框架 |\n| `batch_copy.py` | ⭐⭐⭐⭐⭐ | 直白简洁 |\n| `batch_service.py` | ⭐⭐⭐⭐ | 外观层清晰，_MISSING 导入略非常规 |\n| `schedule_summary_freeze.py` | ⭐⭐⭐⭐⭐ | 小而精 |\n| `schedule_summary_degradation.py` | ⭐⭐⭐☆ | 过度 bool()/int()，但职责清晰 |\n| `schedule_summary_assembly.py` | ⭐⭐⭐☆ | 参数爆炸（47+32），但显式优于隐式 |\n| `schedule_summary.py` | ⭐⭐⭐☆ | import as 别名、行数逼近门禁 |\n| `excel_validators.py` | ⭐⭐⭐⭐ | isfinite 修复干净利落 |\n\n## import as 别名模式评估\n\n`schedule_summary.py` 使用 `from .assembly import _cfg_value as cfg_value` 模式将私有函数重命名为公开函数。这是可行的做法，但造成\"一个函数两个名字\"的认知分裂——在 assembly 模块中它是 `_cfg_value`（私有），在 summary 模块中它是 `cfg_value`（公开）。如果有人直接 `from schedule_summary_assembly import _cfg_value`，就绕过了命名约束。但在当前代码库的封闭使用场景下，这个风险可接受。",
      "conclusionMarkdown": "第三轮审查确认：无新增静默回退，所有已注册的白名单异常处理保持不变。过度兜底模式主要集中在排产汇总模块的类型包装上（不影响正确性但增加噪音）。代码整体简洁度良好，批次服务拆分尤其出色。",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": "可以安全提交。唯一建议修复的是 batch_excel_import.py:83 的 or 0（低优先级），其余均为接受风险的样式问题。",
      "findingIds": [
        "F-排产汇总类型包装噪音"
      ]
    }
  ],
  "findings": [
    {
      "id": "F-批次复制中裸类型转换",
      "severity": "low",
      "category": "other",
      "title": "批次复制中裸类型转换",
      "descriptionMarkdown": "batch_copy.py 第40行对 b.quantity 直接使用 int() 转换，如果数据库中存在脏数据会产生非业务友好异常。但所有创建路径都经过校验，实际触发概率极低。",
      "recommendationMarkdown": "可保持现状。如追求一致性可用 parse_finite_int，但属于低收益改动。",
      "evidence": [
        {
          "path": "core/services/scheduler/batch_copy.py",
          "lineStart": 40,
          "lineEnd": 40
        },
        {
          "path": "core/services/scheduler/batch_copy.py"
        }
      ],
      "relatedMilestoneIds": [
        "R2_logic_boundary"
      ],
      "trackingStatus": "accepted_risk"
    },
    {
      "id": "F-normalize-int-的-or-0-语义模糊",
      "severity": "low",
      "category": "maintainability",
      "title": "normalize_int 的 or 0 语义模糊",
      "descriptionMarkdown": "batch_excel_import.py 第83行 svc._normalize_int(...) or 0，由于 _normalize_int 在 allow_none=False 时应抛异常而非返回 None，or 0 永远不应被触发。给读者传递了错误暗示。",
      "recommendationMarkdown": "去掉 or 0 或加注释说明纯防御意图。",
      "evidence": [
        {
          "path": "core/services/scheduler/batch_excel_import.py",
          "lineStart": 83,
          "lineEnd": 83
        },
        {
          "path": "core/services/scheduler/batch_excel_import.py"
        }
      ],
      "relatedMilestoneIds": [
        "R2_logic_boundary"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-批量编辑路由空值语义缺文档",
      "severity": "low",
      "category": "other",
      "title": "批量编辑路由空值语义缺文档",
      "descriptionMarkdown": "scheduler_batches.py 第358-361行将空字符串统一转为 None 视为'不更新'，用户无法通过批量编辑清空交期或备注。这是合理的设计决策但缺少注释说明。",
      "recommendationMarkdown": "在路由代码中加注释说明此设计意图。",
      "evidence": [
        {
          "path": "web/routes/scheduler_batches.py",
          "lineStart": 358,
          "lineEnd": 361
        },
        {
          "path": "web/routes/scheduler_batches.py"
        }
      ],
      "relatedMilestoneIds": [
        "R2_logic_boundary"
      ],
      "trackingStatus": "accepted_risk"
    },
    {
      "id": "F-排产汇总类型包装噪音",
      "severity": "low",
      "category": "maintainability",
      "title": "排产汇总类型包装噪音",
      "descriptionMarkdown": "schedule_summary_degradation.py 的 _compute_downtime_degradation（267-301行）对已经确定类型的变量反复用 bool()/int() 包装。例如 auto_assign_enabled 在第268行已经是 bool，但在第281/285行又分别 bool() 了两次；load_partial_fail_count 在第272行通过 _meta_int 已经是 int，但第289/297行又 int() 了两次。同样的模式也出现在 schedule_summary.py 的 _build_runtime_state 和 _build_fallback_state 中。这些转换全部冗余，增加了约 30 处无效的类型噪音，但不影响正确性。",
      "recommendationMarkdown": "在后续清理轮次中统一去除已确定类型的冗余 bool()/int()/list() 包装，保留跨模块边界入口的防御。",
      "evidence": [
        {
          "path": "core/services/scheduler/schedule_summary_degradation.py",
          "lineStart": 280,
          "lineEnd": 301
        },
        {
          "path": "core/services/scheduler/schedule_summary.py",
          "lineStart": 272,
          "lineEnd": 278
        },
        {
          "path": "core/services/scheduler/schedule_summary.py",
          "lineStart": 337,
          "lineEnd": 343
        },
        {
          "path": "core/services/scheduler/schedule_summary_degradation.py"
        },
        {
          "path": "core/services/scheduler/schedule_summary.py"
        }
      ],
      "relatedMilestoneIds": [
        "R3_fallback_elegance"
      ],
      "trackingStatus": "accepted_risk"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:cf9925bc59d61aba0e65a0528b95bc6568e9b4611e929d63bd4e5d0558d57fc7",
    "generatedAt": "2026-04-04T18:32:59.252Z",
    "locale": "zh-CN"
  }
}
```
