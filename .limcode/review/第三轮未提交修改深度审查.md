# 第三轮未提交修改深度审查
- 日期: 2026-04-05
- 概述: 第三轮深度审查：逐一验证前两轮发现的5个问题修复实现，深入引用链追踪，检查逻辑严谨性与边界正确性
- 状态: 已完成
- 总体结论: 有条件通过

## 评审范围

# 第三轮未提交修改深度审查

**日期**: 2026-04-05

## 审查范围

在前两轮审查（共发现 5 个去重后的问题）和修复 plan 全部标记完成后，本轮按以下方法做终局验证：

1. **修复验证**：逐一读取修改文件，检查每个修复是否正确实现、是否引入新问题
2. **引用链追踪**：深入上下游调用者/被调用者，确认修复不破坏既有行为
3. **边界审查**：针对每个修复点构造边界用例做心理推演，检查是否仍有遗漏
4. **测试覆盖**：审查新增/修改的测试是否真正覆盖了修复的核心分支

### 问题-任务对应表

| # | 问题 | 修复任务 |
|---|------|----------|
| 1 | SGS 探测式自动分配污染统计计数 | auto_assign 增加 probe_only |
| 2 | 端点扫描回归把 safe_url_for 当强制 url_for | 正则增加负向后顾 |
| 3 | Excel 路由数值校验与服务契约漂移 | 日历/工时/供应商三处统一严格解析 |
| 4 | 静默吞异常护栏退化为按文件计数 | 升级为 file:func 指纹 |
| 5 | excel_validators.py 冗余 float() 和多余空行 | 代码风格清理 |

## 评审摘要

- 当前状态: 已完成
- 已审模块: core/algorithms/greedy/auto_assign.py, core/algorithms/greedy/scheduler.py, core/algorithms/greedy/dispatch/sgs.py, core/algorithms/greedy/algo_stats.py, tests/regression_sgs_scoring_fallback_unscorable.py, tests/regression_greedy_scheduler_algo_stats_auto_assign.py, tests/regression_template_urlfor_endpoints.py, core/services/common/excel_validators.py, web/routes/scheduler_excel_calendar.py, web/routes/process_excel_part_operation_hours.py, web/routes/process_excel_suppliers.py, core/services/common/strict_parse.py, core/services/common/number_utils.py, core/services/scheduler/number_utils.py, core/infrastructure/errors.py, tests/regression_excel_route_strict_numeric.py, tests/test_architecture_fitness.py
- 当前进度: 已记录 2 个里程碑；最新：M2
- 里程碑总数: 2
- 已完成里程碑: 2
- 问题总数: 1
- 问题严重级别分布: 高 0 / 中 0 / 低 1
- 最新结论: ## 第三轮审查最终结论 ### 总体判定：通过（有条件） 经过对 17 个核心模块的逐文件审查、完整引用链追踪和边界用例推演，**前两轮发现的全部 5 个问题（去重后）均已正确修复**，修复实现整体简洁优雅，未引入新的 BUG、静默回退或过度兜底。 ### 5 项修复验证结果 | 任务 | 问题 | 实现 | 测试 | 判定 | |------|------|------|------|------| | 1 | SGS 探测计数污染 | ✅ 闭包切换，一行解决 8 处调用 | ⚠️ 假覆盖（测试用例未触达目标路径） | 实现正确，建议补测试 | | 2 | 端点扫描误报 | ✅ 负向后顾精确排除 | ✅ 契约与实现一致 | 完全通过 | | 3 | Excel 路由数值漂移 | ✅ 三处统一复用服务层严格解析 | ✅ 覆盖布尔/NaN/Inf/科学计数法 | 完全通过 | | 4 | 静默吞异常护栏退化 | ✅ 升级为 file:func 指纹+计数 | ✅ 检测能力显著提升 | 完全通过 | | 5 | 冗余 float() 和空行 | ✅ 已清理 | ✅ 返回类型链路验证无误 | 完全通过 | ### 核心事实 - **无新 BUG**：从数值解析（`strict_parse._parse_finite_float`）到路由预览/确认（`scheduler_excel_calendar`、`process_excel_part_operation_hours`、`process_excel_suppliers`）到算法评分（`dispatch_sgs`）到结果持久化的完整链路均经过边界推演验证 - **无静默回退**：`except Exception` 全部改为 `except ValidationError`（或 `_count` 闭包消除计数副作用），非法输入显式拒绝并给出有意义错误信息 - **无过度兜底**：`parse_finite_float`/`parse_required_float` 是单一严格解析入口，路由层不再手写重复版本 - **设计简洁**：`probe_only` 用一行闭包替代 8 个 `if`；负向后顾一行正则替代复杂字符串操作；`Counter` + 函数指纹替代行号白名单 ### 唯一开放问题 | 级别 | 问题 | 影响 | 建议 | |------|------|------|------| | 低 | SGS 探测计数回归测试假覆盖 | 不影响功能正确性，但回归安全网有盲区 | 提交后补充 SGS+内部工序+自动分配的测试用例 | ### 与前两轮审查的对照 前两轮共识别 6 个问题（去重后 5 个）： - 4 个中等 → 全部修复并通过验证 ✅ - 1 个低等 → 修复并通过验证 ✅ - 本轮新发现 1 个低等（假覆盖）→ 不阻塞提交
- 下一步建议: 当前代码可以提交。提交后建议补充一个使用 dispatch_mode='sgs' + source='internal' + auto_assign_enabled=True + resource_pool 非空的回归测试，消除 probe_only 修复的假覆盖盲区。
- 总体结论: 有条件通过

## 评审发现

### SGS 探测计数修复的回归测试假覆盖

- ID: probe-only-false-coverage
- 严重级别: 低
- 分类: 测试
- 跟踪状态: 开放
- 相关里程碑: M1
- 说明:

  regression_sgs_scoring_fallback_unscorable.py 第 140-152 行新增了 probe_keys 断言，但测试中所有工序均为外协（source=external），SGS 评分阶段不会走到内部工序的 auto_assign 分支，6 个 probe_keys 断言全部以零值平凡通过。即使删掉 probe_only 修复，断言仍不会失败。需要一个使用 SGS 模式 + 内部工序 + 自动分配 + resource_pool 的测试用例才能真正覆盖 probe_only 路径。
- 建议:

  补充一个使用 dispatch_mode='sgs'、source='internal'、auto_assign_enabled=True、resource_pool 非空的测试用例，验证评分阶段的探测调用不污染 fallback_counts。
- 证据:
  - `tests/regression_sgs_scoring_fallback_unscorable.py:140-152`
  - `core/algorithms/greedy/dispatch/sgs.py:228-263`
  - `tests/regression_sgs_scoring_fallback_unscorable.py`
  - `core/algorithms/greedy/dispatch/sgs.py`

## 评审里程碑

### M1 · 算法层与测试工具修复验证（任务1/2/5）

- 状态: 已完成
- 记录时间: 2026-04-05T05:40:45.970Z
- 已审模块: core/algorithms/greedy/auto_assign.py, core/algorithms/greedy/scheduler.py, core/algorithms/greedy/dispatch/sgs.py, core/algorithms/greedy/algo_stats.py, tests/regression_sgs_scoring_fallback_unscorable.py, tests/regression_greedy_scheduler_algo_stats_auto_assign.py, tests/regression_template_urlfor_endpoints.py, core/services/common/excel_validators.py
- 摘要:

  ## 任务 1：SGS 探测式自动分配计数污染修复

  ### 实现正确性 ✅

  `auto_assign.py` 第 26 行新增 `probe_only: bool = False` 参数，第 37 行用闭包 `_count = increment_counter if not probe_only else (lambda *_args, **_kwargs: None)` 一行实现全部 8 处计数调用的有条件切换。方案简洁优雅，无重复代码，无多余分支。

  ### 引用链追踪 ✅

  - `scheduler.py` 第 579 行新增 `probe_only: bool = False`，第 595 行透传 `probe_only=probe_only` — 正确
  - `sgs.py` 第 249 行：评分阶段调用 `chooser(... probe_only=True ...)` — 正确
  - `scheduler.py` 第 441-454 行：正式排产调用 **不传** `probe_only`，默认 `False` — 记数行为保持不变 ✅
  - `algo_stats.py::increment_counter` 直接修改 `scheduler._algo_stats["fallback_counts"]`，当 `probe_only=True` 时 `_count` 为空操作，不会修改任何外部状态 ✅

  ### 副作用分析 ✅

  `auto_assign_internal_resources` 中除了 `increment_counter` 的 8 处调用外，所有其他变量（`machine_candidates`、`op_candidates`、`best`、`best_pair` 等）都是局部变量，不会泄漏到调用方。因此 `probe_only=True` 时函数的唯一外部副作用（统计计数）被完全消除，排产结果（返回值 `best_pair`）不受影响。

  ### 测试覆盖 ⚠️

  `regression_sgs_scoring_fallback_unscorable.py` 第 140-152 行新增了对 `probe_keys` 的断言，但该测试的两个工序都是**外协**（`source="external"`），SGS 评分阶段根本不会走到内部工序的 `auto_assign` 分支（`sgs.py` 第 228-263 行），因此这 6 个 probe_keys 断言**全部以零值通过**——即使删掉 `probe_only` 修复，断言仍会通过。

  `regression_greedy_scheduler_algo_stats_auto_assign.py` 使用 `dispatch_mode="batch_order"`（非 SGS），所以也不会触达 SGS 评分中的 probe_only 路径。

  **结论**：当前没有任何测试真正覆盖 "SGS + 内部工序 + 自动分配 + probe_only=True" 这条路径。实现本身显然正确（一行闭包切换），但回归测试提供的是**假覆盖**。

  ---

  ## 任务 2：端点扫描回归正则修正 ✅

  `regression_template_urlfor_endpoints.py` 第 60 行：`pat_url_for = re.compile(r"(?<!safe_)url_for\(\s*['\"]([^'\"]+)['\"]")`

  负向后顾 `(?<!safe_)` 精确排除 `safe_url_for(` 中的子串匹配，不影响独立 `url_for(`、`{{ url_for(` 等合法模式。第 61 行的 `pat_safe` 仍独立提取 `safe_url_for`。两套集合完全分离，测试契约与实现描述一致。

  ---

  ## 任务 5：excel_validators.py 代码风格清理 ✅

  - 第 194 行：`parse_finite_float(sh, field="可用工时", allow_none=False)` — `float()` 包装已去除
  - 第 216 行：`parse_finite_float(eff, field="效率", allow_none=False)` — `float()` 包装已去除
  - 追踪返回类型：`parse_finite_float(allow_none=False)` → `parse_required_float()` → `_parse_finite_float()` 返回 `float(parsed)`，类型确认为 `float`，外层包装确实冗余
  - 第 200 行和第 222 行的连续双空行已修复为单空行 ✅
- 结论:

  任务 1 实现正确优雅，但回归测试存在假覆盖；任务 2 和任务 5 修复完整，无遗留问题。
- 证据:
  - `core/algorithms/greedy/auto_assign.py:26-37#probe_only + _count closure`
  - `core/algorithms/greedy/scheduler.py:564-596#_auto_assign_internal_resources`
  - `core/algorithms/greedy/dispatch/sgs.py:236-249#probe_only=True`
  - `core/algorithms/greedy/scheduler.py:439-454#_schedule_internal 正式调用（不传 probe_only）`
  - `tests/regression_sgs_scoring_fallback_unscorable.py:140-152#probe_keys 假覆盖断言`
  - `tests/regression_template_urlfor_endpoints.py:60-61#(?<!safe_) 负向后顾`
  - `core/services/common/excel_validators.py:194-221#float() 包装已去除、空行已修复`
- 问题:
  - [低] 测试: SGS 探测计数修复的回归测试假覆盖

### M2 · Excel 路由契约对齐与测试护栏升级验证（任务3/4）

- 状态: 已完成
- 记录时间: 2026-04-05T05:41:45.520Z
- 已审模块: web/routes/scheduler_excel_calendar.py, web/routes/process_excel_part_operation_hours.py, web/routes/process_excel_suppliers.py, core/services/common/strict_parse.py, core/services/common/number_utils.py, core/services/scheduler/number_utils.py, core/infrastructure/errors.py, tests/regression_excel_route_strict_numeric.py, tests/test_architecture_fitness.py
- 摘要:

  ## 任务 3：Excel 路由数值校验漂移修复

  ### 日历路由（scheduler_excel_calendar.py）✅

  两处 `validate_row`（预览第 197-221 行 / 确认第 331-354 行）均已从原始 `float()` 改为 `parse_finite_float(sh, field="可用工时")` 和 `parse_finite_float(eff, field="效率")`，`except Exception` 改为 `except ValidationError as e: return e.message`。

  **完整调用链验证**：
  ```
  parse_finite_float(value, field=...) [number_utils.py]
    → parse_required_float(value, field=...) [strict_parse.py]
      → _parse_finite_float(value, field=...) [strict_parse.py]
        → isinstance(value, bool) → 拒绝 True/False
        → float(value) → 解析为浮点
        → math.isfinite(parsed) → 拒绝 NaN/Inf
  ```

  **边界推演**：
  | 输入 | 行为 | 正确性 |
  |------|------|--------|
  | `True` | `isinstance(True, bool)` → 拒绝 | ✅ 与服务层一致 |
  | `float("nan")` | `math.isfinite = False` → 拒绝 | ✅ |
  | `float("inf")` | `math.isfinite = False` → 拒绝 | ✅ |
  | `"NaN"` | `float("NaN") = nan` → 拒绝 | ✅ |
  | `0` | 解析为 `0.0`，由路由自行检查 `< 0` | ✅ |
  | `"5e0"` | `float("5e0") = 5.0`，有限数 → 接受 | ✅（科学计数法用于工时合法） |

  文件头部第 18 行已导入 `from core.services.common.number_utils import parse_finite_float`。✅

  ### 零件工时路由（process_excel_part_operation_hours.py）✅

  `_parse_seq` 第 47 行增加 `isinstance(value, bool)` 拦截，第 58 行增加 `"e" in s.lower()` 拦截。

  **边界推演**：
  | 输入 | 行为 | 正确性 |
  |------|------|--------|
  | `True` | `isinstance(True, bool)` → `None` | ✅ 与 `_coerce_int` 一致 |
  | `"5e0"` | `"e" in "5e0"` → `None` | ✅ 与 `_coerce_int` 一致 |
  | `5` | 非 `bool`，是 `int` → `int(5) = 5` | ✅ |
  | `3.0` | `is_integer()` → `int(3) = 3` | ✅ |
  | `"10"` | 不含 `e`，匹配 `\d+` → `int(10)` | ✅ |

  `_validate_row` 第 133-136 行用 `parse_finite_float(..., allow_none=True)` 校验工时，与服务层的 `allow_none` 语义一致。✅

  ### 供应商路由（process_excel_suppliers.py）✅

  `_normalize_supplier_default_days` 第 94-97 行改用 `parse_required_float(raw_value, field="默认周期", min_value=0, min_inclusive=False)`。

  **边界推演**：
  | 输入 | 行为 | 正确性 |
  |------|------|--------|
  | `True` | `_parse_finite_float → isinstance(True, bool)` → 拒绝 | ✅ |
  | `0` | `_parse_finite_float` 通过 → `_ensure_min_float(0.0, min=0, inclusive=False)` → `0.0 > 0.0 = False` → 拒绝 | ✅ |
  | `False` | `isinstance(False, bool)` → 拒绝 | ✅ |
  | `float("nan")` | `math.isfinite = False` → 拒绝 | ✅ |
  | `5.0` | 通过全部校验 → `row["默认周期"] = 5.0` | ✅ |

  文件头部第 18 行已导入 `from core.services.common.strict_parse import parse_required_float`。✅

  ### 回归测试覆盖（regression_excel_route_strict_numeric.py）✅

  - `test_parse_seq_rejects_bool_and_scientific`：覆盖 `True/False/"5e0"/"1E2"` 被拒绝，`5/"10"/3.0` 被接受
  - `test_supplier_default_days_rejects_invalid_numeric`：覆盖 `True/False/NaN/Inf` 被拒绝，`5.0` 被接受
  - 日历路由的 `validate_row` 是闭包无法直接导入测试，但其使用的 `parse_finite_float` 与 `excel_validators.py` 完全相同，已有 `regression_calendar_no_tx_hardening.py` 端到端覆盖

  ---

  ## 任务 4：静默吞异常护栏升级

  ### 指纹方案 ✅

  `_find_enclosing_name` 第 392-401 行：沿 `_ast_parent` 链上溯，找到最近的 `FunctionDef`/`AsyncFunctionDef`/`ClassDef`，返回其 `name`；无封闭定义则返回 `<module>`。

  `_scan_file` 第 453-481 行：先构建 `_ast_parent` 引用，再扫描 `except Exception:` + `pass/...`，输出 `f"{fp}:{_find_enclosing_name(h)}"`。

  ### 白名单格式升级 ✅

  第 408-451 行：`known_violations` 从旧的 `file:line → Set` 升级为 `Counter({"file:func": count})`。每个条目明确记录函数名和该函数内的命中次数。

  ### 比较逻辑 ✅

  第 483-497 行：
  ```python
  current_violations: Counter = Counter()
  ...
  for hit, count in sorted(current_violations.items()):
      allowed = int(known_violations.get(hit, 0))
      if count > allowed:
          new_violations.append(...)
  ```

  相比旧的文件级计数，新方案的检测能力提升：
  - **跨函数替换可检测**：在同文件内将 `func_A` 的吞异常删除、在 `func_B` 新增 → 指纹变化，被检出
  - **行号漂移免疫**：函数内增删代码不改变函数名 → 指纹稳定
  - **同函数增减可检测**：`Counter` 按 `(file, func)` 统计次数，增加一处 → 超出白名单 → 被检出

  **微弱残余风险**：同函数内删一处旧的、加一处新的 → 次数不变 → 不被检出。但此场景在实际开发中发生概率极低，且已比文件级计数严格得多。可接受。
- 结论:

  任务 3 的三处路由修复均与服务层契约完全对齐，边界用例推演全部通过；任务 4 的指纹方案显著优于旧的文件级计数，实际检测能力大幅提升。两项修复均无新引入的 BUG 或过度兜底。
- 证据:
  - `web/routes/scheduler_excel_calendar.py:197-221#预览 validate_row 改用 parse_finite_float`
  - `web/routes/scheduler_excel_calendar.py:331-354#确认 validate_row 改用 parse_finite_float`
  - `web/routes/process_excel_part_operation_hours.py:46-68#_parse_seq 布尔拒绝+科学计数法拒绝`
  - `web/routes/process_excel_suppliers.py:90-100#_normalize_supplier_default_days 改用 parse_required_float`
  - `core/services/common/strict_parse.py:32-41#_parse_finite_float 布尔拒绝+非有限拒绝`
  - `core/services/common/strict_parse.py:59-64#parse_required_float 链路`
  - `tests/regression_excel_route_strict_numeric.py:10-40#回归测试覆盖`
  - `tests/test_architecture_fitness.py:404-497#test_no_silent_exception_swallow 指纹升级`

## 最终结论

## 第三轮审查最终结论

### 总体判定：通过（有条件）

经过对 17 个核心模块的逐文件审查、完整引用链追踪和边界用例推演，**前两轮发现的全部 5 个问题（去重后）均已正确修复**，修复实现整体简洁优雅，未引入新的 BUG、静默回退或过度兜底。

### 5 项修复验证结果

| 任务 | 问题 | 实现 | 测试 | 判定 |
|------|------|------|------|------|
| 1 | SGS 探测计数污染 | ✅ 闭包切换，一行解决 8 处调用 | ⚠️ 假覆盖（测试用例未触达目标路径） | 实现正确，建议补测试 |
| 2 | 端点扫描误报 | ✅ 负向后顾精确排除 | ✅ 契约与实现一致 | 完全通过 |
| 3 | Excel 路由数值漂移 | ✅ 三处统一复用服务层严格解析 | ✅ 覆盖布尔/NaN/Inf/科学计数法 | 完全通过 |
| 4 | 静默吞异常护栏退化 | ✅ 升级为 file:func 指纹+计数 | ✅ 检测能力显著提升 | 完全通过 |
| 5 | 冗余 float() 和空行 | ✅ 已清理 | ✅ 返回类型链路验证无误 | 完全通过 |

### 核心事实

- **无新 BUG**：从数值解析（`strict_parse._parse_finite_float`）到路由预览/确认（`scheduler_excel_calendar`、`process_excel_part_operation_hours`、`process_excel_suppliers`）到算法评分（`dispatch_sgs`）到结果持久化的完整链路均经过边界推演验证
- **无静默回退**：`except Exception` 全部改为 `except ValidationError`（或 `_count` 闭包消除计数副作用），非法输入显式拒绝并给出有意义错误信息
- **无过度兜底**：`parse_finite_float`/`parse_required_float` 是单一严格解析入口，路由层不再手写重复版本
- **设计简洁**：`probe_only` 用一行闭包替代 8 个 `if`；负向后顾一行正则替代复杂字符串操作；`Counter` + 函数指纹替代行号白名单

### 唯一开放问题

| 级别 | 问题 | 影响 | 建议 |
|------|------|------|------|
| 低 | SGS 探测计数回归测试假覆盖 | 不影响功能正确性，但回归安全网有盲区 | 提交后补充 SGS+内部工序+自动分配的测试用例 |

### 与前两轮审查的对照

前两轮共识别 6 个问题（去重后 5 个）：
- 4 个中等 → 全部修复并通过验证 ✅
- 1 个低等 → 修复并通过验证 ✅
- 本轮新发现 1 个低等（假覆盖）→ 不阻塞提交

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mnlbyx56-qbx15h",
  "createdAt": "2026-04-05T00:00:00.000Z",
  "updatedAt": "2026-04-05T05:42:00.738Z",
  "finalizedAt": "2026-04-05T05:42:00.738Z",
  "status": "completed",
  "overallDecision": "conditionally_accepted",
  "header": {
    "title": "第三轮未提交修改深度审查",
    "date": "2026-04-05",
    "overview": "第三轮深度审查：逐一验证前两轮发现的5个问题修复实现，深入引用链追踪，检查逻辑严谨性与边界正确性"
  },
  "scope": {
    "markdown": "# 第三轮未提交修改深度审查\n\n**日期**: 2026-04-05\n\n## 审查范围\n\n在前两轮审查（共发现 5 个去重后的问题）和修复 plan 全部标记完成后，本轮按以下方法做终局验证：\n\n1. **修复验证**：逐一读取修改文件，检查每个修复是否正确实现、是否引入新问题\n2. **引用链追踪**：深入上下游调用者/被调用者，确认修复不破坏既有行为\n3. **边界审查**：针对每个修复点构造边界用例做心理推演，检查是否仍有遗漏\n4. **测试覆盖**：审查新增/修改的测试是否真正覆盖了修复的核心分支\n\n### 问题-任务对应表\n\n| # | 问题 | 修复任务 |\n|---|------|----------|\n| 1 | SGS 探测式自动分配污染统计计数 | auto_assign 增加 probe_only |\n| 2 | 端点扫描回归把 safe_url_for 当强制 url_for | 正则增加负向后顾 |\n| 3 | Excel 路由数值校验与服务契约漂移 | 日历/工时/供应商三处统一严格解析 |\n| 4 | 静默吞异常护栏退化为按文件计数 | 升级为 file:func 指纹 |\n| 5 | excel_validators.py 冗余 float() 和多余空行 | 代码风格清理 |"
  },
  "summary": {
    "latestConclusion": "## 第三轮审查最终结论\n\n### 总体判定：通过（有条件）\n\n经过对 17 个核心模块的逐文件审查、完整引用链追踪和边界用例推演，**前两轮发现的全部 5 个问题（去重后）均已正确修复**，修复实现整体简洁优雅，未引入新的 BUG、静默回退或过度兜底。\n\n### 5 项修复验证结果\n\n| 任务 | 问题 | 实现 | 测试 | 判定 |\n|------|------|------|------|------|\n| 1 | SGS 探测计数污染 | ✅ 闭包切换，一行解决 8 处调用 | ⚠️ 假覆盖（测试用例未触达目标路径） | 实现正确，建议补测试 |\n| 2 | 端点扫描误报 | ✅ 负向后顾精确排除 | ✅ 契约与实现一致 | 完全通过 |\n| 3 | Excel 路由数值漂移 | ✅ 三处统一复用服务层严格解析 | ✅ 覆盖布尔/NaN/Inf/科学计数法 | 完全通过 |\n| 4 | 静默吞异常护栏退化 | ✅ 升级为 file:func 指纹+计数 | ✅ 检测能力显著提升 | 完全通过 |\n| 5 | 冗余 float() 和空行 | ✅ 已清理 | ✅ 返回类型链路验证无误 | 完全通过 |\n\n### 核心事实\n\n- **无新 BUG**：从数值解析（`strict_parse._parse_finite_float`）到路由预览/确认（`scheduler_excel_calendar`、`process_excel_part_operation_hours`、`process_excel_suppliers`）到算法评分（`dispatch_sgs`）到结果持久化的完整链路均经过边界推演验证\n- **无静默回退**：`except Exception` 全部改为 `except ValidationError`（或 `_count` 闭包消除计数副作用），非法输入显式拒绝并给出有意义错误信息\n- **无过度兜底**：`parse_finite_float`/`parse_required_float` 是单一严格解析入口，路由层不再手写重复版本\n- **设计简洁**：`probe_only` 用一行闭包替代 8 个 `if`；负向后顾一行正则替代复杂字符串操作；`Counter` + 函数指纹替代行号白名单\n\n### 唯一开放问题\n\n| 级别 | 问题 | 影响 | 建议 |\n|------|------|------|------|\n| 低 | SGS 探测计数回归测试假覆盖 | 不影响功能正确性，但回归安全网有盲区 | 提交后补充 SGS+内部工序+自动分配的测试用例 |\n\n### 与前两轮审查的对照\n\n前两轮共识别 6 个问题（去重后 5 个）：\n- 4 个中等 → 全部修复并通过验证 ✅\n- 1 个低等 → 修复并通过验证 ✅\n- 本轮新发现 1 个低等（假覆盖）→ 不阻塞提交",
    "recommendedNextAction": "当前代码可以提交。提交后建议补充一个使用 dispatch_mode='sgs' + source='internal' + auto_assign_enabled=True + resource_pool 非空的回归测试，消除 probe_only 修复的假覆盖盲区。",
    "reviewedModules": [
      "core/algorithms/greedy/auto_assign.py",
      "core/algorithms/greedy/scheduler.py",
      "core/algorithms/greedy/dispatch/sgs.py",
      "core/algorithms/greedy/algo_stats.py",
      "tests/regression_sgs_scoring_fallback_unscorable.py",
      "tests/regression_greedy_scheduler_algo_stats_auto_assign.py",
      "tests/regression_template_urlfor_endpoints.py",
      "core/services/common/excel_validators.py",
      "web/routes/scheduler_excel_calendar.py",
      "web/routes/process_excel_part_operation_hours.py",
      "web/routes/process_excel_suppliers.py",
      "core/services/common/strict_parse.py",
      "core/services/common/number_utils.py",
      "core/services/scheduler/number_utils.py",
      "core/infrastructure/errors.py",
      "tests/regression_excel_route_strict_numeric.py",
      "tests/test_architecture_fitness.py"
    ]
  },
  "stats": {
    "totalMilestones": 2,
    "completedMilestones": 2,
    "totalFindings": 1,
    "severity": {
      "high": 0,
      "medium": 0,
      "low": 1
    }
  },
  "milestones": [
    {
      "id": "M1",
      "title": "算法层与测试工具修复验证（任务1/2/5）",
      "status": "completed",
      "recordedAt": "2026-04-05T05:40:45.970Z",
      "summaryMarkdown": "## 任务 1：SGS 探测式自动分配计数污染修复\n\n### 实现正确性 ✅\n\n`auto_assign.py` 第 26 行新增 `probe_only: bool = False` 参数，第 37 行用闭包 `_count = increment_counter if not probe_only else (lambda *_args, **_kwargs: None)` 一行实现全部 8 处计数调用的有条件切换。方案简洁优雅，无重复代码，无多余分支。\n\n### 引用链追踪 ✅\n\n- `scheduler.py` 第 579 行新增 `probe_only: bool = False`，第 595 行透传 `probe_only=probe_only` — 正确\n- `sgs.py` 第 249 行：评分阶段调用 `chooser(... probe_only=True ...)` — 正确\n- `scheduler.py` 第 441-454 行：正式排产调用 **不传** `probe_only`，默认 `False` — 记数行为保持不变 ✅\n- `algo_stats.py::increment_counter` 直接修改 `scheduler._algo_stats[\"fallback_counts\"]`，当 `probe_only=True` 时 `_count` 为空操作，不会修改任何外部状态 ✅\n\n### 副作用分析 ✅\n\n`auto_assign_internal_resources` 中除了 `increment_counter` 的 8 处调用外，所有其他变量（`machine_candidates`、`op_candidates`、`best`、`best_pair` 等）都是局部变量，不会泄漏到调用方。因此 `probe_only=True` 时函数的唯一外部副作用（统计计数）被完全消除，排产结果（返回值 `best_pair`）不受影响。\n\n### 测试覆盖 ⚠️\n\n`regression_sgs_scoring_fallback_unscorable.py` 第 140-152 行新增了对 `probe_keys` 的断言，但该测试的两个工序都是**外协**（`source=\"external\"`），SGS 评分阶段根本不会走到内部工序的 `auto_assign` 分支（`sgs.py` 第 228-263 行），因此这 6 个 probe_keys 断言**全部以零值通过**——即使删掉 `probe_only` 修复，断言仍会通过。\n\n`regression_greedy_scheduler_algo_stats_auto_assign.py` 使用 `dispatch_mode=\"batch_order\"`（非 SGS），所以也不会触达 SGS 评分中的 probe_only 路径。\n\n**结论**：当前没有任何测试真正覆盖 \"SGS + 内部工序 + 自动分配 + probe_only=True\" 这条路径。实现本身显然正确（一行闭包切换），但回归测试提供的是**假覆盖**。\n\n---\n\n## 任务 2：端点扫描回归正则修正 ✅\n\n`regression_template_urlfor_endpoints.py` 第 60 行：`pat_url_for = re.compile(r\"(?<!safe_)url_for\\(\\s*['\\\"]([^'\\\"]+)['\\\"]\")`\n\n负向后顾 `(?<!safe_)` 精确排除 `safe_url_for(` 中的子串匹配，不影响独立 `url_for(`、`{{ url_for(` 等合法模式。第 61 行的 `pat_safe` 仍独立提取 `safe_url_for`。两套集合完全分离，测试契约与实现描述一致。\n\n---\n\n## 任务 5：excel_validators.py 代码风格清理 ✅\n\n- 第 194 行：`parse_finite_float(sh, field=\"可用工时\", allow_none=False)` — `float()` 包装已去除\n- 第 216 行：`parse_finite_float(eff, field=\"效率\", allow_none=False)` — `float()` 包装已去除\n- 追踪返回类型：`parse_finite_float(allow_none=False)` → `parse_required_float()` → `_parse_finite_float()` 返回 `float(parsed)`，类型确认为 `float`，外层包装确实冗余\n- 第 200 行和第 222 行的连续双空行已修复为单空行 ✅",
      "conclusionMarkdown": "任务 1 实现正确优雅，但回归测试存在假覆盖；任务 2 和任务 5 修复完整，无遗留问题。",
      "evidence": [
        {
          "path": "core/algorithms/greedy/auto_assign.py",
          "lineStart": 26,
          "lineEnd": 37,
          "symbol": "probe_only + _count closure"
        },
        {
          "path": "core/algorithms/greedy/scheduler.py",
          "lineStart": 564,
          "lineEnd": 596,
          "symbol": "_auto_assign_internal_resources"
        },
        {
          "path": "core/algorithms/greedy/dispatch/sgs.py",
          "lineStart": 236,
          "lineEnd": 249,
          "symbol": "probe_only=True"
        },
        {
          "path": "core/algorithms/greedy/scheduler.py",
          "lineStart": 439,
          "lineEnd": 454,
          "symbol": "_schedule_internal 正式调用（不传 probe_only）"
        },
        {
          "path": "tests/regression_sgs_scoring_fallback_unscorable.py",
          "lineStart": 140,
          "lineEnd": 152,
          "symbol": "probe_keys 假覆盖断言"
        },
        {
          "path": "tests/regression_template_urlfor_endpoints.py",
          "lineStart": 60,
          "lineEnd": 61,
          "symbol": "(?<!safe_) 负向后顾"
        },
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 194,
          "lineEnd": 221,
          "symbol": "float() 包装已去除、空行已修复"
        }
      ],
      "reviewedModules": [
        "core/algorithms/greedy/auto_assign.py",
        "core/algorithms/greedy/scheduler.py",
        "core/algorithms/greedy/dispatch/sgs.py",
        "core/algorithms/greedy/algo_stats.py",
        "tests/regression_sgs_scoring_fallback_unscorable.py",
        "tests/regression_greedy_scheduler_algo_stats_auto_assign.py",
        "tests/regression_template_urlfor_endpoints.py",
        "core/services/common/excel_validators.py"
      ],
      "recommendedNextAction": null,
      "findingIds": [
        "probe-only-false-coverage"
      ]
    },
    {
      "id": "M2",
      "title": "Excel 路由契约对齐与测试护栏升级验证（任务3/4）",
      "status": "completed",
      "recordedAt": "2026-04-05T05:41:45.520Z",
      "summaryMarkdown": "## 任务 3：Excel 路由数值校验漂移修复\n\n### 日历路由（scheduler_excel_calendar.py）✅\n\n两处 `validate_row`（预览第 197-221 行 / 确认第 331-354 行）均已从原始 `float()` 改为 `parse_finite_float(sh, field=\"可用工时\")` 和 `parse_finite_float(eff, field=\"效率\")`，`except Exception` 改为 `except ValidationError as e: return e.message`。\n\n**完整调用链验证**：\n```\nparse_finite_float(value, field=...) [number_utils.py]\n  → parse_required_float(value, field=...) [strict_parse.py]\n    → _parse_finite_float(value, field=...) [strict_parse.py]\n      → isinstance(value, bool) → 拒绝 True/False\n      → float(value) → 解析为浮点\n      → math.isfinite(parsed) → 拒绝 NaN/Inf\n```\n\n**边界推演**：\n| 输入 | 行为 | 正确性 |\n|------|------|--------|\n| `True` | `isinstance(True, bool)` → 拒绝 | ✅ 与服务层一致 |\n| `float(\"nan\")` | `math.isfinite = False` → 拒绝 | ✅ |\n| `float(\"inf\")` | `math.isfinite = False` → 拒绝 | ✅ |\n| `\"NaN\"` | `float(\"NaN\") = nan` → 拒绝 | ✅ |\n| `0` | 解析为 `0.0`，由路由自行检查 `< 0` | ✅ |\n| `\"5e0\"` | `float(\"5e0\") = 5.0`，有限数 → 接受 | ✅（科学计数法用于工时合法） |\n\n文件头部第 18 行已导入 `from core.services.common.number_utils import parse_finite_float`。✅\n\n### 零件工时路由（process_excel_part_operation_hours.py）✅\n\n`_parse_seq` 第 47 行增加 `isinstance(value, bool)` 拦截，第 58 行增加 `\"e\" in s.lower()` 拦截。\n\n**边界推演**：\n| 输入 | 行为 | 正确性 |\n|------|------|--------|\n| `True` | `isinstance(True, bool)` → `None` | ✅ 与 `_coerce_int` 一致 |\n| `\"5e0\"` | `\"e\" in \"5e0\"` → `None` | ✅ 与 `_coerce_int` 一致 |\n| `5` | 非 `bool`，是 `int` → `int(5) = 5` | ✅ |\n| `3.0` | `is_integer()` → `int(3) = 3` | ✅ |\n| `\"10\"` | 不含 `e`，匹配 `\\d+` → `int(10)` | ✅ |\n\n`_validate_row` 第 133-136 行用 `parse_finite_float(..., allow_none=True)` 校验工时，与服务层的 `allow_none` 语义一致。✅\n\n### 供应商路由（process_excel_suppliers.py）✅\n\n`_normalize_supplier_default_days` 第 94-97 行改用 `parse_required_float(raw_value, field=\"默认周期\", min_value=0, min_inclusive=False)`。\n\n**边界推演**：\n| 输入 | 行为 | 正确性 |\n|------|------|--------|\n| `True` | `_parse_finite_float → isinstance(True, bool)` → 拒绝 | ✅ |\n| `0` | `_parse_finite_float` 通过 → `_ensure_min_float(0.0, min=0, inclusive=False)` → `0.0 > 0.0 = False` → 拒绝 | ✅ |\n| `False` | `isinstance(False, bool)` → 拒绝 | ✅ |\n| `float(\"nan\")` | `math.isfinite = False` → 拒绝 | ✅ |\n| `5.0` | 通过全部校验 → `row[\"默认周期\"] = 5.0` | ✅ |\n\n文件头部第 18 行已导入 `from core.services.common.strict_parse import parse_required_float`。✅\n\n### 回归测试覆盖（regression_excel_route_strict_numeric.py）✅\n\n- `test_parse_seq_rejects_bool_and_scientific`：覆盖 `True/False/\"5e0\"/\"1E2\"` 被拒绝，`5/\"10\"/3.0` 被接受\n- `test_supplier_default_days_rejects_invalid_numeric`：覆盖 `True/False/NaN/Inf` 被拒绝，`5.0` 被接受\n- 日历路由的 `validate_row` 是闭包无法直接导入测试，但其使用的 `parse_finite_float` 与 `excel_validators.py` 完全相同，已有 `regression_calendar_no_tx_hardening.py` 端到端覆盖\n\n---\n\n## 任务 4：静默吞异常护栏升级\n\n### 指纹方案 ✅\n\n`_find_enclosing_name` 第 392-401 行：沿 `_ast_parent` 链上溯，找到最近的 `FunctionDef`/`AsyncFunctionDef`/`ClassDef`，返回其 `name`；无封闭定义则返回 `<module>`。\n\n`_scan_file` 第 453-481 行：先构建 `_ast_parent` 引用，再扫描 `except Exception:` + `pass/...`，输出 `f\"{fp}:{_find_enclosing_name(h)}\"`。\n\n### 白名单格式升级 ✅\n\n第 408-451 行：`known_violations` 从旧的 `file:line → Set` 升级为 `Counter({\"file:func\": count})`。每个条目明确记录函数名和该函数内的命中次数。\n\n### 比较逻辑 ✅\n\n第 483-497 行：\n```python\ncurrent_violations: Counter = Counter()\n...\nfor hit, count in sorted(current_violations.items()):\n    allowed = int(known_violations.get(hit, 0))\n    if count > allowed:\n        new_violations.append(...)\n```\n\n相比旧的文件级计数，新方案的检测能力提升：\n- **跨函数替换可检测**：在同文件内将 `func_A` 的吞异常删除、在 `func_B` 新增 → 指纹变化，被检出\n- **行号漂移免疫**：函数内增删代码不改变函数名 → 指纹稳定\n- **同函数增减可检测**：`Counter` 按 `(file, func)` 统计次数，增加一处 → 超出白名单 → 被检出\n\n**微弱残余风险**：同函数内删一处旧的、加一处新的 → 次数不变 → 不被检出。但此场景在实际开发中发生概率极低，且已比文件级计数严格得多。可接受。",
      "conclusionMarkdown": "任务 3 的三处路由修复均与服务层契约完全对齐，边界用例推演全部通过；任务 4 的指纹方案显著优于旧的文件级计数，实际检测能力大幅提升。两项修复均无新引入的 BUG 或过度兜底。",
      "evidence": [
        {
          "path": "web/routes/scheduler_excel_calendar.py",
          "lineStart": 197,
          "lineEnd": 221,
          "symbol": "预览 validate_row 改用 parse_finite_float"
        },
        {
          "path": "web/routes/scheduler_excel_calendar.py",
          "lineStart": 331,
          "lineEnd": 354,
          "symbol": "确认 validate_row 改用 parse_finite_float"
        },
        {
          "path": "web/routes/process_excel_part_operation_hours.py",
          "lineStart": 46,
          "lineEnd": 68,
          "symbol": "_parse_seq 布尔拒绝+科学计数法拒绝"
        },
        {
          "path": "web/routes/process_excel_suppliers.py",
          "lineStart": 90,
          "lineEnd": 100,
          "symbol": "_normalize_supplier_default_days 改用 parse_required_float"
        },
        {
          "path": "core/services/common/strict_parse.py",
          "lineStart": 32,
          "lineEnd": 41,
          "symbol": "_parse_finite_float 布尔拒绝+非有限拒绝"
        },
        {
          "path": "core/services/common/strict_parse.py",
          "lineStart": 59,
          "lineEnd": 64,
          "symbol": "parse_required_float 链路"
        },
        {
          "path": "tests/regression_excel_route_strict_numeric.py",
          "lineStart": 10,
          "lineEnd": 40,
          "symbol": "回归测试覆盖"
        },
        {
          "path": "tests/test_architecture_fitness.py",
          "lineStart": 404,
          "lineEnd": 497,
          "symbol": "test_no_silent_exception_swallow 指纹升级"
        }
      ],
      "reviewedModules": [
        "web/routes/scheduler_excel_calendar.py",
        "web/routes/process_excel_part_operation_hours.py",
        "web/routes/process_excel_suppliers.py",
        "core/services/common/strict_parse.py",
        "core/services/common/number_utils.py",
        "core/services/scheduler/number_utils.py",
        "core/infrastructure/errors.py",
        "tests/regression_excel_route_strict_numeric.py",
        "tests/test_architecture_fitness.py"
      ],
      "recommendedNextAction": null,
      "findingIds": []
    }
  ],
  "findings": [
    {
      "id": "probe-only-false-coverage",
      "severity": "low",
      "category": "test",
      "title": "SGS 探测计数修复的回归测试假覆盖",
      "descriptionMarkdown": "regression_sgs_scoring_fallback_unscorable.py 第 140-152 行新增了 probe_keys 断言，但测试中所有工序均为外协（source=external），SGS 评分阶段不会走到内部工序的 auto_assign 分支，6 个 probe_keys 断言全部以零值平凡通过。即使删掉 probe_only 修复，断言仍不会失败。需要一个使用 SGS 模式 + 内部工序 + 自动分配 + resource_pool 的测试用例才能真正覆盖 probe_only 路径。",
      "recommendationMarkdown": "补充一个使用 dispatch_mode='sgs'、source='internal'、auto_assign_enabled=True、resource_pool 非空的测试用例，验证评分阶段的探测调用不污染 fallback_counts。",
      "evidence": [
        {
          "path": "tests/regression_sgs_scoring_fallback_unscorable.py",
          "lineStart": 140,
          "lineEnd": 152
        },
        {
          "path": "core/algorithms/greedy/dispatch/sgs.py",
          "lineStart": 228,
          "lineEnd": 263
        },
        {
          "path": "tests/regression_sgs_scoring_fallback_unscorable.py"
        },
        {
          "path": "core/algorithms/greedy/dispatch/sgs.py"
        }
      ],
      "relatedMilestoneIds": [
        "M1"
      ],
      "trackingStatus": "open"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:a6e4708f77df5941ee9d2ea02c9b87d2d4702c59cec5e6903fbe06936cb92637",
    "generatedAt": "2026-04-05T05:42:00.738Z",
    "locale": "zh-CN"
  }
}
```
