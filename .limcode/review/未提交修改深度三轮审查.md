# 未提交修改深度三轮审查
- 日期: 2026-04-05
- 概述: 对当前工作区未提交修改进行深度三轮审查：正确性、优雅性、回归覆盖
- 状态: 已完成
- 总体结论: 通过

## 评审范围

# 未提交修改深度三轮审查

**日期**: 2026-04-04

## 审查范围

根据 plan 文件，本次修改涵盖以下4个阶段：
1. 统一批次表格导入成功后的告警外显限流策略
2. 人员专属日历"可用工时/效率"解析收口到 `parse_finite_float()`
3. 补齐 `BatchService.update()` 图号切换保护的直达回归
4. 清理误导性防御代码（`or 0`）

## 审查方法

- **第一轮**：逐文件检查业务代码修改的正确性、逻辑严谨性
- **第二轮**：检查测试覆盖的充分性、回归测试的有效性
- **第三轮**：交叉验证代码与 plan 的一致性，检查是否有遗漏

## 评审摘要

- 当前状态: 已完成
- 已审模块: core/services/common/excel_validators.py, core/services/scheduler/batch_excel_import.py, web/routes/scheduler_excel_batches.py, web/routes/scheduler_batches.py, web/routes/scheduler_bp.py, core/services/scheduler/batch_service.py, core/services/scheduler/batch_write_rules.py, core/services/scheduler/calendar_admin.py, tests/test_excel_import_hardening.py, tests/regression_calendar_no_tx_hardening.py, tests/regression_scheduler_batch_template_warning_surface.py, tests/regression_batch_service_update_reject_part_change_without_rebuild.py, tests/regression_batch_excel_import_reject_part_change_without_rebuild.py, tests/regression_excel_operator_calendar_cross_midnight.py
- 当前进度: 已记录 3 个里程碑；最新：M3
- 里程碑总数: 3
- 已完成里程碑: 3
- 问题总数: 2
- 问题严重级别分布: 高 0 / 中 0 / 低 2
- 最新结论: ## 三轮深度审查最终结论 ### 总体判定 本次未提交修改 **达成了 plan 设定的全部5项验收标准**，可以放心提交。 ### 正确性 - **无 BUG**：所有4个阶段的业务代码修改逻辑正确，校验链路从预览到落库完整闭合 - **无静默回退**：人员日历"可用工时/效率"已真正收口到 `parse_finite_float()`，布尔值/NaN/Inf 均显式拒绝，不存在隐式降级 - **无过度兜底**：`or 0` 已清理，告警外显统一为共享限流函数，无误导性防御残留 - **无部分写入风险**：`BatchService.update()` 的图号保护在 `build_update_payload` 阶段（事务开启前）即抛出异常 ### 优雅性与简洁性 - 告警外显策略通过单一共享函数 `_surface_schedule_warnings` 实现，三条批次路径调用统一 `limit=3` - 数值解析通过 `parse_finite_float` 统一门面实现单点规则，无散落的裸 `float()` 解析 - 仅存两项微瑕（均为低优先级）： 1. `parse_finite_float` 返回值外多余 `float()` 包装（2处） 2. 函数体内连续双空行（2处） ### 测试覆盖 - 人员日历预览/确认/落库三段均有专项回归，覆盖 True/False/NaN/Inf/-Inf 共18组断言 - 告警限流有端到端路由测试，含去重/截断/汇总全路径验证 - 图号保护有服务层直达回归和表格导入回归，均断言无部分写入 - 正向场景（跨午夜日历、正常建批）不受影响已验证 ### 遗留事项（非本轮范畴） - `schedule_summary` 中的冗余 `bool()/int()/list()` 包装，plan 已明确另开独立整理轮次
- 下一步建议: 顺手修复 excel_validators.py 中2处 float() 冗余包装和2处多余空行后即可提交
- 总体结论: 通过

## 评审发现

### parse_finite_float 返回值外包冗余 float()

- ID: F-parse-finite-float-返回值外包冗余-float
- 严重级别: 低
- 分类: 可维护性
- 跟踪状态: 开放
- 相关里程碑: M1
- 说明:

  excel_validators.py 第194行和第217行，将 parse_finite_float() 的返回值再包一层 float()。parse_finite_float(allow_none=False) 的返回类型已经是 float，外层 float() 是纯冗余操作。虽然不影响正确性，但与本轮'清理误导性防御代码'的精神略有矛盾。
- 建议:

  去掉外层 float() 包装，直接赋值即可。
- 证据:
  - `core/services/common/excel_validators.py:194`
  - `core/services/common/excel_validators.py:217`
  - `core/services/common/excel_validators.py`

### 函数体内多余空行

- ID: F-函数体内多余空行
- 严重级别: 低
- 分类: 可维护性
- 跟踪状态: 开放
- 相关里程碑: M3
- 说明:

  excel_validators.py 第200-201行、第223-224行在函数体内出现连续两个空行，而同文件其它位置均使用单空行分隔。不影响功能，纯视觉不一致。
- 证据:
  - `core/services/common/excel_validators.py:200-201`
  - `core/services/common/excel_validators.py:223-224`
  - `core/services/common/excel_validators.py`

## 评审里程碑

### M1 · 第一轮：核心业务代码正确性审查

- 状态: 已完成
- 记录时间: 2026-04-05T03:39:20.237Z
- 已审模块: core/services/common/excel_validators.py, core/services/scheduler/batch_excel_import.py, web/routes/scheduler_excel_batches.py, web/routes/scheduler_batches.py, web/routes/scheduler_bp.py, core/services/scheduler/batch_service.py, core/services/scheduler/batch_write_rules.py, core/services/scheduler/calendar_admin.py
- 摘要:

  ## 阶段二（人员日历严格数值契约）

  **`excel_validators.py`** 第189-222行：可用工时/效率的解析已从裸 `float()` 改为 `parse_finite_float()`。

  链路验证：
  - `parse_finite_float` → `parse_required_float` → `_parse_finite_float` → 第33-34行 `isinstance(value, bool)` 检查 → 拒绝布尔值 ✅
  - `_parse_finite_float` 第39-40行 `math.isfinite()` 检查 → 拒绝 NaN/Inf ✅
  - 空值预检在 `parse_finite_float` 之前执行，用于提供默认值（工作日8/0、效率1.0/假期默认效率），逻辑正确 ✅

  落库路径验证（`CalendarAdmin._build_operator_calendar`）：
  - `_normalize_float` → `parse_finite_float` → 同样拒绝布尔/NaN/Inf ✅
  - 预览/确认/落库三段语义一致 ✅

  ## 阶段一（告警外显限流策略统一）

  **`scheduler_excel_batches.py`** 第386-387行：导入成功后调用 `_surface_schedule_warnings(svc.consume_user_visible_warnings(), limit=3)`，与 `scheduler_batches.py` 第186行一致。

  `_surface_schedule_warnings` 函数（`scheduler_bp.py` 第43-52行）逻辑：
  - 通过 `_normalize_warning_texts` 去重、去空 ✅
  - 展示上限截断 ✅
  - 剩余条数汇总 ✅

  ## 阶段三（批次更新图号保护）

  **`batch_service.py`** 第219-222行：`update()` 方法固定 `auto_generate_ops=False`，配合 `batch_write_rules._ensure_part_switch_allowed()` 在 `build_update_payload` 阶段（事务开始之前）即抛出 `ValidationError`，保证无部分写入 ✅

  ## 阶段四（清理误导性防御）

  **`batch_excel_import.py`** 第83行：`svc._normalize_int(pr.data.get("数量"), field="数量", allow_none=False)` — 无 `or 0` 存在，已清理 ✅
- 结论:

  ## 阶段二（人员日历严格数值契约） **`excel_validators.py`** 第189-222行：可用工时/效率的解析已从裸 `float()` 改为 `parse_finite_float()`。 链路验证： - `parse_finite_float` → `parse_required_float` → `_parse_finite_float` → 第33-34行 `isinstance(value, bool)` 检查 → 拒绝布尔值 ✅ - `_parse_finite_float` 第39-40行 `math.isfinite()` 检查 → 拒绝 NaN/Inf ✅ - 空值预检在 `parse_finite_float` 之前执行，用于提供默认值（工作日8/0、效率1.0/假期默认效率），逻辑正确 ✅ 落库路径验证（`CalendarAdmin._build_operator_calendar`）： - `_normalize_float` → `parse_finite_float` → 同样拒绝布尔/NaN/Inf ✅ - 预览/确认/落库三段语义一致 ✅ ## 阶段一（告警外显限流策略统一） **`scheduler_excel_batches.py`** 第386-387行：导入成功后调用 `_surface_schedule_warnings(svc.consume_user_visible_warnings(), limit=3)`，与 `scheduler_batches.py` 第186行一致。 `_surface_schedule_warnings` 函数（`scheduler_bp.py` 第43-52行）逻辑： - 通过 `_normalize_warning_texts` 去重、去空 ✅ - 展示上限截断 ✅ - 剩余条数汇总 ✅ ## 阶段三（批次更新图号保护） **`batch_service.py`** 第219-222行：`update()` 方法固定 `auto_generate_ops=False`，配合 `batch_write_rules._ensure_part_switch_allowed()` 在 `build_update_payload` 阶段（事务开始之前）即抛出 `ValidationError`，保证无部分写入 ✅ ## 阶段四（清理误导性防御） **`batch_excel_import.py`** 第83行：`svc._normalize_int(pr.data.get("数量"), field="数量", allow_none=False)` — 无 `or 0` 存在，已清理 ✅
- 证据:
  - `core/services/common/excel_validators.py:189-222`
  - `web/routes/scheduler_excel_batches.py:386-388`
  - `core/services/scheduler/batch_service.py:219-222`
  - `core/services/scheduler/batch_excel_import.py:83`
- 问题:
  - [低] 可维护性: parse_finite_float 返回值外包冗余 float()

### M2 · 第二轮：测试覆盖充分性审查

- 状态: 已完成
- 记录时间: 2026-04-05T03:41:12.384Z
- 已审模块: tests/test_excel_import_hardening.py, tests/regression_calendar_no_tx_hardening.py, tests/regression_scheduler_batch_template_warning_surface.py, tests/regression_batch_service_update_reject_part_change_without_rebuild.py, tests/regression_batch_excel_import_reject_part_change_without_rebuild.py, tests/regression_excel_operator_calendar_cross_midnight.py
- 摘要:

  ## 测试覆盖矩阵

  ### 阶段二 — 人员日历严格数值契约（三段覆盖完整）

  | 层级 | 测试 | 覆盖要点 |
  |---|---|---|
  | 预览校验 | `test_operator_calendar_validator_rejects_bool_and_nonfinite_numeric_inputs` | 覆盖"可用工时"和"效率"两个字段各4种非法值（True/False/NaN/Inf），共8个断言，使用 `==` 严格匹配完整报错信息 |
  | 路由层 | `test_operator_calendar_preview_and_confirm_reject_bool_numeric_cells` | 端到端：上传含布尔单元格的表格 → 预览显示错误 → 确认被拒绝 → 数据库无写入 |
  | 落库层 | `regression_calendar_no_tx_hardening` 第240-252行 | 直接调用 `upsert_operator_calendar_no_tx`，True/False/NaN/Inf/-Inf 各5组，逐条断言抛出 `ValidationError` 并确认数据库行数为0 |

  ### 阶段一 — 告警外显限流策略

  | 场景 | 测试 | 覆盖要点 |
  |---|---|---|
  | 手工建批 | `test_scheduler_batch_template_warning_surface` | 端到端创建含外协工种的批次，断言 `success` 和 `warning` 信息都出现 |
  | 表格导入 | `test_scheduler_excel_batch_confirm_surfaces_warnings_with_limit` | 用 monkeypatch 注入7条告警（含空行/重复），断言只显示前3条，第4/5条不显示，并显示"另有 2 条告警"汇总 |

  ### 阶段三 — 批次更新图号保护

  | 入口 | 测试 | 覆盖要点 |
  |---|---|---|
  | 服务层直达 | `regression_batch_service_update_reject_part_change_without_rebuild` | 直接调用 `svc.update(part_no="P_NEW")`，断言 `ValidationError`，断言 part_no/part_name/quantity/due_date/remark/op_count 全部未变化 |
  | 表格导入 | `regression_batch_excel_import_reject_part_change_without_rebuild` | 通过 `import_from_preview_rows` 尝试变更图号（未开启自动生成工序），断言同样抛出 `ValidationError` 并无部分写入 |

  ### 覆盖充分性判定

  - 预览/确认/落库三段一致性：✅ 完整覆盖
  - 正向场景不受影响：✅ `regression_excel_operator_calendar_cross_midnight` 验证正常跨午夜班次计算不受影响
  - 告警限流的去重/截断/汇总：✅ 测试数据刻意包含空行和重复，验证去重逻辑
  - 无部分写入保证：✅ 两个图号保护测试均断言数据库数据完全未变化
  - 错误信息断言精度：预览校验层使用 `==` 精确匹配，落库层使用 `in` 子串匹配（可接受，因为同时用 `_expect_validation_error` 保证异常类型正确）
- 结论:

  ## 测试覆盖矩阵 ### 阶段二 — 人员日历严格数值契约（三段覆盖完整） | 层级 | 测试 | 覆盖要点 | |---|---|---| | 预览校验 | `test_operator_calendar_validator_rejects_bool_and_nonfinite_numeric_inputs` | 覆盖"可用工时"和"效率"两个字段各4种非法值（True/False/NaN/Inf），共8个断言，使用 `==` 严格匹配完整报错信息 | | 路由层 | `test_operator_calendar_preview_and_confirm_reject_bool_numeric_cells` | 端到端：上传含布尔单元格的表格 → 预览显示错误 → 确认被拒绝 → 数据库无写入 | | 落库层 | `regression_calendar_no_tx_hardening` 第240-252行 | 直接调用 `upsert_operator_calendar_no_tx`，True/False/NaN/Inf/-Inf 各5组，逐条断言抛出 `ValidationError` 并确认数据库行数为0 | ### 阶段一 — 告警外显限流策略 | 场景 | 测试 | 覆盖要点 | |---|---|---| | 手工建批 | `test_scheduler_batch_template_warning_surface` | 端到端创建含外协工种的批次，断言 `success` 和 `warning` 信息都出现 | | 表格导入 | `test_scheduler_excel_batch_confirm_surfaces_warnings_with_limit` | 用 monkeypatch 注入7条告警（含空行/重复），断言只显示前3条，第4/5条不显示，并显示"另有 2 条告警"汇总 | ### 阶段三 — 批次更新图号保护 | 入口 | 测试 | 覆盖要点 | |---|---|---| | 服务层直达 | `regression_batch_service_update_reject_part_change_without_rebuild` | 直接调用 `svc.update(part_no="P_NEW")`，断言 `ValidationError`，断言 part_no/part_name/quantity/due_date/remark/op_count 全部未变化 | | 表格导入 | `regression_batch_excel_import_reject_part_change_without_rebuild` | 通过 `import_from_preview_rows` 尝试变更图号（未开启自动生成工序），断言同样抛出 `ValidationError` 并无部分写入 | ### 覆盖充分性判定 - 预览/确认/落库三段一致性：✅ 完整覆盖 - 正向场景不受影响：✅ `regression_excel_operator_calendar_cross_midnight` 验证正常跨午夜班次计算不受影响 - 告警限流的去重/截断/汇总：✅ 测试数据刻意包含空行和重复，验证去重逻辑 - 无部分写入保证：✅ 两个图号保护测试均断言数据库数据完全未变化 - 错误信息断言精度：预览校验层使用 `==` 精确匹配，落库层使用 `in` 子串匹配（可接受，因为同时用 `_expect_validation_error` 保证异常类型正确）
- 证据:
  - `tests/test_excel_import_hardening.py:182-219#test_operator_calendar_validator_rejects_bool_and_nonfinite_numeric_inputs`
  - `tests/test_excel_import_hardening.py:224-270#test_operator_calendar_preview_and_confirm_reject_bool_numeric_cells`
  - `tests/regression_calendar_no_tx_hardening.py:240-252#落库层布尔/NaN/Inf拒绝`
  - `tests/regression_scheduler_batch_template_warning_surface.py:116-180#test_scheduler_excel_batch_confirm_surfaces_warnings_with_limit`
  - `tests/regression_batch_service_update_reject_part_change_without_rebuild.py:19-66`
  - `tests/regression_batch_excel_import_reject_part_change_without_rebuild.py:21-82`

### M3 · 第三轮：plan 对照验收与遗漏检查

- 状态: 已完成
- 记录时间: 2026-04-05T03:42:38.341Z
- 摘要:

  ## plan 验收标准逐条核对

  | # | 验收标准 | 状态 | 依据 |
  |---|---|---|---|
  | 1 | 表格导入不再无上限逐条外显告警 | ✅ 达成 | `scheduler_excel_batches.py:387` 使用 `_surface_schedule_warnings(limit=3)` |
  | 2 | 不再对布尔值做隐式数值化 | ✅ 达成 | `parse_finite_float` → `_parse_finite_float` 第33行显式拒绝布尔 |
  | 3 | 布尔/NaN/Inf 三段语义一致 | ✅ 达成 | 预览层用 `get_operator_calendar_row_validate_and_normalize`；确认层复校同一校验器；落库层用 `CalendarAdmin._normalize_float` → 同一 `parse_finite_float` |
  | 4 | `update()` 图号保护有直达回归 | ✅ 达成 | `regression_batch_service_update_reject_part_change_without_rebuild` 断言5个字段+工序数不变 |
  | 5 | 不改变既有正确场景 | ✅ 达成 | 跨午夜日历、正常批次创建等既有回归未受影响 |

  ## 告警外显调用一致性

  | 调用点 | 文件位置 | `limit` 值 |
  |---|---|---|
  | 手工建批 | `scheduler_batches.py:186` | 3 |
  | 重建工序 | `scheduler_batches.py:377` | 3 |
  | 表格导入 | `scheduler_excel_batches.py:387` | 3 |
  | 排产运行 | `scheduler_run.py:64` | 默认5（不同场景） |
  | 周计划 | `scheduler_week_plan.py:191` | 默认5（不同场景） |

  三个批次相关路径 `limit` 统一为3，排产运行/周计划是独立场景使用默认5——策略一致，无漏网。

  ## 遗漏检查

  - plan 明确暂缓的 `schedule_summary` 冗余包装清理：确认未被触及 ✅
  - `or 0` 误导性防御：确认已清理（`batch_excel_import.py:83` 不再包含 `or 0`）✅
  - 是否有其他隐式布尔数值化残留：`excel_validators.py` 中的"可用工时"和"效率"已修复，其他数值字段（如批次"数量"）使用 `parse_finite_int` 同样拒绝布尔值 ✅

  ## BUG 排查结论

  - **无阻断级 BUG**：所有修改点逻辑正确，校验链路完整
  - **无静默回退**：非法输入均显式报错，不存在隐式降级路径
  - **无过度兜底**：仅存轻微冗余（`float()` 包装），无误导性防御残留
- 结论:

  ## plan 验收标准逐条核对 | # | 验收标准 | 状态 | 依据 | |---|---|---|---| | 1 | 表格导入不再无上限逐条外显告警 | ✅ 达成 | `scheduler_excel_batches.py:387` 使用 `_surface_schedule_warnings(limit=3)` | | 2 | 不再对布尔值做隐式数值化 | ✅ 达成 | `parse_finite_float` → `_parse_finite_float` 第33行显式拒绝布尔 | | 3 | 布尔/NaN/Inf 三段语义一致 | ✅ 达成 | 预览层用 `get_operator_calendar_row_validate_and_normalize`；确认层复校同一校验器；落库层用 `CalendarAdmin._normalize_float` → 同一 `parse_finite_float` | | 4 | `update()` 图号保护有直达回归 | ✅ 达成 | `regression_batch_service_update_reject_part_change_without_rebuild` 断言5个字段+工序数不变 | | 5 | 不改变既有正确场景 | ✅ 达成 | 跨午夜日历、正常批次创建等既有回归未受影响 | ## 告警外显调用一致性 | 调用点 | 文件位置 | `limit` 值 | |---|---|---| | 手工建批 | `scheduler_batches.py:186` | 3 | | 重建工序 | `scheduler_batches.py:377` | 3 | | 表格导入 | `scheduler_excel_batches.py:387` | 3 | | 排产运行 | `scheduler_run.py:64` | 默认5（不同场景） | | 周计划 | `scheduler_week_plan.py:191` | 默认5（不同场景） | 三个批次相关路径 `limit` 统一为3，排产运行/周计划是独立场景使用默认5——策略一致，无漏网。 ## 遗漏检查 - plan 明确暂缓的 `schedule_summary` 冗余包装清理：确认未被触及 ✅ - `or 0` 误导性防御：确认已清理（`batch_excel_import.py:83` 不再包含 `or 0`）✅ - 是否有其他隐式布尔数值化残留：`excel_validators.py` 中的"可用工时"和"效率"已修复，其他数值字段（如批次"数量"）使用 `parse_finite_int` 同样拒绝布尔值 ✅ ## BUG 排查结论 - **无阻断级 BUG**：所有修改点逻辑正确，校验链路完整 - **无静默回退**：非法输入均显式报错，不存在隐式降级路径 - **无过度兜底**：仅存轻微冗余（`float()` 包装），无误导性防御残留
- 问题:
  - [低] 可维护性: 函数体内多余空行

## 最终结论

## 三轮深度审查最终结论

### 总体判定

本次未提交修改 **达成了 plan 设定的全部5项验收标准**，可以放心提交。

### 正确性

- **无 BUG**：所有4个阶段的业务代码修改逻辑正确，校验链路从预览到落库完整闭合
- **无静默回退**：人员日历"可用工时/效率"已真正收口到 `parse_finite_float()`，布尔值/NaN/Inf 均显式拒绝，不存在隐式降级
- **无过度兜底**：`or 0` 已清理，告警外显统一为共享限流函数，无误导性防御残留
- **无部分写入风险**：`BatchService.update()` 的图号保护在 `build_update_payload` 阶段（事务开启前）即抛出异常

### 优雅性与简洁性

- 告警外显策略通过单一共享函数 `_surface_schedule_warnings` 实现，三条批次路径调用统一 `limit=3`
- 数值解析通过 `parse_finite_float` 统一门面实现单点规则，无散落的裸 `float()` 解析
- 仅存两项微瑕（均为低优先级）：
  1. `parse_finite_float` 返回值外多余 `float()` 包装（2处）
  2. 函数体内连续双空行（2处）

### 测试覆盖

- 人员日历预览/确认/落库三段均有专项回归，覆盖 True/False/NaN/Inf/-Inf 共18组断言
- 告警限流有端到端路由测试，含去重/截断/汇总全路径验证
- 图号保护有服务层直达回归和表格导入回归，均断言无部分写入
- 正向场景（跨午夜日历、正常建批）不受影响已验证

### 遗留事项（非本轮范畴）

- `schedule_summary` 中的冗余 `bool()/int()/list()` 包装，plan 已明确另开独立整理轮次

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mnl7lw80-uxyoic",
  "createdAt": "2026-04-05T00:00:00.000Z",
  "updatedAt": "2026-04-05T03:43:02.266Z",
  "finalizedAt": "2026-04-05T03:43:02.266Z",
  "status": "completed",
  "overallDecision": "accepted",
  "header": {
    "title": "未提交修改深度三轮审查",
    "date": "2026-04-05",
    "overview": "对当前工作区未提交修改进行深度三轮审查：正确性、优雅性、回归覆盖"
  },
  "scope": {
    "markdown": "# 未提交修改深度三轮审查\n\n**日期**: 2026-04-04\n\n## 审查范围\n\n根据 plan 文件，本次修改涵盖以下4个阶段：\n1. 统一批次表格导入成功后的告警外显限流策略\n2. 人员专属日历\"可用工时/效率\"解析收口到 `parse_finite_float()`\n3. 补齐 `BatchService.update()` 图号切换保护的直达回归\n4. 清理误导性防御代码（`or 0`）\n\n## 审查方法\n\n- **第一轮**：逐文件检查业务代码修改的正确性、逻辑严谨性\n- **第二轮**：检查测试覆盖的充分性、回归测试的有效性\n- **第三轮**：交叉验证代码与 plan 的一致性，检查是否有遗漏"
  },
  "summary": {
    "latestConclusion": "## 三轮深度审查最终结论\n\n### 总体判定\n\n本次未提交修改 **达成了 plan 设定的全部5项验收标准**，可以放心提交。\n\n### 正确性\n\n- **无 BUG**：所有4个阶段的业务代码修改逻辑正确，校验链路从预览到落库完整闭合\n- **无静默回退**：人员日历\"可用工时/效率\"已真正收口到 `parse_finite_float()`，布尔值/NaN/Inf 均显式拒绝，不存在隐式降级\n- **无过度兜底**：`or 0` 已清理，告警外显统一为共享限流函数，无误导性防御残留\n- **无部分写入风险**：`BatchService.update()` 的图号保护在 `build_update_payload` 阶段（事务开启前）即抛出异常\n\n### 优雅性与简洁性\n\n- 告警外显策略通过单一共享函数 `_surface_schedule_warnings` 实现，三条批次路径调用统一 `limit=3`\n- 数值解析通过 `parse_finite_float` 统一门面实现单点规则，无散落的裸 `float()` 解析\n- 仅存两项微瑕（均为低优先级）：\n  1. `parse_finite_float` 返回值外多余 `float()` 包装（2处）\n  2. 函数体内连续双空行（2处）\n\n### 测试覆盖\n\n- 人员日历预览/确认/落库三段均有专项回归，覆盖 True/False/NaN/Inf/-Inf 共18组断言\n- 告警限流有端到端路由测试，含去重/截断/汇总全路径验证\n- 图号保护有服务层直达回归和表格导入回归，均断言无部分写入\n- 正向场景（跨午夜日历、正常建批）不受影响已验证\n\n### 遗留事项（非本轮范畴）\n\n- `schedule_summary` 中的冗余 `bool()/int()/list()` 包装，plan 已明确另开独立整理轮次",
    "recommendedNextAction": "顺手修复 excel_validators.py 中2处 float() 冗余包装和2处多余空行后即可提交",
    "reviewedModules": [
      "core/services/common/excel_validators.py",
      "core/services/scheduler/batch_excel_import.py",
      "web/routes/scheduler_excel_batches.py",
      "web/routes/scheduler_batches.py",
      "web/routes/scheduler_bp.py",
      "core/services/scheduler/batch_service.py",
      "core/services/scheduler/batch_write_rules.py",
      "core/services/scheduler/calendar_admin.py",
      "tests/test_excel_import_hardening.py",
      "tests/regression_calendar_no_tx_hardening.py",
      "tests/regression_scheduler_batch_template_warning_surface.py",
      "tests/regression_batch_service_update_reject_part_change_without_rebuild.py",
      "tests/regression_batch_excel_import_reject_part_change_without_rebuild.py",
      "tests/regression_excel_operator_calendar_cross_midnight.py"
    ]
  },
  "stats": {
    "totalMilestones": 3,
    "completedMilestones": 3,
    "totalFindings": 2,
    "severity": {
      "high": 0,
      "medium": 0,
      "low": 2
    }
  },
  "milestones": [
    {
      "id": "M1",
      "title": "第一轮：核心业务代码正确性审查",
      "status": "completed",
      "recordedAt": "2026-04-05T03:39:20.237Z",
      "summaryMarkdown": "## 阶段二（人员日历严格数值契约）\n\n**`excel_validators.py`** 第189-222行：可用工时/效率的解析已从裸 `float()` 改为 `parse_finite_float()`。\n\n链路验证：\n- `parse_finite_float` → `parse_required_float` → `_parse_finite_float` → 第33-34行 `isinstance(value, bool)` 检查 → 拒绝布尔值 ✅\n- `_parse_finite_float` 第39-40行 `math.isfinite()` 检查 → 拒绝 NaN/Inf ✅\n- 空值预检在 `parse_finite_float` 之前执行，用于提供默认值（工作日8/0、效率1.0/假期默认效率），逻辑正确 ✅\n\n落库路径验证（`CalendarAdmin._build_operator_calendar`）：\n- `_normalize_float` → `parse_finite_float` → 同样拒绝布尔/NaN/Inf ✅\n- 预览/确认/落库三段语义一致 ✅\n\n## 阶段一（告警外显限流策略统一）\n\n**`scheduler_excel_batches.py`** 第386-387行：导入成功后调用 `_surface_schedule_warnings(svc.consume_user_visible_warnings(), limit=3)`，与 `scheduler_batches.py` 第186行一致。\n\n`_surface_schedule_warnings` 函数（`scheduler_bp.py` 第43-52行）逻辑：\n- 通过 `_normalize_warning_texts` 去重、去空 ✅\n- 展示上限截断 ✅\n- 剩余条数汇总 ✅\n\n## 阶段三（批次更新图号保护）\n\n**`batch_service.py`** 第219-222行：`update()` 方法固定 `auto_generate_ops=False`，配合 `batch_write_rules._ensure_part_switch_allowed()` 在 `build_update_payload` 阶段（事务开始之前）即抛出 `ValidationError`，保证无部分写入 ✅\n\n## 阶段四（清理误导性防御）\n\n**`batch_excel_import.py`** 第83行：`svc._normalize_int(pr.data.get(\"数量\"), field=\"数量\", allow_none=False)` — 无 `or 0` 存在，已清理 ✅",
      "conclusionMarkdown": "## 阶段二（人员日历严格数值契约） **`excel_validators.py`** 第189-222行：可用工时/效率的解析已从裸 `float()` 改为 `parse_finite_float()`。 链路验证： - `parse_finite_float` → `parse_required_float` → `_parse_finite_float` → 第33-34行 `isinstance(value, bool)` 检查 → 拒绝布尔值 ✅ - `_parse_finite_float` 第39-40行 `math.isfinite()` 检查 → 拒绝 NaN/Inf ✅ - 空值预检在 `parse_finite_float` 之前执行，用于提供默认值（工作日8/0、效率1.0/假期默认效率），逻辑正确 ✅ 落库路径验证（`CalendarAdmin._build_operator_calendar`）： - `_normalize_float` → `parse_finite_float` → 同样拒绝布尔/NaN/Inf ✅ - 预览/确认/落库三段语义一致 ✅ ## 阶段一（告警外显限流策略统一） **`scheduler_excel_batches.py`** 第386-387行：导入成功后调用 `_surface_schedule_warnings(svc.consume_user_visible_warnings(), limit=3)`，与 `scheduler_batches.py` 第186行一致。 `_surface_schedule_warnings` 函数（`scheduler_bp.py` 第43-52行）逻辑： - 通过 `_normalize_warning_texts` 去重、去空 ✅ - 展示上限截断 ✅ - 剩余条数汇总 ✅ ## 阶段三（批次更新图号保护） **`batch_service.py`** 第219-222行：`update()` 方法固定 `auto_generate_ops=False`，配合 `batch_write_rules._ensure_part_switch_allowed()` 在 `build_update_payload` 阶段（事务开始之前）即抛出 `ValidationError`，保证无部分写入 ✅ ## 阶段四（清理误导性防御） **`batch_excel_import.py`** 第83行：`svc._normalize_int(pr.data.get(\"数量\"), field=\"数量\", allow_none=False)` — 无 `or 0` 存在，已清理 ✅",
      "evidence": [
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 189,
          "lineEnd": 222
        },
        {
          "path": "web/routes/scheduler_excel_batches.py",
          "lineStart": 386,
          "lineEnd": 388
        },
        {
          "path": "core/services/scheduler/batch_service.py",
          "lineStart": 219,
          "lineEnd": 222
        },
        {
          "path": "core/services/scheduler/batch_excel_import.py",
          "lineStart": 83,
          "lineEnd": 83
        }
      ],
      "reviewedModules": [
        "core/services/common/excel_validators.py",
        "core/services/scheduler/batch_excel_import.py",
        "web/routes/scheduler_excel_batches.py",
        "web/routes/scheduler_batches.py",
        "web/routes/scheduler_bp.py",
        "core/services/scheduler/batch_service.py",
        "core/services/scheduler/batch_write_rules.py",
        "core/services/scheduler/calendar_admin.py"
      ],
      "recommendedNextAction": null,
      "findingIds": [
        "F-parse-finite-float-返回值外包冗余-float"
      ]
    },
    {
      "id": "M2",
      "title": "第二轮：测试覆盖充分性审查",
      "status": "completed",
      "recordedAt": "2026-04-05T03:41:12.384Z",
      "summaryMarkdown": "## 测试覆盖矩阵\n\n### 阶段二 — 人员日历严格数值契约（三段覆盖完整）\n\n| 层级 | 测试 | 覆盖要点 |\n|---|---|---|\n| 预览校验 | `test_operator_calendar_validator_rejects_bool_and_nonfinite_numeric_inputs` | 覆盖\"可用工时\"和\"效率\"两个字段各4种非法值（True/False/NaN/Inf），共8个断言，使用 `==` 严格匹配完整报错信息 |\n| 路由层 | `test_operator_calendar_preview_and_confirm_reject_bool_numeric_cells` | 端到端：上传含布尔单元格的表格 → 预览显示错误 → 确认被拒绝 → 数据库无写入 |\n| 落库层 | `regression_calendar_no_tx_hardening` 第240-252行 | 直接调用 `upsert_operator_calendar_no_tx`，True/False/NaN/Inf/-Inf 各5组，逐条断言抛出 `ValidationError` 并确认数据库行数为0 |\n\n### 阶段一 — 告警外显限流策略\n\n| 场景 | 测试 | 覆盖要点 |\n|---|---|---|\n| 手工建批 | `test_scheduler_batch_template_warning_surface` | 端到端创建含外协工种的批次，断言 `success` 和 `warning` 信息都出现 |\n| 表格导入 | `test_scheduler_excel_batch_confirm_surfaces_warnings_with_limit` | 用 monkeypatch 注入7条告警（含空行/重复），断言只显示前3条，第4/5条不显示，并显示\"另有 2 条告警\"汇总 |\n\n### 阶段三 — 批次更新图号保护\n\n| 入口 | 测试 | 覆盖要点 |\n|---|---|---|\n| 服务层直达 | `regression_batch_service_update_reject_part_change_without_rebuild` | 直接调用 `svc.update(part_no=\"P_NEW\")`，断言 `ValidationError`，断言 part_no/part_name/quantity/due_date/remark/op_count 全部未变化 |\n| 表格导入 | `regression_batch_excel_import_reject_part_change_without_rebuild` | 通过 `import_from_preview_rows` 尝试变更图号（未开启自动生成工序），断言同样抛出 `ValidationError` 并无部分写入 |\n\n### 覆盖充分性判定\n\n- 预览/确认/落库三段一致性：✅ 完整覆盖\n- 正向场景不受影响：✅ `regression_excel_operator_calendar_cross_midnight` 验证正常跨午夜班次计算不受影响\n- 告警限流的去重/截断/汇总：✅ 测试数据刻意包含空行和重复，验证去重逻辑\n- 无部分写入保证：✅ 两个图号保护测试均断言数据库数据完全未变化\n- 错误信息断言精度：预览校验层使用 `==` 精确匹配，落库层使用 `in` 子串匹配（可接受，因为同时用 `_expect_validation_error` 保证异常类型正确）",
      "conclusionMarkdown": "## 测试覆盖矩阵 ### 阶段二 — 人员日历严格数值契约（三段覆盖完整） | 层级 | 测试 | 覆盖要点 | |---|---|---| | 预览校验 | `test_operator_calendar_validator_rejects_bool_and_nonfinite_numeric_inputs` | 覆盖\"可用工时\"和\"效率\"两个字段各4种非法值（True/False/NaN/Inf），共8个断言，使用 `==` 严格匹配完整报错信息 | | 路由层 | `test_operator_calendar_preview_and_confirm_reject_bool_numeric_cells` | 端到端：上传含布尔单元格的表格 → 预览显示错误 → 确认被拒绝 → 数据库无写入 | | 落库层 | `regression_calendar_no_tx_hardening` 第240-252行 | 直接调用 `upsert_operator_calendar_no_tx`，True/False/NaN/Inf/-Inf 各5组，逐条断言抛出 `ValidationError` 并确认数据库行数为0 | ### 阶段一 — 告警外显限流策略 | 场景 | 测试 | 覆盖要点 | |---|---|---| | 手工建批 | `test_scheduler_batch_template_warning_surface` | 端到端创建含外协工种的批次，断言 `success` 和 `warning` 信息都出现 | | 表格导入 | `test_scheduler_excel_batch_confirm_surfaces_warnings_with_limit` | 用 monkeypatch 注入7条告警（含空行/重复），断言只显示前3条，第4/5条不显示，并显示\"另有 2 条告警\"汇总 | ### 阶段三 — 批次更新图号保护 | 入口 | 测试 | 覆盖要点 | |---|---|---| | 服务层直达 | `regression_batch_service_update_reject_part_change_without_rebuild` | 直接调用 `svc.update(part_no=\"P_NEW\")`，断言 `ValidationError`，断言 part_no/part_name/quantity/due_date/remark/op_count 全部未变化 | | 表格导入 | `regression_batch_excel_import_reject_part_change_without_rebuild` | 通过 `import_from_preview_rows` 尝试变更图号（未开启自动生成工序），断言同样抛出 `ValidationError` 并无部分写入 | ### 覆盖充分性判定 - 预览/确认/落库三段一致性：✅ 完整覆盖 - 正向场景不受影响：✅ `regression_excel_operator_calendar_cross_midnight` 验证正常跨午夜班次计算不受影响 - 告警限流的去重/截断/汇总：✅ 测试数据刻意包含空行和重复，验证去重逻辑 - 无部分写入保证：✅ 两个图号保护测试均断言数据库数据完全未变化 - 错误信息断言精度：预览校验层使用 `==` 精确匹配，落库层使用 `in` 子串匹配（可接受，因为同时用 `_expect_validation_error` 保证异常类型正确）",
      "evidence": [
        {
          "path": "tests/test_excel_import_hardening.py",
          "lineStart": 182,
          "lineEnd": 219,
          "symbol": "test_operator_calendar_validator_rejects_bool_and_nonfinite_numeric_inputs"
        },
        {
          "path": "tests/test_excel_import_hardening.py",
          "lineStart": 224,
          "lineEnd": 270,
          "symbol": "test_operator_calendar_preview_and_confirm_reject_bool_numeric_cells"
        },
        {
          "path": "tests/regression_calendar_no_tx_hardening.py",
          "lineStart": 240,
          "lineEnd": 252,
          "symbol": "落库层布尔/NaN/Inf拒绝"
        },
        {
          "path": "tests/regression_scheduler_batch_template_warning_surface.py",
          "lineStart": 116,
          "lineEnd": 180,
          "symbol": "test_scheduler_excel_batch_confirm_surfaces_warnings_with_limit"
        },
        {
          "path": "tests/regression_batch_service_update_reject_part_change_without_rebuild.py",
          "lineStart": 19,
          "lineEnd": 66
        },
        {
          "path": "tests/regression_batch_excel_import_reject_part_change_without_rebuild.py",
          "lineStart": 21,
          "lineEnd": 82
        }
      ],
      "reviewedModules": [
        "tests/test_excel_import_hardening.py",
        "tests/regression_calendar_no_tx_hardening.py",
        "tests/regression_scheduler_batch_template_warning_surface.py",
        "tests/regression_batch_service_update_reject_part_change_without_rebuild.py",
        "tests/regression_batch_excel_import_reject_part_change_without_rebuild.py",
        "tests/regression_excel_operator_calendar_cross_midnight.py"
      ],
      "recommendedNextAction": null,
      "findingIds": []
    },
    {
      "id": "M3",
      "title": "第三轮：plan 对照验收与遗漏检查",
      "status": "completed",
      "recordedAt": "2026-04-05T03:42:38.341Z",
      "summaryMarkdown": "## plan 验收标准逐条核对\n\n| # | 验收标准 | 状态 | 依据 |\n|---|---|---|---|\n| 1 | 表格导入不再无上限逐条外显告警 | ✅ 达成 | `scheduler_excel_batches.py:387` 使用 `_surface_schedule_warnings(limit=3)` |\n| 2 | 不再对布尔值做隐式数值化 | ✅ 达成 | `parse_finite_float` → `_parse_finite_float` 第33行显式拒绝布尔 |\n| 3 | 布尔/NaN/Inf 三段语义一致 | ✅ 达成 | 预览层用 `get_operator_calendar_row_validate_and_normalize`；确认层复校同一校验器；落库层用 `CalendarAdmin._normalize_float` → 同一 `parse_finite_float` |\n| 4 | `update()` 图号保护有直达回归 | ✅ 达成 | `regression_batch_service_update_reject_part_change_without_rebuild` 断言5个字段+工序数不变 |\n| 5 | 不改变既有正确场景 | ✅ 达成 | 跨午夜日历、正常批次创建等既有回归未受影响 |\n\n## 告警外显调用一致性\n\n| 调用点 | 文件位置 | `limit` 值 |\n|---|---|---|\n| 手工建批 | `scheduler_batches.py:186` | 3 |\n| 重建工序 | `scheduler_batches.py:377` | 3 |\n| 表格导入 | `scheduler_excel_batches.py:387` | 3 |\n| 排产运行 | `scheduler_run.py:64` | 默认5（不同场景） |\n| 周计划 | `scheduler_week_plan.py:191` | 默认5（不同场景） |\n\n三个批次相关路径 `limit` 统一为3，排产运行/周计划是独立场景使用默认5——策略一致，无漏网。\n\n## 遗漏检查\n\n- plan 明确暂缓的 `schedule_summary` 冗余包装清理：确认未被触及 ✅\n- `or 0` 误导性防御：确认已清理（`batch_excel_import.py:83` 不再包含 `or 0`）✅\n- 是否有其他隐式布尔数值化残留：`excel_validators.py` 中的\"可用工时\"和\"效率\"已修复，其他数值字段（如批次\"数量\"）使用 `parse_finite_int` 同样拒绝布尔值 ✅\n\n## BUG 排查结论\n\n- **无阻断级 BUG**：所有修改点逻辑正确，校验链路完整\n- **无静默回退**：非法输入均显式报错，不存在隐式降级路径\n- **无过度兜底**：仅存轻微冗余（`float()` 包装），无误导性防御残留",
      "conclusionMarkdown": "## plan 验收标准逐条核对 | # | 验收标准 | 状态 | 依据 | |---|---|---|---| | 1 | 表格导入不再无上限逐条外显告警 | ✅ 达成 | `scheduler_excel_batches.py:387` 使用 `_surface_schedule_warnings(limit=3)` | | 2 | 不再对布尔值做隐式数值化 | ✅ 达成 | `parse_finite_float` → `_parse_finite_float` 第33行显式拒绝布尔 | | 3 | 布尔/NaN/Inf 三段语义一致 | ✅ 达成 | 预览层用 `get_operator_calendar_row_validate_and_normalize`；确认层复校同一校验器；落库层用 `CalendarAdmin._normalize_float` → 同一 `parse_finite_float` | | 4 | `update()` 图号保护有直达回归 | ✅ 达成 | `regression_batch_service_update_reject_part_change_without_rebuild` 断言5个字段+工序数不变 | | 5 | 不改变既有正确场景 | ✅ 达成 | 跨午夜日历、正常批次创建等既有回归未受影响 | ## 告警外显调用一致性 | 调用点 | 文件位置 | `limit` 值 | |---|---|---| | 手工建批 | `scheduler_batches.py:186` | 3 | | 重建工序 | `scheduler_batches.py:377` | 3 | | 表格导入 | `scheduler_excel_batches.py:387` | 3 | | 排产运行 | `scheduler_run.py:64` | 默认5（不同场景） | | 周计划 | `scheduler_week_plan.py:191` | 默认5（不同场景） | 三个批次相关路径 `limit` 统一为3，排产运行/周计划是独立场景使用默认5——策略一致，无漏网。 ## 遗漏检查 - plan 明确暂缓的 `schedule_summary` 冗余包装清理：确认未被触及 ✅ - `or 0` 误导性防御：确认已清理（`batch_excel_import.py:83` 不再包含 `or 0`）✅ - 是否有其他隐式布尔数值化残留：`excel_validators.py` 中的\"可用工时\"和\"效率\"已修复，其他数值字段（如批次\"数量\"）使用 `parse_finite_int` 同样拒绝布尔值 ✅ ## BUG 排查结论 - **无阻断级 BUG**：所有修改点逻辑正确，校验链路完整 - **无静默回退**：非法输入均显式报错，不存在隐式降级路径 - **无过度兜底**：仅存轻微冗余（`float()` 包装），无误导性防御残留",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": null,
      "findingIds": [
        "F-函数体内多余空行"
      ]
    }
  ],
  "findings": [
    {
      "id": "F-parse-finite-float-返回值外包冗余-float",
      "severity": "low",
      "category": "maintainability",
      "title": "parse_finite_float 返回值外包冗余 float()",
      "descriptionMarkdown": "excel_validators.py 第194行和第217行，将 parse_finite_float() 的返回值再包一层 float()。parse_finite_float(allow_none=False) 的返回类型已经是 float，外层 float() 是纯冗余操作。虽然不影响正确性，但与本轮'清理误导性防御代码'的精神略有矛盾。",
      "recommendationMarkdown": "去掉外层 float() 包装，直接赋值即可。",
      "evidence": [
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 194,
          "lineEnd": 194
        },
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 217,
          "lineEnd": 217
        },
        {
          "path": "core/services/common/excel_validators.py"
        }
      ],
      "relatedMilestoneIds": [
        "M1"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-函数体内多余空行",
      "severity": "low",
      "category": "maintainability",
      "title": "函数体内多余空行",
      "descriptionMarkdown": "excel_validators.py 第200-201行、第223-224行在函数体内出现连续两个空行，而同文件其它位置均使用单空行分隔。不影响功能，纯视觉不一致。",
      "recommendationMarkdown": null,
      "evidence": [
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 200,
          "lineEnd": 201
        },
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 223,
          "lineEnd": 224
        },
        {
          "path": "core/services/common/excel_validators.py"
        }
      ],
      "relatedMilestoneIds": [
        "M3"
      ],
      "trackingStatus": "open"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:3b6b338692d99eb6a2756775c25de7ee6bcbd5ca2e414831e3dbe6926481ce88",
    "generatedAt": "2026-04-05T03:43:02.266Z",
    "locale": "zh-CN"
  }
}
```
