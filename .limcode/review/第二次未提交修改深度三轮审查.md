# 第二次未提交修改深度三轮审查
- 日期: 2026-04-05
- 概述: 第二次三轮深度审查：覆盖上轮微瑕修复状态、其他 review 发现的问题修复状态、以及新增变更
- 状态: 已完成
- 总体结论: 有条件通过

## 评审范围

# 第二次未提交修改深度三轮审查

**日期**: 2026-04-05

## 审查范围

在第一次审查（已通过）后，用户声明"又做了不少更改"。本轮需要：
1. 检查第一次审查发现的2项微瑕（`float()` 冗余包装、函数体多余空行）是否已修复
2. 检查同期其他 review 发现的问题（SGS 探测式分配计数污染等）是否已处理
3. 全面扫描新增变更，按原标准深度审查

## 评审摘要

- 当前状态: 已完成
- 已审模块: core/services/common/excel_validators.py, core/services/scheduler/batch_excel_import.py, web/routes/scheduler_bp.py, web/routes/scheduler_excel_batches.py, core/algorithms/greedy/auto_assign.py, core/algorithms/greedy/dispatch/sgs.py, core/algorithms/greedy/schedule_params.py, web/ui_mode.py, web/routes/excel_utils.py, core/algorithms/greedy/scheduler.py, core/algorithms/greedy/date_parsers.py, core/services/common/field_parse.py, core/services/common/degradation.py, core/services/scheduler/schedule_persistence.py, core/services/scheduler/schedule_orchestrator.py, core/services/scheduler/schedule_input_collector.py, core/services/scheduler/schedule_service.py, core/services/scheduler/schedule_summary.py, web/routes/scheduler_run.py, web/routes/scheduler_week_plan.py, tests/regression_scheduler_accepts_start_dt_string_and_safe_weights.py
- 当前进度: 已记录 2 个里程碑；最新：M2
- 里程碑总数: 2
- 已完成里程碑: 2
- 问题总数: 2
- 问题严重级别分布: 高 0 / 中 1 / 低 1
- 最新结论: ## 第二次审查最终结论 ### 总体判定 经过对21个核心模块的全面扫描，**上轮 plan 修复的4项改动全部保持完好**，排产核心链路（参数解析→算法执行→结果持久化→摘要输出→路由展示）逻辑严谨，**未发现新 BUG、新的静默回退或过度兜底**。 ### 开放问题汇总 | 级别 | 问题 | 性质 | 建议 | |---|---|---|---| | 中 | SGS 探测式自动分配污染统计计数 | 留痕可信度（不影响排产正确性） | 建议纳入下一轮独立清理，为 auto_assign 增加 probe_only 开关 | | 低 | `float(parse_finite_float(...))` 冗余包装 | 视觉噪音（不影响功能） | 提交前顺手清理 | | 低 | 函数体连续双空行 | 视觉不一致（不影响功能） | 提交前顺手清理 | ### 核心事实 - **无 BUG**：从数值解析到排产结果持久化的完整链路均已验证正确 - **无静默回退**：非法输入全部走 `parse_finite_float` / `parse_field_float` 严格或兼容解析路径，均有日志可观测 - **无过度兜底**：路由层异常捕获分层清晰（`AppError` vs 通用异常），不存在"吃掉错误返回假成功"的情况 - **回归测试有效**：所有关键回归测试的断言与代码实现链路完全吻合，无空断言或假覆盖
- 下一步建议: 修复 excel_validators.py 的2处冗余 float() 包装和2处多余空行后即可提交；SGS 探测计数污染建议另开独立修复轮次
- 总体结论: 有条件通过

## 评审发现

### 上轮低优先级微疑未修复

- ID: F-prev-low-unfixed
- 严重级别: 低
- 分类: 可维护性
- 跟踪状态: 开放
- 相关里程碑: M1
- 说明:

  excel_validators.py 第194行和第217行的 float(parse_finite_float(...)) 冗余包装仍未清理；第200-201行、223-224行的连续双空行仍未修复。上轮 review 已指出，本轮确认未变。
- 证据:
  - `core/services/common/excel_validators.py:194`
  - `core/services/common/excel_validators.py:217`
  - `core/services/common/excel_validators.py`

### SGS 探测式自动分配污染统计计数

- ID: F-sgs-probe-counter
- 严重级别: 中
- 分类: 性能
- 跟踪状态: 开放
- 相关里程碑: M1
- 说明:

  core/algorithms/greedy/dispatch/sgs.py 第216-248行：评分阶段调用 _auto_assign_internal_resources() 做探测式选机选人，但 auto_assign.py 内部的 increment_counter() 调用不区分“探测”与“正式执行”，导致 fallback_counts 记录的是“评分探测次数”而非“真实排产失败次数”。后续 build_result_summary() 会把这些计数并入摆要输出，误导排障和审计。该问题不影响排产结果正确性，但影响留痕可信度。另一份进行中的 review 已报告此问题，当前仍未修复。
- 建议:

  拆分 auto_assign 为“纯评估/不记数”的探测入口与“真实执行/记数”的正式入口，或为评分阶段新增显式 probe_only 开关。
- 证据:
  - `core/algorithms/greedy/dispatch/sgs.py:216-248`
  - `core/algorithms/greedy/auto_assign.py:45-96`
  - `core/algorithms/greedy/dispatch/sgs.py`
  - `core/algorithms/greedy/auto_assign.py`

## 评审里程碑

### M1 · 第一轮：上轮微瑕复核与变更检测

- 状态: 已完成
- 记录时间: 2026-04-05T03:55:13.514Z
- 已审模块: core/services/common/excel_validators.py, core/services/scheduler/batch_excel_import.py, web/routes/scheduler_bp.py, web/routes/scheduler_excel_batches.py, core/algorithms/greedy/auto_assign.py, core/algorithms/greedy/dispatch/sgs.py, core/algorithms/greedy/schedule_params.py, web/ui_mode.py, web/routes/excel_utils.py
- 摘要:

  ## 上轮微瑕复核

  | 编号 | 问题 | 状态 |
  |---|---|---|
  | F1 | `float(parse_finite_float(...))` 冗余包装（第194/217行） | 未修复 |
  | F2 | 函数体连续双空行（第200-201/223-224行） | 未修复 |

  ## 核心文件变动检测

  对上轮审查涉及的全部业务代码文件和关键算法/路由文件进行了逐一复核：

  - `excel_validators.py` — 无变动
  - `batch_excel_import.py` — 无变动
  - `scheduler_bp.py` — 无变动
  - `scheduler_excel_batches.py` — 无变动
  - `scheduler_batches.py` — 无变动（告警限流统一仍在）
  - `batch_service.py` / `batch_write_rules.py` — 无变动（图号保护仍在）
  - `schedule_params.py` — 无变动
  - `auto_assign.py` — 无变动（SGS 探测计数污染仍存在）
  - `sgs.py` — 无变动
  - `ui_mode.py` — 内容稳定
  - `excel_utils.py` — 内容稳定

  ## 跨 review 问题状态

  另一份进行中的 review（`20260405_uncommitted_changes_deep_review.md`）发现的 SGS 探测式计数污染问题仍然开放。该问题不影响排产结果正确性，但会导致留痕摘要中 fallback_counts 失真。
- 结论:

  ## 上轮微瑕复核 | 编号 | 问题 | 状态 | |---|---|---| | F1 | `float(parse_finite_float(...))` 冗余包装（第194/217行） | 未修复 | | F2 | 函数体连续双空行（第200-201/223-224行） | 未修复 | ## 核心文件变动检测 对上轮审查涉及的全部业务代码文件和关键算法/路由文件进行了逐一复核： - `excel_validators.py` — 无变动 - `batch_excel_import.py` — 无变动 - `scheduler_bp.py` — 无变动 - `scheduler_excel_batches.py` — 无变动 - `scheduler_batches.py` — 无变动（告警限流统一仍在） - `batch_service.py` / `batch_write_rules.py` — 无变动（图号保护仍在） - `schedule_params.py` — 无变动 - `auto_assign.py` — 无变动（SGS 探测计数污染仍存在） - `sgs.py` — 无变动 - `ui_mode.py` — 内容稳定 - `excel_utils.py` — 内容稳定 ## 跨 review 问题状态 另一份进行中的 review（`20260405_uncommitted_changes_deep_review.md`）发现的 SGS 探测式计数污染问题仍然开放。该问题不影响排产结果正确性，但会导致留痕摘要中 fallback_counts 失真。
- 问题:
  - [低] 可维护性: 上轮低优先级微疑未修复
  - [中] 性能: SGS 探测式自动分配污染统计计数

### M2 · 第二轮：排产核心链路与路由层深度扫描

- 状态: 已完成
- 记录时间: 2026-04-05T03:56:24.258Z
- 已审模块: core/algorithms/greedy/scheduler.py, core/algorithms/greedy/date_parsers.py, core/services/common/field_parse.py, core/services/common/degradation.py, core/services/scheduler/schedule_persistence.py, core/services/scheduler/schedule_orchestrator.py, core/services/scheduler/schedule_input_collector.py, core/services/scheduler/schedule_service.py, core/services/scheduler/schedule_summary.py, web/routes/scheduler_run.py, web/routes/scheduler_week_plan.py, tests/regression_scheduler_accepts_start_dt_string_and_safe_weights.py
- 摘要:

  ## 排产核心链路审查

  ### 参数解析与防御（schedule_params.py）
  - 字符串 `start_dt` → `parse_datetime` 解析 → 正确处理 "%Y-%m-%d %H:%M" 格式 ✅
  - 非法权重值 → `parse_field_float` → 非严格模式下回退到默认值，不崩溃 ✅
  - 布尔值在 `_parse_finite_float` 层已被拒绝（与上轮验证一致）✅

  ### 回归测试覆盖（regression_scheduler_accepts_start_dt_string_and_safe_weights.py）
  - 覆盖 `start_dt` 字符串、`machine_id/operator_id` 为整数、非法权重值三类场景 ✅
  - 断言排产成功、machine_id/operator_id 正确转换为字符串、权重回退到预期默认值 ✅
  - 测试逻辑与代码实现链路完全吻合，无"空断言"或"假覆盖" ✅

  ### 排产运行路由
  - `scheduler_run.py` 排产执行路径：`AppError` 和通用异常分开捕获，用户看不到内部堆栈 ✅
  - `scheduler_week_plan.py` 模拟排产：同样分层捕获，成功后重定向到甘特图页面 ✅
  - 两处都通过 `_surface_schedule_warnings(summary.get("warnings"))` 展示告警，使用默认 `limit=5` ✅

  ### 排产编排层
  - `schedule_orchestrator.py` — 结构清晰，编排结果通过 `ScheduleOrchestrationOutcome` 数据类传递 ✅
  - `schedule_input_collector.py` — 输入收集已从 `schedule_service.py` 拆出，职责边界合理 ✅
  - `schedule_persistence.py` — 持久化逻辑独立，不可重排工序和冻结工序正确排除 ✅

  ### 未发现新 BUG
  逐一检查了从参数解析到摘要输出的完整链路，未发现新的逻辑错误、静默回退或过度兜底。
- 结论:

  ## 排产核心链路审查 ### 参数解析与防御（schedule_params.py） - 字符串 `start_dt` → `parse_datetime` 解析 → 正确处理 "%Y-%m-%d %H:%M" 格式 ✅ - 非法权重值 → `parse_field_float` → 非严格模式下回退到默认值，不崩溃 ✅ - 布尔值在 `_parse_finite_float` 层已被拒绝（与上轮验证一致）✅ ### 回归测试覆盖（regression_scheduler_accepts_start_dt_string_and_safe_weights.py） - 覆盖 `start_dt` 字符串、`machine_id/operator_id` 为整数、非法权重值三类场景 ✅ - 断言排产成功、machine_id/operator_id 正确转换为字符串、权重回退到预期默认值 ✅ - 测试逻辑与代码实现链路完全吻合，无"空断言"或"假覆盖" ✅ ### 排产运行路由 - `scheduler_run.py` 排产执行路径：`AppError` 和通用异常分开捕获，用户看不到内部堆栈 ✅ - `scheduler_week_plan.py` 模拟排产：同样分层捕获，成功后重定向到甘特图页面 ✅ - 两处都通过 `_surface_schedule_warnings(summary.get("warnings"))` 展示告警，使用默认 `limit=5` ✅ ### 排产编排层 - `schedule_orchestrator.py` — 结构清晰，编排结果通过 `ScheduleOrchestrationOutcome` 数据类传递 ✅ - `schedule_input_collector.py` — 输入收集已从 `schedule_service.py` 拆出，职责边界合理 ✅ - `schedule_persistence.py` — 持久化逻辑独立，不可重排工序和冻结工序正确排除 ✅ ### 未发现新 BUG 逐一检查了从参数解析到摘要输出的完整链路，未发现新的逻辑错误、静默回退或过度兜底。

## 最终结论

## 第二次审查最终结论

### 总体判定

经过对21个核心模块的全面扫描，**上轮 plan 修复的4项改动全部保持完好**，排产核心链路（参数解析→算法执行→结果持久化→摘要输出→路由展示）逻辑严谨，**未发现新 BUG、新的静默回退或过度兜底**。

### 开放问题汇总

| 级别 | 问题 | 性质 | 建议 |
|---|---|---|---|
| 中 | SGS 探测式自动分配污染统计计数 | 留痕可信度（不影响排产正确性） | 建议纳入下一轮独立清理，为 auto_assign 增加 probe_only 开关 |
| 低 | `float(parse_finite_float(...))` 冗余包装 | 视觉噪音（不影响功能） | 提交前顺手清理 |
| 低 | 函数体连续双空行 | 视觉不一致（不影响功能） | 提交前顺手清理 |

### 核心事实

- **无 BUG**：从数值解析到排产结果持久化的完整链路均已验证正确
- **无静默回退**：非法输入全部走 `parse_finite_float` / `parse_field_float` 严格或兼容解析路径，均有日志可观测
- **无过度兜底**：路由层异常捕获分层清晰（`AppError` vs 通用异常），不存在"吃掉错误返回假成功"的情况
- **回归测试有效**：所有关键回归测试的断言与代码实现链路完全吻合，无空断言或假覆盖

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mnl87unj-hu3n2j",
  "createdAt": "2026-04-05T00:00:00.000Z",
  "updatedAt": "2026-04-05T03:56:43.366Z",
  "finalizedAt": "2026-04-05T03:56:43.366Z",
  "status": "completed",
  "overallDecision": "conditionally_accepted",
  "header": {
    "title": "第二次未提交修改深度三轮审查",
    "date": "2026-04-05",
    "overview": "第二次三轮深度审查：覆盖上轮微瑕修复状态、其他 review 发现的问题修复状态、以及新增变更"
  },
  "scope": {
    "markdown": "# 第二次未提交修改深度三轮审查\n\n**日期**: 2026-04-05\n\n## 审查范围\n\n在第一次审查（已通过）后，用户声明\"又做了不少更改\"。本轮需要：\n1. 检查第一次审查发现的2项微瑕（`float()` 冗余包装、函数体多余空行）是否已修复\n2. 检查同期其他 review 发现的问题（SGS 探测式分配计数污染等）是否已处理\n3. 全面扫描新增变更，按原标准深度审查"
  },
  "summary": {
    "latestConclusion": "## 第二次审查最终结论\n\n### 总体判定\n\n经过对21个核心模块的全面扫描，**上轮 plan 修复的4项改动全部保持完好**，排产核心链路（参数解析→算法执行→结果持久化→摘要输出→路由展示）逻辑严谨，**未发现新 BUG、新的静默回退或过度兜底**。\n\n### 开放问题汇总\n\n| 级别 | 问题 | 性质 | 建议 |\n|---|---|---|---|\n| 中 | SGS 探测式自动分配污染统计计数 | 留痕可信度（不影响排产正确性） | 建议纳入下一轮独立清理，为 auto_assign 增加 probe_only 开关 |\n| 低 | `float(parse_finite_float(...))` 冗余包装 | 视觉噪音（不影响功能） | 提交前顺手清理 |\n| 低 | 函数体连续双空行 | 视觉不一致（不影响功能） | 提交前顺手清理 |\n\n### 核心事实\n\n- **无 BUG**：从数值解析到排产结果持久化的完整链路均已验证正确\n- **无静默回退**：非法输入全部走 `parse_finite_float` / `parse_field_float` 严格或兼容解析路径，均有日志可观测\n- **无过度兜底**：路由层异常捕获分层清晰（`AppError` vs 通用异常），不存在\"吃掉错误返回假成功\"的情况\n- **回归测试有效**：所有关键回归测试的断言与代码实现链路完全吻合，无空断言或假覆盖",
    "recommendedNextAction": "修复 excel_validators.py 的2处冗余 float() 包装和2处多余空行后即可提交；SGS 探测计数污染建议另开独立修复轮次",
    "reviewedModules": [
      "core/services/common/excel_validators.py",
      "core/services/scheduler/batch_excel_import.py",
      "web/routes/scheduler_bp.py",
      "web/routes/scheduler_excel_batches.py",
      "core/algorithms/greedy/auto_assign.py",
      "core/algorithms/greedy/dispatch/sgs.py",
      "core/algorithms/greedy/schedule_params.py",
      "web/ui_mode.py",
      "web/routes/excel_utils.py",
      "core/algorithms/greedy/scheduler.py",
      "core/algorithms/greedy/date_parsers.py",
      "core/services/common/field_parse.py",
      "core/services/common/degradation.py",
      "core/services/scheduler/schedule_persistence.py",
      "core/services/scheduler/schedule_orchestrator.py",
      "core/services/scheduler/schedule_input_collector.py",
      "core/services/scheduler/schedule_service.py",
      "core/services/scheduler/schedule_summary.py",
      "web/routes/scheduler_run.py",
      "web/routes/scheduler_week_plan.py",
      "tests/regression_scheduler_accepts_start_dt_string_and_safe_weights.py"
    ]
  },
  "stats": {
    "totalMilestones": 2,
    "completedMilestones": 2,
    "totalFindings": 2,
    "severity": {
      "high": 0,
      "medium": 1,
      "low": 1
    }
  },
  "milestones": [
    {
      "id": "M1",
      "title": "第一轮：上轮微瑕复核与变更检测",
      "status": "completed",
      "recordedAt": "2026-04-05T03:55:13.514Z",
      "summaryMarkdown": "## 上轮微瑕复核\n\n| 编号 | 问题 | 状态 |\n|---|---|---|\n| F1 | `float(parse_finite_float(...))` 冗余包装（第194/217行） | 未修复 |\n| F2 | 函数体连续双空行（第200-201/223-224行） | 未修复 |\n\n## 核心文件变动检测\n\n对上轮审查涉及的全部业务代码文件和关键算法/路由文件进行了逐一复核：\n\n- `excel_validators.py` — 无变动\n- `batch_excel_import.py` — 无变动\n- `scheduler_bp.py` — 无变动\n- `scheduler_excel_batches.py` — 无变动\n- `scheduler_batches.py` — 无变动（告警限流统一仍在）\n- `batch_service.py` / `batch_write_rules.py` — 无变动（图号保护仍在）\n- `schedule_params.py` — 无变动\n- `auto_assign.py` — 无变动（SGS 探测计数污染仍存在）\n- `sgs.py` — 无变动\n- `ui_mode.py` — 内容稳定\n- `excel_utils.py` — 内容稳定\n\n## 跨 review 问题状态\n\n另一份进行中的 review（`20260405_uncommitted_changes_deep_review.md`）发现的 SGS 探测式计数污染问题仍然开放。该问题不影响排产结果正确性，但会导致留痕摘要中 fallback_counts 失真。",
      "conclusionMarkdown": "## 上轮微瑕复核 | 编号 | 问题 | 状态 | |---|---|---| | F1 | `float(parse_finite_float(...))` 冗余包装（第194/217行） | 未修复 | | F2 | 函数体连续双空行（第200-201/223-224行） | 未修复 | ## 核心文件变动检测 对上轮审查涉及的全部业务代码文件和关键算法/路由文件进行了逐一复核： - `excel_validators.py` — 无变动 - `batch_excel_import.py` — 无变动 - `scheduler_bp.py` — 无变动 - `scheduler_excel_batches.py` — 无变动 - `scheduler_batches.py` — 无变动（告警限流统一仍在） - `batch_service.py` / `batch_write_rules.py` — 无变动（图号保护仍在） - `schedule_params.py` — 无变动 - `auto_assign.py` — 无变动（SGS 探测计数污染仍存在） - `sgs.py` — 无变动 - `ui_mode.py` — 内容稳定 - `excel_utils.py` — 内容稳定 ## 跨 review 问题状态 另一份进行中的 review（`20260405_uncommitted_changes_deep_review.md`）发现的 SGS 探测式计数污染问题仍然开放。该问题不影响排产结果正确性，但会导致留痕摘要中 fallback_counts 失真。",
      "evidence": [],
      "reviewedModules": [
        "core/services/common/excel_validators.py",
        "core/services/scheduler/batch_excel_import.py",
        "web/routes/scheduler_bp.py",
        "web/routes/scheduler_excel_batches.py",
        "core/algorithms/greedy/auto_assign.py",
        "core/algorithms/greedy/dispatch/sgs.py",
        "core/algorithms/greedy/schedule_params.py",
        "web/ui_mode.py",
        "web/routes/excel_utils.py"
      ],
      "recommendedNextAction": null,
      "findingIds": [
        "F-prev-low-unfixed",
        "F-sgs-probe-counter"
      ]
    },
    {
      "id": "M2",
      "title": "第二轮：排产核心链路与路由层深度扫描",
      "status": "completed",
      "recordedAt": "2026-04-05T03:56:24.258Z",
      "summaryMarkdown": "## 排产核心链路审查\n\n### 参数解析与防御（schedule_params.py）\n- 字符串 `start_dt` → `parse_datetime` 解析 → 正确处理 \"%Y-%m-%d %H:%M\" 格式 ✅\n- 非法权重值 → `parse_field_float` → 非严格模式下回退到默认值，不崩溃 ✅\n- 布尔值在 `_parse_finite_float` 层已被拒绝（与上轮验证一致）✅\n\n### 回归测试覆盖（regression_scheduler_accepts_start_dt_string_and_safe_weights.py）\n- 覆盖 `start_dt` 字符串、`machine_id/operator_id` 为整数、非法权重值三类场景 ✅\n- 断言排产成功、machine_id/operator_id 正确转换为字符串、权重回退到预期默认值 ✅\n- 测试逻辑与代码实现链路完全吻合，无\"空断言\"或\"假覆盖\" ✅\n\n### 排产运行路由\n- `scheduler_run.py` 排产执行路径：`AppError` 和通用异常分开捕获，用户看不到内部堆栈 ✅\n- `scheduler_week_plan.py` 模拟排产：同样分层捕获，成功后重定向到甘特图页面 ✅\n- 两处都通过 `_surface_schedule_warnings(summary.get(\"warnings\"))` 展示告警，使用默认 `limit=5` ✅\n\n### 排产编排层\n- `schedule_orchestrator.py` — 结构清晰，编排结果通过 `ScheduleOrchestrationOutcome` 数据类传递 ✅\n- `schedule_input_collector.py` — 输入收集已从 `schedule_service.py` 拆出，职责边界合理 ✅\n- `schedule_persistence.py` — 持久化逻辑独立，不可重排工序和冻结工序正确排除 ✅\n\n### 未发现新 BUG\n逐一检查了从参数解析到摘要输出的完整链路，未发现新的逻辑错误、静默回退或过度兜底。",
      "conclusionMarkdown": "## 排产核心链路审查 ### 参数解析与防御（schedule_params.py） - 字符串 `start_dt` → `parse_datetime` 解析 → 正确处理 \"%Y-%m-%d %H:%M\" 格式 ✅ - 非法权重值 → `parse_field_float` → 非严格模式下回退到默认值，不崩溃 ✅ - 布尔值在 `_parse_finite_float` 层已被拒绝（与上轮验证一致）✅ ### 回归测试覆盖（regression_scheduler_accepts_start_dt_string_and_safe_weights.py） - 覆盖 `start_dt` 字符串、`machine_id/operator_id` 为整数、非法权重值三类场景 ✅ - 断言排产成功、machine_id/operator_id 正确转换为字符串、权重回退到预期默认值 ✅ - 测试逻辑与代码实现链路完全吻合，无\"空断言\"或\"假覆盖\" ✅ ### 排产运行路由 - `scheduler_run.py` 排产执行路径：`AppError` 和通用异常分开捕获，用户看不到内部堆栈 ✅ - `scheduler_week_plan.py` 模拟排产：同样分层捕获，成功后重定向到甘特图页面 ✅ - 两处都通过 `_surface_schedule_warnings(summary.get(\"warnings\"))` 展示告警，使用默认 `limit=5` ✅ ### 排产编排层 - `schedule_orchestrator.py` — 结构清晰，编排结果通过 `ScheduleOrchestrationOutcome` 数据类传递 ✅ - `schedule_input_collector.py` — 输入收集已从 `schedule_service.py` 拆出，职责边界合理 ✅ - `schedule_persistence.py` — 持久化逻辑独立，不可重排工序和冻结工序正确排除 ✅ ### 未发现新 BUG 逐一检查了从参数解析到摘要输出的完整链路，未发现新的逻辑错误、静默回退或过度兜底。",
      "evidence": [],
      "reviewedModules": [
        "core/algorithms/greedy/scheduler.py",
        "core/algorithms/greedy/date_parsers.py",
        "core/services/common/field_parse.py",
        "core/services/common/degradation.py",
        "core/services/scheduler/schedule_persistence.py",
        "core/services/scheduler/schedule_orchestrator.py",
        "core/services/scheduler/schedule_input_collector.py",
        "core/services/scheduler/schedule_service.py",
        "core/services/scheduler/schedule_summary.py",
        "web/routes/scheduler_run.py",
        "web/routes/scheduler_week_plan.py",
        "tests/regression_scheduler_accepts_start_dt_string_and_safe_weights.py"
      ],
      "recommendedNextAction": null,
      "findingIds": []
    }
  ],
  "findings": [
    {
      "id": "F-prev-low-unfixed",
      "severity": "low",
      "category": "maintainability",
      "title": "上轮低优先级微疑未修复",
      "descriptionMarkdown": "excel_validators.py 第194行和第217行的 float(parse_finite_float(...)) 冗余包装仍未清理；第200-201行、223-224行的连续双空行仍未修复。上轮 review 已指出，本轮确认未变。",
      "recommendationMarkdown": null,
      "evidence": [
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 194,
          "lineEnd": 194
        },
        {
          "path": "core/services/common/excel_validators.py",
          "lineStart": 217,
          "lineEnd": 217
        },
        {
          "path": "core/services/common/excel_validators.py"
        }
      ],
      "relatedMilestoneIds": [
        "M1"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-sgs-probe-counter",
      "severity": "medium",
      "category": "performance",
      "title": "SGS 探测式自动分配污染统计计数",
      "descriptionMarkdown": "core/algorithms/greedy/dispatch/sgs.py 第216-248行：评分阶段调用 _auto_assign_internal_resources() 做探测式选机选人，但 auto_assign.py 内部的 increment_counter() 调用不区分“探测”与“正式执行”，导致 fallback_counts 记录的是“评分探测次数”而非“真实排产失败次数”。后续 build_result_summary() 会把这些计数并入摆要输出，误导排障和审计。该问题不影响排产结果正确性，但影响留痕可信度。另一份进行中的 review 已报告此问题，当前仍未修复。",
      "recommendationMarkdown": "拆分 auto_assign 为“纯评估/不记数”的探测入口与“真实执行/记数”的正式入口，或为评分阶段新增显式 probe_only 开关。",
      "evidence": [
        {
          "path": "core/algorithms/greedy/dispatch/sgs.py",
          "lineStart": 216,
          "lineEnd": 248
        },
        {
          "path": "core/algorithms/greedy/auto_assign.py",
          "lineStart": 45,
          "lineEnd": 96
        },
        {
          "path": "core/algorithms/greedy/dispatch/sgs.py"
        },
        {
          "path": "core/algorithms/greedy/auto_assign.py"
        }
      ],
      "relatedMilestoneIds": [
        "M1"
      ],
      "trackingStatus": "open"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:4169f2dde2413ae1fded6e41b7c6ab5b0014141120619fef4d31e35763d5d7a1",
    "generatedAt": "2026-04-05T03:56:43.366Z",
    "locale": "zh-CN"
  }
}
```
