# 架构解耦收口整改计划三轮审核
- 日期: 2026-04-05
- 概述: 对 .limcode/plans/2026-04-05_架构解耦收口整改计划.md 进行三轮递进审核：第一轮审结构完整性与可溯源性，第二轮审技术方案准确性与风险，第三轮审排序合理性与验收矩阵。
- 状态: 已完成
- 总体结论: 有条件通过

## 评审范围

# 架构解耦收口整改计划三轮审核

- 日期：2026-04-05
- 对象：`.limcode/plans/2026-04-05_架构解耦收口整改计划.md`
- 方法：三轮递进——第一轮聚焦结构与可溯源性，第二轮聚焦技术方案准确性与实施风险，第三轮聚焦排序合理性与验收矩阵充分性。

## 评审范围

审查目标是整改计划本身（非源码），重点检验：
1. 计划是否完整、精确地响应了两份 review 的所有开放问题
2. 每个任务的修改范围描述是否与源码现状吻合
3. 步骤与验证矩阵是否充分覆盖了回退风险

## 评审摘要

- 当前状态: 已完成
- 已审模块: .limcode/plans/2026-04-05_架构解耦收口整改计划.md
- 当前进度: 已记录 3 个里程碑；最新：R3
- 里程碑总数: 3
- 已完成里程碑: 3
- 问题总数: 3
- 问题严重级别分布: 高 0 / 中 0 / 低 3
- 最新结论: ## 三轮审核总结 **结论：有条件通过，可按计划执行。** 本计划质量上乘，经过三轮递进审核，各维度评估如下： ### 第一轮：结构完整性与可溯源性 ✅ 通过 - 两份 review 的全部 8 项开放问题（3 中 + 5 低/中）均有精确对应的任务 - 5 个任务的文件范围描述与源码现状逐一验证吻合 - 先决约束、职责映射、"不做的事"边界清晰合理 ### 第二轮：技术方案准确性与风险 ⚠ 有条件通过 - 任务 1/2/4/5 方案可行且无重大风险 - **任务 3 需要在执行前澄清一个关键技术细节**：`cfg_get()` 内部有 `except Exception: pass`，窄化读取 helper 不能简单包装它，必须绕过它直接做属性/字典访问，否则无法区分"配置为空"和"读取故障" - 两处次要补充：① 错误行收集接口应明确保留 `collect_error_rows`；② `get_ui_mode()` 的 cookie 异常也应补 warning ### 第三轮：排序合理性与验收矩阵充分性 ✅ 通过 - 任务排序与先决约束一致：正确性缺口 → 一致性收口 → 静默回退治理 → 死代码删除 → 入口简化 - 10 条验收测试覆盖了 5 个任务的所有核心改动路径 - 建议在最终验证矩阵中补入 `regression_excel_import_result_semantics.py` 和 `regression_dict_cfg_contract.py` ### 执行建议 1. 执行者在开始任务 3 前，应先在步骤 3 的实现描述中明确"窄化读取 helper 不经过 `cfg_get()`，直接做 `dict.get` / `getattr` 访问" 2. 任务 2 步骤 3 中明确保留 `collect_error_rows` 作为唯一公开名称 3. 任务 3 步骤 3 中顺带处理 `get_ui_mode()` 的 cookie 读取异常 4. 最终验证矩阵补入上述两个回归测试 5. 其余可按原计划顺序直接执行，无阻塞项
- 下一步建议: 按计划顺序执行，在开始任务 3 前先澄清窄化读取 helper 的实现方式（绕过 cfg_get 直接访问），并在最终验证矩阵中补入 regression_excel_import_result_semantics.py 和 regression_dict_cfg_contract.py。
- 总体结论: 有条件通过

## 评审发现

### [中] 窄化读取 helper 需绕过 cfg_get 内部静默吞异常：config_adapter.py 的 cfg_get() 自身在第 18 行和第 24 行有 except Exception: pass，如果窄化 helper 只是包装 cfg_get()，仍无法检测到底层真实故障。实现时必须绕过 cfg_get() 直接进行属性/字典访问。

- ID: F-other-1
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: R2

### [低] 错误行收集接口收敛方向未明确指定保留名称：extract_error_rows 是原始实现，collect_error_rows 是薄包装，12 个路由都用后者。应明确保留 collect_error_rows，将 extract_error_rows 加入删除清单。

- ID: F-other-2
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: R2

### [低] cookie 读取异常也被静默吞掉：get_ui_mode() 第 214 行 except Exception: pass 也会吞掉 cookie 读取异常，虽然风险低于 DB 读取，但与先决约束不完全一致。

- ID: F-other-3
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: R2

## 评审里程碑

### R1 · 第一轮：结构完整性与可溯源性审核

- 状态: 已完成
- 记录时间: 2026-04-05T09:42:30.073Z
- 已审模块: .limcode/plans/2026-04-05_架构解耦收口整改计划.md
- 摘要:

  ### 审核维度

  检查计划是否完整响应了两份 review 的全部 8 项开放问题（3 项来自架构解耦整改实现深度审查、5 项来自未提交修改深度审查），以及每个任务的范围描述是否与源码现状一致。

  ### 问题-任务覆盖矩阵

  | review 开放问题 | 对应 plan 任务 | 覆盖判定 |
  |---|---|---|
  | `dead-aliases-in-excel-utils`（低） | 任务 2 步骤 3 | ✅ 精确对应 |
  | `summary-compat-verbose`（低） | 任务 4 步骤 2 | ✅ 精确对应 |
  | `entry-over-engineering`（中） | 任务 5 | ✅ 精确对应 |
  | `scheduler-param-silent-fallback`（中） | 任务 3 | ✅ 精确对应 |
  | `schedule-wrapper-drift`（低） | 任务 4 步骤 1-2 | ✅ 精确对应 |
  | `ui-mode-silent-degradation`（中） | 任务 3 | ✅ 精确对应 |
  | `process-routes-missing-extra-state`（中） | 任务 1 | ✅ 精确对应 |
  | `excel-demo-bypasses-upload-guard`（低） | 任务 2 | ✅ 精确对应 |

  **结论：8/8 开放问题全部有对应任务，无遗漏，无越界。**

  ### 源码现状与范围描述一致性

  1. **任务 1**：`process_excel_routes.py` 第 134 行的 `build_preview_baseline_token(...)` 确实未传入 `extra_state`，与其他 6 个已补齐 `extra_state` 的导入路由（供应商、工种、工序工时、人员设备关联、设备导入、工作日历）形成明显对比。范围描述准确。

  2. **任务 2**：`excel_demo.py` 第 110-155 行确实存在 `file.read() + BytesIO + openpyxl.load_workbook()` 独立读取链，绕过了 `read_uploaded_xlsx()`；`excel_utils.py` 第 165-168 行的 4 个私有别名经搜索确认无外部消费者。范围描述准确。

  3. **任务 3**：`schedule_params.py` 第 67-71、118-122、138-142、164-168 行确实存在 4 处 `try: cfg_get(...) except: 默认值` 静默吞异常；`ui_mode.py` 第 194 行 `except Exception: return None` 和第 294 行 `except Exception: return None` 确实无任何告警日志。范围描述准确。

  4. **任务 4**：`schedule_service.py` 第 63-122 行两个兼容包装确实只在定义处出现，第 337-338 行传给 `collect_schedule_run_input` 的是原始函数而非包装函数。`schedule_summary.py` 第 367-377 行 `svc` 提取逻辑确实在 `if ctx is None` 和 `else` 两个分支中完全重复。范围描述准确。

  5. **任务 5**：`app.py` 和 `app_new_ui.py` 确实 95% 相同（唯一差异是 `"default"` vs `"new_ui"`）。`_module_globals()` 占 21 行，`entrypoint.py` 的 `deps_from_module_globals()` 占 33 行。现有测试确实通过 monkey-patching 入口模块全局变量实现，而非通过 `deps=` 参数注入。范围描述准确。

  ### 结构性评价

  - **先决约束**明确了优先级：正确性缺口 > 静默回退 > 净化删除 > 入口简化。与两份 review 的严重级别排序一致。
  - **文件与职责映射**清晰列出了每个修改文件的用途和本轮职责边界。
  - **"不做的事"**合理：不重写 Excel 框架、不全量改 `cfg_get`、不回填文档。这能有效控制改动范围。
  - 每个任务都遵循"先写失败用例 → 确认失败原因 → 最小实现 → 定向验证 → 受影响回归 → 收尾"的固定模式，结构统一，执行者不会遗漏步骤。
- 结论:

  第一轮通过。计划完整覆盖了两份 review 的全部开放问题，范围描述与源码现状高度吻合，结构规范且优先级排序合理。
- 证据:
  - `web/routes/process_excel_routes.py:134#build_preview_baseline_token`
  - `web/routes/excel_demo.py:110-155#preview`
  - `web/routes/excel_utils.py:165-168`
  - `core/algorithms/greedy/schedule_params.py:67-72`
  - `web/ui_mode.py:182-295`
  - `core/services/scheduler/schedule_service.py:63-122`
  - `core/services/scheduler/schedule_summary.py:360-381`
  - `app.py`
  - `app_new_ui.py`
  - `web/bootstrap/entrypoint.py:100-133`

### R2 · 第二轮：技术方案准确性与实施风险审核

- 状态: 已完成
- 记录时间: 2026-04-05T09:43:37.441Z
- 摘要:

  ### 审核维度

  逐一审核 5 个任务的技术方案是否在源码现状下可行、是否存在实施风险、是否有遗漏。

  ### 任务 1：方案可行，无重大风险

  - `RouteParser.parse()` 的解析结果确实依赖两组运行时数据：① `self.op_types_repo.list()` 返回的工种列表（name/category 影响内外部判定），② `self._build_supplier_map()` 返回的供应商映射（supplier_id/default_days 影响外协周期初始化）。
  - 计划建议的快照字段（`op_type_id/name/category` + `supplier_id/op_type_id/default_days/status` + `strict_mode`）准确覆盖了所有影响解析结果的输入因子。
  - 已有的供应商导入页 `extra_state=_supplier_op_type_snapshot()` 提供了现成模式可参照。
  - 快照需按稳定主键排序，计划已明确提到"按稳定顺序输出"，风险已覆盖。

  ### 任务 2：方案可行，一处描述需澄清

  - `read_uploaded_xlsx()` 返回格式与 `excel_demo.preview()` 手工解析输出完全一致，切换无数据契约风险。
  - `get_excel_backend()` 是公开工厂函数，所有正式路由都在使用。
  - `excel_demo.py` 确认无任何对 `read_uploaded_xlsx` 的引用。
  - 但步骤 3 对 `extract_error_rows` / `collect_error_rows` 的收敛方向不够精确。

  ### 任务 3：方向正确，一处关键技术细节需在执行前澄清

  - `cfg_get()` 自身在第 18、24 行有 `except Exception: pass`，会把底层异常吞掉并返回 `default`。
  - 如果窄化 helper 只包装 `cfg_get()`，仍检测不到真实故障。必须绕过 `cfg_get()` 直接访问。
  - `ui_mode.py` 方案合理，但 `get_ui_mode()` 第 214 行 cookie 异常也被静默吞掉，与先决约束不完全一致。

  ### 任务 4：简单可靠，无风险

  - 搜索确认两个兼容包装在 `schedule_service.py` 中只有定义、无调用。删除安全。
  - `svc` 提取重构是纯机械操作。

  ### 任务 5：方案可行，测试改造工作量需注意

  - `app_main()` 已有 `deps=` 参数，删除 `module_globals` 后入口文件可缩减到约 15 行。
  - 测试改造约 40-50 行重写，逻辑更清晰。
  - 入口文件从 67 行缩减到约 15 行，修改触发点从 5 个降到 2 个。

  ### 跨任务风险评估

  - 任务 1 和任务 2 共享 `excel_utils.py` 的 `build_preview_baseline_token`，但任务 2 不修改该函数，无冲突。
  - 任务 3 和任务 4 修改的部分不交叉。
  - 任务 5 独立于前 4 个任务。
- 结论:

  第二轮有条件通过。5 个任务的技术方案总体可行且自洽，但任务 3 的配置读取改造方案存在一个需要在执行前澄清的关键技术细节（`cfg_get` 内部的静默吞异常可能导致窄化读取 helper 检测不到真实故障），以及两处次要的完整性补充建议。
- 证据:
  - `core/services/process/route_parser.py:137-255#RouteParser.parse`
  - `core/algorithms/greedy/config_adapter.py:6-26#cfg_get`
  - `web/routes/process_excel_suppliers.py:169-174`
  - `web/routes/excel_utils.py:98-99#extract_error_rows`
  - `web/routes/excel_utils.py:144-145#collect_error_rows`
  - `web/bootstrap/entrypoint.py:176-186#app_main`
- 问题:
  - [低] 其他: [中] 窄化读取 helper 需绕过 cfg_get 内部静默吞异常：config_adapter.py 的 cfg_get() 自身在第 18 行和第 24 行有 except Exception: pass，如果窄化 helper 只是包装 cfg_get()，仍无法检测到底层真实故障。实现时必须绕过 cfg_get() 直接进行属性/字典访问。
  - [低] 其他: [低] 错误行收集接口收敛方向未明确指定保留名称：extract_error_rows 是原始实现，collect_error_rows 是薄包装，12 个路由都用后者。应明确保留 collect_error_rows，将 extract_error_rows 加入删除清单。
  - [低] 其他: [低] cookie 读取异常也被静默吞掉：get_ui_mode() 第 214 行 except Exception: pass 也会吞掉 cookie 读取异常，虽然风险低于 DB 读取，但与先决约束不完全一致。

### R3 · 第三轮：排序合理性与验收矩阵充分性审核

- 状态: 已完成
- 记录时间: 2026-04-05T09:43:53.180Z
- 摘要:

  ### 审核维度

  检验 5 个任务的排序是否正确反映了先决约束，以及最终验证矩阵是否充分覆盖了所有改动的影响面。

  ### 任务排序评估

  计划的排序为：任务 1（正确性缺口） → 任务 2（一致性收口） → 任务 3（静默回退治理） → 任务 4（死代码删除） → 任务 5（入口简化）。

  与先决约束的一致性分析：

  | 先决约束 | 对应任务 | 排序位置 | 评价 |
  |---|---|---|---|
  | 正确性缺口优先修复 | 任务 1（预览/确认不一致） | #1 | ✅ 正确排在最前 |
  | 静默回退治理 | 任务 3 | #3 | ✅ 在正确性之后、净化之前 |
  | 净化删除不先于正确性 | 任务 2（死别名）、任务 4（死代码） | #2、#4 | ⚠ 任务 2 排在任务 3 前面，但任务 2 兼有一致性修复（演示页上传链路统一），不是纯净化，排序可接受 |
  | 入口简化最后 | 任务 5 | #5 | ✅ 正确排在最后 |

  **结论：排序合理，与先决约束一致。**

  ### 验收矩阵充分性

  最终验证矩阵包含 10 条测试，逐一分析覆盖情况：

  | 验证项 | 覆盖任务 | 验证类型 | 评价 |
  |---|---|---|---|
  | `regression_process_excel_routes_extra_state_guard.py` | 任务 1 | 新增定向测试 | ✅ 覆盖核心修复 |
  | `regression_excel_demo_upload_guard.py` | 任务 2 | 新增定向测试 | ✅ 覆盖核心修复 |
  | `regression_schedule_params_read_failure_visible.py` | 任务 3 | 新增定向测试 | ✅ 覆盖核心修复 |
  | `test_ui_mode.py` | 任务 3 | 已有测试扩展 | ✅ 覆盖可观测性 |
  | `regression_schedule_summary_v11_contract.py` | 任务 4 | 已有回归 | ✅ 覆盖摘要契约 |
  | `regression_schedule_orchestrator_contract.py` | 任务 4 | 已有回归 | ✅ 覆盖编排层 |
  | `regression_schedule_service_passes_algo_stats_to_summary.py` | 任务 3+4 | 已有回归 | ✅ 跨任务覆盖 |
  | `regression_runtime_lock_reloader_parent_skip.py` | 任务 5 | 已有测试改造 | ✅ 覆盖入口行为 |
  | `regression_excel_preview_confirm_extra_state_guard.py` | 任务 1 间接 | 已有回归 | ✅ 覆盖通用基线机制 |
  | `regression_batch_excel_preview_confirm_strict_mode_extra_state_guard.py` | 任务 1 间接 | 已有回归 | ✅ 覆盖严格模式基线 |

  **覆盖评估：10 条测试覆盖了 5 个任务的所有核心改动路径和关键回归面。**

  ### 人工复核点评估

  计划列出的 5 个人工复核点（`preview_baseline` 含快照、`excel_demo` 无直读、参数读取失败可观测、`ui_mode` 有告警、入口无 `_module_globals`）均可通过简单的代码搜索或 grep 验证，操作性强。

  ### 可能的覆盖盲区

  1. **任务 2 对 `excel_utils.py` 删除死别名后的影响**：矩阵中 `regression_excel_import_result_semantics.py` 和 `test_team_pages_excel_smoke.py` 在任务 2 的局部步骤中列出，但未纳入最终验证矩阵。建议至少补入其中一个。
  2. **任务 3 改动 `config_adapter.py` 后对其他 `cfg_get()` 消费者的影响**：计划明确说"不改 `cfg_get()` 全局语义"，只新增独立函数。但最终验证矩阵中 `regression_dict_cfg_contract.py` 虽在任务 3 的局部回归中，也未纳入最终矩阵。建议补入。

  ### 总体判定

  计划质量上乘：问题覆盖完整、源码引用精确、任务排序合理、步骤模式统一、验收矩阵基本充分。存在三个可改进点（均为低或中等严重度），均不阻塞执行。
- 结论:

  第三轮通过。任务排序合理，验收矩阵覆盖了每个任务的核心改动路径，但建议补充两个回归测试以覆盖跨任务影响区域。
- 下一步建议:

  执行者在开始任务 3 前，应先在步骤 3 的实现描述中明确"窄化读取 helper 不经过 cfg_get()，而是直接做 dict.get / getattr 访问"。其余可按原计划顺序执行。

## 最终结论

## 三轮审核总结

**结论：有条件通过，可按计划执行。**

本计划质量上乘，经过三轮递进审核，各维度评估如下：

### 第一轮：结构完整性与可溯源性 ✅ 通过

- 两份 review 的全部 8 项开放问题（3 中 + 5 低/中）均有精确对应的任务
- 5 个任务的文件范围描述与源码现状逐一验证吻合
- 先决约束、职责映射、"不做的事"边界清晰合理

### 第二轮：技术方案准确性与风险 ⚠ 有条件通过

- 任务 1/2/4/5 方案可行且无重大风险
- **任务 3 需要在执行前澄清一个关键技术细节**：`cfg_get()` 内部有 `except Exception: pass`，窄化读取 helper 不能简单包装它，必须绕过它直接做属性/字典访问，否则无法区分"配置为空"和"读取故障"
- 两处次要补充：① 错误行收集接口应明确保留 `collect_error_rows`；② `get_ui_mode()` 的 cookie 异常也应补 warning

### 第三轮：排序合理性与验收矩阵充分性 ✅ 通过

- 任务排序与先决约束一致：正确性缺口 → 一致性收口 → 静默回退治理 → 死代码删除 → 入口简化
- 10 条验收测试覆盖了 5 个任务的所有核心改动路径
- 建议在最终验证矩阵中补入 `regression_excel_import_result_semantics.py` 和 `regression_dict_cfg_contract.py`

### 执行建议

1. 执行者在开始任务 3 前，应先在步骤 3 的实现描述中明确"窄化读取 helper 不经过 `cfg_get()`，直接做 `dict.get` / `getattr` 访问"
2. 任务 2 步骤 3 中明确保留 `collect_error_rows` 作为唯一公开名称
3. 任务 3 步骤 3 中顺带处理 `get_ui_mode()` 的 cookie 读取异常
4. 最终验证矩阵补入上述两个回归测试
5. 其余可按原计划顺序直接执行，无阻塞项

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mnlkon5f-efmhhz",
  "createdAt": "2026-04-05T00:00:00.000Z",
  "updatedAt": "2026-04-05T09:44:04.985Z",
  "finalizedAt": "2026-04-05T09:44:04.985Z",
  "status": "completed",
  "overallDecision": "conditionally_accepted",
  "header": {
    "title": "架构解耦收口整改计划三轮审核",
    "date": "2026-04-05",
    "overview": "对 .limcode/plans/2026-04-05_架构解耦收口整改计划.md 进行三轮递进审核：第一轮审结构完整性与可溯源性，第二轮审技术方案准确性与风险，第三轮审排序合理性与验收矩阵。"
  },
  "scope": {
    "markdown": "# 架构解耦收口整改计划三轮审核\n\n- 日期：2026-04-05\n- 对象：`.limcode/plans/2026-04-05_架构解耦收口整改计划.md`\n- 方法：三轮递进——第一轮聚焦结构与可溯源性，第二轮聚焦技术方案准确性与实施风险，第三轮聚焦排序合理性与验收矩阵充分性。\n\n## 评审范围\n\n审查目标是整改计划本身（非源码），重点检验：\n1. 计划是否完整、精确地响应了两份 review 的所有开放问题\n2. 每个任务的修改范围描述是否与源码现状吻合\n3. 步骤与验证矩阵是否充分覆盖了回退风险"
  },
  "summary": {
    "latestConclusion": "## 三轮审核总结\n\n**结论：有条件通过，可按计划执行。**\n\n本计划质量上乘，经过三轮递进审核，各维度评估如下：\n\n### 第一轮：结构完整性与可溯源性 ✅ 通过\n\n- 两份 review 的全部 8 项开放问题（3 中 + 5 低/中）均有精确对应的任务\n- 5 个任务的文件范围描述与源码现状逐一验证吻合\n- 先决约束、职责映射、\"不做的事\"边界清晰合理\n\n### 第二轮：技术方案准确性与风险 ⚠ 有条件通过\n\n- 任务 1/2/4/5 方案可行且无重大风险\n- **任务 3 需要在执行前澄清一个关键技术细节**：`cfg_get()` 内部有 `except Exception: pass`，窄化读取 helper 不能简单包装它，必须绕过它直接做属性/字典访问，否则无法区分\"配置为空\"和\"读取故障\"\n- 两处次要补充：① 错误行收集接口应明确保留 `collect_error_rows`；② `get_ui_mode()` 的 cookie 异常也应补 warning\n\n### 第三轮：排序合理性与验收矩阵充分性 ✅ 通过\n\n- 任务排序与先决约束一致：正确性缺口 → 一致性收口 → 静默回退治理 → 死代码删除 → 入口简化\n- 10 条验收测试覆盖了 5 个任务的所有核心改动路径\n- 建议在最终验证矩阵中补入 `regression_excel_import_result_semantics.py` 和 `regression_dict_cfg_contract.py`\n\n### 执行建议\n\n1. 执行者在开始任务 3 前，应先在步骤 3 的实现描述中明确\"窄化读取 helper 不经过 `cfg_get()`，直接做 `dict.get` / `getattr` 访问\"\n2. 任务 2 步骤 3 中明确保留 `collect_error_rows` 作为唯一公开名称\n3. 任务 3 步骤 3 中顺带处理 `get_ui_mode()` 的 cookie 读取异常\n4. 最终验证矩阵补入上述两个回归测试\n5. 其余可按原计划顺序直接执行，无阻塞项",
    "recommendedNextAction": "按计划顺序执行，在开始任务 3 前先澄清窄化读取 helper 的实现方式（绕过 cfg_get 直接访问），并在最终验证矩阵中补入 regression_excel_import_result_semantics.py 和 regression_dict_cfg_contract.py。",
    "reviewedModules": [
      ".limcode/plans/2026-04-05_架构解耦收口整改计划.md"
    ]
  },
  "stats": {
    "totalMilestones": 3,
    "completedMilestones": 3,
    "totalFindings": 3,
    "severity": {
      "high": 0,
      "medium": 0,
      "low": 3
    }
  },
  "milestones": [
    {
      "id": "R1",
      "title": "第一轮：结构完整性与可溯源性审核",
      "status": "completed",
      "recordedAt": "2026-04-05T09:42:30.073Z",
      "summaryMarkdown": "### 审核维度\n\n检查计划是否完整响应了两份 review 的全部 8 项开放问题（3 项来自架构解耦整改实现深度审查、5 项来自未提交修改深度审查），以及每个任务的范围描述是否与源码现状一致。\n\n### 问题-任务覆盖矩阵\n\n| review 开放问题 | 对应 plan 任务 | 覆盖判定 |\n|---|---|---|\n| `dead-aliases-in-excel-utils`（低） | 任务 2 步骤 3 | ✅ 精确对应 |\n| `summary-compat-verbose`（低） | 任务 4 步骤 2 | ✅ 精确对应 |\n| `entry-over-engineering`（中） | 任务 5 | ✅ 精确对应 |\n| `scheduler-param-silent-fallback`（中） | 任务 3 | ✅ 精确对应 |\n| `schedule-wrapper-drift`（低） | 任务 4 步骤 1-2 | ✅ 精确对应 |\n| `ui-mode-silent-degradation`（中） | 任务 3 | ✅ 精确对应 |\n| `process-routes-missing-extra-state`（中） | 任务 1 | ✅ 精确对应 |\n| `excel-demo-bypasses-upload-guard`（低） | 任务 2 | ✅ 精确对应 |\n\n**结论：8/8 开放问题全部有对应任务，无遗漏，无越界。**\n\n### 源码现状与范围描述一致性\n\n1. **任务 1**：`process_excel_routes.py` 第 134 行的 `build_preview_baseline_token(...)` 确实未传入 `extra_state`，与其他 6 个已补齐 `extra_state` 的导入路由（供应商、工种、工序工时、人员设备关联、设备导入、工作日历）形成明显对比。范围描述准确。\n\n2. **任务 2**：`excel_demo.py` 第 110-155 行确实存在 `file.read() + BytesIO + openpyxl.load_workbook()` 独立读取链，绕过了 `read_uploaded_xlsx()`；`excel_utils.py` 第 165-168 行的 4 个私有别名经搜索确认无外部消费者。范围描述准确。\n\n3. **任务 3**：`schedule_params.py` 第 67-71、118-122、138-142、164-168 行确实存在 4 处 `try: cfg_get(...) except: 默认值` 静默吞异常；`ui_mode.py` 第 194 行 `except Exception: return None` 和第 294 行 `except Exception: return None` 确实无任何告警日志。范围描述准确。\n\n4. **任务 4**：`schedule_service.py` 第 63-122 行两个兼容包装确实只在定义处出现，第 337-338 行传给 `collect_schedule_run_input` 的是原始函数而非包装函数。`schedule_summary.py` 第 367-377 行 `svc` 提取逻辑确实在 `if ctx is None` 和 `else` 两个分支中完全重复。范围描述准确。\n\n5. **任务 5**：`app.py` 和 `app_new_ui.py` 确实 95% 相同（唯一差异是 `\"default\"` vs `\"new_ui\"`）。`_module_globals()` 占 21 行，`entrypoint.py` 的 `deps_from_module_globals()` 占 33 行。现有测试确实通过 monkey-patching 入口模块全局变量实现，而非通过 `deps=` 参数注入。范围描述准确。\n\n### 结构性评价\n\n- **先决约束**明确了优先级：正确性缺口 > 静默回退 > 净化删除 > 入口简化。与两份 review 的严重级别排序一致。\n- **文件与职责映射**清晰列出了每个修改文件的用途和本轮职责边界。\n- **\"不做的事\"**合理：不重写 Excel 框架、不全量改 `cfg_get`、不回填文档。这能有效控制改动范围。\n- 每个任务都遵循\"先写失败用例 → 确认失败原因 → 最小实现 → 定向验证 → 受影响回归 → 收尾\"的固定模式，结构统一，执行者不会遗漏步骤。",
      "conclusionMarkdown": "第一轮通过。计划完整覆盖了两份 review 的全部开放问题，范围描述与源码现状高度吻合，结构规范且优先级排序合理。",
      "evidence": [
        {
          "path": "web/routes/process_excel_routes.py",
          "lineStart": 134,
          "lineEnd": 134,
          "symbol": "build_preview_baseline_token"
        },
        {
          "path": "web/routes/excel_demo.py",
          "lineStart": 110,
          "lineEnd": 155,
          "symbol": "preview"
        },
        {
          "path": "web/routes/excel_utils.py",
          "lineStart": 165,
          "lineEnd": 168
        },
        {
          "path": "core/algorithms/greedy/schedule_params.py",
          "lineStart": 67,
          "lineEnd": 72
        },
        {
          "path": "web/ui_mode.py",
          "lineStart": 182,
          "lineEnd": 295
        },
        {
          "path": "core/services/scheduler/schedule_service.py",
          "lineStart": 63,
          "lineEnd": 122
        },
        {
          "path": "core/services/scheduler/schedule_summary.py",
          "lineStart": 360,
          "lineEnd": 381
        },
        {
          "path": "app.py"
        },
        {
          "path": "app_new_ui.py"
        },
        {
          "path": "web/bootstrap/entrypoint.py",
          "lineStart": 100,
          "lineEnd": 133
        }
      ],
      "reviewedModules": [
        ".limcode/plans/2026-04-05_架构解耦收口整改计划.md"
      ],
      "recommendedNextAction": null,
      "findingIds": []
    },
    {
      "id": "R2",
      "title": "第二轮：技术方案准确性与实施风险审核",
      "status": "completed",
      "recordedAt": "2026-04-05T09:43:37.441Z",
      "summaryMarkdown": "### 审核维度\n\n逐一审核 5 个任务的技术方案是否在源码现状下可行、是否存在实施风险、是否有遗漏。\n\n### 任务 1：方案可行，无重大风险\n\n- `RouteParser.parse()` 的解析结果确实依赖两组运行时数据：① `self.op_types_repo.list()` 返回的工种列表（name/category 影响内外部判定），② `self._build_supplier_map()` 返回的供应商映射（supplier_id/default_days 影响外协周期初始化）。\n- 计划建议的快照字段（`op_type_id/name/category` + `supplier_id/op_type_id/default_days/status` + `strict_mode`）准确覆盖了所有影响解析结果的输入因子。\n- 已有的供应商导入页 `extra_state=_supplier_op_type_snapshot()` 提供了现成模式可参照。\n- 快照需按稳定主键排序，计划已明确提到\"按稳定顺序输出\"，风险已覆盖。\n\n### 任务 2：方案可行，一处描述需澄清\n\n- `read_uploaded_xlsx()` 返回格式与 `excel_demo.preview()` 手工解析输出完全一致，切换无数据契约风险。\n- `get_excel_backend()` 是公开工厂函数，所有正式路由都在使用。\n- `excel_demo.py` 确认无任何对 `read_uploaded_xlsx` 的引用。\n- 但步骤 3 对 `extract_error_rows` / `collect_error_rows` 的收敛方向不够精确。\n\n### 任务 3：方向正确，一处关键技术细节需在执行前澄清\n\n- `cfg_get()` 自身在第 18、24 行有 `except Exception: pass`，会把底层异常吞掉并返回 `default`。\n- 如果窄化 helper 只包装 `cfg_get()`，仍检测不到真实故障。必须绕过 `cfg_get()` 直接访问。\n- `ui_mode.py` 方案合理，但 `get_ui_mode()` 第 214 行 cookie 异常也被静默吞掉，与先决约束不完全一致。\n\n### 任务 4：简单可靠，无风险\n\n- 搜索确认两个兼容包装在 `schedule_service.py` 中只有定义、无调用。删除安全。\n- `svc` 提取重构是纯机械操作。\n\n### 任务 5：方案可行，测试改造工作量需注意\n\n- `app_main()` 已有 `deps=` 参数，删除 `module_globals` 后入口文件可缩减到约 15 行。\n- 测试改造约 40-50 行重写，逻辑更清晰。\n- 入口文件从 67 行缩减到约 15 行，修改触发点从 5 个降到 2 个。\n\n### 跨任务风险评估\n\n- 任务 1 和任务 2 共享 `excel_utils.py` 的 `build_preview_baseline_token`，但任务 2 不修改该函数，无冲突。\n- 任务 3 和任务 4 修改的部分不交叉。\n- 任务 5 独立于前 4 个任务。",
      "conclusionMarkdown": "第二轮有条件通过。5 个任务的技术方案总体可行且自洽，但任务 3 的配置读取改造方案存在一个需要在执行前澄清的关键技术细节（`cfg_get` 内部的静默吞异常可能导致窄化读取 helper 检测不到真实故障），以及两处次要的完整性补充建议。",
      "evidence": [
        {
          "path": "core/services/process/route_parser.py",
          "lineStart": 137,
          "lineEnd": 255,
          "symbol": "RouteParser.parse"
        },
        {
          "path": "core/algorithms/greedy/config_adapter.py",
          "lineStart": 6,
          "lineEnd": 26,
          "symbol": "cfg_get"
        },
        {
          "path": "web/routes/process_excel_suppliers.py",
          "lineStart": 169,
          "lineEnd": 174
        },
        {
          "path": "web/routes/excel_utils.py",
          "lineStart": 98,
          "lineEnd": 99,
          "symbol": "extract_error_rows"
        },
        {
          "path": "web/routes/excel_utils.py",
          "lineStart": 144,
          "lineEnd": 145,
          "symbol": "collect_error_rows"
        },
        {
          "path": "web/bootstrap/entrypoint.py",
          "lineStart": 176,
          "lineEnd": 186,
          "symbol": "app_main"
        }
      ],
      "reviewedModules": [],
      "recommendedNextAction": null,
      "findingIds": [
        "F-other-1",
        "F-other-2",
        "F-other-3"
      ]
    },
    {
      "id": "R3",
      "title": "第三轮：排序合理性与验收矩阵充分性审核",
      "status": "completed",
      "recordedAt": "2026-04-05T09:43:53.180Z",
      "summaryMarkdown": "### 审核维度\n\n检验 5 个任务的排序是否正确反映了先决约束，以及最终验证矩阵是否充分覆盖了所有改动的影响面。\n\n### 任务排序评估\n\n计划的排序为：任务 1（正确性缺口） → 任务 2（一致性收口） → 任务 3（静默回退治理） → 任务 4（死代码删除） → 任务 5（入口简化）。\n\n与先决约束的一致性分析：\n\n| 先决约束 | 对应任务 | 排序位置 | 评价 |\n|---|---|---|---|\n| 正确性缺口优先修复 | 任务 1（预览/确认不一致） | #1 | ✅ 正确排在最前 |\n| 静默回退治理 | 任务 3 | #3 | ✅ 在正确性之后、净化之前 |\n| 净化删除不先于正确性 | 任务 2（死别名）、任务 4（死代码） | #2、#4 | ⚠ 任务 2 排在任务 3 前面，但任务 2 兼有一致性修复（演示页上传链路统一），不是纯净化，排序可接受 |\n| 入口简化最后 | 任务 5 | #5 | ✅ 正确排在最后 |\n\n**结论：排序合理，与先决约束一致。**\n\n### 验收矩阵充分性\n\n最终验证矩阵包含 10 条测试，逐一分析覆盖情况：\n\n| 验证项 | 覆盖任务 | 验证类型 | 评价 |\n|---|---|---|---|\n| `regression_process_excel_routes_extra_state_guard.py` | 任务 1 | 新增定向测试 | ✅ 覆盖核心修复 |\n| `regression_excel_demo_upload_guard.py` | 任务 2 | 新增定向测试 | ✅ 覆盖核心修复 |\n| `regression_schedule_params_read_failure_visible.py` | 任务 3 | 新增定向测试 | ✅ 覆盖核心修复 |\n| `test_ui_mode.py` | 任务 3 | 已有测试扩展 | ✅ 覆盖可观测性 |\n| `regression_schedule_summary_v11_contract.py` | 任务 4 | 已有回归 | ✅ 覆盖摘要契约 |\n| `regression_schedule_orchestrator_contract.py` | 任务 4 | 已有回归 | ✅ 覆盖编排层 |\n| `regression_schedule_service_passes_algo_stats_to_summary.py` | 任务 3+4 | 已有回归 | ✅ 跨任务覆盖 |\n| `regression_runtime_lock_reloader_parent_skip.py` | 任务 5 | 已有测试改造 | ✅ 覆盖入口行为 |\n| `regression_excel_preview_confirm_extra_state_guard.py` | 任务 1 间接 | 已有回归 | ✅ 覆盖通用基线机制 |\n| `regression_batch_excel_preview_confirm_strict_mode_extra_state_guard.py` | 任务 1 间接 | 已有回归 | ✅ 覆盖严格模式基线 |\n\n**覆盖评估：10 条测试覆盖了 5 个任务的所有核心改动路径和关键回归面。**\n\n### 人工复核点评估\n\n计划列出的 5 个人工复核点（`preview_baseline` 含快照、`excel_demo` 无直读、参数读取失败可观测、`ui_mode` 有告警、入口无 `_module_globals`）均可通过简单的代码搜索或 grep 验证，操作性强。\n\n### 可能的覆盖盲区\n\n1. **任务 2 对 `excel_utils.py` 删除死别名后的影响**：矩阵中 `regression_excel_import_result_semantics.py` 和 `test_team_pages_excel_smoke.py` 在任务 2 的局部步骤中列出，但未纳入最终验证矩阵。建议至少补入其中一个。\n2. **任务 3 改动 `config_adapter.py` 后对其他 `cfg_get()` 消费者的影响**：计划明确说\"不改 `cfg_get()` 全局语义\"，只新增独立函数。但最终验证矩阵中 `regression_dict_cfg_contract.py` 虽在任务 3 的局部回归中，也未纳入最终矩阵。建议补入。\n\n### 总体判定\n\n计划质量上乘：问题覆盖完整、源码引用精确、任务排序合理、步骤模式统一、验收矩阵基本充分。存在三个可改进点（均为低或中等严重度），均不阻塞执行。",
      "conclusionMarkdown": "第三轮通过。任务排序合理，验收矩阵覆盖了每个任务的核心改动路径，但建议补充两个回归测试以覆盖跨任务影响区域。",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": "执行者在开始任务 3 前，应先在步骤 3 的实现描述中明确\"窄化读取 helper 不经过 cfg_get()，而是直接做 dict.get / getattr 访问\"。其余可按原计划顺序执行。",
      "findingIds": []
    }
  ],
  "findings": [
    {
      "id": "F-other-1",
      "severity": "low",
      "category": "other",
      "title": "[中] 窄化读取 helper 需绕过 cfg_get 内部静默吞异常：config_adapter.py 的 cfg_get() 自身在第 18 行和第 24 行有 except Exception: pass，如果窄化 helper 只是包装 cfg_get()，仍无法检测到底层真实故障。实现时必须绕过 cfg_get() 直接进行属性/字典访问。",
      "descriptionMarkdown": null,
      "recommendationMarkdown": null,
      "evidence": [],
      "relatedMilestoneIds": [
        "R2"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-other-2",
      "severity": "low",
      "category": "other",
      "title": "[低] 错误行收集接口收敛方向未明确指定保留名称：extract_error_rows 是原始实现，collect_error_rows 是薄包装，12 个路由都用后者。应明确保留 collect_error_rows，将 extract_error_rows 加入删除清单。",
      "descriptionMarkdown": null,
      "recommendationMarkdown": null,
      "evidence": [],
      "relatedMilestoneIds": [
        "R2"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-other-3",
      "severity": "low",
      "category": "other",
      "title": "[低] cookie 读取异常也被静默吞掉：get_ui_mode() 第 214 行 except Exception: pass 也会吞掉 cookie 读取异常，虽然风险低于 DB 读取，但与先决约束不完全一致。",
      "descriptionMarkdown": null,
      "recommendationMarkdown": null,
      "evidence": [],
      "relatedMilestoneIds": [
        "R2"
      ],
      "trackingStatus": "open"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:2882e8513b3ffa075a835e5065e47fdf2e14a113ac07fd0cd2ea04d0eef64ea0",
    "generatedAt": "2026-04-05T09:44:04.985Z",
    "locale": "zh-CN"
  }
}
```
