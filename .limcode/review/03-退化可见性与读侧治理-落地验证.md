# 03_退化可见性与读侧治理_落地验证
- 日期: 2026-04-03
- 概述: 对照计划文档 03_退化可见性与读侧治理 检查已勾选项的代码落地情况
- 状态: 已完成
- 总体结论: 有条件通过

## 评审范围

# 03_退化可见性与读侧治理 — 代码落地验证审查

**审查日期**: 2026-04-05
**审查范围**: 计划文档中 10 项待办（#p03-1 ~ #p03-10），重点验证已勾选的 9 项代码是否真正符合计划要求。
**未勾选项**: #p03-3（甘特日历失败透传）标记为未完成，本次审查跳过。

## 评审摘要

- 当前状态: 已完成
- 已审模块: core/algorithms/evaluation.py, core/algorithms/greedy/external_groups.py, core/infrastructure/backup.py, core/services/system/system_config_service.py, core/services/system/system_maintenance_service.py, core/services/scheduler/schedule_summary.py, web/routes/system_backup.py, web/routes/system_utils.py, core/services/personnel/operator_machine_query_service.py, core/services/process/unit_excel/template_builder.py, web/bootstrap/plugins.py, core/services/scheduler/schedule_optimizer_steps.py, scripts/convert_rotary_shell_unit_excel.py, templates/personnel/detail.html, templates/system/logs.html, templates/system/backup.html
- 当前进度: 已记录 2 个里程碑；最新：M2
- 里程碑总数: 2
- 已完成里程碑: 2
- 问题总数: 1
- 问题严重级别分布: 高 0 / 中 0 / 低 1
- 最新结论: ## 审查结论 对照 `03_退化可见性与读侧治理` 计划文档的 10 项待办，逐项验证了 9 项已勾选的代码落地情况（#p03-3 甘特日历失败透传标记为未完成，不在本次审查范围内）。 ### 逐项判定 | 编号 | 待办项 | 判定 | |------|--------|------| | #p03-1 | 算法指标显式统计坏交期与未排批次 | ✅ 完全符合 | | #p03-2 | 外协周期默认1.0收口为历史兼容读取 | ✅ 完全符合 | | #p03-3 | 甘特日历失败透传 | ⏳ 未勾选，跳过 | | #p03-4 | 恢复链返回码改语义 | ✅ 完全符合 | | #p03-5 | 系统配置读侧 dirty_fields/dirty_reasons | ✅ 完全符合 | | #p03-6 | 预热失败与插件启动留痕失败外显 | ✅ 完全符合 | | #p03-7 | 退化可见性回归测试覆盖 | ✅ 基本符合（1处低优先级缺口） | | #p03-8 | SystemJobState.last_run_time 脏值重分类 | ✅ 完全符合 | | #p03-9 | 人机关联读侧脏标记与页面提示 | ✅ 完全符合 | | #p03-10 | 单元产品转换诊断出口与脚本汇总 | ✅ 完全符合 | ### 总体评估 **9 项已勾选修改全部符合计划要求。** 代码落地范围覆盖了： - **算法层**：指标新增字段 + 透传到摘要/历史 - **基础设施层**：恢复链三段语义、系统配置脏值标记、维护任务脏时间三态 - **服务层**：退化事件统一收集器 + 外协周期兼容计数 + 预热失败计数 - **页面展示层**：系统设置/日志页脏值提示、人员详情页脏标记、恢复操作提示文案 - **脚本层**：转换脚本诊断汇总区分"普通成功"和"带退化成功" - **回归测试**：计划要求的 10 个必增回归全部存在，断言与对外口径一致 ### 低优先级缺口 恢复链回归测试只覆盖了 `copied_pending_verify` 状态，`verified` 最终状态缺少独立回归断言。此缺口不影响正确性，但建议后续补齐。 ### 遗留项 `#p03-3`（甘特日历失败透传到页面）尚未实施，是当前唯一未完成的待办项。
- 下一步建议: 1. 完成 #p03-3 甘特日历失败透传实施；2. 为恢复链 verified 状态补充独立回归断言
- 总体结论: 有条件通过

## 评审发现

### 恢复链缺少 verified 状态的独立回归断言

- ID: F-恢复链缺少-verified-状态的独立回归断言
- 严重级别: 低
- 分类: 测试
- 跟踪状态: 开放
- 相关里程碑: M2
- 说明:

  回归测试 `regression_backup_restore_pending_verify_code.py` 只验证了 `copied_pending_verify` 状态。由于 `verified` 状态需要通过 `ensure_schema` 成功后 `replace()` 赋值，完整端到端链条中 `code=verified` 并没有被回归测试直接覆盖。这不影响当前正确性（路由层 `system_backup.py:270` 已有该逻辑），但建议后续补一个端到端测试覆盖 `verified` 最终状态。
- 建议:

  在 `regression_backup_restore_pending_verify_code.py` 中增加对 `ensure_schema` 后 `code=verified` 的断言路径覆盖。
- 证据:
  - `tests/regression_backup_restore_pending_verify_code.py`
  - `web/routes/system_backup.py`

## 评审里程碑

### M1 · #p03-1, #p03-2, #p03-4, #p03-5, #p03-8 核心读侧治理落地验证

- 状态: 已完成
- 记录时间: 2026-04-03T16:31:01.857Z
- 已审模块: core/algorithms/evaluation.py, core/algorithms/greedy/external_groups.py, core/infrastructure/backup.py, core/services/system/system_config_service.py, core/services/system/system_maintenance_service.py, core/services/scheduler/schedule_summary.py, web/routes/system_backup.py, web/routes/system_utils.py
- 摘要:

  逐项检查了算法指标、外协周期兼容链、恢复链语义、系统配置脏值标记、维护任务脏时间五个已勾选项的代码落地情况，结论如下：

  **#p03-1（算法指标显式统计坏交期与未排批次）✅ 符合**
  - `ScheduleMetrics` 已新增 `invalid_due_count`、`unscheduled_batch_count`、`invalid_due_batch_ids_sample`、`unscheduled_batch_ids_sample` 四个字段
  - `compute_metrics()` 遍历所有批次，使用 `_parse_due_date_state()` 显式区分坏交期与正常无交期
  - 未排批次在 `finish_by_batch` 中找不到结果时显式计入
  - `build_result_summary()` 把两个计数、样本、退化事件都写入摘要和历史
  - 回归测试 `regression_schedule_summary_invalid_due_and_unscheduled_counts.py` 覆盖了关键断言

  **#p03-2（外协周期默认1.0收口为历史兼容读取）✅ 符合**
  - `external_groups.py` 使用 `parse_field_float` 带 `strict_mode` 参数，兼容模式下 fallback=1.0
  - `_record_compat_counters()` 从 `DegradationCollector` 中提取 `legacy_external_days_defaulted` 和 `blank_required` 计数并透传到 `algo_stats`
  - 严格模式下该分支会抛异常
  - `schedule_summary.py` 从 `fallback_counts` 提取计数，写入退化事件和警告
  - 回归测试覆盖了兼容模式的计数断言和严格模式的拒绝断言

  **#p03-4（恢复链返回码语义）✅ 符合**
  - `BackupManager.restore()` 复制完成后返回 `code="copied_pending_verify"`
  - `backup_restore()` 路由在 `ensure_schema` 成功后才通过 `replace()` 将 code 改为 `"verified"`
  - 校验失败时保持 `copied_pending_verify` 并给出明确提示
  - 操作日志记录 `restore_code` 字段
  - 回归测试验证了正常恢复后的 code 和提示文案

  **#p03-5（系统配置脏值标记）✅ 符合**
  - `SystemConfigSnapshot` 新增 `dirty_fields: list[str]` 和 `dirty_reasons: Dict[str, str]`
  - `_read_snapshot()` 中 `_mark_dirty()` 在归一化、钳制、回退时均记录字段和原因
  - `to_dict()` 输出脏标记
  - 系统备份页和日志页模板均正确渲染脏值提示
  - 回归测试注入了四种脏值场景并断言全部字段和原因可见

  **#p03-8（维护任务脏时间外显）✅ 符合**
  - `ParsedJobTime` 数据类区分 `missing`/`invalid`/`valid` 三种状态，保留 `raw` 原始值
  - `_is_due()` 返回 `parsed.state` 和 `parsed.raw`
  - `_get_job_state_map()` 将 `last_run_state` 和 `last_run_raw` 透传给页面
  - 系统日志页模板在 `last_run_state == 'invalid'` 时显示"时间损坏（原始值：...）"
  - 回归测试注入坏时间戳并断言页面输出包含预期文案
- 结论:

  逐项检查了算法指标、外协周期兼容链、恢复链语义、系统配置脏值标记、维护任务脏时间五个已勾选项的代码落地情况，结论如下： **#p03-1（算法指标显式统计坏交期与未排批次）✅ 符合** - `ScheduleMetrics` 已新增 `invalid_due_count`、`unscheduled_batch_count`、`invalid_due_batch_ids_sample`、`unscheduled_batch_ids_sample` 四个字段 - `compute_metrics()` 遍历所有批次，使用 `_parse_due_date_state()` 显式区分坏交期与正常无交期 - 未排批次在 `finish_by_batch` 中找不到结果时显式计入 - `build_result_summary()` 把两个计数、样本、退化事件都写入摘要和历史 - 回归测试 `regression_schedule_summary_invalid_due_and_unscheduled_counts.py` 覆盖了关键断言 **#p03-2（外协周期默认1.0收口为历史兼容读取）✅ 符合** - `external_groups.py` 使用 `parse_field_float` 带 `strict_mode` 参数，兼容模式下 fallback=1.0 - `_record_compat_counters()` 从 `DegradationCollector` 中提取 `legacy_external_days_defaulted` 和 `blank_required` 计数并透传到 `algo_stats` - 严格模式下该分支会抛异常 - `schedule_summary.py` 从 `fallback_counts` 提取计数，写入退化事件和警告 - 回归测试覆盖了兼容模式的计数断言和严格模式的拒绝断言 **#p03-4（恢复链返回码语义）✅ 符合** - `BackupManager.restore()` 复制完成后返回 `code="copied_pending_verify"` - `backup_restore()` 路由在 `ensure_schema` 成功后才通过 `replace()` 将 code 改为 `"verified"` - 校验失败时保持 `copied_pending_verify` 并给出明确提示 - 操作日志记录 `restore_code` 字段 - 回归测试验证了正常恢复后的 code 和提示文案 **#p03-5（系统配置脏值标记）✅ 符合** - `SystemConfigSnapshot` 新增 `dirty_fields: list[str]` 和 `dirty_reasons: Dict[str, str]` - `_read_snapshot()` 中 `_mark_dirty()` 在归一化、钳制、回退时均记录字段和原因 - `to_dict()` 输出脏标记 - 系统备份页和日志页模板均正确渲染脏值提示 - 回归测试注入了四种脏值场景并断言全部字段和原因可见 **#p03-8（维护任务脏时间外显）✅ 符合** - `ParsedJobTime` 数据类区分 `missing`/`invalid`/`valid` 三种状态，保留 `raw` 原始值 - `_is_due()` 返回 `parsed.state` 和 `parsed.raw` - `_get_job_state_map()` 将 `last_run_state` 和 `last_run_raw` 透传给页面 - 系统日志页模板在 `last_run_state == 'invalid'` 时显示"时间损坏（原始值：...）" - 回归测试注入坏时间戳并断言页面输出包含预期文案
- 证据:
  - `core/algorithms/evaluation.py:61-117#ScheduleMetrics`
  - `core/algorithms/evaluation.py:140-176#compute_metrics:坏交期/未排批次统计`
  - `core/algorithms/greedy/external_groups.py:28-33#_record_compat_counters`
  - `core/infrastructure/backup.py:28-33#RestoreResult`
  - `core/infrastructure/backup.py:316-321#restore:copied_pending_verify`
  - `web/routes/system_backup.py:249-270#backup_restore:三段语义`
  - `core/services/system/system_config_service.py:34-59#SystemConfigSnapshot.dirty_fields`
  - `core/services/system/system_config_service.py:83-144#_read_snapshot:_mark_dirty链`
  - `core/services/system/system_maintenance_service.py:19-38#ParsedJobTime+_parse_db_dt`
  - `web/routes/system_utils.py:139-161#_get_job_state_map:状态透传`

### M2 · #p03-6, #p03-9, #p03-10, #p03-7 展示与脚本诊断落地验证

- 状态: 已完成
- 记录时间: 2026-04-03T16:31:44.567Z
- 已审模块: core/services/personnel/operator_machine_query_service.py, core/services/process/unit_excel/template_builder.py, web/bootstrap/plugins.py, core/services/scheduler/schedule_optimizer_steps.py, scripts/convert_rotary_shell_unit_excel.py, templates/personnel/detail.html, templates/system/logs.html, templates/system/backup.html
- 摘要:

  检查了预热失败外显、插件留痕退化、人机关联脏标记、单元产品转换诊断出口和回归测试覆盖度，结论如下：

  **#p03-6（预热失败与插件启动留痕失败外显）✅ 符合**
  - `schedule_optimizer_steps.py:258` 在预热失败时调用 `increment_counter(optimizer_algo_stats, "ortools_warmstart_failed_count")`，不再仅写日志
  - `schedule_summary.py` 收集该计数后写入退化事件 `ortools_warmstart_failed`、摘要警告和 `degradation_counters`
  - `web/bootstrap/plugins.py` 在留痕写入失败或异常时调用 `_append_plugin_degradation()`，产出 `plugin_bootstrap_telemetry_failed` 事件和 `degraded=True` 标记
  - 回归测试 `regression_warmstart_failure_surfaces_degradation.py` 用 mock 验证了预热失败后摘要中退化计数器和警告的可见性
  - 回归测试 `regression_plugin_bootstrap_telemetry_failure_visible.py` 用 mock 验证了留痕失败后 `degraded=True` 和退化事件的可见性

  **#p03-9（人机关联读侧脏标记）✅ 符合**
  - `_normalize_row()` 在归一化 `skill_level` 和 `is_primary` 时通过 `_mark_dirty()` 记录字段和原因
  - 结果行增加 `dirty_fields` 和 `dirty_reasons`
  - `personnel_pages.py:127-145` 提取脏行汇总并传入模板
  - `detail.html:95-148` 渲染了"当前展示包含 N 条历史兼容归一结果"的汇总提示和逐字段脏原因
  - 回归测试 `regression_operator_machine_dirty_flags_visible.py` 注入脏关联数据后验证了页面输出

  **#p03-10（单元产品转换诊断出口）✅ 符合**
  - `ConvertedTemplates` 增加 `diagnostics: Dict[str, Any]` 字段，内含 `events`、`counters`、`samples`
  - `UnitTemplateBuilder.build()` 使用 `DegradationCollector` 收集默认补齐、推断字段、兼容行数
  - `scripts/convert_rotary_shell_unit_excel.py` 的 `main()` 根据诊断总数区分"转换完成"和"转换完成（带退化成功）"，并打印诊断汇总
  - 回归测试 `regression_unit_excel_converter_diagnostics_visible.py` 验证了诊断计数器、样本和脚本输出文案

  **#p03-7（回归测试覆盖度）✅ 基本符合**
  - 计划要求的 10 个必增回归全部存在，断言逻辑与计划对外口径一致
  - 唯一缺口：恢复链 `verified` 最终状态缺少独立回归覆盖（详见发现 F1）
- 结论:

  检查了预热失败外显、插件留痕退化、人机关联脏标记、单元产品转换诊断出口和回归测试覆盖度，结论如下： **#p03-6（预热失败与插件启动留痕失败外显）✅ 符合** - `schedule_optimizer_steps.py:258` 在预热失败时调用 `increment_counter(optimizer_algo_stats, "ortools_warmstart_failed_count")`，不再仅写日志 - `schedule_summary.py` 收集该计数后写入退化事件 `ortools_warmstart_failed`、摘要警告和 `degradation_counters` - `web/bootstrap/plugins.py` 在留痕写入失败或异常时调用 `_append_plugin_degradation()`，产出 `plugin_bootstrap_telemetry_failed` 事件和 `degraded=True` 标记 - 回归测试 `regression_warmstart_failure_surfaces_degradation.py` 用 mock 验证了预热失败后摘要中退化计数器和警告的可见性 - 回归测试 `regression_plugin_bootstrap_telemetry_failure_visible.py` 用 mock 验证了留痕失败后 `degraded=True` 和退化事件的可见性 **#p03-9（人机关联读侧脏标记）✅ 符合** - `_normalize_row()` 在归一化 `skill_level` 和 `is_primary` 时通过 `_mark_dirty()` 记录字段和原因 - 结果行增加 `dirty_fields` 和 `dirty_reasons` - `personnel_pages.py:127-145` 提取脏行汇总并传入模板 - `detail.html:95-148` 渲染了"当前展示包含 N 条历史兼容归一结果"的汇总提示和逐字段脏原因 - 回归测试 `regression_operator_machine_dirty_flags_visible.py` 注入脏关联数据后验证了页面输出 **#p03-10（单元产品转换诊断出口）✅ 符合** - `ConvertedTemplates` 增加 `diagnostics: Dict[str, Any]` 字段，内含 `events`、`counters`、`samples` - `UnitTemplateBuilder.build()` 使用 `DegradationCollector` 收集默认补齐、推断字段、兼容行数 - `scripts/convert_rotary_shell_unit_excel.py` 的 `main()` 根据诊断总数区分"转换完成"和"转换完成（带退化成功）"，并打印诊断汇总 - 回归测试 `regression_unit_excel_converter_diagnostics_visible.py` 验证了诊断计数器、样本和脚本输出文案 **#p03-7（回归测试覆盖度）✅ 基本符合** - 计划要求的 10 个必增回归全部存在，断言逻辑与计划对外口径一致 - 唯一缺口：恢复链 `verified` 最终状态缺少独立回归覆盖（详见发现 F1）
- 证据:
  - `core/services/personnel/operator_machine_query_service.py:25-84#_normalize_row+dirty_fields`
  - `core/services/process/unit_excel/template_builder.py:15-86#ConvertedTemplates.diagnostics`
  - `web/bootstrap/plugins.py:10-93#bootstrap_plugins+退化事件`
  - `core/services/scheduler/schedule_optimizer_steps.py:254-259#ortools_warmstart_failed`
  - `scripts/convert_rotary_shell_unit_excel.py:64-120#main:诊断汇总`
  - `templates/personnel/detail.html:95-148#脏值页面展示`
- 问题:
  - [低] 测试: 恢复链缺少 verified 状态的独立回归断言

## 最终结论

## 审查结论

对照 `03_退化可见性与读侧治理` 计划文档的 10 项待办，逐项验证了 9 项已勾选的代码落地情况（#p03-3 甘特日历失败透传标记为未完成，不在本次审查范围内）。

### 逐项判定

| 编号 | 待办项 | 判定 |
|------|--------|------|
| #p03-1 | 算法指标显式统计坏交期与未排批次 | ✅ 完全符合 |
| #p03-2 | 外协周期默认1.0收口为历史兼容读取 | ✅ 完全符合 |
| #p03-3 | 甘特日历失败透传 | ⏳ 未勾选，跳过 |
| #p03-4 | 恢复链返回码改语义 | ✅ 完全符合 |
| #p03-5 | 系统配置读侧 dirty_fields/dirty_reasons | ✅ 完全符合 |
| #p03-6 | 预热失败与插件启动留痕失败外显 | ✅ 完全符合 |
| #p03-7 | 退化可见性回归测试覆盖 | ✅ 基本符合（1处低优先级缺口） |
| #p03-8 | SystemJobState.last_run_time 脏值重分类 | ✅ 完全符合 |
| #p03-9 | 人机关联读侧脏标记与页面提示 | ✅ 完全符合 |
| #p03-10 | 单元产品转换诊断出口与脚本汇总 | ✅ 完全符合 |

### 总体评估

**9 项已勾选修改全部符合计划要求。** 代码落地范围覆盖了：
- **算法层**：指标新增字段 + 透传到摘要/历史
- **基础设施层**：恢复链三段语义、系统配置脏值标记、维护任务脏时间三态
- **服务层**：退化事件统一收集器 + 外协周期兼容计数 + 预热失败计数
- **页面展示层**：系统设置/日志页脏值提示、人员详情页脏标记、恢复操作提示文案
- **脚本层**：转换脚本诊断汇总区分"普通成功"和"带退化成功"
- **回归测试**：计划要求的 10 个必增回归全部存在，断言与对外口径一致

### 低优先级缺口

恢复链回归测试只覆盖了 `copied_pending_verify` 状态，`verified` 最终状态缺少独立回归断言。此缺口不影响正确性，但建议后续补齐。

### 遗留项

`#p03-3`（甘特日历失败透传到页面）尚未实施，是当前唯一未完成的待办项。

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mnj4bdur-fta7n4",
  "createdAt": "2026-04-03T00:00:00.000Z",
  "updatedAt": "2026-04-03T16:32:10.864Z",
  "finalizedAt": "2026-04-03T16:32:10.864Z",
  "status": "completed",
  "overallDecision": "conditionally_accepted",
  "header": {
    "title": "03_退化可见性与读侧治理_落地验证",
    "date": "2026-04-03",
    "overview": "对照计划文档 03_退化可见性与读侧治理 检查已勾选项的代码落地情况"
  },
  "scope": {
    "markdown": "# 03_退化可见性与读侧治理 — 代码落地验证审查\n\n**审查日期**: 2026-04-05\n**审查范围**: 计划文档中 10 项待办（#p03-1 ~ #p03-10），重点验证已勾选的 9 项代码是否真正符合计划要求。\n**未勾选项**: #p03-3（甘特日历失败透传）标记为未完成，本次审查跳过。"
  },
  "summary": {
    "latestConclusion": "## 审查结论\n\n对照 `03_退化可见性与读侧治理` 计划文档的 10 项待办，逐项验证了 9 项已勾选的代码落地情况（#p03-3 甘特日历失败透传标记为未完成，不在本次审查范围内）。\n\n### 逐项判定\n\n| 编号 | 待办项 | 判定 |\n|------|--------|------|\n| #p03-1 | 算法指标显式统计坏交期与未排批次 | ✅ 完全符合 |\n| #p03-2 | 外协周期默认1.0收口为历史兼容读取 | ✅ 完全符合 |\n| #p03-3 | 甘特日历失败透传 | ⏳ 未勾选，跳过 |\n| #p03-4 | 恢复链返回码改语义 | ✅ 完全符合 |\n| #p03-5 | 系统配置读侧 dirty_fields/dirty_reasons | ✅ 完全符合 |\n| #p03-6 | 预热失败与插件启动留痕失败外显 | ✅ 完全符合 |\n| #p03-7 | 退化可见性回归测试覆盖 | ✅ 基本符合（1处低优先级缺口） |\n| #p03-8 | SystemJobState.last_run_time 脏值重分类 | ✅ 完全符合 |\n| #p03-9 | 人机关联读侧脏标记与页面提示 | ✅ 完全符合 |\n| #p03-10 | 单元产品转换诊断出口与脚本汇总 | ✅ 完全符合 |\n\n### 总体评估\n\n**9 项已勾选修改全部符合计划要求。** 代码落地范围覆盖了：\n- **算法层**：指标新增字段 + 透传到摘要/历史\n- **基础设施层**：恢复链三段语义、系统配置脏值标记、维护任务脏时间三态\n- **服务层**：退化事件统一收集器 + 外协周期兼容计数 + 预热失败计数\n- **页面展示层**：系统设置/日志页脏值提示、人员详情页脏标记、恢复操作提示文案\n- **脚本层**：转换脚本诊断汇总区分\"普通成功\"和\"带退化成功\"\n- **回归测试**：计划要求的 10 个必增回归全部存在，断言与对外口径一致\n\n### 低优先级缺口\n\n恢复链回归测试只覆盖了 `copied_pending_verify` 状态，`verified` 最终状态缺少独立回归断言。此缺口不影响正确性，但建议后续补齐。\n\n### 遗留项\n\n`#p03-3`（甘特日历失败透传到页面）尚未实施，是当前唯一未完成的待办项。",
    "recommendedNextAction": "1. 完成 #p03-3 甘特日历失败透传实施；2. 为恢复链 verified 状态补充独立回归断言",
    "reviewedModules": [
      "core/algorithms/evaluation.py",
      "core/algorithms/greedy/external_groups.py",
      "core/infrastructure/backup.py",
      "core/services/system/system_config_service.py",
      "core/services/system/system_maintenance_service.py",
      "core/services/scheduler/schedule_summary.py",
      "web/routes/system_backup.py",
      "web/routes/system_utils.py",
      "core/services/personnel/operator_machine_query_service.py",
      "core/services/process/unit_excel/template_builder.py",
      "web/bootstrap/plugins.py",
      "core/services/scheduler/schedule_optimizer_steps.py",
      "scripts/convert_rotary_shell_unit_excel.py",
      "templates/personnel/detail.html",
      "templates/system/logs.html",
      "templates/system/backup.html"
    ]
  },
  "stats": {
    "totalMilestones": 2,
    "completedMilestones": 2,
    "totalFindings": 1,
    "severity": {
      "high": 0,
      "medium": 0,
      "low": 1
    }
  },
  "milestones": [
    {
      "id": "M1",
      "title": "#p03-1, #p03-2, #p03-4, #p03-5, #p03-8 核心读侧治理落地验证",
      "status": "completed",
      "recordedAt": "2026-04-03T16:31:01.857Z",
      "summaryMarkdown": "逐项检查了算法指标、外协周期兼容链、恢复链语义、系统配置脏值标记、维护任务脏时间五个已勾选项的代码落地情况，结论如下：\n\n**#p03-1（算法指标显式统计坏交期与未排批次）✅ 符合**\n- `ScheduleMetrics` 已新增 `invalid_due_count`、`unscheduled_batch_count`、`invalid_due_batch_ids_sample`、`unscheduled_batch_ids_sample` 四个字段\n- `compute_metrics()` 遍历所有批次，使用 `_parse_due_date_state()` 显式区分坏交期与正常无交期\n- 未排批次在 `finish_by_batch` 中找不到结果时显式计入\n- `build_result_summary()` 把两个计数、样本、退化事件都写入摘要和历史\n- 回归测试 `regression_schedule_summary_invalid_due_and_unscheduled_counts.py` 覆盖了关键断言\n\n**#p03-2（外协周期默认1.0收口为历史兼容读取）✅ 符合**\n- `external_groups.py` 使用 `parse_field_float` 带 `strict_mode` 参数，兼容模式下 fallback=1.0\n- `_record_compat_counters()` 从 `DegradationCollector` 中提取 `legacy_external_days_defaulted` 和 `blank_required` 计数并透传到 `algo_stats`\n- 严格模式下该分支会抛异常\n- `schedule_summary.py` 从 `fallback_counts` 提取计数，写入退化事件和警告\n- 回归测试覆盖了兼容模式的计数断言和严格模式的拒绝断言\n\n**#p03-4（恢复链返回码语义）✅ 符合**\n- `BackupManager.restore()` 复制完成后返回 `code=\"copied_pending_verify\"`\n- `backup_restore()` 路由在 `ensure_schema` 成功后才通过 `replace()` 将 code 改为 `\"verified\"`\n- 校验失败时保持 `copied_pending_verify` 并给出明确提示\n- 操作日志记录 `restore_code` 字段\n- 回归测试验证了正常恢复后的 code 和提示文案\n\n**#p03-5（系统配置脏值标记）✅ 符合**\n- `SystemConfigSnapshot` 新增 `dirty_fields: list[str]` 和 `dirty_reasons: Dict[str, str]`\n- `_read_snapshot()` 中 `_mark_dirty()` 在归一化、钳制、回退时均记录字段和原因\n- `to_dict()` 输出脏标记\n- 系统备份页和日志页模板均正确渲染脏值提示\n- 回归测试注入了四种脏值场景并断言全部字段和原因可见\n\n**#p03-8（维护任务脏时间外显）✅ 符合**\n- `ParsedJobTime` 数据类区分 `missing`/`invalid`/`valid` 三种状态，保留 `raw` 原始值\n- `_is_due()` 返回 `parsed.state` 和 `parsed.raw`\n- `_get_job_state_map()` 将 `last_run_state` 和 `last_run_raw` 透传给页面\n- 系统日志页模板在 `last_run_state == 'invalid'` 时显示\"时间损坏（原始值：...）\"\n- 回归测试注入坏时间戳并断言页面输出包含预期文案",
      "conclusionMarkdown": "逐项检查了算法指标、外协周期兼容链、恢复链语义、系统配置脏值标记、维护任务脏时间五个已勾选项的代码落地情况，结论如下： **#p03-1（算法指标显式统计坏交期与未排批次）✅ 符合** - `ScheduleMetrics` 已新增 `invalid_due_count`、`unscheduled_batch_count`、`invalid_due_batch_ids_sample`、`unscheduled_batch_ids_sample` 四个字段 - `compute_metrics()` 遍历所有批次，使用 `_parse_due_date_state()` 显式区分坏交期与正常无交期 - 未排批次在 `finish_by_batch` 中找不到结果时显式计入 - `build_result_summary()` 把两个计数、样本、退化事件都写入摘要和历史 - 回归测试 `regression_schedule_summary_invalid_due_and_unscheduled_counts.py` 覆盖了关键断言 **#p03-2（外协周期默认1.0收口为历史兼容读取）✅ 符合** - `external_groups.py` 使用 `parse_field_float` 带 `strict_mode` 参数，兼容模式下 fallback=1.0 - `_record_compat_counters()` 从 `DegradationCollector` 中提取 `legacy_external_days_defaulted` 和 `blank_required` 计数并透传到 `algo_stats` - 严格模式下该分支会抛异常 - `schedule_summary.py` 从 `fallback_counts` 提取计数，写入退化事件和警告 - 回归测试覆盖了兼容模式的计数断言和严格模式的拒绝断言 **#p03-4（恢复链返回码语义）✅ 符合** - `BackupManager.restore()` 复制完成后返回 `code=\"copied_pending_verify\"` - `backup_restore()` 路由在 `ensure_schema` 成功后才通过 `replace()` 将 code 改为 `\"verified\"` - 校验失败时保持 `copied_pending_verify` 并给出明确提示 - 操作日志记录 `restore_code` 字段 - 回归测试验证了正常恢复后的 code 和提示文案 **#p03-5（系统配置脏值标记）✅ 符合** - `SystemConfigSnapshot` 新增 `dirty_fields: list[str]` 和 `dirty_reasons: Dict[str, str]` - `_read_snapshot()` 中 `_mark_dirty()` 在归一化、钳制、回退时均记录字段和原因 - `to_dict()` 输出脏标记 - 系统备份页和日志页模板均正确渲染脏值提示 - 回归测试注入了四种脏值场景并断言全部字段和原因可见 **#p03-8（维护任务脏时间外显）✅ 符合** - `ParsedJobTime` 数据类区分 `missing`/`invalid`/`valid` 三种状态，保留 `raw` 原始值 - `_is_due()` 返回 `parsed.state` 和 `parsed.raw` - `_get_job_state_map()` 将 `last_run_state` 和 `last_run_raw` 透传给页面 - 系统日志页模板在 `last_run_state == 'invalid'` 时显示\"时间损坏（原始值：...）\" - 回归测试注入坏时间戳并断言页面输出包含预期文案",
      "evidence": [
        {
          "path": "core/algorithms/evaluation.py",
          "lineStart": 61,
          "lineEnd": 117,
          "symbol": "ScheduleMetrics"
        },
        {
          "path": "core/algorithms/evaluation.py",
          "lineStart": 140,
          "lineEnd": 176,
          "symbol": "compute_metrics:坏交期/未排批次统计"
        },
        {
          "path": "core/algorithms/greedy/external_groups.py",
          "lineStart": 28,
          "lineEnd": 33,
          "symbol": "_record_compat_counters"
        },
        {
          "path": "core/infrastructure/backup.py",
          "lineStart": 28,
          "lineEnd": 33,
          "symbol": "RestoreResult"
        },
        {
          "path": "core/infrastructure/backup.py",
          "lineStart": 316,
          "lineEnd": 321,
          "symbol": "restore:copied_pending_verify"
        },
        {
          "path": "web/routes/system_backup.py",
          "lineStart": 249,
          "lineEnd": 270,
          "symbol": "backup_restore:三段语义"
        },
        {
          "path": "core/services/system/system_config_service.py",
          "lineStart": 34,
          "lineEnd": 59,
          "symbol": "SystemConfigSnapshot.dirty_fields"
        },
        {
          "path": "core/services/system/system_config_service.py",
          "lineStart": 83,
          "lineEnd": 144,
          "symbol": "_read_snapshot:_mark_dirty链"
        },
        {
          "path": "core/services/system/system_maintenance_service.py",
          "lineStart": 19,
          "lineEnd": 38,
          "symbol": "ParsedJobTime+_parse_db_dt"
        },
        {
          "path": "web/routes/system_utils.py",
          "lineStart": 139,
          "lineEnd": 161,
          "symbol": "_get_job_state_map:状态透传"
        }
      ],
      "reviewedModules": [
        "core/algorithms/evaluation.py",
        "core/algorithms/greedy/external_groups.py",
        "core/infrastructure/backup.py",
        "core/services/system/system_config_service.py",
        "core/services/system/system_maintenance_service.py",
        "core/services/scheduler/schedule_summary.py",
        "web/routes/system_backup.py",
        "web/routes/system_utils.py"
      ],
      "recommendedNextAction": null,
      "findingIds": []
    },
    {
      "id": "M2",
      "title": "#p03-6, #p03-9, #p03-10, #p03-7 展示与脚本诊断落地验证",
      "status": "completed",
      "recordedAt": "2026-04-03T16:31:44.567Z",
      "summaryMarkdown": "检查了预热失败外显、插件留痕退化、人机关联脏标记、单元产品转换诊断出口和回归测试覆盖度，结论如下：\n\n**#p03-6（预热失败与插件启动留痕失败外显）✅ 符合**\n- `schedule_optimizer_steps.py:258` 在预热失败时调用 `increment_counter(optimizer_algo_stats, \"ortools_warmstart_failed_count\")`，不再仅写日志\n- `schedule_summary.py` 收集该计数后写入退化事件 `ortools_warmstart_failed`、摘要警告和 `degradation_counters`\n- `web/bootstrap/plugins.py` 在留痕写入失败或异常时调用 `_append_plugin_degradation()`，产出 `plugin_bootstrap_telemetry_failed` 事件和 `degraded=True` 标记\n- 回归测试 `regression_warmstart_failure_surfaces_degradation.py` 用 mock 验证了预热失败后摘要中退化计数器和警告的可见性\n- 回归测试 `regression_plugin_bootstrap_telemetry_failure_visible.py` 用 mock 验证了留痕失败后 `degraded=True` 和退化事件的可见性\n\n**#p03-9（人机关联读侧脏标记）✅ 符合**\n- `_normalize_row()` 在归一化 `skill_level` 和 `is_primary` 时通过 `_mark_dirty()` 记录字段和原因\n- 结果行增加 `dirty_fields` 和 `dirty_reasons`\n- `personnel_pages.py:127-145` 提取脏行汇总并传入模板\n- `detail.html:95-148` 渲染了\"当前展示包含 N 条历史兼容归一结果\"的汇总提示和逐字段脏原因\n- 回归测试 `regression_operator_machine_dirty_flags_visible.py` 注入脏关联数据后验证了页面输出\n\n**#p03-10（单元产品转换诊断出口）✅ 符合**\n- `ConvertedTemplates` 增加 `diagnostics: Dict[str, Any]` 字段，内含 `events`、`counters`、`samples`\n- `UnitTemplateBuilder.build()` 使用 `DegradationCollector` 收集默认补齐、推断字段、兼容行数\n- `scripts/convert_rotary_shell_unit_excel.py` 的 `main()` 根据诊断总数区分\"转换完成\"和\"转换完成（带退化成功）\"，并打印诊断汇总\n- 回归测试 `regression_unit_excel_converter_diagnostics_visible.py` 验证了诊断计数器、样本和脚本输出文案\n\n**#p03-7（回归测试覆盖度）✅ 基本符合**\n- 计划要求的 10 个必增回归全部存在，断言逻辑与计划对外口径一致\n- 唯一缺口：恢复链 `verified` 最终状态缺少独立回归覆盖（详见发现 F1）",
      "conclusionMarkdown": "检查了预热失败外显、插件留痕退化、人机关联脏标记、单元产品转换诊断出口和回归测试覆盖度，结论如下： **#p03-6（预热失败与插件启动留痕失败外显）✅ 符合** - `schedule_optimizer_steps.py:258` 在预热失败时调用 `increment_counter(optimizer_algo_stats, \"ortools_warmstart_failed_count\")`，不再仅写日志 - `schedule_summary.py` 收集该计数后写入退化事件 `ortools_warmstart_failed`、摘要警告和 `degradation_counters` - `web/bootstrap/plugins.py` 在留痕写入失败或异常时调用 `_append_plugin_degradation()`，产出 `plugin_bootstrap_telemetry_failed` 事件和 `degraded=True` 标记 - 回归测试 `regression_warmstart_failure_surfaces_degradation.py` 用 mock 验证了预热失败后摘要中退化计数器和警告的可见性 - 回归测试 `regression_plugin_bootstrap_telemetry_failure_visible.py` 用 mock 验证了留痕失败后 `degraded=True` 和退化事件的可见性 **#p03-9（人机关联读侧脏标记）✅ 符合** - `_normalize_row()` 在归一化 `skill_level` 和 `is_primary` 时通过 `_mark_dirty()` 记录字段和原因 - 结果行增加 `dirty_fields` 和 `dirty_reasons` - `personnel_pages.py:127-145` 提取脏行汇总并传入模板 - `detail.html:95-148` 渲染了\"当前展示包含 N 条历史兼容归一结果\"的汇总提示和逐字段脏原因 - 回归测试 `regression_operator_machine_dirty_flags_visible.py` 注入脏关联数据后验证了页面输出 **#p03-10（单元产品转换诊断出口）✅ 符合** - `ConvertedTemplates` 增加 `diagnostics: Dict[str, Any]` 字段，内含 `events`、`counters`、`samples` - `UnitTemplateBuilder.build()` 使用 `DegradationCollector` 收集默认补齐、推断字段、兼容行数 - `scripts/convert_rotary_shell_unit_excel.py` 的 `main()` 根据诊断总数区分\"转换完成\"和\"转换完成（带退化成功）\"，并打印诊断汇总 - 回归测试 `regression_unit_excel_converter_diagnostics_visible.py` 验证了诊断计数器、样本和脚本输出文案 **#p03-7（回归测试覆盖度）✅ 基本符合** - 计划要求的 10 个必增回归全部存在，断言逻辑与计划对外口径一致 - 唯一缺口：恢复链 `verified` 最终状态缺少独立回归覆盖（详见发现 F1）",
      "evidence": [
        {
          "path": "core/services/personnel/operator_machine_query_service.py",
          "lineStart": 25,
          "lineEnd": 84,
          "symbol": "_normalize_row+dirty_fields"
        },
        {
          "path": "core/services/process/unit_excel/template_builder.py",
          "lineStart": 15,
          "lineEnd": 86,
          "symbol": "ConvertedTemplates.diagnostics"
        },
        {
          "path": "web/bootstrap/plugins.py",
          "lineStart": 10,
          "lineEnd": 93,
          "symbol": "bootstrap_plugins+退化事件"
        },
        {
          "path": "core/services/scheduler/schedule_optimizer_steps.py",
          "lineStart": 254,
          "lineEnd": 259,
          "symbol": "ortools_warmstart_failed"
        },
        {
          "path": "scripts/convert_rotary_shell_unit_excel.py",
          "lineStart": 64,
          "lineEnd": 120,
          "symbol": "main:诊断汇总"
        },
        {
          "path": "templates/personnel/detail.html",
          "lineStart": 95,
          "lineEnd": 148,
          "symbol": "脏值页面展示"
        }
      ],
      "reviewedModules": [
        "core/services/personnel/operator_machine_query_service.py",
        "core/services/process/unit_excel/template_builder.py",
        "web/bootstrap/plugins.py",
        "core/services/scheduler/schedule_optimizer_steps.py",
        "scripts/convert_rotary_shell_unit_excel.py",
        "templates/personnel/detail.html",
        "templates/system/logs.html",
        "templates/system/backup.html"
      ],
      "recommendedNextAction": null,
      "findingIds": [
        "F-恢复链缺少-verified-状态的独立回归断言"
      ]
    }
  ],
  "findings": [
    {
      "id": "F-恢复链缺少-verified-状态的独立回归断言",
      "severity": "low",
      "category": "test",
      "title": "恢复链缺少 verified 状态的独立回归断言",
      "descriptionMarkdown": "回归测试 `regression_backup_restore_pending_verify_code.py` 只验证了 `copied_pending_verify` 状态。由于 `verified` 状态需要通过 `ensure_schema` 成功后 `replace()` 赋值，完整端到端链条中 `code=verified` 并没有被回归测试直接覆盖。这不影响当前正确性（路由层 `system_backup.py:270` 已有该逻辑），但建议后续补一个端到端测试覆盖 `verified` 最终状态。",
      "recommendationMarkdown": "在 `regression_backup_restore_pending_verify_code.py` 中增加对 `ensure_schema` 后 `code=verified` 的断言路径覆盖。",
      "evidence": [
        {
          "path": "tests/regression_backup_restore_pending_verify_code.py"
        },
        {
          "path": "web/routes/system_backup.py"
        }
      ],
      "relatedMilestoneIds": [
        "M2"
      ],
      "trackingStatus": "open"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:3220d42e9c26df2861a824e8082c1f2eeff1a37a3ce54d9c3ae3c7c346167766",
    "generatedAt": "2026-04-03T16:32:10.864Z",
    "locale": "zh-CN"
  }
}
```
