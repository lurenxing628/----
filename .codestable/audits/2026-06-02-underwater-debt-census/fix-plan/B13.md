# B13 · 顶层facade/re-export残渣

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：B13 = 顶层 facade/re-export 迁移残渣，被 SP05 拓扑契约钉成"冻结死兼容面"。5 条债全部 load_bearing=false（无一承重护栏），全部排进【Batch-14，全局最晚批次】——铁律：B13 facade 删除必须晚于 B05(R29 number_utils)/B06(R33 compat/value_policies)/B09(R52 ready_queue)收敛（PHASE0 §10.2），否则那些桶还经老顶层路径走测试时先删 shim 会让测试经老路径红。

整体策略：这 5 条没有一条是"裸删"。每条都先迁消费方/先认账 roadmap/先补 parity，再动 facade，且删除时必须同步退 SP05 拓扑契约里登记它们的那几行（删文件不退契约=契约测试红，是记账失败不是安全失守，但仍会挂 CI）。灵魂线检查：5 条删了都只会响亮 ImportError/ModuleNotFoundError，没有一条在兜底坏数据——honors 灵魂暗线。

内部排序（按"前置条件最早清掉"）：
1. R43（最独立，只需先认账/修订 roadmap:522 的延期决定 + 迁 15 个测试 import + 退 wrapper_import_order_contract 整文件 + 改 SP05 三个 ROUTE_* 区块）；
2. R31（最简，删 core/shared/value_policies.py:9 源定义一行；common 侧 :11/:29 被 R33 整文件删吞并，须与 B06 的 R33 协调避免改已删文件）；
3. R20（须等 B01 给 LB01 钉好承重注释，因为 5 个导入方里 feedback_service.py:48 就是 LB01 承重文件；改 import 行时绝不碰 347-356/451-453）；
4. R26（须等 B05/B06/B09 收敛 + 三步迁移：迁 2 个离线脚本→深路径、重指 71 处测试 import、改 SP05 SERVICE_BEHAVIOR_COMPAT 区块 + :642-658 文档断言）；
5. R19（须与 B06 的 R15、B11 的 R13/R18 在两个共享文件上串行；且我盘上实证发现 Phase1 漏看的循环导入+分层双重阻断，naive"三处全收口到 snapshot"不可行，需 owner 裁断收口落点）。

最危险的两点：(a) R19 的 naive 收口被双重阻断（见该条 misfix_risk），(b) R20/R26/R31/R43 删 facade 时漏退对应 SP05/docs 契约行→CI 红。

**内部排序**：按前置条件最早清除排序，全部落 Batch-14（全局最晚批次，§10.2 facade 晚于 B05/B06/B09 收敛）：R43（仅需认账 roadmap，最独立）→ R31（删 1 行源定义，并入 R33 协调）→ R20（待 B01 钉 LB01 注释）→ R26（待 B05/B06/B09 收敛 + 3 步迁移）→ R19（待 B06 R15 / B11 R13·R18 串行 + owner 裁断收口落点）。R31 与 R20 的前置（R33、B01）互不相关可并行准备；R26 与 R19 前置最重，殿后。

### R43 · P3 — 迁移到 domains/scheduler 后的别名残渣；真身由 sc · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — 盘上实测 9 个顶层 wrapper 全在：web/routes/scheduler_analysis.py、scheduler_batch_detail.py、scheduler_batches.py、scheduler_config.py、scheduler_excel_batches.py、scheduler_excel_calendar.py、scheduler_ops.py、scheduler_run.py、scheduler_week_plan.py + web/routes/_scheduler_compat.py。scheduler_run.py:7-8 = sys.modules[__name__]=_impl 强别名；scheduler_excel_batches.py 是逐符号 re-export 变体。roadmap 延期决定行号已复核：.codestable/roadmap/p1-scheduler-debt-cleanup/p1-scheduler-debt-cleanup-roadmap.md:522『先保留旧 wrapper，避免一次改动冲击启动链』（注：pack 写 web/routes/scheduler_run.py:7-8 准；Phase1 提及 roadmap:485-498/522，盘上确切命中行=:522）。
- **具体修法**：删+退测试，但绝非裸删，三前置缺一不可：(1) 先认账或修订 roadmap p1-scheduler-debt-cleanup-roadmap.md:522 明示的『先保留旧 wrapper 避免冲击启动链』延期决定——现在删属抢跑该决定（非安全问题，是流程问题），必须 owner 拍板把该行改为『已收口』或显式批准提前；(2) 把盘上实测 15 个 test-only 消费文件的 `import web.routes.scheduler_xxx` / `from web.routes.scheduler_xxx` 迁到 domains 叶子路径 web.routes.domains.scheduler.scheduler_xxx（清单：regression_request_scope_app_logger_binding、regression_scheduler_analysis_route_contract、regression_scheduler_batch_detail_merge_hint_fast_path、regression_scheduler_batch_detail_route_contract、regression_scheduler_batches_degraded_visibility、regression_scheduler_candidate_analysis_contract、regression_scheduler_config_manual_url_normalization、regression_scheduler_config_route_contract、regression_scheduler_excel_batches_helper_injection_contract、regression_scheduler_ops_update_route_contract、regression_scheduler_route_enforce_ready_tristate、regression_scheduler_run_surfaces_resource_pool_warning、regression_scheduler_week_plan_summary_observability、test_schedule_summary_observability、test_scheduler_run_view_result_contract）；(3) 同步删/改两处 wrapper 专属契约：tests/regression_scheduler_wrapper_import_order_contract.py 整文件（LEGACY_SCHEDULER_WRAPPERS:12-22 列全 9 个）+ tests/test_sp05_path_topology_contract.py 的 ROUTE_COMPAT_MODULES(:94)/ROUTE_BEHAVIOR_COMPAT_SYMBOLS(:105)/SCHEDULER_REAL_ROUTE_FILES(:150) 三个区块。三步做完才能删 9 wrapper + _scheduler_compat.py。灵魂线：_scheduler_compat.load_scheduler_route_module 缺目标抛 ModuleNotFoundError（无静默兜底），删前删后都不破灵魂线。
- **爆炸半径**：生产：零（factory.py:449 只 import 根 web.routes.scheduler；registrar 直接 import 14 个 domains 叶子；启动链 passivity 由 SP05:484-571 在真实生产路径独立守护，删 wrapper 不解护栏）。测试：15 个测试文件 import 受影响 + regression_scheduler_wrapper_import_order_contract.py 整文件 + SP05 三个 ROUTE_* 区块。
- **跨债依赖**：无同文件跨债边（pack 的 B13 cross_bucket_same_file_edges 不含 R43）。唯一外部依赖=roadmap:522 延期决定的人为前置。本桶内最独立，可最先准备。
- **必补回归测试**：迁移后必须断言：(a) 15 个路由契约测试经 domains 叶子路径仍全绿（行为不变）；(b) SP05 path topology 全绿（ROUTE_COMPAT_MODULES/ROUTE_BEHAVIOR_COMPAT_SYMBOLS/SCHEDULER_REAL_ROUTE_FILES 删条后 :412 SCHEDULER_REAL_ROUTE_FILES 文件存在检查 + :430/:435/:534 别名遍历不再枚举已删模块）；(c) 启动链 passivity 专项（SP05:484-571 registrar AST _REGISTERED=False、叶子 import 无副作用）仍绿，证明删 wrapper 未解启动链护栏。
- **误修风险与防呆**：未来 LLM 最可能：(1) 直接 rm 9 个 wrapper 而漏退 regression_scheduler_wrapper_import_order_contract.py → 该契约 import 已删模块 collect 期就红；(2) 漏改 SP05 三个 ROUTE_* 区块 → 拓扑契约红；(3) 不认账 roadmap:522 就删 → 抢跑团队明示的延期决定。防呆：本条 fix 三步顺序固化在计划里；删除提交必须同提交退两处契约 + 注明 roadmap:522 已 owner 批准。绝不能反向把 domains 真身改指回 wrapper。
- **落地批次/优先级**：Batch-14（全局最晚，§10.2）。本桶内排第 1 位（前置最轻=仅 roadmap 认账，不依赖任何其他桶的代码收敛）。

### R31 · P6 — 定义后无人消费的死常量，散布在『定义点(shared:9)+re-ex · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/shared/value_policies.py:9 定义 WRITE_INTERNAL_ONLY='write_internal_only'（源定义点）；经 core/services/common/value_policies.py:11 import + :29 列入 __all__。全仓 `grep WRITE_INTERNAL_ONLY` 仅命中这 3 行（1 源定义 + 2 re-export），零真实消费。盘上复核灵魂线证据：compat_parse 唯一读 policy.write_mode 处只比 ==WRITE_OPTIONAL（report 引 :165/188/208）；core/shared/value_policies.py 内 16 个 FieldPolicy 的 write_mode 仅用 WRITE_REQUIRED/WRITE_OPTIONAL/WRITE_NOT_APPLICABLE（实测 :46-208），无一用 WRITE_INTERNAL_ONLY；core/shared/value_policies.py 无 __all__（删 :9 一行干净，不需同步改本文件 __all__）。
- **具体修法**：直删，删 2 文件共 3 处：(1) R31 自删 core/shared/value_policies.py:9 的 `WRITE_INTERNAL_ONLY = "write_internal_only"` 源定义行；(2) common 侧 core/services/common/value_policies.py:11(import 行) + :29(__all__ 条目) —— 这两处不由 R31 单独删，因为 R33(B06) 要删整个 core/services/common/value_policies.py 这个 facade 文件（盘上确认 R33 location=core/services/common/compat_parse.py 同类 field_parse.py/value_policies.py 三个零生产 re-export facade）。所以 common 侧 :11/:29 随 R33 整文件删除自动消失。R31 只需保证『删 shared:9 源定义』与『R33 删 common facade』同批，避免 R31 去改一个 R33 已删的文件。灵魂线：删死常量无运行期影响，纯 P6 清理。
- **爆炸半径**：极低。生产零引用（grep 实证）。测试：regression_value_policies_matrix_contract.py 盘上实测只从 core.services.common.value_policies import WRITE_OPTIONAL/WRITE_REQUIRED（:21-22），NOT import WRITE_INTERNAL_ONLY，故删该常量不破该契约的 import；matrix 断言（:53/:88）也只校验 default_days/due_date 的 WRITE_REQUIRED/WRITE_OPTIONAL，与死常量无关。
- **跨债依赖**：与 R33(B06) 同文件跨桶（PHASE0 §2/§3：value_policies.py R31(B13)↔R33(B06)）——但注意这是『同 basename 两个文件』的精确关系：R31 的源定义在 core/shared/value_policies.py，R33 删的 facade 是 core/services/common/value_policies.py。二者是 import 链关系（common 从 shared 转出）。排序硬约束：common 侧删改并入 R33（B06，Batch-7）；shared 源侧(:9) 由 R31 自删（Batch-14）。须协调让两边在不互相踩已删文件的前提下完成——建议 R33 先删 common facade，R31 再删 shared:9（此时无人再 import 该符号）。
- **必补回归测试**：无需新增专门契约（P6 死常量）。验收：(a) regression_value_policies_matrix_contract.py 仍绿（不依赖该常量）；(b) 全仓 `grep -rn WRITE_INTERNAL_ONLY` 删后返回零命中；(c) R33 删 common facade 后其 __all__ parity 检查（若有）不再期望该符号。
- **误修风险与防呆**：未来 LLM 最可能：(1) 只删 shared:9 而 common:11 仍 import 它 → common 文件 ImportError（若 R33 尚未删该文件）；(2) 反向——以为该常量『可能将来要用』而保留，违背 P6 直删。防呆：fix 里固化『shared:9 删除必须与 R33 删 common facade 同批或在其后』；删除提交跑全仓 grep 证零残留。误判风险点：不要把 core/shared/value_policies.py（活模块，含 16 个 FieldPolicy）当成 facade 整文件删——只删 :9 一行。
- **落地批次/优先级**：Batch-14。本桶内排第 2 位。实际删除动作并入/紧随 B06 的 R33（Batch-7 删 common facade），shared 源侧删除在 Batch-14 收尾。

### R20 · P3 — 一层不做事的 service re-export 垫片；分区内『是否走 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/operation_execution_labels.py 实测 36 行（pack 写 :1-36 准），全文件从 core.models.operation_execution_labels import 15 个符号并 __all__ 原样转出，零新增。AST 复核：model __all__=17（实测），shim __all__=15，15 ⊂ 17 严格超集，5 个导入方改连 model 行为字节级一致。盘上复核 5 个导入方：core/services/scheduler/operation_execution_feedback_service.py:48（相对 `from .operation_execution_labels import (`）、operation_execution_feedback_support.py:26（相对同款）、web/routes/domains/scheduler/scheduler_resource_dispatch_execution_routes.py:16（绝对 `from core.services.scheduler.operation_execution_labels import execution_action_label`）、tests/regression_operation_execution_event_foundation.py:17、tests/regression_plan_vs_actual_review.py:10。同分区 6 处已直连 model 实证（gantt_tasks.py:8、resource_dispatch_actual_record_service.py:9、resource_dispatch_actual_import.py:9、resource_dispatch_execution_enrichment.py:5、web/viewmodels/scheduler_resource_dispatch_execution.py:10，+ state_builder）——『service 不直连 model 标签』约定客观未贯彻。
- **具体修法**：推荐方向 (A) 删垫片（与既有 6 处直连 model 的事实对齐，比方向 B『收编统一门面』改动小且符合现状）：(1) 把 3 个生产导入方改连 model——feedback_service.py:48 与 feedback_support.py:26 的相对 `from .operation_execution_labels import ...` 改为 `from core.models.operation_execution_labels import ...`；scheduler_resource_dispatch_execution_routes.py:16 已是绝对 service 路径，改为 core.models.operation_execution_labels；(2) 2 个测试导入方（event_foundation.py:17、plan_vs_actual_review.py:10）同改连 model；(3) 删文件后必须同步把路径从 tests/regression_aps_three_gap_docs_quality_gate.py:122 的 KEY_PYTHON_FILES 元组移除 + 从开发者指南删对应条目（盘上实测 :181 `missing_python=[path for path in KEY_PYTHON_FILES if path not in text]` + :183 `assert missing_python==[]`，删文件不同步=文档同步门禁红，记账失败非安全失守）。灵魂线：标签坏数据兜底由 model 函数实现（execution_status_label→'状态未识别' 暴露错误），删垫片无法削弱。**承重协调**：feedback_service.py 即 LB01 承重文件，改其 :48 import 行时绝不能碰 347-356 硬拒 + 451-453 写死消毒区——必须等 B01 先给 LB01 钉好承重注释（护栏裸奔期勿动）。
- **爆炸半径**：生产 3 文件（feedback_service/feedback_support/resource_dispatch_execution_routes）改 import；其中 feedback_service.py 是 LB01 承重文件（高危同居）。测试 2 文件改 import + docs_quality_gate KEY_PYTHON_FILES 元组 + 开发者指南条目。行为字节级零影响。
- **跨债依赖**：PHASE0 §3 最危险边相关：operation_execution_feedback_service.py 上 LB01(B01) ↔ R20 改 :48 import。Phase1 边 LB01→R20『承重先于动同文件』：B01 承重注释必须先落，R20 才能改 feedback_service:48。与 R17(B11) 也同居 feedback_service（R17 删 :28-31 死导入）——R20 改 :48、R17 改 :28-31，行号不同但同文件，须与 B11 串行或同批协调避免撞行号。
- **必补回归测试**：改 import 前后跑 tests/regression_operation_execution_event_foundation.py 与 tests/regression_plan_vs_actual_review.py 自证行为不变（pack precondition 明示）。删文件后验收：(a) docs_quality_gate :181/183 绿（KEY_PYTHON_FILES 已摘该路径 + 指南已删条目）；(b) feedback_service.py 347-356/451-453 未被碰（diff 审查 + LB01 契约测试仍绿）；(c) 全仓 grep `services.scheduler.operation_execution_labels` 零命中。
- **误修风险与防呆**：未来 LLM 最可能：(1) 改 feedback_service:48 import 时手滑碰到紧邻的 LB01 承重段（347-356 硬拒/451-453 写死消毒）→ 打穿写侧拒绝护栏；(2) 删垫片漏退 docs_quality_gate:122 元组 → 文档同步门禁红；(3) 选方向 B（收编统一门面，把 6 处直连改走垫片）反而扩大改动面且与现状背离。防呆：本条 fix 钉死『须等 B01 LB01 注释落地』『改 import 仅动 import 行』；删除提交同步退 docs gate。绝不在 feedback_service 内顺手『统一』标签导入路径而触碰承重逻辑。
- **落地批次/优先级**：Batch-14。本桶内排第 3 位（前置=B01 的 LB01 承重注释，属 Batch-1 ROOT，较早可清）。与 B11 的 R17 在 feedback_service.py 上须协调。

### R26 · P3 — config→config/、summary→summary/ 包拆分 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — 5 个顶层 shim 全实测纯 from .config.X / .summary.X 转出：core/services/scheduler/config_service.py（5 行，转 .config.config_service.ConfigService）、config_snapshot.py（转 .config.config_snapshot 的 ScheduleConfigSnapshot/build_schedule_config_snapshot）、config_validator.py（转 .config.config_validator.normalize_preset_snapshot）、schedule_summary.py（转 .summary.schedule_summary 多符号）、schedule_summary_types.py（转 .summary.schedule_summary_types 多符号）。⚠️纠偏#8 盘上证实：2 个离线非测试消费者仍在老路径——tools/capture_networkx_phase0_baseline.py:17（from core.services.scheduler.config_service import ConfigService）、audit/2026-03/20260316_schedule_audit_probes.py:87（from core.services.scheduler.schedule_summary import build_overdue_items）。测试侧实测 53 个文件、71 处 import 经这 5 个 shim 进。SP05 钉死点实测：tests/test_sp05_path_topology_contract.py SERVICE_BEHAVIOR_COMPAT_SYMBOLS:21-31 映射 5 个、:329-340 断言 strong alias+__all__+is-identity、:642-658 文档树断言 config_service.py 标注『兼容薄门面』。
- **具体修法**：删+退测试，三步协调迁移（非裸 rm，SP05 冻结是有意决定）：(1) 把 2 个离线非测试消费者迁到深路径——tools/capture_networkx_phase0_baseline.py:17 改 `from core.services.scheduler.config.config_service import ConfigService`、audit/2026-03/20260316_schedule_audit_probes.py:87 改 `from core.services.scheduler.summary.schedule_summary import build_overdue_items`（否则它们 ImportError）；(2) 重指 53 个测试文件、71 处 import 到深路径（.config.config_service / .config.config_snapshot / .config.config_validator / .summary.schedule_summary / .summary.schedule_summary_types）；(3) 从 test_sp05_path_topology_contract.py 删这 5 条：SERVICE_BEHAVIOR_COMPAT_SYMBOLS(:21-31) + SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS(:33-82) 对应条目，并改文档树断言(:642-658 期望 config_service.py 标注『兼容薄门面』→改为不再期望）。三步做完再删 5 个 shim 文件。灵魂线：删后失败=响亮 ImportError，无静默兜底。
- **爆炸半径**：生产核心 core/web=0（全走深路径或 __getattr__）。离线脚本 2 个（必须先迁）。测试 53 文件/71 import。SP05 拓扑契约 + 文档树断言。误删任一 shim 而不走三步=破 SP05 + 71 处 import + 2 离线脚本。
- **跨债依赖**：facade 晚于收敛硬约束（PHASE0 §10.2）：R26 删 config/summary shim 须晚于 B05(R29 number_utils,Phase1 边 R29→R26)/B06(R33,Phase1 边 R33→R26)/B09 收敛——若这些桶仍经顶层 config/summary 老路径走测试，先删 shim 会让测试经老路径红。**澄清 PHASE0 §2/§3 的 config_snapshot.py 跨桶边（R26↔R71）**：Phase1 location_status 明示 R26 的 config_snapshot.py(顶层 shim) 与 R71 touch 的 config_snapshot.py 是 basename 假碰撞——盘上证实存在两个文件：core/services/scheduler/config_snapshot.py（R26 的 shim）与 core/services/scheduler/config/config_snapshot.py（深实现，R71 经 schedule_config_runtime_read.py:68 关联的是后者）。故 R26 删顶层 shim 与 R71 不在同一物理文件，§3 那条边是 basename 误报（见 open_questions）。
- **必补回归测试**：验收：(a) SP05 path topology 全绿（5 条 SERVICE_BEHAVIOR_COMPAT 删后，:124-127 ALL_COMPAT 集合 + :321/328 遍历不再枚举 5 个顶层模块）；(b) 71 处测试 import repoint 到深路径后全绿；(c) 2 离线脚本迁移后可独立 import（手动跑 import 自证）；(d) lazy_runtime 相关绿。
- **误修风险与防呆**：未来 LLM 最可能：(1) 信普查『0 生产消费』直接 rm 5 个 shim → 2 个离线脚本 ImportError（普查证据已被纠偏#8 推翻）；(2) 删 shim 漏改 SP05 SERVICE_BEHAVIOR_COMPAT_SYMBOLS/PUBLIC_SYMBOLS + :642-658 文档断言 → 拓扑契约红；(3) 在 B05/B06/B09 尚未收敛时抢删 → 老路径测试红。防呆：fix 三步顺序 + §10.2 排最晚批次固化；删除提交必须证明 2 离线脚本已迁 + 71 import 已 repoint + SP05 三处已改。
- **落地批次/优先级**：Batch-14（全局最晚，§10.2 facade 晚于 B05/B06/B09）。本桶内排第 4 位（前置最重：依赖 3 个桶收敛 + 三步迁移）。

### R19 · P5 — 已有导出的公共 positive_op_ids 收口点，provide · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — 三处实测确认语义漂移：core/services/scheduler/execution_snapshot.py:27 `positive_op_ids`（:39 `return sorted(out)`，public 进 __all__:70，被 :43/:61 用，sha256 指纹 :50-51 依赖排序）；core/services/scheduler/execution_fact_provider.py:51 `_positive_op_ids`（:63 `return out` 不排序，被 :92 list_by_op_ids 用）；data/repositories/operation_execution_event_repo.py:63 `_positive_ids`（:75 `return out` 不排序，被 :235/:268/:292 用）。三者职责一致，唯一差异=是否排序（公开版排序、两私有版不排序）。**Phase1 漏看的关键阻断（盘上新证）**：execution_snapshot.py:7 `from core.services.scheduler.execution_fact_provider import ExecutionFact, ExecutionFactProvider`——snapshot 已 import provider，故 Phase1 建议的『provider→snapshot.positive_op_ids』会造成循环导入。
- **具体修法**：收口到单一实现，但 naive『三处全收口到 execution_snapshot.positive_op_ids』被双重阻断，必须避开：阻断A=循环导入（snapshot.py:7 已 import provider，provider 再 import snapshot=环）；阻断B=分层红线（repo 在 data 层，snapshot 在 core/services 层，data→service 收口=越层违规，test_architecture_fitness viewmodel/repo 规则与 §4 分层红线）。red line #3 又禁止为已有概念新建第二模块（除 R09 唯一例外）。**推荐落点（需 owner 确认）**：把规范版 `positive_op_ids`（int 化+去重+sorted，签名取最宽 Iterable[Any]→List[int]）下沉到一个【已存在且 service+data 都能 import、且不 import provider/snapshot 因而无环】的模块。盘上验证的最佳候选=core/models/operation_execution_event.py（repo 已在 :6 import 它，data→core.models 合法且全仓普遍，该文件仅 import ._helpers 无环）——属『向已存在 core/models 模块加函数』非『新建模块』，不违 red line #3。然后：execution_snapshot.positive_op_ids 改为从该 model 落点 import/re-export（保 public + __all__ + sorted 给指纹）；provider._positive_op_ids 删，改 import model 落点（list_by_op_ids 改用规范版）；repo._positive_ids 删，改 import model 落点。**务必保 sorted 给 snapshot 指纹稳定**；provider 改用排序版后 list_by_op_ids 迭代序变为排序序——盘上证实其唯一调用方=:105 facts_by_op_id（dict 化，顺序无关），且无其他直接消费 list_by_op_ids 处，安全；repo 私有版仅喂 IN() 占位符且 SQL 已 ORDER BY op_id ASC，改排序无下游影响。备选落点=core/shared（pure util 层，data→core.shared 当前盘上无先例但无规则禁止）——但 core/models 落点更稳（data→core.models 已大量存在）。**绝不**用 lazy/函数内 import 去硬绕 snapshot 循环（治标且是 smell）。
- **爆炸半径**：生产 3 文件（execution_snapshot/execution_fact_provider/operation_execution_event_repo）+ 1 落点文件（core/models/operation_execution_event.py 加函数）。snapshot 指纹消费方（execution_snapshot.py:43/61，下游 gantt_adjustment_publish/scenario/run guardrails 等经 collect_execution_snapshot/build_execution_snapshot）依赖排序——收口后排序语义不变=安全。provider/repo 改排序经实证下游顺序无关。
- **跨债依赖**：两个共享文件跨桶串行（pack B13 edges + PHASE0 §2/§3）：execution_fact_provider.py 上 R19 ↔ R13(B11,:20-21/42-43/96) / R15(B06,:66)——Phase1 边 R15→R13『同文件同改』，R19 改 :51/:92 须与 B06/B11 进同批或串行避免撞行号；operation_execution_event_repo.py 上 R19 ↔ R18(B11,:260-265)——Phase1 边 R13→R18，R19 删 :63 _positive_ids 须与 B11 删/改相邻方法串行。即 R19 必须排在 B06(R15)、B11(R13/R18) 这两文件动作之后或同批。
- **必补回归测试**：必补 parity/契约：(a) 新增契约断言『model 落点 positive_op_ids、(收口后)provider 路径、repo 路径对同一含重复/含非正/乱序输入，去重集合一致且 model 落点输出严格升序』——把三处『同一概念单一来源』绑死，防未来再分叉；(b) 端到端：execution-snapshot revision/sha256 指纹回归（盘上 tests/regression_gantt_adjustment_publish_execution_revision.py 校验 execution_snapshot_revision/op_ids/op_count，:54/:91-92）必须证明收口后 digest 不变（排序语义保住）；(c) test_architecture_fitness 分层绿（落点在 core/models，data/service import 均合法，无越层、无新增 LOCAL_PARSE_HELPER——实测 _positive_ids/_positive_op_ids 不在 LOCAL_PARSE_HELPER_NAMES 名单，安全）；(d) 收口后全仓无 import 环（snapshot 不再被 provider 反向依赖）。
- **误修风险与防呆**：未来 LLM 最可能：(1) 照 Phase1 字面让 provider `from .execution_snapshot import positive_op_ids` → 循环导入炸（snapshot:7 已 import provider）；(2) 让 repo import core.services.scheduler.execution_snapshot → 越层违规（data→service）；(3) 为图省事新建 core/shared/op_ids.py → 违 red line #3『为已有概念新建第二模块=制造新 P5』；(4) 强行让 provider/repo 不排序版冒充指纹源 → 不同 digest 隐蔽 bug。防呆：本条 fix 明确『落点必须是已存在、无环、service+data 都可达的模块（首选 core/models/operation_execution_event.py）』『保 sorted 给指纹』；parity 契约把三路绑死；分层 fitness 测试兜底越层。owner 须先裁断落点（见 open_questions）。
- **落地批次/优先级**：Batch-14（全局最晚）。本桶内排第 5 位（前置最复杂：须 B06 R15 / B11 R13·R18 在两共享文件上先动或同批 + owner 裁断收口落点 + 循环导入/分层双重规避）。

**本单元待 owner 裁断**：
- R19 收口落点须 owner 拍板：naive『三处全收口到 execution_snapshot.positive_op_ids』被双重阻断——(A) 循环导入（盘上实证 execution_snapshot.py:7 已 `from ...execution_fact_provider import ExecutionFactProvider`，provider 再反向 import snapshot=环，Phase1 未发现此阻断）；(B) 分层红线（repo 在 data 层，snapshot 在 service 层，data→service 越层）。red line #3 又禁止为已有概念新建模块（除 R09 例外）。我推荐下沉到【已存在的 core/models/operation_execution_event.py】（repo 已 import 它、无环、data/service 均可达，属向已存在模块加函数非新建模块）。需 owner 确认：(1) 是否接受『向已存在 core/models 模块加 positive_op_ids 函数』不算违反 red line #3 的新建模块；(2) 还是更倾向 core/shared（pure util 但当前盘上无 data→core.shared 先例）；(3) 或 owner 认为 R19 应降级为『仅补 parity 契约+原地把 provider/repo 也改 sorted 消除漂移、保留三份』（不收口，纯绑契约）。
- PHASE0 §2/§3 把 config_snapshot.py 列为 R26(B13)↔R71(B03) 跨桶同文件边，但盘上证实是 basename 假碰撞：存在两个不同文件——core/services/scheduler/config_snapshot.py（R26 的顶层 shim）与 core/services/scheduler/config/config_snapshot.py（深实现，R71 经 schedule_config_runtime_read.py:68 关联的是后者）。Phase1 R26 location_status 也已点出此假碰撞。请 owner 在 Phase3 排序时把这条 §3 边标记为误报删除，避免无谓串行化 R26 与 R71（二者实际不动同一物理文件）。
- R43 删除前置是『认账/修订 roadmap p1-scheduler-debt-cleanup-roadmap.md:522 的延期决定（先保留旧 wrapper 避免冲击启动链）』——这是流程而非安全前置。需 owner 决定：现在就批准提前清理（本轮 Phase 2 只出计划不改码，实际删除在执行期），还是继续延期、把 R43 留到 wrapper 清理专项 PR。若继续延期，R43 在本桶应标记『计划已备、执行待 roadmap 放行』。
- R26 的 53 测试文件/71 import + 2 离线脚本迁移工作量不小，且必须晚于 B05/B06/B09 三桶收敛。需 owner 确认这三桶收敛完成的判定信号（建议=对应 batch 验收全绿且老顶层路径无测试依赖），作为 R26 动手的明确闸门，避免 B13 与前序桶在执行期抢跑。
