# B15 · 逐字两份簇

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：本桶=3 条「逐字两份簇」，全部经回盘 Read/grep 复核，行号 100% ✅准，cross_bucket_same_file_edges 为空（pack 确认），不与任何其它桶同文件——可整体放 Batch-15 独立并行，不阻塞他桶也不被他桶阻塞。但三条命运不同，必须分别对待，绝不能"一刀切消重"：

- R70 是【死副本直删】不是 twin 收口（Phase1 纠偏#11 已盘上坐实）：schedule_service.py:46 那份本文件零调用 + 无 __all__ + 无符号 importer + facade_delegation 测试零引用，删它零行为影响；live 份只剩 input_collector:79 一处，删完天然单点，无需新建 helper、无需"收口"。误删 live 份=灾难。
- R68 踩灵魂线（used_default/parse_failed 第二元组位），是真正要小心的一条：必须先补 parity 测试（断言两路对同一 meta 连 parse_failed 都一致）再收口，收口点用 summary 包内既有解析收口模块 summary_count_parse.py（两文件 import 它零环），严禁把 (value, parse_failed) 二元组压扁成单值（=杀灵魂线）。
- R69 是 P4 气味 + 护栏文件：收口单点之外，护栏文件内的 `except (TypeError, ValueError): return 0` 坏 seq 静默归 0 必须改 loud（红线#1）。已盘上校准可达性（Phase1 #16 纪律）：BatchOperation.seq 在 model 边界已被 parse_int_or_default(...field="seq") loud coerce 成 int，故真实 BatchOperation 路径上该 except 近乎死防御，loud raise 投入风险极低、收益=护栏一致性。

内部排序：先 R70（零依赖纯删、零风险）→ 再 R68（parity 门控，medium，灵魂线）→ 再 R69（需 loud-raise 设计裁断 + 收口家选择）。三条互不改同文件，"co_change_within_group" 只意味"同批"不意味"同文件冲突"。

**内部排序**：R70 → R68 → R69。理由：R70 是零前置纯死码删除（最安全先落，立即缩小本桶面）；R68 必须等其 parity 测试在 Batch-1 ROOT 落地后才能收口（灵魂线 used_default 保真门控），且 medium 最高严重度需最稳的安全网；R69 需 owner 先裁断 loud-raise 方向与收口家归属（唯一设计选择），放最后。三条彼此无 file 冲突，可并行，但若串行按此序风险递增、心智负担递增。

### R70 · P5（报告框为'逐字两份均 loud raise'，实为死副本+单 live，本 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — 已回盘 Read 双份函数体逐字节相同：core/services/scheduler/schedule_service.py:46-50 ↔ core/services/scheduler/run/schedule_input_collector.py:79-83，均为 `exc=ValidationError(message,field="排产"); exc.details=dict(exc.details or {}); exc.details["reason"]=reason; raise exc`。关键纠偏#11 盘上坐实：schedule_service.py 内 `grep _raise_schedule_empty_result` 仅命中 :46 一行（=def 自身，零调用方），文件无 __all__，全仓无 `from ...schedule_service import _raise_schedule_empty_result`，tests/regression_schedule_service_facade_delegation.py 零引用该符号 → schedule_service:46 是死副本；live 份在 input_collector:173/:228/:251 直调 + :356 作 raise_schedule_empty_result_fn= 传入。
- **具体修法**：【纯删，不收口、不新建 helper】删除 schedule_service.py:46-50 整个死 `_raise_schedule_empty_result` 函数定义。删后全仓仅剩 input_collector:79 一处定义=天然单点，R70 即闭合，无需任何'收口到上游 helper'动作（Phase1 已证 twin 框架不成立）。不动 input_collector:79 一个字节。不删 schedule_service.py:7 的 `from core.infrastructure.errors import BusinessError, ErrorCode, ValidationError`——ValidationError 在同文件 :217 仍被 `raise ValidationError("系统正在执行排产...")` 使用，删 import 会 NameError（已 grep 确认 :217 用法）。
- **爆炸半径**：生产：零。死副本无任何调用方/导入方，删除不改变任何运行路径。input_collector live 份的空结果 reason 契约（ValidationError field='排产' + details['reason']）完全不动。测试：tests/regression_schedule_service_facade_delegation.py 不引用该符号（已 grep），不会红；空结果行为由 regression_schedule_history_not_created_for_empty_schedule.py、regression_schedule_service_empty_reschedulable_rejected.py 行为级覆盖（import ScheduleService 不 pin 私有符号），删死码不触达。SP05 stage_record 关注 input_collector 的 run/ 迁移，与本私有 fn 无关。
- **跨债依赖**：无跨桶同文件边（pack cross_bucket_same_file_edges=[]）。同桶 R68/R69 co_change 仅'同批'语义、触不同文件，无 file 冲突。无前置/后置依赖，Batch-15 内可最先落。
- **必补回归测试**：无需新增测试（纯死码删除，零行为变化）。必跑保绿：regression_schedule_service_facade_delegation.py（确认 facade 委托面不含此符号）、regression_schedule_history_not_created_for_empty_schedule.py、regression_schedule_service_empty_reschedulable_rejected.py（空结果 reason 装配不变）。可选防呆：在 facade_delegation 或新增一行 grep 守卫断言全仓 `def _raise_schedule_empty_result` 仅出现 1 次（钉死单点，防未来复活第二份）。
- **误修风险与防呆**：最高危误修=未来 LLM 看'逐字两份'就删错边——删掉 live 的 input_collector:79 而留死的 schedule_service:46，直接打穿空结果主链（NameError/调用失败）。次危=多此一举把两份'合并'到新建 core/shared helper，凭空给死路径续命+制造 P5 新收口点（违红线#3）+ churn live 路径。防呆：删除目标严格锁定 schedule_service.py:46-50，判定依据写进计划=该处零 in-file 调用方（grep 计数=1）；明令禁止触碰 input_collector:79；用'单点 grep 守卫'断言删后仅剩 1 个 def。绝不引入兜底（本就是 loud raise，保持 loud）。
- **落地批次/优先级**：Batch-15（独立并行，PHASE1 §4）。本桶内最先落：零前置、零风险、纯删，先落以缩小本桶面。

### R68 · P5 + soul_line_sensitive=true（降级 meta bo · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准 — 已回盘 Read 双份 `_meta_bool_state` 函数体逐字节相同：core/services/scheduler/summary/schedule_summary_degradation.py:123-140 ↔ core/services/scheduler/summary/schedule_summary_downtime_degradation.py:30-47（签名/分支/字面集合 {true,1,yes,y,on}/{false,0,no,n,off} 全一致）。全仓 `grep _meta_bool_state` 仅命中此二文件（其余命中均为审计 JSON），无第三处。灵魂线证据已盘上坐实：第二元组位=parse_failed 标记，degradation 侧 :371 `if attempted_parse_failed or build_ok_parse_failed:` 直接消费、downtime 侧 :125-127 聚合进 `meta_parse_failed`，删/压扁该位=静默吞坏 meta。注：`_meta_int_state` 仅 downtime:23 有、degradation 用的是不同的 `_metric_int`:143——精确 dup 只此一函数，收口范围恰好 1 个函数。
- **具体修法**：【先 parity 后收口，严禁压扁二元组】(1) Batch-1 ROOT 先补 parity 测试（见 regression_tests），断言两份对同一 (meta,key,default) 矩阵输出完全一致【含 parse_failed 位】。(2) 收口到 summary 包内既有解析收口模块 core/services/scheduler/summary/summary_count_parse.py（已是本包 parse 收口点，被 summary_visible_degradation/summary_size_guard_fields/schedule_summary_contract 等导入，仅 import 标准库 math/typing，两份 degradation 文件 import 它零环险）——把单份 `_meta_bool_state` 提到该模块，两文件改为 `from .summary_count_parse import _meta_bool_state`（或去下划线公开名）。备选家=downtime_degradation.py（因 degradation.py:14 已 import 它，方向天然），但语义上 summary_count_parse 更正、且避免'通用 bool 解析寄居 downtime 模块'的概念错位。(3) 必须 1:1 搬运、保留 (value, parse_failed) 二元组与全部分支语义，零逻辑改动。不得新建第二概念模块（红线#3）。
- **爆炸半径**：生产：5 个 prod_consumer 全在这两文件内（degradation:369/370 resource_pool_attempted/build_ok；downtime:112/115/118 load_ok/extend_attempted/extend_ok），收口后调用点不变只改 import 来源。降级摘要正确性=灵魂线：两路 parse_failed 一旦分叉→前端降级提示分叉。测试：无符号级引用；两文件被 regression_frontend_ui_language_polish.py、test_sp05_path_topology_contract.py、regression_sp06_no_duplicate_defs.py 文件级引用（不 pin 本函数）。
- **跨债依赖**：无跨桶同文件边。本债自带 parity 前置边：PHASE1 §2『R68 → R68-converge | parity 先于收敛』——parity 测试必须在 Batch-1 落地，收敛动作在 Batch-15。与 R69/R70 仅同批不同文件，无冲突。
- **必补回归测试**：新增 parity 契约（Batch-1 ROOT，纯增量）：构造输入矩阵 [key 缺失 / meta[key]=None / True / False / int 0 / int 1 / int 2(越界) / 'yes' / 'off' / 'garbage' / 非 bool/int/str 如 [] ]，对 default∈{True,False} 断言 degradation._meta_bool_state(...) == downtime._meta_bool_state(...) 【两个返回元素都比，尤其 parse_failed】；并断言越界 int / garbage str / 异类型 → parse_failed=True（钉死 used_default 灵魂线语义）。收口后该测试改为对'单一收口点'断言同矩阵行为不变。必跑保绿：现有降级行为契约（regression_schedule_summary_*、resource_dispatch/gantt *_surface_degraded、partial_overdue_summary_surfaces_warning 等）确认 parse_failed 仍冒泡。注：regression_sp06_no_duplicate_defs.py TARGETS 不含本二文件（只查单文件内重复 def），抓不到跨文件 dup，收口不会红也不被它挡（已读 :8-19 确认）。
- **误修风险与防呆**：最高危=未来 LLM'优化'时把返回值从 (bool, bool) 压扁成单 bool，或收口时丢掉越界/garbage→(default,True) 分支，改成静默返回 default 不标 parse_failed——直接杀灵魂线（坏 meta 被当正常默认值吞掉，降级提示永不触发）。次危=不补 parity 就直接删一份指向另一份，留无回归网。三危=为'统一'新建 core/shared/meta_parse 之类第二概念模块（违红线#3，summary 包已有 summary_count_parse 这个家）。防呆：parity 测试显式断言 parse_failed 位 + 越界/garbage 路径；计划写死'收口点=summary_count_parse.py 既有模块，禁新建'；评审 checklist 钉'二元组不可压扁'。绝不加兜底默认。
- **落地批次/优先级**：收口在 Batch-15（PHASE1 §4『B15 逐字两份收口』，保 used_default loud）；parity 测试前置在 Batch-1 ROOT（PHASE1 §3/§4.Batch-1 明列『R22/R68 parity』）。本桶内第二落（R70 之后），因需 Batch-1 安全网先到位。

### R69 · P4气味 + in_guard_file=true（_op_seq 两份且均静默 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — 已回盘 Read 双份 `_op_seq` 逐字节相同：core/services/scheduler/run/schedule_execution_persistence_guard.py:48-52 ↔ core/services/scheduler/run/schedule_input_runtime_support.py:19-23，两份均 `try: return int(getattr(op,'seq',0) or 0) except (TypeError, ValueError): return 0`。⚠️文案纠偏#12 盘上坐实：是 typed except (TypeError,ValueError) 非裸 except，但坏 seq 仍静默归 0。可达性已校准（Phase1 #16 纪律）：op:BatchOperation.seq 在 model 边界 core/models/batch_operation.py:81 已被 `parse_int_or_default(seq, 0, field='seq')` loud coerce（core/models/_helpers.py:101-107 对无法解析值 raise ValueError）→ 真实 BatchOperation 路径上 op.seq 恒为干净 int，该 typed-except 近乎死防御（仅当 op 非 BatchOperation 或 seq 被外部污染成非可 int 化对象时触发）。
- **具体修法**：【收口单点 + 护栏内改 loud，严禁保留静默归 0】(1) 把 `_op_seq` 收口到 run/ 包内一个两文件都能无环 import 的既有低层模块。推荐 core/services/scheduler/run/schedule_input_contracts.py（runtime_support 已 `from .schedule_input_contracts import _build_freeze_window_seed_with_meta`；persistence_guard 当前未 import 它，新增 persistence_guard→schedule_input_contracts 边经核为无环：schedule_input_contracts 仅 import core.services.common.build_outcome，无回指 persistence_guard 路径）。op 形参为 Any，函数纯属性读取，落在 services 层不需 import core.models，零越层。(2) 同时把 `except (TypeError, ValueError): return 0` 改为 loud raise（红线#1，护栏文件内坏值更应暴露）：抛 AppError(ErrorCode.SCHEDULE_CONFLICT 或合适码) 或 ValueError，消息带 op 标识与坏 seq 原值，使 persistence guard 的 revision 排序不被污染的坏 seq 短路。保留 `getattr(op,'seq',0) or 0`（None/0→0 是 model 合法默认，非兜底）。(3) 两文件删本地副本改 import 单点。绝不在合并后保留静默 return 0。
- **爆炸半径**：生产：3 个 prod_consumer——persistence_guard:143 `completed_seq=_op_seq(completed_op)`、:149 `_op_seq(op)<=completed_seq`（revision 校验过滤，:144 `completed_seq<=0` 短路）、runtime_support:216 `seq=_op_seq(op)`（_downstream_operations 过滤 seq>completed_seq）。改 loud 后：坏 seq 不再被当 0 漏判后继工序需重算。鉴于 seq 在 model 边界已 loud coerce，实际生产数据触发 raise 概率极低（投入风险低、收益=护栏一致性，符合 Phase1 #16 适度投工）。测试：无符号级引用；persistence_guard 被 regression_aps_three_gap_docs_quality_gate.py:126、test_sp05 文件级列入（不 pin 函数体）。
- **跨债依赖**：无跨桶同文件边。无前置/后置债依赖（blast 明示『无跨债依赖』）。与 R68/R70 仅同批不同文件。唯一设计前置=owner 裁断收口家 + loud-raise 方向（见 open_questions）。
- **必补回归测试**：新增护栏契约（落地前补）：(a) 对 op.seq=非可 int 化对象（如 object()/[]）断言 `_op_seq` RAISES（loud）而非 return 0——把灵魂线钉成可执行；(b) 对 op.seq=None / 0 断言 ==0（model 合法默认保留）；(c) 单点 grep 守卫：全仓 `def _op_seq` 仅 1 处。必跑保绿：regression_operation_execution_state_revision.py、regression_schedule_persistence_reschedulable_contract.py、regression_scheduler_reschedule_execution_minimum_guardrails.py、regression_scheduler_reschedule_execution_facts.py（revision/persistence 顺序行为不因 loud 化而回归——正常 int seq 路径不变）。
- **误修风险与防呆**：最高危=收口时图省事沿用静默 `return 0`，把 P4 气味原样搬进单点（红线#1，护栏文件内尤甚：坏 seq→0 会让 :149 `_op_seq(op)<=completed_seq` 误判、漏拦后继工序排到已完工序之前）。次危=收口家选错造成跨层/环：若有人图近把它塞进 core.models 或让 services→models 反向 import 制造环（op 是 Any 根本不需 model 类型，禁止越层）。三危=误把 model 边界已 loud 的事实当'所以 except 可留'借口而保留静默。防呆：契约测试(a)断言坏 seq raise；计划写死'收口家须 services 层、op:Any 不 import models'；明列'except 必改 loud，禁 return 0'。
- **落地批次/优先级**：Batch-15（PHASE1 §4『R69 _op_seq 两份收口单点 + 护栏文件内评估坏 seq 改 loud，严禁静默归 0』）。本桶内第三落（R70、R68 之后），因需 owner 先定收口家与 loud 方向。

**本单元待 owner 裁断**：
- R69 收口家归属需 owner 拍板：推荐 core/services/scheduler/run/schedule_input_contracts.py（runtime_support 已依赖、persistence_guard 新增 import 经核无环、services 层无越层）；备选=新建极小 run/op_keys.py（语义最干净但触红线#3'勿为已有概念新建模块'的谨慎区——_op_seq 当前无既有收口点，严格说不属'已有概念'，故新建非硬违规，但保守起见优先既有模块）。请 owner 在'最小新增边'(schedule_input_contracts) vs '语义最干净'(新模块) 间裁断。
- R69 loud-raise 的异常类型与错误码：persistence guard 内坏 seq 应抛 AppError(ErrorCode.SCHEDULE_CONFLICT) 还是 ValueError？前者与护栏既有 _execution_guard_conflict 风格一致、能被上层 flash；后者更轻。鉴于 model 边界已 loud coerce 使该路径近乎不可达（Phase1 #16），owner 需确认是否值得投 AppError 完整装配，还是 ValueError + 注释即可。
- R68 收口家 summary_count_parse.py 的命名贴合度：该模块名为 'count_parse' 但已实际承载 summary 包内多种 meta 解析收口；把 bool-state 解析放入是否需要顺带把模块语义注释/改名为通用 summary-meta-parse 收口点？涉及是否同步迁 downtime 侧 _meta_int_state(:23) 一并归位（它目前只在 downtime 单边，非 dup，不在本债范围，但同属 meta 解析族）——请 owner 决定本轮是否扩到 _meta_int_state 一并收，还是严格只动 R68 的 _meta_bool_state。
