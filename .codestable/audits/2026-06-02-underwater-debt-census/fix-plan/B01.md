# B01 · 承重核心·execution_review四层纵深护栏 + N1 guard收口 + plan_id死面包屑

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。


## 子单元 B01a-LB-CORE

**本单元策略**：本单元=B01 桶承重核心的 5 个承重点(execution_review「只复盘正式采用方案」纵深防御 4 处 LB01/LB02/LB05/LB06 + plan-identity 读边界 LB03)。整体策略=【纯增量·零生产逻辑改动】:只①把散在远处(schema CHECK/UI 文案/导航强制/契约测试)的护栏意图,用 §90 已起草的「我是故意的」中文注释钉回发生不对称的改动点本地;②补/认账绑死不变量的契约测试。严禁删/合并/统一/透传参数(违反=该条作废重写)。全部落 Batch-1(全局 ROOT,纯增量),是 B02/B05/B11/B13 动 execution_review/navigation_publish/feedback_service/resource_dispatch 这些高频文件前的「护栏先钉好、后续才不在裸奔期被打穿」的地基。\n\n回盘复核结论:LB01(feedback_service:347-354+451-453)✅准、文件 clean≡b08162cd;LB02≡LB05(execution_review.py:141 同一处两 finding,一次注释满足)✅准、clean;LB06 ⚠️行号 off-by-one——真实盘上 reports_page_support.py:367/369(报告写 :366/368,系转录偏移非工作树漂移,文件 clean≡b08162cd)+navigation_context.py:80-82 ✅准;LB03 🔧已超前治理——旧 _bool_from_summary 全仓零命中,已改 _parsed_summary_flag_is_true(fail_closed=True)+可观测字段 result_summary_parse_failed/_reason,4 文件 git-modified,守卫测试 regression_scheduler_plan_identity_summary_guardrail.py 仍 untracked(??)。\n\n两个关键纠偏必须采纳:(A)LB06 承重机制=显示标签身份+nav-context 泄漏+默认日期窗,NOT 行数据(core 不收参,行恒 adopted);(B)LB03 §90 LB-B4 文案已 STALE——它描述的是治理前 fail-OPEN「返回 False 容忍放行」,而现盘是 fail-CLOSED(fail_closed=True 判模拟→阻断),方向相反,绝不可逐字粘贴 LB-B4,须改写为「认账 fail-closed 治理方向」注释。

**内部排序**：单元内 5 债互不改同一行段,可并行落注释;但按时间敏感度与下游门控排序:\n(1) LB03 最先——守卫测试 untracked(??),并行 Claude 的 git clean/checkout 会删它(MEMORY 已记 concurrent-claude-git-clean-wipes-untracked),删后治理裸奔。立即 git add 四治理文件入账 + 守卫测试纳管,是抢时间窗的动作,排第一。\n(2) LB01 次之——PHASE0 §3「最危险的边」,B11 删 R17(同文件同函数 _build_event_payload)前必须 LB01 注释先落(§10.1)。high severity。\n(3) LB02≡LB05 第三——同 execution_review.py:141,一次注释满足两 finding;门控 R62(同文件 Batch-3 清三档死分支)。\n(4) LB06 第四——门控 R42/R56/R57(同 navigation_context.py / reports_page_support.py)。§90 称其为「最脆弱、重中之重」,但因 core 侧行数据已结构性 adopted-only,web 侧标签/nav 才是承重,放最后但注释一字不可省。\n注:四条 LB01/LB02/LB05/LB06 + LB03 全在同一 Batch-1 提交内交付,「先后」指审阅/落笔顺序而非跨提交,目的是确保 git diff 仅含注释+测试、零生产逻辑。

### LB01 · P2 承重护栏(load_bearing=true, _adv_refuted= · sev=high · load_bearing=True

- **当前位置(已复核)**：✅准 — core/services/scheduler/operation_execution_feedback_service.py:347-354 (_load_current_official_schedule 硬拒 requested/effective_plan_role!=ROLE_ADOPTED 或 source_table!=SOURCE_SCHEDULE 或 scenario_id is not None → raise _conflict('not_current_official_plan')) + :451-453 (_build_event_payload 写死 source_table=SOURCE_SCHEDULE / effective_plan_role=ROLE_ADOPTED / scenario_id=None)。已 Read 盘上逐行确认,文件 git status 干净、git diff b08162cd 为空(提交态≡基线),报告行号完全一致。:455 _REPORTED_STATUS_BY_ACTION[action] 紧贴写死区(R17 编辑边界)。
- **具体修法**：唯一合法动作=补「我是故意的」中文注释 + 认账已存在契约测试(零删除/零透传)。逐字文案直接复用 §90 LB-A3:
【贴在 :451 上方】
# 故意写死、故意忽略 context 的同名字段:这是消毒层(defense-in-depth)。
# 即便上游校验被绕过,持久化也绝不落 candidate/scenario 身份的现场事件——
# 现场执行事实只能挂在正式采用方案上,否则会污染下游重排护栏的决策依据。
# 勿改成透传 context.source_table/effective_plan_role/scenario_id。
【贴在 :348 上方(或 :347 方法定义上方)】补一条说明硬拒分支是写侧 adopted-only 第一道闸、与 :451-453 消毒层是纵深而非冗余的注释(可凝练 §90 LB-A3 同义)。契约测试断言点(均已存在,无需新建):feedback_routes.py:336 端到端 POST start 带 plan_role=baseline_best / adopted+scenario-plain / version=1 历史三 case → 409 not_current_official_plan 且 _event_count==0;exception_feedback.py:485 异常写侧同三 case → 409;event_foundation.py:272 repo 级 insert_event(source_table='candidate_rows') 必 raise(schema CHECK 底)。precondition(c) 端到端回归已满足,注释落地即闭环。
- **爆炸半径**：炸到的生产文件(误改时):operation_execution_feedback_service.py 本体;下游 execution_fact_provider.py:35(纯按 op_id 读无 role/scenario 过滤)→ run/schedule_execution_guardrails.py:186(facts_by_op_id 喂重排护栏)→ 坏数据进排产决策(数据完整性事故)。反向:operation_execution_event_repo.py:191-193 insert_event 用 payload.get('source_table') or 'schedule' 透传不强制常量——证明 451-453 写死非冗余;若改透传,合法 adopted 反馈反触发 IntegrityError→AppError(DB_INTEGRITY_ERROR) 变用户报错。本条只补注释,无生产爆炸半径。
- **跨债依赖**：【必须先做】本注释先落,B11 才能删 R17(同文件 operation_execution_feedback_service.py:12 EXECUTION_EVENT_EXCEPTION 死导入 + :48 operation_execution_labels 垫片导入 + support.py:64 死键),R17 合法改动仅限删 import 一/两行,绝不碰 347-354/451-453(PHASE0 §3「最危险的边」LB01↔R17 同 _build_event_payload 函数)。【先于】R20(B13 改 :48 import 行)亦须等本注释。【同源】LB02/LB05/LB06 是同一「只复盘正式采用方案」不变量的读侧/web 侧对称点;R54 投影 can_write_feedback 等 plan-guard 字段与本债身份判定同源。
- **必补回归测试**：无需新建(precondition 已满足):①feedback_routes.py:336 端到端写侧拒绝(候选/scenario/历史三 case→409 + 零落库);②exception_feedback.py:485 异常写侧同款;③event_foundation.py:272 schema CHECK 写入禁线。可加一条注释守护测试(可选):断言 _build_event_payload 输出的 source_table/effective_plan_role/scenario_id 恒为 SOURCE_SCHEDULE/ROLE_ADOPTED/None,与 context 同名字段解耦(把『不透传』钉成机器可验)。
- **误修风险与防呆**：未来 LLM 最可能以『ExecutionFeedbackContext 都带了 requested_plan_role/source_table/scenario_id 字段,为何 _build_event_payload 不透传』为名,把 :451-453 三写死改成 context.*,或以『简化重复校验』删 :348-354 硬拒分支。本地防呆=注释明写『故意忽略 context、勿改透传』;机制防呆=event_foundation:272 + feedback_routes:336 端到端回归会红。B11 动 R17 时顺手『清理/重构』_build_event_payload 是次高危——注释 + §10.1 顺序(B01 先落)双重拦截。
- **落地批次/优先级**：Batch-1(全局 ROOT,纯增量零结构)。单元内排序第 2(LB03 之后),因其为『最危险的边』、门控 B11 的 R17,且 high severity。

### LB02 · P2 承重护栏(load_bearing=true, refuted=false · sev=medium · load_bearing=True

- **当前位置(已复核)**：✅准 — core/services/report/execution_review.py:141 (execution_review(self, version, *, date_from, date_to, batch_id, resource_type, resource_id) 形参刻意不含 plan_role/scenario_id) + :112/:123 (_execution_review_plan_rows 两分支恒 plan_role=ROLE_ADOPTED, scenario_id=None) + :153 (host._resolve_plan(v, ROLE_ADOPTED, None)) + :166-167 (返回 plan_label=plan_role_label(ROLE_ADOPTED)/plan_role=ROLE_ADOPTED) + :178 export_execution_review_xlsx 同样不收。已 Read 逐行确认,文件 clean≡b08162cd,grep『故意/刻意/不对称』仍零命中。
- **具体修法**：在 :141 签名上方补 §90 LB-A2 逐字文案(零删除/零统一):
# 本方法故意只复盘 ROLE_ADOPTED,不收/不透传 plan_role/scenario_id——
# 与 overdue/utilization/downtime 三兄弟的签名不对称是刻意的护栏,不是遗漏。
# 勿为『统一四张报表方法签名』而加 plan_role/scenario_id 形参:
# 会让模拟预览/对比参考方案的明细以『正式现场复盘』身份呈现给车间并可导出。
并在 :112/:123/:153 三处硬钉旁各补一行短注(同 §90 LB-A2 同义,可凝练为『# 硬钉 adopted/None=护栏,勿改形参透传』),把不变量可见化到执行现场。契约测试:新增请求级负向回归补最大盲区(见 regression_tests)。LB02 与 LB05 是同一处两 finding,本注释一次满足两条。
- **爆炸半径**：炸到:execution_review.py 本体;export_execution_review_xlsx(:178→:188 调 execution_review);web/routes/reports_page_support.py:327(页路由)/reports_export_routes.py:101(导出路由);report_plan_helpers.py:56-106(_list_plan_rows_* 完整透传 role/scenario 进 list_detail_rows——execution_review 是数据层最后一道把关);schedule_plan_query_service.py:176/:460/:494(透传即换源表)。误改后果:预览/对比方案明细拼进『计划vs实际』并经 xlsx 导出,返回 dict 不拦截 is_scenario_preview→静默冒充正式(违灵魂线)。本条只补注释+测试,无生产爆炸半径。
- **跨债依赖**：【同点合并】LB05 与本债同 execution_review.py:141(不同 _partition:scheduler-plan-identity vs core-svc-domain),修法合并为同一注释 + 同一组请求级回归。【必须先做】本注释先落,R62(B01 Batch-3,同文件 :287-348 清三档标签死分支)才能动,清理时绝不碰 :112/:123/:153/:166-167 五处硬钉、更不能给 :141 加形参。【web 侧对称】LB06 是本服务护栏的 web 路由层对应(reports_page_support 写死喂同一 service);R54 投影 guard 字段时不得改变 execution_review 固定 adopted 取数语义。
- **必补回归测试**：【必补·补当前最大盲区】新增请求级合同测试(全仓现无):client.get('/reports/execution-review?version=..&plan_role=baseline_best&scenario_id=..') 断言 ①返回 rows 与渲染 plan_role 仍为正式采用方案;②导出 xlsx 不含预览/对比数据;③(service 层)若未来加形参,收到非 adopted role / 非 None scenario_id 必 raise ValidationError(loud,不静默回退)、返回前校验 resolution.is_scenario_preview is False。现有 backlink:355 / plan_vs_actual:358-359 仅是链接生成/导航的负向 not-in 断言,NOT 请求级——不可当替代。LB02/LB05/LB06 三条共享此盲区,一组请求级回归(service 级覆盖 LB02/05 + 页路由级覆盖 LB06)一次补齐三 precondition。
- **误修风险与防呆**：未来 LLM 以『与 overdue/utilization/downtime 三兄弟对齐、统一四报表签名』之名给 :141 补 plan_role/scenario_id 形参并在路由接上 request 值——diff 看似正向重构,CI 因缺请求级回归而全绿,但 service 层契约已破、预览静默冒充正式。本地防呆=:141 + 三处硬钉注释明写『刻意不对称、勿加形参』;机制防呆=新增请求级负向回归。R62 重构同文件时是次高危触发点。
- **落地批次/优先级**：Batch-1(纯增量)。单元内排序第 3(与 LB05 合并)。门控 R62(Batch-3)。

### LB05 · P2 承重护栏(load_bearing=true, refuted=false · sev=high · load_bearing=True

- **当前位置(已复核)**：✅准 — core/services/report/execution_review.py:141(与 LB02 同一处)。LB05 强调 :112/:123 _list_plan_rows_* 硬钉 plan_role=ROLE_ADOPTED,scenario_id=None + :153 _resolve_plan(v,ROLE_ADOPTED,None) + :166-167 固定标签 + export_execution_review_xlsx(:178)同不收。已 Read 确认盘上一致,文件 clean≡b08162cd。LB05 与 LB02 是同一承重不对称的两条独立 finding(_partition: core-svc-domain),修法合并为同一注释动作。
- **具体修法**：与 LB02 合并:同一份 §90 LB-A2 注释贴 :141 + :112/:123/:153 三处硬钉旁短注,一次满足 LB02+LB05 两 finding(零删除/零统一)。即时最低成本动作=补注释把散在 forbidden-params/design.md 的不变量可见化到服务层执行现场。若未来确需放开历史正式版本(非本批工作),precondition:必须先在 service 内对非 ROLE_ADOPTED role/非 None scenario_id 显式 raise ValidationError(loud)、再给 aggregate_states_by_op_ids 加 role/scenario 作用域过滤、再路由入站加闸门并保留 web forbidden-params 守卫、最后用 regression_plan_vs_actual_review/reports_workbench_navigation_contract 锁死——本批不做,仅注释。
- **爆炸半径**：同 LB02:execution_review.py 本体 + report_plan_helpers.py:29-106(host 不设防、完整透传)+ operation_execution_event_repo.py:267(下游 join 无法自卫)+ web forbidden-params 守卫认知错位。误改→模拟/历史方案明细以『计划和现场实际』正式身份呈现给车间(预览冒充既成事实,数据完整性事故)。本条只补注释,无生产爆炸半径。
- **跨债依赖**：【同点合并】与 LB02 同 execution_review.py:141,一次注释 + 一组请求级回归同时满足两条(避免重复投工)。【必须先做】先于 R62(同文件死分支清理)。【web 侧】LB06 web 层若被『统一从 request 读 plan_role』会与本服务层硬钉产生两层认知错位——任一层单独『统一』即打穿纵深,故 LB02/LB05/LB06 注释须同批落齐。
- **必补回归测试**：与 LB02 共享同一组请求级回归(见 LB02 regression_tests)。LB05 额外强调:测试须覆盖 aggregate_states_by_op_ids 喂任何 op_id 都照配的下游自卫缺失——故请求级断言必须验到『非 adopted 上下文下 service 仍硬钉 adopted』,而非仅链接层。现有 plan_vs_actual_review.py:350-359 / workbench_link_guardrails.py:131,149,170 仅钉 viewmodel link builder,不钉 service 入参路径,是盲区本身。
- **误修风险与防呆**：同 LB02:『统一四报表签名』重构是引爆动作。LB05 特有提示=保护证据藏在 web viewmodel forbidden-params + roadmap design.md,改 service 的人看不到——所以注释钉回服务层本地是唯一对无记忆协作者有效的阻止信号。防呆同 LB02。
- **落地批次/优先级**：Batch-1(纯增量)。单元内排序第 3(与 LB02 合并为同一注释动作)。

### LB06 · P2 承重护栏(load_bearing=true, refuted=false · sev=high · load_bearing=True

- **当前位置(已复核)**：⚠️行号已变(新行号)——真实盘上 web/routes/reports_page_support.py:367 (plan_resolution=page_plan_resolution(services.schedule_plan_query_service, version, 'adopted', None)) + :369 (page_date_range_or_version_span(engine, int(version or 0), 'adopted', None, raw_date_from, raw_date_to))，二者在 execution_review_page_context()(:364)内。报告写 :366/:368 系 off-by-one 转录偏移(文件 git status 干净、git diff b08162cd 为空,非工作树漂移)。web/navigation_context.py:80-82 ✅准(current_workbench_navigation_context 内 is_execution_review 为真时 plan_role=ROLE_ADOPTED、scenario_id='')，_is_execution_review_request 在 :42-47(endpoint=='reports.execution_review_page' 或 path=='/reports/execution-review')。两文件均 clean≡b08162cd。
- **具体修法**：在 :367 上方补 §90 LB-A1 逐字文案(并把 navigation_context.py:80-82 同义注释钉上),零删除/零统一/不改成从 request 读:
# 故意写死 adopted/None,不从 request 读 plan_role/scenario_id:这是护栏,不是漏掉的方言。
# 计划和现场实际只复盘正式采用方案;放开会让模拟预览/对比方案冒充正式复盘展示给车间。
# 勿为『统一所有报表页从 request 读参数』而改成 request_plan_role()/request_scenario_id()。
:369 默认日期窗处补一行短注(同义,强调 span 也须用 adopted plan)。navigation_context.py:80-82 上方补『is_execution_review 强制 adopted/清 scenario 是护栏,守未发布 context 的回退路径,勿删』。若仅为消魔法字面量方言,可抽同文件具名收口 execution_review_plan_context()(内部强制 adopted/None)替两处字面量(不新建模块、不收口到 §4 之外)——但本 Batch-1 建议仅注释、抽收口留作可选(见 open_questions)。
- **爆炸半径**：炸到:reports_page_support.py 本体;reports.py:36-37(execution_review_page→context)；report_plan_template_fields.py→templates/reports/execution_review.html:56,61(标签渲染)；navigation_context.py:77-99(published override 链)→ build_report_navigation_links 下游链接。误改(:367/:369 改成 request_*)→ page_plan_resolution 走 resolve_plan_view→_resolve_scenario_plan(query_service:182) 渲染『模拟方案甲』/preview 标签盖 adopted 行(header≠body 自欺,违灵魂线不自欺)+ nav context 经 :373 泄漏 plan_role/scenario_id 进下游链接 + 默认日期窗用 preview span。本条只补注释,无生产爆炸半径。
- **跨债依赖**：【必须先做】本注释 + navigation_context.py:80-82 注释先落,R42(同文件 reports_page_support.py + navigation_context.py:86 删 plan_id 死面包屑)、R56(navigation_context.py:42-47/:80-82 字面量匹配护栏,与 LB06 共享 :80-82)、R57(navigation_context.py:76-98 双构建路径 fallback)才能动——R42 清 :86 plan_id 绝不碰 :80-82 强制分支;R56/R57 重构双路径必须保留 :80-82 强制 + reports_page_support.py:367/369 写死(两条独立护栏守不同代码路径,非冗余)。【web↔core 对称】LB06 是 LB02/LB05(core execution_review service)的 web 层对应,三条须同批落齐避免两层认知错位。
- **必补回归测试**：【必补·页路由级盲区】新增 GET /reports/execution-review?plan_role=baseline_best&scenario_id=xxx 断言:①渲染的『排产方案』标签仍为正式采用方案、source_text 不出现预览/对比文案;②发布的 nav context plan_role=adopted 且无 scenario_id；③默认日期窗为 adopted plan span。现有只钉 viewmodel link builder(workbench_link_guardrails.py:131,149,170 / workbench_links_contract.py:281)、navigation_contract.py:84,130 仅 no-param/superseded-adopted——均不覆盖 :367/:369 页路由带显式 plan_role= 参数的场景。可与 LB02/LB05 的请求级回归合并为一个 execution-review adopted-only 端到端测试文件(service 级 + 页路由级)。注:regression_scheduler_plan_identity_summary_guardrail.py:59-74 经 :367 链路间接覆盖坏摘要可见性(LB06↔LB03 交叉)。
- **误修风险与防呆**：§90 列为『b08162cd 后新增最脆弱两处之一』。未来 LLM 以『消除参数方言、统一所有报表页都从 request 读 plan_role』之名把 :367/:369 的 'adopted',None 改成 request_plan_role()/request_scenario_id()——diff 像无害一致性清理,CI 因缺页路由级回归全绿,实为安全回退(标签/nav 不变量破)。本地防呆=两处写死 + navigation_context.py:80-82 注释明写『故意忽略 request 是护栏』;机制防呆=新增页路由级回归。绝不可给 core execution_review() 加形参(行数据承重在 core 须原样保留)。R56/R57 重构 navigation_context 是次高危触发点。
- **落地批次/优先级**：Batch-1(纯增量)。单元内排序第 4。门控 R42/R56/R57(SCC-NAV)。

### LB03 · P4 灵魂线债(load_bearing=true 但 _verdict ref · sev=low · load_bearing=True

- **当前位置(已复核)**：🔧已治理(工作树超前) — 原报告 _bool_from_summary(builder.py:24-29 except (TypeError,ValueError):return False)全仓 git-grep 零命中,已不存在。现盘 core/services/scheduler/schedule_plan_identity_builder.py:24-27 _parsed_summary_flag_is_true(parsed, key, *, fail_closed=False)(parse_failed 时返回 bool(fail_closed));:141 build_plan_identity 调用处显式 fail_closed=True(P4 由静默『安全默认 False/容忍放行』改为 fail-CLOSED 判模拟);新增可观测字段 :184-185 result_summary_parse_failed=bool(summary_parse.parse_failed)/result_summary_parse_reason;另有 :136 source 非法 raise ValueError、:152-153 result_status∈_BLOCKED_RESULT_STATUSES 独立兜底。四文件 git-modified:schedule_plan_identity_builder.py / core/models/schedule_plan_identity.py(:31,58 字段+to_dict)/ core/models/schedule_plan_resolution.py(:90 透传)/ web/routes/reports_plan_template_fields.py(:37『当前排产摘要读取失败』可见缺口)。守卫测试 tests/regression_scheduler_plan_identity_summary_guardrail.py 仍 untracked(git status ??)。注:盘上函数名为 latest_official_version(builder.py:30,返回首个 version>0),报告/verdict 提的『latest_executable_official_version 全量扫历史』实指 is_current_executable_official_version 字段链按行调 build_plan_identity。
- **具体修法**：本债 P4 灵魂线【已治理】,剩余动作仅三件,零重新动逻辑:(1) 确认治理随 builder/identity/resolution/template_fields 四文件一并提交入账(当前 git-modified,须进同一提交);(2) 守卫测试 regression_scheduler_plan_identity_summary_guardrail.py 立即 git add 纳入版本管理(当前 ?? untracked,防并行 git clean 删);(3) 在 :141 调用处(fail_closed=True)与 :24-27 helper 定义处补一行注释,认账 fail-closed 是故意的。⚠️注释文案不可逐字粘贴 §90 LB-B4——LB-B4 描述的是治理前 fail-OPEN『对坏 summary 返回 False 是有意容忍/放行』,与现盘 fail-CLOSED(fail_closed=True 判模拟→阻断写反馈)方向相反,已 STALE。应改写为:
# fail_closed=True:坏/无法解析的 result_summary 在身份判定上按『模拟』处理(fail-closed),
# 配合 result_summary_parse_failed 可观测字段在前台暴露『当前排产摘要读取失败』可见缺口——
# 这是灵魂线『坏数据不静默兜底』的落地:用非致命标记而非 raise(latest_official_version 身份链
# 按行扫历史,改 raise 会让一条 legacy 坏行炸全链可用性)。安全不变量另由 result_status='simulated'
# (ctx.simulate 原子同写、Gate1 独立先检)兜底,不单依赖本 summary 判定。
- **爆炸半径**：治理已生效,无新增爆炸半径。涉及链:builder.py:141→is_simulation→_is_official_plan(:79-86)/is_current_official(:154);身份字段下游消费 is_current_executable_official_version/can_write_feedback:web/viewmodels/scheduler_workbench_links.py、web/routes/dashboard.py、scheduler_resource_dispatch.py、scheduler_navigation_publish.py;可观测字段 result_summary_parse_failed → reports_plan_template_fields.py:37 用户可见文案。误改风险=有人改回 fail_closed=False(退回静默吞噬)或按旧报告改 raise(可用性放大)。
- **跨债依赖**：【弱耦合】与 B01 其他四债不同文件,无同改边。【强绑】B02 的 R23(schedule_plan_query_service boolean 归一,同属 §4 方案身份收口族)——R23 收口 boolean 时不得让 summary 解析退回静默。【共享文件】治理文件 schedule_plan_identity.py/schedule_plan_resolution.py 被 R54(guard 字段投影)+dashboard 等多债共享 is_current_executable_official_version 投影,R54 收口 guard 字段时须保留 result_summary_parse_failed 不被剥离。
- **必补回归测试**：守卫测试已存在(untracked,须 git add 纳管):regression_scheduler_plan_identity_summary_guardrail.py — :25 test_bad_current_result_summary_is_visible_gap_and_blocks_feedback_urls(坏 JSON→dashboard/review 出现『当前排产摘要读取失败』可见 + 禁写 URL);:77 test_bad_newer_summary_does_not_promote_previous_official_version(fail-closed 端到端:latest result_summary_parse_failed=True→is_current_executable_official_version False,旧版本 previous 也 False、不被提升);:114 test_dashboard_invalid_plan_identity_is_visible_gap_instead_of_500(可见缺口而非 500)。这三条恰好钉死 fail-closed 语义 + 可见缺口契约 + 不提升旧版本,纳管后即护栏。
- **误修风险与防呆**：两类高危:(1) 他人按旧报告/§90 LB-B4 STALE 文案『P4 改 loud raise』动手→与现 fail-closed 治理冲突 + 触发 verdict 警示的『一条 legacy 坏行炸 latest 身份全链可用性』放大事故;(2) 误把 :141 的 fail_closed=True 改回 False(退回静默当『非模拟』容忍放行,重违灵魂线)。本地防呆=:141/:24-27 注释明写 fail_closed=True 故意 + 引用 verdict『勿改 raise』;机制防呆=守卫测试三断言(尤其 :105 is_current_executable_official_version is False)会红。最大时间敏感风险=守卫测试 untracked,并行 Claude git clean 删后治理裸奔——必须本批最先 git add(MEMORY: concurrent-claude-git-clean-wipes-untracked)。
- **落地批次/优先级**：Batch-1(纯增量,Phase1 §Batch-1 明列『LB03 确认四文件入账 + 立即 git add 守卫测试 + :141 注释』)。单元内排序第 1(守卫测试 untracked 的时间窗最紧)。

**本单元待 owner 裁断**：
- LB03 注释文案授权:§90 LB-B4 起草的逐字文案(『对坏 summary 返回 False 是有意的容忍...不可改 raise』)描述的是治理前 fail-OPEN 状态,与现盘 fail-CLOSED(fail_closed=True 判模拟→阻断)方向相反、已 STALE。本计划已改写为『认账 fail-closed 治理方向』的新文案。需 owner 裁断:确认采用改写文案、废弃 LB-B4 逐字粘贴(否则照搬 LB-B4 会与现治理矛盾)。
- LB03 守卫测试 regression_scheduler_plan_identity_summary_guardrail.py 当前 untracked(??),且四治理文件 git-modified 但未提交。需 owner 确认:是否在本 Batch-1 即把『治理(四文件)+ 守卫测试』作为既成事实纳入提交入账(本轮只读不改生产代码,但 git add 已存在的工作树治理+测试属于『认账』而非『改逻辑』)——抑或治理入账归属另一已在途的提交、本计划仅负责补 :141 注释?边界需裁断,避免与并行工作撞车或漏 add 致裸奔。
- LB02/LB05/LB06 共享同一请求级回归盲区(GET /reports/execution-review?plan_role=非adopted&scenario_id=X)。Phase1 Batch-1 要求『一次补齐三条 precondition』。需 owner 确认:由一个合并测试文件同时覆盖 service 级(LB02/05:断 service 仍硬钉 adopted)+ 页路由级(LB06:断标签/nav context/默认日期窗仍 adopted),还是拆两个文件?建议合并以防重复,但拆分更贴合 core/web 分层——需裁断。
- LB06 是否在本批顺手抽具名收口 execution_review_plan_context()(内部强制 adopted/None)替换 :367/:369 两处魔法字面量?precondition 选项(3)允许但非必须。本计划建议 Batch-1 仅补注释(保持 git diff 纯注释+测试、零结构改动),抽收口留作后续可选项。需 owner 确认是否接受『仅注释、暂不抽收口』。
- LB01 是否需要新增一条『_build_event_payload 输出与 context 同名字段解耦』的注释守护测试(断言输出恒为 SOURCE_SCHEDULE/ROLE_ADOPTED/None)?现有 feedback_routes:336/exception_feedback:485/event_foundation:272 已端到端覆盖拒绝路径,precondition(c) 已满足;此守护测试为可选增强(把『不透传』钉成单元级机器可验)。需 owner 裁断投入与否。


## 子单元 B01b-N1-PLANID

**本单元策略**：本单元4条债是 b08162cd 工作台收口在 SCC-NAV 接缝处长出的"新失忆债",全部围绕 execution_review「只复盘正式采用方案」这条灵魂护栏。两类:(A)承重立约类 R54(N1,high)+R56(P2,medium)——护栏正确性散在易漂移的手维结构里,只能收口+补注释+绑契约,严禁删/统一/透传;(B)死面包屑直删类 R42+R60(P6)——plan_id 全链零 resolver 消费,grep 已盘上复证 core/+data/ 零命中,合并为单次下线。

内部排序严格遵循 Phase1 DAG 的 B01-最先落脊梁:
1) **R56 先(Batch-1 注释网)**:navigation_context.py 未漂移、行号精确,只补"我是故意的"注释(13.N3 文案直接复用)+绑既有 rename 守卫 regression_plan_vs_actual_review.py:350-359。纯增量零结构,给后面所有动这文件的债钉死 :80-82 不能碰。
2) **R54 次(Batch-2 guard 真相源,独立成批)**:让收口点 build_workbench_plan_context 承载 plan-guard 字段(从 plan_role_filter_fields 单一来源),4 套手维列表(含报告漏列、本 agent 盘上复证的第4套 dashboard_workbench.py:19-24)统一 delegate。这是 R42/R60 删 plan_id 形参、以及跨桶 R44(B02)动 navigation_publish 的签名地基,必须原子先落。
3) **R42+R60 末(Batch-3 plan_id 下线,合并单动作)**:rebase 在 R54 新签名上删整条读-存-吐-字段表链 + 同步退 navigation_contract 三断言(只退 plan_id,保 back_to)+ 同步删 3 处 roadmap 制度化。

最危险失效模式(本 agent 复核坐实):R54 漏收第4套 dashboard_workbench → fail-OPEN(_is_formal_adopted_context 对 is_comparison/is_superseded 用 truthy 判定,漏拷=falsy=不拦=旧正式版本冒充现行采用方案放行写侧)。这是结构性"未来单改一处即静默失护栏"风险,非当前已坏——4套当前都恰好带齐3关键键。R54 与 R42/R60 必须同批次或显式串行(同改 build_workbench_plan_context 签名/dict,否则互撞行号)。

**内部排序**：严格按 Phase1 全局排序 DAG 的 B01-脊梁(B01 必须最先落):R56(Batch-1,纯增量注释+契约,给 navigation_context.py 钉死不可碰区)→ R54(Batch-2,独立成批,把 guard 字段收口进 build_workbench_plan_context 确立唯一真相源,4套手维列表 delegate)→ R42+R60(Batch-3,合并为单次 plan_id 全链下线,rebase 在 R54 新签名上)。理由链:(1)R56 是承重注释属 Batch-1 ROOT,纯增量零结构,先铺安全网;(2)R54 是 N1 真相源,R42/R60 删 plan_id 形参要 rebase 其签名、跨桶 R44(B02)动 navigation_publish 也须等 R54 guard 收口,故必须独立先落原子完成;(3)R42/R60 是同一 plan_id 死面包屑两面(共享 link_query:118/154 emit 点),必须同批合并否则留半截残渣。R54↔R42 同改 build_workbench_plan_context 签名/dict,Phase3 排序上 R54 与 R42/R60 必须同批次或显式串行（R54 先加 guard 字段，R42 后删 plan_id），不可并行互撞行号。

### R56 · P2(承重护栏,只补注释类). b08162cd 整文件新增=最脆弱接缝. 护栏 · sev=medium · load_bearing=True

- **当前位置(已复核)**：✅准(精确命中,navigation_context.py 整文件 NOT 在16文件漂移名单,git status 无 M 标记,行号与报告完全一致). web/navigation_context.py:42-47 _is_execution_review_request(字面量匹配在:47 `endpoint=='reports.execution_review_page' or path.rstrip('/')=='/reports/execution-review'`); web/navigation_context.py:80-82 current_workbench_navigation_context 消费(:81 `plan_role=ROLE_ADOPTED if is_execution_review`; :82 `scenario_id='' if is_execution_review`). 字面量 rename 暴露面盘上复证57处(web/+tests/),其中活护栏依赖点: page_manuals_registry.py:47 / scheduler_navigation_links.py:122 / scheduler_workbench_links.py:25. override 绕过路径在 :77-79 _navigation_context_override(经 publish 侧已含 guard).
- **具体修法**：唯一合法动作=①补'我是故意的'中文注释(直接逐字复用 13.N3 已起草文案,贴在 navigation_context.py:80 上方):`# 故意:复盘页(execution_review)强制 plan_role=adopted、scenario_id="",剥离 request 的方案身份。# 这是护栏——计划和现场实际只复盘正式采用方案,放开会让模拟预览/旧正式版本冒充现行正式复盘。# 勿删此 if 特例,勿因重命名路由而让 _is_execution_review_request 失配(它靠 endpoint/path 字面量)。` 另在 :42 _is_execution_review_request 上方补一行说明它是字面量匹配护栏判定核心、与 override 路径(:77-79,由 publish 侧 navigation_publish 已含 guard 负责)分工。②绑/强化既有契约测试 regression_plan_vs_actual_review.py:350-359(真正的 rename 守卫:访问 /reports/utilization 带 scenario_id 后断言 execution-review 链接不含 plan_role/scenario_id+顶导禁用'计划和现场实际只复盘正式采用方案'). 零删除/统一/透传,不新建模块,严禁因'字面量难看'重构匹配方式.
- **爆炸半径**：全站导航 chrome(render_bridge.init_ui_mode 注入 Jinja globals→每页渲染调 current_workbench_navigation_context). 复盘页若泄漏 scenario_id 则正式方案复盘被模拟方案身份污染,违背收口目标. 注释本身零运行期改动(git diff 仅注释+测试断言).
- **跨债依赖**：Phase1边: LB06→R56(承重先于动同文件,navigation_context.py:80-82 同段,R56注释先落或同提交)、R56→R57(navigation_context.py:80-82 同几行共享,R57 fallback 收敛须保留80-82强制 adopted). 同文件四债 LB06/R42/R56/R57 同桶 B01(PHASE0§2),本桶内一次性协调. R42 删:86 plan_id 时绝不能碰:80-82(R56本体). 本债是 Batch-1 ROOT,必须先于 R54/R42/R60 落地(给同文件后续动作钉死不可碰区).
- **必补回归测试**：①绑牢 regression_plan_vs_actual_review.py:350-359(本质行为锁:复盘页不泄漏 scenario_id/plan_role). ②建议在该契约补一条'换路由名/换 path 后 scenario_id 仍不泄漏'的断言意图(注释挡不住 rename,契约测试才是真守卫). ③既有 regression_page_manual_registry.py:512(endpoint 字面量登记,rename 连带破坏面)、regression_scheduler_workbench_links_contract.py:283-295/307-327(preview 禁用/参数收敛)保持绿. 验收:零生产逻辑改动,现有契约全绿.
- **误修风险与防呆**：未来 LLM 最可能:(a)把字面量匹配'优化'成集中常量/删 if 特例当坏味道清理→即便有注释仍失护栏,故契约测试必须覆盖 rename 后不泄漏断言;(b)重命名路由而漏改 :47 字面量→护栏静默失配,57处暴露面任一改动都可能触发. 本地防呆=注释 + regression_plan_vs_actual_review:350-359 + page_manual_registry:512 三重绑定. 红线:load_bearing+P2,绝不删/统一/透传.
- **落地批次/优先级**：Batch-1(承重注释+契约测试网,全局 ROOT,纯增量零结构). 本桶内最先落.

### R54 · P5(收口类). 同一份 plan-guard 字段投影被切成4套手维 key  · sev=high · load_bearing=True

- **当前位置(已复核)**：⚠️行号已变+报告少1套(盘上复证为4套手维列表,报告只点名3套;4个源文件除 navigation_publish 外均在漂移名单,已逐一回盘). 收口点 plan_role_filter_fields ✅准:core/services/scheduler/schedule_result_view_context.py:277-338(返回~26键全集,含全部 guard 字段). 4套手维列表:①web/viewmodels/scheduler_reports_workbench.py:40-55 _copy_plan_guard_fields(mappings:42-52,9键,源键 requested_role/selected_role/is_official/is_preview,无 plan_role_status/can_write_feedback),调用:93;②web/routes/domains/scheduler/scheduler_navigation_publish.py:12-24 _PLAN_GUARD_FIELD_NAMES(11键,含 plan_role_status:15+can_write_feedback:23),_plan_guard_fields:75-77,_publish_context:80-88;③web/routes/domains/scheduler/scheduler_resource_dispatch.py:63-77 _copy_plan_guard_fields(9键元组:64-74,无 plan_role_status/can_write_feedback),调用:102;④【报告漏列、盘上新证】web/viewmodels/dashboard_workbench.py:19-24 _PLAN_GUARD_FIELD_NAMES(12键,含 plan_role_status+can_write_feedback+plan_identity_error),应用:137-139. 收口地基 build_workbench_plan_context:183-251,sink dict:226-251【确认零 guard 字段】只透传 plan_role/is_preview/can_write_feedback. 下游消费者 _is_formal_adopted_context:271-289(报告271-287,+2行),execution_review 放行点:312调用,can_emit_feedback_write_urls:381-387二次复用. ⚠️关键纠偏:plan_identity_error(第4套含)是 dashboard 本地错误标记(dashboard.py:121设),非 plan-guard 字段、不在 plan_role_filter_fields 产出——收口时须保留 dashboard 该键的本地来源,不可强塞进通用收口点.
- **具体修法**：P5 收口到【已存在】统一点 build_workbench_plan_context(scheduler_workbench_links.py:183-251),非新建模块:①给 build_workbench_plan_context 增加承载 plan-guard 字段的入参(或接收一个 plan_resolution/guard dict),内部从单一来源 plan_role_filter_fields(schedule_result_view_context.py:277)产出全集 guard 字段写进 sink dict:226-251;②4套手维列表全部改为 delegate 到收口点输出/plan_role_filter_fields 全集,删除各自私有 key 列表与映射(reports_workbench:40-55、navigation_publish:12-24+75-77、resource_dispatch:63-77、dashboard_workbench:19-24);③dashboard 第4套的 plan_identity_error 是本地错误标记,保留其 dashboard.py:121 本地来源,不并入通用收口点(它不是 plan-guard 字段);④load_bearing=true 所以收口同时补'我是故意的'注释钉死键集单一来源(复用 §90 N1 节口径:护栏字段必须从 plan_role_filter_fields 单一产出,漏拷 is_comparison/is_superseded 即 fail-open 放行旧正式版本冒充现行,勿再散成手维列表). 严禁删某套而不迁下游、严禁透传裸 key.
- **爆炸半径**：报表全入口卡+行级动作+甘特/周计划/分析/派工导航上下文4条链全依赖手拷字段完整性,漏一键即 execution_review 写侧护栏静默失效. 触碰生产文件:scheduler_workbench_links.py(收口点)、scheduler_reports_workbench.py、scheduler_navigation_publish.py、scheduler_resource_dispatch.py、dashboard_workbench.py、schedule_result_view_context.py(单一来源). navigation_publish 跨桶同居 R44(B02)+R58(B01承重),改它收 guard 字段时不得碰 R58 的:86 context.update 承重逻辑.
- **跨债依赖**：Phase1边: R54→R42(同改 build_workbench_plan_context:187形参/:229存储,必须同批次否则互撞行号/dict键位移)、R54→R44(承重先于动同文件,B01 guard 收口进 build_workbench_plan_context 必须先落,B02 的 R44 改 navigation_publish:31-32 才能动,且不碰 _PLAN_GUARD_FIELD_NAMES 闸门). co-change: R56(navigation_publish 是 R54 投影源之一,navigation_context.py:64 调 build_workbench_plan_context)、R60(navigation_links.py 同文件相邻,删 plan_id 字段表勿动 guard). PHASE0§10硬约束#1:B01(本债)必须最先落且 guard 收口+契约测试后,B02/B11 才能动同文件,否则护栏裸奔期打穿.
- **必补回归测试**：必须保留/迁移既有4个 guard 契约(删任一手维列表前确认其契约改读收口点输出):①regression_reports_workbench_navigation_contract.py:406-443 test_scheduler_navigation_publish_keeps_plan_guard_fields(钉11键经 navigation_publish 透传齐全+execution_review 顶导被禁);②regression_resource_dispatch_site_records_frontend_contract.py:177-211 test_execution_review_link_keeps_server_side_guard_fields(6个 blocked_fields 含 is_superseded/is_comparison 逐一验证拦截);③regression_scheduler_workbench_link_guardrails.py:120-162(_is_formal_adopted_context 下游读端逐键);④regression_dashboard_workbench_contract.py:101-103,145-147,236(dashboard 第4套 guard 字段). 新增/强化:断言4套全部 delegate 到收口点后,grep 确认无残留独立 key 列表;is_comparison/is_superseded_by_newer_version 仍齐全(fail-closed 不退化). 收口点 regression_schedule_result_view_context.py:176,222-233 输出键对齐绿.
- **误修风险与防呆**：未来 LLM 最可能:(a)只收编报告点名的3套而漏 dashboard_workbench 第4套→收口不完整,债残留 fail-open 风险仍在(本 agent 已盘上坐实第4套存在,Phase2 必须纳入);(b)以'统一/简化'名义删某套手维列表而不迁下游 _is_formal_adopted_context→直接打穿护栏;(c)透传裸 key 而非从单一来源产出→换地方复发 N1;(d)误把 dashboard 的 plan_identity_error 当 plan-guard 字段塞进 plan_role_filter_fields→污染通用收口点. 本地防呆=收口注释钉死'键集来源=plan_role_filter_fields 单一,漏拷 is_comparison/is_superseded=fail-open'+4契约测试绑死下游读端. 红线:load_bearing,只能收口+绑契约,严禁删/合并/透传.
- **落地批次/优先级**：Batch-2(B01 guard 字段收口,N1 真相源,单债独立成批). 紧随 Batch-1(R56等注释网)之后、先于 Batch-3(R42/R60)与 Batch-4(B02 R44). 本桶内第2顺位.

### R42 · P6(死码直删类). plan_id 从 request 读入→存进工作台上下文 · sev=medium · load_bearing=False

- **当前位置(已复核)**：⚠️行号部分已变(已逐一回盘). 读入: web/navigation_context.py:86 `plan_id=_request_arg('plan_id')`✅准(整文件未漂移); web/routes/reports_page_support.py:98 `plan_id=_request_text('plan_id')`⚠️(报告:97漂移+1)、:135 build_report_context 调用⚠️(报告:134漂移+1); web/routes/dashboard.py:172 `'plan_id':_request_arg('plan_id')`✅. 存储: web/viewmodels/scheduler_workbench_links.py:187 形参✅、:229 `'plan_id':_text(plan_id) or None`✅(仅存 dict). 透传: scheduler_reports_workbench.py:61形参/:76✅、dashboard_workbench.py:121✅. 回吐URL: scheduler_workbench_link_query.py:118/154✅. 字段表: scheduler_navigation_links.py:12/52✅、reports_export_support.py:14✅. 【死证盘上复核】git grep -nP '\bplan_id\b' core/ data/ = 0命中(全 web/ 命中仅这14处真链,无任何 args.get('plan_id')作解析键). 对比 back_to=真消费(resource_dispatch _current_back_to:79→redirect _page_url).
- **具体修法**：P6 纯直删(与 R60 合并为单次 plan_id 下线,rebase 在 R54 新签名后):删读入(navigation_context:86、reports_page_support:98/135、dashboard:172)+存储(scheduler_workbench_links:187形参/:229 dict 键)+透传(reports_workbench:61/76、dashboard_workbench:121)+回吐(link_query:118/154)+字段表(navigation_links:12,export_support:14 由 R60 处置). 同步退 navigation_contract:122/126 两条 plan_id 断言(:109 URL 含 plan_id 的 fixture 也调整,但保留同测试的 back_to 断言:123/127). 同步删3处 roadmap 制度化:items.yaml:289、reports-workbench-backlink-acceptance.md:52、reports-workbench-backlink-checklist.yaml:82(三处均把'plan_id 与 back_to'并列,只删 plan_id 保 back_to). 红线:纯删不加兜底/不加'保留字段未启用'垫片,删前已盘上确认 back_to 真消费不受影响.
- **爆炸半径**：所有工作台/报表/导出链接 URL 与隐藏表单少挂一个无效 plan_id. 触碰生产:navigation_context/reports_page_support/dashboard/scheduler_workbench_links/scheduler_reports_workbench/dashboard_workbench/scheduler_workbench_link_query/scheduler_navigation_links/reports_export_support. 触碰测试:regression_reports_workbench_navigation_contract.py(drift名单内). 触碰 in-progress feature roadmap 3处(需先对齐). 误删风险低(无功能依赖).
- **跨债依赖**：Phase1边: R42→R60(强 co-change,共享 link_query:118/154 emit点,实为同一 plan_id 死面包屑两面,只删一边留半截字段表残渣,MUST 同删)、R54→R42(同改 build_workbench_plan_context:187/:229,R54加 guard 字段、R42 删 plan_id,必须同批次否则互撞行号). LB06→R42 + R42 同文件 R56(navigation_context.py:删:86 plan_id 时绝不碰:80-82 is_execution_review 强制 adopted/清 scenario_id 护栏=R56/LB06本体). 须 rebase 在 Batch-2(R54)新签名后.
- **必补回归测试**：同步退 regression_reports_workbench_navigation_contract.py:122 `assert query['plan_id']==['PLAN-RPT']`、:126 `assert hidden_inputs['plan_id']==['PLAN-RPT']`(该测试 test_report_navigation_keeps_plan_id_and_return_context :105 内 plan_id 与 back_to 交织,删 plan_id 断言必须保留:123/:127 的 back_to 断言,测试可重命名去掉 plan_id 语义). 删后 grep 验证全仓 web/ 零 plan_id(plan_identity/plan_idempotency 除外). 验收:navigation_contract 余下断言绿、back_to 透传仍绿、SP05 拓扑绿、roadmap 3处同步删后无悬挂 exit_check.
- **误修风险与防呆**：未来 LLM 最可能:(a)只删读存吐链而漏 roadmap 3处制度化→下次看 roadmap 又把 plan_id 供养回来(本次删了下次又长);(b)单删 R42 留 R60 字段表(或反之)→半截残渣 plan_id 仍被读被存只是不进字段表,更隐蔽;(c)删 plan_id 时顺手碰 navigation_context:80-82 R56 护栏或误删同字段表里真承重键(back_to/scenario_id/date_*/resource_*);(d)加'保留字段未启用'垫片当温柔删除→违灵魂线(死面包屑就该真删). 本地防呆=R42+R60 同 PR 单次下线 + roadmap 3处同步 + 删前 grep 死证. 红线:纯删不兜底.
- **落地批次/优先级**：Batch-3(B01 plan_id 下线+死码清理,与 R60 合并执行). 紧随 Batch-2(R54 guard 收口)后,rebase 其新签名. 本桶内第3顺位(与 R60 同动作).

### R60 · P6(死码直删类). R60=R42 的'合同字段表承认'子 facet:pla · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准(已逐一回盘). 回吐URL: web/viewmodels/scheduler_workbench_link_query.py:118/154✅(与 R42 同点). 合同字段表: web/viewmodels/scheduler_navigation_links.py:12 plan_id 在 _REPORT_CONTEXT_FIELD_NAMES 元组(:11-28)内✅、:52 _has_navigation_context 判空(仅判存在不解析)✅;另 web/routes/reports_export_support.py:14 plan_id 在 _EXPORT_CONTEXT_KEYS(:13-)✅. 字段表唯一消费者: scheduler_navigation_links.py:189-198 preserved_report_context_fields 迭代字段表生成隐藏表单字段. 死证同 R42: core/+data/ zero \bplan_id\b.
- **具体修法**：P6 直删(与 R42 合并为单一动作,避免双改同测试):从 _REPORT_CONTEXT_FIELD_NAMES(scheduler_navigation_links.py:12)+_EXPORT_CONTEXT_KEYS(reports_export_support.py:14)两张字段表元组移除 plan_id 条目(保持元组其余成员完整);scheduler_navigation_links.py:52 _has_navigation_context 判空列表里的 plan_id 也移除. emit 点 link_query:118/154 由 R42 统一下线. 红线:纯删不加兜底;删元组行只动 plan_id 一项,绝不连带删同表真承重透传键(back_to/scenario_id/date_from/date_to/start_date/end_date/query_date/period_preset/batch_id/resource_*).
- **爆炸半径**：所有工作台/报表链接 URL 与隐藏表单少挂无效 plan_id(影响面=R42 子集,无新增独立面). 触碰生产:scheduler_navigation_links.py、reports_export_support.py、scheduler_workbench_link_query.py(emit 与 R42 共点). 触碰测试:与 R42 共享 navigation_contract:122/126.
- **跨债依赖**：Phase1边: R42→R60(MUST 合并,共享 link_query:118/154,只删一边=半截)、R54同文件 navigation_links.py 相邻(删 _REPORT_CONTEXT_FIELD_NAMES 里 plan_id 勿误动相邻其他字段;该文件也 import build_workbench_plan_context). scheduler_navigation_links/link_query 非承重护栏文件,但经 build_workbench_plan_context 调用链与 navigation_context.py(LB06/R56)耦合.
- **必补回归测试**：无额外独立测试改动——navigation_contract:122/126 由 R42 统一退(若各自为政会重复改同一测试,故合并). 验收:两张字段表(navigation_links:12 + export_support:14)都清后,隐藏表单/导出 URL 不再挂 plan_id;preserved_report_context_fields(:189-198)迭代输出无 plan_id;其余字段表成员(back_to/scenario_id/date_*/resource_*)透传契约仍绿.
- **误修风险与防呆**：未来 LLM 最可能:(a)单删 R60 字段表留 R42 读存→plan_id 仍被读被存只是不进表,变更隐蔽残渣;(b)漏清两张表之一(navigation_links:12 vs export_support:14)→隐藏表单或导出仍挂 plan_id;(c)删元组行时手滑波及相邻真承重键. 本地防呆=与 R42 同 PR、两表一次清、删后 grep 全仓 web/ 零 plan_id. 红线:纯删不兜底.
- **落地批次/优先级**：Batch-3(B13... 不,B01 plan_id 下线,与 R42 合并执行,单一动作). 本桶内第3顺位(与 R42 同批同动作).

**本单元待 owner 裁断**：
- R54 收口点选择需 owner 拍板:Phase0§4 把收口族写为 build_workbench_plan_context(guard 字段收口),但 plan_role_filter_fields(schedule_result_view_context.py:277)才是 guard 字段的物理单一来源。建议让 build_workbench_plan_context 内部调用/接收 plan_role_filter_fields 产出——但 build_workbench_plan_context 在 web/viewmodels 层、plan_role_filter_fields 在 core/services 层,需确认 web→core 调用方向不撞分层红线(应合法,web 是最上层),以及收口点入参形态(加显式 guard 入参 vs 接 plan_resolution dict 整体)由 owner 定。
- R54 第4套 dashboard_workbench.py:19-24 含 plan_identity_error,本 agent 复证它是 dashboard 本地错误标记(dashboard.py:121 设),非 plan-guard 字段、不在 plan_role_filter_fields 全集产出。收口时 dashboard 这套 delegate 到收口点后,plan_identity_error 必须保留其 dashboard 本地来源单独 merge——需 owner 确认这不算'收口不彻底',即 plan_identity_error 不应被强行并入通用 guard 收口点。
- R58(本桶,P2 承重,scheduler_navigation_publish.py:86 context.update 覆盖 can_write_feedback)与 R54 同改 navigation_publish 的 _publish_context。R54 让收口点承载 guard 字段后,:86 的 context.update(guard_fields) 是否仍必要、能否顺势让 R58 的护栏削弱一并消解(builder-gated≡raw 验证后剔除 can_write_feedback),需与 R58 owner 协调是否同批处置,避免 R54 改完 navigation_publish 后 R58 又来二次改同函数。
- R42 roadmap 3处制度化(items.yaml:289/acceptance.md:52/checklist.yaml:82)删除涉及 in-progress feature(reports-workbench-backlink + aps-frontend-workbench),需先与 roadmap owner 对齐:确认 plan_id 确为'真死面包屑'而非'设计 doc:225 有意延期的保留字段'(报告判定 back_to=有意延期、plan_id=真死,但二者在 roadmap 文案里并列,删 plan_id 保 back_to 需 owner 背书,以免误删在途契约)。


## 子单元 B01c-REST

**本单元策略**：本单元=B01"其余真债"中的6条REST/导航/复盘呈现层债,全部寄居在 SCC-NAV / 资源派工执行车道两个高频文件群,且都受全局硬约束"B01 必须最先落"(承重注释+R54 guard 收口铺好护栏与唯一真相源后,后续 B02/B05/B11 才能动这些文件)。内部排序:(1) R58 唯一承重(load_bearing=TRUE),走 Batch-1 纯增量注释(从13.N5/90节LB-A5复用文案),先落以给同文件 R44/R54 钉死"哪段不能动";(2) 其余5条(R57/R62/R66/R07/R08)是 Batch-3 结构动作,全部 rebase 在 Batch-2 的 R54 把 guard 字段收口进 build_workbench_plan_context 之后。Batch-3 内细序:R66(纯删死函数,风险最低)→ R62(execution_review 三档压扁残骸三处原子同删,须 LB02/LB05 的 Batch-1 注释先落)→ R57(双路径,默认降级为补注释,须 R56/LB06 的 Batch-1 注释先落)→ R07(P4 改 loud raise,补 AppError/ErrorCode 导入)→ R08(P6 删死开关分支)。两条跨桶硬边收尾:R07↔R09(B05)同 resource_dispatch_execution_service.py、R08↔R09(B05)同 scheduler_resource_dispatch_execution.py——B01 的 R07/R08 必须先落,B05 的 R09 在 Batch-6 才在稳定行号上迁 _positive_int(PHASE0 §10.1/§3)。最危险失效模式:R62 误把 task_card 体系有真测试的 *_machine_identity_label 当成 R62 目标删(实际 R62 只动 execution_review 报表的 *_resource_identity_label/*_export_label,零测试);R07 裸删触发 :147-148 AttributeError→500 谎报写失败;R57 真求证撞 flow-contract 透传断言;R58 加兜底踩灵魂线或删整行 update 丢10个 guard 字段。

**内部排序**：R58(承重注释·Batch-1·纯增量) → [R54 非本单元·Batch-2·guard 收口地基] → Batch-3 结构序: R66(纯删·最低险) → R62(三档压扁·须LB02/LB05注释先落) → R57(双路径·默认补注释·须R56/LB06注释先落) → R07(P4 loud raise) → R08(P6 删死开关)。R07/R08 必须先于 B05 的 R09(Batch-6)动同文件。

### R58 · P2(护栏被动削弱) · sev=low · load_bearing=True

- **当前位置(已复核)**：⚠️行号已变(文件 git status=M 已漂)。报告 :79-84/覆盖点 :83;盘上实测 _publish_context 在 web/routes/domains/scheduler/scheduler_navigation_publish.py:80-88,覆盖语句 `context.update(guard_fields)` 在【第86行】。builder gate 在 web/viewmodels/scheduler_workbench_links.py:145-157(未漂✅),:154 `can_write = bool(can_write_feedback and formal_adopted)`;build_workbench_plan_context 在 :244 emit gated can_write_feedback。已实证 :86 用未门控 guard_fields 原始值覆盖回 builder gated 值。
- **具体修法**：默认=只补'我是故意的'注释(红线2:承重只能补注释+绑契约,严禁删/合并/统一/透传/加兜底)。注释贴在 :86 context.update 上方,改编自13.N5+90节LB-A5:`# 故意:context.update 会用未门控的 guard_fields 覆盖 builder 已 gate 的 can_write_feedback。# 这是已知护栏削弱(_feedback_guard_context 只放行 formal_adopted 可写)。# 当前零消费(写链接走 resource_dispatch 的 can_emit_feedback_write_urls,非此 nav context);# 勿据此 nav context 的 can_write_feedback 渲染写按钮——会绕过门控。# 收敛方向=剔除 update 里的 can_write_feedback 让 builder gated 值存活,绝不加兜底/透传。`。可选结构收口(仅 Phase2 验证 builder-gated≡raw 且 keep_plan_guard_fields 绿后):从 update 的 guard_fields 里【单独剔除 can_write_feedback 一个键】(非删整行 update,保留其余10键),让 builder gated 值存活。已验证:keep_plan_guard_fields 用 superseded_plan(plan_role=adopted,is_preview=False)→formal_adopted=True→gated==raw==True,剔除后契约仍绿。
- **爆炸半径**：当前零消费(grep 证 nav 链接不传播 can_write_feedback;真写链接放行走 resource_dispatch 的 can_emit_feedback_write_urls,检 _is_formal_adopted_context)。潜伏:未来据 nav context 渲染写按钮即绕门控。prod:scheduler_navigation_publish.py:104/132/163(publish_*)+scheduler_analysis.py:52/scheduler_gantt.py:216/scheduler_week_plan.py:309。test:regression_reports_workbench_navigation_contract.py:406-472 test_scheduler_navigation_publish_keeps_plan_guard_fields(PIN expected_guard_fields 11键含 can_write_feedback==True for superseded,三处 publish_* 调用点断言在 :438-440/455-457/470-472)。
- **跨债依赖**：PHASE0 §3 三债同文件硬约束:scheduler_navigation_publish.py 上 R44(B02)/R54(B01)/R58(B01) 必须同批或显式串行。R58→R44 边(同文件同改):R58 注释先落。R58 自身 load_bearing=TRUE。注释走 Batch-1;结构剔除(若做)在 Batch-4 与 R44/R54 协调。
- **必补回归测试**：keep_plan_guard_fields 三处 publish_*(analysis/gantt/week-plan)的 current_workbench_navigation_context()[can_write_feedback]==True(superseded)与 context[key]==value 保持绿;若剔除 update can_write_feedback,先以一条 builder-gated≡raw 的断言验证(superseded→formal_adopted→gated True)。零生产逻辑改动时 git diff 仅注释。
- **误修风险与防呆**：LLM 最可能:(1)为'消除覆盖'加兜底/默认值→踩灵魂线红线1;(2)把整行 context.update(guard_fields) 删掉→丢其余10个 guard 字段→破 keep_plan_guard_fields 全部断言;(3)R44/R54 先落移 :86 行号,R58 需按 `context.update(guard_fields)` 符号重定位非裸行号。防呆:注释明确'收敛=只剔除 can_write_feedback 一个键,非删整行 update';契约测试 PIN 11键全集作为防呆红线;承重注释先落(Batch-1)给同文件后续债钉死边界。
- **落地批次/优先级**：Batch-1(承重注释,全局 ROOT 纯增量)。结构剔除(可选)在 Batch-4 B02 与 R44/R54 同批协调。

### R66 · P6(reports_workbench 私有副本死函数,P5 退化残骸,从未接 · sev=low · load_bearing=False

- **当前位置(已复核)**：⚠️行号已变(文件 git status=M 已漂)。报告 :141-154;盘上实测 `def _context_summary(context: Dict[str, Any], suffix: str = '')` 在 web/viewmodels/scheduler_reports_workbench.py:【第144行】,函数体 :144-157。已 grep 证本文件零调用(仅 :144 定义处命中)、非 __all__、私有 _ 前缀。真正生效的同名函数在 web/viewmodels/scheduler_workbench_links.py:254(签名不同 (context, target_page, view),:360 build_workbench_link 内调)。
- **具体修法**：纯删除(6条中风险最低,红线4:删前已确认零消费方)。删 scheduler_reports_workbench.py:144-157 整个死 _context_summary。【必须按 `def _context_summary(context: Dict[str, Any], suffix: str = '')` 完整签名定位,严禁照报告 :141-154 裸行号删(文件已漂)】。注意 suffix 形参是 reports_workbench 版独有特征,workbench_links 版形参是 target_page/view——以此区分,绝不误删 :254 那个 LIVE 同名函数。
- **爆炸半径**：纯死代码,删除无任何运行期/契约影响。prod:无(reports_page_support.py:21 导入本模块但 import 块不含 _context_summary)。test:无(tests/ 零引用)。
- **跨债依赖**：PHASE0 §2 同文件:R54(B01,P5 收口,scheduler_reports_workbench.py)。R54 还同在 navigation_publish+resource_dispatch(3漂移文件)。删 R66 与 R54 收口在不同符号,但同文件须协调避免行号冲突。
- **必补回归测试**：无需新增(纯死码删除)。删后 import scheduler_reports_workbench 模块仍 OK;现有全套测试绿即可。
- **误修风险与防呆**：LLM 最可能:(1)basename 同名陷阱——误删 scheduler_workbench_links.py:254 那个真正生效的 LIVE 同名函数;(2)文件已漂,按报告 :141-154 裸行号删→删错行;(3)R54 先落再移行号。防呆:按完整签名(含 suffix 形参)定位;R54/R66 串行或同提交(R54 Batch-2 先,R66 Batch-3 后,按符号定位)。
- **落地批次/优先级**：Batch-3 B01-structural。与同文件 R54(Batch-2)协调:R54 先落,R66 后删按符号定位。

### R62 · P3(三档身份系统掏空成单值,旧键+模板 != 分支+导出 or 回退三处 no · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准(核心)— core/services/report/execution_review.py 未漂(committed≡基线)。_resource_pair_payload 在 :336-348,:348 `return {display_label, identity_label, export_label}` 三键恒等(均=display)✅。同病压扁四组 _label 三连实测:exception_affected_machine_*:287-289、exception_affected_operator_*:290-292、planned_resource_*:303-305、actual_resource_*:310-312 ✅。模板 ⚠️(templates/reports/execution_review.html=M漂移)死 `!=` 副行实测仍在 :131/132/135/136 ✅。导出 ✅ core/services/report/exporters/xlsx.py:410/411/414/415 `or` 回退未漂。LB02/LB05 护栏(:112/123/153/141/166-167 强制 ROLE_ADOPTED)与 R62 死分支在不同方法,已核实隔离。
- **具体修法**：P3 半截收敛(删 no-op,非删行为):三处消费方原子同改——(a)_resource_pair_payload:348 收成单 display 键 + 四组 _label 三连(:287-292/303-305/310-312)压成单键;(b)同步删模板 :131/132/135/136 的 `{% if *_identity_label != *_label %}` 死副行(按 `!=` 模式定位,文件已漂);(c)删 xlsx :410/411/414/415 的 `*_export_label or *_label` 死回退。【绝不碰 :100-174 的 ROLE_ADOPTED 强制段(LB02/LB05),R62 与护栏在不同方法】。关键区分:R62 目标=execution_review 报表的 *_resource_identity_label/*_resource_export_label/*_affected_*_export_label(零测试);绝非 task_card/event_payload 体系的 *_machine_identity_label/*_operator_identity_label(有真测试,见下),后者是 ResourceIdentity 真差异化,严禁动。
- **爆炸半径**：模板死副行(text-meta div)恒不渲染;xlsx 导出 or 恒不回退;每资源3重复键零信息差。prod:execution_review.py:354(_planned_resource_identity)/:375(_state_resource_identity)、模板 :131-136、xlsx :410-415。test:无(已 grep 证 tests/ 零处断言 execution_review 报表的 identity/export 变体差异;regression_plan_vs_actual_review 不 PIN 这些键)。
- **跨债依赖**：PHASE0 §2 同文件 LB02/LB05(承重 high)。LB02→R62 边(承重先于动同文件):LB02 注释先落(Batch-1),R62 清三档死分支绝不碰 :112/123/153/141/166-167 写死 ROLE_ADOPTED 硬钉,更不给 :141 签名加 plan_role/scenario_id 形参。
- **必补回归测试**：因零现有契约,收敛后须补一条'execution_review 每资源单 display 键'契约(断言 _resource_pair_payload 返回单键、模板渲染 1 个 display 值无 text-meta 副 div、xlsx 列值取 display),防再生半截残骸。regression_plan_vs_actual_review 保持绿。
- **误修风险与防呆**：LLM 最可能:(1)误把 task_card 体系有真测试的 *_machine_identity_label(regression_operation_execution_feedback_routes.py:183-219)、*_affected_*_identity_label(regression_operation_execution_exception_feedback.py:106-109)当成 R62 目标删→破真测试(最危险陷阱);(2)三处不同步→新半截残骸;(3)模板已漂按裸行号删而非 `!=` 模式定位;(4)碰 LB02/LB05 forced-adopted 段;(5)把'压扁'误解为'恢复三档'反向补真身份(无规划)。防呆:计划明确 R62 报表键 vs ResourceIdentity 键的区分清单;模板按 `!=` 模式定位;LB02/LB05 注释先落(Batch-1)。
- **落地批次/优先级**：Batch-3 B01-structural。LB02/LB05 承重注释(Batch-1)先落。

### R57 · P3(双构建路径:override 经真实 plan_resolution,fa · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准 — web/navigation_context.py:76-98(current_workbench_navigation_context;fallback 分支 :80-98)。文件提交态≡基线(git status 无 M),行号未漂。实测 :81 `plan_role = ROLE_ADOPTED if is_execution_review else _request_arg('plan_role')`;:82 `scenario_id = '' if is_execution_review else _request_arg('scenario_id')`;:87 `plan_role or ROLE_ADOPTED`;:84-98 build_workbench_plan_context。证实 fallback 直接信任未经 PlanIdentity 解析的 raw plan_role/scenario_id。override 路径走 :77-79(g 上 _navigation_context_override,经真实 plan_resolution)。
- **具体修法**：默认=补注释钉双路径有意分歧(降级修法)。理由:flow-contract test_workbench_non_adopted_review_entry_is_disabled_and_plain_chinese(regression_aps_workbench_flow_contract.py:363-396)PIN 了 fallback 对非 adopted plan_role 的【原样透传】(:385 home_query['plan_role']==['baseline_best']),且真正的安全网是 fallback 从不设 guard 字段→_is_formal_adopted_context→False→execution-review 入口恒 disabled(:373-377 断言无 execution-review link,:379 断言'计划和现场实际只复盘正式采用方案')。故 fallback 透传 raw plan_role 是【有意保留跨页上下文】,护栏由 disabled 入口独立兜,非靠 fallback 求证。注释钉:'fallback 故意原样透传 request.args 以保留跨页筛选;execution-review 入口护栏由缺失 guard 字段→disabled 独立保证,不靠此处求证 plan_role;勿删 :80-82 的 is_execution_review 强制 adopted/清 scenario(R56/LB06 护栏)'。若 owner 坚持真收敛(让 fallback 经 schedule_plan_query_service/build_plan_identity 求证,收口到§4方案身份族,绝不新建),须先改 flow-contract 透传断言,风险高。
- **爆炸半径**：非复盘 scheduler 页跨页回跳链接 query 参数;下游按透传 plan_role 重新取数,可能旧版本误标 adopted。prod:navigation_context.py:106/114/123(build_workbench/scheduler/report_navigation_links)→render_bridge.py:73-91(注册 Jinja globals)→templates/components/{workbench_nav_macros:6,ui_macros:342,reports_nav_macros:15}。test:regression_reports_workbench_navigation_contract.py:439/456/458/459/471(直接断言 current_workbench_navigation_context() 字典值)+regression_aps_workbench_flow_contract.py:363-396(新增未跟踪,PIN fallback 原样透传 plan_role)。
- **跨债依赖**：navigation_context.py 同文件四债:R56(:42-47,80-82,P2 零注释护栏)与 R57 fallback 共享 :80-82;LB06(reports_page_support.py:367/369+navigation_context.py:80-82 承重 forced-adopted)同享 :80-82;R42(:86 plan_id 死面包屑)同块 build_workbench_plan_context 调用。R56→R57、LB06→R57 边(承重先于动同文件):R56/LB06 注释必须先落或同提交,否则 R57 收敛误删 :80-82 护栏。
- **必补回归测试**：补注释方向:复用 flow-contract:373-379 断言 fallback 页 execution-review 入口恒 disabled。若真收敛:须新增双路径 parity(fallback 与 override 对同一 version/plan_role/scenario_id 产出一致 plan identity)+保持 navigation_context.py:80-82 forced-adopted 未碰+(改写 flow-contract 透传断言后)新断言绿。
- **误修风险与防呆**：LLM 最可能:(1)为'统一双路径'删 :80-82 强制 adopted/清 scenario→破 execution-review 只复盘正式方案护栏(LB06/R56);(2)让 fallback 真求证改写 plan_role→撞 flow-contract:385 home_query['plan_role']==['baseline_best'] 透传断言;(3)同文件 R42/R56/LB06 先动移行号(本文件当前未漂,B01 内任一债先改即漂)。防呆:先落 R56/LB06 的 Batch-1 注释钉 :80-82;flow-contract 透传断言作为防 over-fix 红线;默认降级补注释而非求证。
- **落地批次/优先级**：Batch-3 B01-structural。建议与 R56/LB06(Batch-1 注释)同批协调,R56/LB06 注释先落。

### R07 · P4(灵魂线:坏数据静默兜底为'正式采用方案/可写'卡) · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/resource_dispatch_execution_service.py:131-141 未漂(committed≡基线)。task_card_for_feedback_context:131 `schedule = self.schedule_repo.get(int(context.schedule_id))`;schedule is None → :132-141 返回 plan_identity={}、plan_identity_label='正式采用方案'、can_write_feedback=True 的正常卡。裸删后控制落 :147-148 schedule.start_time/.end_time→AttributeError ✅实证。NOT_FOUND raise 范式已存在于同链路 operation_execution_feedback_service.py:365 `raise AppError(ErrorCode.NOT_FOUND, '排程记录不存在，请刷新后重试。', details={'reason': 'not_found'})`✅。errors.py:63 AppError、:8/:15 ErrorCode.NOT_FOUND='1002' 存在✅。
- **具体修法**：P4 改 loud raise(红线1正确修法,严禁裸删/严禁加兜底)。把 :132-141 整支改为 `raise AppError(ErrorCode.NOT_FOUND, '排程记录不存在，请刷新后重试。', details={'reason': 'not_found'})`(对齐 feedback_service.py:365 既有同文案模式)。必补导入:本文件 :5 仅 `from core.infrastructure.errors import ValidationError`,须补 `AppError, ErrorCode`。已验证分支生产不可达:两条写路径在建卡前都先过 schedule 存在性写门禁——actual-record 路径 routes:265 record_actual_situation→resource_dispatch_actual_record_service.py:215/_task_ref_from_feedback_context→:218 raise NOT_FOUND if None(返回 result 前);legacy-action 路径 routes:278 _record_legacy_action→operation_execution_feedback_service.py:105 _load_current_official_schedule→:365 raise NOT_FOUND if None(写事务内)。卡在 routes:184/201 写步骤成功后才建,故 schedule 已在同连接证存在。改 raise 不影响 happy path、不削任何写门禁。
- **爆炸半径**：仅影响单条写入成功后回显卡正确性,不影响写门禁(门禁独立强校验)。prod callers:scheduler_resource_dispatch_execution_routes.py:184(_task_card_for_result,被 :279 legacy 调)/:201(_actual_record_result_payload,被 :266 调)/:264-265(record_actual 写步骤)/:278-279(legacy 写步骤)。test:regression_scheduler_candidate_resource_dispatch_contract.py:364/382 仅用 get_execution_context(非 task_card_for_feedback_context),未 PIN :131-141→转 raise 不破现有测试。
- **跨债依赖**：PHASE0 §3 硬边:resource_dispatch_execution_service.py 上 R07(B01)↔R09(B05,:23 _positive_int 收口)。R07→R09 边(同文件同改+B01 先于 B05):B01 必须先落,R09 在 Batch-6 改 _positive_int 时该文件已被 R07 动过,须串行(PHASE0 §10.1/§10.5,R09 新建 parse_optional_positive_int 先于其迁移)。R08(同执行车道 viewmodel)。
- **必补回归测试**：新增一条:mock schedule_repo.get 返 None 调 task_card_for_feedback_context,断言 raise AppError(ErrorCode.NOT_FOUND)(PIN 该分支转 raise);happy path(schedule 存在)仍产正常卡。
- **误修风险与防呆**：LLM 最可能:(1)裸删 :132-141→控制落 :147-148 schedule.start_time AttributeError→500 谎报写失败(verdict precondition 明确警示);(2)改成兜底/降级默认卡→踩灵魂线红线1;(3)忘补 AppError/ErrorCode 导入→NameError;(4)R09(B05)先落移行号。防呆:verdict 钉死'不要裸删 131-141';新增回归 PIN raise;导入检查;B01 先于 B05 串行。
- **落地批次/优先级**：Batch-3 B01-structural(R07 在此批)。R09(Batch-6 B05)须在 B01 动完本文件后串行。

### R08 · P6(休眠开关+生产不可达的'保护未开启'死面包屑文案) · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — web/viewmodels/scheduler_resource_dispatch_execution.py 未漂(committed≡基线)。_FEEDBACK_DISABLED_REASON:24='现场记录保护还没开启，暂不能填写现场记录。'✅;_fill_actual_disabled_reason:226-227(`if not feedback_write_enabled: return _FEEDBACK_DISABLED_REASON`)✅;build_execution_payload:361-362(`elif not feedback_write_enabled: disabled_reason = _FEEDBACK_DISABLED_REASON`)✅;:333 读 context.feedback_write_enabled✅。(True,False)组合不可达:resource_dispatch_execution_service.py:117 can_write_feedback 与 :120 feedback_write_enabled 同设 `bool(plan_role_fields.get('can_write_feedback'))` 同源恒等✅,:204 _empty_context 两者皆 False。
- **具体修法**：默认方案A(P6 死码删除):删 viewmodel :226-227 与 :361-362 的 `if/elif not feedback_write_enabled` 分支 + :24 的 _FEEDBACK_DISABLED_REASON 常量,【保留 feedback_write_enabled 参数】(参数仍被 :233 build_available_actions 的 `can_write and feedback_write_enabled` 消费,非纯死参)。删后 _fill_actual_disabled_reason 只剩 `if not can_write` + 兜底状态文案;build_execution_payload 的 disabled_reason 只剩 `if not can_write`。已 grep 证全仓零字符串断言 '现场记录保护'/FEEDBACK_DISABLED/disabled_reason,(True,False)不可达,删不破任何测试、不新增兜底(删的恰是死分支)。可选方案B(彻底并参数 feedback_write_enabled→can_write_feedback):须同步改4处签名 service:129 默认参+routes:184/192/201 三 kwarg+test build_task_card 调用,去 kwarg 后再删分支。【两法前须 roadmap owner 确认】commit 65870e47 'resource dispatch execution lane' 近期不上线真'现场记录保护总开关'配置;若有规划则保留转注释钉'为未来总开关预留'。
- **爆炸半径**：删死分支+死文案不改任何生产行为((True,False)不可达)。真写门禁=operation_execution_feedback_service.py:361(not can_write_feedback 即 raise)+resource_dispatch_actual_record_service.py:137-149(_execution_context_for_write/_ensure_context_can_write 仅基于 can_write_feedback/plan_role/source_table/scenario,全程不读 feedback_write_enabled)。'还没开启'文案真实用户永不可见。prod:service:120/139/165/204、routes:184/192/201 恒传 True。test:regression_resource_dispatch_workbench_lane_contract.py:174 传 (can_write_feedback=False, feedback_write_enabled=False),因 can_write=False 在 :224/:233 双重短路,死分支不执行;断言只覆盖 part_label/actual_time_label/actual_delta_summary。
- **跨债依赖**：scheduler_resource_dispatch_execution.py 同文件 R09(B05,:32 _positive_int 副本)。R08→R09 边(同文件同改):删死分支与 _positive_int 收口须串行避免撞行号(B01 先于 B05)。R07(同执行车道 service)。
- **必补回归测试**：删后 lane_contract:164-179 仍绿(死分支本就因 can_write=False 短路不执行)。P6 纯删无需新增断言(现有 lane_contract 覆盖足够);可选补一条注释/测试钉 (True,False) 不可达。
- **误修风险与防呆**：LLM 最可能:(1)选方案B 删整个 feedback_write_enabled 参数但漏改4处签名(service:129/routes:184/192/201/test)→调用方 TypeError;(2)删 :24 常量但漏删 :226-227 或 :361-362 引用→NameError;(3)把死开关'激活'成真 toggle(无规划);(4)R09(B05)先落移行号。防呆:默认方案A 只删分支+常量、保参数;方案B 须签名清单逐一改;roadmap owner 确认无总开关后再删。
- **落地批次/优先级**：Batch-3 B01-structural(R08 在此批)。R09(Batch-6 B05)须在 B01 动完本文件后串行。先经 roadmap owner 确认无未来总开关规划。

**本单元待 owner 裁断**：
- R57 修法方向裁断:默认降级为'补注释钉双路径有意分歧'(安全由 fallback 缺 guard 字段→execution-review 入口恒 disabled 独立兜),还是走真收敛(让 fallback 经 schedule_plan_query_service 求证 plan_role/scenario_id)?后者会撞新增 flow-contract regression_aps_workbench_flow_contract.py:385 的 home_query['plan_role']==['baseline_best'] 原样透传断言,须 owner 先批准改写该透传断言。Phase1 默认补注释。
- R08 须 roadmap owner(commit 65870e47 'resource dispatch execution lane')确认:近期是否计划在此接缝上线真正的'现场记录保护总开关'配置?确认无规划→按默认方案A 删死分支+死文案(保参数);若有规划→保留并转注释钉'为未来总开关预留'。
- R58 裁断:默认只补承重注释(Batch-1),还是在 Batch-4 顺手做结构收口(从 context.update 的 guard_fields 单独剔除 can_write_feedback 一个键,让 builder gated 值存活)?已验证 builder-gated≡raw(superseded 用例 formal_adopted=True→gated==raw==True),剔除后 keep_plan_guard_fields 契约仍绿;但属承重债,需 owner 显式批准结构动作而非纯注释。
- R62 裁断:默认'收掉已掏空的三档残骸'(三键压成单 display + 删模板 != 副行 + 删 xlsx or 回退),还是反向'恢复真实三档身份渲染'(display/identity/export 各承不同信息)?后者需求证有此演进规划。Phase1 默认收残骸。
