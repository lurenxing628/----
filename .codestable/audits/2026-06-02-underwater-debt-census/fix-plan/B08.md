# B08 · 关键链甘特

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：本桶=关键链甘特强连通簇(SCC-GANTT),4 条债实为 3 个动作。核心实证:(1) R11≡R63 是同一条债的两条记录——`gantt_service_support.py:32-51` 与 `gantt_critical_chain_provider.py:104-128` 两份 `_normalize_critical_chain_result` 逐字段语义相同(已盘上逐行比对确认:is_available 默认 True、available 时清 reason/reason_code、edge_type_stats 四键默认、edge_count int 兜底、`reason_code or 'unknown'` 完全一致),一次抽单份 helper 到两文件已共同 import 的 `gantt_critical_chain.py` 公共落点即同时消除,绝不当两条分别投工。`git cat-file` 已证 b08162cd 含 gantt_service_support.py,R11 报告"未提交(A)"过时,以 R63"已提交坐实 P5"为准。(2) R12 是 P4 灵魂线缺口(`gantt_critical_chain.py:84` 静默丢坏时间行仍报 available:True),但盘上实证丢行分支近死:scenario 写入经 `gantt_adjustment_projection.py:143 (end<=start raise)` + `:164 (_apply_change new_end<=new_start raise)` 已校验 st<et,Schedule/候选源 schema NOT NULL+引擎写入——故 R12 应补可观测(dropped_count+critical_chain_partial,照搬 gantt_tasks 的 DegradationCollector 范式)但低投入,且严禁裸删 :84 过滤(删后 None 流入 :114 排序/:262 max→TypeError→整链降级)。(3) R55 是呈现失真:filtered 关键链把周窗口+资源子集喂进"要求整版输入"的算法(算法不变量 :347 明示不按周截断),以 available:True+同名 makespan 对外无 scope 标记——补 scope=filtered/full 标记,不裸删过滤。内部强排序:R11/R63 统一归一【必须最先】,因 R12 加 dropped_count、R55 加 scope 都要落到那【单份】helper,不先统一则改两副本=复活 R11/R63。R12/R55 新键同穿白名单(统一后=单 _normalize helper + provider _copy_critical_chain_result:95-101 cache 复制 + gantt_contract._public_critical_chain:19-40)。跨桶:gantt_service.py 与 R10(B14,:60-62 死方法直删)同文件,须同批/串行,B08 动 gantt_service.py(R55 在 :335/:375)时不碰 R10 区域。非承重文件,但归一调用本身对 support 路径承重(raw compute 缺 available/edge_type_stats 默认),抽取时必保留归一调用,绝不可把 critical_chain_for_plan_detail_filter 简化为 return raw。

**内部排序**：先 R11≡R63(抽单份 _normalize_critical_chain_result 到 gantt_critical_chain.py 公共落点,support:32-51 与 provider:104-128 两路改调它)→ 再 R12(在统一后的单 helper 上加 dropped_count/critical_chain_partial)与 R55(在同一单 helper 上加 scope)并行落键,二者共用三道白名单宜同批。R10(B14)同 gantt_service.py 同批删 :60-62。理由:R11/R63 是 R12/R55 的硬前置(fix_invalidation),不先统一则 dropped_count/scope 各改两副本=复活重复债。

### R63 · P5(第二份私有实现:关键链结果归一被逐字复制成第二份)。R11≡R63 同一债 · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/gantt_service_support.py:32-51 `_normalize_critical_chain_result` 与 core/services/scheduler/gantt_critical_chain_provider.py:104-128 `_normalize_critical_chain_result`(staticmethod)两份均在盘上,逐行比对确认逐字段语义相同。git committed-state ≡ 基线 b08162cd(`git diff b08162cd` 对这两文件空输出,且非 16 文件漂移名单成员),行号准。`git cat-file -e b08162cd:...gantt_service_support.py` 已证该文件 b08162cd 已含 → R63'已提交坐实 P5'为真,优于 R11'未提交(A)'过时记述。两文件均已 `from .gantt_critical_chain import ...`(support:7、provider:10)→ 天然公共落点存在。
- **具体修法**：收口到【已存在】公共落点:抽单份 `_normalize_critical_chain_result` 到两文件已共同 import 的 core/services/scheduler/gantt_critical_chain.py(§4 收口点族『关键链』=gantt_critical_chain 公共落点,非新建)。support 版(:32-51 module 级函数)与 provider 版(:104-128 staticmethod)两路都改调它(或 support import provider 范式,但落到 gantt_critical_chain.py 更中性)。逐字保留归一行为:is_available 默认 True、available 时清 reason/reason_code、edge_type_stats 四键默认{process,machine,operator,unknown}=0、edge_count int 兜底、`reason_code or ('unknown' if not is_available else '')`。【绝对红线】绝不可把 critical_chain_for_plan_detail_filter(support:54-58)简化为直接 return compute_critical_chain_from_rows 的 raw——那会丢掉 available/edge_type_stats/reason_code 归一,使坏数据以缺字段形态流入 _public_critical_chain 并让 collect_gantt_degradation_events(support:61-80,靠 available is False 判定)漏报降级(违灵魂线)。
- **爆炸半径**：生产:抽取后 support:58 与 provider:171 两调用点改指单份;出口汇入 gantt_service.py:375(filtered/support)/:377(unfiltered/provider)→:389 build_gantt_contract→gantt_contract.py:89 _public_critical_chain 白名单。前端 makespan/reason_code 透到 static/js/gantt_contract.js、gantt_legend.js。测试:regression_gantt_contract_snapshot.py:143-184(字段集快照,subset 检查,不破)、regression_gantt_critical_chain_provider.py:88-498(monkeypatch compute_critical_chain/compute_critical_chain_from_rows,不碰 _normalize → 抽取安全)、regression_gantt_critical_chain_unavailable.py:143-173、regression_gantt_task_detail_panel_contract.py:128。已 grep 证:tests/ 无任何直接引用 _normalize_critical_chain_result → 抽取不破测试。
- **跨债依赖**：R11≡R63 同一动作(本条即代表该统一动作)。【必须早于】R12(加 dropped_count)与 R55(加 scope):若不先统一,R12/R55 各改两副本=复活本债(Phase1 边 R11→R12、R11→R55 收口前置)。同 gantt_service.py 跨桶 R10(B14,:60-62 死方法)须同批/串行(PHASE0 §3)。
- **必补回归测试**：补一条契约测试钉死『单一归一来源』:断言 support 路径与 provider 路径对同一 raw 输入产出逐字段相同的归一结果(parity),防未来 LLM 再分叉成两份。复用现有 regression_gantt_critical_chain_provider.py 的 monkeypatch 框架构造 raw,断言 support.critical_chain_for_plan_detail_filter 与 provider.get_critical_chain 归一后 ids/edges/makespan_end/edge_type_stats/edge_count/available/reason/reason_code 全等。现有 contract_snapshot/provider/unavailable 三契约保持绿。
- **误修风险与防呆**：未来 LLM 最可能:(1) 抽取时顺手把 critical_chain_for_plan_detail_filter 简化为 return raw(丢归一,坏数据静默缺字段)→ 用 parity 契约+灵魂线注释『此归一调用对 support 路径承重,raw 缺 available/edge_type_stats 默认会让降级漏报,严禁简化为 return raw』钉在抽取点;(2) 只改一份副本忘另一份 → 单一来源契约测试阻止;(3) 把 provider 的 bool(is_available) 与 support 的细微写法当『差异』而保留两份 → 实证 is_available 必为 bool,bool() 是 no-op,无差异。
- **落地批次/优先级**：Batch-9(B08 关键链甘特,SCC-GANTT)。内部【最先】:统一归一是 R12/R55 的硬前置。本桶自成簇不阻塞他桶。

### R11 · P5(同 R63)。两路都活:gantt_service.py:375 crit · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 + 🔧字段过时澄清 — 与 R63 同一债。gantt_service_support.py:32-51 vs gantt_critical_chain_provider.py:104-128 两份均在盘上确认。🔧 R11 reference_chain 称 gantt_service_support.py'新增未提交(A)'已被 `git cat-file -e b08162cd:gantt_service_support.py` 推翻(b08162cd 已含),以 R63'已提交坐实'为准。R11=R63,按 Phase1 纠偏#1#2 当【同一条债的两条记录】处理。
- **具体修法**：【不单独投工】=R63 同一动作。R11 与 R63 由同一次『抽单份 _normalize_critical_chain_result 到 gantt_critical_chain.py 公共落点』同时消除。修法、收口点、红线全部见 R63 条目(收口到 gantt_critical_chain.py 公共落点,绝不简化为 return raw)。Phase3 排期把 R11/R63 合并为单个 work item,不可当两条分别派工(否则重复投入且可能两人撞改同两文件)。
- **爆炸半径**：同 R63(共享出口 gantt_service.py:375/377→:389→gantt_contract.py:89,共享测试 contract_snapshot/provider/unavailable)。无额外独立爆炸面。
- **跨债依赖**：R11≡R63(Phase1 纠偏#1)。与 R63 合并为单动作,该动作必须早于 R12+R55。
- **必补回归测试**：同 R63(单一归一来源 parity 契约,一条覆盖两记录)。
- **误修风险与防呆**：最大误修风险=Phase3 把 R11/R63 当两条债分别排期 → 两次改同两文件互撞、或一人改完另一人扑空。防呆:在 Phase3 master plan 显式标注 R11≡R63 合并 work item,引 Phase1 纠偏#1#2。其余误修风险同 R63。
- **落地批次/优先级**：Batch-9,与 R63 同一 work item 最先落。

### R12 · P4 灵魂线缺口:_build_nodes 静默丢坏时间行(:84 contin · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — `if not st or not et or not (st < et): continue` 确在 core/services/scheduler/gantt_critical_chain.py:84,位于 _build_nodes(:79)。全文件已 grep 证无 partial/dropped/skipped/degraded 字样(信号缺口属实)。兄弟范式确认:gantt_tasks.py:196 `_record_bad_time_row(collector, scope='gantt.tasks', row=dict(row))`,helper 在 _sched_display_utils.py:84 `record_bad_time_row(collector,*,scope,row,message='存在开始或结束时间写法不对的排程行，已跳过。')` 发 code='bad_time_row_skipped'。gantt_tasks.py:355 `if not tasks and collector.to_counters().get('bad_time_row_skipped',0)>0` 给 empty_reason。committed-state≡基线,行号准。
- **具体修法**：补可观测(P4 正确修法=补可观测降级标记,严禁加兜底):照搬 gantt_tasks 的 DegradationCollector 范式,在 _build_nodes(:79-100)统计 :84 丢弃行数(改为返回 nodes+dropped_count,或线程化一个 collector),经 _compute_critical_chain_from_loaded_rows(:312-334)把 `dropped_count`(int)+ `critical_chain_partial`(bool=dropped_count>0)注入结果 dict。【保留 :84 过滤本身】——删了会让 start/end=None 流入 :114 _build_process_prev 排序与 :262 _sink_id 的 max→TypeError→被 :340/:358 try/except 接住→整链降为 available:False(过度激进、功能回归)。新键必须加进【R11/R63 统一后的单份 _normalize helper】(否则归一层 build fixed dict 时被静默剥离=换地方自欺);provider cache 路径 _copy_critical_chain_result(:95-101)经 dict(result) 复制全键已保留新键(已核);gantt_contract._public_critical_chain 成功路(:40 return raw)保留新键、不可用路(:22-39 build fixed dict)不带 partial(合理,不可用时无『部分』语义)。【投入量级】低:已实证 scenario 写入经 gantt_adjustment_projection.py:143(end<=start raise)+:164(_apply_change new_end<=new_start raise)校验 st<et,Schedule/候选源 schema NOT NULL+引擎写入,:84 丢行分支近死——补留痕是灵魂线合规的防御性可观测,不为不可达路径过度投工。
- **爆炸半径**：生产:_build_nodes/_compute_critical_chain_from_loaded_rows 签名微调(内部函数,无外部 caller 直接依赖返回形状除经 compute_critical_chain_from_rows:337/compute_critical_chain:344);新键经统一 _normalize→gantt_contract:89→JS。无生产行为变化(仅多两个观测键)。测试:regression_gantt_contract_snapshot.py:143-184(line 122 subset 检查 + 仅查已知键存在,加 dropped_count/critical_chain_partial 不红,已核机制)、regression_gantt_critical_outline_sync.py:1842(degradation_counters bad_time_row_skipped 计数范式)、regression_gantt_critical_chain_unavailable.py:215-248(边级 raise 政策契约,坐实节点级 :84 静默是已立政策缺口)。
- **跨债依赖**：【依赖 R11/R63 先统一】:dropped_count 必须落到单份 _normalize,否则改两副本复活 R11/R63(Phase1 边 R11→R12 收口前置)。与 R55 共用三道白名单,宜同批改(统一后=单 _normalize + provider _copy:95-101 + contract:19-40)。同 gantt_service.py 无直接交集(R12 在 gantt_critical_chain.py),但同桶 R55 在 gantt_service.py 与 R10(B14)协调。
- **必补回归测试**：补一条:构造含坏时间行(st>=et 或缺字段)的 rows 喂 compute_critical_chain_from_rows,断言返回 dropped_count==坏行数 且 critical_chain_partial==True 且 available 仍为 True(链路仍出有用结果)、且坏行未进 ids;再断言新键穿过 build_gantt_contract 后在 data['critical_chain'] 中存活(穿白名单验证)。对照 regression_gantt_critical_chain_unavailable.py:215-248 边级 raise 政策保持。
- **误修风险与防呆**：未来 LLM 最可能:(1) 裸删 :84 过滤想『让坏数据 raise』→ 实测会 TypeError→available:False 整链降级(功能回归),用 downstream 注释『:84 是丢一行坏数据仍出有用链的刻意韧性,只可丢行+留痕不可裸删』钉死;(2) 把丢行改成 raise/兜底默认时间 → 违灵魂线,注释明确『补 dropped_count 可观测,不是 raise 也不是填默认』;(3) 只加键到一份副本(若先于 R11/R63)→ 强制 R11/R63 前置;(4) 忘记把新键加进 _normalize fixed dict → 被静默剥离,用穿白名单测试抓。
- **落地批次/优先级**：Batch-9,内部次序在 R11/R63 之后,与 R55 同批。鉴于丢行分支近死,优先级在本桶内偏低,但灵魂线合规必做(低投入)。

### R55 · 呈现失真:filtered 关键链把周窗口+资源/批次子集 rows 喂进『要求 · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/gantt_service.py:335(detail_filters=plan_detail_filter_kwargs)、:336-344(list_plan_detail_rows_between_for_resolution 喂周窗口 wr.start_str/wr.end_exclusive_str + 子集 detail_filters)、:375(critical_chain_for_plan_detail_filter(rows,detail_filters))均在;gantt_service_support.py:57(compute_critical_chain_from_rows 子集 rows)在。算法不变量 gantt_critical_chain.py:347『输入：某一 version 的全量排程（不按周截断）』确在。路由 web/routes/domains/scheduler/scheduler_gantt.py 确认:gantt_resource 存在时 data_kwargs['resource_type']=view、['resource_id']=gantt_resource(触发 filtered 路径)。全仓 grep 证无 scope=filtered/scope_full 标记(缺口属实)。committed-state≡基线,行号准。
- **具体修法**：补 scope 标记(呈现失真正确修法=补 scope=filtered/full,严禁裸删过滤):在结果 dict 加 `scope` 键。语义来源=调用点知情:support 路径 critical_chain_for_plan_detail_filter(:54-58)收到 filters 非空 → scope='filtered';provider 路径 get_critical_chain(:171)无 filter → scope='full'。scope 须落进【R11/R63 统一后的单份 _normalize helper】并默认 'full'、由 filtered 调用点显式置 'filtered'(loud/显式,非静默推断)。穿三道白名单:单 _normalize(主闸,build fixed dict 必须认 scope 键)+ provider _copy_critical_chain_result(:95-101,dict 复制保留,已核)+ gantt_contract._public_critical_chain(:40 成功路 return raw 保留)。【绝不裸删 :336 周窗口过滤或 :335 detail_filters】——那会改变 filtered 视图的既有数据范围语义(违灵魂线『呈现失真不补反砍』)。前端 gantt_legend.js:147-185『完工』显示宜据 scope 条件标注『(筛选视图)』,属下游 JS 改动。
- **爆炸半径**：生产:gantt_service.py:375(filtered)→support:57;provider 路径加 scope='full'。出口 gantt_contract.py:89→makespan_end 经 static/js/gantt_legend.js:147-185 + gantt_contract.js:109-129。只读显示正确性:带任一过滤时关键链/makespan 是子集冒充整版,加 scope 标记后消歧,不损坏数据(blast=medium)。测试:regression_gantt_contract_snapshot.py:143-184(subset 检查,加 scope 键不破)、regression_gantt_task_detail_panel_contract.py:128、regression_gantt_critical_chain_provider.py(无 filter 对照路径应 scope='full')。
- **跨债依赖**：【依赖 R11/R63 先统一】:scope 落单份 _normalize,与 R12 dropped_count 共用三道白名单宜同批(Phase1 边 R11→R55 收口前置)。【同 gantt_service.py 跨桶 R10(B14,:60-62)】:动 gantt_service.py 须与 R10 同批/串行(PHASE0 §3),B08 改 :335/:375 区域、R10 删 :60-62 区域,互不重叠但同文件协调避免行号二次漂移,不碰对方逻辑。
- **必补回归测试**：补一条:filtered 请求(带 gantt_resource/gantt_batch)断言 data['critical_chain']['scope']=='filtered';无 filter 请求断言 scope=='full';断言 scope 穿过 build_gantt_contract 存活(穿白名单)。若采纳前端标注,补 JS 契约或在 contract_snapshot 增 scope 键存在断言。
- **误修风险与防呆**：未来 LLM 最可能:(1) 为『修正失真』直接把 filtered 路径关键链改成强制整版/或 unavailable → 改变既有行为且可能非用户所需,注释钉死『呈现失真只补 scope 标记,不改过滤范围、不裸删过滤』;(2) scope 用静默默认推断而非调用点显式置值 → 违灵魂线显式原则,注释要求 filtered 点显式 scope='filtered';(3) 忘加进 _normalize fixed dict → 被剥离,穿白名单测试抓;(4) 当成在途中间态搁置 → 见 open_questions,本债在 committed b08162cd 已 ship 且无 scope 标记,判定为当下债(非在途),但 medium+无独立 verdict,落手前需 owner 签字确认补标记方向。
- **落地批次/优先级**：Batch-9,内部次序在 R11/R63 之后,与 R12 同批(共用白名单)。与 R10(B14)同 gantt_service.py 需 Phase3 跨桶同批协调。

**本单元待 owner 裁断**：
- R55 无独立对抗 verdict(PHASE0 §6/Phase1 纠偏#15):filtered 关键链补 scope=filtered/full 标记是已实证的当下债(committed b08162cd 已 ship 且全仓无 scope 标记,非在途中间态)。但 severity=medium 且呈现失真涉及前端『完工时间』显示语义,需 owner 裁断:(a) 仅补后端 scope 标记 + 前端据此标注『筛选视图』(推荐,PHASE0 §7 mandated 方向),还是 (b) filtered 时关键链/makespan 直接置 unavailable(reason='filtered_view') 更诚实但改变既有显示。建议 (a)。
- R12 投入量级裁断:已实证 scenario 写入路径经 gantt_adjustment_projection.py:143/:164 校验 st<et,Schedule/候选源 NOT NULL+引擎写入,:84 丢行分支近死。owner 需确认:是否仍按灵魂线合规补 dropped_count/critical_chain_partial 留痕(推荐,低投入且符合『坏数据不准静默兜底』),还是接受『源头已校验』判定为可接受现状只补注释说明。建议补留痕(灵魂线优先)。
- R11≡R63 在 Phase3 master plan 必须合并为单个 work item(Phase1 纠偏#1#2)。需 Phase3 owner 确认排期工具不会因两个 debt id 重复派工撞改 gantt_service_support.py + gantt_critical_chain_provider.py 同两文件。
- 跨桶 R10(B14)与 R55/本桶同 gantt_service.py:Phase3 需裁断 B08 与 B14 是否进同一批次原子提交(推荐),还是显式串行(B14 R10 删 :60-62 在前、B08 改 :335/:375 在后),以避免行号二次漂移与 SP05/契约测试撞车。
