# B17 · 纯叶子死代码

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：本桶 4 条全是 P6/low/load_bearing=false 的纯叶子死代码,grep 已实证生产+测试零有效引用,统一修法=直删(+原子化简/退测试),无一条踩灵魂线、无一条碰承重文件、无跨桶同文件边(pack 的 cross_bucket_same_file_edges=[])。落地批次=Phase1 §4 Batch-16(B16+B17 独立叶子,DAG 无门控,Batch-1 后任意时点可并行),不阻塞也不被任何其他桶阻塞。2026-06-08 执行终态：R64/R65/R61/R53 均已 fixed；R61 已按推荐方案把两条负向测试重定向到 live 入口 normalize_report_resource_filter(behavior 等价,覆盖不退)。

**内部排序**：R64+R65、R61、R53 均已 fixed。本段只保留历史排序和防重复说明；后续不要重复删除 R61 的 plan 死簇，也不要回退测试重定向。

### R64 · P6 — 本提交收口拆分时新建即死的私有 helper,从未接线。body `_ · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — web/viewmodels/scheduler_navigation_links.py:66-67(def _has_navigation_date_range + 单行 body)。该文件不在 16 文件漂移名单(提交态≡基线 b08162cd),已 Read 确认行号准。全仓 grep `_has_navigation_date_range` 仅命中 :66 def 一处,生产/tests/tools 零调用。
- **具体修法**：直删 :66-67 整个 def(并删一行相邻空行,从 :64-65 两空行中收掉一行,保持 PEP8 2-blank 间距,避免 E303;E 规则对非测试文件启用)。不需补『我是故意的』注释(非承重)。删除范围严格限定 :66-67 这一个 def,不碰相邻的 _has_navigation_context(:47-63,被 :71/:114/:142/:175 真用)与 _use_plain_scheduler_chrome(:70-71)。无 call-site 需修(它根本没有调用点)。无 import 孤儿(_has_value 仍被 :49/:63/:67之外多处用,context.get 是 builtin)。
- **爆炸半径**：运行期零(死码)。Phase1 blast 确认:prod_consumers=空,test_consumers=空,contract_topology_tests=空,downstream_effect=仅可读性。删后行为零变化。
- **跨债依赖**：与 R65 同文件(scheduler_navigation_links.py),Phase1 co_change_within_group=[R65]。建议与 R65 同次提交(同文件减少行号二次漂移),但 R64 本身与 R65 无逻辑耦合(各删各的 def),即使分开也不互相 NameError。无其他跨债边。
- **必补回归测试**：无需新增专门回归(纯死码删除)。兜底验收:删后跑 tests/regression_reports_workbench_navigation_contract.py(:159/:172 调 build_scheduler_navigation_links)仍绿——该契约走 _navigation_link 真分支,与本 helper 无关。可选:全量 grep `_has_navigation_date_range` 返回 0 行作为删净证据。
- **误修风险与防呆**：极低。未来 LLM 唯一可能的错=误以为它是 _has_navigation_context(:47)的近亲而连带改/删后者(后者是 live 护栏,被 4 处用)。防呆:计划里钉死『只删 :66-67 这一个 def,_has_navigation_context 与 _use_plain_scheduler_chrome 一字不动』。因非承重、无 import 孤儿,无其他陷阱。
- **落地批次/优先级**：Batch-16(B16+B17 独立叶子,Phase1 §4)。无关键路径门控,Batch-1 后任意时点可落。建议与 R65 打包成同一原子提交(同文件)。

### R65 · P6 死分支 — 收口拆分遗留的兜底 url 构造器;数据上不存在 plain_ · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — web/viewmodels/scheduler_navigation_links.py:74-78(def _target_url)+ :160(唯一调用点 `plain_url or _target_url(context, str(target_page), view=view)`)。已 Read 验证 build_scheduler_navigation_links 的 specs(:144-152)共 9 条,第 3 字段 plain_url 全为非空字符串字面量('/scheduler/'、'/scheduler/batches'、'/scheduler/config'、'/scheduler/resource-dispatch'、'/scheduler/gantt?view=machine'、'…view=operator'、'/scheduler/analysis'、'/scheduler/week-plan'、'/scheduler/calendar')→ :160 `plain_url or …` 中 plain_url 恒真,_target_url 永不求值。grep 确认 _target_url 仅 :74 def + :160 一处引用,tests/tools 零命中。
- **具体修法**：原子三件套(必须同一次提交,缺一即破):①删 def _target_url(:74-78);②把 :160 从 `link = _plain_link(label, plain_url or _target_url(context, str(target_page), view=view), target_page=str(target_page or key))` 化简为 `link = _plain_link(label, plain_url, target_page=str(target_page or key))`(去掉 ` or _target_url(...)`);③删两个就此孤儿的 import:第 4 行 `from urllib.parse import urlencode` 与第 6 行 `from .scheduler_workbench_link_query import query_for_target`(二者仅在 _target_url 内 :75/:76 使用,grep 已证文件内别处不用;query_for_target 在 scheduler_workbench_link_query.py/scheduler_workbench_links.py 另有 live 使用,删本文件 import 不会使源符号孤儿)。注意保留 :7 的 TARGET_PAGE_PATHS import(:177 build_report_navigation_links 仍用)。非承重,不补注释。
- **爆炸半径**：运行期零(死分支)。Phase1 blast:prod_consumers 仅 :160(但 plain_url 恒真使其永不调用),test_consumers=空,contract_topology_tests=空。downstream_effect:化简 :160 后输出 url 与化简前逐字相同(都返回 plain_url 字面量),对 build_scheduler_navigation_links 的 plain-chrome 分支零行为变化。
- **跨债依赖**：与 R64 同文件(co_change=[R64])。建议同次提交。无跨桶同文件边。R65 自身的三件套是『原子』:def 删 + :160 化简必须同次,否则留对已删函数引用→NameError;import 删虽不致 NameError 但属同一动作。
- **必补回归测试**：无需新增(死分支删除)。强制验收:跑 tests/regression_reports_workbench_navigation_contract.py(经 web.navigation_context re-export 调 build_scheduler_navigation_links,:159/:172 断言 gantt 链 query 不串味)必须仍绿——这些断言走 :157-158 _navigation_link 真分支,不碰被删的 else/plain 分支,化简 :160 不影响。建议补一行 grep 断言:全仓 `_target_url`、文件内 `urlencode`/`query_for_target` 删后返回 0(防孤儿 import 残留)。
- **误修风险与防呆**：中(本桶最高)。两个真实陷阱:①未来 LLM 删 def 却忘记化简 :160 → NameError 立刻炸 build_scheduler_navigation_links(任何排产页顶部导航 500);防呆=计划明文『def 删与 :160 化简同次,且改完本地 import 这个模块自检不报 NameError』。②删 def + 化简 :160 后忘删 :4/:6 孤儿 import —— 因 pyproject.toml:35 全局 ignore F401,ruff/fast_static_precheck 不会报,会留静默残渣骗后人『这文件还在用 urlencode』;防呆=把『删 :4 urlencode + :6 query_for_target』写进 R65 的删除清单并附 grep 验证,不能依赖静态门。
- **落地批次/优先级**：Batch-16(Phase1 §4 明列 R65『def 删须同步 :160 化简』)。与 R64 打包同一原子提交。Batch-1 后任意时点,无门控。

### R61 · 已 fixed · P6 出生即死 — 40 行 Python 行匹配实现,仅被合同测试供养制造『存 · sev=low · load_bearing=False

- **执行终态**：2026-06-08 已 fixed。旧 `filter_plan_rows_for_report_context`、`_plan_row_matches_batch`、`_plan_row_matches_resource` 在 core/web/data/tests Python 代码中零命中；生产计划行过滤仍走 SQL(data/repositories/schedule_plan_query_repo.py 的 append_detail_filters,来自 schedule_resource_sql_filters.py)。
- **已执行修法**：删死簇:旧①filter_plan_rows_for_report_context(:172-187);②仅供它的 helper _plan_row_matches_batch(:160-161)与 _plan_row_matches_resource(:164-169)(及其间空行)。已保留:_row_text(:156,当前被 downtime live :161/:191/:209/:223 真用)、normalize_report_resource_filter(:119,被当前 :252 downtime 份 + report_engine.py:146/167/279/383 + web/request_resource_context.py:39 + web/routes/reports_request_support.py:55 真用)、整个 filter_downtime_rows_for_report_context live 孪生簇(当前 :244-263,report_engine.py:406 live)。测试侧已删 import filter_plan_rows_for_report_context；两条负向测试已改为直接调用 `normalize_report_resource_filter(...)`。
- **爆炸半径**：运行期零(死)。Phase1 blast:prod_consumers=生产零引用(已证),test_consumers=4 处(import+3 调用),contract_topology_tests=同 4 处续命合同。downstream=去维护成本+消除『过滤发生在 Python 层』的层级误导。⚠️绝不能误伤孪生 filter_downtime(report_engine:406 live)及其测试 :16-61(:28/:39/:51/:59)。
- **跨债依赖**：无 — Phase1 co_change_within_group=[],fix_invalidation_risk 明示『无其他债依赖』。完全独立,与本桶 R64/R65/R53 不同文件,任意先后。
- **验证终态**：`tests/scheduler_analysis/test_report_context_filters_contract.py` 13 passed；ruff 通过；Python 3.8 语法扫描 0 发现；全仓 `rg` 证三死符号零命中，downtime/normalize live 符号仍在。
- **误修风险与防呆**：当前最大风险变成重复执行或误删 live 孪生。禁删 `filter_downtime_rows_for_report_context`(当前 :244,live)、`_row_text`(:156)、`normalize_report_resource_filter`(:119)，禁回退测试重定向。
- **落地批次/优先级**：2026-06-08 已 fixed；本字段仅保留历史批次归属，不表示仍待执行。

### R53 · P6 — 重构成 ScheduleRunState 后真消费已移入 _coerc · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/algorithms/greedy/dispatch/batch_order.py:74 `_ = scheduled_count`。已 Read 验证:形参 scheduled_count(:39)真消费在 :58(_coerce_state(scheduled_count=scheduled_count),→ ScheduleRunState.from_legacy);:75 return 用 run_state.scheduled_count(property),与 :74 无关;:74 是压制『未使用参数』告警的空操作面包屑,参数早被用。已用 ruff 实跑该文件=All checks passed(说明 :74 删后不会触发任何 lint:F841 只管局部赋值未用、ARG 规则未在 pyproject.toml select 列表[E/F/W/I/UP/B]内,故不会冒出 unused-argument)。
- **具体修法**：直删 :74 这一行 `_ = scheduled_count`(整行,连同其单独占行)。不碰 :39 形参声明(对外签名,benchmark/契约依赖)、不碰 :58 真消费、不碰 :75 return。非承重,不补注释。删后 scheduled_count 仍在 :58 被『用』(作为 kwarg 传入 _coerce_state),无 unused-arg 风险,无需补任何替代抑制行。
- **爆炸半径**：1 行删除,无行为影响(Phase1 blast:downstream_effect=无,test_consumers=无)。数据流不变:state 路径仍由 :58 注入 scheduled_count。
- **跨债依赖**：无 — Phase1 co_change=[],fix_invalidation_risk=none。独立单文件,与本桶其余 3 条不同文件。
- **必补回归测试**：无需新增。兜底验收:删后跑 tests/test_greedy_refactor_contracts.py、tests/test_greedy_ordering_contract.py、tests/regression_batch_order_bid_unboundlocal.py、tests/regression_batch_order_override_dedup.py 全绿(均不依赖该 no-op);本地 ruff check batch_order.py 仍 All checks passed。
- **误修风险与防呆**：低,但有一个明确陷阱:存在独立同形行 core/algorithms/greedy/dispatch/sgs.py:127 `_ = scheduled_count`,【不是本债、不在本桶任何成员里】。未来 LLM 若用 `grep '_ = scheduled_count'` 批量删会误连 sgs.py:127 一起删。防呆=计划明文『R53 只删 batch_order.py:74 这一行,sgs.py:127 是独立行,本债不处置、不要顺手删』。另一陷阱=误删 :39 形参(会破坏对外签名/调用方传参)——明文只删弃值行,不动签名。
- **落地批次/优先级**：Batch-16(独立叶子,Phase1 §4 列 R53『删 :74 一行』)。无门控,Batch-1 后任意时点;可与 R61 合并成『杂项叶子删除』小提交,或单独落。

**本单元待 owner 裁断**：
- R61 重定向方案已执行:2026-06-08 按推荐方案把两条负向测试从死函数入口改为直接调 live 的 normalize_report_resource_filter,保住『班组不支持/缺编号』的 loud-校验覆盖；不再需要 owner 裁断重定向 vs 净删。
- sgs.py:127 的 `_ = scheduled_count` 是否也是死空操作:它与 R53(batch_order.py:74)同形,但不在本次债清单/任何桶内。本桶严格不处置。若 owner 认为它同样是面包屑死码,应另立债走对应 core-algorithms 桶,不要并入 R53 顺手删(避免越权改动未审计代码)。
