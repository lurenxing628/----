# B16 · P4灵魂线

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：本桶 = 两条孤立的 P4 灵魂线债（R32 备份完整性、R40 库存写库），都 not load_bearing、severity=low、各自独立文件、cross_bucket_same_file_edges 为空（B16 pack 实测）。整体策略=把两处"坏数据静默兜底"改成 loud raise，绝不许加任何新兜底/降级默认值（红线1）。两债互不依赖（不同文件、无共享边），可并行；但实战影响 R32 远大于 R40（R32 关乎备份/恢复可信度，坏备份会被 _auto_rollback 当救命快照盖活库放大损坏；R40 是 service 层 _norm_float + model 层 from_row 双重已挡的不可达防御死角，纯无害死代码）。故内部排序 R32 在前、R40 在后。落地批次=Phase1 §4 Batch-16（B16+B17 叶子/P4），前置只需 Batch-1 承重注释网先落（其实本桶两文件都不在承重清单，弱依赖），不门控任何其他桶。复核结论：R32 backup.py:334-352 五处行号全 ✅准且严重度倒挂（执行抛异常=更严重却只 warning 放行，非ok结果反而 raise）已 Read 坐实；R40 material_repo.py:64-72 ✅准，唯一调用方 material_service.py:113 上游 :99-100 已 _norm_float 强校验、MaterialRepository 仅在 MaterialService 内构造（request_services.py:155-158）无旁路，except 分支生产不可达已证实。两债均无任何续命测试钉死其吞咽行为（grep 实测），改 raise 不会让现有测试变红，但都必须新补一条契约测试钉死"坏值即 raise 不落地"。

**内部排序**：R32 先、R40 后。依据：两债不同文件、cross_bucket_same_file_edges=[]、co_change 仅彼此（不同文件互不影响），故无强制先后、可并行；但按实战影响排序——R32 关乎备份/恢复可信度且坏备份会被 _auto_rollback 当救命快照盖活库放大数据损坏（真实危害），R40 是 service+model 双重已挡的不可达防御死角（当前无错被吞、纯无害死码），故 R32 优先投工。两者同属 Batch-16，前置仅 Batch-1 承重注释网（且本桶两文件均不在承重清单，弱依赖），均不门控其他桶。

### R32 · P4 灵魂线（坏数据静默兜底）。PRAGMA integrity_check 的 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/infrastructure/backup.py 已 Read 复核：:334 rows=dest.execute('PRAGMA integrity_check').fetchall() or []；:335-337 except Exception as e 仅 fallback_log(warning,'…完整性检查执行失败（已忽略）…')；:338-342 else 分支 msg0!='ok' 时 fallback_log(error)+raise RuntimeError；:343 os.replace(tmp_path,backup_path) 把未校验 .tmp 升正式备份；:346-352 finally 最佳努力清 .tmp（已在，抛错不残半成品）。严重度倒挂坐实：执行抛异常（IO/锁/database disk image malformed=更严重损坏迹象）反而只 warning 放行，返回非 ok 行（较轻）才 raise。git status --porcelain 确认 backup.py 未被修改，提交态≡基线 b08162cd，行号全准。
- **具体修法**：改 loud raise（严禁保留 warning 吞咽，严禁加任何'校验不了就照样落地'的兜底）。把 :335-337 的 except 分支由'只 warning 后落到 :343 os.replace'改为与 :340-342 else 分支一致：except Exception as e: fallback_log(self.logger,'error',f'备份后的数据库完整性检查执行失败：{e}'); raise RuntimeError(f'备份后的数据库完整性检查执行失败（视为不可信备份，不落地）：{e}') from e。语义=无法运行 integrity_check 的库不算可信备份，绝不 os.replace 升正式。finally :346-352 .tmp 清理已在，raise 不残半成品。建议在 :335 改动点本地补一行'我是故意的'中文注释：『校验执行失败比返回非 ok 更严重（库损坏/IO/锁），故必须 raise 不落地——有个坏备份比没有更危险，会被 restore 自动回滚当救命快照盖活库』，钉死这是有意的 loud 行为防未来被改回 warning。前置（非阻塞顺手收尾）：system_backup.py:107-113 手动备份路由当前只 catch MaintenanceWindowError，RuntimeError 会落到 web/error_handlers.py:100-114 的通用 500 页（'服务器内部错误，请查看日志'）——建议同改为捕获通用备份失败 flash 清晰错误+redirect（注意：仍 loud 不落地、不产出备份，只是改善错误呈现，非吞错；且该 500 路径今天已因 :340-342 else 存在，本次只是多一个触发器经既有路径）。
- **爆炸半径**：生产消费方（Phase1 实测）：migration_runner.py:106 探针 src.backup / :188 before_migrate manager.backup（改 raise 后 :189 重抛→按 :61 文档契约'取不到可用备份即阻断迁移'，更正确，非破坏不变量）；backup_task.py:88 auto（:149 except Exception 记错不崩，优雅承接）；factory.py:137 exit（:142 except Exception，优雅）；system_backup.py:108 manual（见 fix 前置）；backup.py:415 restore→backup(before_restore) 内部；_auto_rollback:387 盲拷。测试：regression_restore_success_condition.py 全程 mock BackupManager+ensure_schema 不触达 :334 校验块（改 except→raise 不会让它红）；regression_system_backup_presenter_contract.py 是 presenter 层不 pin backup() 吞咽；smoke_phase9.py:106/138/152 POST /system/backup/create 期望 200 = happy path（好库 integrity ok，无变化）；regression_maintenance_window_mutex.py / regression_backup_restore_pending_verify_code.py:38 真 backup 但走维护锁/happy path。grep 实测无任何测试钉死本警告吞咽行为或 else-raise 行为。
- **跨债依赖**：无硬跨债依赖。B16 pack cross_bucket_same_file_edges=[]，Phase1 R32 co_change_within_group=[]。唯一内部前置 R32→R32-raise（system_backup.py:107-113 错误呈现收尾，非阻塞，可同改或后补）。与同桶 R40 是 P4 灵魂线对但不同文件、互不影响。lb_colocation_danger=none（backup.py 不在 PHASE0 §5 承重清单）。落地：Batch-16，Batch-1 后任意时点可落，不门控他桶。
- **必补回归测试**：新补一条 backup integrity 契约测试，断言两半对称：(1) mock dest.execute 使 'PRAGMA integrity_check' 抛异常时 → manager.backup() raise（RuntimeError）、backup_path 不存在（未被 os.replace 升正式）、tmp_path 已清；(2)（强化既有未覆盖的 else 分支）mock integrity_check 返回非 'ok' 行时同样 raise 且不落地。两断言一起钉死'执行失败与非ok结果都必须 loud 不落地'，封堵未来把 except 改回 warning。可选：再加一条断言改 raise 后 migration_runner before_migrate 失败即阻断迁移（对齐 :61 契约）。
- **误修风险与防呆**：最可能的误修=未来 LLM 以'有个备份好过没有/不要因校验失败就不备份'为名把 except 改回 warning 静默放行（why_debt 明确承认这种立场，但 verdict refuted=True 已判其为真债）——这会重新打穿灵魂线。防呆：①改动点本地'我是故意的'注释写明'无法校验完整性的库不算可信备份，必须 raise'+严重度倒挂理由；②新契约测试钉死 execution-fail→raise→不落地，改回 warning 立即变红。次要误修=有人顺手给 _copy_db_file/migration_backup.py:52-58 的回滚盲拷加 integrity 复检时改成'校验不过就跳过/静默继续'（又一层兜底）——若做该加固必须 loud（喂回 _auto_rollback 既有 RestoreResult(ok=False) 失败路径），不可静默跳过；该加固属独立后补、不入本次核心修法。
- **落地批次/优先级**：Batch-16（Phase1 §4 / DAG，B16+B17 叶子/P4），前置仅 Batch-1 承重注释网（弱依赖，backup.py 不在承重清单）。本桶内排第 1（实战影响最大：备份/恢复可信度+回滚放大损坏风险）。不门控任何关键路径。

### R40 · P4 灵魂线（但不可达防御死角）。stock_qty 不可转 float 时 e · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — data/repositories/material_repo.py 已 Read 复核：update 方法 :51-82；:63 val=updates.get(key)；:64 if key=='stock_qty'；:66 val 为 None/空串→continue（不改）；:68-69 try: val=float(val)；:70 except Exception；:71 注释'留给服务层校验；这里保持原值'；:72 val=updates.get('stock_qty')（保留原始坏值）；:74-75 set_parts.append+params.append(val)→:81 UPDATE Materials 把坏值写进 REAL 列。不可达性已证实：唯一调用方 material_service.py:113 self.repo.update(mid,updates)，上游 :99-100 已 self._norm_float(stock_qty,field='库存数量',min_v=0.0)（_norm_float :32-41 实测：None/空→0.0，float() 失败→raise ValidationError，<min_v→raise），坏值在 service 层即被拦；MaterialRepository 仅在 MaterialService(:22)/BatchMaterialService(:20，但该 service 不调 mat_repo.update) 内构造，经 request_services.py:155-158 装配，无任何路由把裸 repo 交给调用方=无旁路。grep 'UPDATE Materials' 全仓仅此 :81 一处。git status 确认文件未修改，行号准。
- **具体修法**：改 loud（严禁保留 :72 静默 val=原值，严禁加新兜底）。最小且分层安全的修法=删 :70-72 的 except 吞咽，让 :69 val=float(val) 在坏值时自然抛原生 ValueError（loud，无需任何 import、无分层耦合）。同时在该处补一行'我是故意的'注释：『service 层 _norm_float 是第一道强校验（库存数量必须数字、>=0），本层不静默保留坏值；若未来有旁路绕过 service 直调 repo，此处 raise 即暴露而非把坏值写进 REAL 列（灵魂线）』。替代方案（需 owner 裁定错误分类）=抛 repo 层具名清晰错误；但 data 层 raise core 的 ValidationError 会引入 data→core 错误耦合，故默认推荐原生 ValueError 或不引依赖的清晰报错。无论哪种，红线=必须去掉静默 val=原值、不得改成又一层兜底。
- **爆炸半径**：极小。唯一调用方 material_service.py:113，上游 _norm_float 已挡（坏值到不了 repo），改 raise 生产路径不可达=对现有行为零影响。测试：regression_models_numeric_parse_hybrid_safe.py:168-175 钉的是 MODEL 层 Material.from_row（NaN→raise、''→0.0），非 repo 的 except 吞咽，属相邻护栏不是本债续命；smoke_e2e_excel_to_schedule.py:649-650 用 stock_qty='0'（合法值、走 service/route）不触本分支。grep 实测无任何测试 exercise repo.update 传坏 stock_qty 依赖静默保留=改 raise 不会让任何测试变红。
- **跨债依赖**：无硬依赖。B16 pack cross_bucket_same_file_edges=[]；Phase1 R40 co_change_within_group=[R32]（同桶 P4 对，但不同文件、互不影响，无续命测试需退）。lb_colocation_danger=none（material_repo.py 不在承重清单）。
- **必补回归测试**：新补一条 material_repo 契约测试：直接构造 MaterialRepository.update(mid,{'stock_qty':'abc'}) 断言 raise（ValueError 或所选具名错误），并断言 Materials 表 stock_qty 未被写入坏值（行未变/无静默坏写）。钉死'repo 是 service _norm_float 之后的第二道 loud 闸门，坏值即 raise 不落库'，封堵未来重新加静默兜底或误删上游 _norm_float。
- **误修风险与防呆**：两种误修：①未来 LLM 重新加回静默 fallback（如 'except: val=0.0' 或 'val=原值'），复活 P4——防呆=新契约测试钉死 raise + 注释写明本层不静默保留坏值；②有人看到'service 已校验'误以为 repo 可放宽、顺手删掉上游 material_service.py:99-100 的 _norm_float（以为 repo 兜底），结果 repo 静默保留坏值→坏值入库——防呆=注释明写'_norm_float 是第一道强校验、repo raise 是第二道，二者不可互相替代'，且 regression_models_numeric_parse_hybrid_safe.py:168-175 model 层守卫保留。严禁以'统一/简化'之名把多层 loud 校验塌成一层兜底。
- **落地批次/优先级**：Batch-16（Phase1 §4 / DAG，B16+B17 叶子/P4），Batch-1 后任意时点可落，不门控他桶。本桶内排第 2（severity=low、不可达死角、实战收益最小，纯灵魂线对齐+防未来旁路）。与 R32 可并行。

**本单元待 owner 裁断**：
- R32 可选后补加固（回滚消费侧 integrity 复检）是否纳入本轮：_copy_db_file(backup.py:354-372) 与 migration_backup.py:52-58 的 restore_db_file_from_backup(shutil.copy2 盲拷) 当前盖活库前零完整性复检，封堵的是'旧版遗留未校验备份被当救命快照盲拷'的残口。但这是行为变更（在紧急回滚路径加校验可能阻断 emergency rollback），且必须 loud（校验不过须喂回 _auto_rollback 既有 RestoreResult(ok=False) 失败路径，绝不可静默跳过）。建议作为独立 follow-up 债、不混入 R32 核心 except→raise，需 owner 裁定是否本轮做及如何处理'回滚被阻断'的取舍。
- R40 raise 形态需 owner 裁定错误分类：默认推荐删 try/except 让 float() 抛原生 ValueError（最省、无 import、不引入 data→core 分层耦合）；若要 repo 层抛具名领域错误（如 ValidationError），会让 data 层 import core 错误体系——需确认仓库是否已有'data 层可抛 core 错误'的既定约定，否则触碰分层口径。无论选哪种，红线=去掉静默 val=原值、不加新兜底。
- R32 前置 system_backup.py:107-113 错误呈现收尾的批次归属：建议与 except→raise 同一次改动落（因 raise 给手动备份路由新增一个经现有通用 500 页的触发器，同改可让用户看到清晰 flash 而非'服务器内部错误'裸 500），但也可独立后补——需 owner 确认是否同批，以免拆成两次改同文件。
