# B14 · 排产主链冻结死码

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：B14 = 排产主链上的冻结死代码，4 条债性质差异大、分散在 3 个不同 Phase1 批次，桶内无任何内部硬依赖（4 债互不改同一文件），全部靠"跨桶同文件边"对外协调。整体策略：R01/R10 纯死代码机械直删但必须 rebase 进各自跨桶邻居的批次（不能单独动文件）；R03 是本桶最危险条目，须把"承重 narrow-except"与"不可达下游脚手架"严格拆开两段处理；R27 直删但被 V22 强制与 R06(B09) 同提交。逐条：(1) R01(P6 medium)——count/has_actionable_schedule_rows 死函数链 + 唯一私有依赖 _iter_actionable_results，靠两层 re-export(run→scheduler schedule_persistence)+__all__+SP05 拓扑断言续命，生产零调用，删法机械但同 schedule_payload_contract.py 与 R04(B05) 共居须同批；SP05:333 的 `tuple(old_module.__all__)==expected` 是硬同步点。(2) R10(P6 low)——get_latest_version_or_1 死方法且名实矛盾(or_1 实返 0)，仅测试 stub 镜像，同 gantt_service.py 与 R55(B08) 共居须同批。(3) R03(混合：承重+不可达 P6)——runner.py:216 窄 except 是 b81f8b3f 治理后的护栏(只补注释，且其约束已被 4 条现存 propagation 契约测试钉死，无需新测试)；下游 FAILED 计数/UI 脚手架因持久化 DB CHECK(v10.py:14 status IN failed/not_run)不可裸删，本轮只登记+defer，等枚举契约 owner 裁断。(4) R27(P3 low)——空 delayed 包 calendar/+batch/(+dispatch=R06+gantt) 搁置脚手架，直删但必须与 R06 V22 同提交(4 空包 + SP05:310/315 缺一则红)。

**内部排序**：桶内 4 债互不改同一文件，无强制串行；落地顺序完全由所挂的跨桶批次决定，不由桶内决定。时序：(1) R03 的承重注释最先落 → Batch-1(全局承重注释网 ROOT)，与其他 LB 注释一起，给主链护栏立约；(2) R01 → Batch-6(随 B05 parse 收口，同 schedule_payload_contract.py 与 R04 同批/串行)；(3) R10 → Batch-9(随 B08 SCC-GANTT，同 gantt_service.py 与 R55/R11/R12/R63 同批)；(4) R27 → Batch-10(随 B09 + R06，V22 联合提交)。R03 下游脚手架清理：无批次(defer，待裁断)。阅读复杂度递增建议：R01/R10(机械直删)→ R27(跨桶 V22 协调直删)→ R03(承重+持久化枚举裁断)。

### R01 · P6（拆分重构后遗留死函数，直删类） · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/run/schedule_payload_contract.py:90 count_actionable_schedule_rows、:94 has_actionable_schedule_rows(:95 内部调 count)、:67-87 _iter_actionable_results(唯一私有依赖，静默跳过无 op_id/坏时间行只计数)、__all__ 在 :410-417(count=:414, has=:415)。两层 re-export 已复核：run/schedule_persistence.py:12-18 的 import 块含 :16 count、:17 has(同块 :13-15 还导入 ValidatedSchedulePayload/ValidatedScheduleRow/build_validated_schedule_payload，须保留)；scheduler/schedule_persistence.py:3 二次 import、:5 __all__=['count','has','persist_schedule']。SP05 断言 tests/test_sp05_path_topology_contract.py:54-55(在 SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS['core.services.scheduler.schedule_persistence'] 元组内)。grep 全仓：生产零调用，唯一非 def 引用是 has() 内部调 count()(:95)。
- **具体修法**：直删，5 个站点 + SP05 必须同一提交。(1) 删 schedule_payload_contract.py 的死簇 _iter_actionable_results(:67-87)+count(:90-91)+has(:94-95)——三者构成闭合死环(_iter 仅被 count 调、count 仅被 has 调、has 无人调)，整簇删除；(2) 删 schedule_payload_contract.py:414-415 两个 __all__ 条目；(3) 删 run/schedule_persistence.py:16-17 两行(只删这两行，保留 :12-15/:18 的 import 块结构，确保 build_validated_schedule_payload 等仍被导入)；(4) 改 scheduler/schedule_persistence.py:3(去掉 count/has，保留 persist_schedule)与 :5 __all__→['persist_schedule']；(5) 改 tests/test_sp05_path_topology_contract.py:54-55(从期望元组删两个串，保留 'persist_schedule')。硬同步铁律：SP05:333 断言 `tuple(old_module.__all__)==expected_symbols`，故 scheduler/schedule_persistence.py:5 与测试 :54-55 必须字节级同提交对齐，否则红。注意 _iter_actionable_results 的静默跳过逻辑是死代码、零生产价值——直删即可，严禁以『保留可观测性』名义把它折叠进 build_validated_schedule_payload 的活校验路径(那条路径用 _strict_positive_int/_build_validated_schedule_row，与 _iter 无关)。
- **爆炸半径**：生产零影响(无任何 prod 路径)。触及文件：schedule_payload_contract.py、run/schedule_persistence.py、scheduler/schedule_persistence.py、tests/test_sp05_path_topology_contract.py。_iter_actionable_results 与活校验入口 build_validated_schedule_payload 完全独立，删它不碰 persist 主链。
- **跨债依赖**：schedule_payload_contract.py 跨桶与 R04(B05 正整数/datetime 收口，落点 _strict_positive_int :50 + 6 调用点 :72/:149/:198/:308/:324/:372)共居。R04 与 R01 改不同区域(R04=_strict_positive_int 簇，R01=_iter/count/has 簇，互不重叠)但同文件，必须同批次或显式串行避免行号互撞。Phase1 边 R04→R01『同文件同改』；Phase1 Batch-6 已把二者归同批。二者间无前后置顺序要求，仅共居约束。
- **必补回归测试**：无需新增测试(纯死代码删除，测『不存在』无意义)。验收：SP05 拓扑契约在 count/has 从期望元组移除后保持绿(这正是该测试在做的事)；SP05 strong-alias/behavior-compat 块(:320-340)通过；`import core.services.scheduler.schedule_persistence` 仍暴露 persist_schedule。
- **误修风险与防呆**：(1) LLM 删了 count/has 却忘改 SP05 元组→红，或更糟『修复红灯』时把函数加回来。防呆：把 5 站点+SP05 绑成单提交 checklist。(2) 过度删除：误删 run/schedule_persistence.py:12-18 整个 import 块，连带 build_validated_schedule_payload 导入丢失→ImportError。防呆：只删 :16-17 两行。(3) 误把 _iter_actionable_results 当作 build_validated_schedule_payload 的活 helper——grep 已证唯一调用者是 count。(4) 灵魂线：勿『保留』_iter 的静默跳过语义折叠进活路径，它是死的、删除正确，不是可观测性回退。
- **落地批次/优先级**：Batch-6(B05 + R01 parse 互锁)，与 R04 同批。独立于 B01 全局根。Medium 优先级。

### R10 · P6（零生产消费死方法，名实矛盾） · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/gantt_service.py:60-62 `def get_latest_version_or_1(self)->int: v=int(self.history_repo.get_latest_version() or 0); return v if v>0 else 0`。名字承诺 or_1、空态实返 0，矛盾坐实。:64 resolve_version 是另一个独立 LIVE 方法(也用 get_latest_version，勿碰)。grep 全仓仅 2 处引用：def(:60)+ tests/regression_scheduler_week_plan_summary_observability.py:59 的 _GanttServiceStub.get_latest_version_or_1(返回 3)。周计划路由 web/routes/domains/scheduler/scheduler_week_plan.py 只调 resolve_week_range(:278) 和 get_week_plan_rows(:284/:377)，从不碰此方法；stub 虽被实例化(:114/:439)但该方法从不被 invoke。
- **具体修法**：直删。(1) 删 gantt_service.py:60-62 整个 get_latest_version_or_1 方法；(2) 删测试镜像 stub tests/regression_scheduler_week_plan_summary_observability.py:59-60(`def get_latest_version_or_1(self): return 3`)——它仅为镜像 API 表面存在、从不被调用。验收前确认 resolve_version(:64)未被触碰(它是活的版本解析路径)。
- **爆炸半径**：生产零影响(零 caller)。触及文件：gantt_service.py、tests/regression_scheduler_week_plan_summary_observability.py。删除还顺带消除『or_1 返回 1』的命名误导(未来读者可能据名以为空态有 version=1 兜底)。
- **跨债依赖**：gantt_service.py 跨桶与 R55(B08，filtered 关键链冒充整版，落点 :375 critical_chain_for_plan_detail_filter)共居。R10(删 :60-62)与 R55(在 :375 加 scope)改不同区域但同文件，须同批或串行。Phase1 边 R10→R55『同文件同改』；Phase1 Batch-9(B08 SCC-GANTT)已把 R10 与 R55/R11/R12/R63 归同批。
- **必补回归测试**：无需新增测试。验收：regression_scheduler_week_plan_summary_observability.py 在 stub 方法删除后保持绿(已 grep 确认该测试真实断言不引用 get_latest_version_or_1，仅 :59 有 def)；跑周计划路由相关测试确认无路径回归。
- **误修风险与防呆**：(1) LLM 删方法却留测试 stub→无害但不彻底(死 stub 残留)。(2) 更糟：未来 LLM 见名『get_latest_version_or_1』而『修 bug』把空态返回改成 1 并接入某路径——复活『空历史伪装成 version=1』的静默兜底(踩灵魂线：空历史应暴露为 no_history/status，不是假造 version 1)。现在直删即免疫。(3) 勿与 resolve_version(:64，活)混淆。防呆：只删 :60-62。
- **落地批次/优先级**：Batch-9(B08 SCC-GANTT)，与 R55 同批。自成簇，不阻塞他桶。Low 优先级。

### R03 · P6（不可达下游死管道）混 P2 承重(narrow-except)——必须拆两 · sev=low · load_bearing=False

- **当前位置(已复核)**：⚠️部分行号已变 — 承重 narrow-except：core/services/scheduler/run/schedule_candidate_runner.py:216 `except CandidateTrialFailure as exc:` ✅准(承重，留)。FAILED 机制：CANDIDATE_STATUS_FAILED(:24)、_failed_plan(:470-483)、_count_candidates(...FAILED)→failed_count(:241)、_baseline_missing_or_failed(:263-267)→baseline_missing_or_failed(:247)。下游：schedule_candidate_summary.py:135(failed_candidate_count)、:140/:166/:191(baseline_missing_or_failed)✅准；web/viewmodels/dashboard_workbench.py:231 ⚠️行号已变(报告:207→盘上:231，在 16 文件漂移名单)`if comparison.get('baseline_missing_or_failed')`；web/viewmodels/scheduler_analysis_candidate_helpers.py:324(failed_candidate_count)、:329(baseline_missing_or_failed)✅准。生产零 raise：grep `raise CandidateTrialFailure` 全仓仅测试(regression_scheduler_candidate_runner_contract.py:470,576)。持久化枚举硬底：core/infrastructure/migrations/v10.py:14 `status TEXT NOT NULL CHECK(status IN ('completed','failed','skipped','not_run'))`。baseline 不可达链已核：specs.py:61 baseline 恒 sequence=0 首跑，runner.py:172 `now()>=deadline` 首 spec 不成立→baseline 永不被 skip。
- **具体修法**：两段严格拆开。(A) 承重 narrow-except :216——逻辑零改动，仅在 :216 上方补『我是故意的』注释。R03 不在 90 节 8 条 LB 预起草名单(它经 Phase1 §1 判为 verdict-confirmed 承重护栏)，故按 90 节风格起草如下三行：`# 只捕获显式 CandidateTrialFailure(候选级失败);其余异常(ValidationError/RuntimeError/TypeError)必须继续上抛。` / `# 这是 b81f8b3f 收窄后的护栏:绝不能以"统一/简化"名义改回 except Exception,` / `# 否则复活被治理掉的"未知异常静默转 failed candidate"静默吞错(踩灵魂线)。` 该约束已被现存 4 条契约测试钉死，注释是唯一缺口。(B) 下游 FAILED 脚手架——本轮 LOW 优先、不裸删。status 枚举 failed/not_run 由持久化 DB CHECK(v10.py:14)强制，枚举契约面(CANDIDATE_STATUS_FAILED/_failed_plan)必须保留(往返 DB)。登记为债但 defer，待枚举契约 owner 裁断。若未来清理推进，至多给 viewmodel 恒-False/恒-0 分支(dashboard_workbench.py:231、scheduler_analysis_candidate_helpers.py:324-329)补 `# 生产不可达:仅持久化 schema enum 往返时可能为真` 的可观测注释，严禁静默删除枚举面或 if 分支。灵魂线：narrow-except 绝不可改回 except Exception。
- **爆炸半径**：(A) narrow-except 注释=零行为变化。(B) 下游若清理=中等：牵动持久化 ScheduleCandidate.status 枚举契约(v10.py:14 CHECK)+ 2 个 viewmodel(dashboard_workbench、scheduler_analysis_candidate_helpers)+ 候选公开摘要字段(schedule_candidate_summary.py:135/140)+ 大量候选契约测试。本轮只落注释→实际 blast≈无。
- **跨债依赖**：桶内无硬依赖(runner.py 是 B14 独占文件，无同文件跨桶边)。承重注释归 Batch-1(全局承重注释网 ROOT)，与其他 LB 注释一起(Phase1 §1 已把 R03 列入)。下游清理(若推进)独立、低优先、须 LATE(枚举契约 owner 裁断之后)。
- **必补回归测试**：无需新增——承重绑定已存在：regression_scheduler_candidate_runner_contract.py:518 test_candidate_runner_propagates_unexpected_contract_errors(TypeError 上抛)、:542 _propagates_runtime_contract_errors(RuntimeError 上抛)、:491 _raises_validation_error_instead_of_selecting_baseline(ValidationError 上抛)、:459 _records_explicit_candidate_trial_failure_and_continues(仅显式 CTF→failed)，外加测试 :473 注释明示『只有显式 CandidateTrialFailure 是候选级失败;ValidationError/RuntimeError/TypeError 必须继续向上抛』。这 4 条共同钉死『窄 except 不得放宽』。补的注释只是在生产侧文档化这些测试已强制的约束。验收：上述 4 测试保持绿(无逻辑改动必绿)。
- **误修风险与防呆**：本桶最高。(1) 灵魂线陷阱：未来 LLM 见 `except CandidateTrialFailure` 紧挨看似死的 FAILED 下游而『简化』成 `except Exception`→静默把 optimizer 契约破坏吞成『失败候选』，正是 b81f8b3f 治理掉的回归。防呆：注释 + 4 条现存 propagation 测试。(2) 过度删除陷阱：LLM 见 failed_count 恒 0/baseline_missing_or_failed 恒 False 而删 CANDIDATE_STATUS_FAILED/_failed_plan/枚举→破 v10.py:14 DB CHECK 往返(持久化 status='failed'/'not_run' 的行加载/校验失败)。防呆：本计划 defer 下游 + 标 DB CHECK 为硬底。(3) 勿在裁断前删 viewmodel if 分支——它们是持久化 schema 的防御面。
- **落地批次/优先级**：承重注释→Batch-1(全局承重注释网 ROOT，最先落)。下游脚手架清理→DEFER(低优先，待枚举契约 owner 裁断，本轮不分配批次)。

### R27 · P3（拆了一半放弃的迁移脚手架，空占位包） · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准 — core/services/scheduler/calendar/__init__.py(0 字节，目录仅含 __init__.py)+ batch/__init__.py(0 字节;pack 称 1 字节，实测 0 字节微差)。同 V22 簇还有 dispatch/__init__.py(0 字节，=R06 B09)+ gantt/__init__.py(0 字节，无独立债 id)。SP05 钉死：tests/test_sp05_path_topology_contract.py:310 存在性元组 ('config','run','summary','batch','dispatch','gantt','calendar') + :315-316 `for delayed_package in ('batch','dispatch','gantt','calendar'): _assert_init_has_no_imports(...)`(helper 在 :173 断言 __init__ 无任何 import)。生产零消费已核：external_groups.py:70/:121 `scheduler.calendar.add_calendar_days` 的 scheduler 是函数形参(:15 `scheduler: Any`，注入的日历服务对象，其 .calendar 属性是 calendar_service/calendar_engine)，非本空包;gantt_service_support.py:75 `scope='scheduler.gantt'` 是字符串字面量非 import;test_greedy_refactor_contracts.py:81 `assert 'scheduler.calendar' not in text` 是反向断言(禁止 dispatch 算法耦合)非依赖。真实日历/批次逻辑在顶层 calendar_engine.py/calendar_service.py/batch_service.py(18+ 活跃 importer)。
- **具体修法**：直删，V22 同提交两件事缺一不可。(1) 删 4 个空包目录 core/services/scheduler/{batch,calendar,dispatch,gantt}/(每个仅一个空 __init__.py)；(2) 改 tests/test_sp05_path_topology_contract.py——从 :310 存在性元组移除 batch/calendar/dispatch/gantt(留 'config','run','summary')，并删除整个 :315-316 delayed-package 循环块(4 包全删后该循环无对象，删块比留空元组更干净)。跨桶联合：R27(本桶)占 calendar+batch，R06(B09)占 dispatch，gantt 无独立 id 但 V22 扫全 4 包→必须与 R06 同一提交。若团队日后真要分包，届时重建包目录即可，不必现在留空壳。
- **爆炸半径**：生产零影响(零运行时消费已证)。触及：4 个删除目录 + tests/test_sp05_path_topology_contract.py。真实 calendar/batch 逻辑留顶层文件(18+ 活跃 importer 不动)。无 roadmap 把它们当待下沉预留位(.limcode/plans/.../05.plan.md:226,230-233 明确批次逻辑保留顶层)。
- **跨债依赖**：硬 V22 同提交与 R06(B09，dispatch/__init__)——Phase0 §2 note + Phase1 边 R06→R27『同文件同改』+ bucket pack co_change_within_group:[R06]。4 空包 + SP05:310/315 必须一个提交落地；删 calendar/batch(R27)而漏 dispatch(R06)+gantt 或反之，都留 SP05 红。故 R27 不能单独落地，须并入 R06 的批次。Phase1 Batch-10(B09+R27)已归同批。
- **必补回归测试**：无需新增。验收：SP05 路径拓扑契约在存在性元组 + delayed-loop 改后保持绿；已 grep 确认无其他测试把这 4 个当包 import；确认 `import core.services.scheduler` 与整套测试仍能 collect(无命名空间包意外)。
- **误修风险与防呆**：(1) 半删陷阱：LLM 删 calendar+batch 却漏 dispatch/gantt 或漏 SP05 改→SP05 红。防呆：V22 单提交 checklist(4 目录 + 2 个 SP05 站点)。(2) LLM 见 SP05 红而『修复』重建空包→债复活。(3) 误把 external_groups.py:70(注入形参)/greedy_refactor:81(反向断言)/gantt_service_support:75(scope 串)当真消费方→假阻塞。防呆：已记录三者均非包 import。(4) 纯目录拓扑，不碰任何安全不变量/灵魂线，无过度防御风险。
- **落地批次/优先级**：Batch-10(B09 + R27 图死版+空包)，必须与 R06 V22 同提交。独立于 B01 根；早于 B13 facade 删除(§10.2)。Low 优先级但被 R06 配对门控。

**本单元待 owner 裁断**：
- R03 下游 FAILED 脚手架(failed_candidate_count / baseline_missing_or_failed 一路传到 web/viewmodels/dashboard_workbench.py:231 的 if 分支 + scheduler_analysis_candidate_helpers.py:324-329 的恒-0/恒-False 展示分支)：到底是『为持久化 schema enum(core/infrastructure/migrations/v10.py:14 CHECK status IN completed/failed/skipped/not_run)保留的防御性契约面』(留着，仅补不可达注释)还是『纯不可达死脚手架』(连同枚举面一起收敛)？Phase0 §6 标 needs_adversarial 但 verdict 数组无独立结论。我的倾向：保留枚举面(DB CHECK 是硬底，删 CANDIDATE_STATUS_FAILED/_failed_plan 会破往返)，但是否给 viewmodel 恒-False 分支补一句『生产不可达：仅持久化 schema enum 往返时可能为真』observability 注释、还是完全不动，需 owner 拍板。
- R03 的 runner.py:216 窄 except『我是故意的』注释文案不在 90 节预起草清单内(R03 不在 8 条 LB 名单，但 Phase1 §1 明确把它列为 verdict-confirmed 承重护栏)。我已按 90 节风格起草(见 fix 字段)，需 owner 确认文案口径是否纳入 Batch-1 承重注释网统一验收，避免与已起草的 LB01-LB08 文案风格不一致。
- R27/R06 的 gantt 空包归属：V22 扫 4 个 delayed 包(batch/calendar/dispatch/gantt)，但 R27=calendar+batch、R06=dispatch，gantt/__init__.py 无独立债 id。删除时 gantt 空包记到谁的提交账？建议并入 R06+R27 的 V22 联合提交并在 Phase3 排序里显式点名 gantt，否则极易在分批时漏删 gantt 导致 SP05:315 delayed-loop 仍引用已删的其他 3 包(或反之)而红。
