# B12 · data/repositories死方法

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：B12 是 data/repositories 死方法族(仓库根 /Users/lurenxing/Documents/GitHub/----/，下列 file:line 沿用 PHASE0/PHASE1 的仓库相对路径以便 Phase3 直接对账）。6 条全部 load_bearing=false、全落在『非承重』repo 文件、cross_bucket_same_file_edges=[]（无跨桶同文件）。病理分两类：(A) 5 条纯死直删 R35/R36/R37/R38/R39（P6,severity=low,grep 已证全仓零引用零测试零 facade 暴露,blast_radius=0)；(B) 1 条 plan-role 迁移残渣 R34（P3,medium,needs_adversarial,_adv_verdict=real_debt/refuted=true)——非裸删：活孪生 schedule_plan_query_repo（带 source_table+candidate_id+scenario_id+未知source raise）已全覆盖且更安全，删前须退 3 处 facade get_type_hints 断言 + 6 个 detail_queries 用例 + 1 处无效 monkeypatch + repoint 1 个 benchmark。整体范式=P6 直删 + R34『删+退测试+repoint benchmark』四件套。全程零灵魂线风险（删的都是死码或更不安全的 version-only 盲路径，护栏在活孪生里）、零承重风险（无 LB 同居）、零分层风险（纯删 data 层方法不引入跨层 import）。本桶唯一真正工程风险是『误删命名近似的活方法』，集中在 R37（list_links_with_machine_names 死 vs list_links_with_operator_info 活）与 R34（list_*_with_details / version 系活方法）。Phase1 纠偏已采纳两条：#9 R34 的 :59 monkeypatch 无效（生产实调 list_by_version_with_details）；#10 R38 三处 list_as_dicts 是结构同形非字面同体（SQL 各异），须各自 grep 证零引用、不可当一份删。

**内部排序**：风险升序 + 同文件聚批：R36 → R37 → (R38+R39 同 part_repo.py 子批) → (R34+R35 同 schedule_repo.py 子批，最后)。理由：(1) 先清纯死无依赖的隔离叶子 R36(batch_operation_repo)、R37(operator_machine_repo，紧盯活近亲)——零爆炸半径任意时点可落；(2) part_repo.py 子批 R38(part 份)+R39 同提交，R38 另两份(op_type/operator)随簇；(3) 最后做 schedule_repo.py 子批 R34(三死方法+四件套退场)+R35(list_between) 同提交——R34 是全桶唯一带测试退场与 benchmark repoint 的最高复杂度条目，放最后，且软排在 B07/R05 collar 扩 team 之后。同文件强制同提交：R34↔R35（list_between 夹在三死方法间）、R38↔R39（同 part_repo.py）。

### R34 · P3 plan-role 迁移残渣（版本-only 死孪生）；needs_adv · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准。已 Read 复核 schedule_repo.py:36 get_version_time_span(:36-59)、:114 list_overlapping_with_details(:114-126)、:128 list_dispatch_rows_with_resource_context(:128-158) 三个 def 行号盘上完全一致，文件不在16文件漂移名单。grep 三方法生产零引用已证：裸名 get_version_time_span 仅 benchmark_fjsp.py:503 + facade 断言:31（_dates 变体 get_version_time_span_dates 是另一活方法 gantt_plan_query.py:50，与本债无关）；list_overlapping_with_details 仅 detail_queries:168 + facade:36 + 无效 monkeypatch:59；list_dispatch_rows_with_resource_context 仅 detail_queries:198/218/225/240/247/254/268/274/288 + facade:39。活孪生 schedule_plan_query_repo.py:257/311/430 + 服务 schedule_plan_query_service.py:187-223 已复核存在。
- **具体修法**：删 schedule_repo.py 三死方法（:36-59 / :114-126 / :128-158），活孪生全覆盖且更安全（schedule_plan_query_repo.py:257/311/430 带 source_table+candidate_id+scenario_id+未知source raise ValueError，防候选/情景冒充 adopted）。同步四件套：(1) 退 regression_schedule_service_facade_delegation.py:31(get_version_time_span)/:36(list_overlapping_with_details)/:38-41(list_dispatch_rows_with_resource_context) 三条 get_type_hints 断言，保留 :33-35(list_version_rows_by_op_ids_start_range 活→freeze_window:177)+:37(list_by_version_with_details 活→gantt_critical_chain:68)；同步清理该测试顶部因此变未引用的 import：ScheduleTimeSpanRow(仅:31用)与 ScheduleDispatchRow(仅:38-41用)删，保留 ScheduleSeedRow(:33用)/ScheduleDetailRow(:37用)。(2) 删 test_schedule_repository_detail_queries.py 6 个用例：test_schedule_overlap_detail_query_keeps_row_shape_order_and_overlap_contract(:165) + 5 个 dispatch 用例(:195/:215/:237/:265/:285)；保留 test_schedule_detail_query_for_version_uses_same_common_shape(:185，用活方法 list_by_version_with_details) 及共享夹具 _conn/_seed/_repo/_ids(:42/:105/:155/:161)。(3) 删 regression_gantt_critical_chain_unavailable.py:59 那行无效 monkeypatch（stub list_overlapping_with_details→[] 但生产不调它，由 :60 stub 真被调的 list_by_version_with_details→raise 反证降级），严格保留 :60 及 :70-78 降级断言。(4) repoint benchmark_fjsp.py:502-503：把 ScheduleRepository(conn).get_version_time_span(version) 改走活孪生 SchedulePlanQueryService(...).get_plan_time_span(version, ROLE_ADOPTED)（返回同 ScheduleTimeSpanRow 形 {version,start_time,end_time}，:506-507 的 span['start_time']/['end_time'] 访问不变）。
- **爆炸半径**：生产零影响（活孪生全覆盖且更安全）。须退/改：facade_delegation 3 条类型断言 + 2 处未引用 import；detail_queries 6 用例（保留 1）；gantt_critical_chain_unavailable:59 无效 monkeypatch；benchmark_fjsp:503 repoint。无 SP05 拓扑、无 __all__/facade（schedule_service.py 与 data/repositories/__init__.py grep 确认零委托）。
- **跨债依赖**：【同文件同改·硬】R35（list_between:61 夹在三死方法之间，同 schedule_repo.py）——Phase1 边 R34→R35，必须同提交避免删后行号二次漂移撞 facade delegation 断言。【前置·软约束】Phase1 DAG 把 B07(R05 collar 扩 team 双join+空id全量) 列于 Batch-8、本桶 Batch-13 之前。⚠️该 R05→R34 边原措辞假设 R34『收敛到 column_name』，但 R34 实为纯删死方法：被删的 schedule_repo.list_dispatch_rows_with_resource_context 已含 team 双join(:151-153) 且零生产引用，删它不碰活孪生 schedule_plan_query_repo.list_dispatch_rows(:430,:447-463 自带 team 过滤,R05 真正作用对象)。故『team 谓词静默丢』失效模式对纯删除不成立——B07→B12 是零成本保险非硬依赖（见 open_questions）。【资源筛选族】R55(B08,gantt_service.py)/R05/R67 同属 ScheduleResourceFilter 收口族但非同文件，无同改，仅 Phase3 族协调留意。
- **必补回归测试**：删后须绿：regression_schedule_service_facade_delegation.py 余 4 测试（constructor / dict_rows 余 2 断言 / bundle_aliases / main 委托）全绿；test_schedule_repository_detail_queries.py 仅余 test_schedule_detail_query_for_version_uses_same_common_shape 绿；regression_gantt_critical_chain_unavailable.py 降级契约(:70-78 available=False / reason_code=repo_exception / degradation_events 含 critical_chain_unavailable) 绿——证删 :59 不改降级行为；benchmark_fjsp.py repoint 后 main() 跑通 schedule_span 不报错。全仓 grep 三死方法名=0。纯删残渣无需新增测试；可选加固=补『三死名 not in dir(ScheduleRepository)』负向 introspection 防复活（见 open_questions）。
- **误修风险与防呆**：①最高危=误删活方法：list_by_version_with_details(:160,活→gantt_critical_chain:68) 与 list_version_rows_by_op_ids_start_range(:71,活→freeze_window:177) 与三死方法同文件且命名近似（都是 list_*_with_details / version），未来 LLM 极易『一起清理』连坐删活方法→甘特关键链/冻结窗口 500。防呆：facade_delegation :33/:37 两条保留断言即活方法护栏，删 PR 里这两断言必须仍在且绿。②误把 :59 无效 monkeypatch 当有效续命而保留三死方法（报告原判已被 Phase1 纠偏#9 推翻）。③benchmark repoint 用错 API：get_plan_time_span_for_resolution 是 keyword-only 且要 source_table，位置传 ROLE_ADOPTED 会 TypeError——须用 get_plan_time_span(version, role)。④误判『保留通用读 API 隐性意图』：已被 _adv_evidence 证伪（facade 测试是 dict 行命名返回类型契约，非保留这 3 个 API；roadmap 无重新接线计划）。
- **落地批次/优先级**：Batch-13（B12 repo 死方法），与 R35 同 schedule_repo.py 子批同提交。本桶内最高复杂度（唯一 P3+needs_adversarial，带四件套退场），medium。软排在 B07/R05 之后（零成本保险）。建议本桶最后做：其余 5 条纯死先清、确认无连带后再动这条带测试退场+benchmark repoint 的。

### R35 · P6 纯死方法（全仓零引用含测试） · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准。已 Read schedule_repo.py:61 list_between(start_time,end_time,version=None)→List[Schedule] 通用区间查询(:61-69)，行号盘上一致。grep 全仓仅 :61 def 行，零调用零测试零模板。文件不在漂移名单。
- **具体修法**：直删 schedule_repo.py:61-69 整个 list_between 方法。无任何引用点需同步退场（grep 已证零调用零测试）。
- **爆炸半径**：零。无生产消费、无测试、无 facade 暴露，不在 SP05 拓扑/facade_delegation 枚举内。
- **跨债依赖**：【同文件同改·硬】与 R34 同 schedule_repo.py（R35 :61 夹在 R34 三死方法 :36/:114/:128 之间）。Phase1 边 R34↔R35：必须同提交，避免任一先删后行号二次漂移。无其他依赖。
- **必补回归测试**：删后须绿：所有 import schedule_repo 的测试不受影响（无人引用 list_between）；facade_delegation 全绿（不枚举 list_between）。无需新增测试。
- **误修风险与防呆**：①误删近似命名的活方法：同文件 list_by_machine(:170,活)/list_by_version(:29,活)/list_version_rows_by_op_ids_start_range(:71,活) 命名都含 list_/version，删时须精确锁定 list_between 单一方法体(:61-69)。②其余几乎无风险——纯死叶子。
- **落地批次/优先级**：Batch-13，与 R34 同 schedule_repo.py 子批同提交。优先级 low。本身无依赖可任意排期，因同文件被 R34 绑定到同批。

### R36 · P6 两个定义后零消费的死查询方法 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准。已 Read batch_operation_repo.py:25 get_by_op_code(op_code)→Optional[BatchOperation](:25-35)、:50 list_by_status(status)→List[BatchOperation](:50-61) 两 def 行号盘上一致。grep 全仓两名各仅 def 行，零调用（含测试/模板/动态 getattr）。BatchOperationRepository 实际被调=get(:13)/list_by_batch(:37)/create/update/delete/delete_by_batch，不含这两个。文件不在漂移名单。
- **具体修法**：直删 batch_operation_repo.py:25-35（get_by_op_code 整方法）+ :50-61（list_by_status 整方法）两段，一并删。op_code 虽是 schema UNIQUE 业务键但从未接线按 code 取行；list_by_status 无人按状态批量取批次工序。
- **爆炸半径**：零。无生产/测试/模板/getattr 引用；facade_delegation 与 SP05 均不枚举这两方法。
- **跨债依赖**：无。batch_operation_repo.py 仅此一债，co_change_within_group=[]。可任意排期。
- **必补回归测试**：删后须绿：任何实例化 BatchOperationRepository 的批次/调度测试不受影响（无人调这两方法）。无需新增测试。
- **误修风险与防呆**：①误删活方法：同文件 get(:13,活)/list_by_batch(:37,活)/create/update/delete/delete_by_batch 都活，删时须精确锁 get_by_op_code(:25-35) 与 list_by_status(:50-61)，勿连坐 get(:13)（与 get_by_op_code 仅差 SQL where）。②get_by_op_code 名字像通用主键查询，未来真要按 op_code 取行须重新实现——当下零引用即死债，删除正确（无现存 roadmap 计划）。
- **落地批次/优先级**：Batch-13，纯死无跨边可任意排期（本桶最早可落之一，零风险）。优先级 low。

### R37 · P6 被 facade 兄弟方法淹没的死查询 · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准。已 Read operator_machine_repo.py:82 list_links_with_machine_names()→List[Dict](:82-90) 行号盘上一致；活近亲 list_links_with_operator_info 在 :92。grep 全仓 list_links_with_machine_names 仅 :82 def 行，零调用；活近亲则 operator_machine_query_service.py:104/105→equipment_pages.py:142 + tests/test_query_services.py:194/222 多处活引用。其 query facade operator_machine_query_service.py 代理了 list_simple_rows/list_with_names_by_machine/list_with_names_by_operator/list_links_with_operator_info/list_simple_rows_for_machine_operator_sets，唯独漏暴露 list_links_with_machine_names（=死的旁证）。文件不在漂移名单。
- **具体修法**：直删 operator_machine_repo.py:82-90 整个 list_links_with_machine_names 方法。零引用点需同步退场。
- **爆炸半径**：零。无生产/测试引用；facade 未暴露；SP05/facade_delegation 不枚举。
- **跨债依赖**：无。operator_machine_repo.py 仅此一债，co_change=[]。可任意排期。
- **必补回归测试**：删后须绿：test_query_services.py 全绿（它测的是活近亲 list_links_with_operator_info:194/222，与本方法无关）；equipment_pages 设备页相关回归绿。无需新增测试。
- **误修风险与防呆**：★本桶最高误删风险：活近亲 list_links_with_operator_info(:92) 与死方法 list_links_with_machine_names(:82) 命名高度对称（machine_names vs operator_info），仅差后缀、相邻 10 行。未来 LLM『清理重复 list_links_*』极易删错那个或两个都删→operator_machine_query_service:105 / equipment_pages:142 设备页 AttributeError/500。防呆：删 PR 必须显式保留 :92 list_links_with_operator_info 且 test_query_services:194/222 仍绿；删除范围严格锁 :82-90 单方法体，grep 确认删的是 machine_names 后缀那个。
- **落地批次/优先级**：Batch-13，纯死无跨边可任意排期。优先级 low。因高误删风险，建议单独成 commit 或紧盯 diff 行范围（仅 :82-90）。

### R38 · P6 结构同形死簇（三处各自零消费） · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准但 dup 认定须修正（Phase1 纠偏#10）。已 Read 三处：op_type_repo.py:73 list_as_dicts(:73-74)、operator_repo.py:85(:85-86)、part_repo.py:71(:71-72) 行号盘上一致。codemap dup_bodies 簇 0bd22fccb1ac5d3b 标『同体(逐字)』不精确——三处 SQL 各异：OpTypes『SELECT op_type_id,name,category … ORDER BY name』/ Operators『SELECT operator_id,name,status,remark,team_id … ORDER BY operator_id』/ Parts『SELECT part_no,part_name,route_raw … ORDER BY part_no』，是『结构同形（单行 fetchall 返回 dict 列表）』非字面同体。grep 全仓 list_as_dicts（含 templates/web 字符串）= 仅三条 def 行，零调用。三文件均不在漂移名单。
- **具体修法**：分别直删三处 list_as_dicts：op_type_repo.py:73-74、operator_repo.py:85-86、part_repo.py:71-72。⚠️因三处 SQL 不同（非一份逐字副本），每处须独立按各自 grep 证零引用后删，不可当『删一份代表三份』；三处恰好都零引用，故可同批一并删，但记账上是三次独立删除。绝不可为『消重』抽公共 helper（=新建零消费活代码，违收口点规则）。
- **爆炸半径**：零。三处均无生产/测试/模板引用；无 facade 暴露；SP05/facade_delegation 不枚举。
- **跨债依赖**：【同文件同改·硬】part_repo.py 的 list_as_dicts(:71) 与 R39 的 list_unparsed(:32) 同文件——R38 删 part 份须与 R39 同批改 part_repo.py 避免行号二次漂移（Phase1 边 R38↔R39）。op_type_repo/operator_repo 两份无其他同文件债，随 R38 簇走。
- **必补回归测试**：删后须绿：任何 import 这三 repo 的零件/页面/算法测试不受影响（无人调 list_as_dicts）。无需新增测试。
- **误修风险与防呆**：①勿把『同体簇』当一份删：三处 SQL 列集/表/排序各异，误以为逐字相同而抽公共 helper『消重』=反而造一个零消费的活模块（违收口点规则，为零概念建模块）；正解=三处各自直删。②误删活方法：三 repo 各有 list()/get()/create 等活方法，删时精确锁 list_as_dicts 单方法。③part_repo 那份与 R39 行号联动，须同提交。
- **落地批次/优先级**：Batch-13，与 R39 同 part_repo.py 子批同提交；op_type/operator 两份随簇。优先级 low。无跨边可任意排期。

### R39 · P6 纯死方法（零消费含测试） · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准。已 Read part_repo.py:32 list_unparsed()→List[Part]（内部转调 self.list(route_parsed='no')，:32-33）行号盘上一致。grep 全仓 list_unparsed 仅 :32 def 行，零调用含测试。文件不在漂移名单。
- **具体修法**：直删 part_repo.py:32-33 整个 list_unparsed 方法。其内部转调的 self.list(route_parsed='no') 走的是活方法 list(:20)，不受影响。零引用点需同步退场。
- **爆炸半径**：零。无生产/测试引用；无 facade 暴露；不在 SP05/facade_delegation 枚举。
- **跨债依赖**：【同文件同改·硬】与 R38 的 part_repo.list_as_dicts(:71) 同 part_repo.py——同批改避免行号二次漂移（Phase1 边 R38↔R39）。无其他依赖。
- **必补回归测试**：删后须绿：零件相关测试不受影响（无人调 list_unparsed）；活方法 list(:20) / list(route_parsed=…) 仍被各处正常调用。无需新增测试。
- **误修风险与防呆**：①勿误删它转调的活方法 list(:20)——list_unparsed 只是 list 的一个 route_parsed='no' 死薄封装，删封装不删被封装的 list。②同文件 R38 list_as_dicts(:71) 联动行号，须同提交。风险极低（纯死叶子）。
- **落地批次/优先级**：Batch-13，与 R38 part 份同 part_repo.py 子批同提交。优先级 low。无跨边可任意排期。

**本单元待 owner 裁断**：
- R34 benchmark repoint 的 API 选择：precondition 文案写 get_plan_time_span_for_resolution(version, ROLE_ADOPTED)，但该方法 keyword-only 且要 source_table/candidate_id，位置传 ROLE_ADOPTED 会 TypeError。建议改用角色入口 SchedulePlanQueryService.get_plan_time_span(version, ROLE_ADOPTED)（返回同 ScheduleTimeSpanRow 形）。需 owner 确认：benchmark 是否接受引入 SchedulePlanQueryService 依赖（当前只 import ScheduleRepository），还是就地保留最小 version-only span 查询（若保留则 get_version_time_span 不能全删=债残留）。倾向 repoint 到 service 彻底删。
- R05→R34 排序边是否仍为硬依赖：Phase1 DAG 把 B07(R05 collar) 排在 B12(R34) 之前，理由是『R34 收敛到 column_name 若先于 R05 扩 team 会丢谓词』。但实证 R34 是纯删死方法（被删的 schedule_repo.list_dispatch_rows_with_resource_context 零生产引用、活孪生 schedule_plan_query_repo.list_dispatch_rows 自带 team 过滤），删它不触碰 R05 作用对象，故该边对纯删除失效。需 Phase3 owner 裁断：是否解绑此边让 B12 更早落（本桶 6 条全是死码，理论上 Batch-1 之后任意时点可清）。
- R34 是否需补『公开方法集负向断言』防复活：当前计划纯删+退断言不新增测试。R34 是 plan-role 安全相关（version-only 盲方法会让候选/情景方案冒充 adopted），是否值得加一条 assert 三死名 not in dir(ScheduleRepository) 的反向护栏防未来 LLM 复活同名盲方法？非必须，需 owner 定。
