# B03 · config双栈 + adapter死壳

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：B03=config 双栈(model↔service)收敛桶,5 条债分三类:① LB07 承重本体(双 ScheduleConfigSnapshot 栈,27业务+graph_downstream+2 degradation=30 注解锁步,只补注释+扩 parity 测试,零删除);② R71/R47 双栈对称的函数级配料表(R71=3 个逐字 helper,R47=双栈对称死参数,均落在 LB07 承重文件上);③ R45/R48 同一物理文件 config_adapter.py(27行死壳整删,与双栈正交)。

【盘上复核结论】B03 全部生产文件【不在】PHASE0 §0 的 16 个工作树漂移名单(git status 确认 clean),提交态≡基线 b08162cd,报告行号基本全准。三处微纠偏:(a) R48 报告称"28行",实测 27 行(off-by-one);(b) R47 报告 why_debt 称"服务侧孪生没这参数"是错的——已盘上证实 service config_field_coercion.py:63/:68 同样声明同样死同样传入(Phase1 纠偏#4 成立),双栈对称;(c) bucket_pack 列的 cross_bucket 边 config_snapshot.py(R26↔R71)是【basename 假碰撞】——R26 是顶层 shim core/services/scheduler/config_snapshot.py(5行re-export,B13领域),R71 在深层 core/services/scheduler/config/config_snapshot.py:153,两个不同物理文件,B03 与 B13 在此处【无真实同文件冲突】。

【内部排序(强约束)】
1. LB07 必须最先落(进 Batch-1 全局 ROOT):两栈 snapshot 类各补"我是故意双栈"注释(复用 90节 LB-B2 文案)+ 把 regression_scheduler_config_spec_sync_contract.py 从"只 pin spec 字段"扩成覆盖 R71 三 helper 行为等价的 parity 契约。这是 R71/R47 收敛的硬前置(parity 先于收敛,否则锁步守卫缺位静默漂移踩灵魂线)。
2. R45+R48 合并为一次 config_adapter.py 整文件删除 + 退 sp06 NO_CFG_GET_TARGETS 路径(:15)。与双栈正交、零 LB 耦合,Batch-1 之后任意时点可落(最早可独立完成),建议并入 Batch-5。
3. R71 收敛(若 owner 批准物理收敛=service 反向复用 model 删 service 侧 3 副本)在 Batch-1 parity 扩展【之后】;受 LB07 承重约束,方向须经整桶裁决(见 open_questions)。
4. R47 双栈对称同删死参数 + 改 4 处调用点,与 R71 同节奏(同在 coercion 双栈文件,Batch-5),承重注释先落。

【灵魂线/承重防呆铁律】收敛全程绝不能碰:model coercion.py:470 graph_downstream_weight 承重置零、:72 MISSING_POLICY_ERROR raise、:153 strict blank loud raise;service config_snapshot.py:89 _graph_downstream_weight_for_visible_weights;read.py:10 read_runtime_cfg_raw_value 承重读取入口。R47 删参数时绝不能把 collector.add(code=blank_required) 这条【有意可观测降级】一起删掉(那会把 loud 降级变静默,踩灵魂线)。

**内部排序**：LB07(Batch-1,承重注释+扩parity,ROOT) → [R45+R48 独立可早,Batch-5] → R71(Batch-5,parity后,owner裁决收敛方向) → R47(Batch-5,与R71同节奏,承重注释后)。硬先后:LB07 parity 扩展 必须先于 R71/R47 任何物理收敛;R47 两栈必须对称同删(只删 model 侧会加大分叉,与 R71/LB07 收敛方向相悖)。

### LB07 · P3 / 承重(load_bearing=true)。同一概念(运行期配置快照+ · sev=medium · load_bearing=True

- **当前位置(已复核)**：✅准。model 栈 core/models/schedule_config_runtime_snapshot.py:8 class ScheduleConfigSnapshot(30 注解:27业务+graph_downstream_weight+degradation_events+degradation_counters);service 栈 core/services/scheduler/config/config_snapshot.py:25 同 30 注解,字段集与默认值当前 parity 成立、零漂移(逐行核对一致)。整组 5 文件齐全(snapshot/coercion/fields/read/weights)。ensure_schedule_config_snapshot model 在 coercion.py:409,service 孪生在 config/config_snapshot.py:336。B03 全部文件【不在】16 工作树漂移名单(git status clean)→提交态≡b08162cd,行号准。grep '故意/刻意/双栈/parity' 在四个核心文件 ZERO 命中→承重注释确实仍缺(失忆债活样本)。
- **具体修法**：【红线2:零删除/统一/透传,只补注释+绑契约】(1) 把 90节 LB-B2 起草文案逐字补到【两栈 snapshot 类定义上方】(model schedule_config_runtime_snapshot.py:7 @dataclass 上方 + service config/config_snapshot.py:24 @dataclass 上方),文案:'⚠️ 本 dataclass 与 <对侧路径> 的 ScheduleConfigSnapshot 必须逐字段同步。这是分层(算法层不能 import 服务层)被迫的双实现,非随意复制。加/改字段必须两栈同改,否则算法用旧默认值、配置页存新值,静默分叉污染排产正确性。收敛前先补 parity 契约测试。'(2) 扩 parity 契约:把 regression_scheduler_config_spec_sync_contract.py(现仅 pin field_type/default/min_value/min_inclusive/choices)扩成【同时覆盖 R71 三 helper 行为等价】(见 R71 断言点),并把 regression_config_snapshot_projection_sync.py 的字段注解去重守卫(:89-101)继续作为字段集锁。绝不动:model coercion.py:470 graph_downstream 承重置零、:72/:153 loud raise;service config/config_snapshot.py:89 _graph_downstream_weight_for_visible_weights;read.py:10 承重读取入口。
- **爆炸半径**：承重本体=动它即动整条排产配置正确性链。生产消费方:model 栈 greedy/schedule_params.py:98/124/138/108/110(算法侧活);service 栈 run/optimizer_config.py:11/131、run/schedule_candidate_runner.py:118、summary/schedule_summary.py、config/config_page_save_service.py:73(配置页保存)、config_bootstrap_service.py:34、config_validator.py:271 等十余处。测试:regression_config_snapshot_projection_sync.py(字段注解去重+degradation parity:132-153)、regression_scheduler_config_spec_sync_contract.py(spec parity)、regression_scheduler_candidate_py38_contract.py:16-18(两栈文件 py38 AST 门禁)、test_sp05_path_topology_contract.py:22/35(顶层 config_snapshot shim→config.config_snapshot 映射+符号白名单)。
- **跨债依赖**：本桶根:必须先于 R71、R47 的任何物理收敛(parity 先于收敛,Phase1 边 LB07→R71、LB07→R47)。与 R45/R48 正交(adapter 只 import FROM model 的 ensure_schedule_config_snapshot,该符号经 schedule_params.py:98 仍活,删 adapter 不动 LB07)。cross_bucket 边 config_snapshot.py(R26/B13↔R71/B03)经盘上复核=basename 假碰撞(R26 是顶层 shim 文件,R71 在深层 config/ 目录),B03 与 B13 在此【无真实同文件冲突】。
- **必补回归测试**：扩 regression_scheduler_config_spec_sync_contract.py 断言两栈三 helper 行为等价(见 R71);新增/保留 projection_sync 字段集锁。验收:projection_sync 绿、spec-sync 扩展后绿、py38 contract 绿、SP05 拓扑绿;git diff 仅注释+测试,零生产逻辑改动。
- **误修风险与防呆**：未来 LLM 最可能:看到两份近 500 行实现,以'消除重复'名义删一栈或把 model 改指 service(立刻造 core.models→core.services 越层+导入环,击穿 AST 0 违规);或给配置页 service 栈加字段忘同步 model 栈(静默分叉)。本地阻止:两栈 snapshot 上方的'我是故意双栈'注释(无记忆协作者唯一有效本地信号)+ spec-sync/projection-sync parity 测试(单边改字段集即红)。绝不能以统一名义抹掉双栈不对称。
- **落地批次/优先级**：Batch-1(全局 ROOT,纯增量零结构)。最高优先,必须最先落——它是 R71/R47 收敛的安全网与锁步守卫前置。

### R71 · P3,load_bearing=false,related=LB07(LB-B2 · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准(逐字两份,diff 三对均 IDENTICAL)。_float_matches_choice: model coercion.py:31 ↔ service config_field_coercion.py:45;_normalize_valid_texts: model coercion.py:49 ↔ service config_field_coercion.py:29;_coerce_degradation_event: model schedule_config_runtime_read.py:68 ↔ service config/config_snapshot.py:153。报告行号全部回盘命中。grep 确认:无任何 tests/ 按名引用这 3 个 helper(spec-sync 只 pin spec 字段)→守卫缺口实锤。service _coerce_degradation_event 仅被本文件 :185 _seed_snapshot_degradation_collector 内部调用。
- **具体修法**：【需先扩收口点(parity),收敛方向须整桶裁决】(1) 前置(Batch-1):扩 regression_scheduler_config_spec_sync_contract.py,新增断言两栈三 helper 行为等价——_float_matches_choice(对 choices×value 真值表一致)、_normalize_valid_texts(对含空白/大小写/重复样本输出 tuple 一致)、_coerce_degradation_event(对合法/缺字段/坏 count 的 dict 输出 DegradationEvent 一致)。(2) 收敛(Batch-5,若 owner 批准物理收敛):service 反向复用 model 栈(core.services→core.models 合法下行边),即 service 侧 import model 的三 helper 并删 service 逐字副本;收口点=已存在的 model 栈(§4 config双栈 model 为源),严禁新建第二模块。注意三 helper 为 _ 私有,物理收敛需接受跨模块 import 私有符号(代码味)或将其提升为两栈可见——此点需 owner 裁断(见 open_questions)。绝不碰 coercion.py:470/:72/:153 承重段、read.py:10 读取入口。
- **爆炸半径**：同 LB07。生产消费方:model coercion.py:46/135/256(三 helper 被调)、read.py:100(_coerce_degradation_event);service config_field_coercion.py:60/139/253、config/config_snapshot.py:185。测试:projection_sync:132-153(构造带 degradation_events 快照间接走 _coerce_degradation_event)、spec-sync(现存最近 parity 但有守卫缺口)、py38 contract:16/18(纳门禁不校验等价)。
- **跨债依赖**：Phase1 边:LB07→R71(parity 先于收敛,硬先后:LB07 承重注释+helper-parity 测试必须先落)、R71→R47(同文件同改,R47 死参数在 coercion 双栈对称,须与 R71 同节奏对称同删)、co_change_within_group=[LB07,R47]。cross_bucket config_snapshot.py(R26/B13)经核=假碰撞(R26 在顶层 shim 文件,非 R71 的深层 config/config_snapshot.py),无真实冲突。
- **必补回归测试**：扩展后的 spec-sync 三 helper 等价断言(收敛后两栈对同一输入仍同值,或收敛为单源后该断言退化为'service delegate 到 model');projection_sync degradation 保留绿。验收:收敛不改任何降级/默认值行为,parity 绿。
- **误修风险与防呆**：未来 LLM 改一栈 helper 忘另一栈(当前 spec-sync 守不住,改 helper 体不变红=静默漂移)。本地阻止:Batch-1 扩展的三 helper parity 断言(任一单边改即红)。若误把收敛做成'model 改指 service'=越层击穿。若先收敛后补 parity=锁步守卫缺位期静默漂移踩灵魂线——必须 parity 先行。
- **落地批次/优先级**：Batch-5(B03 config 双栈收敛)。前置 parity 在 Batch-1。物理收敛与否=open_question(owner 裁决:鉴于 LB07 load_bearing+helper trivial,'仅 parity 锁定不物理收敛'可能已足够)。

### R47 · P6 / trivial,load_bearing=false。签名上的双栈对称 · sev=trivial · load_bearing=False

- **当前位置(已复核)**：✅准 + Phase1 纠偏#4 确认双栈对称(推翻报告'服务侧没这参数')。model: def _record_blank_choice_degradation coercion.py:83,死参数 raw_value 在 :88,函数体 :91-98 只 collector.add(code=blank_required) 用 field_name/fallback,从不读 raw_value(已逐行核);两调用点 :155、:210 仍传 raw_value=raw_value。service 孪生: def config_field_coercion.py:63,raw_value:68 同样死(体 :71-78 不读);调用点 :158、:207。【防呆已核】同文件兄弟 _record_invalid_choice_degradation(model:101/service:81)真在用 raw_value(model:116/:119,sample=str(raw_value or ''))——绝不能误删它的参数。ZERO 测试按名引用此 helper→无测试需退。
- **具体修法**：【直删·两栈对称同改·共4处调用点】删 raw_value 形参 2 处(model coercion.py:88、service config_field_coercion.py:68)+ 改调用点 4 处(model :155、:210;service :158、:207,各去掉 raw_value=raw_value 实参)。两栈必须对称同删(只删 model 侧会加大 model↔service 逐字分叉,与 R71/LB07 收敛方向相悖、反造新锁步差)。保留函数体内 collector.add(code=blank_required) 整条降级记录不动。承重防呆:只动 :88 形参与 :155/:210(model)、:68 与 :158/:207(service);绝不碰 coercion.py:470 graph_downstream 置零、:72/:153 loud raise、:73-80 MISSING_POLICY_ERROR raise。
- **爆炸半径**：无运行期行为变化(blast=无):删形参+改实参不改任何消息/降级结果。生产消费=上述 4 个调用点,全在 coercion 双栈内,无外部调用方。测试:零(无测试构造/引用该 helper)。
- **跨债依赖**：Phase1 边:R71→R47(同文件同改,双栈对称同删)、LB07→R47(承重先于动同文件,承重注释先落)。co_change_within_group=[LB07,R71]。须与 R71 同批(Batch-5)、承重注释(Batch-1)之后。
- **必补回归测试**：无需新增专门测试(纯死参数)。验收:删后 ensure/coerce 路径行为不变,blank 字段仍记 code=blank_required 降级(可加一条小断言:空字符串 enum/yes_no 字段在非 strict 下仍产生 blank_required 降级事件,守住灵魂线);R71 扩展的两栈 helper 签名 parity 顺带锁住'两侧同删'。
- **误修风险与防呆**：未来 LLM 最可能:(1) 只删 model 侧忘 service 孪生(报告本身就漏报 service 侧)→单栈分叉;(2) 删参数时顺手把 collector.add 降级记录一起删→静默吞掉 blank 降级踩灵魂线;(3) 误把在用的 _record_invalid_choice_degradation 的 raw_value 也删掉→消息丢当前值。本地阻止:R71 扩展的 helper 签名 parity 断言(两栈不对称即红)+ 灵魂线 blank_required 降级断言。
- **落地批次/优先级**：Batch-5(随 R71 双栈收敛同批,两栈对称同删)。承重注释(Batch-1)之后。

### R45 · P6,load_bearing=false。死壳:包在本分区收口点 ensure · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准(符号行号命中)。core/algorithms/greedy/config_adapter.py:10 class CriticalConfigReadResult、:16 def read_schedule_config_value、:26 def read_critical_schedule_config(薄封装 ensure_schedule_config_snapshot+getattr+吞异常成 .error 字段)。⚠️全文件实测 27 行(报告/R48 称 28,off-by-one)。grep 全仓:三符号【生产零引用】,唯一引用=tests/regression_sp06_no_duplicate_defs.py:15 把文件路径列入 NO_CFG_GET_TARGETS(仅 AST 扫描路径,不 import 任何符号)。greedy/__init__.py 无 re-export。
- **具体修法**：【删+退测试·与 R48 合并为一次整文件删除】删除 core/algorithms/greedy/config_adapter.py 整文件(R45 死壳视角与 R48 迁移残渣视角是同一物理文件,合并处理)+ 同一提交从 tests/regression_sp06_no_duplicate_defs.py:15 的 NO_CFG_GET_TARGETS 列表移除该路径(否则文件删后 path.read_text 抛 FileNotFoundError,测试红)。前置自证:grep 三符号生产零引用(已核)。
- **爆炸半径**：无运行期影响——纯死代码。生产零引用(已 grep 证实)。唯一测试牵连=sp06:15 路径成员(同提交退)。SP05 拓扑【不】映射 config_adapter(已核 test_sp05_path_topology_contract.py 只映射 config_snapshot 系列)→删此文件不撞 SP05 契约。
- **跨债依赖**：Phase1 边:R45→R48(同一物理文件的两种叙述,合并为一次整文件删除)、co_change_within_group=[R48]。与 LB07/R71/R47 双栈收敛【正交】(adapter 只 import FROM model 的 ensure_schedule_config_snapshot,该符号经 schedule_params.py:98 仍活,删壳不动双栈本体、不引入跨层 import)。最早可独立落(Batch-1 之后任意时点)。
- **必补回归测试**：无需新增。验收:删文件 + 退 sp06 NO_CFG_GET_TARGETS:15 同提交;全删后 grep 确认零生产引用;sp06 测试绿(test_sp06_targets_do_not_use_cfg_get_fallback_helpers 不再读该路径);SP05 拓扑绿(本就不含该文件)。
- **误修风险与防呆**：误修风险低。最可能:删文件忘退 sp06:15 路径→测试 FileNotFoundError 红(快速暴露,非静默)。或未来有人误以为这是活收口点而新增调用→反而复活死壳;故删除本身=消除'本分区收口点有此外部用法'假象,正向。防呆:删除与退路径必须同一提交(原子)。
- **落地批次/优先级**：Batch-5(B03,与 R48 合并)。独立最早可落、零 LB 耦合,可视调度提前到 Batch-1 之后任意点。

### R48 · P3,load_bearing=false。整模块迁移残渣:配置校验链路重构(f · sev=low · load_bearing=False

- **当前位置(已复核)**：⚠️行号微纠偏:报告称'全文件28行',实测 core/algorithms/greedy/config_adapter.py 为 27 行(off-by-one)。符号 read_schedule_config_value:16 / read_critical_schedule_config:26 ✅命中。与 R45 为同一物理文件的两种叙述(R45=死壳视角,R48=整模块迁移残渣视角)。替代路径已盘上确认:schedule_params.py:59 def _snapshot_attr,缺快照→:62 raise ValidationError、缺字段→:64 raise、读失败→:68 raise(全 loud,符合灵魂线'缺数据 loud raise 不兜底'),取代旧门面'错误塞进 .error 字段'。生产零引用(同 R45)。
- **具体修法**：【删+退测试·与 R45 同一次整文件删除】见 R45:删除 config_adapter.py 整文件 + 同提交退 tests/regression_sp06_no_duplicate_defs.py:15 路径。R48 与 R45 不是两次动作,是同一文件删除的两个叙述视角,务必合并为单提交,避免重复投工或半删。无 SP05 牵连(已核)。
- **爆炸半径**：无运行期影响:替代路径 schedule_params.py:59 _snapshot_attr 已就位(snapshot 为 None 或缺字段即 raise ValidationError)。删旧门面反而消除'错误塞进结果对象绕过校验'的误用风险。测试牵连仅 sp06:15。
- **跨债依赖**：Phase1 边:R45→R48(同文件同改,合并为一次整文件删除 + 退 sp06 路径)、co_change_within_group=[R45]。与 config 双栈(LB07/R71/R47)收敛正交——删 adapter 不改双栈,新路径 _snapshot_attr 已是收敛后形态。无 SP05 拓扑牵连。
- **必补回归测试**：同 R45:删文件 + 退 sp06 NO_CFG_GET_TARGETS:15 同提交;grep 零生产引用;sp06 绿。可顺带断言替代路径 _snapshot_attr 缺字段 loud raise(守灵魂线)仍在。
- **误修风险与防呆**：误修风险低。最可能:把 R45/R48 当两条债分别投工(实为同文件)→重复或半删;或删文件忘退 sp06 路径→红(快速暴露)。防呆:Phase3 排序须把 R45+R48 锁为单一动作;删除与退路径原子同提交。绝不保留旧门面'吞异常成 .error'风格(与灵魂线'缺数据即抛'相悖)。
- **落地批次/优先级**：Batch-5(与 R45 合并为单次整文件删除)。独立、零 LB 耦合、最早可落。

**本单元待 owner 裁断**：
- R71 物理收敛方向裁决:R71 三 helper 是 LB07(load_bearing=true)双栈锁步的函数级配料表。红线2 规定 load_bearing 只补注释+契约测试。R71 自身 load_bearing=false 但 related=LB07。是否真要物理收敛(service import model 删 service 副本)?物理收敛需跨模块 import model 的 _ 私有 helper(代码味),或将其提升为两栈可见。【建议】鉴于 helper trivial 且 LB07 双栈是分层强制,最稳妥可能是'仅扩 parity 测试把三 helper 锁成两栈等价、不做物理收敛'(双栈保留但守卫到位),把 R71 降级为'守卫补齐'而非'消重收敛'。需 owner 在 Batch-1 完成后、Batch-5 启动前拍板:物理收敛 vs 仅 parity 锁定。
- R47 与 R71 的提交粒度:R47(死参数对称删)和 R71(helper 收敛)同在 coercion 双栈文件、co_change。若 R71 裁为'仅 parity 不物理收敛',R47 仍应独立两栈对称删(纯 P6)。需确认 R47 是否可不依赖 R71 收敛结论、仅依赖 LB07 承重注释+helper 签名 parity 即可单独落地(我的判断:可以,R47 是干净的双栈对称死参数删除,只要承重注释先落)。
- LB07 parity 测试落点:扩展点选 regression_scheduler_config_spec_sync_contract.py(已有 model↔service spec parity 骨架,最自然)还是 regression_config_snapshot_projection_sync.py(已有字段注解去重+degradation 保留)?二者都在 py38 contract 门禁名单内。建议扩前者(spec-sync)加三 helper 行为等价断言,后者保留字段集锁。需 owner 确认不与现有 spec-sync 两个用例语义冲突。
