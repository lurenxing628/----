# R03 逐债档案 — 候选"失败"态全套机制生产不可达(P6)

> 只读不改产物。所有断言附当前代码 file:line 证据。owner_pending=true → 下游清理段标「待裁,暂不给终态修法」。

## 字段 1：当前真实 file:line(回盘)+ 漂移记录

按符号名 rg 回盘(2026-06-05),逐符号对账：

| 符号 | registry 旧线索 | 当前实际 file:line | 漂移 |
|---|---|---|---|
| `except CandidateTrialFailure as exc:`(承重护栏) | old_location `:216`;phase1_blast 也称 `:216 仍准` | `core/services/scheduler/run/schedule_candidate_runner.py:216` | **无漂移**(精确命中,lb 行号当前可信) |
| `class CandidateTrialFailure(RuntimeError)` | — | `schedule_candidate_runner.py:28` | — |
| `CANDIDATE_STATUS_FAILED = "failed"` | — | `schedule_candidate_runner.py:24`,使用于 `:241,476,494(__all__)` | — |
| `_failed_plan(...)` | — | def `schedule_candidate_runner.py:470`,`status=CANDIDATE_STATUS_FAILED` `:476`;唯一调用点 `:217` | — |
| `failed_count` / `_count_candidates(..., FAILED)` | — | `schedule_candidate_runner.py:241`(产出),`_count_candidates` def `:251` | — |
| `_baseline_missing_or_failed` | — | def `schedule_candidate_runner.py:263`,产出 `:247` | — |
| `failed_candidate_count`(summary 流出) | `summary.py:135` | `core/services/scheduler/run/schedule_candidate_summary.py:135`;字段名清单 `:161` | **无漂移** |
| `baseline_missing_or_failed`(summary 流出) | `summary.py:140` | `schedule_candidate_summary.py:140`;字段名清单 `:166,191` | **无漂移** |
| `_failed_candidate_labels` + `!= "failed"`(helpers 展示) | phase1_blast `:324,329`;current_evidence `:361,366` | def `web/viewmodels/scheduler_analysis_candidate_helpers.py:360`,`!= "failed"` `:365`;消费 `failed_candidate_count` `:382`、`baseline_missing_or_failed` `:390` | **phase1_blast `:324,329` 已陈旧(漂移约 +36 行)**;current_evidence `:361,366` 基本准(差 ~1 行,实为 360/365) |
| `comparison.get("baseline_missing_or_failed")`(workbench UI 死分支) | phase1_blast `:231`(原报告 `:207`) | `web/viewmodels/dashboard_workbench.py:156`(全文件唯一一处) | **显著漂移:registry 称 `:231`/`:207`,实际 `:156`**(向上移,registry 行号全错) |
| candidates viewmodel 透传 | `:194,206,230,242,260,265` | `web/viewmodels/scheduler_analysis_candidates.py`:`failed_candidate_count` `:213,250,283`、`baseline_missing_or_failed` `:225,262,288`、`_failed_candidate_labels` `:224,261,287` | **全部漂移**(registry 行号与现状不符,符号仍在但位置全移) |

漂移结论：**承重核心行(runner.py:216)零漂移、lb 行号可信**;summary.py 准;但 **helpers.py / dashboard_workbench.py / candidates.py 三个下游消费者行号 registry 全部陈旧**——下游若推进清理必须以本回盘行号为准,不可照抄 registry。

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`。
- 一致。本轮唯一 actionable 项 = 给 `schedule_candidate_runner.py:216` 上方补「我是故意的 / b81f8b3f 护栏」中文注释,**当前源码仍无该注释**(Read `:205-218` 确认 `:216` 上方直接是 `:215` 闭括号,无注释)。注释未落 → planned 名副其实。
- 反证一致性:契约测试 `tests/candidate/test_scheduler_candidate_runner_contract.py:473` 已用**测试形式**钉死同一意图(`# 只有显式 CandidateTrialFailure 是候选级失败；ValidationError / RuntimeError / TypeError 必须继续向上抛。`),说明意图早被治理认可,**只缺源码侧那行人类可见注释**。

## 字段 3：病理 / 严重度 / 桶 / 簇

- **病理 P6**(为不可达态铺下游管道)。**回盘实证 P6 成立**:`CandidateTrialFailure` 在整个 `core/web/data` 生产代码中**零 raise 点**(rg `raise .*CandidateTrialFailure` 仅命中 tests `:470,576`);`_run_single_candidate`(`:270`)与 `_run_candidate_optimization`(`:299`)函数体内均无抛出。故 `:216` 的 except 在生产中永不触发 → `_failed_plan`(:470)永不调用 → `failed_count` 恒 0、`baseline_missing_or_failed` 在"有 baseline 且 completed"时恒 False → 一路喂到 `dashboard_workbench.py:156` 的 `if comparison.get("baseline_missing_or_failed")` 死分支(生产永不进)。
- **严重度 low**。理由:不可达 = 无用户可见错误,纯结构债;且承重护栏一侧反而要"保留"。
- **桶 B14**(scheduler-run);`home=B14`。
- **簇 ISOLATED**:`same_file_siblings=[]`、`interference_edges=[]`。runner.py 是 B14 独占文件,无同文件跨桶边。
- `needs_adversarial=true` 且 `adv_refuted=false`,但 PHASE0 §6 无独立 verdict(`verdict=null`)→ 字段 4/7 须额外谨慎,倾向保守。
## 字段 4：修法类

**严格拆两段(铁律 3：承重神圣 + 铁律 7：owner_pending)**：

**(A) 承重护栏 `runner.py:216` —— 修法类 = 仅补注释(load_bearing 一侧)。** 逻辑零改动。`lb_no_touch` 明确:verdict-confirmed 承重,narrow-except `CandidateTrialFailure` 防 b81f8b3f 删掉的"未知异常静默转 failed candidate"静默吞错复活。**禁区行(绝不可动)**:
- `:216` `except CandidateTrialFailure as exc:` —— 禁改回 `except Exception`、禁加 `except (..., RuntimeError)` 扩面、禁删。
- `:217` `return _failed_plan(...)` —— 禁透传/改写返回值。
- `:28` `class CandidateTrialFailure(RuntimeError)` —— 禁改基类。

唯一动作:在 `:216` 上方插入三行中文注释(R03 不在 90 节 8 条 LB 预起草名单,按 90 节风格起草):
```
# 只捕获显式 CandidateTrialFailure(候选级失败);其余异常(ValidationError/RuntimeError/TypeError)必须继续上抛。
# 这是 b81f8b3f 收窄后的护栏:绝不能以"统一/简化"名义改回 except Exception,
# 否则复活被治理掉的"未知异常静默转 failed candidate"静默吞错(踩灵魂线)。
```
该约束已被 `tests/candidate/test_scheduler_candidate_runner_contract.py:473,486,488` 钉死,注释是唯一缺口。**注意:此段不触灵魂线**(不新增兜底/吞错,反而是保护既有 loud-propagate 边界)。

**(B) 下游 FAILED 脚手架(summary/helpers/workbench/candidates) —— owner_pending=true,待裁,暂不给终态修法、暂不分配执行批次。** 因 `verdict=null` 且涉及**已落库 `ScheduleCandidate.status` 枚举契约**(completed/failed/skipped/not_run)+ 2 个 viewmodel + 公开摘要字段。**绝不裸删**(裸删枚举契约面会破坏持久化兼容)。本档案仅把依赖/爆炸半径分析透(见字段 8),终态(留/降级标记/登记 defer)交 owner 裁断。即便推进,也禁用 P4 灵魂线手法:不得静默回退,若要标"此分支生产不可达"只能补可观测注释或显式标记,不得 `except` 吞。

> 本债不涉 P5 收口点(簇 ISOLATED,无同收口符号),故无"收口到已存在统一点"动作。

## 字段 5：同文件碰撞债

`same_file_siblings=[]`、`interference_edges=[]`。runner.py 是 **B14 独占文件**,无同文件其他债。
→ **无碰撞**:不存在「谁先谁后 / 撞行号 / dict 键位移」问题。(A) 段插 3 行注释在 `:216` 上方,会把 `:216` 及以下整体下移 3 行;但因无同文件兄弟债依赖这些行号,且承重逻辑不变,行号位移仅影响后续核查回盘,不影响任何修法正确性。

## 字段 6：同收口点债

无。簇 ISOLATED,无其他债收口到 `CandidateTrialFailure` / `CANDIDATE_STATUS_FAILED` / `_failed_plan` / `baseline_missing_or_failed` 等本债符号。
→ **无顺序敏感性**。(A) 段承重注释独立可落;(B) 段 owner 裁断后独立推进,与全局其他债无收口冲突。
## 字段 7：收口行为差异及 parity 测试

**(A) 承重注释段:无收口、无行为差异**——纯注释插入,零分支变化,无需新 parity 测试。既存契约 `runner_contract.py:470-488`(造 `CandidateTrialFailure` → 断言 `status == FAILED`、`baseline_missing_or_failed is False`)与 `:576,632,647,648`(造缺/失败 baseline → 断言 `baseline_missing_or_failed is True`)已是该承重边界的 parity 守卫,**只读、不迁、不改**。

**(B) 下游脚手架(owner_pending,以下仅为裁断输入,不作终态)**:若 owner 选择"清理/删除不可达管道",则存在隐蔽行为差异需 parity 钉死,反例如下：
- `_baseline_missing_or_failed`(runner.py:263-267)语义 = **「无 baseline 候选」也返回 True**(`:267 return True`),并非仅"baseline failed"。即 `baseline_missing_or_failed=True` 在生产中**经"missing"路径可达**(非纯死分支!)——故 dashboard_workbench.py:156 的 if 分支**在 baseline 缺失场景生产可达**。死的是 `failed_count>0` / `CANDIDATE_STATUS_FAILED` 这条"failed"半支,不是整个 baseline 半支。
- **反例**:若有人误把 `dashboard_workbench.py:156` 当纯死分支裸删,会静默吞掉"baseline 缺失"的真实用户告警(helpers.py:390 同理消费 `baseline_missing_or_failed` 出告警文案)。
- 故 (B) 段 owner 裁断必须先区分 **missing(可达,留)** vs **failed(不可达,可登记 defer)** 两条语义——这正是 R03 P6 判定里最易踩的边界,**parity 测试须对 None / missing / failed / completed 四态逐分支断言等价**,不可笼统删除。

## 字段 8：爆炸半径(phase1_blast 9 字段 + 回盘核对)

- **生产消费者(回盘后真实行号)**:
  - `core/services/scheduler/run/schedule_candidate_summary.py:135`(`failed_candidate_count` 流出)、`:140`(`baseline_missing_or_failed` 流出)、字段名清单 `:161,166,191`。
  - `web/viewmodels/scheduler_analysis_candidate_helpers.py:360`(`_failed_candidate_labels` def)、`:365`(`!= "failed"`)、`:382`(消费 `failed_candidate_count`)、`:390`(消费 `baseline_missing_or_failed` 出告警)。
  - `web/viewmodels/dashboard_workbench.py:156`(死/半死分支 `if comparison.get("baseline_missing_or_failed")`)。
  - `web/viewmodels/scheduler_analysis_candidates.py:213,250,283`(`failed_candidate_count` 透传)、`:225,262,288`(`baseline_missing_or_failed` 透传)、`:224,261,287`(labels)。
- **测试消费者(动手前须先看)**:`tests/candidate/test_scheduler_candidate_runner_contract.py:15,470,473,486,488,576,632,647,648`;`tests/candidate/test_scheduler_candidate_summary_contract.py:87,149,170,195,230,293,295`;`tests/web_pages/test_dashboard_workbench_contract.py:83`;`tests/candidate/test_scheduler_candidate_display_contract.py:84,98` 及 `_analysis/_analysis_links/_persistence_contract` 系列。四个文件均**已确认存在**。
- **downstream_effect**:仅做 (A) 注释 → blast = 0(零逻辑)。做 (B) 清理 → 牵动已落库 `ScheduleCandidate.status` 枚举契约 + 2 viewmodel + 公开摘要字段,**blast 中等**。
- **灾难链(删错/统一错静默炸什么)**:
  1. 若把 `:216 except CandidateTrialFailure` 统一回 `except Exception`(踩 lb 禁区)→ 复活 b81f8b3f 治理掉的静默吞错:`ValidationError/RuntimeError/TypeError` 被静默转成 failed candidate,排产真错被吞、用户拿到"看似只是某方案失败"的假象 → 灵魂线灾难。
  2. 若把 `dashboard_workbench.py:156` / `helpers.py:390` 当纯死分支裸删 → 静默吞掉"baseline 缺失"真实告警(见字段 7 反例),用户看不到"没有基准方案"的提示。
  3. 若裸删 `CANDIDATE_STATUS_FAILED` 枚举 → 破坏已落库 status 字段持久化兼容(历史行可能含 "failed"),反序列化/校验炸。

## 字段 9：承重毗邻标记

- **本债自身即含 LB**:`runner.py:216` narrow-except 为 verdict-confirmed 承重护栏(`load_bearing` 字段虽标 false,但 `lb_no_touch` 非空 → 按铁律 3 当承重处理)。
- **禁区行(重申)**:`runner.py:28`(class 定义)、`:216`(except 子句)、`:217`(_failed_plan 调用)。这三行只许"上方/旁边补注释",禁删除/统一/合并/改基类/扩异常面/透传。
- 同文件无其他独立 LB 债(簇 ISOLATED)。`_failed_plan`(:470)、`_baseline_missing_or_failed`(:263)虽属同一不可达管道,但其"留 vs 清"归 owner_pending (B) 段,非承重禁区——但 (B) 推进时不得连带改动 (A) 的三禁区行。
## 字段 10：分层 / 导入环风险

- **0 风险**。(A) 段纯注释,不增任何 import。
- runner.py 现有 import(回盘 `:1-21`)全部合规:`math/time/dataclasses/types/typing` 标准库 + `core.infrastructure.errors` + `core.services.scheduler.config.config_snapshot` + 同包相对 `.`(health/selection/specs/graph_report/optimizer)。无 `core.algorithms→core.services`、无 `core.models→core.services`、无 import web。
- (B) 段若推进清理,改动落在 summary(core)与 viewmodels(web),各自层内,不跨层、不成环——但仍须 owner 裁断,本档案不动。
- AST 违规预期 = 0。

## 字段 11：测试耦合(动手前先迁哪些测试)

- **(A) 注释段:零测试迁移**。注释不改行为,既存契约 `runner_contract.py:470-488,576-648` 继续绿。建议:落注释后照跑该文件确认不挂(注释行不该影响断言)。
- **(B) owner 裁断后若清理(暂不执行)**:须先动 `runner_contract.py`(FAILED 态断言 `:486,632,647,648`)、`summary_contract.py`(`failed_candidate_count`/`baseline_missing_or_failed` 流出断言)、`dashboard_workbench_contract.py:83`、`candidate_display/_analysis/_analysis_links/_persistence_contract` 系列——但因涉枚举契约面,**测试不可随源码裸删**,须改为"missing 可达 / failed 不可达"的 parity 断言(见字段 7)。本轮不迁。

## 字段 12：前置依赖

- **(A) 承重注释段**:无硬前置。归 **Batch-1 全局承重注释网 ROOT**(Phase1 §1 已把 R03 列入),与其他 LB 注释一起最先落。桶内无硬依赖(runner.py B14 独占,无同文件跨桶边)。无前置 F 门。
- **(B) 下游脚手架清理**:前置 = **owner 对 `ScheduleCandidate.status` 枚举契约裁断**(留全枚举 vs 收窄)。须 **LATE**(枚举契约 owner 裁断之后),低优先,本轮不分配批次。无其他债阻塞;它也不阻塞其他债(ISOLATED)。
- 综上:R03 (A) 可立即随 Batch-1 落;(B) 挂起待裁。owner_pending=true。

## 对抗核验(第二双眼睛)

独立 rg/grep 回盘(2026-06-05,只读),逐条挑战档案断言:

**1. 真实行号 — 全部一致。**
- `rg -n "except CandidateTrialFailure" schedule_candidate_runner.py` → `:216`(承重行,零漂移,与档案/registry old_location 一致)。
- `class CandidateTrialFailure` → `:28`;`CANDIDATE_STATUS_FAILED` → `:24,241,476,494`;`_failed_plan` def `:470`、唯一调用 `:217`;`_baseline_missing_or_failed` def `:263`、产出 `:247`——全部命中档案。
- 下游:summary.py `:135,140,161,166,191` ✓;helpers.py `_failed_candidate_labels` def `:360`、`!= "failed"` `:365`、消费 `:382,390` ✓;dashboard_workbench.py `:156`(全文件唯一)✓;candidates.py `:213,224,225,250,261,262,283,288` ✓。**registry/phase1_blast 旧行号(workbench:231/:207、helpers:324/329)确属陈旧,档案已正确标红并以回盘为准。**

**2. 承重判断 — 成立。** Read `:205-218` 确认 `:215` 闭括号、`:216` except 上方零注释(planned 名副其实)。`rg "raise .*CandidateTrialFailure" core/ web/ data/ tests/` → 仅 `tests/...runner_contract.py:470,576`,**生产侧零 raise**;`_run_candidate_optimization`(:299)函数体内只有 `optimize_schedule_fn` 调用,无 raise——P6"生产不可达"实证成立。

**3. missing 可达反例 — 复现。** Read `:263-267`:`return True`(无 baseline 候选)+ `:266 return status != COMPLETED`。故 `baseline_missing_or_failed=True` 经 missing 路径生产可达,dashboard_workbench.py:156 if **非纯死分支**,裸删会静默吞"baseline 缺失"告警。契约 `:647 missing.baseline_missing_or_failed is True`、`:648 failed_baseline ... is True`、`:488 ... is False`(completed)已四态钉死。档案"须 None/missing/failed/completed 四态 parity"正确。

**4. 承重误删风险 — 无。** 档案 (A) 段仅补三行注释、明列禁区 `:28/:216/:217`,无删除/统一/透传/改基类/扩异常面,未触灵魂线。契约测试 `:473`(中文意图注释)真实存在。

**5. owner_pending 越界 — 无。** (B) 段明确标"待裁、暂不给终态修法、暂不分配批次",仅透依赖/爆炸半径,合规。

**结论:PASS。** 行号一致、承重判断与 missing 反例均独立复现、无承重误删、无 owner_pending 越界。
