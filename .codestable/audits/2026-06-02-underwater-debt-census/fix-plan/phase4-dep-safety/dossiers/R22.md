# R22 逐债档案 — default_plan_resolution_dict 手搓 plan_identity 双私有实现绕过收口

> 本轮只读不改、只产计划。所有断言附当前工作区 file:line 证据。
> bucket B02 · severity medium · pathology P5 · cluster C01 · load_bearing=false · owner_pending=true

---

## 字段 1 — 当前真实 file:line（回盘）+ 漂移记录

**主符号**（rg 回盘，2026-06-05 工作区）：
- `def default_plan_resolution_dict(plan_role)` = `core/services/scheduler/schedule_result_view_context.py:73`
- 手搓内层 `plan_identity = {…}` 起点 = `:77`，闭合 `:100`（22 键，见字段 7）
- 手搓外层 `return {…}` 起点 = `:101`，闭合 `:150`
- 内部 fallback 调用 `_resolution_to_dict` → `default_plan_resolution_dict()` = `:152`
- `no_history` 路径调用 = `:449`（`plan_resolution=default_plan_resolution_dict(raw_plan_role)`）
- `missing_history` 路径调用 = `:457`

**canonical 收口点**（回盘）：
- `build_plan_identity(...)` def = `core/services/scheduler/schedule_plan_identity_builder.py:158`（返回 `PlanIdentity`）
- `PlanIdentity.to_dict` = `core/models/schedule_plan_identity.py:46`（24 键，含 `result_summary_parse_failed`@58 / `result_summary_parse_reason`@59）
- `SchedulePlanResolution.to_dict` = `core/models/schedule_plan_resolution.py:64`

**漂移记录：**
| 线索来源 | 老行号 | 实际行号 | 漂移 |
|---|---|---|---|
| `default_plan_resolution_dict` def | old=73-147 / evidence=73 | **73** | 无 |
| 手搓内层 `plan_identity` dict | old/evidence=77（@77-100） | **77-100** | 无 |
| 手搓外层 dict | old/evidence=101（@101-147） | **101-150** | 闭合行 147→150 微漂 +3 |
| `no_history` 调用 | phase1_blast=440 | **449** | **+9（漂移现形，registry phase1_blast 行号过时）** |
| `missing_history` 调用 | phase1_blast=448 | **457** | **+9（同上过时）** |
| `_resolution_to_dict` fallback | phase1_blast=152 | **152** | 无 |
| `_identity_bool` 静默 False 路径 | phase1_blast=271-274 | **271-274** | 无 |

> 结论：`location_status:"✅准"` **部分失真**——def/内层 dict 准，但 phase1_blast 标注的 no_history@440 / missing_history@448 已 +9 漂移到 449/457（本文件可能被同分支其它改动顶下去）。回盘后以 449/457 为准。

## 字段 2 — 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = "planned"`。
- 当前代码核对：手搓内层 dict（:77-100）确实仍**只有 22 键**，缺 `result_summary_parse_failed` / `result_summary_parse_reason`（Python 比对见字段 7）；`tests/` 中 `rg` parity / `build_plan_identity().to_dict()` 断言**全空**（仅 `regression_scheduler_plan_identity_evidence_contract.py` import 了 build_plan_identity 但用作别的契约）。
- 计划第 1 步（parity 测试）、第 2 步（收口）**均未落地**。
- **状态一致：** 与 current_evidence 完全吻合，确为 `planned` 未动。无 in_progress 痕迹。

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- **病理 P5**：default_plan_resolution_dict 在 :77-150 手搓 plan_identity（内层 22 键）+ resolution（外层）**双私有实现**，绕过 `build_plan_identity`（builder:158）与 `SchedulePlanResolution.to_dict`（resolution:64）。
- **drift 已现形（非潜在）**：canonical `PlanIdentity.to_dict` 现 24 键，手搓侧 22 键，**差集恰为 `{result_summary_parse_failed, result_summary_parse_reason}`**（字段 7 给 Python 实锤）。LB03 在途给 canonical 加了这两键、手搓侧未跟，报告时键集漂移=0、今天=2，病理从"潜在"升级到"已发生"。
- **严重度 medium**：手搓 dict 仅供**只读无历史页**（no_history/missing_history @449/457）；写/派工安全闸门另走真 identity re-resolve（见字段 8），故不会静默放行写操作 → 不升 high。但只读页的 plan_role 过滤字段会因缺键静默偏 False（字段 8 灾难链）。
- **桶 B02**（home=B02，scheduler-plan-identity 分区）；**簇 C01**（身份族）。

## 字段 4 — 修法类

**类别：收口（P5），但 `owner_pending=true` → 待 owner 裁断，暂不给终态修法、暂不分配执行批次。** 以下为依赖透析后的*候选*修法（非定稿）：

- **第 1 步（precondition，归 Batch-1 纯增量网，必先于收口）**：补 parity 契约测试——对 `VALID_PLAN_ROLES` 全集断言
  `default_plan_resolution_dict(role)['plan_identity']` 的键集 + 取值 ==
  `build_plan_identity(..., source_table=SOURCE_SCHEDULE, ...).to_dict()` 对应字段。该测试**只增不改**，落地后立即把今天的 22≠24 drift 钉成红线。
- **第 2 步（收口动作，Batch-4，须在 B01 之后）**：把 :77-100 手搓内层 dict 删除、改为委托 `build_plan_identity(...).to_dict()`；外层 dict 的 plan_resolution 形态可委托 `SchedulePlanResolution.to_dict`。
- **P5 收口点必须是已存在点**（铁律 5）：收口到 **`build_plan_identity`（builder:158）+ `SchedulePlanResolution.to_dict`（resolution:64）**，二者皆已存在、是 canonical 身份族唯一点，**不新建任何第二模块**。本债不属 R09 例外。
- **灵魂线合规**：收口本身**不新增兜底/静默回退**；恰相反——它消除现有的"缺键静默 False"隐式回退。若收口时遇到 role 非法，**保留 gantt_plan_query.py:33-39 的 loud `ValidationError("未知的排产方案角色：…")` 文案包装**（见字段 6），不得改成静默。

## 字段 5 — 同文件碰撞债

**R22 自身 `same_file_siblings = []`（registry 实证）。** interference_edges 中 R23/R44/R54 的 `same_file:true` 是**粗粒度共现标记，非真实编辑碰撞**，逐一核实：

- **R23**：interference 标 `file:view_context.py`，但 R23 真实 `primary_file = schedule_plan_role.py`（registry 实证）。**工作区中不存在 `view_context.py` 文件**（`find` 空）——这是 registry 的文件名简写。R23 改 `schedule_plan_role.py` 的 `_normalize_role`，**与 R22 的 :73-150 区段零重叠**。非真碰撞。
- **R44**：真实 `primary_file = scheduler_navigation_publish.py`，其修法编辑 `web/routes/domains/scheduler/scheduler_navigation_publish.py:36` 的 `selected_plan_role` 副本。本文件内的 `selected_plan_role` 在 **:201**（远离 R22 的 :73-150），R44 不动 :73-150。**不撞行号、不撞 dict 键位移。**
- **R54**：真实 `primary_file = scheduler_reports_workbench.py`（LB=true 承重），改 viewmodels 的 plan-guard 字段列表，`rg` 确认**不触碰 schedule_result_view_context.py**。非真碰撞。

> **碰撞结论：R22 收口（删 :77-100、改委托）在本文件内是孤立区段，与任何 sibling 不撞行号、不撞 dict 键位移。** 唯一需协调的是**跨文件的 R21**（见字段 6），不是同文件碰撞。

## 字段 6 — 同收口点债 + 顺序敏感性

**收口到同一符号 `build_plan_identity` 的债：LB03**（interference `same_symbol:true, sym:build_plan_identity`）。

- **LB03**（`schedule_plan_identity_builder.py`，**LB=true 承重，P4**）：`_bool_from_summary` 静默吞坏 JSON，影响 is_official/可执行版本/可派工。LB03 正是**给 canonical 加了 `result_summary_parse_failed/reason` 两键**的债（drift 来源）。
  - **顺序敏感：** R22 收口后将**继承** canonical 的 24 键含这两个 parse 键。LB03 若把 P4 静默吞改成 loud raise / 降级标记，会改变这两键的*取值语义*，但**不改键集**——故 R22 parity 测试用键集 + 取值断言后，LB03 的取值改动会被 parity 测试覆盖到，**二者顺序：先 R22 parity 钉键集（Batch-1），LB03 与 R22 收口都在其后，互不破坏键集**。
  - **R22 不改 LB03**：只 CALL `build_plan_identity`，不动 builder 内部（铁律 3，LB03 承重）。

**跨文件死兼容 wrapper：R21**（`gantt_plan_query.py:32-39`，P3，LB=false）——**非同收口点，但顺序强耦合：**
- R21 想删 `gantt_plan_query.py:32-39` 的 `default_plan_resolution_dict` wrapper（回盘确认该 wrapper 仍在：try → `_default_plan_resolution_dict`，except 重抛 `未知的排产方案角色：{role}` loud 文案）。
- **R22 收口 precondition 依赖该 wrapper 续命**——contract test `tests/schedule/summary/test_schedule_result_view_context.py:294-301`（`test_gantt_plan_query_wrapper_keeps_legacy_bad_role_message`，回盘确认在）钉死"未知的排产方案角色：bad"文案。
- **顺序铁律：R21 删 wrapper 会让 R22"保留遗留文案包装"约束失效。** 二者必须协调；co-change 组 = {R21, R44}（同 Batch-4 身份族）。**建议：R22 收口时显式保留 wrapper，或 R21 推迟到 R22 之后并改为只删真正死代码、保留文案分支。**

## 字段 7 — 收口行为差异 + 所需 parity 测试

**Python 实锤键集比对（本轮盘上跑，2026-06-05）：**
```
canonical PlanIdentity.to_dict  : 24 键
手搓 plan_identity (:77-100)    : 22 键
MISSING (canonical − 手搓)       : ['result_summary_parse_failed', 'result_summary_parse_reason']
EXTRA   (手搓 − canonical)       : []  ← 手搓无多余键，纯缺 2 键
```

**新旧两路对边界值的分支差异（反例）：**
1. **缺键 → 静默 False 反例**（最关键）：手搓 dict 无 `result_summary_parse_failed` 键。下游 `_identity_bool`（:271-274）逻辑是 `if key in plan_identity: ... else bool(data.get(key))`，外层 `plan_role_filter_fields`（:329-330）读 `plan_identity.get("result_summary_parse_failed") or data.get(...)` → 手搓侧两路皆缺 ⇒ **恒得 `False`**。canonical 路在解析失败时会给 `True`。**反例：** 一条 result_summary 损坏的方案，走 canonical → `result_summary_parse_failed=True`（报表/派工会拦）；走 no_history 手搓 dict → 静默 `False`（误判为正常）。但注意手搓 dict 只服务 no_history/missing_history（此时本就无 summary），故实际触发面窄 → 支撑 medium 而非 high。
2. **委托后多出 3 键无害**：canonical 比手搓还多 `schedule_result_status / schedule_lock_status / detail_saved`（手搓其实已有这 3 键，见 :86/:96/:99），下游均 `.get()` 读取 → 无 KeyError。
3. **外层 `is_official` 取值法差异**：手搓外层 :142 用 `bool(plan_identity["is_official"])` **方括号硬取**；收口后若改 `.get()` 反更稳（缺键不抛）。属正向改善，非破坏。

**所需 parity 测试（Batch-1，先于收口）：**
- 对 `VALID_PLAN_ROLES` 全集，断言 `default_plan_resolution_dict(role)['plan_identity']` 键集 **== `build_plan_identity(...source_table=schedule...).to_dict()` 键集**（exact `==`，**不是** `>=`）。
- **复用警告**：现有 `regression_scheduler_plan_identity_evidence_contract.py:197-218` 用 `set(identity) >= {…22 键}`（superset，且只列 22 键、漏列两 parse 键），**抓不到本 drift**。新 parity 测试必须用 exact `==` 且以 canonical 24 键为基准，不可照抄这份 22 键超集断言。

## 字段 8 — 爆炸半径（9 字段对账 + 灾难链）

**生产消费者（回盘修正行号）：**
- `schedule_result_view_context.py:152`（`_resolution_to_dict` fallback）、**:449（no_history，phase1_blast 标 440 已 +9 漂）**、**:457（missing_history，phase1_blast 标 448 已 +9 漂）**
- `gantt_service.py`、`gantt_critical_chain_provider.py`、`gantt_plan_query.py:12,34`（R21 死兼容 wrapper，仅被测试续命）
- `web/routes/report_plan_preview.py`、`web/routes/domains/scheduler/scheduler_week_plan.py`、`scheduler_navigation_publish.py`（R44 同文件消费）
- 下游键读取：`reports_plan_template_fields.py:45,61`（读 `result_summary_parse_failed`）、`schedule_plan_resolution.py:90`、多个 viewmodels（`scheduler_workbench_links.py:276/359`、`scheduler_resource_dispatch.py` 等）。

**测试消费者：**
- `tests/schedule/summary/test_schedule_result_view_context.py:18`（直接 import）、`:298`（经 gantt_plan_query wrapper 续命 R21）
- `tests/schedule/route_view/test_scheduler_plan_identity_evidence_contract.py`（build_plan_identity 契约，收口后 parity 参照源，但当前为 22 键 superset，需升级）

**灾难链（删错/统一错会静默炸什么）：**
- **改坏手搓 dict / 统一时漏键** → no_history/missing_history 页 `plan_role_filter_fields` 经 `_identity_bool`（:271-274 缺键静默 False） → 资源派工字段（`resource_dispatch_service.py:386/397` 等）与报表 plan_role 过滤字段**全 False 误判或 KeyError**。
- **缓解事实**：写/派工的真正安全闸门**另走真 identity re-resolve**（`operation_execution_feedback_service.py:355-362`、`resource_dispatch_actual_record_service.py:134-152`），手搓 dict 仅供只读无历史页 → **不会静默放行写操作**。这是 severity 钉在 medium 的根据。
- 委托后多出的 3 键下游均 `.get()` 读取，无害；外层 :142 `is_official` 方括号取值收口后改 `.get()` 反更稳。

## 字段 9 — 承重毗邻标记 + 禁区行

- **本文件 `schedule_result_view_context.py` 不在承重护栏名单**；R22 `load_bearing=false`、`lb_no_touch=null`。
- 同文件**无 LB 债**（R44/R54 虽 same_file 标记但真实 primary_file 在别处；R54 LB=true 但承重落在 `scheduler_reports_workbench.py`，不在本文件区段）。
- **同收口符号 `build_plan_identity` 所在的 `schedule_plan_identity_builder.py` 是承重（LB03，B01）**——R22 **只 CALL 不改** builder 内部。**禁区行（绝不在 R22 收口中改动）：** `schedule_plan_identity_builder.py:158`（build_plan_identity 签名）及其函数体、`core/models/schedule_plan_identity.py:46-71`（PlanIdentity.to_dict 24 键定义）。R22 对这些只读取/调用。

## 字段 10 — 分层 / 导入环风险

- **收口目标 `build_plan_identity` 位于 `core.services.scheduler`，与 `schedule_result_view_context.py` 同层同包**（回盘：builder 顶部仅 import `core.models.*`，view_context 已 import `core.models.schedule_plan_role` + `data.repositories...SOURCE_SCHEDULE`）。
- 委托 `build_plan_identity` / `SchedulePlanResolution.to_dict` **不引入任何跨层 import**：
  - 不出现 `core.algorithms → core.services`（不涉及 algorithms）
  - 不出现 `core.models → core.services`（builder 是 services，模型是 models，方向正确：services 调 services + 读 models）
- **导入环风险：零。** `schedule_result_view_context.py` 已与 builder 同包；新增 `from .schedule_plan_identity_builder import build_plan_identity` 是包内同层依赖，builder 不反向 import view_context（回盘 builder import 列表无 view_context）。**0 AST 违规可保。**

## 字段 11 — 测试耦合（动手前先迁/补哪些测试）

1. **先补（Batch-1，precondition）**：新 parity 测试（字段 7），exact `==` 对 VALID_PLAN_ROLES 全集断言键集+取值。这是收口的红线门。
2. **先升级**：`regression_scheduler_plan_identity_evidence_contract.py:197-218` 当前 `>= {22 键}` superset 漏两 parse 键，应改 exact 24 键（否则它继续放过 drift）。属同族治理，可与 R22 parity 合并。
3. **保持绿、不可改语义**：`regression_schedule_result_view_context.py:294-301`（gantt_plan_query 遗留文案 `未知的排产方案角色：bad`）——R22 收口须保留 wrapper 才能保持此测试绿（与 R21 协调点）。
4. 收口后跑全量 `regression_schedule_result_view_context.py` + `regression_scheduler_plan_identity_evidence_contract.py` + `result_summary_parse` 相关 5 个测试文件回归。

## 字段 12 — 前置依赖（先做哪些债 / 哪些 F 门）

按 planned_deps_hint + 本轮回盘：
1. **全局 #1：B01 先落** —— LB03 承重注释 + `build_workbench_plan_context` guard 收口（B01）必须先于 B02 身份族动工，R22 才动方案身份族。
2. **Batch-1 parity 测试就位**（字段 7 的新测试 + 评分契约升级）—— 收口的硬 precondition，必须先于 R22 第 2 步收口。
3. **与 R21 协调**：R21 **绝不可先于 R22 删** `gantt_plan_query.py:32-39` 的 wrapper（否则 R22"保留遗留文案包装"约束失效）。建议 R21 推迟到 R22 收口之后，或 R21 改为只删死代码、保留 loud 文案分支。
4. **与 LB03 排序**：LB03（P4 静默吞错改 loud/降级标记）可与 R22 收口同 Batch-4 之后，因二者只在 `build_plan_identity` 取值层交互、不改键集，parity 测试覆盖即可。
5. **co-change 组**：{R21, R44}（同 Batch-4 身份族），需同窗口协调。
6. **owner_pending=true**：以上为依赖透析；**终态修法与执行批次待 owner 裁断后定稿。**

---

### 对账小结（与既有 registry 分析的出入）
- `phase1_blast.location_status:"✅准"` **部分失真**：no_history/missing_history 调用行已从 440/448 漂到 **449/457（+9）**。其余（def@73、内层@77、_identity_bool@271-274、wrapper、收口符号）回盘准。
- `interference_edges` 的 R23 `file:view_context.py` 标记**误导**：R23 真实 primary_file 是 `schedule_plan_role.py`，且工作区无 `view_context.py`，非 R22 真同文件碰撞。
- `regression_scheduler_plan_identity_evidence_contract.py` 既有 22 键 superset 契约**抓不到本 drift**，需升级为 24 键 exact——这是为什么 drift 能从 0 涨到 2 而 CI 全绿的根因。

---

## 对抗核验（第二双眼睛）

**结论：PASS（找不到反证）。** 独立 rg/grep + Python 实锤逐项挑战，未发现可推翻的反例。`owner_pending=true` 守则、承重禁区、收口点真实性、分层无环均成立。

**重 grep 命令与结果（2026-06-05 工作区）：**
- `rg -n "def default_plan_resolution_dict" schedule_result_view_context.py` → **73** ✅；内层 `plan_identity = {`@**77**、外层 `return {`@**101** ✅；`default_plan_resolution_dict` 调用 → fallback@**152**、no_history@**449**、missing_history@**457** ✅（phase1_blast 标 440/448 确已 +9 漂，档案已主动修正，非档案错）。
- `rg -n "def build_plan_identity" schedule_plan_identity_builder.py` → **158** ✅；`PlanIdentity.to_dict`@**46**、两 parse 键@**58/59** ✅；`schedule_plan_resolution.py`：`class SchedulePlanResolution`@43 → 其 `to_dict`@**64** ✅（@24 是 RoleOption.to_dict，归属无误，非同名混淆）。
- **键集 Python 实锤复跑**：手搓内层 **22 键**、canonical to_dict **24 键**，`MISSING={result_summary_parse_failed, result_summary_parse_reason}`、`EXTRA=[]` —— 与字段 7 字字吻合。sed 确认手搓侧已含 schedule_result_status@86/schedule_lock_status@96/detail_saved@99（字段 7 反例 2 自洽）。
- `_identity_bool`@**271-274** 逻辑 `if key in plan_identity: bool(.get) else bool(data.get)` ✅；`plan_role_filter_fields`@**329-330** 用 `plan_identity.get(...) or data.get(...)` ✅ → 手搓缺键两路皆 None ⇒ 恒 False，灾难链反例成立。
- gantt_plan_query wrapper@**32-39**：try `_default_plan_resolution_dict` → except ValidationError 重抛 loud `未知的排产方案角色：{requested_role}`@38 ✅；contract test `test_gantt_plan_query_wrapper_keeps_legacy_bad_role_message`@**294**、assert 文案@**300** ✅（档案标 294-301 区间一致）。
- evidence_contract `set(identity) >= {…}`@**194**：逐键数清**恰为手搓 22 键、漏列两 parse 键** → superset 对 24 键恒真，drift 0→2 而 CI 全绿根因坐实 ✅；`tests/` 内 `default_plan_resolution_dict` × `build_plan_identity` 无任何 `==`/parity 交叉断言（**parity 确未落地**，符合 planned）。
- **分层/环**：builder 顶部仅 import `core.models.*`（@5/6/14），**不反向 import view_context**（rg 空）；view_context 尚未 import builder（rg 空，符合 planned）。委托是包内同层 + 读 models，**0 跨层、0 环** ✅。

**承重误删风险：无。** `load_bearing=false`/`lb_no_touch=null` 实证为真；档案候选修法为「补 parity 测试 → 委托 canonical」收口，无擅自删/统一承重 builder 内部；禁区行（builder:158、plan_identity:46-71）标注正确，R22 只 CALL 不改。

**owner_pending 守则：合规。** 字段 4/12 明确「待 owner 裁断、暂不给终态、暂不分配执行批次」，候选修法均标「非定稿」，**未擅自定稿或分配批次**。

**仅有的档案微瑕（不构成争议、不改结论）：** evidence_contract superset 实测在 194 起（字段 7 文中引 197-218），属同段行号微漂；档案已主动修正 phase1_blast 的 440/448→449/457，方向正确。无需第三方裁断。

