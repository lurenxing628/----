# R19 逐债档案 · positive-op-id 过滤三处漂移收口

> 只读不改产物。债 id=R19 | bucket=B13 | 簇=C01 | 严重度=low | 病理=P5 | 承重=false | owner_pending=false | status=fixed
> 主文件: core/services/scheduler/execution_snapshot.py | 同文件兄弟: R13/R15/R18
> **2026-06-09 交接补登**：O04/O05/O37 已裁。交接时的执行顺序曾是 R15（已 fixed）→ R19 → R13；R19 的 repo 私有版按 O04 保留并补“顺序无关”注释，禁止下沉到 service 层或新建模块。R13 已在 2026-06-10 补确认并 fixed。
> ✅ **2026-06-09 执行补登**：R19 已 fixed。`execution_snapshot.positive_op_ids` 保 sorted 并补指纹排序承重注释；`execution_snapshot` 顶层不再 import provider，改为 `collect_execution_snapshot_for_plan_rows` 内局部 import，避免 provider 收口时形成运行时环；`execution_fact_provider._positive_op_ids` 委托既有 `positive_op_ids`，missing 文案新顺序锁定为最小正 op_id；`operation_execution_event_repo._positive_ids` 按 O04 保留私有版并补顺序无关注释/parity，不下沉 service、不碰 R18 stub。
> ✅ **2026-06-10 R13 后补登**：R13 已按 O06 完成，owner 已确认单机无仓库外读者；`ExecutionFact.last_event_schedule_*`、`_fact_from_state` 的 `latest` 形参/实参、旧字段赋值与孤儿 `_latest_events_by_scope` 已删除。G09 provider 链已收口；G10 的 R18 stub 护栏也已随后补注释 fixed。

## 1) 当前真实 file:line(回盘)+漂移记录

三处同概念实现,本轮 rg 独立回盘(`rg -n "positive_op_ids|_positive_ids|return out|return sorted"`):

| 处 | 符号 | old_location_DONT_TRUST | 执行前 evidence(DONT_TRUST_AFTER_FIXED) | 本轮实测 | 漂移 |
|---|---|---|---|---|---|
| 公开收口点 | `positive_op_ids(values)` | execution_snapshot.py:27 | 执行前旧锚点已失效 | **execution_snapshot.py:29 def / :42 `return sorted(out)`** | fixed：保 sorted，并在 :41 补指纹排序注释 |
| provider 私造 | `_positive_op_ids(values)` | execution_fact_provider.py:51 | 执行前旧锚点已失效 | **execution_fact_provider.py:69 def / :70 `return positive_op_ids(values)`** | fixed：委托既有收口点；snapshot 顶层 provider import 已移除防环 |
| repo 私造 | `_positive_ids(values)` | operation_execution_event_repo.py:63 | :67 def / :79 `return out` | **operation_execution_event_repo.py:67 def / :80 `return out`(不排序)** | O04 保私有版，:68 补顺序无关注释，测试钉效果 |

- 公开导出确认:execution_snapshot.py:123-128 `__all__` 含 `"positive_op_ids"`(:127)。
- 导入边确认:execution_snapshot.py:7-8 仅在 `TYPE_CHECKING` 下导 `ExecutionFact`；`ExecutionFactProvider` 已挪到 `collect_execution_snapshot_for_plan_rows` 内局部 import(:112，:111 有『勿上提』防环承重注释)，provider 顶层 import `positive_op_ids` 不再形成运行时环。
- 当前终态:provider 已复用 snapshot 的 sorted canonical；repo 仍保私有版不排序。差异保留原因不是漏收口，而是 repo 属 data 层，不能导入 service 层收口点。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = "fixed"`。本轮实测：snapshot 仍排序且补注释；provider `_positive_op_ids` 已委托 `positive_op_ids`；repo 仍保私有版不排序但补“顺序无关”注释和 `_batch_ids_by_op_ids` 效果 parity。
- 一致:状态=fixed 与代码现实吻合。R13 也已 fixed；R18 已按 G10 独立补护栏注释 fixed，R19 不再阻塞。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P5**(收口缺失/同概念多份私造):执行前已有**导出的公共** `positive_op_ids`，provider/repo 各私造第 2、3 套实现且发生**语义漂移**(是否 `sorted`)；执行后 provider 已收口，repo 因分层保私有版。
- **严重度 low**:排序差异未引发现行 bug(repo 落 dict 顺序无关;provider 原先仅影响 raise 消息里的首个 op_id;见 §7/§8)。本次 fixed 后，provider 文案变化已由 exact parity 锁定，repo 顺序无关由 parity 锁定。
- **桶 B13**(home=B13)| **簇 C01**(scheduler-exec-diag 分区)。

## 4) 修法类

**收口到 X(部分收口)+ owner_pending=false。** O04/O05/O37 已裁，当前终态修法口径如下:

naive 计划「三处全收口到 `execution_snapshot.positive_op_ids`」已被拆成安全终态:
- **已消解的阻断 A(导入环)**:旧 execution_snapshot 顶层 import provider 会让 provider 反向 import snapshot 成环；R19 已把 `ExecutionFactProvider` 改到函数内局部 import，所以 provider 可以安全复用 `positive_op_ids`。
- **仍成立的阻断 B(分层红线)**:repo 在 `data/repositories`(data 层),`positive_op_ids` 在 `core/services`(service 层);repo 收 service = data→service 越层违规(违 §6 分层红线 + test_architecture_fitness)。
- **red line #3**:禁止为已有概念新建第二模块(唯一例外=R09 `parse_optional_positive_int`),故不能随手开新 helper 模块。

故**只能拆分**:
1. provider `_positive_op_ids`(同 service 层)→ 已收口到 `positive_op_ids`。为避免原 `execution_snapshot -> provider` 顶层边导致 provider 反向 import snapshot 成环，先把 `ExecutionFactProvider` 改为 `collect_execution_snapshot_for_plan_rows` 内局部 import，再让 provider 顶层 import `positive_op_ids`。
2. repo `_positive_ids` 因分层**不能**收 service;已按 O04 保留 repo 私有版 + 补「顺序无关」中文注释 + 绑 parity 测试。不得下沉/新建收口模块。
- **务必保 sorted 给 snapshot**:指纹稳定性依赖排序(§7),收口实现的 canonical 版必须 `sorted`。
- **执行结果**:R19 已按“先补 positive_op_ids 黄金用例，再改 provider；repo 只补注释/parity，不做跨层收口”完成。R13 也已按 O06 完成；G09 provider 链已收口。

## 5) 同文件碰撞债(主文件 execution_snapshot.py + 两共享文件)

主文件 execution_snapshot.py 上 R19 是唯一符号(`positive_op_ids` :29-42);same_file_siblings R13/R15/R18 实际落在**两个共享文件**(非 snapshot.py 本身),碰撞发生在收口"路过"它们时:

| sibling | 桶 | 所在文件:行 | 改哪段 | 与 R19 谁先谁后 | 撞行号/dict键位移风险 |
|---|---|---|---|---|---|
| **R15** | B06 | execution_fact_provider.py `_parse_execution_time` | provider 内下方相邻段 | **R15 已 fixed**;R19 执行前按符号重 rg | **同文件且紧邻**:R15 改动已落，R19 禁信旧 :70/:82 |
| **R13** | B11 | execution_fact_provider.py 字段/形参/实参/孤儿 helper | provider 内别处 | **R19 先，R13 后，均已 fixed** | **同文件**:R19 改 `_positive_op_ids` 后，R13 已按符号重 rg 删除旧字段尾巴 |
| **R18** | B11 | operation_execution_event_repo.py 相邻 stub raise | repo 内别处 | R19 repo 处保私有版补注释，R18 独立处理 stub | R19 不删 `_positive_ids`，只补注释/parity 时与 R18 零行号删除冲突 |

要点:R19 横跨 provider + repo 两共享文件。当前硬序已按 O37 收敛为 **R15 fixed → R19 fixed → R13 fixed**；repo 处按 O04 保留私有版并已补注释/parity，不再把 R19 误排成后置待裁项。

## 6) 同收口点债(收口到同一符号 / 同概念聚集)

- E16 `__all__` 历史边已由红队第2轮 P-RT22-02 判为伪串行登记: R19/R01/R46 的 `__all__` 分属三文件，R46 不动 execution_snapshot.py；不作为 R19 go/no-go 门。R19 只需守住本文件 `positive_op_ids` 公开导出不被误删。
- core/models 现有 positive 概念聚集点(本轮实测,潜在 canonical 落点):`scheduler_public_errors.py:167 _positive_int`、`operation_execution_scope.py:9 parse_positive_execution_int`、`operation_execution_event.py:137 _event_positive_int`——**均非 op-id 序列过滤语义**(它们是单值/字段校验),不能直接复用;若 owner 选落 models,需新增"序列版"且论证不违 red line #3。
- 顺序敏感性:R19 与 R01/R46 的 E16 `__all__` 边仅登记备查、不作执行门；与 R13/R15/R18 共享文件但不同符号 = 行号顺序敏感(§5)。

## 7) 收口行为差异及所需 parity 测试

**逐分支等价性**:三处对 int 化 / `<=0` 排除 / 去重逻辑**完全等价**;唯一非等价分支=**输出顺序**。反例与影响:

- **反例(顺序)**:输入 `[3, 1, 2, 1]` →
  - snapshot `positive_op_ids` = `[1, 2, 3]`(`sorted`)
  - provider `_positive_op_ids` 已随 snapshot 收口为 `[1, 2, 3]`
  - repo `_positive_ids` 保私有版为 `[3, 1, 2]`(插入序,去重后)，但只用于 SQL IN + dict，顺序无关
- **各消费点对顺序敏感度(实测)**:
  - snapshot :87-90 把 ids 顺序拼进 payload 再 sha256(:91)→ **指纹强依赖排序**;若把 snapshot 改成不排序版,指纹值变 = 破坏快照修订稳定性(灾难,§8)。⇒ canonical 实现**必须保 sorted**。
  - provider :126 `missing = [op_id for ... if op_id not in out]`,:128 `raise ...op_id={missing[0]}`→ 已随 sorted 收口，缺失文案锁定为排序后第一个缺失正 op_id。`include_op_ids=[30,20,10,20,0,-1,"x",None]` 且实际只有 10 时，文案为 `op_id=20`。
  - repo :410-422 `_batch_ids_by_op_ids`(2026-06-10 R18 注释后现盘,旧 :408-420):ids 喂 SQL `IN (...)` 并落 `out[id]` dict → **顺序完全无关**;收 sorted/不收都不影响结果。
- **所需 parity 测试(收口前必补)**:
  1. `positive_op_ids` 黄金用例已补:空/None、含 0 与负数、含非 int(`"x"`/`None`)、重复 → 锁定 `[1,2,3]` 排序输出(钉住指纹依赖)。
  2. provider 收口 parity 已补:缺失文案显式断言新排序口径 `op_id=20`，loud raise 不得变成静默。
  3. repo 保留私有版 parity 已补:用 `_batch_ids_by_op_ids` 断言乱序/重复/坏值输入与排序输入返回同一 dict，作为「故意分叉」守卫。

## 8) 爆炸半径(prod/test 消费者 + 灾难链)

**生产消费者(实测回盘)**:
- snapshot `positive_op_ids`:execution_snapshot.py:80(`build_execution_snapshot`,喂指纹)、:96(`op_ids=ids`)、:114(`collect_execution_snapshot_for_plan_rows`)。上游 import snapshot 的 4 处:`run/schedule_execution_persistence_guard.py:9`、`gantt_adjustment_publish_service.py:34`、`gantt_adjustment_scenario_service.py:16`、`run/schedule_execution_guardrails.py:17`——均经 `build_execution_snapshot`/`collect_*` 间接吃指纹。
- provider `_positive_op_ids`:仅 execution_fact_provider.py:126(`facts_by_op_id_for_scopes`:110 内的 missing 检查)。该方法结果再流入 :131 `facts_by_op_id_for_plan_rows`(:95 `facts_by_op_id` 现为 loud-raise stub,不消费)。
- repo `_positive_ids`:operation_execution_event_repo.py:410(`_batch_ids_by_op_ids`,SQL IN 查询;R18 注释后现盘,旧 :408)。【2026-06-10 纠正】该链路实为**生产死代码**:全仓 grep `_batch_ids_by_op_ids` 仅命中定义/注释/parity 测试,历史唯一调用方 aggregate_states_by_op_ids 已是 loud-raise stub(:406),活路径 aggregate_states_by_scopes 不经过它;registry 旧记的 :235/:268/:292 aggregate 消费路径已随 stub 化失效。

**测试消费者**:已补直接/近直接守卫。`tests/operation_execution/test_operation_execution_scope_read_contract.py` 覆盖 `positive_op_ids` 排序、snapshot revision 顺序稳定、provider missing 文案；`tests/operation_execution/test_operation_execution_event_foundation.py` 覆盖 repo `_batch_ids_by_op_ids` 顺序无关。

**灾难链(删错/统一错会静默炸什么)**:
1. **指纹漂移(最严重,静默)**:若误把 canonical 实现统一成"不排序"或让 snapshot 走不排序路径 → `build_execution_snapshot` 的 sha256 revision 对同一输入产出不同值 → 快照/重订指纹漂移 → 下游 4 处 guard/publish/scenario 的快照比对、变更检测**静默失真**。现已用黄金用例拦截。
2. **越层崩溃(响)**:若强行让 repo import service 收口 → test_architecture_fitness 直接 fail(响,可拦)。
3. **导入环(响)**:provider 反向 import snapshot → 运行时 ImportError / 循环导入(响,可拦)。
4. **报错文案漂(轻、可见)**:provider missing[0] 顺序变,运维看到的 op_id 变,无功能损。

## 9) 承重毗邻标记

- 本债 `load_bearing=false`、`lb_no_touch=null`、`phase1_blast.lb_colocation_danger="none"`。
- 三个文件本轮未见 LB 标记符号毗邻(无 `lb_no_touch` 禁区行)。
- **但**:execution_snapshot.py 的 sha256 指纹路径(:80→:89-93)虽非正式 LB,**事实上承重**(快照修订稳定性的唯一来源)。故即便 R19 非承重,`sorted` 这一行(:42)实为**事实承重行**——收口实现必须保留排序语义,等同承重待遇:改它须补「我是故意的:指纹稳定性依赖排序,canonical 实现强制 sorted」注释 + parity。无其他禁区行。

## 10) 分层 / 导入环风险

- **导入环(阻断 A，已消解)**:旧 execution_snapshot.py:7 顶层 `from core.services.scheduler.execution_fact_provider import ...` 会让 provider 反向 import snapshot 成环。R19 已把 `ExecutionFactProvider` 改为 `collect_execution_snapshot_for_plan_rows` 内局部 import；provider 再顶层 import `positive_op_ids` 不再形成运行时环。
- **分层红线(阻断 B / 0 AST 违规)**:repo 在 `data/repositories`,当前 import 仅 `core.models.*` + `.base_repo` + `.operation_execution_state_aggregation`(实测 repo:6-15)。`core.algorithms→core.services`、`core.models→core.services` 等越层均不存在,不得引入。repo 收 service 层 = 新增 data→service 越层 import = 违红线。
- **合规收口形态**:canonical 实现下沉到 core/models(最低层,snapshot/provider/repo 三者均可合法向下 import,且不成环)是唯一同时绕开 A+B 的形态——但触 red line #3"为已有概念新建第二模块",须 owner 裁断"models 安家是否算合法归位"。在裁断前,**保守解=provider 收口(同层)+ repo 保留私有版补注释**,零越层、零环。

## 11) 测试耦合(动手前先迁哪些测试)

- R19 已在既有测试文件内补直接/效果 parity。执行前“三函数零符号级直接测试”的缺口已关闭:
  1. 已为 `positive_op_ids` 补 §7 黄金用例(钉指纹排序契约),作为收口前的回归基线。
  2. 已补 provider missing 文案 parity。
  3. repo 保留私有版，已补"顺序无关"守卫测试。
- 间接覆盖在 snapshot/provider/repo 各自调用方的现有测试里(快照构建、facts 查询、batch 聚合)；本轮已补直接/效果 parity 并跑绿，不再留下“只靠间接信号”的缺口。

## 12) 前置依赖(先做哪些债 / 哪些 F 门)

1. **O04/O05/O37 已裁且 R19 已执行**:repo 保私有版 + 顺序无关注释/parity；provider 链按 R15→R19→R13。R15/R19/R13 均已 fixed。
2. **两共享文件串行纪律**:
   - execution_fact_provider.py:R19 已按符号重 rg 后执行；R13 也已在 R19 后按符号重 rg fixed。
   - operation_execution_event_repo.py:R19 已只保私有版并补“顺序无关”注释/parity，未改成 data→service import，未顺手删 R18 stub。
3. **E16 `__all__` 伪边登记**: R01/R46 不再作为 R19 前置；只需确认 R19 不误删 execution_snapshot.py 的 `positive_op_ids` 导出。
4. **F 门**:0 AST 越层 + 无导入环门必须在收口形态确定后通过(test_architecture_fitness);parity 测试门(§11)先建后改。
5. **批次定位**:registry 已回写为 R19 fixed。G09 provider 链已 fixed；G10 repo 链 R18 stub 护栏也已 fixed。
6. owner_pending=false:R19 不再因 owner 未裁而暂停；已补 parity 并完成。

## 对抗核验(第二双眼睛)

**核验日期** 2026-06-09 | **方式** 独立 rg/grep 回盘 + 三函数体逐行比对 + 导入边/分层/消费者重验 + 主动找反例。

**重 grep 命令与结果**:
- `rg -n "def positive_op_ids|return sorted|TYPE_CHECKING|ExecutionFactProvider" execution_snapshot.py` → :5 `TYPE_CHECKING`，:7-8 类型检查导入，:29 def / :42 `return sorted(out)`，:112 函数内局部 import `ExecutionFactProvider`(:111 防环注释)。
- `rg -n "def _positive_op_ids|positive_op_ids|missing" execution_fact_provider.py` → :14 import `positive_op_ids`，:69 def / :70 `return positive_op_ids(values)`，:126 missing 检查。
- `rg -n "def _positive_ids|return out|_batch_ids_by_op_ids|list_latest" operation_execution_event_repo.py` → :67 def / :80 `return out`(repo 私有版不排序)，:408(SQL IN)，:400/:402 stub loud raise 未动。
- `rg -n "__all__|positive_op_ids" execution_snapshot.py` → :123 `__all__` / :127 `"positive_op_ids"`；消费点 :80/:114。
- `rg -n "^from |^import " operation_execution_event_repo.py` → 仍无 `core.services` import；repo 未越层收 service。
- `rg -n "positive_op_ids|_batch_ids_by_op_ids_is_order_independent" tests/operation_execution` → 已命中 scope_read_contract 黄金用例/provider exact 文案测试，以及 foundation repo 顺序无关 parity。

**逐项裁决**:
- **行号** 一致:snapshot:29/:42、provider:69/:70、repo:67/:80、`__all__`:123-128 全部与正文 §1 吻合(provider/snapshot 行号经 2026-06-10 R13 收口+防环注释落地后现盘重测)。
- **收口形态** 复核通过:provider 已委托 sorted canonical；repo 保私有版，int 化 / `<=0` 排除 / `seen` 去重契约与 canonical 等效但不跨层导入。
- **导入环已消解 / 分层红线守住**:snapshot 仅局部 import provider，provider 顶层 import snapshot helper 可运行；repo 仍未 import service。
- **指纹依赖排序** 复核 :80→:89-93 sha256 路径属实,canonical 必须保 sorted;provider missing[0](:126-128)已由 exact 文案 parity 锁定;repo:410(R18 注释后现盘) SQL IN 顺序无关——三消费点敏感度判定全准。
- **承重误删风险:无**。load_bearing=false、lb_no_touch=null 与 registry 一致。档案修法全程为「补注释 + parity」,**未混入删除/统一/透传/加形参**;且主动把「sorted 行=事实承重」标出并约束等同承重待遇,守护到位。
- **owner_pending 纪律:守住**。旧待裁口径已被 O04/O05/O37 覆盖；当前为 owner_pending=false、status=fixed。R19 parity 与代码均已落地，不再排在 G09 待办里。
- **灵魂线** 复核:修法未新增兜底/静默回退/except 吞错;P5 收口约束「保 loud raise、parity 显式断言文案变更」符合不静默原则。

**2026-06-09 补记**:registry 的 owner_pending 与执行批次口径已按 O04/O05/O37 回写；若后续仍发现旧行号，以本档“按符号重 rg”纪律为准。

**结论:PASS → fixed**。行号已按 2026-06-09 现盘更新；provider 已收口、repo 保私有版、snapshot sorted 承重注释与 parity 已落；分层/导入环/灵魂线无违。2026-06-10 已补记：R13 也已 fixed，G10 的 R18 stub 护栏也已随后补注释 fixed。

**2026-06-10 深度复审补记**(20-agent 对抗核验后落档):
1. **repo 链生产死代码**:`_positive_ids`→`_batch_ids_by_op_ids` 全链无生产调用方(详见 §8 纠正)。本档"repo 保私有版避免 data 依赖 service"的论证对象实为死代码,顺序无关 parity 测试钉的是死方法——移交 **R18** repo stub 护栏批次一并裁决:删除 helper+parity 测试,或显式记"死代码暂留"。
2. **保序副本残留登记**:上层还有两份逐行同构的"正数+去重"清洗副本——`core/services/scheduler/gantt_service.py:258-269`(`_execution_facts_by_op_id` 内联)与 `web/routes/dashboard.py:51-62`(`_row_op_ids`),均**先于 R19 存在**、保输入序,与真相源 sorted 语义分叉。今日无害(产物仅作 include_op_ids 喂 provider,内部再经 `_positive_op_ids` 清洗,无顺序消费方),且 `test_no_new_local_parse_helpers` 的名字清单不追踪此类符号。后续统一时直接改调 `positive_op_ids` 即可(唯一行为差异是 include_op_ids 顺序,无依赖)。
3. **防环结构加固**:`execution_snapshot.py:111` 已补"勿上提"承重注释(2026-06-10)——此前防环只靠无注释的局部导入约定;且 commit message 引用的 `test_no_circular_service_dependencies` 按 core/services 下一级包聚合并跳过同包边,其正则也不匹配相对导入写法,**对 provider↔snapshot 这类同包(scheduler)模块环结构性失明**,不能当防环证据。若要 gate 真护住,需把环检测降到模块级并覆盖相对导入。
