# R50 逐债依赖与安全批次档案

> 标题：mean_positive 死函数 —— 生产用内联均值，该助手仅测试续命
> bucket=B05 / home=B05 / cluster=C01 / pathology=P6(死代码,直删类) / severity=low
> partition=core-algorithms / load_bearing=false / owner_pending=false / needs_adversarial=false
> 同文件兄弟：R49、R51 ｜ 计划批次：Batch-7(B06 dispatch_rules.py 三债同批)
> ✅ 2026-06-08 B 执行核销：R50/G24 已 fixed。`mean_positive` 与仅它使用的 `import statistics` 已删除；`import math` 保留并继续服务 `build_dispatch_key`；非有限 `proc_hours` 回退测试保留，只退 `mean_positive` 续命断言。以下旧行号保留为执行前证据，后续勿重复施工。

---

## 1) 执行前真实 file:line(回盘)+漂移记录

> 2026-06-08 执行后说明：本节旧行号仅作历史留档；当前 `mean_positive` 与 `statistics` import 已删除。

回盘命令：`rg -n 'def mean_positive' core/ web/ data/` + `rg -n 'mean_positive' --type py`。

| 符号 | registry old_location(勿信) | 当前真实位置(回盘) | 漂移 |
|---|---|---|---|
| `def mean_positive` | dispatch_rules.py:112 | **core/algorithms/dispatch_rules.py:112-132**(函数体含 :132 `return float(statistics.fmean(vals))`) | **行号无漂移**(:112 命中)；但 **路径有澄清**：registry 写 basename `dispatch_rules.py`，真实全路径 = `core/algorithms/dispatch_rules.py`(非 `greedy/dispatch/`) |
| `import statistics` | (推测 :4) | **core/algorithms/dispatch_rules.py:4**，全文件仅 :132 使用 | 无漂移 |
| 真实均值收口点(reference_chain) | 报告写 `sgs.py:150` | **core/algorithms/greedy/dispatch/sgs.py:150 `_average_proc_hours`**(:169 `sum(samples)/float(len(samples))`、:170 `ctx.increment("dispatch_key_avg_proc_hours_fallback_count")`) | **顶层 `core/algorithms/sgs.py` 不存在**；真路径在 `greedy/dispatch/sgs.py`，但同行号同语义，结论成立 |
| 测试续命点 | regression…safe.py:20/62/63 | `tests/resource_dispatch/test_dispatch_rules_nonfinite_proc_hours_safe.py:20`(import 名)、`:61-63`(注释+用例) | 无漂移 |

证据：`core/algorithms/dispatch_rules.py:112` `def mean_positive(values: Dict[str, float]) -> float:`；`:132` `return float(statistics.fmean(vals))`；`:4` `import statistics`；文件总长 `wc -l = 132`(mean_positive 为**文件最后一个函数**，:132 即 EOF)。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = fixed`（2026-06-08 B 执行核销）。
- 执行结果：`mean_positive` 生产死函数已删除；`statistics` import 已删除；`math` import 仍保留并被 `build_dispatch_key` 使用。
- 测试结果：`tests/resource_dispatch/test_dispatch_rules_nonfinite_proc_hours_safe.py` 只退 `mean_positive` import 和断言，保留 `build_dispatch_key` 非有限 `proc_hours` 回退契约。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6 死代码(直删类)**：`mean_positive` 全仓零生产消费者；真实 avg_proc_hours 由 `greedy/dispatch/sgs.py:150 _average_proc_hours` 内联 `sum/len` 计算(:169)，与本助手无调用关系。该助手语义(只对 `>0` 且有限值取均值，空→0.0)在生产路径上无任何调用点续命，仅被回归测试 :62 调用而存活。
- 严重度 **low**：纯残渣，删除零生产影响(phase1_blast.downstream_effect=「纯死残渣，删除零生产影响」)。
- 桶 **B05 / home B05**；簇 **C01**(同 dispatch_rules.py / sgs.py 死代码-解析器簇)。

## 4) 修法类

**已执行：直删类(P6)**——非收口、非承重、非 raise。owner_pending=false：

- **生产侧已删**：`core/algorithms/dispatch_rules.py` 旧 `def mean_positive` 整体删除。
- **import 清理已完成**：`statistics` 全文件零残留后删除；`import math` 保留。
- **测试侧已外科退场**：`test_dispatch_rules_nonfinite_proc_hours_safe.py` 仅删除 `mean_positive` import 与续命断言，保留 `DispatchInputs` / `DispatchRule` / `build_dispatch_key` 回退契约。

## 5) 同文件碰撞债(siblings)

同文件 `core/algorithms/dispatch_rules.py` 共驻 3 债，**全部 planned**，须**同批(Batch-7)**改：

| sibling | 改哪段 | 行号位置 | 与 R50 撞行号? |
|---|---|---|---|
| **R49**(死别名) | `_due_exclusive = due_exclusive` @ **:25** | 文件**顶部** | **不撞**——R50 删 :112-132(文件**末尾**)，删除点在 R49 之**下游**，删 R50 不影响 :25 行号；删 R49 会使 :25 以下整体**上移 1 行**，把 mean_positive 从 :112 推到 :111 |
| **R51**(宽容解析器) | `def parse_dispatch_rule`(及可能的 `parse_strategy`)@ **:28+** | 文件**中上部** | **不撞**——同理在 R50 删除点之上 |

**协调结论**：
- **物理 dict 键位移风险 = 无**：本文件 R50 删除区是**函数定义**(112-132)，不含任何 dict 字面量；R49/R51 territory 也是定义/别名，不存在「dict 键位移」类碰撞。
- **行号互撞风险 = 单向**：唯一风险是「拿旧行号补丁去打已被上游删动过的文件」。因 R50 在最末尾，**R50 应最后删**或三债用同一次 PR/diff 一次性改完(planned_batch_hint 即此意)。推荐顺序：先删上游 R49(:25)、R51(:28+)，最后删 R50(:112-132)——这样每一步都不会让先前补丁的行号失效（末尾删除不回推上游行号）。反之若先删 R50，再删 R49/R51 也安全，但中途任何「按 :112 抓 mean_positive」的脚本须重算。
- 三债**同批同 PR** 是硬约束(planned_deps_hint 明示)，避免任一债单独 merge 后另一债的 file:line 漂移。

## 6) 同收口点债

- **R50 是直删，不收口到任何统一符号**——无「同收口点」竞争。
- 唯一语义近邻是 `greedy/dispatch/sgs.py:150 _average_proc_hours`(真实生产均值)，但 R50 **不收口到它、不引用它**，只是用它来论证 `mean_positive` 是死残渣。删 R50 **不触碰** sgs.py，二者无顺序敏感性。
- 注：sgs.py 上另有干扰边 R25(`interference_edges` why=`file:sgs.py`)，但 R25 与 R50 **不同文件改动**(R50 改 dispatch_rules.py，不动 sgs.py)，无收口竞争、无顺序约束。

## 7) 收口行为差异及 parity 测试

**不适用 —— R50 为直删而非收口，无新旧两路 parity 问题。**

- 删除前后唯一行为差异：`mean_positive` 这个**测试可见符号消失**。生产行为零变化(零生产消费者)。
- 不需要 parity 测试。需要的只是**回归保护**：确认删除后 `regression_dispatch_rules_nonfinite_proc_hours_safe.py` 的**保留部分**(:26-59 build_dispatch_key 回退契约)仍 `print("OK")` 通过——即删完跑一次该回归脚本自证未误伤活契约。
- 反例视角(供复核「真死」)：若有人误以为 `mean_positive`(忽略 Inf/≤0，空→**0.0**) 与 sgs 的 `_average_proc_hours`(`sum/len`，无样本→走 fallback `increment` 计数) 是同一路，**实为不等价**：边界 `{}`→mean_positive 返回 `0.0` 而 sgs 走 fallback 分支并打点。但因 `mean_positive` 零生产调用，此差异**不进入任何生产路径**，故仍判直删，无需迁移其语义。

## 8) 爆炸半径

**9 字段(phase1_blast)落盘对照 + 回盘核实：**

- **生产消费者(prod_consumers)**：**0**。回盘 `rg 'mean_positive' --type py` 在 `core/ web/ data/ scripts/` 生产侧命中 = 0，仅 dispatch_rules.py:112 定义。真实 avg_proc_hours 在 `greedy/dispatch/sgs.py:166-170` 内联 `sum/len`。callgraph `edges.json` 对 `mean_positive` 零命中(无入边) —— 印证孤儿。
- **测试消费者(test_consumers)**：`tests/resource_dispatch/test_dispatch_rules_nonfinite_proc_hours_safe.py:20`(import 名)/`:62`/`:63`(唯一调用与断言)。
- **下游影响(downstream_effect)**：纯死残渣，删除零生产影响。
- **co_change_within_group**：R49、R51(同文件同批)。
- **contract_topology_tests**：none —— regression…safe.py 是**行为回归**(验 build_dispatch_key 非有限回退)，非拓扑契约；R50 删除不削弱该回归(只删其中 mean_positive 那 2 行)。
- **fix_invalidation_risk**：none(就 R50 本身)；唯一外部约束是同文件三债同批改避免行号互撞(见 §5)。
- **lb_colocation_danger**：none(本文件无承重债，见 §9)。
- **location_status**：✅ 准(dispatch_rules.py:112)。
- **灾难链(删错/统一错会静默炸什么)**：
  - 若**误删 import math(:3)**而生产其他位置用 math → ImportError 启动即炸(**显式**，非静默)。规避：R50 删除范围**不含 :3**，且删前 grep math 用量。
  - 若**误删测试 :26-59 build_dispatch_key 回退用例** → 非有限 proc_hours 回退契约失去回归保护，将来 sgs/dispatch_rules 退化为「把 Inf 候选错误置顶/置底」**不会被测试拦住**(**静默**回归)。规避：§4 明列 :26-59 为禁区。
  - 若**误删 statistics 但未删 mean_positive**(顺序写反) → mean_positive 内 :132 NameError，回归脚本 import 即炸(**显式**)。规避：先删函数体再删 import。

## 9) 承重毗邻标记

- 本债 `load_bearing=false`，`lb_no_touch=null`。
- 同文件 `dispatch_rules.py` 回盘 `rg '我是故意的|load.?bearing|承重|LB'` = **NONE**，无承重债毗邻(phase1_blast.lb_colocation_danger=none 印证)。
- **禁区行(非承重，但属其他债/活契约，R50 不得碰)**：
  - `core/algorithms/dispatch_rules.py:25`(R49 territory `_due_exclusive`)、`:28+`(R51 territory `parse_dispatch_rule`)——不在 R50 删除范围。
  - `core/algorithms/dispatch_rules.py:3 import math`——R50 不删。
  - `tests/…nonfinite_proc_hours_safe.py:15-59`(`main`/`find_repo_root`/`build_dispatch_key` 回退契约)、`:64-65`——R50 仅动 :20 import 名 + :61-63。

## 10) 分层 / 导入环风险

- **删除类不新增任何 import** → 零越层、零新环风险。
- 反向核实：删 mean_positive 后剩余 import(`math` 保留、`statistics` 删除、`.greedy.date_parsers`、`.priority_constants` 不动)均为 core 内/标准库，partition=core-algorithms，无 `core.algorithms→core.services` 或 `core.models→core.services` 越层(分层红线 0 违规)。
- AST 风险 = 0(无新 import 边)。

## 11) 测试耦合

动手前需迁/改的测试(就 R50)：
- **改**`tests/resource_dispatch/test_dispatch_rules_nonfinite_proc_hours_safe.py`：:20 去 `mean_positive` 名、删 :61-63。**此为唯一直接耦合 mean_positive 的测试**。
- 同文件其余 9 个 import dispatch_rules 的测试(`test_schedule_params_direct_call_contract.py`、`regression_sgs_*`、`test_greedy_refactor_contracts.py`、`regression_dispatch_rule(s)_*` 等)**均不引用 mean_positive**(回盘 `rg 'mean_positive' tests/` 仅命中上述 1 文件 3 行)，无需迁移。
- 删完自证：跑 `tests/resource_dispatch/test_dispatch_rules_nonfinite_proc_hours_safe.py` 应 `print("OK")`(保留的 build_dispatch_key 回退契约仍通过)。

## 12) 前置依赖

- **前置已闭合**：不依赖任何 F 门。
- **批次约束已完成**：R50 已随 R49、R51 在同一 G24 原子 diff 内删除。
- owner_pending=false；无需 owner 裁断。

## 2026-06-08 执行留档

- 已删：`mean_positive`、`import statistics`、测试中的 `mean_positive` import 与断言。
- 已保留：`import math`、`DispatchInputs`、`DispatchRule`、`build_dispatch_key`、非有限 `proc_hours` 回退断言。
- 残留检查口径：`mean_positive` 在生产/测试 Python 源内应零命中。

---

## 执行前对抗核验(历史第二双眼睛)

> 以下为 2026-06-05 执行前的只读核验记录，用来说明为什么当时允许直删；2026-06-08 执行后，`mean_positive` 与 `statistics` import 已不存在，勿当成当前代码事实。

**结论：PASS**(默认怀疑，独立重 grep 后未找到反证)。核验人：对抗核验 agent。

### 重跑命令与结果(全部在当前工作区独立执行)

1. `rg -n 'def mean_positive' core/ web/ data/ scripts/` → **唯一命中** `core/algorithms/dispatch_rules.py:112`。行号 :112 一致，零漂移。
2. `rg -n 'mean_positive' --type py` → 全仓 **4 命中** = 定义 `dispatch_rules.py:112` + 测试 `regression…safe.py:20/61/63`(及调用 :62)。**生产引用 = 0**，真死叶子，档案 §8 prod_consumers=0 成立。
3. `rg -n '\bstatistics\b' core/algorithms/dispatch_rules.py` → 仅 `:4 import` + `:132 statistics.fmean` 两处。删 mean_positive 后 statistics 全文件零引用，可一并删 import，档案 §4 前置条件成立。
4. `rg -n '\bmath\b' core/algorithms/dispatch_rules.py` → `:3 import` + `:78`(build_dispatch_key 非有限判定) + `:95`(ATC `math.exp`) + `:128`(mean_positive 内)。**math 在 :78/:95 有 mean_positive 之外的真实用量**，故 `import math`(:3) **必须保留**——档案 §4/§9 将 :3 划入禁区正确。⚠️此处确认：math import 是**真实承重边**(误删→启动期 ImportError，§8 灾难链已标显式炸)，档案处置正确，未误删。
5. Read 函数体 :112-132 → docstring + `math.isfinite & >0` 过滤 + 空→`0.0` + `statistics.fmean`，与档案描述一致；:132 即 EOF(`wc -l`=132)，mean_positive 为文件最末函数，末尾删除不回推上游行号，§5 协调结论成立。
6. `rg -n '_due_exclusive|def parse_dispatch_rule' core/algorithms/dispatch_rules.py` → R49 `_due_exclusive` @ **:25**、R51 `parse_dispatch_rule` @ **:28**，均在 R50 删除区(:112-132)**上游**，不撞行号。§5 单向行号互撞分析成立。
7. Read `tests/resource_dispatch/test_dispatch_rules_nonfinite_proc_hours_safe.py` 全文(71 行) → :20 import 行确为 `DispatchInputs, DispatchRule, build_dispatch_key, mean_positive`(R50 仅去 mean_positive 一名)；:61 注释 + :62-63 用例为 mean_positive 唯一续命；:25-59 为 build_dispatch_key 非有限 proc_hours 回退活契约(:58-59 断言 primary 有限且按 avg 回退)。**禁区划定正确**。`rg -l 'mean_positive' tests/` → 仅此 1 文件，§11 测试耦合范围成立。
8. `find core -name sgs.py` → 真路径 `core/algorithms/greedy/dispatch/sgs.py`，顶层 `core/algorithms/sgs.py` **NOT_EXIST**(档案已正确澄清)；`_average_proc_hours` @ :150、`sum(samples)/float(len(samples))` @ :169、`ctx.increment("dispatch_key_avg_proc_hours_fallback_count")` @ :170 全部命中，真实生产均值收口确在此。R50 **不收口、不引用** sgs.py，仅用作论证死残渣，§6 无收口竞争成立。

### 对抗性挑战(主动找反例)

- **「mean_positive 与 _average_proc_hours 实为同一路、删一个会断生产」？** 否。前者忽略 Inf/≤0、空→`0.0`；后者 `sum/len` 无样本走 fallback 打点，语义不等价；且 mean_positive 零生产调用，差异不进生产路径。判直删成立，无需迁移语义。
- **修法是否混入删除/统一/透传/加形参以外的危险动作？** 否。R50 为纯 P6 直删 + 外科退测试，无收口、无统一、无加形参、无兜底/静默回退新增(灵魂线无违反)。
- **owner_pending 擅自给终态/分批？** registry owner_pending=false，给终态修法合规，未越权。
- **分层/导入环？** 删除类零新增 import，AST 风险 0，无 core.algorithms→core.services 越层。一致。

### 核验判定

| 维度 | 结论 |
|---|---|
| 行号复核 | **一致**(:112 def / :132 fmean / :4 import statistics / :3 import math / R49 :25 / R51 :28 / sgs.py:150/169/170 全部命中) |
| 真死叶子(零生产引用) | **属实**(grep=0) |
| 承重判断 | **一致**(load_bearing=false 成立；唯一真实承重边 `import math` 已正确划禁区，未误删) |
| 收口点真实性 | **N/A**(直删不收口；语义近邻 sgs.py 真实存在且不被触碰) |
| 分层/导入环 | **一致**(零新增 import) |
| 承重误删风险 | **无** |
| 需第三方裁断 | **n** |
