# R64 逐债档案 — `_has_navigation_date_range` 新建即死全仓零引用

> 主文件: `web/viewmodels/scheduler_navigation_links.py` | 桶 B17 | 簇 C01 | 病理 P6 | 严重度 low | 承重 false | owner_pending false
> 本档案只读不改,仅产计划。所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。

## 字段 1 — 当前真实 file:line(回盘)+ 漂移记录

回盘命令: `rg -n "_has_navigation_date_range" --type py` → 全仓唯一命中:
- `web/viewmodels/scheduler_navigation_links.py:66` — `def _has_navigation_date_range(context: Dict[str, Any]) -> bool:`
- `:67` — `return _has_value(context.get("date_from")) and _has_value(context.get("date_to"))`

漂移记录: **无漂移**。old_location_DONT_TRUST = `:66`,当前实际 = `:66`。def 头 :66、body :67。文件总长 205 行(`wc -l`)。registry `location_status:✅准` 与回盘一致。

call-site 回盘: `rg`(–type py)对符号名全仓仅 1 处命中,即 def 自身,**零调用点**。callgraph(`docs/_panorama_data/callgraph_2026-06-05/edges.json`)交叉验证:符号仅作为 `from`(主调方)出现一条边 `_has_navigation_date_range → _has_value`(kind=local,同文件内调用),**从不作为 `to`(被调方)** 出现 —— 双源(rg + callgraph)共证:无任何代码调用它。

> registry `planned_fix_hint` 内 "_has_value 仍被 :49/:63/:67 多处用" 有一处口误:**:63 用的是 `_text` 不是 `_has_value`**(见 :63 `_text(context.get("plan_role")) != ROLE_ADOPTED`)。但不影响结论 —— `_has_value` 真实用点为 :49(`_has_navigation_context` 内)与 :67(本死 def 内),删 :67 后 :49 仍在,`_has_value`(:35)不会成孤儿。
## 字段 2 — 当前状态 + 与 status evidence 一致性

`status_2026_06_05 = planned`。current_evidence_2026_06_05: "web/viewmodels/scheduler_navigation_links.py:66-67 死 helper `_has_navigation_date_range` 原样存在;全仓 grep 仅 :66 def 一处零调用,未删"。

回盘 **完全一致**: def 原样在 :66-67、零调用、未删除。状态为 planned(尚未动手),与证据相符。无 in_progress 痕迹(无半截删除、无 NameError 隐患)。

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- **病理 P6**: 本提交新建即死,全仓零引用(含 tests),收口/拆分遗留死码。`_has_navigation_date_range`(:66-67)是一个语义自洽的小 helper(判断 context 是否同时有 date_from 与 date_to),但从被建出来那一刻起就没有任何调用方 —— 典型"写了没接线"的死码。
- **严重度 low**: 仅可读性/误导债。运行期零影响(死码不在任何执行路径上)。删与不删行为完全等价。
- **桶 B17** / **home B17**: 独立叶子债桶(Phase1 §4 标注 B16+B17 为独立叶子,无关键路径门控)。
- **簇 C01**: 同文件聚簇(`scheduler_navigation_links.py` 内 R64/R65,以及跨文件同链的 R42)。
## 字段 4 — 修法类

**直删(纯叶子,非承重 → 无需『我是故意的』注释)**。

- 删除范围严格限定 **:66-67** 这一个 def(def 头 + 单行 body),并收掉相邻一行空行: 当前 :64-65 是两空行、:68-69 是两空行(`cat -A` 已证)。删 def 后须保持 `_has_navigation_context`(止于 :63)与 `_use_plain_scheduler_chrome`(:70)之间恰好 2 空行(PEP8 顶层 def 间距),避免 E303(多余空行)/ E302。即:删 :66-67 后再吃掉 :64-65 或 :68-69 其中一组里的一行,使 :63 与 :70 之间净留 2 空行。
- **禁碰相邻函数**: `_has_navigation_context`(:47-63,被 :114/`_use_plain_scheduler_chrome`:71 真用)、`_use_plain_scheduler_chrome`(:70-71)、`_has_value`(:35-36)。
- **无 call-site 需改**: 零调用点(rg + callgraph 双证),不存在"删 def 留悬空调用"风险。
- **无 import 孤儿**: 本 def 仅本地调用 `_has_value`(:35,本文件定义),不引入任何 import;删它不使任何 import 变孤儿。
- 非 P5 收口债 —— 无收口点。非 P4 —— 不涉及兜底/raise。纯直删。

## 字段 5 — 同文件碰撞债

同文件兄弟 = **R65**(`same_file_siblings`),另有同文件干扰边 **R42**(`interference_edges`, same_file=true / same_symbol=false)。

### R65 — `_target_url` 死分支三件套(same_file, **不同符号**)
- 改哪段: R65 是**原子三件套**: ① 删 `def _target_url`(当前 **:74-78**); ② 把 **:160** 的 `_plain_link(label, plain_url or _target_url(...), ...)` 化简为 `_plain_link(label, plain_url, ...)`; ③ 删两个孤儿 import: **:4** `from urllib.parse import urlencode`、**:6** `from .scheduler_workbench_link_query import query_for_target`。(:160 当前态已 grep 证实仍是 `plain_url or _target_url(context, str(target_page), view=view)` 未化简。)
- **谁先谁后 / 撞行号**: R64 删 :66-67(位于 R65 的 :74-78 **上方**)。一旦 R64 先落,R65 的 `_target_url` 会从 :74-78 上移 ~2-3 行(取决于吃掉几行空行),:160 同步上移。**反之**,R65 先落则删 :4/:6 两行 import + 一段 def,会把 R64 的 :66 大幅上移(import 在更上方)。→ **两条都改本文件且都做删除,行号互相漂移**。
- **结论**: 二者**逻辑零耦合**(各删各的 def,即使分开提交也不会 NameError —— R64 的 `_has_navigation_date_range` 不被 R65 改的任何符号引用,R65 改的 `_target_url`/import 也不碰 R64)。但为避免**行号二次漂移**,强建议 **R64 + R65 打成同一原子提交**(planned_deps_hint / co_change_within_group=[R65] 一致)。同次提交时建议基于原始行号一次性算好补丁、自上而下删(先 :66-67 再 :74-78),避免边删边算偏移。
- **不撞 dict 键位移**: 两债都只删函数体,不动任何 dict 字面量的键序,无键位移风险。

### R42 — plan_id 死面包屑(same_file, 但本文件不改任何行)
- R42 是跨文件全链债(navigation_context.py / reports_page_support.py / scheduler_workbench_links.py / scheduler_workbench_link_query.py)。其 `interference_edges` 标 same_file 仅因 `scheduler_navigation_links.py` 出现在边集合里,但 R42 的实际修改点**不落在本文件的 :66-67 / :74-78 / :4 / :6**。→ 与 R64 **无行号碰撞、无逻辑耦合**,可完全独立。

## 字段 6 — 同收口点债

**无**。R64 是直删债,不收口到任何符号;`_has_navigation_date_range` 本身也不是任何其他债的收口目标(零被调,callgraph 仅 `from` 不作 `to`)。R65 删的是另一个独立 def,二者不共享收口点。无顺序敏感性(除字段 5 述及的纯文本行号漂移外)。
## 字段 7 — 收口行为差异及所需 parity 测试

**不适用(N/A)**。R64 是直删,不是收口,**无"新旧两路"**之分 —— 删除前后行为完全一致(死码本就不在任何执行路径)。无需 parity 测试。删除后唯一须验证的是:① import/符号无悬空(已证 `_has_value` 仍被 :49 用、本 def 无 call-site);② 文件能正常 import(冒烟即可);③ lint 通过(2 空行间距,避免 E303/E302)。

## 字段 8 — 爆炸半径

- **生产消费者**: **零**。`prod_consumers = ["生产零引用(已grep证实)"]`;rg + callgraph 双证:`_has_navigation_date_range` 全仓仅 :66 def,从不被任何符号调用。
- **测试消费者**: **零**。`test_consumers = ["无"]`;`rg "_has_navigation_date_range" tests/` 零命中(包含在全仓 rg 的唯一命中即 def 自身)。
- **downstream_effect**: 仅可读性/误导,无运行期影响。删除后行为零变化。
- **灾难链(删错/统一错会静默炸什么)**: 风险极低。唯一可设想的"删错"是**手滑多删一行波及相邻 `_use_plain_scheduler_chrome`(:70-71)或 `_has_navigation_context`(:47-63)**—— 这两个是**真活函数**(`_has_navigation_context` 被 :49 自身递归引用关系外,被 `build_workbench_navigation_links`:114 与 `_use_plain_scheduler_chrome`:71 调用;`_use_plain_scheduler_chrome` 被 :114 调用)。误删它们会让导航链路 NameError / 行为改变(plain chrome 判定失效)→ 但这属于操作失误而非债本身的爆炸半径。**只要删除严格锚定 `def _has_navigation_date_range` 这一个符号,爆炸半径=0。**
- **fix_invalidation_risk**: none(registry 一致)。

## 字段 9 — 承重毗邻标记

- **本文件无承重**: `lb_colocation_danger = none — scheduler_navigation_links.py 不在承重清单`;R64 `load_bearing=false`、`lb_no_touch=null`。
- **禁区行(非承重,但为防误删而列)**: 严禁触碰 `_has_navigation_context`(:47-63)、`_use_plain_scheduler_chrome`(:70-71)、`_has_value`(:35-36)、`_text`(:31)—— 这些是同文件的活函数,与 R64 仅相邻、非承重但删错即 NameError/行为改变。唯一可删 = `def _has_navigation_date_range`(:66-67)及其紧邻一行空行。
## 字段 10 — 分层 / 导入环风险

**零风险**。R64 是删除,不新增任何 import。本文件现有 import(回盘 :1/:3/:4/:6/:7)全为 stdlib(`__future__`/`typing`/`urllib.parse`)+ 同层 `web.viewmodels` 兄弟模块(`.scheduler_workbench_link_query`、`.scheduler_workbench_links`),**无任何跨层(core.services / core.algorithms / core.models)import**。删一个本地 def 不可能引入越层 import 或导入环。分层红线 0 AST 违规保持。

## 字段 11 — 测试耦合

- **动手前无需迁任何测试**。`rg "_has_navigation_date_range" tests/` = 0(死符号零测试覆盖)。
- 全仓引用本模块的 2 个测试 `tests/web_pages/test_reports_workbench_navigation_contract.py`、`tests/schedule/route_view/test_scheduler_navigation_unknown_plan_role_contract.py` 都只通过 `web.navigation_context.build_scheduler_navigation_links` 走公开入口(:8/:153/:159/:172 等),**从不触碰 `_has_navigation_date_range`**。删除 R64 对它们零影响。
- 建议:落删后跑这 2 个 regression 做冒烟,确认导航链路公开行为不回归(虽与 R64 无逻辑关系,但同文件改动顺手验证)。

## 字段 12 — 前置依赖

- **无硬前置债**: B17 是独立叶子,无关键路径门控(Phase1 §4)。`planned_batch_hint = Batch-16`,Batch-1 后任意时点可落。
- **F 门**: 仅需常规 lint(E303/E302 间距)+ import 冒烟 + 上述 2 个 navigation regression 通过即可,无专门契约门。
- **协调建议(非硬依赖)**: 与 **R65** 同文件,建议同一原子提交以减少行号二次漂移(见字段 5)。但二者逻辑独立,即使错序/分提交也不会 NameError。R42 完全独立,无需协调。
- **owner_pending = false**: 无需 owner 裁断,可直接给出终态修法(直删 :66-67)与执行批次(Batch-16,与 R65 同提交)。

## 对抗核验(第二双眼睛)

> 核验日期 2026-06-05,只读不改,默认怀疑;低危纯死叶子债(severity=low,非承重,非owner_pending)→ 快核(行号 + 直删真零引用 + 测试耦合)。

**重 grep 命令与结果**:
- `rg -n "_has_navigation_date_range" --type py` → 全仓唯一命中 `web/viewmodels/scheduler_navigation_links.py:66`(def 头),`:67` 为 body。**零漂移**,与档案/registry `old_location :66` 完全一致。
- `rg -n "_has_navigation_date_range" tests/` → exit=1 零命中。**测试零耦合**,删除不需迁任何测试。
- `rg -n "_has_value" web/viewmodels/scheduler_navigation_links.py` → :35(def)/:49(`_has_navigation_context` 真用)/:67(本死 def)。**删 :67 后 :49 仍在,`_has_value` 不成孤儿**,确证。
- `sed -n '30,80p'` 实读 :66-67 = `def _has_navigation_date_range(context)` + 单行 `return _has_value(date_from) and _has_value(date_to)`;相邻活函数 `_has_navigation_context`(:47-63)、`_use_plain_scheduler_chrome`(:70-71)、`_target_url`(:74-78,即 R65)原样在位。`wc -l` = 205,与档案一致。

**逐项挑战结论**:
- 行号: 一致(无漂移)。
- 调用者/消费者: 双源(rg + 档案引 callgraph 仅作 `from` 不作 `to`)共证零调用、零测试;无反例。
- 承重: load_bearing=false / lb_no_touch=null,非承重清单。修法为纯直删,**未混入删除以外的统一/透传/加形参/兜底**,无承重误删风险。
- 分层/导入环: 删本地 def 不新增 import,无越层、无环。
- 收口点: N/A(P6 死码非 P5 收口债,无新旧两路,无需 parity);非 P4 不涉兜底/raise,不违灵魂线。
- owner_pending: false,档案给终态修法+批次属合规(无擅自裁断)。
- 唯一隐患(档案已述): 手滑波及相邻活函数 → NameError;锚定单符号 `def _has_navigation_date_range` 即规避,属操作失误非债爆炸半径。
- registry `planned_fix_hint` :63 口误(实为 `_text`)系档案对 registry 的主动纠正,不影响删除结论。

**核验结论: PASS**。行号一致,直删真零引用,测试零耦合,非承重无误删风险,无需第三方裁断。
