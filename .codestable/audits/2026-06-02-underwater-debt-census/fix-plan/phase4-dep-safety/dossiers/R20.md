# R20 逐债档案 — service 层 operation_execution_labels.py 纯转出垫片

> 桶 B13 / 簇 C01 / 病理 P3 / 严重度 low / 承重 false / owner_pending false
> 回盘日 2026-06-05；本档只读不改、只产计划。

## 1. 当前真实 file:line（grep 回盘 + 漂移记录）
**垫片本体（删除目标）**
- `core/services/scheduler/operation_execution_labels.py:1-35` — 实测 35 行（registry current_evidence 说"36行"，差 1 行，无尾换行，**轻微漂移**）。`:1-17` 从 `core.models.operation_execution_labels` import 15 符号，`:19-35` `__all__` 原样转出 15 符号，零新增、字节级转发。location_status 标"✅准"——本体行号准确。

**生产消费方（经垫片，3 处）— registry 行号全部漂移：**
| 消费方 | registry 旧线索 | 回盘实际 | 漂移 |
|---|---|---|---|
| `core/services/scheduler/operation_execution_feedback_service.py` | :48 / :50 | **:52** `from .operation_execution_labels import (` | +2~+4 |
| `core/services/scheduler/operation_execution_feedback_support.py` | :26 / :27 | **:24** `from .operation_execution_labels import (` | -2~-3 |
| web 路由侧 | planned_fix_hint 说 `..._routes.py:16`；prod_consumers 说 `..._context.py:11` | **`web/routes/domains/scheduler/scheduler_resource_dispatch_execution_context.py:11`** `from core.services.scheduler.operation_execution_labels import execution_action_label` | prod_consumers 准；**planned_fix_hint 写错文件名**（见 §4 矛盾） |

**测试消费方（经垫片，2 处）— 行号漂移：**
| 测试 | registry 旧线索 | 回盘实际 | 漂移 |
|---|---|---|---|
| `tests/regression_operation_execution_event_foundation.py` | :17 | **:21** `from core.services.scheduler.operation_execution_labels import (` | +4 |
| `tests/regression_plan_vs_actual_review.py` | :10 | **:13** `from core.services.scheduler.operation_execution_labels import action_to_event_type` | +3 |

**文档门禁 pin — registry 行号漂移：**
- `tests/regression_aps_three_gap_docs_quality_gate.py`：registry 说 `:122 KEY_PYTHON_FILES + :181 断言`，实测 `:115 KEY_PYTHON_FILES`（元组起）、**`:128`**（pin 路径 `"core/services/scheduler/operation_execution_labels.py"`）、`:184` 断言函数 `test_developer_guide_lists_regression_tests_and_key_python_files`、**`:188`** `missing_python = [...]` + `:191 assert missing_python == []`。漂移约 +6~+7。
- 指南正文：`docs/dev/aps_three_gap_quality_gate.md:151` 真有该路径条目（registry blast 未给行号，本档补全）。

**漂移总结：本体行号准（±1），全部 5 个消费方 + 文档门禁的 registry 行号均漂移（±2~+7），且 planned_fix_hint 误写 routes.py（实为 context.py）。**

## 2. 当前状态 + 与 status evidence 一致性
- registry `status_2026_06_05` = **planned**；current_evidence 描述（"仍是 35 行纯转出垫片，3 生产 + 2 测试经 shim 未迁，B01 的 LB01 承重注释前置未落地"）与回盘**完全一致**：垫片仍在、5 处经垫片消费方全部健在、无任何已动手痕迹。
- 状态自洽，无"声称已修实未修"或"声称未动实已动"的矛盾。本债处于纯 planned，可安全排期。

## 3. 病理 / 严重度 / 桶 / 簇
- **病理 P3** — 不做事的 service re-export 垫片。`core/services/scheduler/operation_execution_labels.py:1-35` 整文件无任何逻辑，纯把 model 的 15 符号原样转出。属"半截迁移转发层"残留。
- **分区内不一致（P3 的具体表征）**：同一概念，分区内 **6 处直连 model**（`gantt_tasks.py:8`、`data/repositories/operation_execution_state_builder.py:18`、`web/viewmodels/scheduler_resource_dispatch_execution.py:10`、`resource_dispatch_execution_enrichment.py:5`、`resource_dispatch_actual_record_service.py:8`、`resource_dispatch_actual_import.py:9`）vs **5 处经垫片**（feedback_service:52 / feedback_support:24 / context:11 / 2 测试）。同一份标签函数两条进货路，是典型语义/路径漂移债。
- **严重度 low** — 字节级转发、无行为分歧、无承重，纯洁癖/记账问题；不修也不爆，修了减一处转发层 + 统一进货路。
- **桶 B13 / 簇 C01** — 与 R08/R09/R12 同簇（但同文件干扰边经核为假，见 §5）。
- **partition: scheduler-exec-diag**。

## 4. 修法类
**修法类：直删垫片 + 退测试（方向 A）**。owner_pending=false、承重=false，可给终态修法。

**与现状对齐的论证**：6 处已直连 model 是分区"主流"，垫片只剩 5 处依赖。删垫片把这 5 处改直连，让全分区统一进货 `core.models.operation_execution_labels`，改动小于方向 B（B = 把 6 处直连改走垫片，逆主流、改动大、且把转发层扶正违背"减层"目标）。**故选 A**。

**5 处 import 改写（垫片→model）：**
1. `feedback_service.py:52` 相对 `from .operation_execution_labels import (...)` → `from core.models.operation_execution_labels import (...)`（**禁区：只改 :52 起的 import 块，绝不碰 LB01 承重区，见 §9**）。
2. `feedback_support.py:24` 同上改写。
3. `scheduler_resource_dispatch_execution_context.py:11` 绝对 service 路径 → `from core.models.operation_execution_labels import execution_action_label`。
4. `tests/regression_operation_execution_event_foundation.py:21` → 改连 model。
5. `tests/regression_plan_vs_actual_review.py:13` → 改连 model。

**删本体 + 摘门禁记账（缺一则文档门禁红）：**
6. 删 `core/services/scheduler/operation_execution_labels.py` 整文件。
7. 摘 `tests/regression_aps_three_gap_docs_quality_gate.py:128` 的元组项 `"core/services/scheduler/operation_execution_labels.py"`。
8. 删指南条目 `docs/dev/aps_three_gap_quality_gate.md:151` 同名行。

> **registry 矛盾纠偏（明确记入档案）**：planned_fix_hint 写"改 `scheduler_resource_dispatch_execution_routes.py:16`"——回盘证伪：`routes.py` **不引用** `operation_execution_labels`（rg 零命中）；真实经垫片的 web 消费方是 `..._context.py:11`（与 prod_consumers 字段一致）。**以 context.py:11 为准。**

> **本债不涉及 P4 / 收口 / raise**：纯删转发层，无兜底/静默回退引入风险；不触灵魂线。

## 5. 同文件碰撞债
**registry interference_edges 声称 R20 与 R08/R09（`scheduler_resource_dispatch_execution.py`）、R12（`gantt_tasks.py`）同文件——经回盘三条边全部证伪为假干扰：**
- R20 真实触及的 web 文件是 `scheduler_resource_dispatch_execution_**context**.py`，**不是** `..._execution.py`；后者 rg `operation_execution_labels` 零命中，R20 根本不碰 → **R08/R09 边假**。
- `gantt_tasks.py:8` 直连 model（不经垫片），R20 删垫片不改 gantt_tasks → **R12 边假**。
- 假干扰根因：registry `all_files` 把整簇文件并入，但 R20 实际触及集只有垫片本体 + feedback_service/support + context + 2 测试 + 门禁记账。

**R20 真实同文件协调点只有一处文件：`operation_execution_feedback_service.py`**，上有两个邻债：
| sibling | 改哪段 | 谁先 | 撞行号? |
|---|---|---|---|
| **LB01 (B01, 承重)** | 钉 `347-356`（审计 raw dict 构造）+ `451-453`（硬拒 raise）的"我是故意的"承重注释 | **B01 必须先落**（Phase1 边 `LB01→R20`「承重先于动同文件」）。注释钉死后 R20 才改 :52 | 不撞（LB01 在 347-453，R20 在 :52，相距远）。但**同文件先后强制**：先 B01 钉注释，再 R20 改 import，避免 R20 的 diff 漂移 LB01 行号导致 B01 注释定位失准 |
| **R17 (B11, 死导入)** | 删 feedback_service 顶部死导入（registry 说 :28-31；回盘 :1-51 顶部 import 块为活导入，R17 真实死行需 B11 自档核，但**确定在 R20 的 :52 之前**） | 行号不同、同文件 | R17 删上方行会**上移** R20 的 :52（删 N 行 → :52 变 :52-N）。故须与 B11 **串行或同批**：若 R17 先删，R20 改前须重新 rg 定位 import 块真实行；若 R20 先改，R17 后删须重新定位 |

> feedback_support.py 上无声明的邻债（R20 在该文件单独动 :24，无碰撞）。

## 6. 同收口点债
**R20 是直删类、不收口**——不存在"收口到同一符号"的语义。删垫片只是把 5 处依赖改为直连已存在的 model 模块 `core.models.operation_execution_labels`（该模块本就是垫片的唯一来源，**不是新建第二模块**，符合铁律 5「收口到已存在点」精神；本质是消除冗余转发层，非引入新收口）。

- model 模块 `core.models.operation_execution_labels` 是分区 6 处直连方早已使用的事实统一点；R20 把剩余 5 处也并过去 → 进货路彻底归一，无顺序敏感性（5 处互不依赖，可任意序改，但**整体须在 LB01 注释落地后、与 R17 协调行号**，见 §5/§12）。
- 无其它债收口到本垫片符号（垫片即将删除，不是收口目标）。

## 7. 收口行为差异及所需 parity 测试
**新旧两路逐分支等价性：严格等价，零行为差异。**
- 垫片 `:1-17` import 的 15 符号与 model `__all__` 的对应 15 项**同一对象**（Python `from X import Y` 绑定同一函数/常量引用）。删垫片后 5 处直连 model，**调用的是同一份代码对象**，无任何分支差异。
- 严格超集证：model `__all__` 17 项 ⊃ 垫片转出 15 项；垫片相对 model 多出集 = ∅（`set(shim)-set(model)=set()`）；model 多出 2 项 `EXECUTION_ACTION_FILL_ACTUAL`、`EXECUTION_ACTION_VIEW_RECORDS`（常量，垫片未转出，但 5 处消费方也不需要——它们只 import 自己用的子集）。
- **灵魂暗线保全**：`execution_status_label` 默认值 `STATUS_LABELS.get(text, "状态未识别")`（`model:83`）、`handling_status_label` 的 `"处理状态未识别"`（`model:117`）均由 model 函数实现，删垫片改直连**无法削弱**这些 loud-degrade 标记（blast downstream_effect 断言成立）。
- **无 None vs raise 这类边界反例**：垫片不含任何转换/默认逻辑，纯转发；不存在"新旧两路对边界值给不同结果"的可能。

**所需 parity 测试**：因严格等价，无需新写专门 parity；**沿用现有 2 测试自证即可**——改完跑 `tests/regression_operation_execution_event_foundation.py`（含 :380 `test_operation_execution_labels_keep_status_and_action_plain_chinese` 锁中文标签）+ `tests/regression_plan_vs_actual_review.py` 绿即证转发等价。再跑文档门禁 `tests/regression_aps_three_gap_docs_quality_gate.py` 确认记账三处同步无遗漏。

## 8. 爆炸半径
**生产消费者（经垫片，删后必须改，3 处）：**
1. `core/services/scheduler/operation_execution_feedback_service.py:52`（import 块 :52-61，取 7 符号）
2. `core/services/scheduler/operation_execution_feedback_support.py:24`（import 块 :24-31，取 6 符号）
3. `web/routes/domains/scheduler/scheduler_resource_dispatch_execution_context.py:11`（取 `execution_action_label`）

**测试消费者（经垫片，2 处）：**
4. `tests/regression_operation_execution_event_foundation.py:21`
5. `tests/regression_plan_vs_actual_review.py:13`

**文档/门禁记账消费者（删文件须同步，2 处）：**
6. `tests/regression_aps_three_gap_docs_quality_gate.py:128`（KEY_PYTHON_FILES 元组 pin；:188 `missing_python` 断言要求该路径出现在 DEV_GUIDE 文本）
7. `docs/dev/aps_three_gap_quality_gate.md:151`（指南正文条目）

**直连 model、R20 不碰（爆炸半径外，6 处，列出以证"删垫片不波及"）：** `gantt_tasks.py:8` / `data/repositories/operation_execution_state_builder.py:18` / `web/viewmodels/scheduler_resource_dispatch_execution.py:10` / `resource_dispatch_execution_enrichment.py:5` / `resource_dispatch_actual_record_service.py:8` / `resource_dispatch_actual_import.py:9`。

**灾难链（删错/漏改会静默炸什么）：**
- **漏改某个 import 方** → `ImportError` 即时炸（loud，非静默）：删垫片后该模块 import 路径失效，启动即报，CI 拦截。**低风险**。
- **漏摘门禁 :128 或漏删指南 :151** → `test_developer_guide_lists_regression_tests_and_key_python_files` 红（`missing_python != []`）。**记账失败，非安全失守**——文档同步门禁机制本身在工作。
- **误碰 feedback_service LB01 区（347-356/451-453）** → 这才是真灾难链：动审计 raw dict 或硬拒 raise 会静默削弱承重逻辑。但这与 R20 的 import 改写**物理隔离**（:52 vs 347-453），只要遵守 §9 禁区即不触发。
- **字节级一致 + ImportError 即时暴露 ⇒ 本债无"静默炸"路径**，最大风险仅为记账门禁红（可立即回退）。

## 9. 承重毗邻标记 + 禁区行
**毗邻承重：是。** R20 的生产消费方 `operation_execution_feedback_service.py` 同时是 **LB01（B01）承重宿主文件**。R20 改其 :52 import 时与 LB01 同文件共存。

**禁区行（R20 绝不可碰，回盘实证）：**
- `core/services/scheduler/operation_execution_feedback_service.py:347-356` — 审计/快照 `raw` dict 构造（`{"context": {"schedule_version":..., "expected_state_revision":..., "requested_plan_role":...}}`），承重：固定字段集喂下游持久化/校验。
- `core/services/scheduler/operation_execution_feedback_service.py:451-453` — `raise _invalid_field_value("affected_machine_id", "请选择有效的影响设备。")` / `("affected_operator_id", "请选择有效的影响人员。")` 硬拒消毒，承重：写死的设备/人员存在性校验拒绝路径。

**R20 在该文件的合法触及面：仅 :52 起的 `from .operation_execution_labels import (...)` 那一个 import 块。** 改写为绝对 model 路径、保持取的 7 符号清单不变。任何对 347-453 区的改动均属越界。
- feedback_support.py 无 LB 标记，:24 import 块可独立改。

## 10. 分层 / 导入环风险
**无越层、无导入环风险——删垫片让分层更干净。**
- 改写方向是 `services/web/tests → core.models.operation_execution_labels`。`core.models` 是分层底座，`core.services`、`web`、`tests` 依赖 `core.models` 是**合法向下依赖**（与已直连的 6 处方向一致）。
- 不引入 `core.algorithms→core.services` 或 `core.models→core.services` 等被禁的越层 import（铁律 6）。R20 改的全是消费侧把进货路从 service 层垫片改到 model 层，方向只会**减少**一次 service→service 的横向转发。
- 删垫片后 `core/services/scheduler/operation_execution_labels.py` 不复存在 → 不可能产生经它的导入环。model 模块自身仅依赖标准库 + 同层 model，无回指 service 的环。
- **0 AST 越层违规**（本债天然满足）。

## 11. 测试耦合
**动手前先迁的测试（2 个 import 改写）：**
1. `tests/regression_operation_execution_event_foundation.py:21` — `from core.services.scheduler.operation_execution_labels import (...)` → `from core.models.operation_execution_labels import (...)`。该测试含 `:380 test_operation_execution_labels_keep_status_and_action_plain_chinese`（锁中文标签纯净），改 import 后必须仍绿。
2. `tests/regression_plan_vs_actual_review.py:13` — `from core.services.scheduler.operation_execution_labels import action_to_event_type` → 改连 model。

**测试改写与生产改写应同一 PR/同批提交**（否则垫片删了测试还指旧路 → 测试侧 ImportError）。

**门禁测试不"迁"而"摘"**：`tests/regression_aps_three_gap_docs_quality_gate.py:128` 是反向断言（要求该路径出现在指南），删文件须摘元组项 + 删指南 :151，二者配套。

**改完验证顺序**：先跑上述 2 个 regression（自证转发等价），再跑 docs_quality_gate（自证记账三同步），全绿方可收。

## 12. 前置依赖
**硬前置（必须先于 R20）：**
1. **LB01（B01）承重注释先落** — Phase1 边 `LB01→R20`「承重先于动同文件」。B01 须先在 feedback_service `347-356` / `451-453` 钉"我是故意的"中文承重注释 + 绑契约/parity 测试，钉死后 R20 才改同文件 :52 import。B01 属 Batch-1 ROOT，较早可清（registry planned_batch_hint）。
2. **与 R17（B11）协调行号** — R17 删 feedback_service 顶部死导入（在 :52 之上），会上移 R20 的 import 行。两者**须串行或同批**：约定先后顺序，后做方动手前**重新 rg 回盘** `from .operation_execution_labels import` 真实行号，不照抄本档的 :52（本档行号为 2026-06-05 快照，R17 一动即漂）。registry planned_deps_hint 已点名此协调。

**门禁前置（F 门）：**
3. 删文件前确认 `docs_quality_gate` 三同步点（gate :128 摘 + 指南 :151 删 + 文件删）在**同一改动批**完成，否则文档同步门禁红。

**无须等待的：** R08/R09/R12 干扰边经核为假（§5），**不构成 R20 前置**；6 处直连 model 方与 R20 无依赖。

**排期建议**：本桶 B13 内第 3 位（registry）。本质是低风险记账型删除，唯一真实约束是「B01 先钉承重注释 + 与 B11/R17 串行避撞行号」。owner_pending=false，可直接进 Batch-14 执行批次（在 B01 ROOT 之后、与 B11 协调窗口内）。

## 对抗核验（第二双眼睛）

**结论：PASS（找不到反证）。** 独立重 grep，挑战行号 / 消费者清单 / 承重 / 收口等价 / 分层，逐项无反证。

**重 grep 命令与结果：**
- `wc -l + tail -c 40 | xxd`：垫片本体 **35 行、无尾换行**（registry 说 36 漂 1，档案 §1 已纠）。本体行号准。
- `rg -n "operation_execution_labels" --type py -g '!docs/**'`：全仓命中 = 6 处直连 model + 5 处经垫片（feedback_service:**52** / feedback_support:**24** / context:**11** / event_foundation:**21** / plan_vs_actual:**13**）+ 垫片本体:1 + 门禁:**128**。**与档案 §1/§8 逐行一致；与 registry 旧行号（48/50/26/27/17/10/122）全部漂移**，档案漂移记录属实。
- `rg ... scheduler_resource_dispatch_execution_routes.py` → **零命中**：档案对 registry planned_fix_hint「误写 routes.py」的纠偏成立，真实 web 消费方是 context.py:11。
- `cat -n 垫片` + `rg __all__ model`：垫片转出 15 符号、model `__all__` 17 项（多 `EXECUTION_ACTION_FILL_ACTUAL/VIEW_RECORDS`），**严格超集 15⊂17 确认**；`from X import Y` 同一对象引用，删垫片改直连零行为差异。
- `rg "状态未识别|处理状态未识别" model` → :83 / :117：loud-degrade 暗线由 model 函数实现，删垫片无法削弱，**未触灵魂线**。
- `sed -n '347,356p;451,453p' feedback_service`：LB01 禁区实证——347+ raw dict 含 `requested_plan_role` 等承重字段、451-453 `raise _invalid_field_value(...)` 硬拒消毒，均健在。`sed '52,62p'` 确认 :52 import 块取 7 符号、与 347-453 物理隔离。feedback_support:24 取 6 符号（无 public_execution_remark），档案 §8 符号数准。
- `sed '124,132p' + rg missing_python` 门禁：:128 元组项确认，:188 `missing_python = [...]` + :191 `assert missing_python == []` 反向断言（要求路径出现在指南文本），指南 :151 条目确认 → 漏摘=门禁红=记账失败，**非安全失守**，档案 §8 灾难链定性准确。

**逐项裁定：**
- 行号复核：**一致**（本体±1 本就是 registry 漂移，档案已纠正并以回盘为准）。
- 承重判断：load_bearing=false 属实；修法为「直删垫片 + 5 处 import 改连 model + 摘门禁记账」，**纯删，无删除承重逻辑 / 无透传 / 无加形参 / 无统一兜底**。毗邻 LB01 禁区（347-356/451-453）档案明列且与改动面物理隔离，**无承重误删风险**。
- owner_pending=false：档案给了终态修法与 Batch-14 排期，**合规**（非 owner_pending 债允许给终态）。
- 收口：非新建第二模块——5 处并入「6 处直连方早已使用的事实统一点」`core.models.operation_execution_labels`，符合「收口到已存在点」，无静默引爆（漏改即 ImportError loud）。
- 分层：方向 services/web/tests → core.models 为合法向下依赖，删垫片减一次 service→service 横向转发，**0 越层、无导入环**。
- P4/灵魂线：纯删转发层，无新增 except 吞错 / 静默回退，loud-degrade 由 model 保全。

**与既有分析的冲突已被档案正确吸收**：registry interference_edges R08/R09/R12 同文件边经回盘为假（R20 触及 ...execution_context.py 非 ...execution.py，gantt_tasks 直连不经垫片），档案 §5 证伪成立；planned_fix_hint 误写 routes.py 已纠为 context.py。无新增反证。

**唯一需执行期复核（非争议）**：R17(B11) 删 feedback_service 顶部死导入会上移 :52，动手前须重新 rg `from .operation_execution_labels import` 真实行，不照抄本档 2026-06-05 快照行号。
