# R31 逐债档案 — WRITE_INTERNAL_ONLY 死常量三处散布

> 只读不改产物。回盘时间 2026-06-05。主文件 basename: value_policies.py（双文件同名：shared 源 + common facade）。
> 桶 B13 / 病理 P6 / 严重度 low / 承重 false / owner_pending false / 簇 C01 / 同文件兄弟 R33。

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

按符号名 `WRITE_INTERNAL_ONLY` rg 全仓回盘（含测试），命中 3 处，全部精确落在 registry 声称行：

| # | 角色 | 文件 | 老行号(DONT_TRUST) | 实际行号(回盘) | 漂移 |
|---|------|------|--------|--------|------|
| 1 | 源定义 | `core/shared/value_policies.py` | `:9` | `:9` `WRITE_INTERNAL_ONLY = "write_internal_only"` | 无 |
| 2 | facade import | `core/services/common/value_policies.py` | `:11` | `:11` `    WRITE_INTERNAL_ONLY,`（在 from core.shared.value_policies import (...) 块内） | 无 |
| 3 | facade __all__ | `core/services/common/value_policies.py` | `:29` | `:29` `    "WRITE_INTERNAL_ONLY",` | 无 |

回盘命令：`rg -n "WRITE_INTERNAL_ONLY" --glob '*.py'` → 仅上述 3 行，core/ web/ data/ 与测试目录无其它命中。
**漂移结论：无漂移，0 行偏差。三处共 3 行，与 current_evidence_2026_06_05 / location_status（标 ✅准）完全吻合。**

## 字段 2：当前状态 + 与 status evidence 一致性

- registry status_2026_06_05 = `planned`。
- 回盘实证：源定义 `:9` 仍为死常量原样，common facade `:11/:29` 仍在，R33 尚未删 facade 文件（`core/services/common/value_policies.py` 仍存在且 37 行完整）。
- **一致：状态 planned 与代码现状（一行未动）完全吻合，无 in_progress 痕迹。**

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 **P6（死常量伪「在用」假象）**：定义后无任何消费方，却通过「源定义 + facade re-export + __all__ 白名单」三处散布，制造出「被导出 = 在用」的假象。
- 实证三连（已 grep）：
  1. 16 个 FieldPolicy 的 `write_mode=` 赋值全在 `core/shared/value_policies.py:46~201`，取值仅 `WRITE_REQUIRED`(:46~171)、`WRITE_OPTIONAL`(:181)、`WRITE_NOT_APPLICABLE`(:191/:201)，**无一个 = WRITE_INTERNAL_ONLY**。
  2. 全仓唯一读 `policy.write_mode` 做分支的 `core/shared/compat_parse.py:165/188/208` 都只比 `== WRITE_OPTIONAL`，**从不比 WRITE_INTERNAL_ONLY**。
  3. git 8ec5b40b 出生即无消费（registry 既有结论，本轮未反驳）。
- 严重度 **low**：纯死代码清理，无运行期行为。
- 桶 **B13**（core-infra-shared 死常量），簇 **C01**。

## 字段 4：修法类

**直删（P6 死常量清理）**。承重 false、owner_pending false，无承重禁区（lb_no_touch=null），可给终态修法。

删除目标 3 处 / 2 文件，但**拆给两条债执行**（核心排序约束）：
- **R31 自删（Batch-14, B13 收尾）**：删 `core/shared/value_policies.py:9` 的 `WRITE_INTERNAL_ONLY = "write_internal_only"` 源定义行。
- **R33 顺带删（Batch-7, B06）**：common facade `core/services/common/value_policies.py:11`（import 条目）+ `:29`（__all__ 条目）—— 因为 R33 要**整文件删除** `core/services/common/value_policies.py`（三个零生产 re-export facade 之一），R31 在该文件的两处删改会被 R33 的整文件删除自动吸收。

非收口债：本债是「删常量」不是「收口到统一点」，无新建模块、无透传、无加形参，符合铁律 5（不新建第二模块）。不涉及灵魂线（无兜底/静默回退/except 吞错，删的是纯字符串常量），符合铁律 4。

## 字段 5：同文件碰撞债（sibling 协调）

唯一同文件兄弟：**R33（B06，Batch-7）**。注意「同 basename 两文件」精确关系：
- R31 主战场源定义在 `core/shared/value_policies.py`；
- R33 删的 facade 是 `core/services/common/value_policies.py`（import 链下游：common 从 shared 转出）。

**各改哪段 / 谁先谁后 / 撞行号风险：**
- R33（Batch-7，先）：整文件删除 `core/services/common/value_policies.py`（含 R31 关注的 `:11`/`:29` 两行被一并抹掉），并先做 step1——把 `tests/config/test_value_policies_matrix_contract.py:18` 的 import 从 `core.services.common.value_policies` 迁回承重点 `core.shared.value_policies`。
- R31（Batch-14，后）：删 `core/shared/value_policies.py:9` 源定义行。
- **撞行号/dict 键位移风险：无。** 两债改的是不同文件；R31 删 `:9` 仅令 `core/shared/value_policies.py` 后续行整体上移 1 行，但该文件无任何按行号寻址的契约 pin，FieldPolicy 定义靠符号名取值不靠行号。R33 整文件删除不留行号悬空。
- **顺序硬约束：R33 先（吸收 common 侧两行 + 迁测试），R31 后（删 shared 源定义）。** 若反序（R31 先删 `:9` 源定义），则 common facade `:11` 的 `from core.shared.value_policies import (..., WRITE_INTERNAL_ONLY, ...)` 会 **ImportError 炸裂**（facade 仍在尝试导入已删符号）——这是本债唯一的真实排序陷阱，必须 R33 删 facade 在前或与 R31 同批原子完成。

## 字段 6：同收口点债 + 顺序敏感性

本债非「收口」类（直删，不收口到任何统一符号），故无「收口到同一符号的其它债」。
唯一顺序敏感性已在字段 5 给出：**R31 删源 vs R33 删 facade 的先后**——R33（删 facade / 迁测试）必须不晚于 R31（删源定义），否则 facade 残留 import 行触发 ImportError。除此之外无其它顺序敏感邻债。

## 字段 7：收口行为差异及 parity 测试

**不适用（本债是直删，非收口）。** 删的是一个零消费死常量字符串，不存在「新旧两路对边界值是否逐分支等价」的问题——没有任何分支读它，删除前后所有边界值（None / 空 / 非正 / 正常）走的代码路径完全不变。
- 无需 parity 测试。
- 唯一需保护的相邻行为是 R33 step1 迁移的策略矩阵契约测试（`tests/config/test_value_policies_matrix_contract.py`），但该测试**不引用 WRITE_INTERNAL_ONLY**（已 grep 实证 import 块仅含 READ_COMPAT/READ_FILTER_ONLY/WRITE_OPTIONAL/WRITE_REQUIRED/get_field_policy/list_field_policies，断言点 `:53` default_days==WRITE_REQUIRED、`:88` due_date==WRITE_OPTIONAL），故 R31 删 `:9` 对该测试零影响。

## 字段 8：爆炸半径（phase1_blast 9 字段已校）

- **生产消费者：零（已 grep 证实）。** 无 FieldPolicy 使用、无 write_mode 分支与之比较。`compat_parse.py:165/188/208` 只比 `==WRITE_OPTIONAL`。
- **facade 文件 importers：零。** `rg "from core.services.common.value_policies"` 在 core/web/data 无命中（仅测试经 R33 迁移前引用过该 facade，且不取 WRITE_INTERNAL_ONLY）。
- **测试消费者：零直接引用 WRITE_INTERNAL_ONLY。** `regression_value_policies_matrix_contract.py:53/88` 只断言其它 write_mode 值。
- **契约/拓扑测试：无契约 pin 此常量。** common facade 的 __all__ parity 若有检查，随 R33 整文件删除处理。
- **downstream_effect：删 3 行无运行期消费方，风险极低。**
- **灾难链（删错/统一错会静默炸什么）：**
  - 正确顺序（R33 先 / R31 后或同批）：无静默炸点，纯净删除。
  - 错误顺序（R31 先删 `:9`，R33 facade 未删）：facade `:11` 残留 `import WRITE_INTERNAL_ONLY` → **ImportError，但是 loud（非静默）**——加载 `core/services/common/value_policies.py` 即崩，符合灵魂线「要响不要哑」。这是唯一爆炸点，且会被 CI 立即捕获，不会静默。

## 字段 9：承重毗邻标记 + 禁区行

- `lb_colocation_danger = none`；`load_bearing=false`；`lb_no_touch=null`。
- 同文件 `core/shared/value_policies.py` 内**无承重债毗邻**（同文件兄弟 R33 的战场在 common facade，非 shared 源；shared 内的 FieldPolicy 矩阵 `:43~205` 由策略矩阵契约测试守护，但那不是承重禁区债，且 R31 只删 `:9` 一行不碰矩阵）。
- **禁区行：无。** 但操作性提醒——R31 只许删 `core/shared/value_policies.py:9` 这一行，**不得顺手动** `:6/:7/:8`（WRITE_REQUIRED/WRITE_OPTIONAL/WRITE_NOT_APPLICABLE，这三者有 16 处 FieldPolicy 在用 + compat_parse 分支在比，删它们会静默改变解析语义）。

## 字段 10：分层 / 导入环风险

- **0 越层风险。** 删除一行常量赋值不新增任何 import。回盘确认 `core/shared/value_policies.py` 与 `core/shared/compat_parse.py` 均**不 import core.services**（grep `import.*services` 在两文件内零命中），删 `:9` 后这一性质不变。
- 现有方向是 `core.services.common.value_policies → core.shared.value_policies`（下游 facade 向上游 shared 转出，方向合规）；R31 删 shared 源、R33 删 common facade，删除只会**减少**边，不会新增导入环或越层。
- 符合铁律 6（0 AST 违规，无 core.algorithms→core.services / core.models→core.services 引入）。

## 字段 11：测试耦合（动手前先迁哪些测试）

- **R31 自身：无需迁测试。** 没有测试引用 WRITE_INTERNAL_ONLY。
- **由 R33 step1 负责（先于本债）：** `tests/config/test_value_policies_matrix_contract.py:18` 的 import 从 `core.services.common.value_policies` 迁回 `core.shared.value_policies`（承重点平移，覆盖无损）。该迁移是 R33 删 facade 的前置，与 R31 删源定义无直接耦合，但二者都依赖测试已指向 shared 承重点这一前提。
- 动手前自证：删 `:9` 后运行 `pytest tests/config/test_value_policies_matrix_contract.py` 应仍全绿（该测试不碰 WRITE_INTERNAL_ONLY）。

## 字段 12：前置依赖

- **硬前置：R33（B06，Batch-7）必须不晚于 R31。** R33 删 common facade（吸收 `:11/:29` 两行）+ 迁测试 import。R31（Batch-14，B13 收尾）删 shared 源 `:9`。
  - 若 R33 已删 facade，则 R31 删 `:9` 是干净的最后一步。
  - 若 R31 抢在 R33 前删 `:9`，facade `:11` 残留 import 立即 ImportError（loud，CI 拦截）。
- **F 门：** 无特殊门控；常规「删除后全量 pytest 绿 + AST 分层检查 0 违规」即可放行。
- 与既有 PHASE0/registry 分析**无冲突**：本档案逐字段复核 phase1_blast 9 字段、planned_fix_hint、planned_deps_hint，回盘行号 0 漂移，结论与 registry 一致。

---

### 一句话总结
R31 是 P6 死常量三处散布，零生产/零测试消费，纯直删 low 债；唯一陷阱是与同名 facade 债 R33 的删除顺序（R33 删 facade 须不晚于 R31 删 shared 源，否则 facade 残留 import 触发 loud ImportError）；无承重、无收口、无 parity、无越层。

---

## 对抗核验（第二双眼睛）— 2026-06-05

**结论：PASS。** 低危纯死叶子债，快核 + 关键反例验证全部坐实，未找到反证。

独立 grep 回盘命令与结果（cwd=`/Users/lurenxing/Documents/GitHub/----`）：

1. `rg -n "WRITE_INTERNAL_ONLY" --glob '*.py'` → 仅 3 命中：`core/services/common/value_policies.py:11`(import)、`:29`(__all__)、`core/shared/value_policies.py:9`(源定义)。**行号 :9/:11/:29 与档案及 registry 完全一致，0 漂移。**
2. 上下文复核：`core/shared/value_policies.py:9` = `WRITE_INTERNAL_ONLY = "write_internal_only"`，紧邻 :6/:7/:8 的 WRITE_REQUIRED/OPTIONAL/NOT_APPLICABLE（确认 :9 是死常量、前三者是活常量，禁区行提醒正确）；common facade :11 在 `from core.shared.value_policies import (...)` 块内、:29 在 `__all__` 列表内，均坐实。
3. `rg -n "write_internal_only"`（全仓含非 py，查字符串字面量硬编码消费）→ **仅 :9 定义本身一处，无任何旁路硬编码消费方。**（档案未显式做字面量级 grep，本核验补足，结论不变）
4. `rg -n "write_mode" core/shared/compat_parse.py` → :165/:188/:208 三处分支**只比 `== WRITE_OPTIONAL`**，从不比 WRITE_INTERNAL_ONLY。零分支消费坐实。
5. `rg -n "write_mode=" core/shared/value_policies.py | ... | sort | uniq -c` → WRITE_REQUIRED×13 + WRITE_NOT_APPLICABLE×2 + WRITE_OPTIONAL×1 = 16 个 FieldPolicy，**无一赋 WRITE_INTERNAL_ONLY**。坐实「16 个 FieldPolicy 无一用它」。
6. facade importer：`rg -n "from core.services.common.value_policies"` → 唯一命中 `tests/config/test_value_policies_matrix_contract.py:18`；其 import 块（:18-25）仅含 READ_COMPAT/READ_FILTER_ONLY/WRITE_OPTIONAL/WRITE_REQUIRED/get_field_policy/list_field_policies，**不引用 WRITE_INTERNAL_ONLY**。`rg WRITE_INTERNAL_ONLY tests/` 零命中。core/web/data 无任何 importer。

**承重误删风险：无。** lb=false、lb_no_touch=null、owner_pending=false；修法为直删，未混入统一/透传/加形参/兜底/静默回退；禁区提醒（不得动 :6/:7/:8）正确且必要。
**owner_pending 越权：不适用（=false）。** 档案未擅自越权（本就有权给终态修法）。
**灵魂线：合规。** 删纯字符串常量，不改 loud/降级语义。
**分层/导入环：合规。** 删一行常量赋值不新增 import，只减边。
**排序陷阱复核：成立。** 正序（R33 删 facade ≤ R31 删源）干净；反序（R31 先删 :9）→ facade :11 残留 import 触发 **loud ImportError**（非静默，CI 拦截），与档案/字段 8 灾难链一致。

**唯一非争议性补注（不改结论）：** 档案对字面量 `"write_internal_only"` 未单独 grep，本核验补做后确认仍零消费——属增强证据，不构成争议。

---

## 2026-06-10 终态补登（G17 收口，R31 → fixed）

详见 `_registry.json` R31 current_evidence。要点：紧随 G23 commit 落地（壳先于源硬序满足，无 facade 残 import 窗口）——common facade 侧 :11/:29 两处已先随 R33 整文件删除被吞，本 commit 只删 `core/shared/value_policies.py` 的 `WRITE_INTERNAL_ONLY` 源定义一行（旧 :9）。删后全仓 `rg WRITE_INTERNAL_ONLY|write_internal_only` 零命中；毗邻 :6/:7/:8 活常量未动（WRITE_NOT_APPLICABLE 零引用属 R30 收口已登记微债候选，非本债扩面）。验证：config+models_domain 259 passed、ruff 全过。
