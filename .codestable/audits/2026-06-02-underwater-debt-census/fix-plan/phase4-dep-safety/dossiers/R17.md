# R17 逐债档案 — _REPORTED_STATUS_BY_ACTION 'exception' 死键 + feedback_service 死导入

> ✅ **2026-06-08 B 执行终态（G07/G08）**：已 fixed。`operation_execution_feedback_service.py` 删除 `EXECUTION_EVENT_EXCEPTION` 死导入；`operation_execution_feedback_support.py` 删除 `_REPORTED_STATUS_BY_ACTION` 推导式里的 `EXECUTION_EVENT_EXCEPTION` 死项及其随之孤立的 import，仅保留 `EXECUTION_ACTION_REPORT_EXCEPTION` 活键。新增测试锁定 `exception` 死键不回潮、`report_exception` 活键仍映射异常状态。未触碰 `_build_event_payload` 的 `SOURCE_SCHEDULE / ROLE_ADOPTED / None` 消毒层，也未触碰 `_parse_feedback_datetime` 的 loud raise。
> 下文为执行前分析留档，行号和“planned”口吻仅作历史证据；当前状态以上方执行终态和 registry fixed 字段为准。

> 桶 B11 / 簇 C01 / 严重度 low / 病理 P6（写后永不读字典键 + 导入后永不用符号）/ 承重 false / owner_pending false
> 本档案只读不改，仅产计划。所有断言附当前工作区 file:line 证据（已 grep 回盘）。

## 0. 一句话结论（先于 12 字段）

> **registry 的「直删 3 行」修法已失效**：主文件 `operation_execution_feedback_support.py` 已被重构——死键不再是字面映射 `EXECUTION_EVENT_EXCEPTION: EXECUTION_STATUS_EXCEPTION`，而是一个 dict 推导式（:74-84）。病理（'exception' 键不可达）**仍成立**，但删除位点与方式全变了。详见 §1 / §4。

---

## §八 12 字段

### 1) 当前真实 file:line（回盘）+ 漂移记录

R17 跨两文件，回盘结果如下（绝不照抄 old_location）：

| 符号 / 位点 | registry old_location / evidence | 当前真实 file:line | 漂移 |
|---|---|---|---|
| support: `EXECUTION_EVENT_EXCEPTION` import | :11（evidence） | `operation_execution_feedback_support.py:11` | 无（精确命中） |
| support: 死键所在表 `_REPORTED_STATUS_BY_ACTION` | :59-66（evidence 称死键在 :64-65） | 表在 `operation_execution_feedback_support.py:74-84`；'exception' 键由推导式 :81 行 `EXECUTION_EVENT_EXCEPTION` 产生 | **结构性漂移 +15 行且形态全变**：表已从字面 dict 重构为 dict 推导式，不再有 `EXECUTION_EVENT_EXCEPTION: EXECUTION_STATUS_EXCEPTION` 这一字面行 |
| service: 死导入 `EXECUTION_EVENT_EXCEPTION` | :12 | `operation_execution_feedback_service.py:12` | 无（精确命中，且 grep 证全文件仅此一处出现，body 零使用，见 §8） |
| service: 表索引使用 `_REPORTED_STATUS_BY_ACTION[action]` | :455 | `operation_execution_feedback_service.py:475` | **+20 行漂移** |
| service: `_normalize_action` 归一 | :247-248 | `operation_execution_feedback_service.py:266-275`（归一在 :268） | **+~20 行漂移** |
| labels: `event_type_to_action` 归一证 | 称在 `labels.py:90-94` / `:91-102` | 真实在 **`core/models/operation_execution_labels.py:91-95`**；`action_to_event_type` 在 :98-102 | **文件路径错**：registry 说的 `labels.py` 实为 `core/models/operation_execution_labels.py`（scheduler 下的 `operation_execution_labels.py` 只是 `:7-8` 再导出，定义在 models 层） |

**关键漂移结论**：`phase1_blast.location_status` 标的「✅准。feedback_support.py:64 死键…均与盘上一致；两文件都不在漂移名单」**已不再成立**。support 文件已被重构，:64 不存在死键字面行；planned_fix_hint 的「删 :64 死键」「删 :11 随之孤立的 import」两步均**无对应行**可删。

### 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`；本档案不改代码 → 维持 planned。
- 与 `current_evidence_2026_06_05` **部分不一致**：evidence 称「:65 仍有死键 `EXECUTION_EVENT_EXCEPTION: EXECUTION_STATUS_EXCEPTION`」「:11 仍 import」「计划三行删除未执行」。回盘发现：`:11` import 仍在（一致）、`feedback_service.py:12` 死导入仍在（一致）、但**死键已不是字面行**而是推导式产物（不一致，evidence 描述的是更早一版的字面 dict）。
- 病理本身（'exception' 键写后永不被 `[action]` 命中）**仍真实成立**，见 §3 证明，故 R17 仍是有效债，只是执行方案需重写。

### 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6**：(a) `_REPORTED_STATUS_BY_ACTION` 中的 `"exception"` 键写后永不读；(b) `feedback_service.py:12` 的 `EXECUTION_EVENT_EXCEPTION` 导入后永不用。
- 严重度 **low**（死代码，零行为影响）；桶 **B11**；簇 **C01**。

**不可达性当前态证明**（全部回盘）：
1. 常量值：`EXECUTION_EVENT_EXCEPTION = "exception"`、`EXECUTION_ACTION_REPORT_EXCEPTION = "report_exception"`（`core/models/operation_execution_event.py:14-15`）。
2. 表推导式（`operation_execution_feedback_support.py:74-84`）：`key = action`，对元组中每个 action 建键。元组含 `EXECUTION_ACTION_REPORT_EXCEPTION`(:80) 与 `EXECUTION_EVENT_EXCEPTION`(:81) **两者**，故表同时含 `"report_exception"` 与 `"exception"` 两个键。`"exception"` 键的值 = `REPORTED_STATUS_BY_EXECUTION_EVENT_TYPE[action_to_event_type("exception")]` = `REPORTED_STATUS_BY_EXECUTION_EVENT_TYPE["exception"]`（`action_to_event_type` 对 "exception" 不改写，:98-102）= `EXECUTION_STATUS_EXCEPTION`（`event.py:50`）。
3. 归一恒不产 "exception"：`_normalize_action`（`feedback_service.py:268`）= `event_type_to_action(action_to_event_type(text))`。对任意输入，最终经 `event_type_to_action`（`operation_execution_labels.py:91-95`）把 `"exception"` 改写为 `"report_exception"`，故 `normalized` 永不为 `"exception"`。
4. 表只被 `[normalized 后的 action]` 索引（`feedback_service.py:269` 成员校验、`:475` `[action]` 取值），故 `"exception"` 键**永不命中** → 死键确证。

### 4) 修法类

**类别：直删（删死键 + 删死导入）。承重 false，无需注释/契约护栏自身**；但因 service 文件是 LB01 承重文件，删除时设禁区（见 §9）。owner_pending=false，可给终态修法。

**registry planned_fix_hint 已失效，重写为当前态修法（共 3 处，仍跨两文件，宜一次原子提交）**：
1. `operation_execution_feedback_service.py:12` —— 删死导入 `EXECUTION_EVENT_EXCEPTION,`（grep 证全文件零使用，§8）。
2. `operation_execution_feedback_support.py:81` —— 从表推导式元组中删一行 `EXECUTION_EVENT_EXCEPTION,`（删后表不再产 `"exception"` 死键；保留 :80 `EXECUTION_ACTION_REPORT_EXCEPTION,` 这个活键）。
   - 注意：这**不是** registry 说的「删 :64 字面死键行」，而是删推导式输入元组里的那个常量项。删后行为等价（死键消失）。
3. `operation_execution_feedback_support.py:11` —— 删 import 元组里 `EXECUTION_EVENT_EXCEPTION,`，因 §2 后该符号在 support 文件内随之孤立（除 :81 外无其他使用，需先确认 support 内 :81 是唯一引用，见 §8 复核）。

**非收口债**：R17 不涉及 P5 收口（不向任何统一点合并）；纯删死代码。`event_type_to_action`/`action_to_event_type`/`REPORTED_STATUS_BY_EXECUTION_EVENT_TYPE` 等真理之源**一律不碰**。

**灵魂线核对**：删死键/死导入不新增任何兜底、静默回退、except 吞错；`_normalize_action` 的 `raise ValidationError`(:269-274) loud 路径保持不动。✅ 合规。

### 5) 同文件碰撞债

**support 文件 `operation_execution_feedback_support.py` 同居：R15、R09。**
- **R15（B06，`_parse_feedback_datetime` raise 版，真实 :225；`_normalize_feedback_datetime` :232-233）**：与 R17 改动行（:11 / :74-84）**物理不重叠**，相距 >140 行；不会撞行号。但灵魂线强约束：R17 删死键时**绝不触碰** R15 的 raise 语义（:225 起的解析-报错路径）。顺序无硬依赖，但建议 R17 先做（B11 早于 B06），R15 后做时只需重读行号（R17 在表区删 1 行 → R15 所在 :225 区段会**上移 1 行**，R15 执行前须重新 grep `_parse_feedback_datetime` 回盘）。
- **R09（同文件，唯一批准的新建例外 `parse_optional_positive_int`）**：R17 在文件头部 import 区(:11)与表区(:74-84)动，R09 若新增解析函数通常落在工具函数区，可能与表区相邻。**dict 键位移风险**：R17 删 :81 一行使其后所有行**上移 1 行**；R09 若插入新函数应在 R17 之后重新定位插入点。建议 R17 先做。

**service 文件 `operation_execution_feedback_service.py` 同居：LB01、R20。**
- **LB01（B01，承重）**：见 §9，硬排序——LB01 必须先落「我是故意的」承重注释 + 契约测试，R17 才能动 service 文件。
- **R20（B13，:48 import 行）**：R17 删的是 :12 import；R20 动 :48 import。两者都在 import 块但不同行。**撞行号风险**：R17 删 :12 一行 → :48 及其后整体**上移 1 行**，R20 执行前须重新 grep 回盘其 import 行真实行号。顺序：R17（B11）早于 R20（B13），R20 后做须重读行号。

### 6) 同收口点债

R17 **不收口到任何统一符号**（纯删死代码，非 P5）。`interference_edges` 中所有边均为 `same_file=true, same_symbol=false`——即「同文件不同符号」碰撞，**无 same_symbol 真收口碰撞**。故无「收口到同一符号的顺序敏感」问题。唯一的共享数据结构是 `_REPORTED_STATUS_BY_ACTION` 表：R17 改其定义（support:74-84），service 在 :269/:475 读它——但 R17 删的是死键，读侧逻辑不变，无顺序敏感。

### 7) 收口行为差异及 parity 测试

R17 **非收口债**，无新旧两路分支等价问题。但删死键后须证「行为零变」，给最小回归断言：
- **断言 1（死键不影响活路）**：对 `action="report_exception"`，归一后 `"report_exception"` ∈ 表，`_REPORTED_STATUS_BY_ACTION["report_exception"]` = `EXECUTION_STATUS_EXCEPTION`（经 `action_to_event_type("report_exception")="exception"` → `REPORTED_STATUS_BY_EXECUTION_EVENT_TYPE["exception"]`）。删 :81 后此活键由 :80 `EXECUTION_ACTION_REPORT_EXCEPTION` 仍产出，值不变 → 报异常落库 `reported_status` 不变。
- **断言 2（死键确实死）**：删前删后，任何用户输入经 `_normalize_action` 都不可能产 `"exception"`；可补一条断言 `"exception" not in {normalize(x) for x in 所有合法 action}`，证删除安全。
- **反例边界**：若有人误以为可直接喂 `event_type="exception"` 绕过归一——`_normalize_action` 必先经 `event_type_to_action` 改写为 `"report_exception"`，且非法值走 :269 `raise ValidationError`（loud），不会静默命中死键。故无 `None vs raise` 类隐患。

### 8) 爆炸半径

**生产消费者（R17 删除点的真实消费）**：
- `_REPORTED_STATUS_BY_ACTION` 表：读侧 `feedback_service.py:269`（成员校验）、`:475`（`[action]` 取 reported_status 写库）。删死键 `"exception"` 对这两处**零影响**（已证 :475 的 `action` 永为归一后值，不会是 "exception"）。
- service `:12` 死导入：grep 证全文件**仅 :12 一处出现**，body 零使用 → 删除零影响。
- support `:11` 死导入：grep 证 support 文件内仅 :11(import) 与 :81(表) 两处；删 :81 后 :11 孤立 → 一并删，零影响。

**测试消费者**：
- `tests/operation_execution/test_operation_execution_event_foundation.py:14/148/316` 引用的是 **`EXECUTION_EVENT_EXCEPTION` 常量本身**（在 event 模块），**非** `_REPORTED_STATUS_BY_ACTION["exception"]` 死键。R17 不删常量，故这些测试不受影响。无测试直接索引该死键。

**灾难链（删错/统一错会静默炸什么）**：
- ⚠️ **最大爆炸风险 = 误删常量定义**：`EXECUTION_EVENT_EXCEPTION` 在多处**真活**——`data/repositories/operation_execution_state_builder.py:7/97/134`、`core/infrastructure/operation_execution_event_data_contract.py:8/99/106`、`web/viewmodels/scheduler_resource_dispatch_execution.py:8/141`、`core/models/operation_execution_event.py:14/28/50/56/57`、`operation_execution_labels.py:7/35/93/101`。R17 只删 **feedback 两文件的局部 import + 表里那一项**，**绝不**碰 `event.py:14` 常量定义或上述任何活点。若误删常量定义 → 状态机/数据契约/web viewmodel 全线 ImportError 或 KeyError，静默面极大。
- ⚠️ **误删活键**：若把 :80 `EXECUTION_ACTION_REPORT_EXCEPTION` 当死键删掉（而非删 :81）→ `"report_exception"` 键消失 → `_normalize_action` 的 :269 成员校验对真实「报异常」操作误判非法 → 用户报异常被静默拒（ValidationError）。**修法须删 :81 而非 :80**。

### 9) 承重毗邻标记 + 禁区行

⚠️ **高危毗邻**：service 文件 `operation_execution_feedback_service.py` 是 **LB01 承重文件**。R17 在该文件只删 :12 一行 import，但全程设以下**禁区行（绝不触碰）**：
- `:367-382` `_load_current_official_schedule` 硬拒区：`requested_plan_role != ROLE_ADOPTED` / `effective_plan_role != ROLE_ADOPTED` / `source_table != SOURCE_SCHEDULE`（:369-371）→ `raise _conflict("not_current_official_plan")`（:374、:382）。这是「只能对当前官方计划反馈」的承重护栏。
- `:471-475` 写死官方计划消毒块：`"source_table": SOURCE_SCHEDULE`(:471)、`"effective_plan_role": ROLE_ADOPTED`(:472)、`"reported_status": _REPORTED_STATUS_BY_ACTION[action]`(:475)。R17 删死键不改 :475 行为（action 永非 "exception"）。
- support 文件侧 `:74-84` 表区删除**不触及任何承重逻辑**（support 无承重标记，承重在 service 的 LB01）。

`lb_no_touch` 字段为 null（R17 自身非承重），但 LB01 毗邻使其执行受 §12 硬排序约束。

### 10) 分层 / 导入环风险

**零越层、零新增导入环**。R17 是**净删除**：删 import + 删表项，不新增任何 import。
- 现有导入方向合规：`core/services/scheduler/*` → `core/models/operation_execution_event`、`core/models/operation_execution_labels`（service→models，合法下沉）。
- 删 `feedback_service.py:12` / `feedback_support.py:11` 的 `EXECUTION_EVENT_EXCEPTION` 只是减少 service→models 的符号引用，不改边方向，AST 违规数维持 0。
- 不触发分层红线（无 `core.algorithms→core.services`、无 `core.models→core.services` 反向）。

### 11) 测试耦合

- **动手前无需迁移测试**：唯一相关测试 `tests/operation_execution/test_operation_execution_event_foundation.py`（:14/148/316）引用的是 `EXECUTION_EVENT_EXCEPTION` 常量与事件类型语义，**不依赖** `_REPORTED_STATUS_BY_ACTION["exception"]` 死键，删死键后该测试应原样通过。
- **建议新增（非阻塞）**：一条 parity 断言（见 §7 断言 2）锁定「'exception' 不可达」，防未来有人重新把死键加回。落点：同一 regression 文件。
- LB01 的契约测试由 LB01（B01）负责落地，R17 不重复造，但 R17 执行后须确认 LB01 契约测试仍绿（验证删 :12 未碰承重）。

### 12) 前置依赖

**硬排序（不可违反）**：
1. **B01 / LB01 必须先完成**：LB01 在 `feedback_service.py` 落「我是故意的」承重注释 + 绑契约/parity 测试**之后**，R17 才能动 `feedback_service.py:12`（删死导入）。理由：service 是最危险承重边（PHASE0 §10.1），护栏裸奔期严禁改动。
2. **F 门**：依赖 AST 分层门（0 越层）持续绿——R17 本身不引入越层，但执行后须复跑 AST 门确认仍 0。
3. **同文件协调（软排序，建议 R17 先）**：R17 删表项使其后行号上移 1 行 → R15（B06）、R09、R20（B13）执行前均须重新 grep 回盘各自符号真实行号（见 §5）。
4. **原子提交**：R17 跨 support(:11/:81) + service(:12) 两文件，三处删除宜一次原子 commit，避免中间态出现「表已无键但 import 仍在」或反之的悬挂。

**结论**：R17 前置 = 仅 **LB01(B01) 落地** + AST 门绿；本身 low/非承重/非收口，是 B11 桶内可早做的清理项，但**不可早于 LB01**。

---

## 附：与既有分析（registry phase1_blast）的冲突清单

1. **`location_status` 失真**：registry 称「✅准…feedback_support.py:64 死键…两文件都不在漂移名单」——实测 support 文件已重构，死键不在 :64 字面行而在 :74-84 推导式（:81 项），**+15 行结构性漂移**。这是本档案最重要的纠偏。
2. **`planned_fix_hint` 的删除位点过时**：原「删 :64 死键 / 删 :11 随之孤立 import」需改为「删 :81 推导式元组项 / 删 :11 孤立 import」（§4）。删除行数仍 3 行、跨文件结论不变。
3. labels 文件路径纠正：真实在 `core/models/operation_execution_labels.py`（非笼统的 `labels.py`）。
4. service 侧 :12 / :455 / :247 等行号有 +20 漂移，已逐一回盘修正（§1）。

其余结论（病理 P6、严重度 low、生产零有效消费、测试不依赖死键、LB01 硬排序、跨两文件原子提交）经回盘**全部确认成立**。

---

## 对抗核验（第二双眼睛）

**结论：PASS。** 独立 rg/grep 回盘，未找到任何反证；档案不仅未照抄 registry，反而正确推翻了 registry 已失真的断言，质量高于真相源。

**重 grep 命令与结果（均在工作区根执行）：**

1. `rg -n "EXECUTION_EVENT_EXCEPTION|EXECUTION_ACTION_REPORT_EXCEPTION|_REPORTED_STATUS_BY_ACTION" core/services/scheduler/operation_execution_feedback_support.py` → 死 import `:11`、表 `_REPORTED_STATUS_BY_ACTION` 定义 `:74`、活键 `EXECUTION_ACTION_REPORT_EXCEPTION :80`、死键源 `EXECUTION_EVENT_EXCEPTION :81`。**与档案 §1 逐一精确命中。**
2. 读 support :70-94：表确为 **dict 推导式**（`:74-84`，`action: REPORTED_STATUS_BY_EXECUTION_EVENT_TYPE[action_to_event_type(action)] for action in (...)`），**非字面 dict**。证实档案 §0 / §1 核心纠偏成立——registry `location_status` 标的「✅准…feedback_support.py:64 字面死键…两文件都不在漂移名单」**确已失真**。
3. service 文件 `grep -n EXECUTION_EVENT_EXCEPTION` → **仅 :12 一处**（import），body 零使用 → 死导入删除安全（档案 §8 成立）。
4. support 文件全文 `grep -n EXECUTION_EVENT_EXCEPTION` → 仅 `:11`(import)+`:81`(表项)两处 → 删 :81 后 :11 必孤立，一并删安全（档案 §4/§8 成立）。
5. 全仓 `grep -rn EXECUTION_EVENT_EXCEPTION core/ data/ web/ tests/`（剔除 R17 三删点）→ 常量真活于 6 文件：定义 `core/models/operation_execution_event.py:14`，活点 labels:7/35/93/101、event:28/50/56/57/340、data_contract:8/99/106、state_builder:7/97/134、web viewmodel:8/141、tests:14/148/316。**误删常量风险盘清，档案 §8 灾难链准确**——R17 只删 feedback 两文件局部，绝不碰这 6 处。
6. `rg -n "def event_type_to_action|def action_to_event_type" core/` → 真定义在 **`core/models/operation_execution_labels.py:91/98`**；`core/services/scheduler/operation_execution_labels.py` 经 `cat` 确认仅 `from core.models.operation_execution_labels import ...` 再导出 shim。证实档案对 registry「labels.py 笼统路径」的纠正正确。不可达逻辑复核：labels:93-94 `if text==EXECUTION_EVENT_EXCEPTION: return EXECUTION_ACTION_REPORT_EXCEPTION` → 归一恒不产 "exception"，死键 :81 产物永不命中 service:269/:475。**病理 P6 证明成立。**
7. service 归一/校验回盘：`_normalize_action :266`、`event_type_to_action(action_to_event_type(text)) :268`、成员校验 + `raise ValidationError` loud 路径 `:269-274`、表索引 `_REPORTED_STATUS_BY_ACTION[action] :475`。**与档案 §1/§3 一致；灵魂线无违规（删死代码不新增兜底/吞错，loud raise 不动）。**
8. LB01 承重禁区行号独立回盘：`_load_current_official_schedule def :367`、角色硬拒 `:369-371`（三个 `!=`）、`raise _conflict("not_current_official_plan") :374/:382`、写死消毒块 `source_table=SOURCE_SCHEDULE :471 / effective_plan_role=ROLE_ADOPTED :472 / reported_status=_REPORTED_STATUS_BY_ACTION[action] :475`。**档案 §9 标的 :367-382 / :471-475 精确命中**；registry 旧标 :347-356 / :450-455 已漂移，档案已正确更新。R20 import 块经 `sed` 确认在 :48 区，与删点 :12 同块不同行。

**承重误删风险：无。** R17 自身非承重（load_bearing=false / lb_no_touch=null）；修法为纯净删除（service:12 死 import + support:81 推导式元组项 + support:11 孤立 import），无删除/统一/透传/加形参混入承重逻辑，无「看着该统一」的越界清理。承重只在毗邻文件（service 的 LB01 区）且已被档案明确划为禁区行。

**owner_pending=false 合规复核：** 档案给出了终态修法与批次（Batch-12 桶内第 1 步），符合非 owner_pending 债的处置；无擅自越权。

**行号复核：一致**（support:11/74-84/80/81、service:12/266/268/269/475、labels:91/93/98/101、event:14、LB01 禁区:367-382/471-475 全部逐一命中工作区）。

**与既有分析冲突：档案侧正确。** registry `location_status` / `planned_fix_hint`（删 :64 字面死键）/ 承重禁区行号（:347-356/:450-455）三项均已对盘失真，档案已逐一纠偏且修正后行为等价（删 :81 而非 :80，死键消失、活键保留）。建议执行批次以本档案 §4 修法为准，勿用 registry planned_fix_hint。

**需升级第三方裁：n。**
