# R44 逐债档案 — scheduler_navigation_publish.selected_plan_role 是 core 收口点的副本

> 债 id: R44 | 桶 B02 | 簇 C01 | 病理 P5 | 严重度 low | 承重 false | owner_pending **true**
> 主文件: web/routes/domains/scheduler/scheduler_navigation_publish.py
> 同文件兄弟: R54、R58 | co-change: R72、R21

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

**回盘命令**：`rg -n "selected_plan_role|_PLAN_GUARD_FIELD_NAMES|ROLE_ADOPTED" web/routes/domains/scheduler/scheduler_navigation_publish.py`

| 符号 | registry 标注行 | 当前真实行 | 漂移 |
|---|---|---|---|
| `def selected_plan_role`（债主体，web 副本） | old_location `:28-29`；evidence 自报 `:36-37`/`:31-32` | **`scheduler_navigation_publish.py:36-37`** | 老 `:28-29` → 实 `:36-37`，**+8 行**；evidence 内部自相矛盾（一处写 :36-37 一处写 :31-32），实测以 :36-37 为准 |
| `def requested_plan_role`（sibling） | evidence 自报 `:24`/`:27-28` | **`:32-33`** | evidence 报 :27-28，实 :32-33，**再漂 +5** |
| `_PLAN_GUARD_FIELD_NAMES`（安全闸门元组） | evidence 标 `:12-29` 又标 `:12-24` | **`:12-31`**（元组体 :12-30，含 16 个字段名，闭括号 :31） | evidence 两处都低估字段数；实为 16 字段 :13-30 |
| `_plan_guard_fields`（闸门取数函数） | evidence 标 `:75-77`/`:75-88` | **`:80-82`** | 漂移 +5 |
| core 收口点 `def selected_plan_role` | evidence `:201-202` | **`core/services/scheduler/schedule_result_view_context.py:201-202`** | ✅ 无漂移，核准 |

**真实代码证据**：
- web 副本 `scheduler_navigation_publish.py:36-37`：`def selected_plan_role(plan_resolution): return str(plan_resolution.get("selected_role") or ROLE_ADOPTED)`
- core 收口点 `schedule_result_view_context.py:201-202`：`def selected_plan_role(plan_resolution): return str((plan_resolution or {}).get("selected_role") or ROLE_ADOPTED)` —— **core 多了 `(plan_resolution or {})` 的 None 防御**。
- 副本 `import ROLE_ADOPTED` 来源校正：registry 描述含糊处暗示从 `schedule_result_view_context` 导入，**实测 `:6` 是 `from core.services.scheduler.schedule_plan_query_service import ROLE_ADOPTED`**（与 core 收口点的 `:8 from core.models.schedule_plan_role import ROLE_ADOPTED` 不同模块，但**同一常量**——见字段 7）。

> ⚠️ 行号高度敏感：本文件已在工作树漂移名单（`git diff b08162cd` +8 行），且 B01（R54/R58）将再动本文件，R44 动手前必须重新回盘。

## 字段 2：当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05` = **planned**；本地 `def selected_plan_role` 仍是 web 自带实现（:36-37），未改为 re-export core，未收口。
- **一致**。`current_evidence` 称「未收口、无 parity 测试、行号已漂」——逐条复核均属实（仅 evidence 自身的行号 :36-37 vs :31-32 与字段数 :12-24 vs :12-31 有内部笔误，已在字段 1 校正）。
- 状态判定：**planned（待修，未动）**，与 status evidence 实质一致。

## 字段 3：病理 / 严重度 / 桶 / 簇

- **病理 P5（重复实现绕过已存在收口点）**：web `:36-37` 的 `selected_plan_role` 与 core `schedule_result_view_context.py:201-202` 是逐字近似副本，core 已是该概念的统一收口点（`gantt_plan_query.py:46-47` 已通过 re-export 收口到它）。web 维护第二份实现 = 经典 P5 副本债。
- **严重度 low**：副本只喂 `effective_plan_role=` 模板显示 kwarg（字段 8），不碰任何安全闸门，行为差异仅在 `plan_resolution=None` 边界（web 会 AttributeError，core 返回 ROLE_ADOPTED）——但生产调用方从不传 None（字段 7）。
- **桶 B02（身份族 / plan_role 概念同一性）；簇 C01**。对抗实证已 refute「web 多兜底、统一会改行为」的原报告方向（见 `planned_fix_hint` Phase1 #5 纠偏；字段 7 详述）。

## 字段 4：修法类

**owner_pending=true → 待 owner 裁断，本档案不给终态修法、不分配执行批次。** 以下为依据现状的推荐方向（非终态）：

- **修法类：收口（re-export，照搬 `gantt_plan_query.py:46-47` 已有范式）**。
- **收口点（已存在，非新建）**：`core/services/scheduler/schedule_result_view_context.py:201-202 selected_plan_role` —— 这是 B02 身份族的统一点，已被 `core/services/scheduler/gantt_plan_query.py:46-47` 以 `from .schedule_result_view_context import selected_plan_role as _selected_plan_role` + `def selected_plan_role(...): return _selected_plan_role(...)` 收口；R44 照搬此范式。**不新建任何模块**（符合铁律 5：收口到已存在点）。
- **推荐落地**：删 `:36-37` 本地 def，把 `:7` 的 `from core.services.scheduler.schedule_result_view_context import default_plan_resolution_dict, plan_role_filter_fields` 扩为同时 import `selected_plan_role`（直接 re-export，因 web 层调用方就 `from .scheduler_navigation_publish import selected_plan_role`，re-export 名字必须仍在本模块命名空间）。
- **不违灵魂线**：core 版比 web 版**更**防御（多 `(plan_resolution or {})`），收口只会保持或增强行为，不新增兜底/不静默回退/不吞错，无需改 loud raise。
- **owner 须裁断的点**：①是否接受「None 输入行为从 AttributeError 变为返回 ROLE_ADOPTED」这一边界变化（生产无 None 调用方，但属可观察行为差异）；②是否等 R21 删 `gantt_plan_query` shim 后改为直接 re-export core（范式归并）。

## 字段 5：同文件碰撞债（scheduler_navigation_publish.py）

同文件三债 R44 / R54 / R58，**全部改 `scheduler_navigation_publish.py`**，行号互撞风险高（本文件已漂移态）：

| sibling | 改哪段 | 与 R44 关系 | 谁先 |
|---|---|---|---|
| **R54**（B01，guard 收口） | `_PLAN_GUARD_FIELD_NAMES :12-31` / `_plan_guard_fields :80-82` 安全闸门路径 | **不同符号、同文件**。R54 动闸门元组，R44 动 `:36-37` getter。R54 落地会插/删字段名 → `_PLAN_GUARD_FIELD_NAMES` 元组行数变 → `:36-37` 整体位移 | **R54 先**（全局#1：B01 先落） |
| **R58**（B01，P2 承重类注释） | 本文件某段补「我是故意的」注释 | **不同符号、同文件**。注释行会插入 → 下方所有行号位移 | **R58 先**（注释先落，且 B01 同批） |
| **R44**（本债，B02） | `:36-37 def selected_plan_role` 改 re-export + `:7` import 扩列 | —— | **R44 最后** |

- **撞行号**：R54 改元组、R58 加注释都会让 `:36-37` 漂移；R44 删 `:36-37` 又会让下方 `requested_plan_role` 之后的 `_plan_guard_fields` 等再漂。**三债必须同批或显式串行**（PHASE0 §3 跨桶同文件边）。
- **dict 键位移**：R44 不碰任何 dict 键（只删一个纯函数 def + 扩一行 import），不引发键位移；但 R54 动 `_PLAN_GUARD_FIELD_NAMES` 元组键集，R44 须确认 R54 落地后 `:7` import 行号、`:36-37` def 行号重新回盘。
- **硬序结论**：**R58 → R54 → R44**（或 B01 整批先落、R44 在 B01 之后单独回盘再改）。

## 字段 6：同收口点债（收口到 `schedule_result_view_context.selected_plan_role`）

收口到**同一 core 符号** `schedule_result_view_context.py:201-202 selected_plan_role` 的其他面：

- **`core/services/scheduler/gantt_plan_query.py:46-47`** —— 已收口（re-export `_selected_plan_role`）。R44 是「同一收口点的第二个 web 入口」，即 R44 落地后会有 navigation_publish + gantt_plan_query 两处 re-export 同一 core 符号。**顺序不敏感**：两处都是纯转发，互不依赖。
- **R21**（co-change，`gantt_plan_query.py`/`scheduler_gantt.py`）：若 R21 先删 `gantt_plan_query.py:46-47` 这层 shim，则 R44 照搬的「范式」悬空——但**非硬阻塞**，R44 可改为直接 `from ...schedule_result_view_context import selected_plan_role`。**顺序敏感性：弱**（仅范式参照，不共享可变状态）。
- 收口点本身 `selected_plan_role` 当前唯一实现在 core，无并发改写者；R22（同 `schedule_result_view_context.py`）若改的是别的符号则不撞，须 owner 核 R22 触及行段。

## 字段 7：收口行为差异及所需 parity 测试

**新旧两路逐分支对照**：

| 输入 `plan_resolution` | web 旧路 `:36-37` `str(pr.get("selected_role") or ROLE_ADOPTED)` | core 新路 `:201-202` `str((pr or {}).get("selected_role") or ROLE_ADOPTED)` | 等价? |
|---|---|---|---|
| `{"selected_role": "baseline_best"}` | `"baseline_best"` | `"baseline_best"` | ✅ |
| `{"selected_role": None}` | `"adopted"` | `"adopted"` | ✅ |
| `{"selected_role": ""}` | `"adopted"`（空串 falsy → or ROLE_ADOPTED） | `"adopted"` | ✅ |
| `{}`（缺键） | `"adopted"` | `"adopted"` | ✅ |
| **`None`** | **AttributeError**（`None.get`） | **`"adopted"`** | ❌ **反例：None → raise vs "adopted"** |

- **唯一行为差异**：输入 `None` 时，web 旧路抛 AttributeError，core 新路返回 `"adopted"`。这是 core **更防御**（原报告称「web 多兜底」方向反了，已 refute）。
- **常量同一性已核**：web 副本 `ROLE_ADOPTED` ← `schedule_plan_query_service:10` ← `core/models/schedule_plan_role.py:5 = "adopted"`；core 收口点 `ROLE_ADOPTED` ← `schedule_result_view_context.py:8` ← 同一 `core/models/schedule_plan_role.py:5`。**两版同一常量值 `"adopted"`，统一不改返回值映射。**
- **所需 parity 测试**：新增 `test_selected_plan_role_parity`，对上表 5 个边界值断言收口前后等价；**显式钉 None 分支的行为变化**（owner 须裁断是否接受 None→"adopted"）。灵魂线禁过度防御，倾向直接接受 core 的 None 容忍；生产无 None 调用方（字段 8），反例实际不可达。

## 字段 8：爆炸半径

**生产消费者（喂给谁）**：
- `web/routes/domains/scheduler/scheduler_week_plan.py:44`（`from .scheduler_navigation_publish import selected_plan_role`）→ `:352 effective_plan_role=selected_plan_role(plan_resolution)`（模板显示 kwarg）。
- `web/routes/domains/scheduler/scheduler_gantt.py:30`（import）→ `:299 effective_plan_role=selected_plan_role(plan_resolution)`（模板显示 kwarg）。
- 两处**入参恒为 dict**（`plan_resolution` 来自上游 resolve，从不为 None），故字段 7 的 None 反例在生产**不可达**。
- 旁证：同概念别路 `scheduler_week_plan.py:232 "effective_plan_role": plan_resolution.get("selected_role") or ROLE_ADOPTED` 内联实现（属另一债面，非本 R44 收口范围，owner 可关联 R10/R72）。

**测试消费者**：
- `tests/web_pages/test_reports_workbench_navigation_contract.py:406-462`（`test_scheduler_navigation_publish_keeps_plan_guard_fields`）import `publish_analysis/gantt/week_plan_navigation_context`，**不 import / 不钉 `selected_plan_role` getter 实现** → R44 收口**不会让该契约测试变红**。无 SP05 拓扑引用。

**灾难链（删错/统一错会静默炸什么）**：
- 因 `selected_plan_role` 只喂 `effective_plan_role=` **模板显示** kwarg，统一错最坏后果 = 周计划 / 甘特页**显示**的方案角色文案错位（low 影响、可观测、非数据/非派工）。
- **绝不触及真安全闸门**：`can_dispatch` / `can_write_feedback` / `is_scenario_preview` / `is_superseded_by_newer_version` / `plan_role_status` 全走独立 `_PLAN_GUARD_FIELD_NAMES :12-31` + `_plan_guard_fields :80-82` 路径，与 `selected_plan_role` 无交集。**误碰闸门=R54 territory，是字段 9 禁区**。

## 字段 9：承重毗邻标记 + 禁区行

- 本债 `load_bearing=false`、`lb_no_touch=null` —— **R44 本身非承重**。
- **同文件毗邻承重**：`scheduler_navigation_publish.py` 同文件含 R58（B01，P2 **承重类**注释债）+ R54（B01，guard 收口）。**§3 跨桶同文件边**：动 R44 时存在误碰 R54/R58 territory 的风险。
- **禁区行（动 R44 时绝不触碰）**：
  - `:12-31` `_PLAN_GUARD_FIELD_NAMES` 安全闸门元组（16 字段：requested_plan_role / effective_plan_role / plan_role_status / is_scenario_preview / is_comparison / is_superseded_by_newer_version / is_official_plan / is_preview_plan / is_current_executable_official_version / can_dispatch / can_write_feedback / plan_identity_error / plan_identity_blocking_error / plan_identity_blocking_scope / result_summary_parse_failed / result_summary_parse_reason）。
  - `:80-82` `_plan_guard_fields`（真派工 / 预览 / 版本闸门取数函数）。
  - `:33` `requested_plan_role`（sibling getter，非本债，勿顺手统一）。
  - **R44 仅允许动 `:36-37`（删 def）+ `:7`（扩 import）两处。**

## 字段 10：分层 / 导入环风险

- **方向合法**：web → core（`web/routes/.../scheduler_navigation_publish.py` import `core/services/scheduler/schedule_result_view_context.py`），符合分层（web 调 core service 是既有合法方向，本文件 `:6-7` 已多次 web→core import）。**不引入越层**（无 core→web、无 core.algorithms→services、无 core.models→services）。
- **导入环风险：无**。`schedule_result_view_context` 不 import 任何 `web.*`；R44 只是在 web 侧多一条指向 core 的边，core 侧不回指 web。`gantt_plan_query.py:8` 已是 `core→core` 同向 import 同一符号，无环先例。
- AST 违规：0（收口不新增任何跨层 import 种类，只是复用已存在的 web→core 边）。

## 字段 11：测试耦合（动手前先迁哪些测试）

- **无需先迁测试**：现有唯一相关契约测试 `tests/web_pages/test_reports_workbench_navigation_contract.py:406-462` 钉的是 `publish_*_navigation_context` 与 guard 字段，**不钉 `selected_plan_role` getter**，收口不破它。
- **动手前新增（非迁移）**：字段 7 的 `test_selected_plan_role_parity`（5 边界值 + None 反例）应在收口**同 PR 内**补，作为收口正确性自证。
- 若 owner 批准收口，建议在 parity 测试里同时覆盖 `gantt_plan_query.py:46-47` re-export，确认三处（core / gantt_plan_query / navigation_publish）返回完全一致。

## 字段 12：前置依赖（先做哪些债 / 哪些 F 门）

**硬序（必须）**：
1. **B01 先落本文件**：R54（guard 收口）+ R58（P2 承重注释）同改 `scheduler_navigation_publish.py`，**全局#1 要求 B01 先于 B02**。R44 必须排在 B01 动完本文件**之后**，且动手前**重新 rg 回盘** `:7`/`:36-37` 真实行号（本文件已漂移态 + B01 会再改行号）。
2. **同文件三债同批或显式串行**：R58 → R54 → R44（字段 5）。
3. **F 门**：收口后须过 AST 分层门（字段 10，预期 0 违规）+ 新增 parity 测试绿（字段 11）+ `regression_reports_workbench_navigation_contract` 不变红（字段 8）。

**软序（非硬阻塞）**：
- **R21**：R44 照搬 `gantt_plan_query.py:46-47` re-export 范式；若 R21 先删该 shim，R44 改为**直接 re-export core**（范式悬空但可降级，非硬阻塞）。
- co-change R72：同概念 `effective_plan_role` 内联实现（week_plan:232），owner 可决定是否随 B02 身份族一并收口。

**owner_pending=true 总结**：依赖 / 爆炸半径已分析透（low 影响、只喂模板显示、不碰闸门、core 更防御、统一不改生产行为）；**终态修法与执行批次待 owner 裁断**（裁断点：None 边界行为变化是否接受、是否等 R21 范式归并、是否同批 R72 内联面）。

## 对抗核验（第二双眼睛）

**结论：PASS（独立复核未找到反证）。**

**重 grep 回盘命令与结果**：
1. `rg -n "selected_plan_role|requested_plan_role|_PLAN_GUARD_FIELD_NAMES|_plan_guard_fields|ROLE_ADOPTED" web/routes/domains/scheduler/scheduler_navigation_publish.py`
   - `def selected_plan_role` → **`:36-37`**（档案字段1 一致）；`def requested_plan_role` → **`:32-33`**（一致）；`_PLAN_GUARD_FIELD_NAMES` 元组体 **`:12-31`**（一致）；`_plan_guard_fields` → **`:80-82`**（一致）；`import ROLE_ADOPTED` → **`:6 from ...schedule_plan_query_service`**（一致，档案字段1校正属实）。
   - 闸门元组实数 16 字段（:13-30）逐字核对：与档案字段9列出的 16 字段名完全一致。
2. `rg -n "def selected_plan_role|ROLE_ADOPTED" core/services/scheduler/schedule_result_view_context.py` → core 收口点 **`:201-202` `return str((plan_resolution or {}).get("selected_role") or ROLE_ADOPTED)`**——确含 `(plan_resolution or {})` None 防御，**核准「core 更防御、原报告方向反了」**。无漂移。
3. `rg -n "selected_plan_role|import" core/services/scheduler/gantt_plan_query.py` → re-export 范式 **`:18 selected_plan_role as _selected_plan_role` + `:46-47 def...return _selected_plan_role(...)`**——范式真实存在，档案字段4/6 引用准确（仅 import 别名行实为 :18，def 实为 :46-47，与档案 :46-47 一致）。
4. `rg -n "selected_plan_role" web/`（消费者回盘）→ 生产消费者实为 **week_plan `:44`(import)/`:352`(调用)、gantt `:30`(import)/`:299`(调用)**；档案字段8标 `:352`/`:299` 一致（registry phase1_blast 旧标的 :337/:298 已漂，档案字段8已更正为现值）。另发现 `reports_plan_template_fields.py:81` 与 `week_plan:232` 为同概念**内联**面——档案字段8/字段4 已正确将其归为 R72/R10 关联面、**明确排除出 R44 收口范围**，无遗漏。
5. `rg -n ... tests/web_pages/test_reports_workbench_navigation_contract.py` → 唯一契约测试只 import `publish_*_navigation_context` 与断言 `requested_plan_role=/effective_plan_role=` 字段值，**不 import 也不钉 `selected_plan_role` getter 实现**——核准「收口不会变红」。
6. `rg -n "from web|import.*web" core/services/scheduler/schedule_result_view_context.py` → 无任何 web import——**核准无导入环**，web→core 单向合法。
7. 常量同一性：`schedule_plan_role.py:5 ROLE_ADOPTED = "adopted"`，web 侧经 `schedule_plan_query_service:10` re-export、core 侧经 `schedule_result_view_context.py:8`——同一常量值，核准。

**承重误删风险：无。** R44 `load_bearing=false`/`lb_no_touch=null`，本身非承重；修法是收口到**已存在** core 点（非新建模块，符合铁律5），照搬真实存在的 gantt_plan_query 范式。档案正确将毗邻 R54/R58 的闸门元组 `:12-31`、`_plan_guard_fields :80-82`、sibling `:32-33` 划为禁区行且修法不触碰，无「看似该统一」的误删/透传/加形参；core 比 web 更防御属既有行为非新增兜底，不违灵魂线。

**owner_pending 合规：PASS。** 档案字段4开宗明义「不给终态修法、不分配执行批次」，全部修法以「推荐方向（非终态）」措辞，3 个裁断点（None 边界/R21 范式归并/R72 同批）明确交还 owner，未擅自定终态或排执行批次。

**唯一可议（不构成争议）**：行号高度敏感——本文件处工作树漂移态且 B01(R58→R54)将再改本文件，档案已多处显式要求「R44 动手前重新回盘」，核验当下行号一致，但执行期须按硬序重盘。属档案已声明的执行约束，非档案缺陷。

**需升级第三方裁：n。**
