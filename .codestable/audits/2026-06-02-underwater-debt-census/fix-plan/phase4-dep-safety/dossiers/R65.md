# R65 逐债档案 — `_target_url` 死分支(9 条 spec 第 3 字段全非空字面量 → `plain_url` 恒真)

> 文件: `web/viewmodels/scheduler_navigation_links.py` | 桶 B17 | 簇 C01 | 严重度 low | 病理 P6 死分支 | 承重 false | owner_pending false
> 本档案已补登 2026-06-08 B 执行终态。2026-06-05 行号是执行前证据,不要再照旧行号重复删除。

## 0) 2026-06-08 B 执行终态补登

- **状态**: fixed。`_target_url` 已从 `web/viewmodels/scheduler_navigation_links.py` 删除。
- **三件套结果**: 旧 `plain_url or _target_url(...)` 已化简成直接使用 `plain_url`;本文件 `urlencode` 与 `query_for_target` 两个孤儿 import 已删除。
- **保留项**: `TARGET_PAGE_PATHS` 仍保留,继续给 `build_report_navigation_links` 使用;没有误删报表导航承重入口。
- **护栏**: `tests/web_pages/test_reports_workbench_navigation_contract.py` 已有 `test_all_nav_specs_have_nonempty_plain_url`,把“9 条 specs 的 plain_url 必须非空”钉成测试。
- **当前回盘**: `rg _target_url core/ web/ data/ tests/` 零命中;旧 `plain_url or _target_url` 零命中。

## 1) 执行前 file:line 证据 + 当前漂移记录

R65 的「原子三件套」三个改点,2026-06-05 执行前全部按符号名 rg 回盘:

| 物件 | 符号 | old_location_DONT_TRUST | 当前真实 file:line | 漂移 |
|---|---|---|---|---|
| ① 死 def | `_target_url` | :74-78,160 | `web/viewmodels/scheduler_navigation_links.py:74-78`(def 体 :74→:78,共 5 行) | **无漂移**(:74-78 精确命中) |
| ② 死分支调用点 | `plain_url or _target_url(...)` | :160 | `:160` | **无漂移** |
| ③a 孤儿 import | `from urllib.parse import urlencode` | :4 | `:4` | **无漂移** |
| ③b 孤儿 import | `from .scheduler_workbench_link_query import query_for_target` | :6 | `:6` | **无漂移** |

证据(回盘命令实采):
- `:74` `def _target_url(context: Dict[str, Any], target_page: str, *, view: Optional[str] = None) -> str:`
- `:75` `query = query_for_target(context, target_page, view=view)`
- `:76` `encoded = urlencode(query)`
- `:77` `path = TARGET_PAGE_PATHS[target_page]`
- `:78` `return f"{path}?{encoded}" if encoded else path`
- `:160` `link = _plain_link(label, plain_url or _target_url(context, str(target_page), view=view), target_page=str(target_page or key))`
- `:4` `from urllib.parse import urlencode`(同文件 urlencode 唯一其他用处在 :76 内,即 `_target_url` 体内)
- `:6` `from .scheduler_workbench_link_query import query_for_target`(同文件 query_for_target 唯一其他用处在 :75 内,即 `_target_url` 体内)

2026-06-08 执行后回盘:`_target_url`、旧 `plain_url or _target_url(...)`、本文件 `urlencode` import、本文件 `query_for_target` import 均已不在当前文件中。上表行号只保留为历史证据。

## 2) 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = fixed`;evidence 已补登为 2026-06-08 B 执行终态:三件套已完成,护栏测试已在。
- 本次回盘:当前已无 `_target_url` def、无旧短路调用点、无本文件孤儿 import → **与 fixed evidence 一致**。
- 死分支恒真性仍由护栏承接:`test_all_nav_specs_have_nonempty_plain_url` 会在未来新增空 `plain_url` spec 时直接变红。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6 死分支**:`_target_url` 是一段语法可达、运行期不可达的死代码(被 `or` 短路永久遮蔽)。
- 严重度 **low**:仅可读性/误导债——读者会误以为「无字面量 url 时回落到动态 `_target_url` 构造」,但 9 条 spec 决定此回落永不发生。**无运行期影响、无数据正确性影响**。
- 桶 **B17**;簇 **C01**;partition `delta-web-workbench`;kind `real`(非幻觉债)。
## 4) 修法类

**已直删并化简(原子三件套已同提交完成)**。承重 false,故允许删除(非「仅注释」)。owner_pending false。

2026-06-08 已完成的三件套:
- ① 删 `_target_url` 整个 def。
- ② 把旧 `plain_url or _target_url(...)` 化简为直接使用 `plain_url`。
- ③ 删本文件两个就此孤儿的 import:`urlencode` 与 `query_for_target`。

**当前铁律**:不要重复执行历史三件套;后续只需确认 `_target_url` 与旧短路表达式零命中,并确认 `TARGET_PAGE_PATHS` 仍被保留。

灵魂线核查(✅ 通过):本修法是**纯删除 + 短路化简**,不新增任何兜底/静默回退/except 吞错/过度防御;不引入新形参、不透传、不合并、不统一。P6 不涉及 P4 的 loud-raise 要求(无静默吞错可改)。

**非收口债**:R65 不收口到任何统一点(铁律 5 不适用),纯局部死代码清除。无新建模块。

## 5) 同文件碰撞债(siblings)

同文件 `scheduler_navigation_links.py` 共 3 条债互为干扰边:R65(本条)、**R64**(声明 sibling)、**R42**(interference same_file)。

### sibling R64 —— `_has_navigation_date_range` 新建即死、全仓零引用
- R64 已与 R65 同提交完成:删 `_has_navigation_date_range`(纯删除,非承重,无调用点,无 import 孤儿)。
- **改的段不同**:R64 在 :66-67(位于 `_target_url` def :74 **上方**);R65 在 :74-78 / :160 / :4 / :6。**两者代码区无重叠**。
- **行号位移现状**:R64/R65 已执行,文件下方锚点已上移。后续 R42/R67 进入本文件前必须按符号重 rg,不要信旧 :74/:160/:186 等裸行号。
- **dict 键位移**:无。两条均不动 `_plain_link` 返回的 dict 结构、不动 specs 元组字段顺序、不动 `_REPORT_CONTEXT_FIELD_NAMES`。无键位移风险。

### interference R42 —— `plan_id` 死面包屑全链(same_file, **not** same_symbol)
- R42/R67 是后续 Batch-C 同文件动作。R64/R65 已完成,它们的旧行号已不可信;R42/R67 必须按符号重定位。

## 6) 同收口点债

**无**。R65 是直删债,不收口到任何统一符号。`interference_edges` 中无 `same_symbol:true` 边(R64、R42 均为 `same_symbol:false`)。故**无同收口点债、无收口顺序敏感性**。铁律 5/6(收口点神圣、收口点须已存在)对 R65 不适用。
## 7) 收口行为差异及所需 parity 测试

R65 非收口债,但历史 :160 的「死分支化简」本质是一次表达式等价替换,需逐分支证明 `plain_url or _target_url(...)` ≡ `plain_url`。该证明已由 2026-06-08 执行补登承接:

- **新旧两路在 9 条 spec 上是否逐分支等价?** 是。`plain_url` 为短路 `or` 的左操作数。Python `a or b`:`a` truthy → 返回 `a`,`b` 不求值;`a` falsy → 返回 `b`。
  - 9 条 spec 的 `plain_url` 全为非空字符串字面量(`'/scheduler/'` 等),**非空字符串恒 truthy** → 9/9 全部短路返回 `plain_url`,`_target_url` 永不求值。
  - 化简为 `plain_url` 后,9/9 仍返回同一非空字面量。**逐分支输出完全相同**。
- **反例搜寻(边界值)**:能让两路分歧的唯一情形是 `plain_url` 为 falsy(`''`、`None`、`0`)。
  - 当前 9 条 spec **无任何一条** `plain_url` 为 falsy(全为 `/scheduler...` 路径)。故当前**无反例**,等价成立。
  - ⚠️ **回归护栏**(语义钉子):等价性**依赖「specs 第 3 字段恒非空」这一不变量**。2026-06-08 已落 `test_all_nav_specs_have_nonempty_plain_url`,未来有人新增 `plain_url=''` 或 `plain_url=None` 的 spec 会直接变红。
- **已落护栏测试**:`test_all_nav_specs_have_nonempty_plain_url` —— 遍历 `specs` 元组,断言每条第 3 字段(`plain_url`)`!= ''` 且 `is not None`。这条把「化简成立的前提」钉成红线,防未来回归。
  - 反例形态:`spec=('x','标签','',None,None)` → 旧路 `'' or _target_url(...)` 求值 `_target_url`;新路返回 `''`。护栏测试 #2 会在新增此类 spec 时立即变红。

## 8) 爆炸半径

phase1_blast 9 字段已核,逐条独立回盘:

- **生产消费者(prod_consumers)**:清理后 **0**。`rg _target_url core/ web/ data/` 零命中,旧唯一引用点已化简为 `plain_url`。
- **测试消费者(test_consumers)**:**0**。`rg _target_url tests/` 零命中;新增护栏只检查 specs plain_url 非空,不消费 `_target_url`。
- **import 孤儿连锁**:
  - `urlencode`:本文件 import 已删;其它文件的 `urlencode` 真用不受影响。
  - `query_for_target`:本文件 import 已删;`scheduler_workbench_links.py`、`scheduler_workbench_link_query.py` 中的真实使用保留。
  - `TARGET_PAGE_PATHS`:本文件 import **保留**,继续服务 `build_report_navigation_links`。这是复核重点。
- **灾难链(删错/化简错会静默炸什么)**:
  1. 删 def 但忘化简 :160 → `_target_url` 未定义 → import/lint 阶段 NameError,`build_scheduler_navigation_links` 不可用 → 调度导航条全挂(虽 fail-fast 非静默,但全栈级破坏)。
  2. 误删 :7 `TARGET_PAGE_PATHS` import(以为是孤儿)→ `:177 build_report_navigation_links` NameError → 报表导航条挂。**这是本债最大爆炸风险**:把「:7 仍被 :177 使用」误判为孤儿一并删。
  3. 化简错成 `plain_url or ''` 之类 → 无意义但不炸;化简成只留 `_target_url(...)`(删错操作数)→ 删了 def 后立刻 NameError。
- **downstream_effect**:仅可读性/误导,无运行期行为变化(死分支本就不执行)。**安全级别高,但执行精度要求高**(易误删 :7)。

## 9) 承重毗邻标记

- `load_bearing = false`;`lb_no_touch = null`;`lb_colocation_danger = none`。
- `scheduler_navigation_links.py` **不在承重清单**。同文件 3 条债(R65/R64/R42)**均非承重**。
- **无 LB 毗邻、无禁区行**。修法允许直删,不受铁律 3「承重仅注释」约束。
## 10) 分层 / 导入环风险

- R65 是**纯删除**:删 def、删两 import、化简一行。**不新增任何 import** → 不可能引入跨层 import,不可能制造导入环。
- 删除方向只会**减少**依赖(本文件不再依赖 `urllib.parse.urlencode` 与 `.scheduler_workbench_link_query`)。
- 分层红线(铁律 6):0 AST 违规。R65 不触碰 `core.algorithms`/`core.services`/`core.models` 任何层,纯属 `web/viewmodels/` 内单文件清理。**无越层、无环风险**。

## 11) 测试耦合

- **执行后无需迁任何测试**。`_target_url` 在 `tests/` 下零命中,删除不破现存测试。
- **已新增护栏**:`test_all_nav_specs_have_nonempty_plain_url` 已落在 `tests/web_pages/test_reports_workbench_navigation_contract.py`。
- 已有 `build_scheduler_navigation_links` 的契约测试继续作为公开行为冒烟;化简后 url 输出不变。

## 12) 前置依赖

- **前置债:无硬前置**。R65 已在 Batch-A/G02 完成。
- **F 门:无**。当前保留回归验证要求:导航相关裸单测 + ruff + py38 语法扫描 + residual rg。
- **协同结果**:已与 sibling **R64 同次原子提交**完成。
- **R65 内部硬约束**:三件套已同提交完成,未留下 `_target_url` 悬空引用。
- 与 interference R42/R67 节奏脱钩;后续进入同文件时按符号重定位。

---
### 返回摘要锚
R65 三件套已完成、非承重、非收口、零 `_target_url` 消费者;最大复核点是确认 `TARGET_PAGE_PATHS` import 仍在并服务报表导航。

---
## 对抗核验(第二双眼睛)2026-06-05

**历史结论:PASS**。独立 rg/grep 全面回盘,未找到任何反证。该核验说明执行前为什么可以删;2026-06-08 执行后以字段 0 的 fixed 终态和当前 rg 结果为准。

重 grep 命令与实采结果:

1. `grep -n "_target_url" scheduler_navigation_links.py` → `:74`(def)、`:160`(唯一调用点)。**与档案零漂移一致**。
2. `grep -n "urlencode" …` → `:4`(import)、`:76`(_target_url 体内唯一用)。`grep -n "query_for_target" …`(本文件)→ `:6`(import)、`:75`(_target_url 体内唯一用)。两 import 删 def 后**确成孤儿**,行号零漂移。
3. `rg -n "_target_url" core web data` → 仅命中 `scheduler_navigation_links.py:74` + `:160`,**全仓无第二引用**。`rg -n "_target_url" tests` → **exit=1 零命中**,直删不破任何测试。
4. **死分支恒真独立证实**:Read :143-153 specs 9 条,第 3 字段(plain_url)全为非空字面量路径(`/scheduler/` 等),:160 `plain_url or _target_url(...)` 因短路 `_target_url` 永不求值。判定成立。
5. **:7 TARGET_PAGE_PATHS 陷阱证实**:`grep -n "TARGET_PAGE_PATHS" …` → `:7`(import)、`:77`(_target_url 内)、`:177`(build_report_navigation_links 真用,Read :177 确认)。删 _target_url 后 **:7 仍被 :177 使用,绝不可删** —— 档案标红正确,这是本债真实最大爆炸点。
6. **孤儿 import 删本文件不波及他处**:`rg query_for_target .` → 另在 `scheduler_workbench_links.py:12/430`、`scheduler_workbench_link_query.py:275` 真用;`rg "from urllib.parse import urlencode"` → 另在 `scheduler_workbench_links.py:4`、`scheduler_resource_dispatch_query.py:4` 真用。删本文件 :4/:6 安全。
7. **承重/分层/灵魂线复核**:load_bearing=false、lb_no_touch=null,不在承重清单 → 无承重误删风险。纯删除不新增 import → 0 跨层 import、无环。修法无兜底/静默回退/except 吞错/透传/加形参,P6 不涉 P4 loud-raise。owner_pending=false 给终态修法合规,无擅自越权。

**承重误删风险:无**。**需第三方裁断:n**。
行号复核:一致。争议点:无。
