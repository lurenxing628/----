# R07 逐债档案 — 写后任务卡 schedule 缺失 loud raise 已在 B 前完成

> **2026-06-09 台账回写终态：fixed，但非 B 战果。** loud-raise 修复在 `6759b0f9`（B 基线祖先）已落地，本轮仅回写事实：该分支现在用 `ValidationError(field="schedule_id")`，不是 O31 计划里的 `AppError(ErrorCode.NOT_FOUND)`。文案和错误类型是否要按 O31 收口仍是后续事项；要求的专项回归测试也尚未补。

## 现盘事实

- `core/services/scheduler/resource_dispatch_execution_service.py::task_card_for_feedback_context` 在 `schedule is None` 时已经 raise。
- 旧的“伪正式采用方案 / can_write_feedback=True”静默卡片支路已不存在。
- 写侧真正门禁仍在 feedback service 路径，不消费这张任务卡作为授权来源。

## 后续边界

- 不把这条记成 B 阶段战果。
- 若后续要对齐 O31，需显式改为 `AppError(ErrorCode.NOT_FOUND)` 并补对应导入和专项回归。
- 不允许改回 `return None`、默认卡片或吞异常。
