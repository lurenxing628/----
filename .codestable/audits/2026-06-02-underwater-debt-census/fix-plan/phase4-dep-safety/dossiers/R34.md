# R34 逐债档案 — schedule_repo 版本-only 查询方法群（plan-role 迁移残渣）

> 本档案只读不改、只产计划。所有断言附当前工作区 file:line 证据（2026-06-05 回盘）。
> owner_pending=true：本债待 owner 裁断，下文给出依赖/爆炸半径全量分析，但**终态修法仅作建议、暂不分配执行批次终态**。

- 债 id: R34 | bucket B12 | partition data-repos | severity medium | pathology P3 | 承重 load_bearing=false | lb_no_touch=null
- 主文件: data/repositories/schedule_repo.py
- 干扰簇: C01 | 同文件兄弟债: R35 | needs_adversarial=true（verdict=real_debt，refuted=true）

---

## 1) 当前真实 file:line（grep 回盘）+ 漂移记录

R34 是「三个死方法群」。按符号名 rg 回盘当前工作区：

| 符号 | old_location（勿信） | 当前真实 file:line（回盘） | 方法体范围 | 漂移 |
|---|---|---|---|---|
| `get_version_time_span` | :36 | data/repositories/schedule_repo.py:36 | :36–:59（`return {"version"...}` 收尾，slice 第30行=文件第59行） | **无漂移** |
| `list_overlapping_with_details` | :114 | data/repositories/schedule_repo.py:114 | :114–:126 | **无漂移** |
| `list_dispatch_rows_with_resource_context` | :128 | data/repositories/schedule_repo.py:128 | :128–:158（`return cast(...)` 收尾） | **无漂移** |

回盘命令：`rg -n 'def get_version_time_span|def list_overlapping_with_details|def list_dispatch_rows_with_resource_context' data/repositories/schedule_repo.py` → 36 / 114 / 128，与盘上三个 def 行号**逐行一致**。schedule_repo.py 不在漂移名单。`location_status` 字段标 ✅准 与回盘吻合。

补充回盘（修法落点行号，slice 推算）：
- get_version_time_span 体：:36 def → :59 return（含 MIN/MAX + TRIM 守卫 SQL，None 收敛逻辑）
- list_between（R35，夹在其间）：:61–:69
- list_overlapping_with_details 体：:114 def → :126 return
- list_dispatch_rows_with_resource_context 体：:128 def → :158 return

## 2) 当前状态 + 与 status evidence 是否一致

- status_2026_06_05 = **planned**。
- 与 current_evidence 一致性：**完全一致**。证据逐条复核：
  - 三死方法 def 行 :36/:114/:128 原样存在 ✅（本轮 rg 命中）；
  - 四件套退场零落地 ✅：facade_delegation.py:31/36/39–40 三条 get_type_hints 断言仍在（本轮 sed 复核 :31/:36/:40）；detail_queries.py:168/198/218/225/240/247/254/268/274/288 十处用例仍调死方法（本轮 rg 命中全部 10 行）；gantt_critical_chain_unavailable.py:59 无效 monkeypatch 仍在；benchmark_fjsp.py:503 仍 `span_repo.get_version_time_span(version)` 未 repoint。
- 结论：planned 状态名副其实，零落地，无 in_progress 痕迹。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P3**：plan-role 迁移残渣。schedule_repo 的三个方法是「版本-only」盲路径——只按 `version` 查 Schedule 表，**没有 plan-role / source_table / candidate_id / scenario_id 维度**。plan-role 迁移后，活孪生迁到 `schedule_plan_query_repo.py`（带 source_table 分支 + 候选/情景守卫 + 未知 source raise ValueError），三个老方法成为「更不安全的死孪生」，仅靠测试与类型契约续命。
- **needs_adversarial=true / verdict=real_debt（refuted=true）**：对抗已判定为真债。死孪生不安全点 = 版本-only 路径无法区分 adopted / 候选 / 情景，若被误用会让候选/情景记录冒充 adopted（活孪生用 source_table 分支 + `_require_candidate_id`/`_require_scenario_id`/未知 source raise 防住）。
- **严重度 medium**：本桶 B12 内最高复杂度（唯一 P3+needs_adversarial，带四件套退场）。但生产零引用，删除本身风险极低；medium 来自「四件套退场协调面 + R35 强同批」的操作复杂度，而非运行时风险。
- **桶 B12（repo 死方法）/ home B12**。
- **簇 C01**：interference_edges 共 9 条，多数是「同文件」弱耦合（R10/R11/R12/R21/R55/R63 同在 gantt_service.py，R05 同在 schedule_plan_query_repo.py，R23 同在 schedule_plan_query_service.py）。**唯一强耦合 = R35（同 schedule_repo.py，同改）**。

## 4) 修法类

**修法类 = 直删 + 退测试（非收口）**。承重=false，灵魂线不触（删的是死路径，不引入任何兜底/静默回退）。

⚠️ **owner_pending=true：以下为建议修法，待 owner 裁断后才定终态、才分配执行批次。** 争议点见末尾「owner 待裁点」。

建议修法五步（全部依据当前 file:line）：
1. **删 schedule_repo.py 三死方法**：`get_version_time_span`(:36–59)、`list_overlapping_with_details`(:114–126)、`list_dispatch_rows_with_resource_context`(:128–158)。
2. **退 facade_delegation.py 三条 get_type_hints 断言**：:31（get_version_time_span）、:36（list_overlapping_with_details）、:39–41（list_dispatch_rows_with_resource_context return == List[ScheduleDispatchRow]）。**保留** :32–34（list_version_rows_by_op_ids_start_range，活，freeze_window 用）+ :37（list_by_version_with_details，活，gantt_critical_chain 用）。
3. **同步退 facade_delegation.py 无用 import**：删 3 方法后 `ScheduleTimeSpanRow`（仅 :31 用）与 `ScheduleDispatchRow`（仅 :40 用）变成未用 import → 删 :12 / :14 两行 import。**保留** :11 `ScheduleDetailRow`（:36/:37 仍用）。【本轮 rg 实证：ScheduleTimeSpanRow/ScheduleDispatchRow 在该文件仅各 1 处断言使用，无第二落点。】
4. **退 detail_queries.py 用例**：:168（list_overlapping_with_details）+ :198/218/225/240/247/254/268/274/288（list_dispatch_rows_with_resource_context 共 9 处）。若整个用例函数失去存在意义则整删用例函数；若仍要测「重叠/调度过滤」语义则迁到活孪生 schedule_plan_query_repo.list_detail_rows_between/list_dispatch_rows（owner 裁断，见 §11）。
5. **删 gantt_critical_chain_unavailable.py:59 无效 monkeypatch**：本轮复核 :59 monkeypatch `list_overlapping_with_details`，但生产实际走 `list_by_version_with_details`（:60 已 setattr 为 `_repo_raise` 反证），:59 是 patch 一个生产根本不调的死方法 → 纯噪声，删之。删后断言仍由 :60 raise 兜住。
6. **repoint benchmark_fjsp.py:503**：见 §8、§10——非删，是改调用点（生产/脚本真消费者）。

**这不是 P5 收口**：活孪生 schedule_plan_query_repo.py 已存在且已被生产全链使用，本债是「删死孪生 + 把仅剩的测试/基准引用迁到已存在的活孪生」，不新建任何模块，不新增收口符号。R09 例外（parse_optional_positive_int）与本债无关。

## 5) 同文件碰撞债（siblings）

唯一同文件兄弟：**R35 = `schedule_repo.list_between`**（本轮回盘 :61–:69，夹在 get_version_time_span(:36–59) 与 list_version_rows_by_op_ids_start_range(:42→实际 R35 之后) 之间）。

- **R35 现状**（本轮独立回盘 registry）：status=planned，severity=**low**，load_bearing=false，title「纯死方法:全仓零引用(含测试)」。本轮 `rg 'list_between' core/ web/ data/ tests/` → **仅命中 :61 定义行本身，零调用零测试**，与 R35 fix_hint「直删 :61–69，无引用点需同步退场」吻合。
- **各改哪段**：R34 删 :36–59 + :114–126 + :128–158（三段，分布在 R35 上方与下方）；R35 删 :61–69（一段，夹在 R34 第一段与第二段之间）。两债**改的是不相交行段**，但**行号互相位移**。
- **谁先谁后 / 撞行号**：
  - **必须同提交（硬约束）**。理由：删 R34 第一段(:36–59) 会让 R35(:61) 整体上移 24 行；若 R34 先单独提交，R35 的 :61–69 行号即刻失效，且 facade_delegation.py 的活方法断言（list_version_rows_by_op_ids_start_range / list_by_version_with_details）位置随之漂移，二次定位易出错。Phase1 DAG 已画边 R34→R35「同 schedule_repo.py，必须同提交避免删后行号二次漂移撞 facade delegation 断言」。
  - **dict 键位移**：不涉及。两债删的都是「方法 def」，非 dict 字面量键；schedule_rows.py 的 TypedDict（ScheduleTimeSpanRow/ScheduleDispatchRow）字段**不动**（仅 import 引用变化，见 §4 步3）。
- **执行顺序建议**：同一提交内，**自下而上删**（先删行号最大的 list_dispatch_rows_with_resource_context :128–158，再 :114–126，再 list_between :61–69，最后 :36–59），避免边删边算偏移。R34 与 R35 不分先后，作为一个原子 diff。

## 6) 同收口点债

**本债不做收口（是直删），故无「收口到同一符号」的同收口债。**

但需登记一个**软关联**：活孪生 `schedule_plan_query_repo.py` 同时是 **R05** 的战场（interference_edge same_file，R05=collar 扩 team 双 join + 空 id 全量）。R05 改的是活孪生 `list_dispatch_rows` 的 WHERE 子句行为（team 过滤），R34 只是「把死孪生删掉、把测试引用迁到活孪生」。

- **顺序敏感性**：Phase1 DAG 把 R05（Batch-8）排在 R34（Batch-13）之前。⚠️**registry 自带勘误**：planned_deps_hint 原措辞曾假设「R34 收敛到 column_name / 收敛到活孪生」，但本轮回盘确认 **R34 实为纯删死方法**，被删的 `schedule_repo.list_dispatch_rows_with_resource_context` 与 R05 改的 `schedule_plan_query_repo.list_dispatch_rows` 是**两个不同符号、不同文件**。因此 R05→R34 并非硬数据依赖，而是软约束：若 §11 选择「把 detail_queries.py 用例迁到活孪生」，则迁移后用例会断言活孪生行为，**应在 R05 team-join 改完后迁**，避免迁到一个即将变行为的方法上、用例随后被 R05 改动二次推翻。若选择「整删用例」则与 R05 完全解耦。
- 除此之外无其它债收口到同一符号。

## 7) 收口行为差异 + 所需 parity 测试

本债主体是直删，**唯一需要 parity 论证的点 = benchmark_fjsp.py:503 的 repoint**（§8 真消费者）。

**死方法 vs 活孪生 · 逐分支等价性（针对 version-only / adopted 路径）：**

本轮逐行比对：
- 死 `get_version_time_span(version)`（schedule_repo.py:36–59）：SQL = `SELECT MIN(start_time), MAX(end_time) FROM Schedule WHERE version=? AND TRIM(CAST(start_time AS TEXT))<>'' AND TRIM(CAST(end_time AS TEXT))<>''`；`if not row: return None`；`if not start_time or not end_time: return None`；`return {"version":int(version),"start_time":str(...),"end_time":str(...)}`。
- 活 `schedule_plan_query_repo.get_plan_time_span(version=, source_table=SOURCE_SCHEDULE, candidate_id=None)`（:257–310，本轮 sed 复核）：`source_table==SOURCE_SCHEDULE` 分支 SQL **逐字符相同**（MIN/MAX + 同样的 TRIM 守卫），同样的 `if not row → None`、`if not start_time or not end_time → None`、同样的 return dict 形状。

**结论：version-only / SOURCE_SCHEDULE 路径下两者逐分支等价（byte-equivalent SQL + 同 None 收敛 + 同 dict 形状）。** 活孪生是严格超集（多 candidate / scenario 两分支 + 未知 source raise ValueError）。

**边界值反例核查（确认无差异）：**
- 空版本（无记录）：两路 MIN/MAX 返回 NULL → `start_time`/`end_time` falsy → 两路均 `return None`。等价。
- 版本存在但全行 start/end 为空串：两路 TRIM 守卫均过滤 → 同上 None。等价。
- **唯一差异点**：调用契约。死方法位置参数 `get_version_time_span(version)`；活孪生**强制 keyword-only**（`*, version, source_table, candidate_id`）。benchmark:503 当前传裸 `version` 位置参，repoint 时必须改成 `get_plan_time_span(version=v, source_table=SOURCE_SCHEDULE, candidate_id=None)` 并 import `SOURCE_SCHEDULE`，否则 TypeError。这是**调用形态差异，非返回值语义差异**。
- **若误传 candidate_id 给活孪生**：死方法根本无此参数；活孪生 candidate 分支才用 candidate_id，SOURCE_SCHEDULE 分支忽略它 → 仍等价。无 None-vs-raise 反例（adopted 路径不触发任何 raise）。

**所需 parity 测试（repoint 自证）**：在 benchmark 改完后，对同一 (conn, version)，断言 `schedule_repo.get_version_time_span(version)`（删之前）与 `schedule_plan_query_repo.get_plan_time_span(version=version, source_table=SOURCE_SCHEDULE, candidate_id=None)` 返回 dict 完全相等（含 None 情形）。此 parity 测试只在删前做一次性回归比对（删后死方法不存在），可写成临时校验脚本或在 detail_queries 迁移用例里加一条等价断言。

## 8) 爆炸半径

**生产消费者（core/ web/ data/，排除测试）= 零。** 本轮 `rg 'get_version_time_span|list_overlapping_with_details|list_dispatch_rows_with_resource_context' core/ web/ data/` 命中仅 3 行——全是 schedule_repo.py 自身的 def 行（:36/:114/:128）。

⚠️ **对抗排雷（已排除假阳性）**：rg 同时命中 gantt_plan_query.py:50 / gantt_service.py:15/179/187 的 `get_version_time_span_dates`——这是**不同符号**（`_dates` 后缀，且签名/语义不同，返回日期对而非 span dict），**与 R34 的 `get_version_time_span` 无关**。tests 侧 `get_version_time_span_dates` 的大量 SimpleNamespace stub 同理是另一符号。删 R34 三方法**不碰** `*_dates` 这一活生产链。

**活孪生生产链（删后承接者，全部已在生产使用）**：
- 活仓 `schedule_plan_query_repo.py`：:257 get_plan_time_span / :311 list_detail_rows_between / :430 list_dispatch_rows（带 source_table + candidate_id + scenario_id + 未知 source raise）。
- 活服务链：schedule_plan_query_service.py:187/196… → gantt_plan_query.py → gantt_service.py:187 → resource_dispatch_service.py / resource_dispatch_execution_service.py。

**测试消费者（删前需逐一退场/迁移）**：
- `list_overlapping_with_details`：tests/test_schedule_repository_detail_queries.py:168 + tests/regression_gantt_critical_chain_unavailable.py:59（无效 monkeypatch，删之）。
- `list_dispatch_rows_with_resource_context`：tests/test_schedule_repository_detail_queries.py:198/218/225/240/247/254/268/274/288（9 处）。
- `get_version_time_span`（裸位置参）：tests/benchmark_fjsp.py:503。
- 类型契约：tests/regression_schedule_service_facade_delegation.py:31/36/39–41（3 断言）。

**真消费者（不是测试桩，是真要跑的脚本）= benchmark_fjsp.py:503**。本轮 sed 复核 :502–509：`span = span_repo.get_version_time_span(version); out["schedule_span"]=span; if makespan_h is None and span: st=strptime(span["start_time"]...); et=strptime(span["end_time"]...)`。它**真读 dict 字段**，删而不 repoint → 基准脚本 `AttributeError: 'ScheduleRepository' object has no attribute 'get_version_time_span'`。

**灾难链（删错/漏改会静默/响亮炸什么）**：
1. 漏 repoint benchmark:503 → 基准脚本运行期 AttributeError（**响亮**，非静默，跑基准时立刻暴露）。
2. 漏退 facade_delegation 3 断言 → import 期 `get_type_hints(ScheduleRepository.get_version_time_span)` AttributeError（**响亮**，测试收集即挂）。
3. 漏删 facade 无用 import（ScheduleTimeSpanRow/ScheduleDispatchRow）→ 不报错但留死 import（lint 噪声，低危）。
4. 漏删 gantt_critical_chain_unavailable.py:59 → monkeypatch 一个不存在的属性，`monkeypatch.setattr` 默认 raising=True → **响亮** AttributeError（测试挂）。
5. 漏退 detail_queries.py 10 处用例 → 用例调不存在方法，AttributeError（**响亮**）。
6. **无静默炸点**：三方法生产零引用，删除不会让任何生产路径静默走错；所有遗漏都是测试/脚本期的响亮 AttributeError。fix_invalidation_risk=none（删的是更不安全的 version-only 盲路径，护栏本就在活孪生）。

## 9) 承重毗邻标记

- **R34 本身 load_bearing=false，lb_no_touch=null。**
- **同文件毗邻承重**：schedule_repo.py **非承重文件**（lb_colocation_danger=none）。本轮回盘文件内符号：get_version_time_span / list_between(R35) / list_version_rows_by_op_ids_start_range / list_overlapping_with_details / list_dispatch_rows_with_resource_context / list_by_version_with_details，无 LB 标记符号。
- **禁区行**：无。本债不触承重，无「我是故意的」注释约束，无 parity/契约绑定禁令。可直删。
- 注：活方法 `list_version_rows_by_op_ids_start_range`（freeze_window 用）与 `list_by_version_with_details`（gantt_critical_chain 用）虽非承重但是活路径，**删 R34 时严禁误删这两个**（它们的 facade 断言 :32–34 / :37 必须保留，见 §4 步2）。这是「相邻活方法保护」而非承重禁区。

## 10) 分层 / 导入环风险

- **schedule_repo.py 删方法本身**：纯删除，不新增任何 import，不可能制造越层或导入环。本轮回盘其 import（:1–9）= `core.models.Schedule` + 同层 sibling repos（base_repo / schedule_detail_query / schedule_rows），删 3 方法后这些 import 仍被 list_between/list_version_rows.../list_by_version_with_details 使用，**不动 schedule_repo.py 的 import**。
- **facade_delegation.py import 修剪**（§4 步3）：仅删未用的 ScheduleTimeSpanRow/ScheduleDispatchRow，是减不是增，零越层风险。
- **benchmark_fjsp.py:503 repoint 的分层风险（重点）**：
  - registry fix_hint 建议 repoint 到「服务层 `SchedulePlanQueryService.get_plan_time_span_for_resolution`」。本轮回盘服务层实际签名是 `SchedulePlanQueryService.get_plan_time_span(version, role)`（schedule_plan_query_service.py:187），**非** fix_hint 写的 `get_plan_time_span_for_resolution(version, ROLE_ADOPTED)`——fix_hint 符号名有偏差，owner 落地时以 :187 实际签名为准。
  - benchmark_fjsp.py 已 import `core.services.scheduler.schedule_service`（:446）与 `data.repositories.ScheduleRepository`（:447），它是 tests/ 下的脚本，**引入 service 或 repo 都不构成生产代码越层**（测试/脚本不在分层 AST 红线管辖的 core/web/data 生产图内）。
  - **更省的等价 repoint = 留在 repo 层**：改用活仓 `schedule_plan_query_repo.get_plan_time_span(version=v, source_table=SOURCE_SCHEDULE, candidate_id=None)`，benchmark 已用 repo（:447 同族），无需新引 service 层，行为 §7 已证等价。owner 二选一时，repo 层 repoint 改动面更小、parity 更直证。
- **0 AST 违规**：本债不引入任何 core.algorithms→core.services / core.models→core.services 等越层 import。

## 11) 测试耦合

**动手前必须先处理的测试（按文件）**：

1. **tests/regression_schedule_service_facade_delegation.py**（类型契约门）：删 :31/:36/:39–41 三断言 + 删 :12/:14 两 import。保留 :32–34 / :37 活断言。这是删 def 的**前置同提交**——否则测试收集期 AttributeError。
2. **tests/test_schedule_repository_detail_queries.py**（10 处死方法调用：:168 + :198/218/225/240/247/254/268/274/288）：
   - **owner 待裁的分叉**：(a) 整删用例函数（若该文件存在的意义就是测这三个死方法）；(b) 迁到活孪生 `schedule_plan_query_repo.list_detail_rows_between`(:311)/`list_dispatch_rows`(:430)，保留「重叠查询 / 资源域过滤」语义覆盖。建议看该文件其它用例是否还测活方法决定整删 vs 迁移（本档不改测试，仅标注）。
   - 若选 (b) 迁移，需注意 §6：迁移落点 `list_dispatch_rows` 是 R05（team-join 扩展）的战场，应排在 R05 之后迁，避免被 R05 二次推翻。
3. **tests/regression_gantt_critical_chain_unavailable.py:59**：删无效 monkeypatch 行。删后该测试仍由 :60（list_by_version_with_details → _repo_raise）驱动「关键链不可用」反证，覆盖不丢。
4. **tests/benchmark_fjsp.py:503**：repoint（§7/§10），并加一次性 parity 断言（删前比对）。

**迁移顺序**：先改/退测试与基准（1→4），同提交删 schedule_repo.py 三方法 + R35 list_between，最后跑全测试自证。

## 12) 前置依赖

- **【同文件同改·硬】R35（list_between :61–69）**：必须与 R34 同提交（§5），避免删后行号二次漂移撞 facade delegation 断言。两者无先后，作为一个原子 diff。
- **【软约束·R05 在前】**：Phase1 DAG 把 R05（Batch-8）排在 R34（Batch-13）之前。仅当 §11 选择「把 detail_queries 用例迁到活孪生 list_dispatch_rows」时为硬前置（迁移落点是 R05 战场）；若选整删用例则解耦。
- **【F 门 / 验证门】**：删后须跑（a）全测试套件（含 facade_delegation 收集通过、detail_queries 通过/已删、gantt_critical_chain_unavailable 通过）；（b）benchmark_fjsp 至少能 import + 跑一次 span 计算不报 AttributeError；（c）分层 AST 检查 0 违规（本债不引越层，应天然通过）。
- **批次定位**：planned_batch_hint = Batch-13（B12 repo 死方法），与 R35 同 schedule_repo.py 子批同提交；本桶内最高复杂度（唯一 P3+needs_adversarial）；软排在 B07/R05 之后（零成本）。
- **owner_pending=true**：**终态批次分配与 detail_queries 用例「整删 vs 迁移」二选一，待 owner 裁断。** 本档案已透爆炸半径/依赖，但不锁终态。

### owner 待裁点（汇总）
1. detail_queries.py 10 用例：整删 vs 迁活孪生（影响是否硬依赖 R05）。
2. benchmark:503 repoint 目标：repo 层活仓 `get_plan_time_span`（改动小、parity 直证）vs 服务层 `SchedulePlanQueryService.get_plan_time_span`（fix_hint 写的 `_for_resolution` 符号名在当前代码不存在，需校正）。
3. 是否在删前补一次性 parity 校验脚本（§7）作为留档证据。

---

## 返回摘要

R34 = schedule_repo.py 三死方法（:36–59 / :114–126 / :128–158）+ R35(list_between :61–69) 同文件强同批；纯直删 + 退四件套测试 + repoint benchmark:503；生产零引用，删错全是响亮 AttributeError 无静默炸点；owner 待裁 detail_queries 整删 vs 迁移 + benchmark repoint 层级。

---

## 对抗核验（第二双眼睛）· 2026-06-05

独立 rg/grep 全量回盘，默认怀疑、找反例。结论：**核验 PASS（带 2 处需更正的事实瑕疵）**。

**A. 行号复核 = 一致。** 命令 `rg -n 'def get_version_time_span|def list_overlapping_with_details|def list_dispatch_rows_with_resource_context|def list_between' data/repositories/schedule_repo.py` → 36 / 61 / 114 / 128，与档案逐行一致，零漂移。

**B. 生产零引用 = 证实。** `rg -n 'get_version_time_span|list_overlapping_with_details|list_dispatch_rows_with_resource_context' core/ web/ data/` 仅命中 schedule_repo.py 自身 3 个 def 行；其余命中均为 `get_version_time_span_dates`（gantt_plan_query.py:50 / gantt_service.py:15/179/187）——**确系不同符号**（`_dates` 后缀，返回日期对非 span dict），档案排雷正确。`list_between` 全仓（含 tests/）仅命中 :61 定义行本身，R35 纯死方法证实。

**C. 测试消费者清单 = 一致。** detail_queries.py `rg -c` = **10**（:168 + 9×dispatch），与档案 §4/§11 的 10 处吻合（注：registry current_evidence 自身写"六个用例"，是 registry 笔误，以盘上 10 为准）。facade :31/:36/:37/:40、benchmark:503、critical_chain:59 全部命中且 :60 已 setattr list_by_version_with_details=_repo_raise 反证，:59 确为 patch 生产不调的死方法=纯噪声，档案分析正确。

**D. 收口点真实存在 + SQL 等价 = 证实。** `schedule_plan_query_repo.py` get_plan_time_span(:257) / list_detail_rows_between(:311) / list_dispatch_rows(:430) 均在。逐行比对死方法(:36–59)与活孪生 SOURCE_SCHEDULE 分支：SELECT MIN/MAX + 同 TRIM 守卫 + `if not row→None` + `if not start/end→None` + 同 return dict 形状，**语义逐字符等价**（死方法 SQL 多行、活方法单行，仅排版差异）；活孪生为严格超集（candidate/scenario 分支 + `_require_*` + 未知 source raise ValueError）。§7 等价论证成立。

**E. 承重 = false 证实，承重误删风险 = 无。** schedule_repo.py 无 LB 标记符号；修法为纯直删，无删除/统一/透传/加形参以外的越界，无兜底/静默回退/except 吞错，灵魂线不触。相邻活方法 list_version_rows_by_op_ids_start_range / list_by_version_with_details 的 facade 断言 :33/:37 档案明确保留，相邻活方法保护到位。删方法不动 schedule_repo.py import（:1–9），分层红线天然 0 违规。

**F. owner_pending 合规。** 档案全程标"建议修法/待裁不锁终态"，未擅自分配执行批次终态，符合铁律 7。

### ⚠️ 需更正的事实瑕疵（不影响 PASS，但 owner 落地前须知）

1. **【档案错】`get_plan_time_span_for_resolution` 实际存在，并非"当前代码不存在"。** 档案 §7(line 90 周边)、§8(line 109)、§10(line 140)、owner 待裁点(line 168) 均断言 fix_hint 写的 `SchedulePlanQueryService.get_plan_time_span_for_resolution` 在当前代码不存在、需校正——经 `rg -n 'get_plan_time_span' core/services/scheduler/schedule_plan_query_service.py` 回盘，该符号**确实存在于 schedule_plan_query_service.py:210**（keyword-only：version/source_table/candidate_id/scenario_id），且 :187 的 `get_plan_time_span(version, role)` 内部就转调它。故 registry fix_hint 的符号名是**正确的**，档案的"勘误"本身是错的。owner 若选服务层 repoint，:210 `get_plan_time_span_for_resolution` 与 :187 `get_plan_time_span(version, role)` 均可用，无需"校正不存在的符号"。

2. **【registry 自身矛盾，非档案错】** registry 顶层 `adv_refuted: false` 与 `verdict.refuted: true` 及档案正文 `refuted=true` 打架。verdict 块与档案一致（real_debt 已驳）；顶层 adv_refuted 字段疑为陈旧未同步，建议 owner 以 verdict.refuted=true 为准。

**核验结论：行号/零引用/承重/分层/收口存在性/SQL 等价 全部 PASS。无承重误删风险。瑕疵 1 反而是利好（收口符号比档案以为的更现成）。无需升级第三方裁断（owner_pending 本就在 owner 手里，待裁点性质不变）。**
