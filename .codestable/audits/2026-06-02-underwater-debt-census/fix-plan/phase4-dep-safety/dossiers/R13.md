# R13 逐债档案 - ExecutionFact last_event_schedule_* 死字段退场

> 主文件: `core/services/scheduler/execution_fact_provider.py`
> 桶 B11 / 病理 P6 / 严重度 medium / 承重 false / owner_pending false / status=fixed
> 簇 C01 / 同文件兄弟 R15(B06 datetime 收口)、R19(B13 positive_op_ids 收口)
> 2026-06-10 执行补登：owner 已确认单机无仓库外读者，按 O06 既定路线执行“先迁测试后删”。R13 已 fixed；R15/R19/R13 provider 串行链收口完成。R18 已随后补 repo stub 护栏注释 fixed，R13 未碰 repo stub 护栏。

## 1) 本轮删了什么

R13 只处理 provider 侧的旧输出字段和被它拖出来的 latest 读取尾巴：

- 删除 `ExecutionFact.last_event_schedule_version` / `ExecutionFact.last_event_schedule_id` 两个字段。
- 删除 `_fact_from_state` 的 `latest` 形参。
- 删除 `_fact_from_state` 里两处 `last_event_schedule_*` 赋值。
- 删除 `facts_by_scope` 中为这两个字段服务的 `latest_events = self._latest_events_by_scope(normalized)` 查询。
- 删除 `_fact_from_state(..., latest_events.get(scope), ...)` 实参。
- 删除已成孤儿的 `_latest_events_by_scope` helper。
- 清理随之变孤儿的 `OperationExecutionEvent` 与 `operation_execution_scope_from_event` import。

## 2) 本轮没有碰什么

- 未碰 `operation_execution_event_repo.py`。
- 未碰 repo `list_latest_events_by_op_ids` / `list_latest_exception_events_by_op_ids` / `aggregate_states_by_op_ids` / `state_revision_for_op` 那组完整计划身份 loud raise 护栏；这些仍归 R18。
- 未碰 provider `list_by_op_ids` / `facts_by_op_id` 的完整计划身份 loud raise。
- 未碰 R15 的空时间短路与坏时间 loud raise。
- 未碰 R19 的 `positive_op_ids` sorted 指纹契约。

## 3) 当前代码事实

按符号重查后的当前事实：

- `rg "last_event_schedule" core web data tests`：0 命中。
- `execution_fact_provider.py` 仍保留 `ExecutionFact` 的真实消费字段：`actual_*`、`state_revision`、`latest_exception_*`、`schedule_version`、`schedule_id`、`source_table`、`effective_plan_role`、`scenario_id`。
- `facts_by_scope` 现在只走 `aggregate_states_by_scopes(normalized)`，不再为了旧字段额外读取 latest events。
- `build_execution_snapshot` 本来就不读 `last_event_schedule_*`，本轮删除不会改变 execution snapshot 指纹输入。

## 4) 测试迁移

已退掉旧字段测试引用：

- `tests/operation_execution/test_operation_execution_scope_read_contract.py`
  - 删除 `scoped_fact.last_event_schedule_version is None` 断言。
  - 改用 `actual_start_time is None` 与 `state_revision == "10:0:0"` 继续证明旧 v1 事件不污染当前 v2 事实。
  - 删除 snapshot 构造模板和 `ExecutionFact(...)` 构造里的两个旧 kwarg。
- `tests/operation_execution/test_scheduler_reschedule_execution_facts.py`
  - 删除 `last_event_schedule_version == 1` 断言。
  - 改为断言 scoped fact 的 `actual_status == "processing"`，保留“旧执行事实存在但不作为新版本 seed”的行为验证。
- `tests/operation_execution/test_operation_execution_event_foundation.py`
  - R13 未改；repo unscoped raises 继续作为 R18 护栏测试保留。

## 5) 验证

本轮已跑：

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest tests/operation_execution/test_operation_execution_scope_read_contract.py tests/operation_execution/test_scheduler_reschedule_execution_facts.py tests/operation_execution/test_operation_execution_event_foundation.py::test_operation_execution_repository_rejects_unscoped_op_reads -q -p no:cacheprovider
```

结果：`35 passed`。

后续提交前仍需按 R13 SOP 再跑 ruff、py38 扫描、JSON / diff / 精确 rg 自检，并做 Sub Agent 双轨对抗复审。

## 6) 后续状态

- G09 provider 链：R15 fixed -> R19 fixed -> R13 fixed，已收口。
- G10 repo 链：R19 repo 私有版 fixed；R18 stub 护栏已补注释 fixed。
- R13 不再阻塞后续批次；后续若处理 R18，仍必须按 R18 dossier 只看 repo stub 护栏，不得倒推恢复 R13 字段。

**2026-06-10 深度复审补记**:
1. **显式裁定——放弃 phase2 B11.md 的两条防复活回归**(查询计数 spy 锁事件查询次数==1、`not hasattr(ExecutionFact, "last_event_schedule_*")` 形状断言)。理由:单机环境、dossier/registry 锚点已记录退场事实、phase4 门禁口径只验"退场原子完成且删后无 TypeError"。此前 phase4 计划隐式放弃但未记录,现补显式裁定消除两份计划口径分歧;后续若要恢复,挂在 scope_read_contract 测试文件。
2. **性能口径纠正**:本次删除的不是"一次无用的 latest-events 小查询",而是与 aggregate_states_by_scopes **完全同参数的全历史事件抓取+`_validate_events_by_scope` 序列校验的整段重复**(旧 facts_by_scope 对同组 scopes 连跑两遍 list_events_by_scopes)——6 个生产消费方每次读 facts 的事件 IO 与校验 CPU 减半,事件越多收益越大。性能考古归因以本条为准。
3. **测试替身卫生**:`_FakeExecutionFactRepo.list_events_by_scopes` 死桩已删(2026-06-10),替身接口面现与 provider 真实消费面一致(仅 `aggregate_states_by_scopes`);序列校验守护未丢——仍由 aggregation mixin 内的存量 list_events_by_scopes 调用触发。
