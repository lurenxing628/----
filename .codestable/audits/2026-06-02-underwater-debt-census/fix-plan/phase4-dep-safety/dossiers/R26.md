# R26 逐债档案 — 顶层 config/summary 5 个 shim（兼容冻结面）

> 干扰簇 C01 | 桶 B13 | 严重度 low | 承重 false | owner_pending=true（待裁，暂不给终态修法/暂不分配执行批次）
> 本档案只读不改，所有断言附当前工作区 file:line 证据（2026-06-05 回盘）。

## 速览
R26 = `core/services/scheduler/` 顶层 5 个纯转出 shim：`config_service.py` / `config_snapshot.py` / `config_validator.py` / `schedule_summary.py` / `schedule_summary_types.py`。生产已全量迁到深路径（`.config.*` / `.summary.*`）或经 `scheduler/__init__.py` `__getattr__` 路由；仅 53 个测试文件 + 2 个离线脚本仍经顶层老路径进。被 `test_sp05_path_topology_contract.py` 钉为冻结兼容面（有意决定）。verdict=depends（普查"0 生产消费者"证据**错**：有 2 个离线非测试消费者）。

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录
5 个顶层 shim 全部实测为纯 `from .config.X / .summary.X import ... + __all__` 转出，**零逻辑**：

| shim | 真实 file:line（转出行）| 转出目标（深路径，已确认存在）|
|------|------|------|
| config_service.py | `core/services/scheduler/config_service.py:3`（`from .config.config_service import ConfigService`）；全文件 5 行 | `core/services/scheduler/config/config_service.py`（17888B）|
| config_snapshot.py | `:3`（`from .config.config_snapshot import ScheduleConfigSnapshot, build_schedule_config_snapshot`）| `core/services/scheduler/config/config_snapshot.py`（17655B）|
| config_validator.py | `:3`（`from .config.config_validator import normalize_preset_snapshot`）| `core/services/scheduler/config/config_validator.py`（10572B）|
| schedule_summary.py | `:3-15`（转出 11 个符号：SUMMARY_SIZE_LIMIT_BYTES, apply_summary_size_guard, best_score_schema, build_overdue_items, build_result_summary, cfg_value, comparison_metric, config_snapshot_dict, due_exclusive, finish_time_by_batch, serialize_end_date）| `core/services/scheduler/summary/schedule_summary.py`（7501B）|
| schedule_summary_types.py | `:3-13`（转出 9 个符号：DEFAULT_TRUNCATION_TIERS, AlgorithmSummaryState, FallbackState, FreezeState, RuntimeState, ScheduleResultStatus, SummaryBuildContext, TruncationTier, WarningState）| `core/services/scheduler/summary/schedule_summary_types.py`（4634B）|

**漂移记录**：
- registry `old_location_DONT_TRUST` 写 `config_service.py:1-6`，实测全文件仅 **5 行**（`wc -l` = 5），转出在 `:3`。漂移 **-1 行**（微）。
- `current_evidence_2026_06_05` 写 `config_service.py:3`，**与回盘一致**（✅准）。
- `location_status` 写 `config_service.py:1-5`，**与回盘一致**（✅准）。
- 结论：current_evidence/location_status 准；仅 old_location 的 `:1-6` 旧值有 -1 漂移，已弃用不影响。

## 字段 2：当前状态 + 与 status evidence 一致性
- registry `status_2026_06_05` = **planned**。
- 回盘与 status evidence **一致**：5 个 shim 全仍在、全仍是纯转出（未删、未改逻辑、三步迁移零落地）；2 个离线消费者仍在老路径（见字段 8）；SP05 仍把它们列为冻结兼容面。无"已开工/半截"迹象。
- 状态判定：**planned 准确**，未 in_progress。

## 字段 3：病理 / 严重度 / 桶 / 簇
- **病理 P3**（迁移兼容尾巴）：`config → config/` 包拆分、`summary → summary/` 包拆分后留下的顶层兼容 shim。属于全项目主病"半截迁移"的尾巴——拆包动作做了，但顶层老入口未拆除，靠 shim 续命。
- **严重度 low**：纯转出无逻辑，误删只会**响亮 ImportError**（不破任何安全/算法不变量），honors 灵魂暗线（无静默兜底）。冻结面是有意决定，不构成正确性风险，仅是代码地图整洁度债。
- **桶 B13**（home=B13；分区 scheduler-config-summary-graph）。`planned_batch_hint` = Batch-14（全局最晚），本桶内排第 4 位（前置最重）。
- **簇 C01**。
- **verdict = depends / real_debt_confirmed（refuted=true）**：普查曾断言"0 生产消费者"，对抗核验**推翻**——存在 2 个离线非测试消费者（tools/ + audit/），它们走顶层老路径，先删 shim 会让它们 ImportError。所以不是裸 rm，须三步协调迁移。

## 字段 4：修法类（owner_pending → 待裁，暂不给终态修法）
**修法类 = 删 + 退测试（三步协调迁移，非裸 rm）。承重=false，可删；但 SP05 冻结是有意决定，须配套退契约。**

**owner_pending=true → 待 owner 裁断，暂不给终态修法、暂不分配执行批次。** 待裁点：
1. SP05 冻结面是否到期解冻？（冻结是有意决定，删 shim = 推翻该决定，须 owner 确认 roadmap 已允许）
2. Batch-14 排期是否仍成立（依赖 B05/B06/B09 三桶先收敛）。

待裁通过后的**候选三步**（仅记录依赖结构，不作为终态指令）：
1. **迁 2 个离线非测试消费者到深路径**（否则它们 ImportError）：
   - `tools/capture_networkx_phase0_baseline.py:17` → `from core.services.scheduler.config.config_service import ConfigService`
   - `audit/2026-03/20260316_schedule_audit_probes.py:87` → `from core.services.scheduler.summary.schedule_summary import build_overdue_items`
2. **重指 53 个测试文件、~93 处顶层 import 到深路径**（`.config.config_service` / `.config.config_snapshot` / `.config.config_validator` / `.summary.schedule_summary` / `.summary.schedule_summary_types`）。
3. **改 SP05 契约**：从 `SERVICE_BEHAVIOR_COMPAT_SYMBOLS`（`tests/test_sp05_path_topology_contract.py:20-31`）删 5 条 + 从 `SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS`（`:33-82`）删对应 5 条；改文档树断言（`:640-658` 的 `migrated_root_names` 含 `config_service.py`，"兼容薄门面"标注逻辑须同步）。
4. 最后删 5 个顶层 shim 文件。

**灵魂线核查**：本修法纯删 + 改 import 路径，不新增任何兜底/静默回退/except 吞错/过度防御。删错只响亮 ImportError，符合 loud-fail。无 P4 改造需求（病理是 P3 不是 P4）。

> ⚠️ registry 字段冲突（已核实）：`phase1_blast.contract_topology_tests` 与 `test_consumers` 称 5 个 shim 在 `SERVICE_STRONG_COMPAT_MODULES`（SP05:21-31）。**当前代码实测它们在 `SERVICE_BEHAVIOR_COMPAT_SYMBOLS`（:20-31）+ `SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS`（:33-82）**；`SERVICE_STRONG_COMPAT_MODULES`（:15-18）当前只含 `schedule_optimizer` / `schedule_optimizer_steps`。删 SP05 时须改的是 BEHAVIOR_* 两个字典，不是 STRONG_*。此为 registry 描述漂移，已在本档案纠正。

## 字段 5：同文件碰撞债（sibling 协调）
registry `same_file_siblings` = **[R71]**。但回盘发现这是 **basename 假碰撞**，需逐个澄清：

### R71（registry 列为同文件 sibling — 实为半真半假）
- R71 主文件 = `core/services/scheduler/config/schedule_config_runtime_coercion.py`，`all_files` 含 `config_field_coercion.py` / `config_snapshot.py` / `schedule_config_runtime_coercion.py` / `schedule_config_runtime_read.py`（model↔service 双栈逐字复制函数群，LB-B2 配料表）。
- **`config_snapshot.py` 双义澄清**（关键）：
  - R26 的 `config_snapshot.py` = **顶层 shim** `core/services/scheduler/config_snapshot.py`（5 行纯转出）。
  - R71 的 `config_snapshot.py` = **深路径** `core/services/scheduler/config/config_snapshot.py`（17655B，真实现）。
  - `schedule_config_runtime_read.py`（R71 的另一文件）实测内容是 runtime cfg 读取器（`read_runtime_cfg_raw_value` 等），**无 `config_snapshot` 符号**，与 R26 无符号重叠。
- **真耦合（非假碰撞）**：R26 顶层 `config_snapshot.py:3` 正是 `from .config.config_snapshot import ...`，即 **R26 的 shim 转出的就是 R71 编辑的深文件**。两者经"shim→深文件"的转出边相连。
- **协调与顺序**：
  - R71 在深文件 `config/config_snapshot.py` 做收口（service 反向复用 model，删 service 侧逐字副本），改的是**符号实现**；R26 删的是**指向该文件的顶层 shim**，改的是**入口路径**。两者**不撞同一行**（不同文件不同层）。
  - **谁先**：**R71 先、R26 后**。理由：R71 的 parity 收口若处于半截（service 副本删了、model 侧 parity 未补全），而此时大量测试仍经 R26 顶层 shim → `.config.config_snapshot` 进，会放大半截风险面；R26 先删 shim 把测试逼到深路径，反而打散 R71 的回归观测面。更稳妥是 R71 完成 parity 契约后，R26 再统一搬迁入口。**且 R26 本就排 Batch-14（全局最晚），R71 在 B03 桶，天然 R71 在前。**
  - **不会撞行号/dict 键位移**：R26 改的 SP05 是 BEHAVIOR_* 字典键；R71 改的是 `regression_scheduler_config_spec_sync_contract.py` parity 契约 + 深文件实现。两者不共享被改文件，无 dict 键位移冲突。

### R01 / R43（interference_edges，非 same_file_siblings — 仅经 SP05 同测试文件相邻）
- **R01**（`schedule_payload_contract.py`，死函数 `count_actionable_schedule_rows` / `has_actionable_schedule_rows`，sev medium）：与 R26 仅经 `test_sp05_path_topology_contract.py` 同文件相邻。R01 改 SP05:54-55，R26 改 SP05:20-82（BEHAVIOR_* 块）+ :640-658。**两者改 SP05 不同行段**，但**同一文件 → 串行改、避免 git 行号漂移误合**。建议同一窗口内顺序编辑 SP05，先 R01 后 R26 或反之均可（无逻辑依赖）。
- **R43**（顶层 9 个 `scheduler_*.py` route wrapper，sev low）：registry `interference_edges` 标 "file:scheduler_config.py"。**回盘澄清：R43 的 `scheduler_config.py` = `web/routes/scheduler_config.py`（route wrapper），与 R26 的 `core/services/scheduler/config/*` 完全不同文件**——纯 basename 碰撞，**无真耦合**。唯一真相邻点仍是 `test_sp05_path_topology_contract.py`：R43 改 SP05 的 `ROUTE_COMPAT_MODULES` / `ROUTE_BEHAVIOR_COMPAT_SYMBOLS` / `SCHEDULER_REAL_ROUTE_FILES`（route 段），R26 改 SERVICE_* 段。**不同段、串行编辑同文件即可**。

## 字段 6：同收口点债 + 顺序敏感性
**R26 本身不"收口"到任何统一符号**——它的修法是**删 shim + 重指 import 到已存在的深路径**（`.config.*` / `.summary.*`），深路径本身就是唯一真实现。所以 R26 不新建收口点，不触发"为已有概念新建第二模块=新 P5"红线（铁律 5）。

**与同收口点债的关系**：
- R26 把测试 + 离线脚本"收"到深路径，这些深路径（`config/config_service.py` 等）是**已存在**的唯一实现点，符合铁律 5"收到已存在的统一点"。
- **顺序敏感性（跨债）**：B05（R29 number_utils）/ B06（R33）/ B09（R52）这三桶若仍有测试经 R26 顶层 `config` / `summary` 老路径走，则它们的收敛动作会触碰 R26 的 shim。`fix_invalidation_risk` 明示：**R26 删 shim 须晚于这三桶收敛**——否则先删 shim 会让这些桶的测试经老路径变红。这是 R26 排 Batch-14（全局最晚）的硬约束来源。
- R26 与 R71 不收口到同一符号（R71 收口到 model 栈实现，R26 不收口只搬入口），见字段 5。

## 字段 7：收口行为差异 + 所需 parity 测试
**R26 是纯转出 shim，删除不改变任何运行时行为**——`from .config.config_service import ConfigService` 与 `from .config_service import ConfigService`（经 shim）解析到**同一对象**（SP05:339-340 已断言 `config_package.ConfigService is config_service_module.ConfigService`，is-identity）。因此：

- **无新旧两路行为差异**：shim 与深路径返回**同一身份对象**，无 `None vs raise`、无边界值分歧、无逐分支不等价问题。删 shim 后调用方拿到的是同一个类/函数。
- **所需 parity 测试 = 无新增**。无需边界 parity，因为不是"两份实现求等价"，而是"删一层零逻辑转发"。
- **唯一须验证的不变量**：删后所有消费者能解析到深路径符号（即不留悬空 import）。这由"重指 import + 跑全测试套件 + 跑 2 个离线脚本"覆盖，属于回归门而非 parity 门。
- 反例边界（确认不适用）：R71 那种"service 副本 vs model 实现可能 None/raise 分叉"是 R71 的 parity 关切，**不流入 R26**——R26 不碰实现，只搬路径。

## 字段 8：爆炸半径（生产/测试消费者 + 灾难链）
### 生产消费者（核心 core/ + web/）= 0（已回盘确认）
对 `core/ web/` 做顶层 shim import 扫描（排除深路径 / shim 文件自身 / `schedule_summary_assembly|degradation|freeze`），**结果为空**。生产经 `scheduler/__init__.py:21` 的 `"ConfigService": ".config.config_service"` + `:35` `__getattr__` 路由到深路径，**不经顶层 shim**。
> ✅ 此条修正普查"0 生产"——0 仅对**核心生产**成立；离线侧非 0（见下）。

### 离线非测试消费者 = 2（普查漏掉，对抗核验补出 → verdict=depends 的根据）
- `tools/capture_networkx_phase0_baseline.py:17` → `from core.services.scheduler.config_service import ConfigService`（:51、:161 实际用 `ConfigService(...)`，是活引用）。
- `audit/2026-03/20260316_schedule_audit_probes.py:87` → `from core.services.scheduler.schedule_summary import build_overdue_items`（:103 实际调用 `build_overdue_items(...)`，活引用）。
- **这 2 个不迁就删 shim → 立即 ImportError**（响亮、非静默）。

### 测试消费者
- **53 个测试文件**（精确去重计数，与 registry "约 53 个" 一致 ✅）经 5 个顶层 shim 进。
- **~93 处顶层 import 行**（精确计数；registry 写 "71 处 import" → **漂移：实测 ~93，比旧值多 ~22 处**，向上漂移，须以 93 为准重指）。分项：config_service 29 / config_snapshot 25 / config_validator 6 / schedule_summary 25 / schedule_summary_types 8。
- SP05 契约钉死点：`tests/test_sp05_path_topology_contract.py:20-31`（BEHAVIOR_COMPAT 模块映射）、`:33-82`（BEHAVIOR_COMPAT 公开符号清单）、`:321-326`（strong alias 循环断言，注：5 shim 不在 STRONG 集）、`:339-340`（is-identity 断言）、`:640-658`（文档树 migrated_root_names 含 config_service.py + "兼容薄门面"标注）。
- 例：`tests/regression_config_service_strict_blank_contract.py`、`regression_schedule_summary_cfg_snapshot_contract.py`、`regression_config_validator_preset_degradation.py` 等。

### 灾难链（删错/统一错会静默炸什么）
- **本债灾难性低**：纯转出，误删/漏迁只会**响亮 ImportError**，不破任何安全/算法不变量，不会静默错算。这是 low 严重度 + honors 灵魂线的根据。
- **次序错（先于 B05/B06/B09 删）→ 那些桶的测试经老路径红**（响亮，非静默）。
- **漏迁 2 离线脚本 → 该脚本 ImportError**（离线运行才暴露，CI 若不跑这俩脚本可能延迟发现 → 需把这俩纳入删前手动验证清单）。
- **不改 SP05 就删 shim → SP05 的 BEHAVIOR_COMPAT 断言红**（importlib.import_module 找不到模块）。
- **最危险的隐性点**：若以 registry 旧值 "71 处" 为重指清单基数，会**漏 ~22 处** import 未迁，留下悬空老路径——以本档案回盘的 **93 处 / 53 文件** 为准。

## 字段 9：承重毗邻标记（LB + 禁区行）
- **R26 自身 load_bearing=false，`lb_no_touch=null`**——5 个 shim 是纯转出，非承重，无禁区行。
- `phase1_blast.lb_colocation_danger` = **none(shim 非承重，纯转出)**，与回盘一致。
- **毗邻承重提示（不在 R26 文件内，但同收口链 R71 侧）**：R71 受 **LB07 承重约束**（model↔service config 双栈是 LB-B2 配料表），R71 的收敛方向须经 B03 整桶裁决。R26 删 shim **不触碰** R71 的承重实现（只删指向它的入口），但**时间上**应让 R71（含 LB07 裁决）先落地，避免在承重收敛半途搬动入口路径放大风险面（见字段 5 顺序）。
- **R26 侧禁区行 = 无**（无 LB，无"我是故意的"注释需求；删除即可，配套退 SP05 契约）。

## 字段 10：分层 / 导入环风险
- **零越层风险**：修法是把 import 从 `core.services.scheduler.config_service` 改到 `core.services.scheduler.config.config_service`——**同一层（core.services）内的子包下沉**，不跨层。不引入 `core.algorithms→core.services` 或 `core.models→core.services` 这类不存在的越层边（铁律 6：0 AST 违规）。
- **零导入环风险**：深路径 `.config.config_service` 等本就是被 shim 转出的目标，已是当前生产 import 的实际终点（经 `__getattr__`）。把消费者直连终点**减少**一跳，不可能新增环。
- **离线脚本迁移**（tools/、audit/）改成 import 深路径，这两个目录是仓库外围工具，依赖方向 tools→core / audit→core 是既有正向边，不成环。
- 结论：分层 / 导入环风险 = **无**。

## 字段 11：测试耦合（动手前先迁哪些测试）
动手前须先迁移/改动的测试（迁 import 路径，非迁文件）：

1. **SP05 契约本体**（`tests/test_sp05_path_topology_contract.py`）——这是冻结面定义者，**必须最先改**，否则无论怎么迁消费者它都会红：
   - 删 `SERVICE_BEHAVIOR_COMPAT_SYMBOLS`（:20-31）中 5 条键。
   - 删 `SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS`（:33-82）中对应 5 条键。
   - 改文档树断言（:640-658）：`migrated_root_names` 处理 `config_service.py` 的"兼容薄门面"标注逻辑（删 shim 后该文件不再存在，断言期望需同步）。
   - ⚠️ R01（改 :54-55）、R43（改 ROUTE_* 段）也改本文件——**串行编辑、各改各段，避免行号漂移误合**（见字段 5）。
2. **53 个测试文件 / ~93 处 import**：逐文件把顶层 import 重指深路径。代表：`regression_config_service_strict_blank_contract.py:9`、`regression_schedule_summary_cfg_snapshot_contract.py`、`regression_config_validator_preset_degradation.py` 等。
3. **不需要迁的测试**：已经走深路径（`.config.* / .summary.*`）或经 `__getattr__` 的测试不动。

> 测试耦合本质是**纯 import 路径替换**，非测试逻辑重写——风险面是"漏一处 = 悬空 import 红"，靠全套件跑 + grep 残留老路径兜底。

## 字段 12：前置依赖（先做哪些债 / 哪些 F 门）
**R26 是全局排期最晚的债之一（Batch-14），前置最重。**

### 硬前置（PHASE0 §10.2：facade 删除晚于收敛）
- **B05 收敛**（R29 number_utils；Phase1 边 R29→R26）——若 B05 测试仍经顶层 config 老路径，先删 R26 shim 会让 B05 测试红。
- **B06 收敛**（R33；Phase1 边 R33→R26）。
- **B09 收敛**（R52）。
- 三桶须**先收敛**，R26 才能删 shim。

### 软前置（同收口链 / 承重）
- **R71 先于 R26**（见字段 5、字段 6、字段 9）：R71 在 B03 桶做 model↔service config 双栈收口（受 LB07 承重 + B03 整桶裁决约束）。R71 编辑的 `config/config_snapshot.py` 正是 R26 shim 的转出目标，应让 R71 parity 收口落地后 R26 再搬入口。R26 排 Batch-14、R71 在更早桶，**天然满足此序**。

### F 门（功能门 / 验证门）
- 删前须能跑通**全测试套件**（含 53 个迁移后的测试文件）。
- 删前须手动跑/验证 **2 个离线脚本**（tools/capture_networkx_phase0_baseline.py、audit/2026-03/20260316_schedule_audit_probes.py）——CI 不一定覆盖它们，需纳入删前手动验证清单。
- SP05 契约改完后须独立绿。

### owner 裁断门（owner_pending=true）
- 上述执行前置之外，**还须 owner 先裁断**：SP05 冻结面解冻许可 + Batch-14 排期确认。**未裁断前，本债不分配执行批次、不给终态修法指令。**

---

## 附：本档案对 registry 的纠偏小结
1. **STRONG vs BEHAVIOR 误标**：registry phase1_blast 称 5 shim 在 `SERVICE_STRONG_COMPAT_MODULES`，实测在 `SERVICE_BEHAVIOR_COMPAT_SYMBOLS`（:20-31）+ PUBLIC（:33-82）；STRONG 集（:15-18）当前只含 2 个 optimizer。改 SP05 时认准 BEHAVIOR_* 两字典。
2. **import 处数漂移**：registry "71 处" → 回盘 **~93 处**（53 文件不变）。重指清单基数用 93。
3. **行号微漂**：config_service.py old_location `:1-6` → 实测 `:1-5`（-1 行）；current_evidence 的 `:3` 准。
4. **R43 假碰撞**：interference "file:scheduler_config.py" 指 `web/routes/scheduler_config.py`，与 R26 的 `core/services/scheduler/config/*` 无关，唯一真相邻仍是 SP05 测试文件。
5. **R71 config_snapshot.py 双义**：R26 = 顶层 shim，R71 = 深 `config/config_snapshot.py`，经 shim 转出边真耦合（非纯假碰撞），R71 先行。

---

## 对抗核验（第二双眼睛）
核验人：独立对抗 agent（只读不改）。结论：**PASS**（找不到反证）。复核命令与结果如下，全部独立重跑、不以本档案/registry 自身为基准。

### 1) 行号 + 转出内容回盘（一致）
`cat -n core/services/scheduler/{config_service,config_snapshot,config_validator,schedule_summary,schedule_summary_types}.py`：
- config_service.py / config_snapshot.py / config_validator.py 全 **5 行**，转出在 `:3`，纯 `from .config.X import ... + __all__`，零逻辑——与档案/`current_evidence`/`location_status` 一致。
- schedule_summary.py 实测 **29 行**（`:3-15` import 块转 11 符号 + `:17-28` __all__），schedule_summary_types.py 实测 **25 行**（`:3-13` 转 9 符号）——与档案"转出行 :3-15 / :3-13"一致（档案的"全文件 5 行"仅指前 3 个 config shim，未误称后两个为 5 行）。
- registry `old_location` `:1-6` 旧值确有 -1 微漂（实测 :1-5），current_evidence `:3` 准。**行号判定：一致。**

### 2) 深目标存在性（一致）
`ls -la core/services/scheduler/config/{config_service,config_snapshot,config_validator}.py core/services/scheduler/summary/{schedule_summary,schedule_summary_types}.py` → 5 个深目标全在（17888/17655/10572/7501/4634 B），转出非悬空。

### 3) 消费者清单（一致，含 verdict=depends 反例复现）
- 核心生产：`rg "from \.\.scheduler\.(5shim) import|from core\.services\.scheduler\.(5shim) import" core/ web/ | grep -v /config/ | grep -v /summary/` → **空**。核心生产 0 成立。
- 离线非测试消费者：`rg -n "...config_service import|...schedule_summary import" tools/ audit/` → **2 处活引用复现**：`tools/capture_networkx_phase0_baseline.py:17`（:51/:161 实例化 `ConfigService(...)`）、`audit/2026-03/20260316_schedule_audit_probes.py:87`（:103 调用 `build_overdue_items(...)`）。**普查"0 生产消费者"被推翻 → verdict=depends 成立**。
- 测试：`rg -l ...` → **53 文件**（一致）。import 处数宽口径 `rg "scheduler\.{shim}\b" tests/ | grep -v 深路径` → 28/25/6/24/8 = **91 处**（我口径），档案 93（差 2 处正则口径微差）——**两者均 ≫ registry 旧值 71，"以 71 为基数会漏 ~20 处悬空"的核心警示成立**。

### 4) SP05 契约位置纠偏（一致，已独立复现）
- `sed -n '15,31p'`：`SERVICE_STRONG_COMPAT_MODULES`（:15-18）当前**仅含 schedule_optimizer / schedule_optimizer_steps 2 项**；5 shim 实测在 `SERVICE_BEHAVIOR_COMPAT_SYMBOLS`（:20-31）+ `SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS`（:33起）。**registry phase1_blast 误标 STRONG 被推翻，档案纠正正确——删 SP05 须改 BEHAVIOR_* 两字典。**
- is-identity 断言复现于 `:339`（import 深模块）+ `:340`（`assert config_package.ConfigService is config_service_module.ConfigService`）——零行为差异、无 parity 门，与字段 7 一致。
- 文档树断言复现：`migrated_root_names` 在 `:641`，含 `"config_service.py"` 在 `:648`，`"兼容薄门面"` 标注逻辑在 `:655`——档案 `:640-658` 准确。

### 5) R43 假碰撞 + R71 真耦合（一致）
- R43：`ls web/routes/scheduler_config.py` 存在；`ls core/services/scheduler/config/scheduler_config.py` **不存在**——确为纯 basename 碰撞，与 R26 无文件重叠，唯一真相邻是 SP05 测试文件。
- R71：R26 顶层 `config_snapshot.py:3` = `from .config.config_snapshot import ...`，转出目标正是 R71 编辑的深文件 `config/config_snapshot.py`——经转出边真耦合，R71 先行序成立。

### 6) 承重 / owner_pending / 灵魂线合规
- **承重误删风险：无**。load_bearing=false、lb_no_touch=null，5 shim 纯转出无不变量；档案修法（删+退测试）未混入"看着像该统一即无害删"——它明确把 SP05 冻结认作有意决定、要求配套退契约，非裸 rm。误删只响亮 ImportError（loud-fail），无静默炸点。
- **owner_pending=true 合规**：档案字段 4/12 明确"待裁，暂不给终态修法、暂不分配执行批次"；候选三步标注为"仅记录依赖结构，不作为终态指令"；未擅自分配执行批次。**未越权。**
- **灵魂线**：修法纯删 + 改 import 路径，无新增兜底/静默回退/except 吞错/过度防御；病理 P3 非 P4，无 loud-raise 改造需求。合规。
- **分层/导入环**：import 从 `core.services.scheduler.X` 改到 `core.services.scheduler.config/summary.X`，同层（core.services）子包下沉，不跨层、不成环。0 AST 违规风险。

### 核验结论
**PASS。** 全部关键断言独立复现：行号一致、深目标在、核心生产 0 + 2 离线消费者反例复现、SP05 STRONG↔BEHAVIOR 纠偏成立、R43 假碰撞 / R71 真耦合成立、import 处数 ≫ 71。无承重误删风险，owner_pending 未越权。唯一可记的微差是 import 计数 91（我）vs 93（档案）的正则口径差，不影响"以 71 为基数会漏迁"的结论方向，无需第三方裁断。
