# R24 逐债档案 · schedule_diagnostic_contract.py 零生产消费孪生副本

> **2026-06-09 台账回写终态：fixed / O23 KEEP。** 本档从旧“路 A 删 core 死副本”改为 O23 口径：`core/services/scheduler/analysis/schedule_diagnostic_contract.py` 刻意保留为诊断合同事实记录/兼容面，并已在代码顶部补 KEEP 注释。活页面的非有限数字 loud-raise 护栏仍在 `web/viewmodels/scheduler_analysis_diagnostic_helpers.py`，绝不反删 web 孪生，也不把活路径裸改指这份 core 合同。下方路 A/路 B 删除分析只保留作历史风险背景。

> 本档案已于 2026-06-09 按 O23 KEEP 回写。owner_pending=false；旧“删 core 死副本 / 路 A 路 B”只作为历史风险背景，不再作为执行方案。

## 字段 1 — 当前真实 file:line(回盘)+ 漂移记录

主文件全路径:`core/services/scheduler/analysis/schedule_diagnostic_contract.py`(整文件 82 行)。
按符号名 `rg` 回盘当前态(2026-06-05):

| 符号 | old_location_DONT_TRUST | 当前真实行 | 漂移 |
|---|---|---|---|
| `_list_or_empty`(私有) | 未记 | `:6` | — |
| `build_diagnostic_link` | 记 13 | `:12` | -1(报告初记微偏,非漂移) |
| `build_diagnostic_item` | 记 24 | `:25` | +1(微偏) |
| `build_diagnostic_section` | 记 45 | `:46` | +1(微偏) |
| `empty_diagnostic_sections` | 未记入 def 清单 | `:73` | — |
| `__all__` | — | `:77-82`(四件套) | — |

回盘结论:**漂移=无(仅 ±1 报告初记微偏,范围 :13-79 完整覆盖真实 :6-82)**,文件不在漂移名单。registry 字段 `location_status` 自述「行号微偏但范围覆盖准」核实属实。
git 态:`git diff --stat` 对该文件**空**(无在途改动);mtime=2026-05-24 18:00(自 5/24 未动)。证据:`rg -n "^def |^__all__" core/services/scheduler/analysis/schedule_diagnostic_contract.py`。

## 字段 2 — 当前状态 + 与 status evidence 一致性

registry 已回写为 `status_2026_06_05 = fixed`，O23 口径为 **KEEP**:
1. 文件整份仍在、四 def 原样(`:12/:25/:46/:73`)是刻意保留，不再解释为“删尚未执行”。
2. 生产零消费回盘仍成立；该文件现在定位为诊断合同事实记录/兼容面，而不是活页面收口点。
3. 代码顶部已补 O23 KEEP 注释，明确活页面的非有限数字 loud-raise 护栏仍在 `web/viewmodels/scheduler_analysis_diagnostic_helpers.py`，绝不反删 web 孪生，也不把活路径裸改指 core 合同。
**一致性=fixed / KEEP 完全一致,无矛盾。**

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- 病理 **P3**(零生产消费的孪生副本):core 这份是 web LIVE 诊断构造器的**纯 dict 拼装**孪生,被 web 本地副本架空。`needs_adversarial=true` 且 `verdict.refuted=true` → 经对抗后**确认 real_debt**(死副本,非误判)。
- 严重度 **medium**;桶/home **B11**;partition `scheduler-config-summary-graph`;簇 **ISOLATED**(无 same_file_siblings、无 interference_edges)。
- 承重 `load_bearing=false`;`lb_no_touch=null`。注意:**承重的灵魂线护栏在 web 孪生 helpers,不在本 core 文件**(见字段 9)。

## 字段 4 — 修法类

**修法类 = KEEP + 保护性注释。** O23 已裁定保留 `core/services/scheduler/analysis/schedule_diagnostic_contract.py` 作为诊断合同事实记录/兼容面，并在代码顶部补注释。`owner_pending=false`，本档不再分配“删 core”执行批次。

旧路 A / 路 B 裁定结果:
- **旧路 A(删 core 死副本)**:已否决/作废。不得再按本档旧计划去删 core 文件、摘 roadmap、剪 core-only 测试。
- **旧路 B(把 core 立为正式收口点=整体重构)**:仍不在本债范围；活路径的 loud-raise 护栏在 web 孪生，裸改指 core 合同会丢护栏，需另立 P5 债评估。

注意红线:本债**不涉及任何 raise/兜底改动**。core 文件无 except、无静默回退、无过度防御(只有 `str(x or "")` 字面强制),删除不触灵魂线;web 孪生的 loud-raise 护栏(`NonFiniteDiagnosticNumber`)**绝不在删除范围**。

## 字段 5 — 同文件碰撞债

`same_file_siblings = []`,`interference_edges = []`,簇 = **ISOLATED**。
回盘确认:`schedule_diagnostic_contract.py` 整文件四 def 仅服务诊断合同,registry 无任何同文件兄弟债登记,本轮 80 债内无第二条债落在此文件。
**结论:无同文件碰撞,不存在「谁先谁后 / 撞行号 / dict 键位移」协调问题。** 删整文件时无邻债被波及。

## 字段 6 — 同收口点债

本债的「收口符号」(`build_diagnostic_link/item/section`、`empty_diagnostic_sections`)在 core 侧**零生产消费**,不是任何活路径的收口点;web 侧活收口点是 `build_diagnostic_sections`(`web/viewmodels/scheduler_analysis_diagnostics.py:108`)→ 经 `build_item:41`/`build_section:62`(`scheduler_analysis_diagnostic_helpers.py`),**与 core 合同符号同名异源、互不 import**。
本轮 80 债内**无其他债收口到 core 这四个符号**(grep 全仓零外部命中)。
**结论:无同收口点债,无顺序敏感性。**

## 字段 7 — 收口行为差异及所需 parity 测试

本债现行修法类是 **KEEP + 注释**,不是删、不是收口/合并，因此不需要新旧两路 parity 测试。但为防后续误把活 web 路径改指 core 合同，此处仍保留两路**本就不等价**的反例，作为「不可裸合并」的护栏证据:

| 场景 | core 合同(死副本) | web 孪生(活路径) | 是否等价 |
|---|---|---|---|
| 数值含 `NaN/Inf` | `build_diagnostic_item(value=NaN)` → **原样塞进 dict**(`value: value`,`:38`),前端可能渲染坏值 | `safe_int/safe_float` → **loud raise `NonFiniteDiagnosticNumber`**(helpers:`:139/:152`) | **不等价**(吞 vs raise) |
| `value` 传 `bool` | core 不区分,`True` 原样入 dict | web `safe_int` 显式 `raise NonFiniteDiagnosticNumber(...布尔值...)`(helpers:`:131`) | **不等价** |
| 空集合 | core `_list_or_empty(None)→[]`(`:6-9`) | web 同义 | 等价(此分支可合) |

**反例结论:core 合同对非有限数字是「静默透传」,web 孪生是「loud raise」——二者对边界值非逐分支等价。** 所以 O23 KEEP 不会把活路径改指 core，也不会反删 web 孪生；若未来另走收口，必须先把护栏下沉并补 NaN/Inf/bool 三反例的 parity 断言。

## 字段 8 — 爆炸半径

**生产消费者:零。** 回盘 `rg "schedule_diagnostic_contract|build_diagnostic_link|build_diagnostic_item|build_diagnostic_section|empty_diagnostic_sections" core web data --type py` → 除文件自身外**全仓零命中**;`rg "import ...schedule_diagnostic_contract" core web data` → **零**(唯一外部 import 是 tests)。活渲染链全程不碰 core:
`analysis.html` ← `scheduler_analysis_vm.py:122` ← `scheduler_analysis_diagnostics.py:108 build_diagnostic_sections` ← `scheduler_analysis_diagnostic_helpers.py:41 build_item / :62 build_section`(web 孪生,独立实现)。

**测试消费者(唯一):** `tests/scheduler_analysis/test_scheduler_analysis_diagnostic_contract.py`
- `:9-13` import core 四件套(`build_diagnostic_item/link/section/empty_diagnostic_sections`);
- `:15` 另 import web `build_diagnostic_sections`、`:16` import `build_analysis_context`(同文件大量测 web,见字段 11)。
- core-only 用例(回盘精确划定):`:19 test_diagnostic_section_returns_complete_payload`、`:63 test_..._uses_empty_lists...`、`:74 test_..._does_not_guess_success...` 三个**纯 core**;另 `:82 test_missing_graph_diagnostic_sections_return_visible_empty_state` 是**混合**用例——仅首行 `assert empty_diagnostic_sections() == []`(`:83`)用 core,其余 `:84+` 全测 web。

**灾难链:**
- **删错方向(误删 web 孪生)= 灾难**:web 孪生 `scheduler_analysis_diagnostic_helpers.py` 独占灵魂线护栏 `NonFiniteDiagnosticNumber/safe_int/safe_float`(`:101/:127/:144`,raise 点 `:131/:139/:141/:148/:152/:154`),误删 → 活诊断页面 `analysis.html` 直接炸 + 丢非有限数字护栏 → **静默渲染坏值或 500**。**绝不能反向删 web。**
- **旧路 A 历史风险(不再执行)**:删除 core 合同曾经只影响 `regression_scheduler_analysis_diagnostic_contract.py` 的 core-only 段，**零生产页面 / 零安全不变量影响**(web 孪生承载全部活渲染)。O23 KEEP 后该删除路径作废。
- **旧路 A 的 fix_invalidation_risk(不再执行)**:PR-9(`networkx` roadmap,status=planned 未开工)把该文件列为 primary_path + ruff/pyright exit_check。历史删除方案若裸删 core 会让 exit_check 引用不存在文件；O23 KEEP 后保留 core 文件，不再需要 roadmap 删除前置。

## 字段 9 — 承重毗邻标记 + 禁区行

- 本 core 文件 `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none`。文件内**无承重逻辑**:四 def 纯 dict 拼装,无 except/无回退/无不变量守卫,仅 `str(x or "")`、`bool(...)`、`_list_or_empty` 字面强制(`:6-74`)。删除不触任何承重点。
- **禁区行(绝不可在本债动)= web 孪生灵魂线护栏**,虽不在主文件但是「毗邻同概念承重」:
  - `web/viewmodels/scheduler_analysis_diagnostic_helpers.py:101 class NonFiniteDiagnosticNumber`
  - `:127 def safe_int` / `:144 def safe_float`(loud-raise 主体)
  - raise 行 `:131 / :139 / :141 / :148 / :152 / :154`(布尔/非有限/不可读 → 显式抛错,**P4 灵魂线**)
  - `:41 build_item` / `:62 build_section`(活构造器)
  这些是活渲染唯一护栏；O23 KEEP 后本债**不删 core、不碰 web 这些行**，后续若另立收口也必须先保护这些 loud-raise 护栏。

## 字段 10 — 分层 / 导入环风险

- core 文件 import 仅 `__future__`、`typing`(`:1/:3`),**不 import 任何 web/core.services/core.algorithms 模块**。O23 KEEP 保留它不会引入越层 import 或导入环——它是叶子(零出边)且零生产入边。
- O23 KEEP 对分层红线 = **零风险**(0 AST 违规,无新增跨层 import)。
- 若未来另立整体重构让 web 改指 core 合同，才会触分层考量:web→core 是合法方向(下行依赖),但护栏下沉需保证 core 不反向 import web、不形成环——**此风险属未来 P5,不属本债 fixed 范围**。

## 字段 11 — 测试耦合(动手前先迁哪些测试)

唯一耦合文件 `tests/scheduler_analysis/test_scheduler_analysis_diagnostic_contract.py`。O23 KEEP 后不删 core、不剪测试。以下旧清单仅为历史“如果曾经走路 A”时的风险记录，后续不得按它直接施工:
1. 历史旧路 A 曾要求删 `:9-13` 的 core 四件套 import block(`build_diagnostic_item/link/section/empty_diagnostic_sections`)。
2. 历史旧路 A 曾要求删三个纯 core 用例整函数:`:19 test_diagnostic_section_returns_complete_payload`、`:63 test_diagnostic_contract_uses_empty_lists_for_missing_collections`、`:74 test_diagnostic_contract_does_not_guess_success_when_status_missing`。
3. 历史旧路 A 曾要求**混合用例 `:82` 仅删首行** `assert empty_diagnostic_sections() == []`(`:83`),**保留函数其余 web 断言**(`:84+` 测 `build_diagnostic_sections` 空态),否则会误杀活路径覆盖。
4. **不动** `:15/:16` 的 web import、不动 `:84` 起所有 `build_diagnostic_sections`/`build_analysis_context` 测试(那是 web 孪生的活覆盖,与 core 解耦)。
- registry phase1_blast 原述「移 :10-13 core import + 那 3 个 core-only 用例」**基本准但漏标 `:82` 混合用例的单行剪除**——本档据回盘补正:第 4 个 core 符号 `empty_diagnostic_sections` 还出现在混合用例 `:83`,必须单行剪除而非整函数删。
- web 键集硬编码断言(registry 记 `:229-240` 区间)与 core 解耦,删 core **不影响**。

## 字段 12 — 前置依赖(先做哪些债 / 哪些 F 门)

- **F 门 / 强前置:** O23 KEEP 后不再需要“先调和 roadmap 再删 core”的前置。
- **同文件 / 同收口前置:无**(簇 ISOLATED,字段 5/6)。
- **批次提示:** registry 已改为 fixed / O23 KEEP；不得再把 R24 排入“删 core”批次。
- **owner 裁断点:** 已裁定为 KEEP，owner_pending=false。

## 对抗核验(第二双眼睛)

回盘日期 2026-06-05。独立 rg/grep 重跑,默认怀疑、主动找反例,结论 **PASS**。

**重 grep 命令与结果:**
1. `rg -n "^def |^__all__" core/services/scheduler/analysis/schedule_diagnostic_contract.py` → `_list_or_empty:6 / build_diagnostic_link:12 / build_diagnostic_item:25 / build_diagnostic_section:46 / empty_diagnostic_sections:73 / __all__:77`。文件 82 行,`git diff --stat` 空。**行号与档案字段1完全一致,漂移=无(仅报告初记 ±1 微偏,范围覆盖准)。**
2. `rg -n "schedule_diagnostic_contract|build_diagnostic_link|build_diagnostic_item|build_diagnostic_section|empty_diagnostic_sections" core web data --type py`(排除文件自身)→ **core/web/data 全仓零命中**。`build_diagnostic_sections`(web 侧同名异源符号)命中 `scheduler_analysis_vm.py:7/:140` + `scheduler_analysis_diagnostics.py:108/:157`,全部 import 自 web,不碰 core。**生产零消费证实。**
3. `rg ... tests` → 唯一 import core 四件套的测试是 `regression_scheduler_analysis_diagnostic_contract.py:9-13`;另 `:15` import web `build_diagnostic_sections`。core-only 用例边界回盘:`test_..._returns_complete_payload:19`、`..._uses_empty_lists...:63`、`..._does_not_guess_success...:74` 三个纯 core;`test_missing_graph...:82` 为混合用例——`:83 assert empty_diagnostic_sections() == []` 用 core,`:84+` 测 web `build_diagnostic_sections`。**档案字段11「:82 单行剪除非整删」补正属实,registry phase1_blast 确实漏标 :82。** 其余测试文件(candidate_plain_language / graph_score_contract / silent_fallback / error_contract)全部 import web `build_diagnostic_sections`,与 core 解耦,删 core 不影响。
4. `rg -n "class NonFiniteDiagnosticNumber|def safe_int|def safe_float|def build_item|def build_section|raise NonFiniteDiagnosticNumber" web/.../scheduler_analysis_diagnostic_helpers.py` → `build_item:41 / build_section:62 / NonFiniteDiagnosticNumber:101 / safe_int:127 / safe_float:144`,raise 行 `:106/:131/:139/:141/:148/:152/:154`。**web 孪生独占灵魂线护栏证实,字段9 禁区行清单准确(另含 :106 一处 `_require_finite` raise)。**
5. `rg -n "schedule_diagnostic_contract" networkx-...-items.yaml` → `:435 primary_path`、`:480 ruff`、`:481 pyright` 三处确把 core 文件与 web helpers 列同一命令。O23 KEEP 后这不再是删除前置；保留 core 文件和对应门禁引用即可。

**承重判断复核:** `load_bearing=false`、`lb_no_touch=null` 属实。core 文件四 def 纯 dict 拼装、无 except/无回退/无不变量守卫；O23 KEEP 后不再执行删除。真护栏在 web 孪生,档案已正确标为禁区行、明确「绝不碰 web」。**无承重误删风险。**

**owner_pending 复核:** 2026-06-09 已裁定为 O23 KEEP，owner_pending=false；旧“待裁路 A/路 B”口径过期。

**收口点/P4 复核:** 档案正确识别路B(把活 web 路径改指零消费的 core 合同)= 新建收口点、违反「收口点须收到已存在统一点」铁律,且会丢非有限数字护栏退化为静默吞错(踩 P4),已建议路B需另立 P5 评估。判断准确。

**主动找反例结果:** 未找到反证。核心论断(死副本/生产零消费/web 孪生承载活渲染/护栏不在删除范围)全部成立,无擅自删动作。

**核验结论:PASS。**
