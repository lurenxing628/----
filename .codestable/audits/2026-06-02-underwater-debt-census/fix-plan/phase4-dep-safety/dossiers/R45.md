# R45 逐债档案 — 死壳 config_adapter（B03 / C01）

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：本 dossier 引用的 `tests/regression_sp06_no_duplicate_defs.py` 已由 A 的 **P1.1（commit `7ca42ca4`）整删**（早于 P6），`NO_CFG_GET_TARGETS` 符号全树零命中。凡『同提交退该文件断言行』的步骤为 **no-op**（无文件可红）；若作白名单守卫引用，须确认该锚点已废或重指。详见 `ANCHOR-DRIFT-2026-06-05-POSTCOMMIT.md §六.3` + `_B_COMPAT_SAFEGUARDS.md §5`。

## 0) 2026-06-08 B 执行终态补登

- **状态：fixed。** `core/algorithms/greedy/config_adapter.py` 已由 `f0443fd9` 删除；R45 与 R48 是同一物理文件的两个视角，删除动作已经一次性完成。
- **旧测试退行：no-op。** `tests/regression_sp06_no_duplicate_defs.py` 已在 A P1.1 整删，`NO_CFG_GET_TARGETS` 当前全树零命中；原计划里的旧清单同步步骤现在没有文件可改，也不应新建测试文件来补这个历史步骤。
- **当前残留核查：通过。** `CriticalConfigReadResult` / `read_schedule_config_value` / `read_critical_schedule_config` / `config_adapter` 在 `core/ web/ data/ tests/ tools/` 当前零命中。
- **禁区继续有效。** `core/algorithms/greedy/schedule_params.py` 是活文件，也是 LB07/R33/R51 一侧的真实邻居；本债已完成后仍不得把“清理 config_adapter”扩散到它。
- **阅读口径：** 下方 2026-06-05 的“当前状态”“动手前”等段落只保留为历史回盘证据，不再表示 R45 仍待执行。

> 只读不改产物。回盘日 2026-06-05。owner_pending=false。

## 1) 历史 file:line（2026-06-05 回盘）+ 执行终态

2026-06-08 终态：本节所列 `config_adapter.py` 行号已经是历史证据，当前文件已删除。删除前整文件 `core/algorithms/greedy/config_adapter.py` 实测 **27 行**（`wc -l` 确认）。三符号回盘：

- `CriticalConfigReadResult`（frozen dataclass）→ `config_adapter.py:10`
- `read_schedule_config_value(config, key)` → `config_adapter.py:16`（:17 调 `ensure_schedule_config_snapshot`，:18 `hasattr` 缺失判定，:20-23 try/getattr，吞 `Exception` 成 `error` 字段）
- `read_critical_schedule_config(config, key)` → `config_adapter.py:26`（仅 :27 透传 `read_schedule_config_value`）
- import 行：`config_adapter.py:6` `from core.models.schedule_config_runtime import ensure_schedule_config_snapshot`

**漂移记录：**
- registry `old_location_DONT_TRUST` = `:10-27`，与实际 **完全一致，无漂移**（三符号 :10/:16/:26 原样）。
- `phase1_blast.location_status` 标注「全文件27行(报告R48称'28行'，实测27，⚠️微差)」——回盘确认 **27 行属实**，R48 叙述的 28 行是 +1 误差（多半把文件末空行算入）。删整文件时此微差无影响。
- **registry 自身的一处行号失准（已抓）**：`planned_deps_hint` 与 `lb_colocation_danger` 称 `ensure_schedule_config_snapshot` 「经 schedule_params.py:98 仍活」。实测 `core/models/schedule_params.py` **不存在**；该文件真实路径为 `core/algorithms/greedy/schedule_params.py`（与 config_adapter 同目录）。registry 把它误写到 `core/models/` 下。此漂移 **不影响 R45 删壳结论**（snapshot 本体活着这一事实成立，只是续命路径在 greedy/ 而非 models/），但下游 LB07/R33/R51 共享 `schedule_params.py` 的分析须按真实路径 `core/algorithms/greedy/schedule_params.py` 核。

## 2) 执行后状态 + 与 status evidence 一致性

- registry 已补登 `status_2026_06_05 = fixed`。字段名沿用 2026-06-05，是 registry 历史结构，不代表仍按 06-05 状态执行。
- 2026-06-05 的 evidence 当时为真：文件存在、生产零引用、旧 sp06 路径成员仍在。2026-06-08 B 执行终态已经变为：文件已删、旧 sp06 文件已不存在、`NO_CFG_GET_TARGETS` 零命中。
- **一致性结论**：R45 不是假阳，而是已被实际清理；本次补登只同步计划台账，避免后续重复投工。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6（死代码 / 死壳）**，`load_bearing=false`。
- 严重度 **low**。桶 **B03**，簇 **C01**，分区 `core-models`（实际物理位置在 `core/algorithms/greedy/`，分区归类是因其 import 自 model 栈的 snapshot 收口点）。
- 性质：`config_adapter` 是包在本分区配置收口点 `ensure_schedule_config_snapshot` 外的一层薄封装（facade）。三个公开符号 **生产零调用、测试零 import**，唯一「引用」是 sp06 查重守卫把文件 **路径** 列进 `NO_CFG_GET_TARGETS` 清单（:15）。被封装的 `schedule_config_runtime` 收口点本身仍活（`core/algorithms/greedy/schedule_params.py` 在用），死的只是这层 adapter 壳。证据：`config_adapter.py:6,16-17,26`；`rg` 全仓三符号零生产引用。

## 4) 修法类

**已完成：直删 + 退测试 no-op**（非收口、非注释、非 raise、非 parity）。

- 已删除整文件 `core/algorithms/greedy/config_adapter.py`（历史 27 行）。
- `tests/regression_sp06_no_duplicate_defs.py:15` 的退行步骤已因 A P1.1 整删该测试文件变成 **no-op**；当前没有文件可退，也不应恢复旧测试文件。
- 与 **R48 合并为同一次整文件删除**的要求已满足：R45（死壳视角）与 R48（迁移残渣视角）是同一物理文件的两种叙述，后续不要拆成第二次动作。

**灵魂线检查（通过）**：config_adapter.py:22-23 存在 `except Exception as exc: return CriticalConfigReadResult(None, error=exc)` 的吞异常成 error 字段——属灵魂线关注的静默兜底，但本债修法是 **整文件删除**，删除即消除该兜底，**无需**也 **不应** 改写为 loud raise（无消费方，改它是无意义投工且违反"只动声明范围"）。删除后该反模式从代码库彻底消失，是净收益。

**非承重确认**：`load_bearing=false`，`lb_no_touch=null`，无需"补『我是故意的』注释 + 绑契约/parity 测试"那条承重修法。

owner_pending=false，给终态修法。

## 5) 同文件碰撞债

`config_adapter.py` 上的同文件债：

- **R48（sibling，same_file + same_symbol `read_critical_schedule_config`）**：R48 = 同一文件的"迁移残渣"叙述，R45 = "死壳"叙述。**不是两次动作，是同一次整文件删除的两个视角**。历史要求是合并为单提交一次性删除；2026-06-08 终态已满足该要求，后续不要再重复投工。撞行号 / dict 键位移：不存在——整文件删除已完成，不做局部行编辑，无残留行号或键位移问题。

`schedule_params.py`（`core/algorithms/greedy/schedule_params.py`，经 `interference_edges` 关联但 **非 config_adapter 同文件**）上的债 **LB07 / R33 / R51**：
- registry `interference_edges` 把它们标为与 R45 `same_file`，**这是 registry 的误判**——它们共享的是 `schedule_params.py`，而 R45 主文件是 `config_adapter.py`。两文件 **不同**。R45 删 config_adapter 壳 **完全不触碰** `schedule_params.py`，故对 LB07/R33/R51 **零碰撞**。`fix_invalidation_risk=none` 成立（adapter 只是 snapshot 收口点的死消费方，删它不动双栈本体）。
- 唯一真实跨文件耦合曾是 R45↔R48（config_adapter）+ R45↔sp06 路径退测试；当前 R45/R48 删除已完成，旧 sp06 文件已不存在，退测试步骤为 no-op。

## 6) 同收口点债

config_adapter 的 import 目标 = 收口点 `ensure_schedule_config_snapshot`（home：`core/models/schedule_config_runtime.py:26` re-export，真身 `core/models/schedule_config_runtime_coercion.py:409`）。

- 该收口点的 **活跃** 生产消费方（`rg` 全仓，与 R45 共享同一收口点但非同债）：`schedule_summary_assembly.py`、`schedule_summary.py`、`schedule_summary_freeze.py`、`schedule_summary_downtime_degradation.py`、`schedule_summary_degradation.py`、`config_page_save_service.py`、`config_snapshot.py`、`schedule_candidate_runner.py`、`optimizer_config.py`、`core/algorithms/greedy/schedule_params.py`，以及收口点自身两文件。
- **顺序敏感性：无**。R45 是把 config_adapter 这个 **死消费方** 摘掉，收口点 `ensure_schedule_config_snapshot` 本体与上述 10+ 活跃消费方 **完全不动**。删壳后收口点仍由活跃路径支撑，调用拓扑不变。
- 双栈收敛债（LB07/R71/R47，config 双栈收敛）与本债 **正交**：adapter 只 import FROM model 的 snapshot 符号，删它不动双栈本体、不引入跨层 import。`fix_invalidation_risk=none`。

## 7) 收口行为差异及 parity 测试

**不适用——本债是直删，不是收口。** 无新旧两路边界值等价问题。

补充说明（防误归类为收口）：config_adapter 的三符号本身就是 snapshot 收口点的薄封装，删它不引入任何"新收口路径替代旧路径"。下游既无消费方，也就无任何调用点需要从"旧 adapter 路径"切到"新统一路径"。因此 **无需 parity 测试**。

历史唯一须保的是旧 sp06 清单不会指向已删除文件；当前旧 sp06 文件已由 A P1.1 删除，本项已降为 no-op，不再作为可执行回归目标（见 §11）。

## 8) 爆炸半径

**生产消费者清单**：当前 `生产零引用`。`CriticalConfigReadResult|read_schedule_config_value|read_critical_schedule_config|config_adapter` 在 `core/ web/ data/` 零命中；删除前“仅自身命中”的历史状态已经随整文件删除消失。

**测试消费者**：当前 `测试零引用`。旧 `tests/regression_sp06_no_duplicate_defs.py:15` 是历史路径成员引用，测试文件已由 A P1.1 删除；`NO_CFG_GET_TARGETS` 当前全树零命中。

**灾难链分析：**
- **下游运行期影响：无**（纯死代码，删除无行为变化）。删除消除"本分区收口点有此外部用法"的假象。
- **删错会静默炸什么：当前无静默链**。历史唯一风险是删文件但旧 sp06 清单仍指向它；现在旧 sp06 文件已不存在，该风险已经自然消失为 no-op。若未来有人恢复同名测试清单，必须重新按当前路径重建证据，不能照抄旧行号。
- **统一错 / 删错本体的风险：none**——R45 删的是死消费方壳，`ensure_schedule_config_snapshot` 收口点本体与 10+ 活跃消费方不在删除范围，不存在"删壳误伤本体"路径。
- 与 LB07 承重：adapter import 的 `ensure_schedule_config_snapshot` 属 LB07 model 栈，但该符号由 `core/algorithms/greedy/schedule_params.py` 等活跃路径续命，删 adapter 壳 **不影响 LB07 承重**。`lb_colocation_danger=none`。

## 9) 承重毗邻标记 + 禁区行

- 本债 `load_bearing=false`，`lb_no_touch=null`。主文件 `config_adapter.py` **非承重文件**，`lb_colocation_danger=none`。
- 同文件（config_adapter.py）**无任何 LB 债毗邻**——整文件死壳，无承重逻辑，无禁区行。可整文件删除。
- 毗邻承重提示（**非本文件，仅边界提示**）：`interference_edges` 列了 LB07 与 R45 共享 `schedule_params.py`（registry 误标 same_file）。LB07 承重本体在 `core/algorithms/greedy/schedule_params.py` 一侧，**R45 删除 config_adapter.py 完全不进 schedule_params.py，故无禁区行交叠**。若未来误把"统一/合并"动作扩散到 schedule_params.py，那是 LB07 的禁区，与 R45 删壳无关；当前只保留“adapter 已删、旧 sp06 no-op、schedule_params 禁碰”的终态纪律。

## 10) 分层 / 导入环风险

- **零新增 import**——本债是删除，不收口、不加形参、不透传。删除只会 **减少** 一条 import 边（`config_adapter.py:6` → `core.models.schedule_config_runtime`），不可能引入越层 import 或导入环。
- 删除方向合规：被删文件在 `core/algorithms/greedy/`（algorithms 层），它原本 import FROM `core/models/`（models 层）—— `algorithms→models` 是合法方向。删掉这条边不破坏分层。
- 分层红线检查：0 AST 违规，不引入 `core.algorithms→core.services`、`core.models→core.services` 等禁止边。删除天然满足。

## 11) 测试耦合

- **当前无需迁测试。** 旧 `tests/regression_sp06_no_duplicate_defs.py` 已删除，`NO_CFG_GET_TARGETS` 零命中，所以旧清单同步步骤不再有可编辑对象。
- 无任何测试 import config_adapter 的三个符号（当前 `tests/` 零命中）。
- 当前验证重点改为残留扫描：确认 `config_adapter` 与三个符号在生产、测试和工具目录零命中；确认 `core/algorithms/greedy/schedule_params.py` 仍存在且未被本债触碰。
- 删后回归不再跑旧 sp06 文件；如需验证，只跑受影响的 schedule_params/greedy 相关裸单测，不为这个历史测试文件做恢复。

## 12) 前置依赖

- **前置债：已满足。** R45 零 LB 耦合、零活跃消费方，已按 Batch-A/G16 补登 fixed。
- **强绑定已完成**：R45 与 **R48** 是同一物理文件的两个叙述视角，删除动作已合并完成；后续不得重复投工。
- **同提交原子项已降为 no-op**：旧 `tests/regression_sp06_no_duplicate_defs.py:15` 路径已不存在，`NO_CFG_GET_TARGETS` 零命中。
- **F 门口径**：当前以残留 `rg` 自证“三符号/文件名零引用”即可；旧 sp06 用例不再存在，不作为可执行验证目标。
- 与 LB07/R71/R47 config 双栈收敛 **正交**，无须等其完成；与 R33/R51（schedule_params.py）**不同文件**，无前置关系。
- 历史批次建议曾写作 registry `planned_batch_hint` = Batch-5（B03，与 R48 合并）；2026-06-08 补登后以 Batch-A/G16 已 fixed 为准，不再待执行。

## 对抗核验（第二双眼睛）

回盘日 2026-06-05。以下独立 rg/grep 是历史删除前证据，用来解释为什么当时允许直删；2026-06-08 之后不得把它当成仍待执行清单。

**重 grep 命令与结果：**

1. `ls -la && wc -l core/algorithms/greedy/config_adapter.py` → 文件存在，**27 行**（档案 §1 称 27 行，registry location_status 称 27、R48 称 28——实测 27 属实，无漂移）。
2. `cat -n config_adapter.py` → `CriticalConfigReadResult` 在 **:10**、`read_schedule_config_value` 在 **:16**、`read_critical_schedule_config` 在 **:26**、吞异常 `except Exception as exc: return CriticalConfigReadResult(None, error=exc)` 在 **:22-23**、import `from core.models.schedule_config_runtime import ensure_schedule_config_snapshot` 在 **:6**——**三符号 + 关键行全部与档案 §1 逐一对齐，零漂移**。
3. `rg -n "CriticalConfigReadResult|read_schedule_config_value|read_critical_schedule_config" core web data tests` → **仅 config_adapter.py 自身命中**（:10/:16/:19/:21/:23/:26/:27），core/web/data/tests 无任何外部引用——死壳「三符号生产零引用、测试零 import」**成立**。
4. `rg -n "config_adapter" core web data tests` → 全仓唯一外部命中 = `tests/regression_sp06_no_duplicate_defs.py:15`（路径成员，非符号 import）——与档案 §8/§11 一致。
5. `find . -name schedule_params.py` → 真身在 **`core/algorithms/greedy/schedule_params.py`**，`core/models/schedule_params.py` 不存在——证实档案 §1 抓到的 registry 误标（`planned_deps_hint`/`lb_colocation_danger` 把 `schedule_params.py:98` 误置于 `core/models/`，实为 greedy/ 同目录）。此漂移不动 R45 删壳结论。
6. `cat -n tests/regression_sp06_no_duplicate_defs.py` → `config_adapter.py` 路径**只在** `NO_CFG_GET_TARGETS`（:15），**不在** `TARGETS`（:8-12，三成员=schedule_summary_assembly/evaluation/calendar_admin，与本债无关）。`test_sp06_targets_do_not_use_cfg_get_fallback_helpers`（:69）在 :72-73 对 NO_CFG_GET_TARGETS 逐个 `path.read_text()`——确认「删文件不退 :15 → FileNotFoundError → 测试红（响亮非静默）」属实，档案 §8/§11 唯一约束链精确。
7. `rg -n "ensure_schedule_config_snapshot" core web data`（排除 config_adapter）→ 收口点本体由 **10+ 活跃消费方**续命（schedule_summary_assembly/summary/freeze/downtime_degradation/degradation、config_page_save_service、config_snapshot、schedule_candidate_runner、optimizer_config、`schedule_params.py:98`，真身 `schedule_config_runtime_coercion.py:409`）——证实「删 adapter 壳不动本体、不影响 LB07 承重」（档案 §6/§8）。

**逐项挑战结论：**
- 行号：**完全一致**，零漂移。
- 调用者/消费者清单：**一致**（生产零引用、测试仅旧 sp06 路径成员）。
- 承重判断：`load_bearing=false`、`lb_no_touch=null`、`owner_pending=false`——**核实无误**。历史修法为直删整文件 + 同步处理旧 sp06 清单，**未混入**删除以外的统一/透传/加形参/兜底动作；吞异常 :22-23 随整文件删除消失（无消费方，档案明确「不应改写为 raise」，符合只动声明范围、不新增静默兜底）。**承重误删风险：无**。
- 分层/导入环：删除只**减少**一条 `algorithms→models` 合法边，零新增 import，无越层、无导入环——一致。
- 收口点真实存在 + 行为等价：本债是**直删非收口**，无新旧两路 parity 问题；收口点 `ensure_schedule_config_snapshot` 真实存在且活跃，删死消费方不改调用拓扑——一致。
- 同文件碰撞：R48 与 R45 为同一物理文件两叙述，历史合并为同一删除动作的要求已满足；LB07/R33/R51 共享的是 `schedule_params.py`（registry `same_file` 误判），与 config_adapter **不同文件，零碰撞**——核实成立。

**历史核验结论：PASS。** 未找到任何反证。R45 为纯死叶子债，行号零漂移，直删真零引用；2026-06-08 终态已 fixed，旧 sp06 清单耦合已因 A P1.1 删除测试文件变为 no-op。无承重误删风险，无需第三方裁断。
