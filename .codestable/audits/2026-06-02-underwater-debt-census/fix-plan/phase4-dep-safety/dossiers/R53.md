# R53 逐债档案 — batch_order.dispatch_batch_order 内 `_ = scheduled_count` 死空操作面包屑

- 桶/Bucket: B17 | 严重度: low | 病理: P6 | 簇: ISOLATED
- 承重: false | owner_pending: false | 状态(registry): fixed
- 主文件: core/algorithms/greedy/dispatch/batch_order.py
- 关联文件: run_state.py（ScheduleRunState，仅读，不动）

> 2026-06-08 B 执行终态：已 fixed。`core/algorithms/greedy/dispatch/batch_order.py` 中的 `_ = scheduled_count` 空操作行已删除；`:39` 形参、`:58` 真消费、`:74` return 均保留。`sgs.py:127` 同形态行不是本债，未动。
> 验证：`PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/algorithm/test_greedy_refactor_contract.py tests/algorithm/test_greedy_scheduler_base_date.py` = **28 passed**；ruff 通过；Python 3.8 语法扫描总发现数 0。

---

## 字段 1 — 当前真实 file:line（回盘）+ 漂移记录

按符号名 rg 回盘（不照抄 old_location）：
- 死空操作行：2026-06-08 已删除。执行前 `rg -n "_ = scheduled_count" core/algorithms/greedy/dispatch/batch_order.py` 命中 **core/algorithms/greedy/dispatch/batch_order.py:74** `    _ = scheduled_count`（唯一一处）；执行后该文件零命中。
- 形参声明：batch_order.py:39 `scheduled_count: int = 0,`（在 `dispatch_batch_order` 签名内，:17 起）。
- 真消费点：batch_order.py:58 `scheduled_count=scheduled_count,`（作为 kwarg 传入 :44 `_coerce_state(...)`，再经 :111 传入 :98 `ScheduleRunState.from_legacy(scheduled_count=scheduled_count)`）。
- 返回点：batch_order.py:74 `return run_state.scheduled_count, run_state.failed_count`（读的是 `run_state` 的 property；旧 :74 死空操作删除后，本行从 :75 上移到 :74）。

**执行前漂移：无。执行后终态已更新。** old_location_DONT_TRUST 标 :74，current_evidence 标 :74，执行前回盘实测 :74，三者一致；2026-06-08 直删后旧 no-op 不再存在，return 上移到现盘 :74。

旁证（非本债，勿动）：sgs.py:127 另有一行同形态 `_ = scheduled_count`，是独立的另一处面包屑，不在 R53 范围内。执行后 `rg -n "_ = scheduled_count" core/algorithms/greedy/dispatch/batch_order.py core/algorithms/greedy/dispatch/sgs.py` 仅剩 sgs.py:127。

## 字段 2 — 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05`: **fixed**（2026-06-08 B 执行补登）。
- current_evidence_2026_06_05 已更新为执行终态：旧 :74 `_ = scheduled_count` 死空操作行已删除，现盘 :39/:58/:74 三个正确性禁区均在。
- 本轮回盘：`batch_order.py` 中 `_ = scheduled_count` 零命中；`sgs.py:127` 同形态行仍在且不是本债 → **状态与 evidence 一致**。

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- 病理 **P6**（死代码 / 无意义残留）：ScheduleRunState 重构后，`scheduled_count` 的真实消费已下沉到 :58→`_coerce_state`→`from_legacy`。:74 的 `_ = scheduled_count` 是当年为压制 linter「未使用参数(unused-argument)」告警留下的空操作面包屑；但参数实际早在 :58 被用，该行已彻底无意义。
- 严重度 **low**：纯弃值行，无任何数据流参与（执行前 return 在 :75 读的是 `run_state.scheduled_count` property，不读局部变量；执行后 return 上移到 :74）。
- 桶 **B17**，簇 **ISOLATED**（同文件无兄弟债，无干扰边）。

## 字段 4 — 修法类

**修法类 = 直删，已完成**（已删旧 :74 整行 `    _ = scheduled_count`，连同其单独占行）。

- 非承重（load_bearing=false，lb_no_touch=null），不补「我是故意的」注释——本行本就是「假装在用」的反向面包屑，删它即去噪。
- **禁区行（绝不碰）**：
  - :39 形参声明 `scheduled_count: int = 0`（对外签名，benchmark/契约/测试 kwarg 依赖，见字段 8/11）；
  - :58 真消费 `scheduled_count=scheduled_count`（数据流入口）；
  - :74 return 行（旧 :75，上移后仍读 property，与本债无关）。
- **删后无 unused-argument 复发风险**：`scheduled_count` 仍在 :58 作为 kwarg 被真实读取，linter 视其为「已使用」，故无需补任何 `# noqa` / `del` / 替代抑制行。这是「直删而非改注释」成立的关键前提，已由回盘证实（:58 实测存在且确为传参）。
- 灵魂线核对：本修法不新增兜底/静默回退/except 吞错/过度防御，纯删 1 行噪声，无观测点变动需求。owner_pending=false，给出终态修法。

## 字段 5 — 同文件碰撞债

**无。** registry `same_file_siblings=[]`、`interference_edges=[]`、`cluster=ISOLATED`、Phase1 `co_change_within_group=[]`。batch_order.py 在本批 80 债中无其他登记债，删 :74 一行不会与任何兄弟债撞行号或撞 dict 键位移。无需协调先后。

（提示：batch_order.py 与 sgs.py:127 的同形态行虽相似，但 sgs.py:127 不是 R53、也不在 same_file_siblings——它在另一个文件，删 R53 不波及它；如未来另立债处理 sgs.py:127，两者文件不同，互不撞行号。）

## 字段 6 — 同收口点债

**不适用。** R53 修法类是「直删」，不收口到任何统一符号，不存在「收口到同一符号的其他债 + 顺序敏感性」问题。无收口点。

## 字段 7 — 收口行为差异及所需 parity 测试

**不适用收口。** 但需对「直删是否真行为等价」做反例排查，结论：删除前后两条调用路径逐分支等价，无 None-vs-raise 类边界差异。

逐路径核验（证据 file:line）：
- **生产路径**（scheduler.py:431 `_run_dispatch`）：传 `state=state`（非 None），**不传** `scheduled_count`（默认 0）。`_coerce_state` 命中 :96-97 `if state is not None: return state` 早返回，根本不读 `scheduled_count`。执行前 :75、执行后 :74 返回的都是 `run_state.scheduled_count`（来自活体 state）。→ 旧 :74 删前删后，返回值都由 `run_state` 决定，局部 `scheduled_count`（=0）从头到尾被丢弃。**等价。**
- **契约测试路径**（test_greedy_refactor_contracts.py:225）：**不传** `state`（None），传 `scheduled_count=5`。`_coerce_state` 走 :98 `from_legacy(scheduled_count=5)`，单 op 派工后自增到 6，执行前 :75、执行后 :74 均返回 6（test:250 `assert scheduled_count == 6`）。旧 :74 的 `_ = scheduled_count` 只是丢弃局部 `5`，与返回的 `6` 无因果。删它，返回仍是 6。**等价。**
- 反例搜索：是否存在「旧 :74 被删后某分支返回值改变」？否——return 表达式两个分量(`run_state.scheduled_count` / `.failed_count`)均不引用 :39 的局部 `scheduled_count`，旧 :74 是该局部变量在函数体内的最后且唯一一次「使用」，且使用方式是弃值，无副作用。无任何边界值(0 / 负 / 大整数 / None)能让删除产生差异。
- **所需 parity 测试：不新增。** 现有 test_greedy_refactor_contracts.py:250-251 的 `(scheduled_count, failed_count) == (6, 2)` 断言已天然守住「返回值不变」这一不变式——删 :74 后该断言必须仍绿，即为回归护栏。

## 字段 8 — 爆炸半径（Phase1 9 字段 + 灾难链）

- **生产消费者**：不适用（空操作弃值行本身无消费者）。`dispatch_batch_order` 的唯一生产调用者是 core/algorithms/greedy/scheduler.py:431（`_run_dispatch`，仅在 `dispatch_mode_key != "sgs"` 分支调用）；rg `dispatch_batch_order` 在 core/web/data 仅命中：scheduler.py:37(import)、:431(call)、batch_order.py:17(def)、dispatch/__init__.py:13/17(re-export)。无 web/ data/ 直接调用。
- **测试消费者**（引用函数，非引用 :74 这行）：
  - tests/algorithm/test_greedy_refactor_contract.py:179/212/225（return 值契约 + 副作用契约）；
  - tests/algorithm/test_greedy_scheduler_base_date.py:61-65（monkeypatch 替身 `_fake_dispatch_batch_order`，读 `kwargs.get("scheduled_count", 0)` ——依赖的是 :39 形参名，**不依赖 :74**）。
  - 这两处都不引用 :74 no-op；删行后断言不受影响。
- **灾难链（删错/删多会静默炸什么）**：
  1. 若手滑误删 :58 `scheduled_count=scheduled_count` → from_legacy 拿不到初始计数 → 契约测试 :250 `assert == 6` 立即翻红（用默认 0 起算只会得 1）→ **响亮失败，非静默**。
  2. 若误删 :39 形参 → scheduler.py:431 与所有传 kwarg 的测试 `TypeError: unexpected keyword`/缺参 → **响亮失败**。
  3. 若误删现盘 :74 return（旧 :75）→ 函数返回 None → 调用点解包/`int()` 报错 → **响亮失败**。
  4. 正确只删 :74 → 无任何下游效应（Phase1 `downstream_effect`: 1 行删除，无行为影响；`fix_invalidation_risk`: none）。
- 结论：唯一真正的风险是「删错行」，且任何删错都会被现有契约测试**响亮**拦截，不存在静默炸的路径。

## 字段 9 — 承重毗邻标记

**无承重毗邻。** load_bearing=false，lb_no_touch=null，Phase1 `lb_colocation_danger`: none——batch_order.py 不在承重清单。文件内无 LB 行需设禁区。

（注：字段 4 列的「禁区行」:39/:58/:74 return（旧 :75）是「正确性禁区」而非「承重禁区」——它们承载真实数据流/对外签名，删错会响亮报错，但不属 load_bearing 承重语义。）

## 字段 10 — 分层 / 导入环风险

**零风险。** 修法是纯删 1 行，不新增任何 import、不动模块依赖。batch_order.py 现有 import（:6 core.algorithms.ordering、:7 core.algorithms.types、:8 core.algorithms.value_domains、:9 core.infrastructure.errors、:11 `..run_context`、:12 `..run_state`）全部保留不变，删 :74 不触发越层 import，也不构成导入环。分层红线（core.algorithms→core.services 不存在）不受影响。AST 违规增量 = 0。

## 字段 11 — 测试耦合

- **动手前需迁移的测试：无。** 现有覆盖即护栏，无需先迁移。
- 删后必须保持绿的护栏：
  - tests/algorithm/test_greedy_refactor_contract.py:225-258（return 契约 `(6,2)` + 七项副作用断言）；
  - tests/algorithm/test_greedy_scheduler_base_date.py（monkeypatch 替身路径，验签名兼容）；
  - registry 列的 tests/algorithm/test_greedy_ordering_contract.py、tests/regression_batch_order_*（行为契约，均不依赖 :74 no-op）。
- 验收命令建议：`python3 -m pytest tests/algorithm/test_greedy_refactor_contract.py tests/algorithm/test_greedy_scheduler_base_date.py -q` 删行后全绿即收。

## 字段 12 — 前置依赖

**无前置债、无前置 F 门。** Phase1 `co_change_within_group=[]`、`fix_invalidation_risk=none`、独立单文件叶子。planned_batch_hint: Batch-16（独立叶子），无门控，Batch-1 之后任意时点可做；可与 R61 合并为「杂项叶子删除」小提交，或单独落。不依赖任何其他债先行，也不阻塞任何债。

---

## 返回串

R53 | 状态: 2026-06-08 已 fixed | 终态行号: 旧 `_ = scheduled_count` 已删除;形参:39 真消费:58 return:74 均保留 | 修法类: 已直删旧 :74 一行(非承重不补注,删后 :58 仍真用故无 unused-arg 复发,禁区:39/:58/:74) | 同文件兄弟需协调: 无(ISOLATED,siblings=[]) | 最大爆炸风险复核: 未误删 :39/:58/:74,28 个窄测试绿 | 与既有分析冲突: 无 | 前置: 已满足 | owner_pending: n

---

## 对抗核验（第二双眼睛，2026-06-05 执行前历史核验）

核验人：对抗核验 agent（只读不改）。结论 **PASS**，未找到反证。

**重 grep 命令与结果（独立回盘，不照抄档案）**：
- `rg -n "_ = scheduled_count" core/ web/ data/` → 仅 2 处：`batch_order.py:74`、`sgs.py:127`（sgs 非本债，文件不同不撞行）。**与档案字段 1 一致。**
- `rg -n "def dispatch_batch_order|scheduled_count|_coerce_state|return run_state|from_legacy" core/algorithms/greedy/dispatch/batch_order.py` → `def`:17、形参 `scheduled_count: int = 0`:39、`run_state = _coerce_state(`:44、真消费 `scheduled_count=scheduled_count`:58、死行 `_ = scheduled_count`:74、`return run_state.scheduled_count, run_state.failed_count`:75、`def _coerce_state`:78、`from_legacy`:98、`scheduled_count=scheduled_count`:111。**:39/:58/:74/:75 四个关键行号逐一坐实，漂移 0。**
- `rg -n "dispatch_batch_order" core/ web/ data/ tests/` → 生产唯一调用 scheduler.py:431；import scheduler.py:37 + dispatch/__init__.py:13/17；测试 test_greedy_refactor_contracts.py:179/212、test_greedy_scheduler_base_date.py:61/65(monkeypatch 替身)。**无 web/data 直调，与字段 8 一致。**

**逐分支等价性独立复验（Read batch_order.py:17-113 + 测试 :170-260 + scheduler.py:427-440）**：
- `_coerce_state`:96 `if state is not None: return state` 早返回坐实 → 生产路径(scheduler.py:431 传 `state=state` 非 None、不传 scheduled_count)根本不读局部 scheduled_count，删 :74 等价。
- 测试路径(test:56 调用、:76 `scheduled_count=5`、:81 `assert scheduled_count == 6`)走 :98 from_legacy，:74 仅弃值局部 5，返回仍由 run_state property 决定为 6，删 :74 等价。**契约测试 `assert == 6` 是天然回归护栏，无需新增 parity 测试，认可档案字段 7。**
- :74 是局部 scheduled_count 在函数体内最后且唯一一次「使用」，且为弃值无副作用；:75 return 两分量均读 `run_state.*` property 不读局部 → 直删真零下游。

**承重 / 分层 / owner_pending 嫌疑排查**：
- load_bearing=false、lb_no_touch=null、cluster=ISOLATED、owner_pending=false → 档案给终态「直删」修法**未越界**，无承重误删风险，无擅自分配批次。
- 纯删 1 行不新增/不删 import，不触发越层(core.algorithms→core.services)或导入环，AST 违规增量 0 → 分层红线不受影响。
- 灵魂线：无新增兜底/静默回退/except 吞错，纯去噪 → 合规。

**核验串**：R53 | 核验: PASS | 行号复核: 一致(:39/:58/:74/:75 四行独立坐实,漂移 0) | 争议点: 无 | 承重误删风险: 无 | 需升级第三方裁: n
