# R18 逐债档案 · operation_execution_event_repo.py

> 本档案只读不改、只产计划。债 id R18 | 桶 B11 | 簇 C01 | 严重度 low | 承重 false | owner_pending false
> 主文件: data/repositories/operation_execution_event_repo.py
> 同文件兄弟: R19(B13, _positive_ids)；R13 已按 2026-06-09 交接口径解耦，不碰 repo stub

## 0. 一句话结论(回盘后修正)

registry 对 R18 的描述是 **scope 重构之前的旧态**。当前态下该方法早已不是「生产零调、仅测试续命的孤儿」，而是一组「**拒绝非 scoped 读**」契约护栏中的一员（单行 `raise _unscoped_execution_read_error()`）。原 planned_fix「直删 + 退测试 + 摘 EXECUTION_EVENT_EXCEPTION import」已全面失真且会**击穿契约**，不可执行。2026-06-09 交接口径已确认：R18 后续独立处置 repo stub 护栏，R13 不再伸进 repo，R19 仅保留 repo 私有版并补顺序无关注释/parity。

---

## 字段一 · 当前真实 file:line(回盘)+ 漂移记录

按符号名 `list_latest_exception_events_by_op_ids` 在当前工作区 `rg` 回盘（已排除 docs/_panorama_data/）：

| 对象 | registry old_location(勿信) | registry current_evidence(DONT_TRUST_AFTER_FIXED) | **真实回盘** | 漂移 |
|---|---|---|---|---|
| 方法定义 | `:260-265` | 旧 evidence 已失效 | **operation_execution_event_repo.py:402** | old→真 **+142 行** |
| foundation 测试断言 | `:173` | 旧 evidence 已失效 | **tests/operation_execution/test_operation_execution_event_foundation.py:385** | planned_fix→真 **+212 行** |

证据（当前代码逐字）：

- `data/repositories/operation_execution_event_repo.py:402`：
  `def list_latest_exception_events_by_op_ids(self, op_ids: Sequence[int]) -> Dict[int, OperationExecutionEvent]: raise _unscoped_execution_read_error()`
  —— 已是单行 stub，方法体 = 抛 `_unscoped_execution_read_error()`（定义在 `:83-84`，返回 `ValueError("现场执行事件必须按完整计划身份读取，不能只按 op_id 聚合。")`）。
- `tests/operation_execution/test_operation_execution_event_foundation.py:384-385`：
  `with pytest.raises(ValueError, match="完整计划身份"):` / `    repo.list_latest_exception_events_by_op_ids([10])`
  —— 是「拒绝非 scoped 读」契约断言，**不是**「续命正向断言」。

**结论：三处行号全部不可信，已全部回盘。** registry 的旧 `current_evidence` 方向对、行号错；`phase1_blast.location_status` 仍按旧查询实现描述，是**旧态残留、已失真**——见字段三。

## 字段二 · 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = planned`。
- 与 status evidence **部分不一致**：
  - 一致点：方法**确实未按 planned_fix 直删**（:402 仍在），从「删除」口径看「planned 未动手」成立。
  - **关键不一致**：current_evidence 说「scope 重构后已改为单行 stub」——这意味着方法**实质上已被另一轮重构改写**（从旧真实查询变成「拒绝读的 loud raise」）。所以本债语义已被 scope 重构**事实上消解/转性**：原病（生产零调的孤儿方法）已不复存在，现在它是契约护栏的一格。registry 的 title/pathology/planned_fix 仍停留在旧态，是 stale。
  - foundation 测试 :385 的 `pytest.raises` 包裹，证明测试也已随 scope 重构改为「断言拒绝」，而非 planned_fix 设想的「正向 续命断言（assert ...[10].id==exception.id）」。planned_fix 第(2)步要退的那行**已不存在**。

判定：本债处于 **「planned 但前提已被上游重构推翻」** 状态。2026-06-09 台账已采纳这一判断：后续按独立护栏注释方案执行，仍是 planned，但不再 owner 待裁，也不按原计划直删。

## 字段三 · 病理 / 严重度 / 桶 / 簇

- **桶**：B11（home=B11）。**簇**：C01（interference_edges 仅与 R19 同文件相连）。
- **registry 标注病理**：P6「公开 repo 方法生产零调，仅基础回归续命」。**严重度** low。
- **回盘修正后的真实病理**：P6 的「孤儿方法」叙事**已不成立**。当前 :402 是 6 个统一 stub 之一（:355/:357/:400/:402/:404/:406 全是 `raise _unscoped_execution_read_error()`），构成「**OperationExecutionEventRepo 拒绝任何非 scoped/仅 op_id 的读**」契约面。这是 scope 重构**有意保留的 loud-raise 护栏**——保留方法签名是为了：调用方若误用旧的「只按 op_id 读」API，得到一个**带中文业务语义的受控 ValueError**，而不是 `AttributeError`（删掉后才会出现）。
- **`EXECUTION_EVENT_EXCEPTION` 验证**：`rg "EXECUTION_EVENT_EXCEPTION" data/repositories/operation_execution_event_repo.py` → **exit 1 / 零命中**。registry planned_fix 第(3)步「摘掉孤立的 EXECUTION_EVENT_EXCEPTION import」**无对象可摘**，是旧态死信息。import 块现仅 `OperationExecutionEvent / normalize.../validate...` 等，且 `OperationExecutionEvent` 仍被 :402 的返回类型注解（`Dict[int, OperationExecutionEvent]`）引用——**不可删**。
- 严重度维持 low（确实零生产影响），但**桶应从 B11「孤儿删除」迁出**，归类为「契约护栏（已 loud-raise，无需动）」。

## 字段四 · 修法类

**修正后修法类 = 注释（补「我是故意的」中文护栏注释）+ parity 测试已存在（无需新增）。**

不再执行 registry 旧删法。理由：

1. **不可直删**。:402 是「拒绝非 scoped 读」契约面的一格（与 :355/:357/:400/:404/:406 同组）。删它会让该 API 从「受控 ValueError」退化为 `AttributeError`，**击穿契约**，且破坏 :370 `test_operation_execution_repository_rejects_unscoped_op_reads` 的 :385 断言（该测试一次性断言 6 个方法都拒绝）。
2. **不可退测试**。:385 是契约断言，不是续命；退掉等于撤掉护栏验证。planned_fix 要退的「正向续命断言」**已不存在**。
3. **不可清 import**。`EXECUTION_EVENT_EXCEPTION` 零引用、无对象；`OperationExecutionEvent` 被类型注解占用、不可动。

**终态修法（low 优先级，R18 独立处理）**：在 :400-406 这组 stub 上方补一行中文护栏注释，钉死「这是故意保留的 loud-raise，禁止删除/改静默」。例如：
`# 故意保留：拒绝任何"只按 op_id"的非 scoped 读，强制走完整计划身份。删除会退化为 AttributeError，禁止改静默回退。`

- **不涉及收口**（P5），故无收口点选择问题。
- **不涉及 raise 改造**（P4）——它本就已是 loud raise，符合灵魂线，无兜底/无静默。
- owner_pending=false；2026-06-09 台账已采纳「旧直删前提被推翻」这一结论。后续执行只按本档补护栏注释，不借 R13 顺手删 repo stub，也不改静默回退。

## 字段五 · 同文件碰撞债

主文件 `operation_execution_event_repo.py` 上有两条相关债，**性质完全不同，须分开看**：

### 碰撞-1：R13（B11，medium，lb=false）—— 旧耦合已解除
- R13 主文件是 `execution_fact_provider.py`，但其 planned_fix **第(4)步伸进本文件**：声称 `list_latest_events_by_op_ids`（R13 关心的兄弟方法，回盘真实位置 **:400**，非 registry 说的 :254-258）在 R13 删 provider 侧 latest 查询后「成纯死码，与 R18 同批一并删」。
- **回盘修正**：:400 同样已是 stub `raise _unscoped_execution_read_error()`，与 R18 的 :402 是**相邻两行的同组护栏**。R13 planned_fix 把「删 :400」和「删 :402（R18）」捆在一起，是**双重旧态误判**——两者都已不是死码，而是契约护栏。
- **谁先谁后 / 撞行号**：2026-06-09 交接口径已经确认 R13 不碰 repo stub，只在 provider 侧处理字段/形参/实参/可能孤儿 helper。R18 自己处理 :400-406 这组护栏注释；R13 与 R18 不再捆绑成一个原子动作。
- **无 dict 键位移**：这两个方法签名/返回类型不变，不涉及任何 dict literal 键顺序。

### 碰撞-2：R19（B13，跨桶同文件串行，low，lb=false）—— 仅文件级相邻，无逻辑耦合
- R19 主文件是 `execution_snapshot.py`；它与本文件的关系仅是「`_positive_ids`（回盘 :67，被 :408 `_batch_ids_by_op_ids` 调用）这个私有 helper 是 R19 收口分析涉及的 repo 侧副本」。
- R19 的收口落点**明确避开本 repo**（见字段六），所以 R19 即使收口也**不会改动 :402 附近**；与 R18 无行号碰撞。registry `planned_deps_hint` 要求「Phase3 与 B13 R19 在 event_repo 上串行」属**保守串行**，避免两债同时改同一文件触发 merge 漂移，但二者改的区段（:67 vs :400-406）相距 330+ 行，物理不重叠。

## 字段六 · 同收口点债

**R18 修法不涉及 P5 收口**（终态是补注释护栏，非收口）。故严格说**无同收口点债**。

为完整性记录相邻收口情况（顺序敏感性 = 与 R18 无关）：
- R19 是真正的收口债，收口点是「公开 `execution_snapshot.positive_op_ids`」，但 registry 已确认该 naive 收口被**双重阻断**：
  - 阻断A = 循环导入（`execution_snapshot.py:7` 已 import provider，provider 再 import snapshot → 环）；
  - 阻断B = 分层红线（repo 在 `data` 层，snapshot 在 `core/services` 层，`data→service` 收口 = 越层违规，撞 `test_architecture_fitness`）。
- 因此 R19 的 `_positive_ids`（本文件 :67）**不会被收口进 R18 的区段**，也不会反向改 :402。R18 与 R19 在「收口点」维度**零交集**。
- 顺序敏感性：无。R18（补注释 :400-406）与 R19（已保留 repo 私有版并补注释/parity）互不依赖；但仍遵 registry 的文件级保守串行（见字段五碰撞-2）。

## 字段七 · 收口行为差异及所需 parity 测试

**R18 无收口** → 无「新旧两路逐分支等价」问题。但**有一个必须守住的边界差异**，且 parity 测试**已存在**：

- **当前契约边界**：`repo.list_latest_exception_events_by_op_ids([10])` → 抛 `ValueError("...完整计划身份...")`（受控、带中文业务语义）。
- **若误按 planned_fix 直删后的边界（反例）**：同一调用 → 抛 `AttributeError: 'OperationExecutionEventRepo' object has no attribute 'list_latest_exception_events_by_op_ids'`（**裸异常、无业务语义、无中文提示**）。
  - 这正是「None vs raise」式反例的变体：**受控 raise vs 失控 AttributeError**。直删使错误从「业务护栏」退化为「属性缺失」，对任何残留/未来调用方是**静默语义损失**。
- **守护的 parity 测试（已存在，无需新增）**：
  `tests/operation_execution/test_operation_execution_event_foundation.py:370` `test_operation_execution_repository_rejects_unscoped_op_reads`，其 :384-385 断言：
  `with pytest.raises(ValueError, match="完整计划身份"): repo.list_latest_exception_events_by_op_ids([10])`。
  该测试已对 6 个 stub 方法逐一断言拒绝（:379/:381/:383/:385/:387/:389），是现成的契约 parity 守卫。
- **补注释修法对该测试零影响**（注释不改行为），测试继续绿。**这是「low 风险、注释类修法」的强证据。**

## 字段八 · 爆炸半径

### 生产消费者清单
- **零生产消费者（三重证据）**：
  1. `rg "list_latest_exception_events" --glob '!docs/...' .` → 全仓仅 :402（定义）+ 测试 :385，无任何 core/web/data 调用点。
  2. callgraph `edges.json` 命中 1 条，方向为**出边** `OperationExecutionEventRepo.list_latest_exception_events_by_op_ids → _unscoped_execution_read_error`（即方法只调 stub helper），**零入边**=无 caller。
  3. 真实「最新异常态」生产来源是 `operation_execution_state_builder.py` 的 `_latest_exception` + `OperationExecutionState.latest_exception_*` 字段（state_builder/state 路径），**不经此方法**。
- **类导出**：`OperationExecutionEventRepository is OperationExecutionEventRepo`（别名，测试 :377 验证）。即使经别名也无人调此方法。

### 测试消费者清单
- `tests/operation_execution/test_operation_execution_event_foundation.py:385`（在 :370 `test_..._rejects_unscoped_op_reads` 内，与另 5 个 stub 方法共一个测试）。
- 无其他测试引用。

### 灾难链（删错 / 统一错会静默炸什么）
1. **直删（按 registry planned_fix）**：
   - 立即炸：`test_operation_execution_repository_rejects_unscoped_op_reads` 在 :385 `pytest.raises(ValueError)` 失败（变 AttributeError，match 不上）→ **红测试，非静默**（这是好的，至少会被 CI 抓到）。
   - 静默风险：若有人**同时退掉 :385 断言**（registry 正是这么计划的），则护栏永久消失。将来若有调用方误用「只按 op_id 读异常事件」的旧 API，得到 `AttributeError` 而非「完整计划身份」业务提示，**排障成本陡增**——这是 P6 旧叙事掩盖下的真实损失。
2. **改静默（违灵魂线，禁止）**：若有人把 stub 改成 `return {}` 兜底，则非 scoped 读会**静默返回空 dict**，调用方误以为「无异常事件」而非「你不该这样读」——典型静默回退坑，**严禁**。
3. **删连带 import**：`OperationExecutionEvent` 被 :402 返回注解占用，若按旧 planned_fix 误删会 `NameError`/类型解析失败（仅在 `from __future__ import annotations` 下推迟，但 `get_type_hints` 会炸）。`EXECUTION_EVENT_EXCEPTION` 已不存在，删它=无操作但说明 planned_fix 失真。

**净爆炸半径**：补注释修法 ≈ 0（不改行为、不改签名、不改 import）。直删修法 = 中（破契约护栏 + 失语义提示），且与 R13 :400 删除必须原子同提交否则二次漂移。

## 字段九 · 承重毗邻标记

- R18 自身：`load_bearing=false`，`lb_no_touch=null`。
- 同文件承重：`rg` 全文**无任何** `load_bearing` / `我是故意` / `别动` / `承重` 护栏注释（exit 1）。`operation_execution_event_repo.py` 非承重文件（与 phase1_blast `lb_colocation_danger=none` 一致）。
- **禁区行**：无承重禁区。但**建议把 :400-406 这组护栏视为「准禁区」**——它们是 scope 重构刻意保留的 loud-raise 契约面，删除/改静默会破契约。本档案推荐的注释修法**恰好就是给这组「准禁区」补上正式的「我是故意的」中文护栏**，把隐性约定显性化。

## 字段十 · 分层 / 导入环风险

- **R18 修法（补注释）零分层影响**：不新增任何 import。
- 本文件当前 import（回盘 :1-15）：`__future__` / `decimal` / `typing` / `core.models.operation_execution_event` / `core.models.operation_execution_scope` / `core.models.operation_execution_state` / `.base_repo` / `.operation_execution_state_aggregation`。
  - **无 `core.services` 导入**（exit 1 验证），不存在 `data→core.services` 越层。
  - `data → core.models` 是允许方向（repo 依赖领域模型，未反向）。**0 AST 违规风险**。
- **导入环风险**：补注释不引入任何 import，零环风险。
- 对照：R19 的收口若 naive 落到本 repo 会触发越层（data→service）+ 循环导入双阻断——但那是 R19 的问题，**R18 不沾**，且 R18 的修法明确不收口、不下沉、不新建模块（遵分层红线 #3：不为已有概念新建第二模块）。

## 字段十一 · 测试耦合

- **动手前需迁的测试：0 个**（补注释类修法不改任何测试）。
- 关键耦合点：`tests/operation_execution/test_operation_execution_event_foundation.py:370` `test_operation_execution_repository_rejects_unscoped_op_reads`，其 :384-385 是 R18 方法的契约断言。**这是要保护的资产，不是要迁的负担**。
- 若 owner 反其道批准「直删」（不建议）：才需要退 :385 那一行（且必须同时退 R13 名下 :383 那一行，因二者在同一 `with pytest.raises` 序列、相邻），并确认删后该 test 仍对剩余 4 个 stub 有效——**这是把契约测试切薄，应在 Phase2 红队复核**。
- 别名测试 :377 `test_operation_execution_repository_alias_is_exported` 与本债无关、不受影响。

## 字段十二 · 前置依赖

### 必须先做的债 / 门
1. **owner 门已关闭**：2026-06-09 台账已采纳「R18 前提已被 scope 重构推翻」这一发现，owner_pending=false；后续按独立护栏注释方案执行，不再等待 owner 裁断。
2. **R13 已解耦**：R13 不碰 repo stub，只在 provider 侧处理字段/形参/实参/可能孤儿 helper。R18 处理 :400-406 护栏注释时，不顺手删 R13 名下 repo 方法，也不要求 R13 同一提交。
3. **跨桶串行 R19（B13）**：registry 要求在 `event_repo.py` 上与 R19 串行。物理区段不重叠（:67 vs :400-406），属保守串行；R19 已先完成 repo 私有版注释/parity，R18 后续只需在其后按现盘补 stub 护栏。

### 不依赖
- 不依赖任何收口点先行（R18 不收口）。
- 不依赖架构/分层改造（零越层）。

### 推荐执行序
`R18 独立补 :400-406 护栏注释 → 跑 foundation 测试自证绿 → 保持 R19 repo 私有版已落的顺序无关注释/parity`。owner_pending=false；2026-06-09 台账已确认旧直删前提失效，后续不再把 R18 重新挂回 owner 待裁或与 R13 合批。

---

## 对抗核验(第二双眼睛)

> 独立重 grep 回盘，默认怀疑。结论：**PASS（无反证）**。

**重 grep 命令与结果（已排除 docs/_panorama_data/）：**

1. `rg -n "def list_latest_exception_events_by_op_ids" .` → 仅 `operation_execution_event_repo.py:402`。**真实行号 :402，与档案字段一完全一致**（old:260→真:402 漂 +142；旧 evidence 已失效）。行号复核：一致。
2. 方法体逐字：`:402 def list_latest_exception_events_by_op_ids(...) -> Dict[int, OperationExecutionEvent]: raise _unscoped_execution_read_error()` —— 确为单行 stub。同组护栏回盘命中 6 格 `:355/:357/:400/:402/:404/:406`，全是 `raise _unscoped_execution_read_error()`，与档案字段三的「6 格契约面」一致。
3. `_unscoped_execution_read_error` 定义 `:83-84` → `return ValueError("现场执行事件必须按完整计划身份读取，不能只按 op_id 聚合。")` —— 文案逐字一致。
4. 全仓调用点 `rg -n "list_latest_exception_events_by_op_ids" .` → 仅 `:402`(定义) + `tests/operation_execution/test_operation_execution_event_foundation.py:385`(断言)。**零生产消费者证实**，与字段八一致。
5. `rg -n "EXECUTION_EVENT_EXCEPTION" operation_execution_event_repo.py` → **exit 1 零命中**。planned_fix 第(3)步「摘 EXECUTION_EVENT_EXCEPTION」无对象，档案「失真」判定成立。
6. 测试 `:370 test_operation_execution_repository_rejects_unscoped_op_reads`，:378-389 对 6 个 stub 逐一 `pytest.raises(ValueError, match="完整计划身份")`，:385 即本债断言。**是拒绝护栏断言，非续命正向断言**，与字段二/七一致。foundation 测试真实行号 :385，与档案一致（planned_fix:173→真:385，evidence:554→真:385 均已回盘修正）。
7. import 块无 `core.services`（`rg core.services` exit 1）→ 无 data→service 越层，0 AST 风险。`OperationExecutionEvent` 被 :402 返回注解占用，不可删——与字段三/十一致。
8. 承重护栏扫描 `rg "load_bearing|我是故意|别动|承重"` → **exit 1**，确认非承重文件，lb=false / lb_no_touch=null 属实。

**承重判断挑战**：lb=false 经验证（无承重注释、phase1_blast `lb_colocation_danger=none`）。但本债是 6 格 loud-raise 契约护栏之一，档案已将其升格为「准禁区」并把修法收敛为「补注释 + 已存在 parity 测试」，**未混入任何删除/统一/透传/加形参**，未把护栏当无害清理。承重误删风险：**无**（且档案主动标红了 registry planned_fix 的「直删 + 退断言」会击穿契约这一反例，方向正确）。

**owner_pending 挑战**：registry owner_pending=false。档案给了终态修法（补注释）但**明确不擅自重分类**；2026-06-09 交接口径已把 R18 从旧直删方案改成独立护栏注释方案，合规。

**收口/分层挑战**：本债不收口（无 P5），不新建模块，不下沉 helper；R19 的越层+循环导入双阻断与 R18 无关。档案处理正确。

**核验结论：PASS。** 行号、调用者清单、承重判断、分层判断、「无收口点」「parity 测试已存在」均经独立 grep 证实，未发现反证。2026-06-09 台账已把 registry 旧 title/pathology/planned_fix 口径改为独立护栏注释方案；R18 仍 planned，但不再 owner 待裁。
