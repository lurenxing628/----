# R54 逐债档案 — execution_review 放行护栏依赖的 plan-guard 字段投影切成 4 套手维 key 列表

> **2026-06-08 B 执行终态：fixed。** `build_workbench_plan_context` 已扩出 `plan_resolution` / `plan_guard_fields` 收口面，并新增 `plan_guard_fields_for_context` 与三组字段表常量；reports/resource/dashboard/gantt/navigation 五面改走既有收口点或共享 helper。保留各入口原有键面形状，保留 L5 `is_comparison_plan` OR，`_PUBLIC_FILTER_DROP_KEYS` 脱敏同步面未 delegate。

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：`tests/regression_scheduler_workbench_link_guardrails.py` 已由 A 的 **P5.1（commit `1fa076fb`）合并**入 `tests/schedule/route_view/test_scheduler_workbench_links_contract.py`（guard 四态矩阵 / `forbidden_keys` / `can_emit` 逐字段断言完整迁入、未去重）。LB02/LB05/R54 的 guard 锚点重指该合并目标，按 `def test_*guardrail` 系列函数名 `rg` 定位、弃裸行号。详见 `_B_COMPAT_SAFEGUARDS.md §B-2`。


> 桶 B01 / 簇 C01 / 病理 P5(收口类) / 严重度 high / 承重 load_bearing=true / owner_pending=false
> 本档案只读不改，只产计划。承重债唯一合法修法 = 收口到已存在点 + 绑契约/parity 测试 + 「我是故意的」注释；严禁删/合并/透传裸 key。

## 字段 1：当前真实 file:line(回盘)+ 漂移记录

R54 本质 = 同一份 plan-guard 字段投影散成 **4 套手维 key 列表**(registry 只点名 3 套，第 4 套 dashboard 漏列且 basename 还写错)。逐一回盘：

| # | 符号 | registry old_location | 实际 file:line(回盘) | 键数 | 漂移 |
|---|------|----------------------|---------------------|------|------|
| L1 | `_copy_plan_guard_fields` (mapping 形) | reports_workbench:40-53(称 9 键) | `web/viewmodels/scheduler_reports_workbench.py:36`(def) / mapping 38-51 / 回写循环 52-54 / call site **98** | **13 键** | ⚠️有：def 行 36 vs 报告 40，键数 9→13(报告少数 4 键)，call site 报告称 93→实为 98 |
| L2 | `_PLAN_GUARD_FIELD_NAMES` 元组 | navigation_publish:12-21(称 8 键) | `web/routes/domains/scheduler/scheduler_navigation_publish.py:12`(元组起) 13-27 / `_plan_guard_fields`:80-82 / `_publish_context`:85-91 | **16 键** | ⚠️有：元组 12-27 vs 报告 12-21，键数 8→16(报告大幅少数)，`_plan_guard_fields` 报告 75-77→实 80-82 |
| L3 | `_copy_plan_guard_fields` (元组 forloop 形) | resource_dispatch:63-73(称 9 键) | `web/routes/domains/scheduler/scheduler_resource_dispatch.py:64`(def) / 元组 65-79 / 回写 80-81 / call site **109**(及 199 第二处 `_workbench_context`) | **15 键** | ⚠️有：def 64 vs 报告 63，键数 9→15，call site 报告称 102→实 109，且有第二 call site 199 |
| L4 | `_PLAN_GUARD_FIELD_NAMES`(report 漏列第 4 套) | registry 写 `dashboard_workbench.py:19-24` | **实为 `web/viewmodels/dashboard_workbench_context.py:8`**(元组起) 9-21 / 应用循环 **130-132** | **12 键** | ⚠️有：basename 错(`dashboard_workbench.py`→实 `dashboard_workbench_context.py`)，行 19→8，应用 137→130 |
| 收口点 | `build_workbench_plan_context` | links.py:183-251 / 签名 187 | `web/viewmodels/scheduler_workbench_links.py:187`(def) / 函数体 187-258 / 返回 dict 229-258 | — | 签名 187 准；**当前完全不收 guard 字段**(只有 is_preview/can_write_feedback)，4 处都是 builder 输出后手补挂 |
| 真相源 | `plan_role_filter_fields` | view_context.py:277 | `core/services/scheduler/schedule_result_view_context.py:277`(def) / 返回 dict 309-347 | 全集 | ✅准。**已发射全部 guard 键**(is_official_plan:338 / is_preview_plan:339 / is_current_executable_official_version:341 / is_superseded_by_newer_version:346 / can_dispatch / can_write_feedback / plan_role_status / result_summary_parse_*) |
| 下游消费(fail-open 风险点) | `_is_formal_adopted_context` | links.py:271-289(称对 is_comparison/is_superseded 用 truthy) | **已重构**：`_is_formal_adopted_context`:300-308 拆成 `_has_blocked_plan_identity`:281 / `_has_preview_identity`:285 / `_has_only_adopted_roles`:288 / `_is_current_official_identity`:**292-297** | — | ⚠️有：truthy 判定现落在 helper `_is_current_official_identity`(292: `not is_comparison and not is_superseded and is_current_executable_official_version is True`)；execution_review 闸在 **links.py:394**，二次复用在 `can_emit_feedback_write_urls`:**469-470** |

**漂移结论**：全部 7 个锚点行号均漂(幅度 4~10 行)，且有 2 处实质性事实修正——(a) 第 4 套真实路径是 `dashboard_workbench_context.py` 非 `dashboard_workbench.py`；(b) 各列表实际键数远多于报告(13/16/15/12，报告称 9/8/9/未列)，说明本轮已比计划快照进一步分化。

## 字段 2：当前状态 + 与 status evidence 一致性

2026-06-08 B 执行终态：registry `status_2026_06_05 = fixed`。下文 2026-06-05 的 planned 回盘只保留为执行前证据；当前 `build_workbench_plan_context` 已承载 guard 收口面，4 套/5 面手维投影已改走收口点或共享 helper。
## 字段 3：病理 / 严重度 / 桶 / 簇

- **病理 P5(收口类)**：一份概念(plan-guard 字段投影)被切成 4 套手维 key 列表，真相源已存在(`plan_role_filter_fields`)但无人收口到它，4 套各自手映射。
- **键集分化(危险根因)**：源键命名两套——L1/L3(reports_workbench / resource_dispatch)从原始 `plan_resolution` 的 `requested_role/selected_role/is_official/is_preview` **手映射改名**；L2/L4(nav_publish / dashboard)从收口点 `plan_role_filter_fields` **取已命名键过滤**。四套键集互不相等(13/16/15/12)。
- **危险方向(fail-OPEN)**：下游 `_is_current_official_identity`(links.py:292) 对 `is_comparison`/`is_superseded_by_newer_version` 用 **`not <truthy>`** 判定 → 任一列表漏拷这两键 = 该键 falsy = `not falsy` 为真 = **不拦** = 旧正式版本冒充现行采用方案，放行 execution_review 写侧护栏(links.py:394 / 469)。
- **安全方向**：同函数对 `is_current_executable_official_version` 用 **`is not True`**(links.py:296) → 漏拷 = None ≠ True = 拦 = fail-closed，反向安全。
- **当前是否已坏**：否。回盘 4 套**恰好都带齐** is_comparison + is_superseded + is_current_executable_official_version 三关键键。债是 **split 结构本身**让未来单改一处即静默失护栏的潜在风险，非当前已坏(needs_adversarial=false / adv_refuted=false，符合)。
- 严重度 high / 桶 B01 / 簇 C01 / load_bearing=true(护栏正确性)。

## 字段 4：修法类

**owner_pending=false。** 旧 owner_pending=true 是执行前历史口径；本轮已按 OWNER 裁定与 PHASE4 G04 路线落地，以下依赖透析只作为执行前风险证据保留。

候选 = **P5 收口到【已存在】点 `build_workbench_plan_context`(scheduler_workbench_links.py:187，非新建模块)**，符合铁律 5(收口到已存在统一点)：
1. 给 `build_workbench_plan_context` 增加承载 plan-guard 字段的入参(或接收一个 plan_resolution/guard dict)，内部从单一来源 `plan_role_filter_fields`(view_context.py:277) 产出 guard 字段全集，写进返回 dict(links.py:229-258)。
2. 4 套手维列表(L1 reports_workbench:36 / L2 nav_publish:12 / L3 resource_dispatch:64 / **L4 dashboard_workbench_context:8——报告漏列必须含**)全部改为 **delegate 到收口点输出 / `plan_role_filter_fields` 全集**，删除各自私有 key 列表与手映射。
3. **承重红线**(load_bearing=true，铁律 3)：收口同时补「我是故意的」中文注释钉死键集来源单一；**严禁**删/合并某套列表而不迁下游；**严禁**透传裸 key 不经收口点。修法只能是「收口到已存在点 + 绑契约/parity 测试 + 注释」。
4. 不得违灵魂线(铁律 4)：收口不得引入兜底/静默回退。注意当前 4 套都带 `if value is not None` / `if key in identity` 的**静默丢键**逻辑(reports_workbench:53 / resource_dispatch:80 / nav_publish:82 `is not None` 过滤 / dashboard:131 `if key in filters`)——收口后若键缺失应走收口点的确定性默认值(`default_plan_resolution_dict` 已在 view_context:94/145 给 is_current_executable_official_version=False / is_superseded=False 的 fail-closed 默认)，**不得**保留「缺了就不写」的静默路径,因为漏键正是 fail-open 入口。

## 字段 5：同文件碰撞债

同文件兄弟债 `["R44","R58","R66"]`，加干扰边带来的 LB02/LB06/R22/R42/R60：

| sibling | 共址文件 | 各改哪段 | 与 R54 撞行号/键位移? | 谁先谁后 |
|---------|---------|---------|----------------------|---------|
| **R42** | resource_dispatch.py + workbench_links.py | R42 删 `build_workbench_plan_context` 的 plan_id 形参(links.py:190)及存储行(dict:231 `"plan_id"`)；R54 在同函数加 guard 字段 | ⚠️**强撞**：同改 `build_workbench_plan_context` 签名(187-210)与返回 dict(229-258)。R42 删 plan_id 行 → dict 键位移；R54 加 guard 字段行 → 互撞。**MUST 同批次** | 必须同批次，否则后者 rebase 到漂移后的 187-258 整块 |
| **R44** | nav_publish.py + schedule_result_view_context.py | R44(B02)改 navigation_publish:31-32 区(非 guard 闸门) | 不直接撞 guard 列表(L2:12-27)，但同文件。按 PHASE0§10#1：B01(本债)guard 收口进 `build_workbench_plan_context` + 契约测试**必须先落**，B02 的 R44 才能动 nav_publish 且不碰 `_PLAN_GUARD_FIELD_NAMES` | **R54 先，R44 后**(承重先于动同文件) |
| **R58** | nav_publish.py + workbench_links.py | R58(B01 承重)在 nav_publish 落护栏逻辑 | 同文件不同段。改 nav_publish 收 guard 字段时**不得碰 R58 承重逻辑** | 跨桶边(PHASE0§3)，需协调；R58 也承重，互不踩 |
| **R66** | reports_workbench.py + workbench_links.py | R66 在两文件 | 同文件 L1(reports_workbench:36-54);需确认 R66 改段是否与 `_copy_plan_guard_fields` 重叠 | 待 R66 档案确认重叠面 |
| R22 | schedule_result_view_context.py | 同收口真相源文件 | 若 R22 动 `plan_role_filter_fields`(277-347) → 撞 R54 的真相全集 | 真相源改动需先于 R54 收口 delegate |
| R60 | workbench_links.py | R60 删 plan_id 字段表，与 guard 投影在 links.py 相邻 | 删 plan_id 字段表时**勿动 guard 相关**(dict:229-258) | 同 R42 族，同批次或明确隔离 |
| LB02/LB06 | reports_workbench.py / workbench_links.py | 承重债共址 | 见字段 9 禁区行 | — |

## 字段 6：同收口点债

收口到同一符号 `build_workbench_plan_context`(links.py:187) 的其他债：**R42**(删 plan_id 形参/存储行) + **R60**(plan_id 字段表)。三者(R54/R42/R60)都改这一个函数的签名或返回 dict：
- **顺序敏感**：R54 加 guard 字段(dict 末尾 229-258) vs R42 删 plan_id(dict:231 + 形参 190) → 任一先做，另一方的 file:line/dict 键序全漂。
- **结论**：R54 + R42(+R60 若动同 dict)**MUST 同批次**，单批次内一次性改完签名与返回 dict，避免互相 rebase。这正是 planned_deps_hint 的 `R54→R42 必须同批次` 边。
- dashboard / reports / resource_dispatch / nav_publish 4 套 delegate 到收口点输出，互不为对方收口点(它们是收口点的下游消费者)，彼此无顺序敏感；但都依赖收口点先承载 guard 字段(步骤 1 先于步骤 2)。

## 字段 7：收口行为差异 + parity 测试

收口将「4 套各自手映射」换成「收口点 `plan_role_filter_fields` 全集」，新旧两路对边界值**不必然逐分支等价**，必须 parity 钉死：

| 维度 | 旧路(手映射) | 新路(plan_role_filter_fields) | 反例风险 |
|------|------------|------------------------------|---------|
| **源键改名** | L1 取 `data.get("requested_role")` 写 `requested_plan_role`(reports_workbench:39)；`selected_role`→`effective_plan_role`:40 | 收口点用 `_truthy_override_or_data(... ("requested_role",) ...)`(view_context:280) | 若 plan_resolution 同时含 `requested_role` 和 `requested_plan_role`，两路取值口径可能不同 → parity 必测「双键并存」 |
| **缺省值** | 手映射缺键 → `if value is not None` 静默不写(reports_workbench:52-54) → 下游读到 None | 收口点 `_identity_bool` / `default_plan_resolution_dict` 给确定布尔(is_superseded=False:95/146) | **关键反例**：旧路 is_superseded 缺 → 不写 → 下游 `not context.get(...)` = `not None` = True = 不拦(fail-open)。新路 → False → 同样不拦。**两路对「缺键」都 fail-open，但新路对 `is_current_executable_official_version` 给 False → `is not True` = 拦**。需逐键验证收口前后对三关键键(is_comparison/is_superseded/is_current_executable_official_version)在 {缺失, None, False, True} 四态下的拦/放结果完全一致 |
| **None vs raise** | 手映射纯 dict 取值，绝不 raise | 收口点亦不 raise(返回 dict) | 灵魂线(铁律 4)：收口**不得**改成 raise；P4 才需 loud raise，本债 P5 维持「确定性默认值」即可，但默认值方向必须 fail-closed |
| **dashboard 第 4 套(报告漏列)** | dashboard:130-132 `if key in filters` 从 filters 拷 12 键 | delegate 收口点 | 若漏迁 L4 → 收口不彻底，债残留(fix_invalidation_risk②) |

**所需 parity 测试**：对 4 套各自的契约测试(字段 11)增加「收口前后逐键四态 parity」断言，尤其 `tests/regression_scheduler_workbench_link_guardrails.py` 的 `_is_formal_adopted_context` 下游读端逐键(120-162)必须对三关键键覆盖 {缺失/None/False/True}×拦放矩阵。
## 字段 8：爆炸半径

**生产消费者清单(回盘核实)：**
- 4 套手维列表本体：reports_workbench:36-54(+call 98) / nav_publish:12-27,80-92 / resource_dispatch:64-81(+call 109,199) / dashboard_workbench_context:8-21(+apply 130-132)。
- 收口点：`build_workbench_plan_context`(workbench_links.py:187-258)——当前不收 guard，4 处后补挂。
- 下游放行判定：`_is_formal_adopted_context`(links.py:300)→ helper `_is_current_official_identity`(292,truthy fail-open 点)/ `_has_blocked_plan_identity`(275)/ `_has_preview_identity`(285)/ `_has_only_adopted_roles`(288)。
- execution_review 闸：links.py:394(`target_page=="execution_review" and not _is_formal_adopted_context`)。
- 二次复用同一 guard：`can_emit_feedback_write_urls`(links.py:469-470)。
- 真相全集源:`plan_role_filter_fields`(view_context.py:277-347)。

**下游链(4 条全依赖手拷字段完整性)**：报表全入口卡 + 行级动作 + 甘特/周计划/分析/派工导航上下文。

**灾难链(删错/统一错会静默炸什么)**：
1. **fail-OPEN 主链**：任一列表漏拷 `is_comparison` 或 `is_superseded_by_newer_version` → 下游 `not context.get(...)` 为真 → `_is_current_official_identity` 误判为现行官方版 → execution_review(links.py:394)放行 → **旧正式版本/比较版本冒充现行采用方案，写侧护栏被绕**，向历史/比较方案写现场事实(脏写、不可逆)。这是 high + load_bearing 的核心。
2. **漏迁第 4 套**：只收编报告点名的 3 套而漏 dashboard_workbench_context(L4)→ 收口不完整，dashboard 入口仍走旧手拷,债残留(fix_invalidation_risk②)。
3. **同批次错配**：R42/R60 先从 `build_workbench_plan_context` 删 plan_id(191/233)而 R54 后改 → R54 须 rebase 到漂移后的 187-258 整块；反之 R54 先加 guard 字段 → R42 删 plan_id 时 dict 键位移(fix_invalidation_risk①)。
4. **静默丢键被保留**：若收口仍保留各路 `if value is not None`/`if key in identity` 的丢键逻辑 → 缺键仍 fail-open，等于没收口。

## 字段 9：承重毗邻标记 + 禁区行

- **本债自身 load_bearing=true**(护栏正确性，lb_no_touch=N1)。
- **同居承重债**：nav_publish.py 还落 **R58(B01 承重)**；reports_workbench.py / workbench_links.py 共址 **LB02 / LB06**(干扰边)。
- **PHASE0§10 硬约束#1**：B01(本债)guard 收口进 `build_workbench_plan_context` + 契约测试**必须先落**，B02 的 R44(同 nav_publish 文件)才能动，且 R44 不得碰 `_PLAN_GUARD_FIELD_NAMES` 闸门(nav_publish:12-27)。
- **禁区行(改 nav_publish 收 guard 时不得碰)**：
  - R58 承重逻辑段(nav_publish.py，需 R58 档案定段，初判在 `_publish_context`:85 之外的护栏分支)。
  - `_is_current_official_identity`(links.py:292-297) 的 `is not True`/`not <truthy>` 判定方向——**严禁**为了「统一」把 `is_current_executable_official_version is not True` 改成 truthy(会把 fail-closed 翻成 fail-open)。
  - `default_plan_resolution_dict`(view_context:94-95,145-146)的 is_superseded=False / is_current_executable_official_version=False fail-closed 默认值，禁改方向。
- **承重唯一合法修法(铁律 3 重申)**：仅① 收口到已存在点 `build_workbench_plan_context` + ② 绑契约测试 + ③ 补「我是故意的」注释钉键集单一来源。**严禁**删/合并某套列表而不迁下游、**严禁**透传裸 key、**严禁**写删除/统一/合并/透传/加形参以外的简化。

## 字段 10：分层 / 导入环风险

- **0 AST 违规要求**：收口点 `build_workbench_plan_context` 在 `web/viewmodels/scheduler_workbench_links.py`，真相源 `plan_role_filter_fields` 在 `core/services/scheduler/`。web→core.services 是**合法方向**(非 core.algorithms→core.services、非 core.models→core.services 禁向)。
- 现状：nav_publish / resource_dispatch / dashboard_workbench_context **已 import** `plan_role_filter_fields`(view_context)，合法。reports_workbench(L1)目前**未**直接 import 真相源(用原始 plan_resolution 手映射)——收口后将新增 `from core.services.scheduler.schedule_result_view_context import plan_role_filter_fields`,仍是合法 web→core，**不造越层、不造导入环**(view_context 不反向 import web)。
- `build_workbench_plan_context` 内部若调 `plan_role_filter_fields` 收集 guard 字段：workbench_links(web/viewmodels)→ view_context(core/services)合法；无环。
- **结论：收口不引入跨层 import、不造导入环。** 符合铁律 6。

## 字段 11：测试耦合(动手前先迁哪些测试)

收口前/同批次必须迁移并强化的契约/parity 测试(全部回盘存在)：
1. `tests/web_pages/test_reports_workbench_navigation_contract.py`:406 `test_scheduler_navigation_publish_keeps_plan_guard_fields`(钉 guard 键经 nav_publish 透传齐全)+ :84/:130/:258-266(execution_review 对 superseded/historical 禁顶导)。
2. `tests/resource_dispatch/test_resource_dispatch_site_records_frontend_contract.py`:177-211(execution_review_link 服务端 guard 字段保全 + 反向断言前端 JS 不得自拷 requested/effective/source_table/scenario_id)。
3. `tests/regression_scheduler_workbench_link_guardrails.py`:23-50(`can_emit_feedback_write_urls` 逐字段)+ **120-162**(`_is_formal_adopted_context` 下游读端逐键，已含 missing/pop:131、=False:136/167、is_comparison=True:137、is_superseded=True:147——parity 矩阵基础已在此)+ :233 forbidden_keys。
4. `tests/web_pages/test_dashboard_workbench_contract.py`:101-103,145-147,236(dashboard 第 4 套 guard 字段)。
5. 旁证：`tests/schedule/summary/test_schedule_result_view_context.py`:176,222-233(收口点输出键对齐)+ `tests/resource_dispatch/test_scheduler_dispatch_plan_identity_guard.py`:419-435(is_current_executable_official_version 护栏)。

**迁移纪律**：删任一手维列表**前**，必须先确认其对应契约测试改读收口点输出(`build_workbench_plan_context` 含 guard);删列表与改测试同 commit，避免护栏裸奔窗口。

## 字段 12：前置依赖(先做哪些债/哪些 F 门才能做本条)

- **必须同批次(互锁)**：**R42**(同改 `build_workbench_plan_context` 签名 191 / dict 233)——planned_deps_hint 的 `R54→R42 必须同批次`。R60 若动同 dict 亦需同批或明确隔离。
- **必须先于 R54 完成**：若 **R22** 要改真相源 `plan_role_filter_fields`(view_context:277-347)→ 真相源稳定后 R54 再 delegate，否则 delegate 目标漂移。
- **R54 必须先于**：**R44**(B02，同 nav_publish 文件)——PHASE0§10#1 硬约束，B01 guard 收口 + 契约测试落地后 R44 才能动。
- **co-change(同改但非阻塞)**：**R56**(nav_publish 是 R54 投影源之一;navigation_context.py:64 调 `build_workbench_plan_context`，与 R56 护栏同文件同函数族)；**R58**(同 nav_publish 承重，互不踩段)。
- **F 门历史口径**：执行前要求 owner 裁断承重护栏修法方向；2026-06-08 已按“给 `build_workbench_plan_context` 加 guard 收口面 + 共享 helper”路线落地，当前不再作为阻塞门。
- **批次定位(planned_batch_hint)**：Batch-2(B01 guard 字段收口，N1 真相源，单债独立成批)；紧随 Batch-1(R56 等注释网)之后、先于 Batch-3(R42/R60)与 Batch-4(B02 R44);B01 桶内第 2 顺位。**注意**:planned_deps 又说 R54↔R42 必须同批次,而 batch_hint 把 R42 列在 Batch-3——此处存在批次划分内部张力(R42 既要 Batch-3 又要与 R54 同批),需 owner 在 Layer4 总计划裁断:要么 R42 提前到 Batch-2 与 R54 合批,要么 R54 延后到含 R42 的批次。**本档案标注此为待裁的批次冲突,不擅自定终态。**

## 对抗核验(第二双眼睛)

核验人:独立第二双眼睛 agent。只读不改,默认怀疑,逐锚点 rg/grep 回盘。结论 **PASS**(债真实、承重判断成立、收口点真实存在、收口确未发生、修法方向合规;发现 2 处键数计数错需更正但不动结论)。

**重 grep 命令与结果(全部在当前工作区跑):**
1. `rg -n "_copy_plan_guard_fields|build_report_context" web/viewmodels/scheduler_reports_workbench.py` → def 在 **36**(✅与档案一致)、call site **98**(✅)。
2. `rg -n "_PLAN_GUARD_FIELD_NAMES|_plan_guard_fields|_publish_context" .../scheduler_navigation_publish.py` → 元组起 **12**、`_plan_guard_fields` **80-82**、`_publish_context` **85**、call **109/137/168**(✅档案一致;档案漏记 168 第三 call,无害)。
3. `rg -n "_copy_plan_guard_fields|_workbench_context" .../scheduler_resource_dispatch.py` → def **64**、call **109** 及第二处 **199**(✅一致)。
4. `rg -n "_PLAN_GUARD_FIELD_NAMES|for key in" web/viewmodels/dashboard_workbench_context.py` → 元组起 **8**、应用循环 **130-132**(✅;basename 确为 `dashboard_workbench_context.py`,档案已修正 registry 的错写 `dashboard_workbench.py`,核验确认修正正确)。
5. `rg -n "def build_workbench_plan_context|def _is_current_official_identity|def _is_formal_adopted_context|def can_emit_feedback_write_urls" .../scheduler_workbench_links.py` → **187 / 292 / 300 / 469**(✅全部与档案一致)。
6. 读 187-211:`build_workbench_plan_context` 签名**确实不收任何 guard 字段**(只有 is_preview:204 / can_write_feedback:205),`plan_id` 形参在 **191**(R42 删此 → 同批次互锁判断成立 ✅)。
7. 读 292-297:`_is_current_official_identity` = `not is_comparison and not is_superseded_by_newer_version and is_current_executable_official_version is True` → **fail-OPEN(前两键 truthy)+ fail-CLOSED(第三键 is True)方向判断完全坐实** ✅。
8. `rg -c` 三关键键 ×4 文件 → **4 套全各含 is_comparison/is_superseded_by_newer_version/is_current_executable_official_version 各 1 次** → 当前未坏、债为 split 结构风险,与 registry `needs_adversarial=false/adv_refuted=false` 一致 ✅。
9. `rg -rln "_PLAN_GUARD_FIELD_NAMES" + "def _copy_plan_guard_fields" web/` → **恰好 4 套,无第 5 套**;`dashboard_workbench.py`(同名近邻文件)内**无** guard 列表,排除遗漏 ✅。

**行号复核:一致**(7 锚点全准,仅 nav_publish 多一处无害 call 168 档案未列)。

**发现 2 处键数计数错(需更正,但不改结论):**
- **L1 reports_workbench 实为 12 键**(行 39-50,`grep -c '": data.get'` = 12),档案字段1表写「13 键」**多算 1**。
- **L4 dashboard_workbench_context 实为 16 键**(行 9-24),档案字段1表 + registry 均写「12 键」**少算 4**(L4 与 L2 元组逐字相同,均 16 键,含 plan_role_status/plan_identity_error/plan_identity_blocking_error/plan_identity_blocking_scope)。
- 影响:不撼动「4 套键集互不相等、split 真实存在」结论(L1=12 / L2=16 / L3=15 / L4=16,L2 与 L4 相等但 L1/L3 仍不同),亦不撼动承重/收口/fail-open 判断。**建议字段1表把 13→12、12→16 订正。**

**承重误删风险:无。** 字段4/9 候选修法严格限「收口到已存在点 build_workbench_plan_context + 绑契约/parity 测试 + 『我是故意的』注释」,明确禁删/合并/透传裸 key/翻 fail-closed→fail-open,禁区行(links.py:292-297 判定方向、view_context default fail-closed 默认值)列全。未混入越权简化。

**owner_pending 合规:是。** 档案全程标注「待 owner 裁断,暂不给终态修法、暂不分配执行批次」,Batch-2 与 R42 同批次冲突如实标为「待裁」未擅定,符合铁律 7。

**分层:0 违规复核通过。** web/viewmodels→core/services 合法;reports_workbench 收口后新增的 view_context import 仍是合法 web→core,view_context 不反向 import web,无环。

**DRY 静默引爆复核(R54 属护栏嫌疑类):** 4 套看似重复但**实为护栏冗余**——任一被「统一」时若丢 is_comparison/is_superseded_by_newer_version 即静默 fail-open。核验确认档案已把此列为最大爆炸风险并要求 parity 逐键四态矩阵({缺失/None/False/True}×拦放)钉死,DRY 动作风险已被正确识别、未被当无害清理。
