# LB04 逐债档案 — normalize_yes_no_wide 双实现承重(无 parity 测试绑定)

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：`tests/regression_boolean_normalize_wide_parity_contract.py` 在全 git 历史**从未作为测试存在**——它是 **B 待新建**的 LB04 黄金基线 parity（收口前不存在）。B 新建时遵 A 的 P6 终态拓扑 `tests/<模块>/test_*.py` + 模块 docstring + 真 `def test_`（过 P7 防回潮门禁规则①②）。详见 `ANCHOR-DRIFT §六.2`。
> **🟢 2026-06-10 终态补登**：parity 已落——fae8829b(2026-06-08) 往既有 `tests/models_domain/test_yesno_normalization_contract.py` 新增 `test_shared_and_service_wide_yesno_normalizers_stay_in_lockstep`(2 default × 4 unknown_policy 全矩阵锁步)，未另建新文件；函数注释三句(分层红线/有意双实现/勿单边改别名集靠契约绑定)已于 2026-06-10 补全；LB04 registry 已翻 fixed。


> 本档案只读不改、只产计划。承重债 load_bearing=true / P5 / 簇 C01。
> 修法铁律:承重=仅「我是故意的」中文注释 + parity 契约测试，零删除/零合并/零透传。

## 1) 当前真实 file:line(grep 回盘)+ 漂移记录

回盘命令 `rg -n "def normalize_yes_no_wide" core/`，结果:

| 角色 | 符号 | old_location_DONT_TRUST | 当前真实 file:line | 漂移 |
|---|---|---|---|---|
| 债主体(下层事实源) | `def normalize_yes_no_wide` | `core/shared/boolean_normalize.py:33` | `core/shared/boolean_normalize.py:33` | **无漂移**(行号准) |
| 对照实现(上层) | `def normalize_yes_no_wide_value` | （registry 未给）| `core/services/common/normalization_matrix.py:168` | 准(phase1_blast.location_status 已核) |
| 第三层薄 shim | `def normalize_yes_no_wide` | （registry 未给）| `core/services/common/enum_normalizers.py:172`(:173 delegate 到 matrix) | 准，**非真第三实现**，是 re-export delegate |

- 债主体的 `def` 在 `boolean_normalize.py:33`，函数体 33-62 行；其上方 31-32 为空行/无注释（证据见 §9 禁区行）。
- old_location 行号 33 与当前一致 → **漂移幅度 0**。该文件自 b08162cd 起 `git diff` 为空（已核，见 §2）。

## 2) 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = planned`。
- 一致性核查(独立回盘):
  - `git diff b08162cd -- core/shared/boolean_normalize.py` = **空**(无任何改动)；matrix 同样空。
  - `boolean_normalize.py:31-32` def 上方**无**「我是故意的 / 勿单边修改」护栏注释（承重修法第一动作未落地）。
  - `rg -ln "normalize_yes_no_wide" tests/` = **零命中**(承重修法第二动作=parity 契约测试未落地)；目标文件 `tests/regression_boolean_normalize_wide_parity_contract.py` 不存在。
- 结论:两件承重修法动作(注释+测试)均未落地 → **状态 planned 与现场证据完全一致**，无 status drift。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P5**(语义全等双实现/收口缺失)，叠加 **承重 load_bearing=true、adv_refuted=false**(对抗未推翻，承重确认)。
- 严重度 **medium**。
- 桶 **B04**(home=B04)；簇 **C01**。
- 实质:`boolean_normalize.normalize_yes_no_wide`(下层 core.shared) 与 `normalization_matrix.normalize_yes_no_wide_value`(上层 core.services.common) 是两套**语义全等**的 wide yes/no 归一，**无任何测试绑定二者等价** → 任一侧单边改别名集/枚举值/policy 默认值即可静默漂移，且消费方分属 core.models 与 core.services 两条独立链(见 §8)。

## 4) 修法类

**修法类 = 注释 + parity 契约测试(承重唯一合法动作)，零删除/零合并/零透传/零透传形参。**

owner_pending=false → 给终态修法:

1. **注释(钉在 `core/shared/boolean_normalize.py:33` def 上方，逐字采用 §90 LB-B1 草案)**:
   - 「这不是 normalization_matrix.normalize_yes_no_wide_value 的冗余副本:」
   - 「core.models / core.algorithms 在下层只能调本函数，删它改指 core.services 会撞分层红线。」
   - 「两套必须保持语义等价——靠 regression 契约测试绑定，勿单边修改别名集/枚举值。」
   - 注:草案提及 core.algorithms 是**分层前瞻论据**；当前回盘 core/algorithms/ 全仓零消费(`rg normalize_yes_no_wide core/algorithms` 空)，应在注释中标为「前瞻」而非「现状」，避免日后被当幻觉误删。
2. **parity 契约测试(新建 `tests/regression_boolean_normalize_wide_parity_contract.py`)**:断言 `boolean_normalize.normalize_yes_no_wide` 与 `normalization_matrix.normalize_yes_no_wide_value` 在【全 wide 别名集 × {是/否/None/空串/未知值} × {default, passthrough, raise, no} 全 unknown_policy】矩阵上逐一同值；任一侧单边改别名集/枚举值即触发红灯。

**P5 收口点裁断:本债 NOT 走收口删除路。** 收口(让两套归一)的唯一合法点已存在=上层 `normalization_matrix.normalize_yes_no_wide_value:168` 反向 delegate 到下层 `boolean_normalize.normalize_yes_no_wide:33`(services→shared 合法下行边)。但 **Batch-1 阶段不做收口，只做注释+测试网**(收口属后续收敛动作，须在安全网就位后单独评估)。绝不允许反向「删 shared 改指 services」(造越层+导入环，见 §10)。

## 5) 同文件碰撞债(siblings)

registry `same_file_siblings = []` → **债主体所在 `core/shared/boolean_normalize.py` 无同文件兄弟债**。本债的注释只钉在该文件 31-33 行，不与任何其它债撞行。

⚠️ **registry interference_edges 与真实 primary_file 对账发现两处失真(本档案不改 registry，仅记录)**:
- edges 标 `LB07 / R33 同 file:schedule_config_runtime_coercion.py` —— 但回盘 LB07 的 primary_file 实为 `schedule_config_runtime_snapshot.py`、R33 实为 `compat_parse.py`，二者**并非**与本债消费方文件同驻。该 edge 系基于「消费链旁触」非「同文件」，标注 same_file=true 偏宽。
- 真正与本债**消费点**(`schedule_config_runtime_coercion.py:192/201/230`)同文件的是 **R47**(死参数 raw_value，:185 附近)与 **R71**(config 双栈逐字复制函数群)，二者 primary_file 均为 `schedule_config_runtime_coercion.py`(已核)。
- 但 LB04 在该文件**不落任何改动**(注释只在 boolean_normalize.py)，故对 R47/R71 **零撞行、零 dict 键位移**;它们改各自函数体，互不重叠。

## 6) 同收口点债 + 顺序敏感性

- 收口点 = `boolean_normalize.normalize_yes_no_wide` / `normalization_matrix.normalize_yes_no_wide_value` / `enum_normalizers.normalize_yes_no_wide` 三符号。
- 回盘:**当前无其它债收口到这三个符号**(same_symbol edges 全为空，interference_edges 里所有 same_symbol=false)。R41 虽同桶且也涉 enum_normalizers.py，但 R41 主体是 `enum_display.py`/process_bp 的中文枚举标签(skill_level/ready_status 等)，**与 yes_no_wide 符号不交**(已核 R41 title)。
- **顺序敏感性:无。** 本债是纯增量安全网(加注释+加测试)，不改任何收口符号体；R41 改 enum_display 标签也不动 yes_no_wide。两债可并行，无谁先谁后约束。

## 7) 收口行为差异 + 所需 parity 测试

**逐分支并排核(boolean_normalize:33-62 vs matrix:168-195)——当前两套是否逐分支等价?**

| 分支 | boolean_normalize.py | normalization_matrix.py | 等价? |
|---|---|---|---|
| 返回字面量 | `_YES="yes"` / `_NO="no"`(:5-6) | `YesNo.YES.value` / `YesNo.NO.value` | **等价**(已核 enums.py:7-8 `YES="yes"`/`NO="no"`) |
| `value is None` | 返回 `default_norm`(:42-43) | 返回 `default_norm`(:177-178) | 等价 |
| 空串 `text==""` | **硬返回 `_NO`，忽略 default**(:48-49) | **硬返回 `YesNo.NO.value`，忽略 default**(:181-182) | 等价(注:此处都无视 default，是共同的暗坑) |
| TRUE wide 集 | `("y","yes","true","1","on")`(:7) + `"是"` | `narrow+("on",)=("y","yes","true","1","on")`(:40) + `"是"` | **当前等价** |
| FALSE wide 集 | `("n","no","false","0","off","")`(:8) + `"否"` | `narrow+("off","")=("n","no","false","0","off","")`(:41) + `"否"` | **当前等价** |
| true/false_aliases 形参 | merge+lower(:46-47,50-52) | merge+lower 内联(:183-185) | 等价 |
| unknown_policy=default | 返回 default_norm(:56-57) | 返回 default_norm(:189-190) | 等价 |
| unknown_policy=passthrough | 返回原 `text`(:58-59) | 返回原 `text`(:191-192) | 等价 |
| unknown_policy=raise | `raise ValueError(text)`(:60-61) | `raise ValueError(text)`(:193-194) | 等价 |
| unknown_policy=其它/no | 返回 `_NO`(:62) | 返回 `YesNo.NO.value`(:195) | 等价 |

**结论:今天两套全分支逐一等价** —— 这正是 P5 的危险:等价但无任何测试钉住，任一侧单边改别名集/枚举值就静默漂移而无人发现。

**所需 parity 测试(反例驱动)**:测试须对下列**会漂移的反例**断言两路同值:
- 别名漂移反例:若有人给 matrix 的 `_YES_NO_TRUE_WIDE` 加 `"t"`/`"yes!"`，或改 narrow 基集 → 同输入两路应同值(红灯=不等价)。
- 枚举值漂移反例:若 `YesNo.YES.value` 从 `"yes"` 被改 → 返回字面量分叉，红灯。
- policy 默认值反例:`unknown_policy` 缺省 boolean_normalize 用 `"no"`(:37)、matrix 用 `"no"`(:172) —— 须断言缺省调用同值(防一侧改缺省)。
- 边界值反例:`None`(→default_norm) vs 空串`""`(→ 硬 NO，**无视 default**) —— 二者结果不同，测试须分别覆盖以防有人「修一致」时误把空串也走 default。
- 矩阵规模:全 wide 别名(7真+6假含空串) × {是,否,None,未知"foo"} × 4 个 unknown_policy = 约 70+ 组合逐一 assert 同值。

## 8) 爆炸半径(消费者清单 + 灾难链)

**生产消费者(下层 boolean_normalize 链)**:
- `core/models/schedule_config_runtime_coercion.py:6` import；`:192 / :201 / :230` 调用(yes/no 配置项校正)。
- `core/services/scheduler/number_utils.py:5` import + `:9 def to_yes_no` / `:17` 包装它(default="no")。
- 经 `to_yes_no` 下游(已核引用路径):
  - `core/services/scheduler/resource_pool_builder.py`
  - `core/services/scheduler/config/config_field_coercion.py`
  - `core/services/scheduler/run/optimizer_config.py`、`run/schedule_input_collector.py`、`run/freeze_window.py`

**生产消费者(上层 matrix 链，经 enum_normalizers shim)**:
- `core/plugins/manager.py:11` import + `:20` 调用(plugin enabled，unknown_policy="default")。
- `core/services/system/system_config_service.py:8` import + `:31` 调用(system_config，default="no")。
- `core/services/personnel/operator_machine_query_service.py:6` import + `:50` 调用(is_primary 主操作工，unknown_policy="no")。

**测试消费者**:`rg normalize_yes_no_wide tests/` = **零**。这正是债本身——无任何测试绑定两套等价。

**业务影响面**:auto_assign / freeze_window / ortools 开关、is_primary 主操作工、plugin enabled、system_config。

**灾难链(删错/统一错会静默炸什么)**:
- 若**删 shared 让 core.models 改指 services**:除分层红线(见 §10)外，会让 schedule_config 校正与 plugin/system_config/personnel 链耦合成单点，任一改动连锁影响调度开关。
- 若**单边改 matrix 别名集**(如挪 narrow 基集):personnel `is_primary`、plugin enabled、system_config 三处静默换语义，而 schedule_config 调度开关侧(走 boolean_normalize)不变 → 两套调度判定对同一原始字符串给出相反 yes/no，**无测试报警**。这是最大静默风险。
- **FINDING-ERROR 实证(修正旧报告)**:旧报告称「core/algorithms/greedy 经 number_utils 引用」为**假** —— 回盘 `core/algorithms/` 全仓无 to_yes_no / boolean_normalize / normalize_yes_no_wide。真消费方全在 `core.services.scheduler` 与 `core.models`，core.algorithms 当前零消费(见 §4 注释「前瞻」标注)。

## 9) 承重毗邻标记 + 禁区行

- **承重文件本身**:`core/shared/boolean_normalize.py` = LB04 承重文件(load_bearing=true)。
- **同文件毗邻 LB**:`same_file_siblings=[]` → 该文件内无其它承重债毗邻，本债是该文件唯一债。
- **禁区行(承重不可触碰)**:
  - `core/shared/boolean_normalize.py:5-8`(`_YES/_NO/_YES_NO_TRUE_WIDE/_YES_NO_FALSE_WIDE` 别名常量)——禁单边改，与 matrix:38-41 须保持等价。
  - `core/shared/boolean_normalize.py:33-62`(`normalize_yes_no_wide` 函数体)——禁删/禁改语义/禁改返回字面量/禁改 policy 默认。
  - `core/services/common/normalization_matrix.py:168-195`(对照实现)——同样禁单边改。
  - **唯一允许动作**:在 `:33` def 上方(现 31-32 空行处)**新增**注释；**新增** parity 测试文件。除此之外该文件零改动。
- **绝对禁令**:禁以「统一到矩阵」名义删 `boolean_normalize.py` 让 `schedule_config_runtime_coercion` 改指 `core.services.common`(造越层+导入环，见 §10)。

## 10) 分层 / 导入环风险

**本档案给定的修法(注释+测试)= 零分层风险**:注释是纯文本，测试文件在 tests/ 不引入跨层 prod import(测试可同时 import core.shared 与 core.services，测试不算分层违规)。

**但若被错误执行成「删 shared 改指 services」会立刻产生**(已回盘证据):
- `core/services/common/normalization_matrix.py:5` 反向 `from core.models.enums import ... YesNo`。
- `core/models/schedule_config_runtime_coercion.py:6` 当前 `from core.shared.boolean_normalize import normalize_yes_no_wide`(合法:models→shared 下行)。
- 若改成 models→services.common，则形成 **`core.models → core.services.common → core.models.enums` 导入环** + **core.models→core.services 越层**，破坏 AST 0 违规结构。
- 已核 `core/shared/boolean_normalize.py` 是**真叶子**(`rg "core\." ` 仅 `from __future__` 与 `typing`，零 core import) → 它能安全被 core.models / core.algorithms 下层调用，正是其承重价值。
- **唯一合法消重方向**(若日后收口):上层 `matrix.normalize_yes_no_wide_value` 反向 delegate 到下层 `boolean_normalize.normalize_yes_no_wide`(services→shared 合法下行)，保 shared 为唯一事实源、删 matrix 内重复体——**而非相反**。本批次不执行此收口。
- 反向风险旁证:若错误删 shared，还会连带击穿 **R29**(`core.services.common/number_utils` 同层假设)的分层前提。

## 11) 测试耦合

- 动手前**无既有测试需迁移**:`rg normalize_yes_no_wide tests/` = 零命中，本债的测试是**从零新建**而非迁移。
- 新建 `tests/regression_boolean_normalize_wide_parity_contract.py` 需同时 import `core.shared.boolean_normalize` 与 `core.services.common.normalization_matrix`(测试跨层 import 合法)。
- 不影响任何现有测试套件(纯增量)。

## 12) 前置依赖

- **前置债:无。** 本债是纯增量安全网，不依赖任何其它债先完成。
- **批次:Batch-1**(承重注释+契约/parity 测试网，全局 ROOT，纯增量零结构)。与 LB01/LB02/LB03/LB05/LB06/LB07/LB08/R56/R58/R03 同批，互不改同一行段，可并行。
- **协同(非阻塞)**:co_change=R41(同桶 B04 但完全独立:不同文件 enum_display.py、无收口冲突、可并行)。
- **F 门**:无特殊门;落地后即作为「后续所有收敛/删除动作」的安全网前置——即**任何针对 yes/no 归一的收敛/删除动作,都必须等本债的 parity 契约测试先就位**(否则收敛无红绿灯保护)。
- fix_invalidation_risk=**none**(纯增量，不影响他债修法；他债也不使本债失效)。

## 对抗核验(第二双眼睛)

核验时间 2026-06-05，独立 rg/grep 回盘，只读不改。

**重 grep 命令与结果**:
- `rg -n "def normalize_yes_no_wide" core/` → 三处:`boolean_normalize.py:33`、`enum_normalizers.py:172`、`normalization_matrix.py:168`(`normalize_yes_no_wide_value`)。**全部与档案一致，漂移 0**。
- Read `boolean_normalize.py` 全文:别名常量 :5-8、函数体 :33-62、分支行号(None→:42-43、空串→:48-49、policy→:55-62)**逐行核实与 §7 表一致**;`^from|^import` 仅 `__future__` 与 `typing` → **真叶子确认**。
- Read `normalization_matrix.py:160-195` + `:1-10`:对照实现 :168-195 分支与 boolean 镜像;:5 `from core.models.enums import ... YesNo`(反向 import 论据成立);wide 集 :40-41=`narrow+("on",)`/`narrow+("off","")` → 展开等于 boolean 的集合(顺序不同、**集合等价**)。
- Read `enum_normalizers.py:168-173`:shim :172 完全 delegate(`return normalize_yes_no_wide_value(...)`) → **非真第三实现确认**。
- `rg -n "normalize_yes_no_wide|boolean_normalize|to_yes_no" core/algorithms/` → **ZERO** → §8 FINDING-ERROR(旧报告 algorithms 引用为假)**实证成立**,注释标「前瞻」正确。
- 消费者逐点核:`schedule_config_runtime_coercion.py:6/192/201/230`、`number_utils.py:5/9/17`、`plugins/manager.py:11/20`、`system_config_service.py:8/31`、`operator_machine_query_service.py:6/50` → **全部命中、行号准**。
- `core/models/enums.py:7-8` `YES="yes"`/`NO="no"` → §7 返回字面量等价成立。

**结论**:
- 行号复核:**一致**(三符号 + 全部消费者 + 分支行号零漂移)。
- 承重判断:**成立**(boolean 真叶子供下层 core.models;删 shared 改指 services 造 models↔services 环 + 越层，与 §10/registry 一致)。
- 修法合规:**注释 + parity 契约测试**,零删除/零合并/零透传;明确禁反向删 shared;P5 收口点(matrix 反向 delegate 到 shared)只记录不执行 → **承重误删风险:无**。
- owner_pending=false:终态修法给得合规，**无擅自越权**。
- 「像重复实为护栏」专项:DRY 统一动作的静默引爆路径(单边改 matrix 别名→personnel/plugin/system_config 与调度开关链对同串给相反 yes/no 且无测试)已被 §8 灾难链覆盖，parity 测试正是其红绿灯 → **统一动作不会被本档案放行成静默引爆**。

**新增观察(档案未记，无害但建议补)**:`enum_normalizers.py:168-169` 自带一份独立的 `_YES_NO_TRUE_WIDE/_FALSE_WIDE` 常量。因 shim :172 完全 delegate 到 matrix、不读自身常量，这两个常量在 wide 路径上是**死代码**,不构成第四实现，不影响 parity 结论;但日后若有人改 shim 让它改读本地常量，即多一处漂移源。建议 parity 测试或注释顺带覆盖此点(非阻塞)。

**核验判定:PASS**(找不到反证;行号、承重、分层、收口点真实性、收口等价性主动找反例均未推翻)。
