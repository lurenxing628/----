# R21 逐债档案 — gantt_plan_query 收口迁移后遗留的死兼容 shim

> 主文件: `core/services/scheduler/gantt_plan_query.py`
> 桶 B02 / 簇 C01 / 病理 P3 / 严重度 medium / 承重 false / owner_pending false
> 本档只读不改,只做依赖与安全批次分析。回盘日 2026-06-05。
>
> **⚠ 2026-06-08 执行终态：fixed。** G27 执行时仅删除 `resolve_plan` / `selected_plan_role` / `_has_explicit_gantt_range` 三个死 shim 及其专属 import，严格保留 `default_plan_resolution_dict` wrapper、`_default_plan_resolution_dict` import、`ValidationError("未知的排产方案角色：{role}")` 遗留文案，以及 `get_version_time_span_dates` / `resolve_gantt_range_for_version` / `attach_gantt_range_metadata` / `build_empty_week_plan_payload` 四个 LIVE range 函数。下文保留执行前风险分析作溯源，凡出现 planned 旧语义，以本横幅为准。

## 字段 1 — 当前真实 file:line(grep 回盘)+ 漂移记录

按符号名 `rg -n "^def ..."` 回盘 `core/services/scheduler/gantt_plan_query.py`(全路径已确认仅此一份):

| 符号 | old_location(勿信) | 当前真实行 | 漂移 |
|---|---|---|---|
| `default_plan_resolution_dict`(KEEP wrapper) | :32 | **:32-39** | 无 |
| `resolve_plan`(死 shim) | :42 | **:42-43** | 无 |
| `selected_plan_role`(死 shim) | :46 | **:46-47** | 无 |
| `_has_explicit_gantt_range`(死 shim) | :59 | **:59-71** | 无 |
| import `_resolve_plan` | — | **:14-16** | — |
| import `_selected_plan_role` | — | **:17-19** | — |
| import `_has_explicit_display_range` | — | **:23-25** | — |

LIVE 辅助函数(不可删,被 gantt_service 真用):`get_version_time_span_dates`@50-56、`resolve_gantt_range_for_version`@74-95、`_empty_message_for_out_of_range`@98-110、`attach_gantt_range_metadata`@113-132、`build_empty_week_plan_payload`@135-156。

**漂移结论:零漂移。** registry `location_status:"✅准"` 属实 —— 报告 42,46,32,59 四点全部逐字命中当前工作树,本文件不在漂移名单。证据:`rg -n "^def resolve_plan|^def selected_plan_role|^def _has_explicit_gantt_range|^def default_plan_resolution_dict" gantt_plan_query.py` → 42/46/59/32。

## 字段 2 — 当前状态 + 与 status evidence 是否一致

状态 **fixed**。2026-06-08 回盘结果：
- `resolve_plan` 死 shim 与 `_resolve_plan` import 已删除 ✅
- `selected_plan_role` 死 shim 与 `_selected_plan_role` import 已删除 ✅
- `_has_explicit_gantt_range` 死 shim 与 `_has_explicit_display_range` import 已删除 ✅
- `default_plan_resolution_dict` wrapper 仍在，仍保留遗留 bad-role 文案重抛逻辑 ✅
- 四个 LIVE range 函数仍在，`gantt_service` 真实调用链未动 ✅

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- **病理 P3(收口迁移做了一半)**:解析规则已收口到 `schedule_result_view_context` / `schedule_result_view_range`,老调用方已全部迁走(见字段 8 实证),但收口后留下 3 个零生产消费的空壳 shim,且 1 个邻居 wrapper(dpr_dict)靠测试人工续命,共同制造「gantt_plan_query 仍是 plan 解析入口」的假象。
- **严重度 medium**:无运行期危害(死代码),但维护期误导强 —— 后人易把 shim 当真入口去改,或误判整模块为死模块而连带删掉 4 个 LIVE range 辅助函数(灾难,见字段 8)。
- **桶 B02 / home B02**(scheduler-plan-identity 身份族)。
- **簇 C01**:与 gantt_service.py / scheduler_gantt.py 同文件邻接形成的甘特身份族干扰簇。

## 字段 4 — 修法类

**修法类 = 直删 + 退测试**(非承重、非 owner_pending,允许动手删)。承重 false → 不受「仅注释」约束。

具体动作(本批只删 3 个真死 shim,严格保留 dpr_dict wrapper):
1. 删 `resolve_plan`(:42-43)→ 同步删 import `_resolve_plan`(:14-16);
2. 删 `selected_plan_role`(:46-47)→ 同步删 import `_selected_plan_role`(:17-19);
3. 删 `_has_explicit_gantt_range`(:59-71)→ 同步删 import `_has_explicit_display_range`(:23-25);
4. **不删任何续命测试**(本条删的 3 个 shim 经 grep 证实无任何测试引用,见字段 11)。

**这不是 P5 收口动作**:收口早已完成(规则在 `schedule_result_view_context`/`schedule_result_view_range`),R21 只是清扫收口后空壳,无新建收口点、无新增形参、无透传、无统一合并。符合铁律 5(不新建第二模块)。

**严格禁区(本批绝不碰)**:
- `default_plan_resolution_dict` wrapper(:32-39)及其 `import _default_plan_resolution_dict`(:11-13)—— R22 precondition 硬约束要求「保留遗留文案包装」,删它即让 R22 失效(见字段 6)。
- 整文件不可删 —— 含 4 个 LIVE range 辅助函数(见字段 8 灾难链)。
- 灵魂线核查:本修法纯删空壳,不新增兜底/静默回退/except 吞错;保留的 dpr_dict wrapper 是 loud raise(:38 抛 ValidationError),不动它即不违灵魂线。

## 字段 5 — 同文件碰撞债

registry `same_file_siblings: []` —— **gantt_plan_query.py 内无其他登记债**,本文件内零碰撞。R21 独占该文件的 shim 区。

唯一需自洽协调的是:R22 的处置对象 `default_plan_resolution_dict` 与 R21 的 3 个删除目标**在同一文件相邻**(:32 vs :42/:46/:59)。但 R22 主文件登记在 `schedule_result_view_context.py`(R22 的私有实现源头),其在本文件的 wrapper 由 R21「保留不动」交还给 R22,二者作用面不重叠:
- R21 删 :42/:46/:59 三段 + 三条 import(:14-16/:17-19/:23-25);
- R22 后续处置 :32-39 wrapper(去留待 R22 定)。

**行号位移影响**:R21 删除发生在 :42 及以后(及 import 区 :14-25),会使 :32-39 的 dpr_dict wrapper **行号上移**(import 区删 9 行 → wrapper 整体上移约 9 行)。若 R22 用 file:line 锚点引用 wrapper,需在 R21 落地后重新回盘 —— 建议 R22 改用符号名锚定 `default_plan_resolution_dict` 而非行号。无 dict 键位移风险(本文件无 dict 字面量被改)。

## 字段 6 — 同收口点债 + 顺序敏感性

R21 删的 3 个 shim 收口到 **2 个已存在的 LIVE 收口点**:`schedule_result_view_context`(resolve_plan/selected_plan_role 的真身)与 `schedule_result_view_range`(has_explicit_display_range 的真身)。收口到同一收口点的其他债:

- **R44**(`scheduler_navigation_publish.selected_plan_role`):与 R21 共享收口点 `schedule_result_view_context.selected_plan_role`。R21 删本文件的 `selected_plan_role` re-export shim(:46),会**抽掉 R44 收口所照搬的 re-export 范式先例**。**顺序敏感(软)**:若 R44 打算「照抄 gantt_plan_query 的 re-export 写法」,R21 先删会让该范式悬空 → R44 应改为**直接 re-export `core` 收口点**(非硬阻塞)。建议 **同批**,R44 不依赖 R21 的 shim 存活。
- **R22**(`schedule_result_view_context.default_plan_resolution_dict` 手搓双私有实现):与 R21 共享 dpr_dict 这一符号家族。**顺序敏感(硬)**:R21 必须**保留** dpr_dict wrapper(:32-39),R22 的 precondition「保留 gantt_plan_query:32-38 遗留文案包装」才成立。**R21 先行、只删另 3 个**,dpr_dict 去留留给 R22。次序:R22 收口 → R21 删 3 shim → R44 协调(同批 Batch-4,内部串行)。

## 字段 7 — 收口行为差异 + 所需 parity 测试

R21 的 3 个 shim 是**纯透传 re-export**(`return _resolve_plan(...)` / `return _selected_plan_role(...)` / `return _has_explicit_display_range(...)`,无任何参数改写、无默认值差异、无异常重写),新旧两路逐分支**完全等价**。删除等于「移除一层无副作用别名」。

- **无需 parity 测试**:因为没有「新路 vs 旧路」之分 —— shim 本就直接调用收口点,删 shim 后调用方早已不经 shim(零调用方,见字段 8)。不存在 None-vs-raise 这类边界分歧。
- **唯一需守护的契约**在 dpr_dict wrapper(R21 保留、不删):它**不是纯透传**,而是把收口点抛出的 `ValidationError` 重写文案为「未知的排产方案角色：{role}」(:35-39 catch→改文案→re-raise),仅在 `field=="plan_role"` 且 role 非空时改写,否则原样 re-raise。该分歧由 `tests/schedule/summary/test_schedule_result_view_context.py:294-301` 续命,R21 不动它 → 该 parity 测试继续生效,交 R22 处置。
- 反例参考(R22 才需考虑):若 R22 误删 wrapper,`default_plan_resolution_dict("bad")` 的错误文案将从「未知的排产方案角色：bad」退化为收口点原始文案 → 续命测试 :300 断言变红。这是 R22 的责任面,非 R21。

## 字段 8 — 爆炸半径(生产消费者 + 测试消费者 + 灾难链)

**生产消费者(3 个删除目标):零。** 实证:
- 全仓唯一 `from ...gantt_plan_query import` 语句是 `gantt_service.py:12`,只导入 4 个 LIVE 辅助函数(`attach_gantt_range_metadata`/`build_empty_week_plan_payload`/`get_version_time_span_dates`/`resolve_gantt_range_for_version`),**不含任一死 shim**。证据:`rg -n "from .*gantt_plan_query import" core/ web/ data/ tests/` 仅命中 gantt_service.py:12。
- `selected_plan_role` 的真实消费者(gantt_critical_chain_provider:21、gantt_service:34、scheduler_gantt、scheduler_week_plan)**全部 import 自 `schedule_result_view_context`**,非本文件 shim。证据:`gantt_critical_chain_provider.py:21: from .schedule_result_view_context import default_plan_resolution_dict, selected_plan_role`。
- callgraph `edges.json` 对 `gantt_plan_query.py::selected_plan_role` 显示 5 条 inbound、`resolve_plan` 2 条 —— **均为按符号名解析的假阳性**(callgraph 同名归并),已用源码 import 实证否决。`_has_explicit_gantt_range` inbound = 0(连假阳性都没有)。

**测试消费者(本条删的 3 个 shim):零。** 唯一命中 `gantt_plan_query.` 的测试是 `tests/schedule/summary/test_schedule_result_view_context.py:294-301`,它只调 `default_plan_resolution_dict("bad")`(R21 保留的 wrapper,不在删除清单)。删 3 死 shim 无任何测试变红。

**灾难链:**
- ☠️ **误删整模块**:若有人见 3 shim 全死就判 gantt_plan_query 整体死模块连带删除 → 打断 `gantt_service` 对 4 个 LIVE range 函数的真调用 → 甘特周计划范围解析(version_span / week_range / empty_message)整条静默断裂。**防线:字段 4 明列「整文件保留」。**
- ☠️ **误删 dpr_dict wrapper**:见字段 6/7,使 R22 precondition 失效 + 续命测试变红。**防线:本批排除 :32-39。**
- ✅ 仅删 3 透传 shim + 3 import:零运行路径触碰,安全。

## 字段 9 — 承重毗邻标记 + 禁区行

- `load_bearing=false`、`lb_no_touch=null` —— R21 本身非承重。
- registry `lb_colocation_danger: "none"` —— **gantt_plan_query.py 非承重护栏文件,同文件无 LB 邻居**。无承重禁区行。
- 但「**非承重禁区**」仍有两条(契约/precondition 禁区,非 LB):
  - **禁区 A**:`:32-39` default_plan_resolution_dict wrapper + `:11-13` 其 import —— R22 precondition 硬约束,本批不碰。
  - **禁区 B**:`:50-156` 全部 LIVE 辅助函数 —— gantt_service 真依赖,整文件保留。

## 字段 10 — 分层 / 导入环风险

**零风险。** R21 是纯删除,不新增任何 import:
- 本文件现有 import 全部指向**同层** `core/services/scheduler/` 内的 sibling(`.schedule_result_view_context`/`.schedule_result_view_range`/`.gantt_range`/`.version_resolution`)+ `core.infrastructure.errors`(infra,合法下游)。删 shim 后剩余 import(ValidationError、_default_plan_resolution_dict、_get_plan_time_span_dates、resolve_schedule_result_week_range、attach_plan_metadata、WeekRange、VersionResolution)均被 LIVE 函数继续使用,无悬空 import。
- 删除不引入跨层 import,不触发 `core.algorithms→core.services` / `core.models→core.services` 等违规边,**0 AST 违规**。
- 无导入环:删的是「本文件→收口点」单向别名,删后该单向边仅减少,不可能制造环。

## 字段 11 — 测试耦合(动手前先迁哪些测试)

**动手前无需迁移任何测试。**
- 本条删的 3 个 shim 零测试引用(字段 8 实证),删后无测试变红。
- 唯一相关测试 `tests/schedule/summary/test_schedule_result_view_context.py:294-301`(`test_gantt_plan_query_wrapper_keeps_legacy_bad_role_message`)只测 R21 **保留**的 dpr_dict wrapper,R21 落地后它**继续通过**,无需迁移 —— 其去留是 R22 的责任(R22 处置 wrapper 时若删 wrapper 才需同步退该测试)。
- 收尾验证建议:删后跑 `pytest tests/schedule/summary/test_schedule_result_view_context.py -q` 应全绿(证明未误伤 wrapper 契约),并 `python -c "import core.services.scheduler.gantt_plan_query"` 验证无 import 残缺。

## 字段 12 — 前置依赖

- **R22 先行(硬序)**:R22 决定 dpr_dict wrapper 去留 → R21 在其后只删另 3 shim,确保不与 R22 抢同一符号家族。实操上 R21 可与 R22 **同批串行**(R22 收口完成后 R21 删 shim),只要 R21 严守「保留 :32-39」即可不必真等 R22 完成。
- **R44 同批(软序)**:R44 收口前若仍要照搬本文件 re-export 范式,需在 R21 删 `selected_plan_role` shim 前确认 R44 已改为直接 re-export core;非硬阻塞,二者宜同批 Batch-4 内协调。
- **F 门**:无专属架构门;仅需常规「分层 AST 检查 + 全量 import 自检」绿灯。
- **批次归属**:Batch-4(B02 身份族),与 R22/R44 同批。内部次序:**R22 收口(需 dpr_dict 在)→ R21 删 3 死 shim + 清 3 import → R44 协调 selected_plan_role 直 re-export core**。
- co_change:R22、R44。owner_pending=false,可直接进入执行批次。

## 对抗核验(第二双眼睛)

核验日 2026-06-05,独立重 grep,默认怀疑,找反证。**结论:PASS,零争议,无承重误删风险。**

**1. 行号——逐字命中,零漂移。**
`rg -n "^def ..." gantt_plan_query.py` → `default_plan_resolution_dict@32`、`resolve_plan@42`、`selected_plan_role@46`、`get_version_time_span_dates@50`、`_has_explicit_gantt_range@59`、`resolve_gantt_range_for_version@74`、`_empty_message_for_out_of_range@98`、`attach_gantt_range_metadata@113`、`build_empty_week_plan_payload@135`。Read :1-71 复核:三 shim 确为纯透传(:43/:47/:66 各 `return _xxx(...)` 无改写);import :14-25 在位。档案 location_status「✅准」属实。

**2. 生产消费者——零(死 shim),挑战 callgraph 假阳性成功。**
`rg -n "from .*gantt_plan_query import|gantt_plan_query\." core/ web/ data/ tests/` 全仓仅 3 处命中:① `gantt_service.py:12` Read 复核确认只取 4 个 LIVE(:13-16 attach_gantt_range_metadata/build_empty_week_plan_payload/get_version_time_span_dates/resolve_gantt_range_for_version),**不含任一死 shim**;② `tests/schedule/summary/test_schedule_result_view_context.py:295/298` 只调 `default_plan_resolution_dict("bad")`(R21 保留的 wrapper)。
对 `resolve_plan` 裸符号全仓回扫:命中均为 `SchedulePlanQueryService.resolve_plan` **实例方法**(schedule_plan_query_service.py:105 定义)及 `service.resolve_plan(...)` 测试调用 / web `getattr(plan_query_service,"resolve_plan")` —— 与 gantt_plan_query.py:42 模块级函数**同名不同物**,源码逐条否决 callgraph 假阳性,档案判断成立。`selected_plan_role` 唯一 import 源 `gantt_critical_chain_provider.py:21` 导自 `schedule_result_view_context`,非本文件 shim——印证档案字段 8。

**3. 收口点真实存在(非新建)。**
`schedule_result_view_context.py:192 def resolve_plan` / `:201 def selected_plan_role`、`schedule_result_view_range.py:29 def has_explicit_display_range` 均为已存在真身。R21 纯删空壳、不新建第二模块,符合铁律 5。

**4. 删后无悬空 import / 无误删共享符号。**
`rg -c` 复核:三待清 import 符号 `_resolve_plan`/`_selected_plan_role`/`_has_explicit_display_range` 各仅 2 次出现(import 行 + 对应 shim 体),删 shim 后归零,可安全连带清 import,无别处使用。保留的 7 个 import 符号(`_default_plan_resolution_dict`、`_get_plan_time_span_dates`、`resolve_schedule_result_week_range`、`attach_plan_metadata`、`WeekRange`、`VersionResolution`、`ValidationError`)计数均≥2,被 LIVE 函数继续引用,删 shim 不致悬空。

**5. 承重 / 灵魂线 / 分层——全清。**
registry `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger="none"`,非承重。修法为纯删透传空壳,**未混入统一/合并/透传/加形参**;保留的 dpr_dict wrapper(:32-39)是 loud raise(:38 抛 ValidationError),不动它即不违灵魂线;纯删除不引入任何 import,0 AST 违规、无导入环。owner_pending=false 处置正确,未发现擅自越权终态修法之外的问题。

**6. 续命测试隔离正确。** `tests/schedule/summary/test_schedule_result_view_context.py:294-301` Read 复核:仅 assert `default_plan_resolution_dict("bad")` 抛「未知的排产方案角色：bad」——只测 R21 **保留**的 wrapper,删 3 shim 后该测试继续通过,无需迁移。R22 硬序(保留 :32-39)、R44 软序均分析到位。

**核验命令留痕**:见上述 `rg -n "^def ..."`、`rg -n "from .*gantt_plan_query import|gantt_plan_query\."`、`rg -n "\bresolve_plan\b|\b_has_explicit_gantt_range\b"`、`rg -c "\b<sym>\b"`、各收口点 `^def` 回盘,及 Read gantt_plan_query.py:1-71 / gantt_service.py:12-36 / 续命测试 :290-302。
