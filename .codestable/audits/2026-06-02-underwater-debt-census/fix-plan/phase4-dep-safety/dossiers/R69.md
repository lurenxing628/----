# R69 逐债档案 — `_op_seq` 两份 + 均静默归 0（其一在 execution persistence 护栏文件）

> 桶 B15 | 簇 ISOLATED | 严重度 low | 承重 false | owner_pending false | 状态 planned
> 本档只读不改、只产计划。所有断言附当前工作区 file:line 证据（2026-06-05 回盘）。

---

## 1) 当前真实 file:line（回盘）+ 漂移记录

按符号名 `_op_seq` 在当前工作区 rg 回盘（绝不照抄 old_location）：

| 符号 / 引用 | registry 给的行号 | 回盘真实行号 | 漂移 |
|---|---|---|---|
| `def _op_seq`（护栏文件） | persistence_guard.py:48 (old) / :49 (evidence) | `core/services/scheduler/run/schedule_execution_persistence_guard.py:49` | 与 evidence 一致；vs old_location +1 |
| `def _op_seq`（runtime_support） | schedule_input_runtime_support.py:19 | `core/services/scheduler/run/schedule_input_runtime_support.py:19` | 无漂移（准） |
| 护栏消费者 A `completed_seq=_op_seq(completed_op)` | blast 写 :143 | `schedule_execution_persistence_guard.py:206` | **+63 行大漂移** |
| 护栏消费者 B `_op_seq(op)<=completed_seq` | blast 写 :149 | `schedule_execution_persistence_guard.py:212` | **+63 行大漂移** |
| runtime_support 消费者 `seq=_op_seq(op)` | blast 写 :216 | `schedule_input_runtime_support.py:216` | 无漂移（准） |

漂移结论：函数定义行号基本准（±1）；**但 phase1_blast.prod_consumers 给的护栏消费者行号 :143/:149 已严重过期，真实为 :206/:212（+63 行）**。原因：persistence_guard.py 在本分支有 resource-dispatch 工作的大量改动，把消费函数 `_completed_downstream_rows` 整体下移了约 63 行。修法时必须用 :206/:212，不可信 blast 的 :143/:149。

两份函数体逐字节相同，已 Read 证实（:49-53 vs :19-23）：
```python
def _op_seq(op: Any) -> int:
    try:
        return int(getattr(op, "seq", 0) or 0)
    except (TypeError, ValueError):
        return 0
```

## 2) 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = planned`。
- 与 current_evidence_2026_06_05 一致：两份 `_op_seq` 仍逐字节相同、仍保留 `except (TypeError, ValueError): return 0` 静默归 0、未收口到 `schedule_input_contracts.py`（rg 该文件确认 `NONE`，无 `_op_seq`）、tests/ 无坏 seq raise 护栏测试（rg `_op_seq` 在 tests/ 无任何符号引用）。
- 文案纠偏（与 location_status 一致）：原报告若写裸 `except:return 0` 不准，**实际是 typed `except (TypeError, ValueError): return 0`**，已 Read 证实。坏 seq 仍静默归 0 这一病理成立。
- persistence_guard.py 对基线有 diff（属 resource-dispatch 工作），但 `_op_seq` 函数体未被改动，只是被整体下移。**状态判定 planned 准确，本债从未动手。**

## 3) 病理 / 严重度 / 桶 / 簇

- **病理**：P4 气味 + `in_guard_file`。`_op_seq` 两份逐字节相同（重复定义债），且**两份均把坏 seq 静默归 0**——其中一份位于 execution persistence 护栏文件（`schedule_execution_persistence_guard.py`），护栏文件内吞错触灵魂线红区（静默兜底 / except 吞错）。
- **严重度**：low。理由：(a) 正常路径下 `BatchOperation.seq: int = 0` 已是 typed int，且上游 `parse_int_or_default(seq, 0, field="seq")`（batch_operation.py:81）已清洗，正常情况 except 分支不触发；(b) 下游污染面有限（仅 revision 校验过滤）。但护栏文件 + 灵魂线红区使其不可纯当重复债直删了事。
- **桶**：B15（home=B15）。
- **簇**：ISOLATED。same_file_siblings = []，interference_edges = []。本债无同文件兄弟、无同符号干扰边。

## 4) 修法类

**收口到 X（单一 `_op_seq`，主链共同上游）+ 护栏文件内评估坏 seq 改 loud / 补可观测，严禁保留静默归 0。**

- owner_pending = false → 可给终态修法方向，但收口家具体落点仍标注「设计前置 = owner 裁断收口家 + loud 方向」（见 §12，registry planned_deps_hint 明示）。
- **收口点（必须是已存在点，铁律#5）**：推荐 `core/services/scheduler/run/schedule_input_contracts.py`。该文件**已存在**，非新建模块——不触发「为已有概念新建第二模块 = 新 P5」。runtime_support 已 `from .schedule_input_contracts import _build_freeze_window_seed_with_meta`（runtime_support.py:9），是其既有低层依赖；persistence_guard 当前未 import 它（rg 确认 `NO IMPORT`），需新增一条 `persistence_guard → schedule_input_contracts` 边——已核为无环（见 §10）。
- **收口动作**：把单一 `_op_seq` 提到 `schedule_input_contracts.py`，两文件各删本地副本改为 `from .schedule_input_contracts import _op_seq`。
- **灵魂线（铁律#4）**：收口后的单点**不得沿用 `except (TypeError, ValueError): return 0`**。P4 必须二选一：
  1. 改 loud raise（坏 seq → `raise AppError(...)`，护栏文件内坏 seq 更应暴露而非归 0）；或
  2. 若 owner 判定坏 seq 在非护栏路径需容忍，则保留归 0 但**补可观测降级标记**（结构化 log / counter），绝不静默。
- **不写**：删除（两份非纯冗余，承重虽 false 但触护栏 + 灵魂线，不可直删了事）、统一后仍静默、加形参。

## 5) 同文件碰撞债

**无。** same_file_siblings = []，interference_edges = []（registry 证实）。

- persistence_guard.py 虽在本分支有 resource-dispatch diff，但该 diff 不属于任何登记债；本债是该文件内唯一登记债。
- runtime_support.py 内无其他登记债共改 `_op_seq` 附近。
- 不会撞行号 / dict 键位移（无并发改同段）。唯一已发生的行号位移来自分支既有 resource-dispatch 改动（已在 §1 记录，非债间碰撞）。

## 6) 同收口点债

**无同收口点债。** 收口家 `schedule_input_contracts.py` 当前无 `_op_seq`（rg 确认 `NONE`），也无其他登记债声明收口到该文件的同一符号。

- 顺序敏感性：本债是收口到 `schedule_input_contracts.py` 的首个 `_op_seq` 落点，不与他债争用同符号。
- 批次内 co_change_within_group = [R68, R70]：仅**同批不同文件**（registry planned_deps_hint「与 R68/R70 仅同批不同文件」「无跨桶同文件边」），非同收口点，无顺序硬依赖。本桶第三落（R70、R68 之后）的排序来自 owner 需先定收口家 / loud 方向，非技术耦合。

## 7) 收口行为差异 + parity 测试

收口前两路逐字节相同（§1 证实），**纯收口（不改 except）= 行为零差异**，无需 parity——但本债的修法核心恰是「收口同时改 loud / 补可观测」，故差异出在 except 分支：

- **正常路径（`op.seq` 为合法 int 或 None→0）**：新旧逐分支等价。`int(getattr(op, "seq", 0) or 0)` 对 `seq=0/None/合法 int` 行为不变。**此分支无需 parity 担忧。**
- **边界分支（`op.seq` 触发 TypeError/ValueError，如 `seq="abc"` 或非数 duck-typed 对象）**：
  - 旧：静默 `return 0` → 进入 :207 `completed_seq<=0 短路 return`（护栏直接放空过滤）/ :212 比较被污染 → **漏判后继工序需重算**（downstream_effect 证实）。
  - 新（loud）：`raise` → 调用链上抛，护栏 loud 失败而非静默漏判。
  - **反例（必须建 parity / 契约测试）**：构造 `op.seq = "x"`（或 `seq` 为不可 int 化对象）→ 断言新路 `raise`（或断言记录降级标记），断言旧路的「静默归 0 → 漏判」不再发生。
- **所需测试（tests/ 当前为零，见 §11）**：
  1. 坏 seq 触发 loud raise（或可观测降级标记）的护栏测试 —— 钉灵魂线方向。
  2. 收口单点 import 契约测试：断言两文件均 `from .schedule_input_contracts import _op_seq`，不存在第二份本地定义（防回归再裂两份）。
  3. 正常 seq 的 revision 过滤行为（:206-216 `_completed_downstream_rows`）收口前后逐分支等价。

## 8) 爆炸半径

**生产消费者清单（回盘真实行号）：**
- `schedule_execution_persistence_guard.py:206` — `completed_seq = _op_seq(completed_op)`（在 `_completed_downstream_rows`，blast 旧值 :143 已漂移 +63）
- `schedule_execution_persistence_guard.py:212` — `_op_batch_id(op) != completed_batch_id or _op_seq(op) <= completed_seq`（revision 校验过滤，blast 旧值 :149 已漂移 +63）
- `schedule_input_runtime_support.py:216` — `seq = _op_seq(op)`（在 `_resolve_algo_ops_to_schedule`，:217 `seq > completed_seq` 过滤待重排工序）

**测试消费者：** 无直接 `_op_seq` 符号引用（rg tests/ 确认）。仅文件名被两处文件清单门禁列入：
- `tests/regression_aps_three_gap_docs_quality_gate.py:132`（blast 写 :126，漂移 +6）—— 列 persistence_guard.py 路径；收口只动函数体、不挪/删该文件 → **不会红**。
- `tests/test_sp05_path_topology_contract.py:84/87/318` —— `SERVICE_ROOTS_WITHOUT_COMPAT` 列**旧根** `core/services/scheduler/schedule_input_runtime_support.py`（注意是 `scheduler/` 根下旧路径，非 `run/` 子包），断言旧根不存在；收口落点在 `run/` 包内既有文件、不在旧根建文件 → **不会红**。

**灾难链（删错 / 统一错会静默炸什么）：**
- 工序排序键 `seq`。坏 seq 静默归 0 → 护栏 :207 `completed_seq<=0` 短路 return（直接放空 downstream 集合）+ :212 `_op_seq(op)<=completed_seq` 比较被污染 → **可能漏判后继工序需重算 revision**，护栏静默失效（属护栏文件内静默兜底，灵魂线红区）。
- runtime_support :216-217 路径：坏 seq 归 0 → `seq > completed_seq` 恒假 → 该 op 静默不进重排集合 → **算法侧漏排工序**。
- 若收口时沿用静默 return 0 = 违灵魂线红线（护栏文件内尤甚，fix_invalidation_risk 证实）。
- **无跨债依赖**（blast 明示）。

## 9) 承重毗邻标记

- **lb_no_touch = null，load_bearing = false** —— 本债无承重禁区行。
- lb_colocation_danger = `none`：persistence_guard.py **不在 PHASE0 §5 承重清单**，无同文件 LB 毗邻。
- **但**它是 execution persistence 护栏文件：收口时 except 分支应评估改 loud（护栏文件内坏 seq 更应暴露而非归 0），**不得在合并后保留静默兜底**。这是灵魂线约束（§4），非承重禁区约束——故修法不受「仅注释」限制，可正常改 except → loud / 补可观测。

## 10) 分层 / 导入环风险

- 收口新增边 `schedule_execution_persistence_guard.py → schedule_input_contracts.py`（同包 `core/services/scheduler/run/`，同层）。
- **无环（已 rg 核）**：`schedule_input_contracts.py` 只 import `core.services.common.build_outcome`（contracts.py:7），**无任何 `persistence_guard` / `runtime_support` 回指**（rg 确认 `NONE - no back-ref`）。故新增边不成环。
- runtime_support 既有边 `→ schedule_input_contracts`（runtime_support.py:9）本就存在，复用同收口点不引入新越层。
- **0 跨层 import**：收口落点与两消费者同属 `core.services.scheduler.run`，不触发 `core.algorithms→core.services` / `core.models→core.services` 等分层红线。

## 11) 测试耦合

- **动手前需迁移的现有测试：无**（tests/ 当前无任何 `_op_seq` 护栏 / 坏 seq raise 测试，rg 确认）。
- 两处文件清单门禁（gate :132、topology :84/87/318）按 §8 分析收口后**均不红**，无需迁移，仅在收口若误挪文件路径时才会触发——属护栏，不需预迁。
- **需新增测试**（见 §7）：坏 seq loud/降级护栏测试 + 单点 import 契约测试 + revision 过滤 parity。这些是修法的一部分（钉灵魂线 + 防再裂两份），非「先迁后改」的前置迁移。

## 12) 前置依赖

- **债依赖：无前置 / 后置债**（blast「无跨债依赖」；planned_deps_hint「无前置/后置债依赖」）。与 R68 / R70 仅同批 B15、不同文件，无技术顺序硬依赖。
- **F 门：无硬 F 门前置**。收口落点在 `run/` 包内既有文件，不触碰 SP05 路径拓扑契约（topology 门禁列的是旧根，§8/§10 已核不红）。
- **唯一设计前置 = owner 裁断**（registry planned_deps_hint / open_questions）：
  1. 收口家落点确认（推荐 `schedule_input_contracts.py`，已核无环）；
  2. **loud 方向**：坏 seq → loud raise vs 保留归 0 + 可观测降级标记。鉴于 `seq` 上游已 `parse_int_or_default` 清洗（batch_operation.py:81），except 正常不触发，倾向 loud raise；但护栏 vs 算法两路是否需差异化处理由 owner 定。
- **批次排序**：Batch-15 本桶第三落（R70、R68 之后），排序来自「owner 需先定收口家 + loud 方向」，非技术阻塞。

---

## 对抗核验（第二双眼睛）— 2026-06-05

独立 rg/grep 回盘，默认怀疑、找反证。命令与结果：

- `rg -n "def _op_seq" <两文件>` → `runtime_support.py:19` / `persistence_guard.py:49`。**与档案 §1 一致（±1 vs old_location）。**
- `rg -n "_op_seq" <两文件>` → 消费者 `persistence_guard.py:206`（`completed_seq=_op_seq(completed_op)`）、`:212`（`_op_seq(op)<=completed_seq`）、`runtime_support.py:216`（`seq=_op_seq(op)`）。**全部坐实档案回盘值；确认 blast.prod_consumers :143/:149 已过期 +63 行，档案用 :206/:212 处理正确。**
- `rg -n "_op_seq" core/ web/ tests/ data/` → 仅上述两文件 5 处，**tests/ 零符号引用，确认**。
- `sed -n` 读两份函数体 :49-53 / :19-23 → 逐字节相同，`except (TypeError, ValueError): return 0`。**确认 typed except（非裸 except），档案文案纠偏正确。**
- 护栏上下文 :205-212 → `if not completed_batch_id or completed_seq <= 0: return out`（:207）+ `_op_seq(op) <= completed_seq: continue`（:212）；runtime_support :217 → `seq > completed_seq`。**灾难链（坏 seq 归 0 → :207 短路放空 / :212 污染过滤 / :217 恒假漏排）成立。**
- 收口点：`ls` 确认 `schedule_input_contracts.py` 已存在（非新建，不触铁律#5）；`rg "^(from|import)"` → 仅 `core.services.common.build_outcome`；`rg "persistence_guard|runtime_support"` → **NO back-ref，新增 guard→contracts 边无环，确认 §10**。`rg "schedule_input_contracts"` 两消费文件 → runtime_support 已 import（:9）、guard `NO IMPORT`，**与档案一致**。
- 门禁回盘：`tests/regression_aps_three_gap_docs_quality_gate.py:132`（档案/blast 写 :126，**漂移 +6，档案 §8 已修正为 :132**）；`tests/test_sp05_path_topology_contract.py:87` 列旧根 `core/services/scheduler/schedule_input_runtime_support.py`（非 run/ 子包），**收口落点在 run/ 既有文件 → 不红，确认**。
- 上游清洗：`batch_operation.py:24 seq: int = 0`（typed）、`:81 parse_int_or_default(seq, 0, field="seq")`。**支撑「正常路径 except 不触发」「severity low」判定，确认。**

**承重核查**：load_bearing=false、lb_no_touch=null、lb_colocation_danger=none。修法明确「不写删除 / 统一后仍静默 / 加形参」，**未把护栏当无害冗余直删，无承重误删风险**。
**灵魂线核查**：P4 修法为「loud raise 或补可观测降级标记，严禁保留静默归 0」，**无新增兜底 / 静默回退 / except 吞错**，合规。
**owner_pending=false 核查**：给了终态修法方向，但收口家落点 + loud 方向标为 owner 设计前置、标 Batch-15 第三落，**未越权硬分配执行批次**，合规。
**分层核查**：收口落点与两消费者同属 `core.services.scheduler.run`，0 跨层 import，不触 algorithms/models→services 红线。

**核验结论：PASS。** 行号、消费者清单、承重/分层/无环/收口点存在性、上游清洗、门禁不红——逐项找反证均未成立。唯一漂移（blast :143/:149 → :206/:212；gate :126→:132）档案已自爆并用回盘真值修正。
