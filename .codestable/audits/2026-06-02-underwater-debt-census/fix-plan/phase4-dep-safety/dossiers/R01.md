# R01 逐债档案 — count/has_actionable_schedule_rows 死函数链

> 本档案只读不改，只产计划。所有断言附 2026-06-05 当前工作区 grep/callgraph 回盘证据。
> 债 id: R01 | 桶 B14 | 簇 C01 | 严重度 medium | 病理 P6 | 承重 false | owner_pending false
> 主文件: core/services/scheduler/run/schedule_payload_contract.py

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

按符号名 rg 回盘（绝不照抄 old_location）。死函数链共 5 站点 + SP05：

| 符号 | old_location_DONT_TRUST | 当前真实 file:line | 漂移 |
|---|---|---|---|
| `count_actionable_schedule_rows` | run/schedule_payload_contract.py:90-95 | core/services/scheduler/run/schedule_payload_contract.py:**90**（body :91） | 无漂移（:90 命中） |
| `has_actionable_schedule_rows` | （同上块 90-95） | …schedule_payload_contract.py:**94**（body :95 内调 count） | 无漂移 |
| `_iter_actionable_results`（唯一私有依赖） | 未单列 | …schedule_payload_contract.py:**67**（body 67-87） | — |
| `__all__` 两条目 | 未单列 | …schedule_payload_contract.py:**414-415**（__all__ 块 410-416） | 无漂移 |
| run 层 re-export（import 块内） | run/schedule_persistence.py:16-17 | core/services/scheduler/run/schedule_persistence.py:**16-17**（import 块 12-18） | 无漂移 |
| scheduler 层 re-export | scheduler/schedule_persistence.py:3,5 | core/services/scheduler/schedule_persistence.py:**3**（import）+ **5**（__all__） | 无漂移 |
| SP05 拓扑断言 | test_sp05:54-55 | tests/gate_meta/test_sp05_path_topology_contract.py:**54-55**（模块键 :53 `core.services.scheduler.schedule_persistence`，`count`=:54、`has`=:55、`persist_schedule`=:56 同块） | **无漂移** |

**总体漂移结论：全部 6 处（5 生产站点 + SP05）零漂移。registry current_evidence_2026_06_05 与 awk 行锚回盘逐字吻合：payload_contract :67/:90/:94/:414-415、run :16-17、scheduler :3/:5、SP05 :54-55（模块键 :53）。`location_status: ✅准` 完全成立。**（核验过程曾用 `sed -n` 滑窗误读 SP05 为 :56-57，经 `awk NR` 行锚复核纠正为 :54-55 = registry 原值。）**

证据：
- payload_contract: `rg -n` → :67 `_iter_actionable_results`、:90 `count`、:91 `return sum(... _iter_actionable_results ...)`、:94 `has`、:95 `return count_actionable_schedule_rows(...) > 0`、:414-415 `__all__`。
- 闭合死环（callgraph edges.json 回盘）：inbound to `has`=**0**；inbound to `count`=**1**（仅 `has`→count，kind=local，:95）；inbound to `_iter`=**1**（仅 `count`→_iter，:91）。三者构成自封闭环，外部零边。
- run re-export: run/schedule_persistence.py:12-18 import 块，:13-15 = ValidatedSchedulePayload/ValidatedScheduleRow/build_validated_schedule_payload（**保留**），:16-17 = 两死符号（删），:18 `)`。
- scheduler re-export: schedule_persistence.py:3 `from .run.schedule_persistence import count_actionable_schedule_rows, has_actionable_schedule_rows, persist_schedule`；:5 `__all__ = ["count_actionable_schedule_rows", "has_actionable_schedule_rows", "persist_schedule"]`。

## 字段 2：当前状态 + 与 status evidence 是否一致

- status_2026_06_05 = **planned**。
- 与 evidence 一致性：**完全一致，原样死码未动**。registry 述「计划的 5 站点 + SP05 同提交删除完全未动」——回盘逐项核实 5 站点全部仍在（payload_contract :67/:90/:94/:414-415、run :16-17、scheduler :3/:5），SP05 两符号断言仍在（:56-57）。无任何已实施迹象 → 状态确为 planned，无 in_progress 漂移。

## 字段 3：病理 / 严重度 / 桶 / 簇

- **病理 P6（死代码 / 假活）**：schedule_service 拆分（commit 78879ec1）后，生产改走 `build_validated_schedule_payload` → `_validate_persist_inputs` 严格校验路径，老的计数/布尔函数失去全部生产消费方。靠「两层 re-export + __all__ + SP05 拓扑断言」三件套共同制造「在用」假象——SP05 断言公开符号存在 → 不敢删 → 死码续命。
- **严重度 medium**：纯死码，无生产行为风险；但占用公开表面、误导后人、与 R04 同文件形成行号互撞约束，故非 low。
- **桶 B14 / home B14**；**簇 C01**；partition = scheduler-run；kind = real（非幻觉债）；needs_adversarial=false、adv_refuted=false。
## 字段 4：修法类

**修法类 = 直删 + 退测试（delete + de-test）**。承重=false、owner_pending=false → 可给终态修法。

同一提交（不可拆）依次删除，**自下而上删以免行号位移自伤**：
1. payload_contract `__all__`：删 :414 `"count_actionable_schedule_rows",`、:415 `"has_actionable_schedule_rows",`（块 410-416，保留 ValidatedSchedulePayload/Row/build_/validate_payload_before_persist 四条）。
2. payload_contract 死簇：删 :67-87 `_iter_actionable_results` 整体 + :90-91 `count` + :94-95 `has`（含函数间空行，连续块 67-95）。三者闭合死环，整簇删。
3. run/schedule_persistence.py import 块：**只删 :16-17 两行**，保留 :12 `from ... import (`、:13-15 三个仍被消费符号、:18 `)`——import 块结构完整不破。
4. scheduler/schedule_persistence.py：改 :3 去掉 `count_actionable_schedule_rows, has_actionable_schedule_rows,`（保留 `persist_schedule`）；改 :5 `__all__` 去掉两符号（保留 `"persist_schedule"`）。
5. SP05 tests/gate_meta/test_sp05_path_topology_contract.py：删 :54 `"count_actionable_schedule_rows",`、:55 `"has_actionable_schedule_rows",`（保留 :53 模块键 `core.services.scheduler.schedule_persistence` + :56 `"persist_schedule"` + :57 `)`）。**registry :54-55 = 回盘真实行号，无漂移。**
6. **（本档案补充，registry planned_fix 漏列）** payload_contract :5 `from typing import ...` 删掉 `Iterator`——它仅 :67 `_iter` 签名用，删死簇后变孤儿 import，flake8 会红。`Tuple/Set/datetime/_strict_positive_int` 均有活用户须保留。

**本债不收口到任何点**（纯删，不是 P5 收口债）。无新建模块。
**灵魂线核对**：纯删除死码，不新增兜底/静默回退/except 吞错/形参；不触碰 loud raise 路径。✅ 合规。
**owner_pending=false** → 给出上述终态修法，可分配执行批次。

## 字段 5：同文件碰撞债（sibling）

唯一同文件 sibling = **R04**（B05，正整数/datetime 收口，落点 `_strict_positive_int` :50 + 6 调用点）。

- **R04 改哪段**：收口 `_strict_positive_int`（:50 定义）的行为，影响其 6 个调用点 :72/:149/:198/:308/:324/:372。
- **R01 改哪段**：删 `_iter`(67-87)/`count`(90-91)/`has`(94-95) 死簇 + __all__(414-415)。
- **重叠点（关键）**：R04 的调用点 **:72 正落在 R01 要删的 `_iter` body（67-87）内**。其余 5 个 R04 站点（:149/:198/:308/:324/:372）在 R01 删除区之外、活代码内，与 R01 零重叠。
- **谁先谁后 / 撞行号风险**：
  - 若 **R01 先删**：`_iter`(67-87) 连同 :72 调用点一起消失 → R04 的待收口点从 6 个降为 5 个，R04 须**重测真实剩余调用点**（否则按旧 6 点 :72/149/198/308/324/372 改会扑空一处）。R01 删 67-95 共约 29 行 → R04 后续所有 :149+ 行号**上移 ~29 行**，R04 必须 grep 重定位，不能照旧行号。
  - 若 **R04 先改**：R04 在 :72（_iter 内）若改了 `_strict_positive_int` 调用形态，R01 随后整段删 _iter，R04 在 :72 的改动白做；且 R04 改动若导致 :90+ 行号位移，R01 的 __all__ 行号也漂。
  - **结论**：二者**无业务前后置顺序**（registry：「无前后置顺序要求，仅共居约束」），但**强行号互撞**。必须 **同批次（Batch-6）原子提交**，或显式串行 + 串行方每步重 grep 回盘行号。**推荐 R01 先删**（缩小 R04 的收口面，让 R04 只面对 5 个真活站点），R04 在 R01 之后 rg 重定位剩余 5 点再收口。

## 字段 6：同收口点债

- **R01 不收口到任何符号**（纯删），故「收口到同一符号的其他债」对 R01 不直接适用。
- 但 R01 删除的 `_iter`(:72) **消费了 R04 的收口标的 `_strict_positive_int`**。这是「R01 删除面 ∩ R04 收口面」的交集：R01 删 :72 = 替 R04 减少一个收口分支。顺序敏感性见字段 5（推荐 R01 先，R04 后）。
- `__all__` 同符号干扰边：registry interference_edges 标 R19、R46 与 R01 `sym:__all__` 同符号。但那是**不同文件的各自 __all__**（R01 动的是 schedule_payload_contract.py:414-415 与 scheduler/schedule_persistence.py:5 的 __all__；R19/R46 在别的模块）。**非同一 __all__ 列表**，无真实行号互撞，仅「都在改某个 __all__」的弱标签。无顺序敏感。
## 字段 7：收口行为差异及所需 parity 测试

**本债是纯删，非收口**，故「新旧两路对边界值是否逐分支等价」**不适用**——不存在「新路」，只有「无路」。

但为防误删，须有**死活证明 parity（删除安全性证明）**而非行为 parity：
- 已用 callgraph edges.json 证：inbound to `has`=0、inbound to `count`=1（仅 has）、inbound to `_iter`=1（仅 count）→ 闭合自环，删除不改变任何外部可观测行为。
- 已用 `rg` 全仓证：core/web/data 除 payload_contract 自身 + 两层 re-export plumbing 外，零调用点。
- **删除后必须跑的回归门**：(a) SP05 拓扑契约测试本身（删函数+删断言同提交后应转绿，否则红=漏删一处）；(b) 全量 import smoke（确认 run/scheduler 两 schedule_persistence 模块仍可 import，build_validated_schedule_payload/persist_schedule 仍导出）；(c) AST 分层门（确认未引入越层 import）。
- **反例边界（说明为何无需行为 parity）**：`count` 对空列表返回 0、对全坏行返回 0、`has` 据此返回 False——这些边界**无任何生产代码观察**，删除不产生 None-vs-raise 类语义差异，因为没有调用方接收返回值。

## 字段 8：爆炸半径

- **生产消费者清单**：**零**（已 grep + callgraph 双证）。唯一「引用」是：
  - `has`(:95) 内部调 `count`(:90)、`count`(:91) 内部调 `_iter`(:67)——闭合自环，随整簇一起删。
  - run/schedule_persistence.py:16-17 re-export（删）。
  - scheduler/schedule_persistence.py:3,5 re-export（改）。
  以上全是「为撑起公开符号假象」的 plumbing，无真实业务消费。
- **测试消费者**：仅 `tests/gate_meta/test_sp05_path_topology_contract.py:54-55`（SERVICE_BEHAVIOR_COMPAT_PUBLIC_SYMBOLS 断言 `schedule_persistence` 公开两符号存在）。
- **灾难链（删错/漏删会静默炸什么）**：
  - **漏删 SP05:54-55 而删了函数** → SP05 红（断言公开符号缺失）。这是**响亮失败**（好），非静默——拓扑契约正是干这个的。
  - **漏删 scheduler/schedule_persistence.py:3 的符号而删了 run 层 import** → `ImportError` at module load，响亮。
  - **误删 run import 块 :13-15（ValidatedSchedulePayload/Row/build_）** → build_validated_schedule_payload 等生产路径 `ImportError`，**这是真正的爆炸点**：必须严守「只删 :16-17」。
  - **误删 :18 `)` 或破坏括号结构** → SyntaxError。
  - 整体：因 0 生产消费方，**不存在静默业务损坏路径**；所有误删都以 ImportError/SyntaxError/测试红 形式响亮暴露。爆炸半径极小，medium 严重度主要来自「与 R04 同文件行号互撞」的操作风险，而非业务风险。

## 字段 9：承重毗邻标记

- **本债承重 load_bearing=false，lb_no_touch=null**。
- **同文件有无 LB**：schedule_payload_contract.py 整体 `lb_colocation_danger: none`（registry phase1_blast）——非承重文件，sibling R04 亦非承重。**无禁区行**。
- 故修法可正常直删，**无需「我是故意的」注释 + 契约/parity 测试**那套承重保护（那是承重债专属）。但仍建议在提交信息说明「死簇整体删除，callgraph 证零外部边」以留审计痕。
## 字段 10：分层 / 导入环风险

- **纯删，不新增任何 import** → **不可能引入越层 import 或导入环**。分层红线（0 AST 违规）自动满足。
- 反向核查删除是否产生**孤儿 import**（flake8 unused-import）：
  - `Iterator`（typing :5）**仅** :67 `_iter` 签名使用 → 删死簇后变孤儿。**必须同步从 :5 `from typing import ...` 删掉 `Iterator`**（这是修法的隐藏第 6 步，registry planned_fix 未点明，本档案补充）。
  - `Tuple`/`Set`/`Optional`/`List`/`Any`/`Dict`/`cast`：均有 :144+/:28+ 等多处活用户，**保留**。
  - `_strict_positive_int`（:50）：除被删的 :72 外，还有 :149/:198/:308/:324/:372 共 5 个活调用点（rg -c=7：1 定义 + 1 待删 :72 + 5 活）→ **保留，不孤儿**。
  - `datetime`（:4）：除 :81/:82（_iter 内）外还有其它活用户（rg -c=6）→ **保留**。
- run/scheduler 两 schedule_persistence 仍互相 import build_validated_schedule_payload/persist_schedule，删两死符号不破坏既有 run→scheduler 依赖方向。无环。

## 字段 11：测试耦合

- **动手前先迁的测试**：仅 `tests/gate_meta/test_sp05_path_topology_contract.py:54-55`。它是**拓扑契约测试**（断言公开符号清单），不是行为测试——「迁」即同提交删除这两行断言（见字段 4 步 5）。无 fixture/setup 需搬。
- 无其它测试引用这两符号（`rg -ln ... tests` 仅命中 SP05 一个文件）。
- 删函数与删 SP05 断言**必须同一提交**：先删断言后删函数会让中间提交 SP05 红；先删函数后删断言同样中间红。原子提交规避。
- 同文件 sibling 测试 R26/R43 也在 test_sp05_path_topology_contract.py（registry interference_edges file:same）——它们改 SP05 的**其它模块键条目**，与 R01 改的 :53-56 `schedule_persistence` 块**不同区域**，但**同文件 → 行号互撞**：若 R26/R43 改动在 :53 之前的行会令 R01 的 :54-55 漂移。R01 改 SP05 时须 grep 重定位 `schedule_persistence` 模块键当前行，不照旧 :54-55。

## 字段 12：前置依赖

- **无前置债**：R01 是纯删死码，不依赖任何其它债先完成。registry：与 R04「无前后置顺序要求，仅共居约束」。
- **共居协调（非前置，但同批次必须）**：与 R04 同批（planned Batch-6）或显式串行。**推荐执行顺序：R01 先删 → R04 后收口**（R01 删掉 :72 后 R04 收口面从 6→5 点，且 R04 须按 R01 删后的新行号 rg 重定位）。这是「同批内排序建议」而非「跨债前置」。
- **F 门（前置门禁）**：
  - 无需等全局根 B01（registry：「独立于 B01 全局根」）。
  - 执行后需过：SP05 拓扑契约门、import smoke 门、AST 分层门（0 违规）。
  - 与 R19/R46 的 `sym:__all__` 弱边为不同文件，不构成前置。
- **owner_pending=false** → 可直接进 Batch-6 排期，无需 owner 裁断。

---

## 对抗核验（第二双眼睛）

核验日期 2026-06-05。只读不改，独立重 grep 回盘，默认怀疑。

**重 grep 命令与结果（全部 awk 行锚实证，不照抄 registry）：**
- `rg -n "_iter_actionable_results|count_actionable_schedule_rows|has_actionable_schedule_rows" core/services/scheduler/run/schedule_payload_contract.py` → :67 `_iter`、:90/:91 `count`、:94/:95 `has`、:414-415 `__all__`。**与档案表格一致。**
- `awk 405-420` payload_contract `__all__`：块 410-417，两死条目确在 :414-415，保留 ValidatedSchedulePayload/Row/build_/validate_payload_before_persist 四条。**一致。**
- `awk 10-20` run/schedule_persistence.py：import 块 :12-18，两死符号确在 :16-17，:13-15=三活符号（保留），:18=`)`。**一致。** 另发现 :19-20 第二个 import 块 `validate_payload_before_persist as _validate_payload_before_persist`，**与 :16-17 删除无关，不受影响**（档案未提及但无碍）。
- `awk 1-10` scheduler/schedule_persistence.py：:3 三符号 import、:5 `__all__` 三符号。**一致。**
- `awk 53-57` SP05：模块键 `core.services.scheduler.schedule_persistence` 在 :53，`count`=:54、`has`=:55、`persist_schedule`=:56、`)`=:57。**:54-55 经 awk 行锚实证为真实行号，与档案表格/修法步骤一致。**

**行号复核结论：一致。** 6 处站点（payload_contract :67/:90/:94/:414-415、run :16-17、scheduler :3/:5、SP05 :54-55）零漂移，逐字吻合。

**承重判断复核：** load_bearing=false、lb_no_touch=null，registry `lb_colocation_danger: none` 实证为非承重文件。直删修法合规，无承重误删风险。

**零消费复核：** `rg -n "count_actionable_schedule_rows|has_actionable_schedule_rows" --type py | grep -v {两 persistence 文件} | grep -v test_sp05` → **空**。除已知 plumbing（两层 re-export + 自封闭死环）外，core/web/data 零调用点。死码判断成立。

**孤儿 import 复核（档案隐藏第 6 步）：** `rg -n "Iterator" payload_contract` → 仅 :5 import + :67 `_iter` 签名两处。删死簇后 `Iterator` 确为孤儿，**必须同删 :5 的 `Iterator`**——档案补充正确。`datetime`(:4) 在 :80 _iter 内用但他处有活用户，保留不孤儿；`_strict_positive_int` 除 :72 外另有 5 活点，保留。**复核通过。**

**死环自封闭复核：** `awk 67-96` 实证 `_iter`(:67-87 yield) ← `count`(:91 调 _iter) ← `has`(:95 调 count)，闭合三角，无外部入边。**成立。**

**sibling R04 协调复核：** `awk 67-96` 实证 R04 收口标的 `_strict_positive_int` 的调用点确在 :72（_iter body 内）。R01 删 _iter 后 R04 收口面 6→5，强行号互撞、须同批/串行重 grep 的判断**成立**。

**分层/导入环复核：** 纯删不新增 import，0 AST 违规自动满足；删两死符号不破坏 run→scheduler 既有依赖方向，无环。**成立。**

**唯一瑕疵（不影响行号，不影响修法）：** 档案字段 2（line 34）、字段 5（line 50）正文残留 ":56-57" 笔误描述 SP05 两符号位置，而字段 1 表格、总结、字段 4 修法步骤 5 用的都是正确的 :54-55（=awk 实证值）。这是档案内部措辞前后不齐，**真实行号判断与修法均正确**，建议落地前把字段 2/5 那两处 ":56-57" 措辞订正为 ":54-55" 以免误导执行者。

**核验裁定：PASS。** 行号一致、承重判断准、零消费/孤儿/死环/分层全部独立复现，无承重误删风险，无需第三方裁断。仅 1 处文字笔误需订正（非行号错误）。
