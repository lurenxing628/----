# LB07 逐债档案 — ScheduleConfigSnapshot 配置快照/校正引擎整栈双实现(承重,锁步手工同步)

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：本 dossier 引用的 `tests/regression_scheduler_candidate_py38_contract.py` 已由 A 的 **P1.1 删除**；py38 语法保护现由仓库级全树扫描 `tools/scan_py38plus_syntax.py`（+ `tests/gate_meta/test_scan_py38plus_syntax.py`）承载，覆盖面 ≥ 原 2-文件硬清单。涉及它的 py38 契约锚点一律改指该全局扫描器。详见 `ANCHOR-DRIFT-2026-06-05-POSTCOMMIT.md §六.3`。


> bucket B03 | cluster C01 | severity medium | pathology P3 | load_bearing=true | owner_pending=true
> 同文件兄弟债: R47, R71 | planned_batch_hint: Batch-1 全局 ROOT,本桶根,必须最先落
> 本档只读不改、只产计划。承重铁律:修法仅「补『我是故意的』中文注释 + 扩 parity 契约」,零删除/统一/透传。

---

## 字段 1 — 当前真实 file:line(grep 回盘 + 漂移记录)
LB07 是「整组双栈」债,不是单符号债。回盘以**两个 @dataclass 本体 + 三个承重收口符号**为锚:

| 符号 / 锚点 | old_location 线索 | 当前真实 file:line | 漂移 |
|---|---|---|---|
| 模型栈 `ScheduleConfigSnapshot` (@dataclass) | snapshot.py:7 | `core/models/schedule_config_runtime_snapshot.py:7` @dataclass / `:8` class | 无 |
| 服务栈 `ScheduleConfigSnapshot` (@dataclass) | config/config_snapshot.py:24 | `core/services/scheduler/config/config_snapshot.py:24` @dataclass / `:25` class | 无 |
| 模型栈 `ensure_schedule_config_snapshot` | coercion.py:409 | `core/models/schedule_config_runtime_coercion.py:409` | 无 |
| 服务栈 `ensure_schedule_config_snapshot` | config_snapshot.py:336 | `core/services/scheduler/config/config_snapshot.py:336` | 无 |
| 模型栈 `normalize_weight_triplet` | (call @ schedule_params.py:124,138) | def @ `core/models/schedule_config_runtime_weights.py:48` | 无(registry 给的是调用点,非定义点) |
| 服务栈 `normalize_weight_triplet` | — | def @ `core/services/scheduler/config/config_weight_policy.py:57` | 无 |
| 模型栈 `coerce_runtime_config_field` | coercion.py:484 | `core/models/schedule_config_runtime_coercion.py:484` | 无 |
| 服务栈 `coerce_runtime_config_field` | config_snapshot.py:312 | `core/services/scheduler/config/config_snapshot.py:312` | 无 |

**字段计数回盘**:两栈 class 体均为 **30 个字段注解**(17 必填 + `graph_*` 11 + `auto_assign_persist` + `degradation_events`/`degradation_counters` 2 个 `field(...,repr=False)`)。模型栈 `:8-39`、服务栈 `:25-56`,逐字段同名同序同默认值,parity 当前成立、零漂移。

**承重禁区行回盘(对照 phase1_blast)**:
- 模型栈置零逻辑:`coercion.py:470` `graph_downstream_weight=0 if int(values["graph_critical_weight"])==0 and int(values["graph_impact_weight"])==0 else int(ScheduleConfigSnapshot.graph_downstream_weight)` —— ✅与 registry 一致。
- 服务栈对侧置零:`config/config_snapshot.py:89-92` helper `_graph_downstream_weight_for_visible_weights(...)`,调用点 `:295`、`:450` —— ✅与 registry 一致。
- loud raise:`coercion.py:71-72` `if policy==MISSING_POLICY_ERROR: raise ValidationError(...)`;`:152-153` `if strict_mode and text=="": raise ValidationError("不能为空")`。**注意 registry lb_no_touch 写「coercion:73-80 MISSING_POLICY_ERROR raise」,实际是 :71-72,有 ~2-7 行向上漂移**(唯一一处行号漂移),但符号语义命中无误。

**整组定位**:`find` 确认 5 个 `schedule_config_runtime_*.py` 全在 `core/models/`,服务孪生在 `core/services/scheduler/config/`(config_snapshot/config_field_spec/config_weight_policy)。另有**顶层 shim** `core/services/scheduler/config_snapshot.py`(4 行,纯 re-export,属 R26/B13,非本债本体)。

## 字段 2 — 当前状态 + 与 status evidence 是否一致
**状态: planned**(registry `status_2026_06_05="planned"`)。

**与 status evidence 完全一致**:registry `current_evidence_2026_06_05` 断言「承重双栈快照本体未动 + 两侧 @dataclass 上方无『故意双栈/逐字段同步』注释」。本轮回盘逐字验证:
- `sed -n '1,8p' schedule_config_runtime_snapshot.py` 与 `sed -n '20,25p' config/config_snapshot.py` grep `故意|刻意|双栈|parity|锁步|同步|two|mirror` —— **两侧 ZERO 命中**,与 evidence「grep 故意|刻意|双栈|parity|两栈 两文件 ZERO 命中」吻合。
- 两个 parity 测试文件 `regression_scheduler_config_spec_sync_contract.py` / `regression_config_snapshot_projection_sync.py` 均存在,但**当前仅覆盖 spec 字段与注解去重,未覆盖三个 helper 函数**(见字段 7)。

结论:本债**尚未开工**(承重注释 + helper-parity 两件计划工作均未落地),状态 planned 与现状一致,无「已修但漏标」或「已标但未修」错位。

## 字段 3 — 病理 / 严重度 / 桶 / 簇
- **病理 P3**(重复/双实现):同一概念「运行期配置快照 + 严格/降级校正 + 权重归一」在 `core.models` 与 `core.services` 两套近乎逐字节实现(各 ~500 行),靠人工锁步同步。
- **承重根因**:不是「随手 copy 出来的可删冗余」,而是分层被迫的中性投影 —— 算法层(`core.algorithms.greedy`)不能 import 服务层(layer_edges 证实 `core.algorithms→core.services` 不存在,字段 10 回盘确认),故必须在 model 栈复刻一份纯净快照供算法消费。两栈对**同一份配置**的口径必须逐字段一致,任一栈加字段忘同步 → 算法看到旧默认值、配置页存新值 → 静默分叉污染排产正确性(踩灵魂暗线)。
- **严重度 medium**:当前 parity 成立、零漂移,无活跃 bug;风险是**未来演进期的失忆债**(下一次有人单边加字段)。承重让它「不能删只能钉」,但不紧急。
- **桶 B03 / 簇 C01**:B03 = core-models 分区的双栈/锁步族;C01 干扰簇内含 LB04(coercion 同文件)、R29(weights 同文件)、R33/R45/R48/R51(schedule_params/config_adapter 同文件)及兄弟 R47/R71。本债是 **C01 / B03 桶根**。

## 字段 4 — 修法类(承重=仅注释+parity;owner_pending=待裁)
**修法类 = 注释 + parity 契约测试(承重,零删除/统一/透传)。owner_pending=true → 待 owner 裁断,暂不给终态修法、暂不分配执行批次。** 以下是**候选**修法(非终态),供 owner 裁断:

(1) **补『我是故意的』中文注释**(纯增量,零结构):
- 落点 A:`core/models/schedule_config_runtime_snapshot.py:7` `@dataclass` 上方;
- 落点 B:`core/services/scheduler/config/config_snapshot.py:24` `@dataclass` 上方;
- 文案(对侧路径互填):「⚠️ 本 dataclass 与 `<对侧路径>` 的 ScheduleConfigSnapshot 必须逐字段同步。这是分层(算法层不能 import 服务层)被迫的双实现,非随意复制。加/改字段必须两栈同改,否则算法用旧默认值、配置页存新值,静默分叉污染排产正确性。收敛前先补 parity 契约测试。」

(2) **扩 parity 契约**:把现有 `regression_scheduler_config_spec_sync_contract.py`(当前只比 spec 字段)扩到**覆盖三个 helper 函数**——对相同输入断言 `ensure_schedule_config_snapshot` / `normalize_weight_triplet` / `coerce_runtime_config_field` 两栈逐分支等价(含边界值,见字段 7)。

**绝对禁止(承重红线)**:删一栈、统一为单实现、让 model 透传 service(会越层)、让 service 反向复用 model 并删 model helper(=R71 物理收敛,必须在 helper-parity 落地后才能动,见字段 6/12)。**owner 须裁断的点**:是否最终允许 R71 做「service 反向复用 model」的物理收敛(`core.services→core.models` 是合法边),还是永久保留双栈仅靠 parity 锁步。本档不替 owner 拍。

## 字段 5 — 同文件碰撞债(R47 / R71 + 其余 same_file 边)
同文件兄弟债 = **R47, R71**。其余 same_file 干扰边:LB04、R29、R33、R45、R48、R51(见 interference_edges)。

| sibling | 同文件锚 | 改哪段 | 与 LB07 谁先谁后 | 撞行号/dict 键位移风险 |
|---|---|---|---|---|
| **R71** | `config/config_snapshot.py` + `coercion.py` + `runtime_read.py` | R71 = service 反向复用 model、删 service 侧重复 helper(物理收敛) | **LB07 必须先**(parity 先于收敛,Phase1 边 LB07→R71)。helper-parity 测试不落地就动 R71 → 锁步守卫缺位 → 静默漂移 → LB07 承重失效 | R71 会删/挪 `config_snapshot.py:89-450` 的 helper,**会大幅移动行号**;LB07 注释钉在 class 顶部(`:24`)相对稳,但 R71 后须复核注释里引的对侧路径是否仍有效 |
| **R47** | `coercion.py` | R47 涉 coercion 校正逻辑收敛 | **LB07 必须先**(Phase1 边 LB07→R47) | R47 动 coercion.py 函数体,可能移动 `:470`/`:71-72`/`:152-153` 承重行号 → LB07 字段 9 禁区行需在 R47 后重新回盘 |
| LB04 | `runtime_coercion.py` | 另一承重债(coercion 同文件) | 两承重同文件,**互不删对方禁区**;先后由 owner 排,建议 LB07 注释先落 | 同文件,改 coercion 任一函数体都可能位移对方锚行 |
| R29 | `runtime_weights.py` | weights 相关 | 与 LB07 normalize_weight_triplet parity 相关,R29 改 weights 须先过 LB07 parity | 改 weights.py:48 函数体会移动 normalize 定义行 |
| R33/R45/R48/R51 | `schedule_params.py` / `config_adapter.py` | 调用侧/adapter 债 | **与 LB07 正交**:R45/R48 的 adapter 只 `import FROM` model 的 `ensure_schedule_config_snapshot`(经 `schedule_params.py:98` 仍活),删 adapter 不动 LB07 本体 | 不撞 LB07 class 体;但 R33/R51 改 schedule_params 调用点,LB07 字段 8 消费者清单行号需复核 |

**关键碰撞结论**:LB07 是 R47/R71 的**安全网前置**,任何动 coercion.py / config_snapshot.py 函数体的 sibling 都会位移 LB07 的承重锚行 → **LB07 注释 + helper-parity 必须最先落**,之后 sibling 收敛才安全。

## 字段 6 — 同收口点债 + 顺序敏感性
LB07 的候选修法**不收口**(纯补注释 + 扩测试),所以本债自身不向某收口符号汇聚。但它**为未来收口设防**:

- 真正的收口动作在 **R71**(让 service 栈反向复用 model 栈的 `ensure_schedule_config_snapshot` / `normalize_weight_triplet` / `coerce_runtime_config_field`,收到**已存在**的 model 栈符号,`core.services→core.models` 合法,符合收口铁律「收到已存在统一点」)。
- **顺序敏感性(硬先后)**:`fix_invalidation_risk` 明确——若在「helper 级 parity 契约测试」落地之前就动 R71,锁步守卫缺位 → 静默漂移 → LB07 承重失效。故链路为:**LB07 承重注释 + helper-parity 测试 → 然后 R71 收敛 → 然后 R47**。
- 不存在「为已有概念新建第二模块」式新 P5(本债恰恰是在治理已有的双栈,不新建)。唯一获批新建例外是 R09 的 `parse_optional_positive_int`,与 LB07 无关。

## 字段 7 — 收口行为差异 + 所需 parity 测试
LB07 自身不收口,但它**定义 R71 收口前必须通过的 parity 验收**。当前 parity 覆盖与缺口:

**已覆盖**:
- `regression_scheduler_config_spec_sync_contract.py:7-19` —— 比 `list_runtime_config_fields()`(model) vs `list_config_fields()`(service)的 key 序、field_type、default、min_value、min_inclusive、choices 逐项相等。**只覆盖 spec 元数据,不覆盖运行期 helper 行为**。
- `regression_config_snapshot_projection_sync.py:89-101` —— 两栈 class 注解去重守卫(删/合并字段会红);`:132-153` service 快照保留 model 降级事件 parity。

**缺口(R71 收口前必补)**——三个 helper 的**逐分支行为等价**测试,边界反例:
1. `ensure_schedule_config_snapshot`:`strict_mode=True` + 缺字段 → 模型栈 `coercion.py:71-72` raise `ValidationError`;`strict_mode=False` + 坏数据 → 静默回落默认 + 记 degradation。**反例**:同一坏 payload,strict 路 raise vs 非 strict 路 None/默认,两栈必须逐分支同行为(否则算法静默吞、配置页报错,口径分叉)。
2. `graph_downstream_weight` 置零:`coercion.py:470`(critical==0 且 impact==0 → 0,否则默认)vs service `config_snapshot.py:89-92`。**反例**:`critical=0,impact=0`→须两栈都得 0;`critical=0,impact=10`→须两栈都得默认 1。任一栈漏置零 → 排产权重错。
3. `normalize_weight_triplet`(model weights.py:48 vs service weight_policy.py:57):权重和为 0、负值、三元不一致的归一结果须逐例等价(`regression_config_snapshot_projection_sync.py:104` 已测 strict 拒绝不一致三元,但未比两栈归一数值)。

**parity 测试设计要点**:同一组边界输入(含 strict/非 strict × 缺/坏/越界数据)喂两栈 helper,断言「返回值 + 是否 raise + raise 的 ValidationError.field + degradation 计数」四者全等。这是 R71 把 model 当唯一真相源前的安全闸。

## 字段 8 — 爆炸半径(生产/测试消费者 + 灾难链)
**生产消费者(回盘确认)**:
- 模型栈(算法路径):`core/algorithms/greedy/schedule_params.py:98,100`(import + 调 `ensure_schedule_config_snapshot`,经 aggregator `core.models.schedule_config_runtime`)、`:108,110`(`coerce_runtime_config_field`)、`:123,124,128,138`(`normalize_weight_triplet`)。
- 服务栈(配置/排产运行路径):`run/optimizer_config.py:11,131`、`run/schedule_candidate_runner.py:10,118`、`summary/schedule_summary.py:8,76,121`、`config/config_page_save_service.py:29,73`(配置页保存)、`config/config_bootstrap_service.py:16,34`(default_snapshot)、`config/config_validator.py:10,271` + `config_preset_service.py` + `config_presets.py` + `config_read_service.py` + `config_write_service.py` + `config_service.py`(服务栈大量消费)。

**测试消费者**:`regression_config_snapshot_projection_sync.py`、`regression_scheduler_config_spec_sync_contract.py`、`regression_scheduler_candidate_py38_contract.py:16,18`(两栈文件 py38 AST 门禁)、`regression_config_field_spec_contract.py:11,13,283-285`、`test_sp05_path_topology_contract.py:22,35,36`(config_snapshot 路径映射 + ScheduleConfigSnapshot 导出白名单)、`regression_schedule_optimizer_cfg_snapshot_contract.py` / `regression_scheduler_candidate_config_contract.py` / `regression_scheduler_graph_on_mode_contract.py` 等十余处构造点。

**灾难链(改错会静默炸什么)**:
- **删一栈** → 越层 import 崩(算法被迫 import service)或大量 service 消费者 ImportError;若改成 model 透传 service → 跨层导入环。
- **统一/合并而漏字段** → 算法用旧默认值、配置页存新值,**model 侧坏/缺数据被静默兜成默认**(非 strict 路),排产用错权重 / 降级判定**且无声**——这是最危险的静默链,直接污染排产正确性。
- **碰承重置零/loud raise**(字段 9 禁区)→ `graph_downstream_weight` 不再随权重置零,或 strict 校验从 raise 退化成兜底 → 踩灵魂暗线,坏配置静默放行。
- **在 helper-parity 前动 R71** → 锁步守卫缺位,日后单边改字段无测试拦截 → 慢性静默漂移。

## 字段 9 — 承重毗邻标记 + 禁区行
**本债 = 承重本体(load_bearing=true)**。同文件/同组毗邻还有承重债 **LB04**(`runtime_coercion.py` 同文件)。禁区行(回盘核对,**只能补注释,绝不删/统一/透传/改逻辑**):

1. **置零承重**:`core/models/schedule_config_runtime_coercion.py:470` `graph_downstream_weight=0 if critical==0 and impact==0 else <默认>`;服务对侧 `core/services/scheduler/config/config_snapshot.py:89-92`(helper `_graph_downstream_weight_for_visible_weights`,调用点 `:295,:450`)。删/改 = 排产权重静默错。
2. **灵魂暗线 loud raise(严禁改兜底)**:`coercion.py:71-72` `if policy==MISSING_POLICY_ERROR: raise ValidationError("缺少配置")`;`coercion.py:152-153` `if strict_mode and text=="": raise ValidationError("不能为空")`(以及 `:164-165,:207-208,:219-220,:257-258,:301-302` 同族 strict raise)。
   - ⚠️ **registry lb_no_touch 写「coercion:73-80」,回盘实际是 :71-72**,~2-7 行向上漂移。**禁区按符号语义认定(`MISSING_POLICY_ERROR` raise 块),不按行号**;R47 改 coercion 后须重新回盘这些行。
3. **30 字段锁步表本体**:两栈 class 体(model `:8-39` / service `:25-56`)只能补注释,不得删/统一/透传任一字段。
4. 不得新增兜底/静默回退/except 吞错(灵魂线):P3 承重的正确治理是**钉住**而非**收敛掉**降级路径;非 strict 的静默回落是**已设计的**降级语义,补注释说明即可,不得当 bug 删。

## 字段 10 — 分层 / 导入环风险
**回盘结论:候选修法(补注释 + 扩 parity)零新增 import,无越层、无导入环风险。**

- `rg "from core.services|import core.services"` 在 5 个 `core/models/schedule_config_runtime_*.py` —— **NONE**,model 栈对 service 零依赖(干净)。
- `rg "from core.services|core.services.scheduler.config"` 在 `core/algorithms/greedy/schedule_params.py` —— **NONE**,算法只走 model 栈(`from core.models.schedule_config_runtime import ...`,经 aggregator `schedule_config_runtime.py:5-16`)。
- 分层事实:`core.algorithms→core.services` 边**不存在**(这正是双栈的根因);`core.services→core.models` 是**合法边**(故 R71 收口方向合法)。

**风险点(留给 R71,非 LB07)**:R71 若让 service 反向 import model,方向 `core.services.scheduler.config→core.models` 合法,不成环——但须确认 model 栈不会回头 import service(已确认 NONE),否则成环。LB07 本档候选修法不触碰任何 import,**0 AST 违规**,分层红线安全。

## 字段 11 — 测试耦合(动手前先迁哪些)
**补注释步骤**:零测试迁移(纯文档增量),但落注释后须跑既有守卫确认不破:
- `regression_scheduler_candidate_py38_contract.py:16,18`(两栈文件须存在 + py38 AST 合规——注释含中文 emoji ⚠️ 须确认 py38 AST 解析不报错;纯注释行不影响 AST,安全)。
- `test_sp05_path_topology_contract.py:22,35,36`(路径映射 + 导出白名单——注释不改导出,安全)。

**扩 parity 步骤(为 R71 铺路)**:在 `regression_scheduler_config_spec_sync_contract.py` **新增** helper 行为 parity 用例(不删原 spec 用例);可与 `regression_config_snapshot_projection_sync.py` 的降级 parity(`:132-153`)并列。**先迁/先补的测试**:helper 级 parity 必须在动 R71 之前落地并全绿,这是 R71 收口的准入门。

**动手前无需迁移已有测试**(补注释不改行为);但 R47/R71 收敛阶段须先把上述 helper-parity 落地,并在收敛后复核所有十余处 `ScheduleConfigSnapshot` 构造点测试(字段 8 测试消费者清单)仍绿。

## 字段 12 — 前置依赖(先做哪些债 / F 门)
**LB07 是本桶根,几乎无前置——它本身是 R71/R47 的前置安全网。**

- **F 门**:候选修法仅补注释 + 扩测试,需 F 门(分层/AST/parity 全绿基线)在动手后复跑确认,但不阻塞开工。Batch-1 全局 ROOT,纯增量零结构,**应最先落**。
- **谁依赖 LB07(Phase1 出边)**:`LB07→R71`、`LB07→R47`(承重注释 + helper-parity 必须先于这两条的任何物理收敛)。
- **LB07 的入边(谁阻塞 LB07)**:无硬前置。同簇 LB04/R29 与 LB07 在 coercion/weights 同文件,建议 LB07 注释先落以免后者改函数体位移承重锚行,但不构成阻塞。
- **owner 裁断点(阻塞终态,不阻塞分析)**:owner 须先决定「永久双栈仅 parity 锁步」vs「R71 物理收敛到 model」,才能给 R71 终态修法;LB07 本档候选(补注释 + 扩 parity)无论哪个裁断都成立,可先行。

**owner_pending=true**:本档已透依赖/爆炸半径,但**不给 LB07 之后链路(R71/R47)的终态修法、不分配执行批次**,待 owner 裁断双栈去留。

---

## 对抗核验(第二双眼睛)

**结论:PASS。** 独立重 grep 全量复核,零反证,行号/承重判断/分层判断/收口点真实性/owner_pending 合规性全部成立。

**重 grep 命令与结果:**
1. `rg -n "@dataclass|^class ScheduleConfigSnapshot"` 两栈文件 → model `:7/:8`、service `:24/:25`,**与档案逐字一致**。
2. `rg -n "def ensure_schedule_config_snapshot|def normalize_weight_triplet|def coerce_runtime_config_field" core/` → model coercion `:409/:484`、weights `:48`;service config_snapshot `:336/:312`、weight_policy `:57`。**六个定义点行号全一致**。
3. 字段计数:`sed -n '8,40p' ... | grep -cE "^\s+[a-z_]+\s*:"` = **30**;service `25,57` 同样 = **30**。parity 成立、零漂移,与档案/registry 一致。
4. 注释缺失:`rg -n "故意|刻意|双栈|parity|锁步|逐字段|mirror|two-stack"` 两栈文件 → **ZERO 命中**,确认承重注释未落、状态 planned 与现状一致。
5. 承重禁区行:`graph_downstream_weight=0 ...` 实锤在 model coercion `:470`;service 对侧 helper `_graph_downstream_weight_for_visible_weights` 定义 `:89`、调用 `:295/:450`。loud raise `MISSING_POLICY_ERROR`→`:71-72`、strict 空值 raise `:153`(同族 `:165/:208/:220/:258/:302`)。**全部命中**。
6. 分层:`rg "from core.services" core/models/schedule_config_runtime_*.py` → **NONE**;`rg "core.services" .../schedule_params.py` → **NONE**(算法只走 model 栈,import 入口 `:98/:108/:124/:138` 经 aggregator `schedule_config_runtime.py:5-29` 真实导出三 helper)。0 AST 违规,候选修法零新增 import。
7. 收口铁律复核:`core.services→core.models` 是**已被使用的合法边**(`config/config_page_outcome.py:6`、`config/config_field_spec.py:6` 已 import core.models),R71 收口「service 反向复用 model 已存在符号」落到**已存在统一点**,非新建第二模块,符合收口铁律。service 栈当前**尚未** import schedule_config_runtime(R71 确未动,LB07 确为其前置)。
8. 顶层 shim `core/services/scheduler/config_snapshot.py` = 5 行纯 re-export(R26/B13,非本债本体),basename 假碰撞结论成立。
9. 灵魂线:非 strict 路 `MISSING_POLICY_FALLBACK_WITH_DEGRADATION`(coercion `:70`)带 `DegradationCollector` 记录,确认是**已设计的可观测降级语义**,非静默吞错——档案「补注释说明、不当 bug 删」判断正确。

**行号漂移记录:** 唯一一处档案已诚实标注——registry `lb_no_touch` 写「coercion:73-80 MISSING_POLICY_ERROR raise」,实测在 `:71-72`(向上 ~2-7 行)。档案字段 1/字段 9 均明确认定「禁区按符号语义命中,不按行号」,处理正确。其余全部零漂移。

**承重误删风险:无。** 档案候选修法严格限定「补『我是故意的』中文注释 + 扩 helper 级 parity 契约」,字段 4/字段 9 显式列禁区:绝不删一栈/统一/透传/越层/动置零逻辑/动 loud raise。零删除、零统一、零透传,无「看着像该统一」的无害清理混入。

**owner_pending 合规:** 档案字段 4/字段 12 明确「待 owner 裁断双栈去留,暂不给 R71/R47 终态修法、暂不分配执行批次」,仅透依赖/爆炸半径,**未擅自给终态修法或排执行批次**,合规。

**像重复实为护栏专项验证:** DRY/统一动作会静默引爆——`rg` 实证算法层无法 import service 层(双栈根因),若强行统一/删 model 栈→算法越层崩或被迫透传成环;若统一漏字段→非 strict 路坏/缺数据静默兜默认,算法用旧默认值、配置页存新值,排产权重/降级判定无声分叉污染正确性。**双实现确为分层被迫的承重护栏,非可删冗余,DRY 不可直接套用。**

**核验签字:** LB07 PASS,无需升级第三方裁断(owner_pending 已是设计内待裁,非核验争议)。
