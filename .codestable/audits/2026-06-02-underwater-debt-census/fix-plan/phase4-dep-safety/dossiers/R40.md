# R40 逐债档案 — material_repo.update 库存数量 except 静默保留原值（P4 防御死角）

> 本档案只读不改、只产计划。承重=false，owner_pending=true → 仅给修法方向，**不给终态修法、不分配执行批次**，但依赖/爆炸半径分析透。

## 1. 当前真实 file:line（回盘）+ 漂移记录

按符号名 `stock_qty` / `except` / `val = updates` 在当前工作区 rg 回盘 `data/repositories/material_repo.py`：

| 元素 | 当前真实行 | 当前代码证据 |
|---|---|---|
| `MaterialRepository.update` 方法体 | `material_repo.py:51-82` | `def update(self, material_id: str, updates: Dict[str, Any]) -> None:`（:51） |
| `stock_qty` 分支入口 | `:64` | `if key == "stock_qty":` |
| 空/None 表示"不改"短路 | `:66-67` | `if val is None or (isinstance(val, str) and val.strip() == ""): continue` |
| `val = float(val)`（loud 转换） | `:69` | `val = float(val)` |
| **病灶 except 吞咽** | `:70` | `except Exception:` |
| 静默保留原值注释 | `:71` | `# 留给服务层校验；这里保持原值` |
| **病灶 静默回退坏值** | `:72` | `val = updates.get("stock_qty")` |
| 坏值写入 UPDATE | `:74-75 → :81` | `set_parts.append(...)` / `params.append(val)` → `UPDATE Materials SET ...` |

**漂移记录**：`old_location_DONT_TRUST=68-72`，实际 except 块在 **70-72**，`float(val)` 在 :69 → **漂移 +2 行（小幅，方法整体未位移）**。registry 的 `current_evidence_2026_06_05`（:70-72）与 `phase1_blast.location_status`（:64-72，:70/:72）**完全准**，本次回盘逐行复核一致。

## 2. 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = planned`；`phase1_blast.location_status = ✅准`。
- 回盘验证：病灶（:70 except + :72 静默保留原值）**仍原样存在**，未被修。`git log` 显示 `material_repo.py` 最近改动为 `95f645c7`（早于当前分支基线 b08162cd），当前分支对该文件**零差异**——与 evidence「git diff b08162cd 该文件零差异」一致。
- 结论：**状态一致，无漂移失真**。仍是 planned，未动手。

## 3. 病理 / 严重度 / 桶 / 簇

- **病理 P4（灵魂线，但不可达死角）**：库存数量 `float()` 转换失败时 `except Exception` 静默把**原始坏值**写回 `val` 并继续拼进 `UPDATE Materials SET stock_qty=?`，违反「坏数据不静默兜底」。
- **严重度 low**：生产路径下 except 分支**不可达**——唯一调用方 `material_service.update`（:113）在 :100 已对 `stock_qty` 做 `_norm_float(field="库存数量", min_v=0.0)` 强校验，坏值在 service 层即被 `ValidationError`（:38/:40）拦下，永远到不了 repo 的 except。当前是**无害死代码**。
- **桶 B16（home=B16）**；**分区 data-repos**；**kind=real**。
- **簇 ISOLATED**（`same_file_siblings=[]`，`interference_edges=[]`）。同桶仅与 R32 有 `co_change_within_group`（P4 灵魂线对），但 R32 不同文件、互不影响。

## 4. 修法类

**类别 = 改 raise（P4 灵魂线对齐）/ 或 补可观测 + 「我是故意的」注释。owner_pending=true → 以下为方向，待 owner 裁断错误分类，暂不给终态修法、暂不分配执行批次。**

owner 需在两条路径中裁断（二选一，两条都满足红线「必须去掉 :72 静默保留原值」）：

- **方向 A（推荐，分层零耦合）**：删 :70-72 的 `except Exception: ... val = updates.get(...)`，让 :69 `val = float(val)` 在坏值时**自然抛原生 `ValueError`（loud）**。无需任何 import、无分层耦合。同时在 :69 旁补一行「我是故意的」中文注释：
  > 『service 层 `_norm_float`（material_service.py:32, update 路径 :100）是第一道强校验（库存数量必须数字、>=0），本层不静默保留坏值；若未来有旁路绕过 service 直调 repo，此处 raise 即暴露而非把坏值悄悄写进 `Materials.stock_qty`（REAL 列）——灵魂线。』
- **方向 B（需 owner 裁定错误分类）**：抛 repo 层具名清晰错误。**红线警告**：若改抛 `core.infrastructure.errors.ValidationError`，会引入 `data → core` 错误耦合（见 §10），违分层取向；故默认仍推荐方向 A 的原生 `ValueError` 或不引依赖的清晰 `raise`。

**禁止项**（灵魂线 + 铁律 4/5）：严禁保留 :72 `val = 原值`；严禁加任何新兜底/静默回退/日志吞错后继续写库；严禁透传坏值。本债**非收口债**（无 P5 收口点，不收口到任何统一符号），故不涉及收口点新建/复用判断。

## 5. 同文件碰撞债

`same_file_siblings = []`，`interference_edges = []` → **同文件无碰撞债**。`material_repo.py` 内只有 R40 一条债。改动局部在 :70-72（删 except 块），不涉及 dict 键位移、不会撞任何兄弟债行号。**无需协调**。

## 6. 同收口点债

R40 **不是收口债**（修法不收口到任何统一符号，只是就地删 except / 改 raise）。因此**无同收口点债**、**无顺序敏感性**。与 registry `planned_fix_hint`「无收口」一致。

## 7. 收口行为差异及 parity 测试

本债**非收口**，故无「新旧两路逐分支等价」式 parity 对账。但作为 P4 改 raise，需关注**行为变更对账**：

- **旧行为**：`update` 收到非数字 `stock_qty`（如 `"abc"`、`NaN`）→ except 吞咽 → 把原始坏值写进 `UPDATE Materials SET stock_qty=?`（坏数据落 REAL 列，静默）。
- **新行为（方向 A）**：同输入 → `float(val)` 抛 `ValueError`，整个 `update` 失败、事务回滚，坏值**不落库**。
- **行为差异反例**：输入 `stock_qty="abc"` 时，旧路=静默写库不报错，新路=`raise ValueError`。这是**期望的灵魂线变更**，不是 regression。
- **所需测试（动手时新增，本债当前无续命测试）**：补一条 repo 层针对性测试——直调 `MaterialRepository.update(mid, {"stock_qty": "abc"})` 断言 `raises (ValueError)`，证明旁路绕过 service 时坏值不再静默落库。**注意**：现有 `regression_models_numeric_parse_hybrid_safe.py:168-175` 钉的是 MODEL 层 `Material.from_row`，**不能**当作本债的契约测试。

## 8. 爆炸半径

- **生产消费者（1，唯一）**：`core/services/material/material_service.py:113` `self.repo.update(mid, updates)`。上游 :100 已 `_norm_float(stock_qty, field="库存数量", min_v=0.0)` 强校验（`_norm_float` 定义 :32，坏值抛 `ValidationError` :38、负值抛 :40）。→ **正常生产路径下坏 stock_qty 永远到不了 repo 的 except，改 raise 对现有行为零影响**。
- **其他调用方排查**：rg `core/ web/ data/` 全量搜 `MaterialRepository` / `material_repo` / `.update(`，仅 `material_service.py:113` 一处调 `material_repo.update`；`batch_material_service.py` 用的是独立 `batch_repo`/`mat_repo`，未触本方法。`web/routes/material.py` 经 `material_svc` 间接调用，仍走 service 校验。→ **无第二条生产调用链**。
- **测试消费者（0 续命）**：`tests/models_domain/test_models_numeric_parse_hybrid_safe.py:168-175`（MODEL 层 `Material.from_row` NaN→raise、''→0.0）、`tests/_scripts_e2e/smoke_e2e_excel_to_schedule.py`（用合法值 `"0"`）——均为**相邻护栏**，不钉 repo except，改动**不会让任何现有测试转红**。
- **灾难链（删错/统一错会静默炸什么）**：风险**极低**。
  - 若误把 :69 `val = float(val)` 也一起删（而非只删 except）→ 非数字直接进 SQL，坏值仍可能落库 → 退化回病灶。修法必须**只删 :70-72，保留 :69 的 float 转换**。
  - 若 owner 选方向 B 引 `core.ValidationError` 而漏改 import 或错误类型 → 抛错类型与 service 层不一致，但因生产路径不可达，**不会炸生产**，仅影响未来旁路的错误语义。
  - 反向：若什么都不改（保留静默）→ 当前无害，但留下「未来旁路绕过 service 直调 repo 时坏值悄悄写进 REAL 列」的隐患（registry `downstream_effect`）。

## 9. 承重毗邻标记

- `load_bearing = false`，`lb_no_touch = null`，`lb_colocation_danger = none`。
- `material_repo.py` **不在承重清单**，同文件无任何 LB 债。
- → **无承重禁区行，无「仅注释」约束**，可正常改 raise（仍受灵魂线红线约束：去静默）。

## 10. 分层 / 导入环风险

- **方向 A（推荐）**：删 except / 抛原生 `ValueError` → **不新增任何 import，0 越层，0 导入环**。`material_repo.py` 当前 import 仅 `core.models.Material`（:5，model 是允许的下游依赖）+ `.base_repo`（:7）。
- **方向 B**：若引 `core.infrastructure.errors.ValidationError` → 新增 `data → core.infrastructure` import。需 AST 红线核验该方向是否被允许；当前 data 层已 import `core.models`，但 `data → core.infrastructure.errors` 属错误耦合取向，**默认不推荐**（见 §4）。
- 分层红线：本债修法**不引入** `core.algorithms→core.services`、`core.models→core.services` 这类禁止边，**0 AST 违规**。

## 11. 测试耦合

- 动手前**无需迁移任何现有测试**——现有 `regression_models_numeric_parse_hybrid_safe.py:168-175` 与 `smoke_e2e_excel_to_schedule.py` 钉的都不是 repo except 分支，改动不影响它们。
- 动手时**新增**（非迁移）一条 repo 层针对性测试（见 §7），覆盖「直调 `material_repo.update` 传非数字 stock_qty → raise」。

## 12. 前置依赖

- **无硬依赖**。`B16 pack cross_bucket_same_file_edges = []`；`co_change_within_group = [R32]`（同桶 P4 对，但不同文件、互不影响，无续命测试需退）；`fix_invalidation_risk = none`。
- **门控**：registry `planned_batch_hint` 标 Batch-1 后任意时点可落，不门控他桶，本桶内排第 2（与 R32 可并行）。但 **owner_pending=true → 待 owner 裁断错误分类（方向 A vs B）后才进执行批次**，本档案不分配批次。
- 需通过的 F 门（执行时）：分层 AST 门（0 越层）、灵魂线门（去静默、无新兜底）。

## 返回行

R40 | 真实行号: MaterialRepository.update=material_repo.py:51-82，病灶 except=:70 / 静默保留原值=:72 | 漂移: 有，+2 行小幅（old 68-72→实 70-72，方法未位移） | 修法类: 改raise/补可观测（owner_pending，待裁，暂不给终态修法/暂不分配批次；推荐删:70-72让:69 float 自然抛 ValueError + 我是故意的注释） | 同文件兄弟需协调: 无（ISOLATED，sibling=[]） | 最大爆炸风险: 误连:69 float 一起删→坏值仍落 REAL 列（退化）；生产路径不可达，改 raise 零影响 | 与既有分析冲突: 无 | 前置: 无硬依赖，与 R32 可并行，待 owner 裁错误分类 | owner_pending: y

---

## 对抗核验（第二双眼睛）

**核验日期 2026-06-05；独立重 grep，只读不改，默认怀疑——逐项找反例均未果，判 PASS。**

### 重 grep 命令与结果

1. **真实行号回盘**：`rg -n "def update|stock_qty|except|val = float|val = updates" data/repositories/material_repo.py` + Read :1-85。
   - 结果：`def update`=**:51**；`val = float(val)`=**:69**；`except Exception:`=**:70**；`# 留给服务层校验；这里保持原值`=**:71**；`val = updates.get("stock_qty")`=**:72**；method 体止于 `self.execute(sql, ...)`=**:82**。
   - → 档案「:51-82 / except :70 / float :69 / 静默保留 :72」**逐行一致，漂移 +2 行（old 68-72→实 70-72）属实，方法整体未位移**。
2. **调用方全量排查**：`rg -n "\.repo\.update\(|material_repo\.update\(|MaterialRepository" core/ web/ data/`。
   - 命中 7 处 `.repo.update(`，但仅 `material_service.py:113` 的 `self.repo` 绑定 `MaterialRepository`（确认 :22 `self.repo = MaterialRepository(...)`）；其余（supplier_service:181 / machine_service:237 / op_type_service:136 / operator_service:175 / resource_team:178 / *_excel_import:90,99,70）的 `self.repo` 是各自独立 repo，**不触本方法**。
   - `batch_material_service.py`：`rg "\.repo\.update"` → 仅 `:101 self.repo.update_qty(...)`（`update_qty`，非 `update`），其 `mat_repo`（:20=MaterialRepository）**未调 update**。
   - → 档案「唯一生产调用方 material_service.py:113」**属实，无第二条生产链**。
3. **service 强校验 = except 死角不可达**：`rg -n -A8 "def _norm_float" material_service.py` + 读 update 路径。
   - 确认 `_norm_float`（:32）坏值在 :36 `float(v)` 失败 → :38 `raise ValidationError`；update 路径 :100 `updates["stock_qty"] = self._norm_float(stock_qty, field="库存数量", min_v=0.0)` 在调 repo（:113）**之前**执行。
   - → 坏 stock_qty 在 service 层即被拦，**repo 的 :70 except 生产路径确实不可达，改 raise 对现行生产零影响**——档案 §3/§8 属实。
4. **分层 import 核验**：`rg -n "^import|^from" data/repositories/material_repo.py`。
   - 仅 `from core.models import Material`（:5）+ `from .base_repo import BaseRepository`（:7）。
   - → 方向 A（删 except、抛原生 `ValueError`）**不新增任何 import，0 越层、0 导入环**；方向 B 引 `core.infrastructure.errors.ValidationError` 会新增 `data→core.infrastructure` 耦合——档案 §10 警告**成立**。
5. **测试消费者复核**：`rg -n "material_repo\.update|MaterialRepository|update\(.*stock_qty" tests/`。
   - **零命中**——tests 下无任何直调 `MaterialRepository.update` 或钉该 except 分支的测试。档案点名的 `regression_models_numeric_parse_hybrid_safe.py:168-175` 钉 MODEL 层 `from_row`，确属相邻护栏非本债续命。
   - → **改动不会让任何现有测试转红**，属实。
6. **git 漂移核验**：`git log --oneline -1 -- data/repositories/material_repo.py` → `95f645c7`（早于基线 b08162cd），当前分支零差异，病灶原样存在——与 evidence 一致。

### 专项核验

- **承重误删风险：无**。`load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none`，`material_repo.py` 不在承重清单，档案未混入删除/统一/透传/加形参的承重误清理；修法是「去静默 + 改 loud」属灵魂线对齐，非 DRY 合并。
- **owner_pending=true 合规：是**。档案 §4/§12 明确「方向 A/B 待 owner 裁断错误分类，暂不给终态修法、不分配执行批次」，**未擅自给终态修法、未分配批次**——合规。
- **灵魂线红线：守住**。修法明确「严禁保留 :72 静默 val=原值、严禁加新兜底/吞错日志后续写库」，方向是 loud raise，**无新增兜底/静默回退**——合规。
- **收口点：本债非 P5 收口债**，修法不收口到任何统一符号，不涉及收口点新建/复用，确认无误。
- **爆炸半径反例尝试**：试找「误删 :69 float 一起删→坏值仍落 REAL 列退化」之外的次生灾难——未发现；唯一勘正提示已在档案 §8 列出（修法必须只删 :70-72、保留 :69）。

### 核验结论

**PASS。** 行号一致、调用链唯一且死角不可达、分层零耦合、零续命测试、owner_pending 与承重红线均合规，无争议，无需升级第三方裁。

---

## 2026-06-10 终态补登（O28 方向 A 收口，R40 → fixed）

详见 `_registry.json` R40 current_evidence。要点：删 except 静默保留原值（旧 :70-72），保留 `float()` 自然抛 ValueError，附「我是故意的（R40/O28）」注释；零新增 import（方向 B 的 data→core 耦合未引入）。新增 `tests/material/test_material_repo_stock_qty_loud_contract.py` 三用例（坏值 raise 不污染库存/空值跳过回归/合法值落库）。生产唯一调用方上游 _norm_float 已拦，零行为影响。
