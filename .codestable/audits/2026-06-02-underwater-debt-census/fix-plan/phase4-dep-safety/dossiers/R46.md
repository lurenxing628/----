# R46 逐债档案 — 死别名 `_safe_identifier`（注释自称内部用,实则零调用）

> **2026-06-08 B 执行终态：fixed。** G40/R46 已按符号重定位后执行：删除 `core/models/scheduler_public_errors.py` 中误导注释 + `def _safe_identifier(...)` + `return public_safe_identifier(...)` 三行死别名。
> **执行说明：** 下文保留的是动手前的只读证据和安全顺序；现盘 LB08 承重注释已在 `LEGACY_PUBLIC_PATTERNS` 上方存在，R46 删除未触碰 legacy 正则桥、`public_safe_identifier`、`_positive_int`、`make_public_error`、`legacy_public_error_message`、`infer_legacy_public_code`、`__all__`，也未触碰同名活函数 `core/infrastructure/migrations/v4_sanitizers.py:_safe_identifier`。

- 债 id: R46 | 桶: B10 | 簇: C01 | 病理: P6 死别名 | 严重度: low
- 主文件: `core/models/scheduler_public_errors.py`
- 承重: false | owner_pending: false | lb_no_touch: null
- 同文件兄弟: LB08 (+ R09 同文件)；同符号边: R01 / R19 (sym:`__all__`)

---

## 1) 执行前 file:line（历史快照）+ 执行后残留核对

以下为 2026-06-05 执行前按 `rg -n '_safe_identifier' core/ web/ data/ tests/` 回盘的历史快照；2026-06-08 G40 执行后，models 层这三行已删除，不再是当前待办目标：

- 执行前误导注释 `# Backward-compatible private alias for callers inside this module.` → `core/models/scheduler_public_errors.py:162`。
- 执行前 `def _safe_identifier(value: Any, *, max_chars: int = 80) -> str:` → `scheduler_public_errors.py:163`。
- 执行前转发体 `return public_safe_identifier(value, max_chars=max_chars)` → `scheduler_public_errors.py:164`。

**执行前漂移：无。** old_location_DONT_TRUST 标 `:163-164`，current_evidence 标 `:162-164`（含注释）。执行时删除目标曾精确锁定为 `:162-164` 三行（注释 162 + def 163 + return 164）；现盘 `rg -n '\b_safe_identifier\b' core/models/scheduler_public_errors.py` 无命中，证明该目标已删除。

**同名混淆排查（关键）**：执行后全仓 `_safe_identifier` 仍命中 `core/infrastructure/migrations/v4_sanitizers.py:37 def _safe_identifier(value: str) -> Optional[str]`，被本文件内 `:121/:122` 调用——这是 **另一个独立局部函数**（位于 infrastructure 层、签名不同、返回 `Optional[str]`、对 SQL 标识符做 sanitize），与已删除的 R46 目标（models 层、返回 `str`、纯转发 public_safe_identifier）**完全无关**。删 R46 已确认没有误删 v4_sanitizers.py 那个同名函数（它有 2 处活引用）。

证据锚点：执行前 `scheduler_public_errors.py:162-164`；执行后 `core/models/scheduler_public_errors.py` 对 `_safe_identifier` 零命中，`v4_sanitizers.py:37,121,122` 仍保留。

## 2) 当前状态 + 与 status evidence 一致性

- status_2026_06_08 = **fixed**。
- B 执行一致性：**已闭合。** 原 planned 证据里的误导注释、`def _safe_identifier(...)` 和转发体已删除；`public_safe_identifier` 仍是公开安全标识符真入口，`_positive_int` 与 legacy 正则反解桥均未动。
- 残留核对：词边界 `rg "\b_safe_identifier\b" core/models/scheduler_public_errors.py` 已无命中；同名活函数 `core/infrastructure/migrations/v4_sanitizers.py:37` 及其 `:121/:122` 调用仍保留。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6 死别名（执行前）**：`_safe_identifier`（执行前 `:163`）是 `public_safe_identifier`（执行前 `:142`）的纯转发包装，注释（执行前 `:162`）自称「Backward-compatible private alias for callers inside this module」，但实测模块内部 make_public_error 全部直调 `public_safe_identifier`，无一处调 `_safe_identifier`；模块外（resource_validation.py / summary_size_guard_fields.py / scheduler_summary_display.py / schedule_persistence_errors.py）一律调 `public_safe_identifier`。注释与现实矛盾——零调用死码 + 误导性注释双重债。执行后该死别名已删除。
- 严重度 **low**：纯死叶子，无运行期行为，删除零风险。
- 桶 **B10**（home=B10）；簇 **C01**。
- partition: core-models；kind: real；needs_adversarial: false / adv_refuted: false（无需对抗核验，纯死码事实可由 grep 闭合证明）。

## 4) 修法类

**直删，已执行**（非收口、非注释、非 raise、非 parity）。承重=false，故允许删除。

执行前精确删除范围：`scheduler_public_errors.py:162-164`（三行，现盘已删除）：

```
162  # Backward-compatible private alias for callers inside this module.
163  def _safe_identifier(value: Any, *, max_chars: int = 80) -> str:
164      return public_safe_identifier(value, max_chars=max_chars)
```

- 误导注释（`:162`）随 def 一并删（注释本身就是债的一部分，不可保留）。
- 删后维持 PEP8 间距：`:160-161` 两空行（隔 public_safe_label）与 `:165-166` 两空行（隔 _positive_int）各保留其一对，即删完上下仍是「函数—两空行—函数」结构，不留三空行也不黏连。
- **禁区行（绝不碰，现盘符号重定位）**：`public_safe_identifier`(`:143`,活,8 处生产消费) / `make_public_error`(`:171`) / `_positive_int`(`:163`,R09 同文件) / LEGACY 正则桥 `LEGACY_PUBLIC_PATTERNS`(`:63`) / `_LEGACY_CODE_PREFIXES`(`:95`) / `legacy_public_error_message`(`:214`) / `infer_legacy_public_code`(`:280`)（LB08 territory） / `__all__`(`:336-346`)。
- `__all__` **无需改动**：`_safe_identifier` 本就不在 `__all__`（现盘仅导出 `public_safe_identifier`，无下划线私有别名导出）。这也说明 R46 与 R01/R19 的 `sym:__all__` 边是「同符号块同居」而非「同行编辑」——R46 删除完全不触碰 `__all__` 列表。

## 5) 执行前同文件碰撞债（历史快照；约束已消费）

执行前同文件 `scheduler_public_errors.py` 上共有 **3 条债**：LB08（承重）、R09（同文件）、R46（本条）。三者改的段落物理分离，但都用绝对行号定位，先后改会撞行号偏移，所以执行前必须协调。当前状态：LB08 已先落，R46 已后删；这条约束已消费，R09 后续只需按符号重新定位 `_positive_int`。

### sibling LB08 — legacy 错误串正则桥（承重 true）
- 执行前 LB08 改段：`:62`（LEGACY_PUBLIC_PATTERNS 定义行）上方 `:61` 空行处**插入**护栏注释 + 绑既有契约测试。**纯插入注释**（承重只补注释，不删不改）。
- 执行前 R46 删段 `:162-164` 在 LB08 注释插入点（`:61`）**之下约 100 行**。
- **撞行号方向（历史约束）**：LB08 在 `:61` 插入注释 → 把其下所有行（含 R46 的 `:162-164`）整体下移；因此 R46 执行时必须按符号重定位，不能照搬旧绝对行号。
- **已执行顺序**：O35 已裁 LB08 注释文案，实际已按 `LB08 注释先落 → R46 删除后落` 执行。R46 是纯死码删除，已让位承重护栏。
- **fix_invalidation：none**——删 R46 与 LB08 修法无调用关系，互不证伪。唯一约束是行号协调；当前已通过串行执行消费。

### sibling R09 — `_positive_int` 可空正整数助手（同文件副本之一）
- 执行前 R09 锚点：`scheduler_public_errors.py:167 def _positive_int(value: Any) -> int`（R09 的 all_files 含本文件）。R46 删除后，现盘 `_positive_int` 已按符号重定位到 `:163`。
- **注意语义分叉**：本文件 `_positive_int` 返回 `int`（≤0 clamp 成 `0`），而执行车道 3 副本返回 `Optional[int]`（≤0/垃圾→None）。**二者契约不同**——这份 clamp-to-0 与 R09 收口目标 parse_optional_positive_int(None 语义) **不等价**。本档案不替 R09 裁断，仅标记此分叉，提醒 R09 owner 勿把 clamp-to-0 副本误并入 None-语义 sink。
- **撞行号方向（历史约束）**：R46 执行前删段 `:162-164` 紧贴执行前 `_positive_int:167`。R46 已删 3 行，现盘 `_positive_int` 为 `:163`。**若 R09 后续动本文件，必须按符号名重新定位**，不可用旧 `:167`。
- **已执行顺序**：R46 已先落，R09 后续再处理。无 dict 键位移（二者都不动 dict）。

**dict 键位移风险：无。** R46 执行前删的是顶层函数定义，不在任何 dict/`__all__` 列表内（`_safe_identifier` 不在 `__all__`）。三债均不改 `make_public_error` 产出的 dict 键。

## 6) 同收口点债

**无收口点。** R46 修法类是**直删**，不收口到任何符号。

- 表面上执行前 `_safe_identifier` 转发到 `public_safe_identifier`，看似「已收口到 public_safe_identifier」；但 R46 的动作不是「把 X 收口到 public_safe_identifier」，而是「删掉一个零调用的死转发」——没有任何活调用方需要改 import 或换符号。`public_safe_identifier` 是被动的转发目标，不因 R46 而改变。
- interference_edges 中的 `sym:__all__`（与 R01/R19）是「同 `__all__` 列表块同居」，**非同收口点**——R46 不增删 `__all__` 任何条目（`_safe_identifier` 本就不在表内）。故对 R01/R19 顺序不敏感：R46 删除与 `__all__` 列表的任何编辑（R01/R19 若动该列表）物理不重叠，可任意先后。
- 唯一真正的「收口」债是 R09（收口 `_positive_int` 到 `core.shared` sink），但 R46 不参与该收口，见 §5 R09 段。

## 7) 收口行为差异及所需 parity 测试

**不适用（修法=直删，非收口）。** 无新旧两路边界值等价问题。

- 由于 `_safe_identifier` 零调用方，删除后无任何行为路径改变——既不存在「新路 vs 旧路对 None/空串/越界 max_chars 是否逐分支等价」的 parity 关切，也不需要新增 parity 测试。
- 唯一需要的「验证」是**死码事实的闭合证明**：执行前词边界 grep `_safe_identifier`（排除 `public_`/`v4_` 同名）生产侧仅命中 def 自身和转发体，测试侧零命中；执行后 models 层零命中。无动态调用风险（非 getattr/字符串拼接调用——`_safe_identifier` 不在 `__all__`，不被 `*` 导入，无反射点）。
- 反例审查（确认无遗漏）：唯一一个「看起来像但不是」的是 `v4_sanitizers.py:37 _safe_identifier`——独立函数、不同签名、有 2 处活引用（`:121/:122`），**删 R46 不得触碰它**。已在 §1 隔离。

## 8) 爆炸半径

**生产消费者（`_safe_identifier`，models 层那个）：0。** 执行前全仓 grep（词边界、排除 `public_`/`v4_`）仅命中 `scheduler_public_errors.py:163` def 自身；执行后 `core/models/scheduler_public_errors.py` 已无 `_safe_identifier` 命中。模块内 `make_public_error` 调的是 `public_safe_identifier`，非 `_safe_identifier`。

**测试消费者：0。** `tests/` 下零命中 `_safe_identifier`（models 版）。

**对比——`public_safe_identifier`（转发目标，禁区，不删）的活消费者清单（8 处生产消费，证明转发目标活着、删别名不影响它）**：
- `core/models/scheduler_public_errors.py:197,200`（make_public_error 内部）
- `core/services/scheduler/run/schedule_persistence_errors.py:6,39`
- `core/services/scheduler/summary/summary_size_guard_fields.py:8,87,90,104,107`
- `core/algorithms/greedy/dispatch/resource_validation.py:5,55,56,59,61`
- `web/viewmodels/scheduler_summary_display.py:15,135,137,154,157`

**downstream_effect：无运行期影响。** 纯死函数 + 误导注释，删后行为零变化。

**灾难链（删错/删多会静默炸什么）**：
1. **误删 `v4_sanitizers.py:37 _safe_identifier`**（同名陷阱）：该函数被 `:121/:122` 活调用，做 SQL 标识符 sanitize。误删 → v4 迁移 SQL 标识符无清洗，`NameError`/迁移崩。**执行前防护**：R46 只允许删当时 `core/models/scheduler_public_errors.py:162-164` 的 models 死别名，且必须文件+符号双锁定；执行后该 models 死别名已无命中。
2. **误删 `public_safe_identifier`（`:142`）**（删多一截）：8 处生产消费立刻 ImportError/AttributeError，错误体 batch_id/op_code 渲染全崩。**防护**：禁区行清单（§4）。
3. **误删 LEGACY 正则桥**（`:62/:94/:218/:284`，LB08 承重）：legacy 中文错误串无法反解回 code，用户可见错误静默降级为通用文案——**静默失真，最危险**。**防护**：§4 禁区 + §5 LB08 协调。
4. **删后留三空行/黏连**：PEP8 lint 噪音，非功能炸——按 §4 维持两空行间距。

**净结论**：R46 自身爆炸半径 = 0（纯死叶子）；所有「爆炸」均来自删错对象（同名/越界/误删禁区），靠行号+文件+符号三锁定杜绝。

## 9) 承重毗邻标记

**同文件有承重债 LB08（load_bearing=true）。** R46 自身 load_bearing=false / lb_no_touch=null，但与承重债**同居一文件**，故删除时承重禁区行神圣：

**禁区行（lb_colocation，绝不删/改/移动，现盘符号重定位）**：
- `LEGACY_PUBLIC_PATTERNS`（`:63`，正则元组定义）
- `_LEGACY_CODE_PREFIXES`（`:95`，前缀映射元组）
- `legacy_public_error_message`（`:214`，反解函数）
- `infer_legacy_public_code`（`:280`）
- `public_error_message_from_detail` 中 legacy 调用（`:272`）
- `build_public_error_records` 中 legacy/infer/make 调用（`:321/:323/:332`）
- `make_public_error`（`:171`）/ `public_safe_identifier`（`:143`）/ `public_safe_label`（`:155`）/ `_positive_int`（`:163`，R09 territory）/ `__all__`（`:336-346`）

R46 执行前删除目标 `:162-164` 与禁区行**物理分离**；执行后已确认现盘禁区全部保留，未波及任何承重正则桥。

## 10) 分层 / 导入环风险

**零风险。** R46 是**纯删除**，不新增任何 import，不新建模块，不引入符号引用。

- 删一个 core-models 层内部死函数 → 不产生跨层 import（不会新增 core.algorithms→core.services / core.models→core.services 这类越层边，因为根本不加 import）。
- 不引入导入环：删除只减少文件内的一个无外部依赖的转发函数（`_safe_identifier` 内只调同文件的 `public_safe_identifier`，无外部 import 依赖）。
- 0 AST 违规：删除操作天然不可能引入分层违规。

## 11) 测试耦合

**无需迁移任何测试。**

- `tests/` 下零测试引用 `_safe_identifier`（models 版）——删除前无测试需要迁移/改写。
- 唯一引用本文件的测试是 `tests/schedule/route_view/test_scheduler_user_visible_messages.py`，但它 import 的是 `build_public_error_records`（现盘 `:697/:723/:745/:760`）与 `scheduler_summary_display`，**与 `_safe_identifier` 无关**。该测试是 LB08 的续命契约目标，R46 删除不触碰其断言，二者解耦。
- 删除后建议跑一次该 regression 作冒烟（确认未误伤同文件其它符号），但**不是 R46 的前置迁移**，纯事后验证。

## 12) 前置依赖

**前置债：LB08（软序约束，非硬阻塞；现已满足）。**
- Phase1 边 `LB08→R46`（planned_deps_hint）：R46 应在 **LB08 注释落定后**删除，避免两人先后改同文件撞行号。
- 该约束**不是功能阻塞**（fix_invalidation_risk=none，二者无调用关系），纯属行号协调。
- 当前执行终态：LB08 注释已先落，R46 已按符号重定位删除，旧 `:162-164` 仅保留为执行前历史锚点。

**前置门：无 F 门。**
- R46 是纯叶子直删，不依赖 F1/F3 等任何收口 sink 的新建（与 R09 不同，R09 须等 F3 建 `parse_optional_positive_int`）。

**与 R09 的次序：R46 先、R09 后。**
- R09 前置 F3（sink 全仓 grep=0，未建），落地远晚于 B10 纯叶子的 R46。R46 已先删 → R09 后续若动本文件 `_positive_int` 时按符号定位（现盘已上移到 `:163`）。

**批次定位**：planned_batch_hint = Batch-11（B10）。现执行终态为 G40/R46 fixed；LB08 注释已先落，R46 已严格删除私有死别名三行，未碰 LEGACY 正则桥 / make_public_error。

---

### 附：核验闭环小结
- 执行前回盘零漂移；执行后状态已 fixed，models 层 `_safe_identifier` 已无残留；
- 死码事实由词边界 grep 闭合证明（生产 0 / 测试 0），同名陷阱（v4_sanitizers / public_safe_identifier）已隔离；
- 修法=直删，不收口、无 parity、无越层、无导入环；
- 唯一真实约束 = 同文件 LB08（承重）+ R09 的行号协调，已通过「LB08 先落、R46 后删」消费；R09 后续仍须按符号定位 `_positive_int`。
- owner_pending=false：已按终态修法完成，无需 owner 裁断。

---

## 对抗核验（第二双眼睛）

核验人：独立重 grep 回盘，只读未改任何生产代码。以下第 1 条为执行后复核，其余原始行号为执行前历史快照；结论 **PASS**。

**重 grep 命令与结果：**

1. 执行后 `rg -n '\b_safe_identifier\b' core/models/scheduler_public_errors.py core/infrastructure/migrations/v4_sanitizers.py` → models 层零命中；仅 `v4_sanitizers.py:37/121/122`（独立同名函数，infrastructure 层，`Optional[str]`，被 :121/:122 活调用，与本债无关）。**models 版死别名已删除，生产/测试/动态引用仍为 0。**
2. 执行前 `sed -n '158,170p'` 逐行复核：注释 `# Backward-compatible...`=**:162**，`def _safe_identifier`=**:163**，`return public_safe_identifier(...)`=**:164**，`_positive_int`=**:167**。**这是历史快照，不代表现盘处理目标。**
3. `rg -n '\bpublic_safe_identifier\b'` → 转发目标活，8+ 处生产消费（resource_validation / summary_size_guard_fields / scheduler_summary_display / schedule_persistence_errors + 模块内 make_public_error）；现盘 `__all__` 只导出 `public_safe_identifier`，**无 `_safe_identifier`**。证实「删别名不动转发目标」。
4. make_public_error 内部实测调 `public_safe_identifier`，**非** `_safe_identifier` —— 死别名定性坐实。
5. `rg "getattr.*_safe_identifier|'_safe_identifier'|\"_safe_identifier\""` → **无动态引用**（非反射调用，删除安全）。
6. 承重禁区执行后复核：`LEGACY_PUBLIC_PATTERNS`=:63、`_LEGACY_CODE_PREFIXES`=:95、`public_safe_identifier`=:143、`public_safe_label`=:155、`_positive_int`=:163、`make_public_error`=:171、`legacy_public_error_message`=:214、`infer_legacy_public_code`=:280 —— **全部保留**。执行前删段 :162-164 与禁区物理分离，执行后禁区仍完整。

**承重误删风险审查：** R46 本身 load_bearing=false / lb_no_touch=null，修法=直删，**未混入统一/透传/加形参/合并**，未把 LEGACY 正则桥当无害清理。同文件承重兄弟 LB08 的禁区行清单准确，三锁定（文件+行号+符号）足以杜绝误删。**无承重误删风险。**

**owner_pending 审查：** =false，给终态修法（直删）合规，无擅自分配执行批次（仅给 planned_batch_hint 建议序，非强分配）。

**协调提醒核准：** 档案对 LB08 先落致 +4 漂移须按符号重定位、R09 的 `:167` clamp-to-0 ≠ None-语义 sink 勿误并的提醒，均经独立验证属实。

**附议提示（非争议）：** `schedule_persistence_errors.py:39` 自带一个 `return public_safe_identifier(...)` 转发体，是另一文件的独立实现，调 public 版、不引用 models 死别名，删 R46 不波及——档案未列但不影响结论。

**结论：行号一致、零引用属实、删除范围精确、无越层无环、无承重误删风险。PASS，无需第三方裁断。**
