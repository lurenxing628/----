# R68 逐债档案 — `_meta_bool_state` 降级判定逻辑逐字两份

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：本 dossier 引用的 `tests/regression_sp06_no_duplicate_defs.py` 已由 A 的 **P1.1（commit `7ca42ca4`）整删**（早于 P6），`NO_CFG_GET_TARGETS` 符号全树零命中。凡『同提交退该文件断言行』的步骤为 **no-op**（无文件可红）；若作白名单守卫引用，须确认该锚点已废或重指。详见 `ANCHOR-DRIFT-2026-06-05-POSTCOMMIT.md §六.3` + `_B_COMPAT_SAFEGUARDS.md §5`。


- 桶/home: B15
- 病理: P5 (跨文件逐字两份) + soul_line_sensitive (used_default loud 二元组)
- 严重度: medium | 承重: false | 簇: ISOLATED | owner_pending: false
- 主文件: `core/services/scheduler/summary/schedule_summary_degradation.py`
- 副文件: `core/services/scheduler/summary/schedule_summary_downtime_degradation.py`

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

按符号名 `rg -n "_meta_bool_state"` 在当前工作区回盘（非照抄 old_location）：

| 实体 | old_location_DONT_TRUST | 当前实际 file:line | 漂移 |
|---|---|---|---|
| 定义 A（resource_pool 路径） | `schedule_summary_degradation.py:123` | `core/services/scheduler/summary/schedule_summary_degradation.py:123` | **无漂移**（行号一致）|
| 定义 B（downtime 路径） | `schedule_summary_downtime_degradation.py:30` | `core/services/scheduler/summary/schedule_summary_downtime_degradation.py:30` | **无漂移**（行号一致）|

两份函数体逐字节比对：`diff <(sed -n '123,140p' …degradation.py) <(sed -n '30,47p' …downtime_degradation.py)` → **IDENTICAL**。
A 体 = degradation.py:124-140；B 体 = downtime_degradation.py:31-47，分支结构与字面量集合（`{"true","1","yes","y","on"}` / `{"false","0","no","n","off"}`）完全相同，且都返回 `(bool, parse_failed)` 二元组。

调用点（`_meta_bool_state(...)`）：
- degradation.py:369 `resource_pool_attempted` (default=False)
- degradation.py:370 `resource_pool_build_ok` (default=True)
- downtime_degradation.py:112 `downtime_load_ok` (default=True)
- downtime_degradation.py:115 `downtime_extend_attempted`
- downtime_degradation.py:118 `downtime_extend_ok` (default=True)

**结论：location_status 与 registry 标注「✅准」一致，零漂移。**

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05`: **planned**。
- 一致性证据：`git diff --stat b08162cd -- <两文件>` 输出为空（RC=0），即自 b08162cd 以来两文件零改动；收口家 `summary_count_parse.py` 仅含 `parse_summary_count`，**未迁入** `_meta_bool_state`；`tests/` 无任何符号引用 `_meta_bool_state` 的 parity 契约（`rg` 仅命中无关的 `latest_summary_parse_state`）。
- **判定：当前态与 status evidence 完全一致——尚未动工（planned），逐字两份仍存在，parity 网尚未铺。**

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 **P5（跨文件逐字重复）+ soul_line_sensitive**：两条降级路径（resource_pool 与 downtime）各持一份逐字相同的 `_meta_bool_state`，改一漏一会让两路对同一坏 meta 给出不同 `used_default` 标记。
- 严重度 **medium**：纯重复债，行为当前一致，无即时正确性损伤；但漂移风险高（一旦单边修改即静默分叉）。
- 桶/home **B15**（逐字两份收口桶）。
- 簇 **ISOLATED**：`same_file_siblings=[]`、`interference_edges=[]`，无 same_file/same_symbol 干扰边；与 R69/R70 仅「同批不同文件」（co_change_within_group），无符号或文件级耦合。

## 字段 4：修法类

**收口（P5 收敛）+ 先补 parity 测试**。承重=false，故允许收口，但 soul_line_sensitive 加两条硬约束：① 严禁压扁 `(bool, parse_failed)` 二元组为单 bool；② 严禁加兜底/静默回退——`used_default`（即第二个返回位 `parse_failed=True`）必须保持 loud（坏 meta 走到 `return bool(default), True` 的两条分支：degradation.py:132/139/140 与 downtime:39/46/47）。

**收口点（必须是已存在点）**：`core/services/scheduler/summary/summary_count_parse.py`。
- 证据：该文件已是本 summary 包的 parse 收口模块（已被 `summary_visible_degradation.py:8`、`schedule_summary_contract.py:6` 导入使用），仅 `import math` + `typing`（summary_count_parse.py:3-4），**不是为本债新建的第二概念模块**——符合收口铁律（收到已存在统一点）。
- 动作：把单份 `_meta_bool_state` 提升到 `summary_count_parse.py`，两份 degradation 文件改为 `from .summary_count_parse import _meta_bool_state`（去掉本地 def，删两份逐字体）。
- 备选收口点 `core/shared`：不推荐——会把 summary 专用解析下沉到通用层，扩大依赖面，且 `summary_count_parse.py` 已是更近的现成点。

**严禁修法清单**：不得改 `_meta_int_state`（downtime:23 独有，不在本债）、不得动 `parse_summary_count` 的签名、不得把两路 default 差异（False vs True）硬编死、不得为「meta 缺键」分支加日志吞错。

## 字段 5：同文件碰撞债

`same_file_siblings=[]`，**无同文件碰撞债**。
- degradation.py 内另有 `_metric_int`(:143) 等函数，但均不在本债登记的同文件 sibling 列表（registry `same_file_siblings=[]`、`interference_edges=[]`）。
- 收口后 degradation.py:123-140 整段 def 被删（17 行），downtime:30-47 整段 def 被删（18 行）——会造成两文件后续行号上移，但因无同文件兄弟债同时改这两文件，**不存在撞行号 / dict 键位移风险**。

## 字段 6：同收口点债 + 顺序敏感性

收口点 `summary_count_parse.py` 当前只住 `parse_summary_count`。
- B15 同批 **R69/R70**：registry `co_change_within_group=["R69","R70"]`，但 `planned_deps_hint` 明确「与 R69/R70 仅同批不同文件，无冲突」——它们各自收别的逐字两份，**不收到 `_meta_bool_state` 这个符号**，对 `summary_count_parse.py` 无新增 def 冲突（除非 R69/R70 也选此模块当家——需在 Batch-15 排程时确认彼此追加的 def 名不撞）。
- **顺序敏感性**：本债内部有强顺序边——`PHASE1 §2「R68 → R68-converge | parity 先于收敛」`：parity 测试必须在 **Batch-1 (ROOT)** 落地，收敛动作在 **Batch-15**。即「先补 parity 网，后删两份合一」，倒序会失去回归保护。

## 字段 7：收口行为差异 + 所需 parity 测试

**收口前两路行为差异分析**：函数体逐字节相同（diff=IDENTICAL），故收口本身**无逻辑差异**——唯一差异在**调用点的 default 实参**：
- resource_pool 路径：`resource_pool_attempted` default=**False**（degradation.py:369）；`resource_pool_build_ok` default=**True**（:370）。
- downtime 路径：`downtime_load_ok` / `downtime_extend_ok` default=**True**（downtime:112/118）。

收口后单份函数对同一 `(meta, key, default)` 必须逐分支等价。**反例/边界值矩阵（parity 测试须覆盖）**：

| 输入 value | 期望 (bool, parse_failed) | 触发分支 |
|---|---|---|
| key 缺失 / None | `(default, False)` | :124-125 / :31-32 |
| `True` (bool) | `(True, False)` | :128 / :35 |
| `1` / `0` (int) | `(True/False, False)` | :130-131 / :37-38 |
| `5` (int, 非0/1) | `(default, True)` ← **loud** | :132 / :39 |
| `"yes"/"on"/"1"` (str) | `(True, False)` | :135-136 / :42-43 |
| `"off"/"no"/"0"` (str) | `(False, False)` | :137-138 / :44-45 |
| `"garbage"` (str) | `(default, True)` ← **loud** | :139 / :46 |
| `[]` / `{}` (其他类型) | `(default, True)` ← **loud** | :140 / :47 |

**关键反例（防压扁二元组）**：`value=5, default=True` 必须得 `(True, True)`——第一位用 default、第二位 `parse_failed=True`。若收口时误把返回简化为单 bool，则丢失 `parse_failed`，前端降级提示丢失「meta 被坏数据降级」信号。parity 测试须 `assertEqual` 完整二元组，**禁止只断言第一位**。
parity 测试落点：`tests/`（regression_tests，Batch-1 ROOT 先行），对上述矩阵同时跑「直接调 summary_count_parse._meta_bool_state」与（收口前）两份各自的实现，断言三者逐格相等。

## 字段 8：爆炸半径

**生产消费者（5 个调用点，全部位于两文件内部，无跨包外部调用）**：
1. degradation.py:369 — `resource_pool_attempted`
2. degradation.py:370 — `resource_pool_build_ok`
3. downtime_degradation.py:112 — `downtime_load_ok`
4. downtime_degradation.py:115 — `downtime_extend_attempted`
5. downtime_degradation.py:118 — `downtime_extend_ok`

`_meta_bool_state` 是模块私有（前缀 `_`），`rg` 全仓无任何 `core/web/data/tests` 外部模块以符号名引用它——爆炸面被锁死在这两文件内。

**测试消费者**：无直接 `_meta_bool_state` 符号引用。两文件被文件级引用于：`tests/web_pages/test_frontend_ui_language_polish.py`、`tests/gate_meta/test_sp05_path_topology_contract.py`、`tests/regression_sp06_no_duplicate_defs.py`（均不 pin 本函数，pin 路径/语言）。

**下游效应**：`downstream_effect` = 降级摘要正确性——resource_pool 降级路径与 downtime 降级路径对同一 meta 给出不同 `used_default` → 前端降级提示分叉。属灵魂线（坏 meta 的 used_default 标记）。

**灾难链**：
- **删错（漏删一份 / 压扁二元组）**：若收口时丢掉 `parse_failed` 位 → 坏 meta 被静默当成 default 接受，前端不再亮「降级因 meta 异常」提示 → **静默正确性损伤，无报错**（违灵魂线 loud 原则）。
- **统一错（先收敛后补 parity，或不补 parity）**：无回归网，单份实现日后任一改动都无法被测试发现两路分叉重现。
- **收口点选错（新建第二模块）**：违收口铁律，等于把 P5 又制造一份。

## 字段 9：承重毗邻标记 + 禁区行

- `load_bearing=false`、`lb_no_touch=null`。
- `lb_colocation_danger`: **none** — 两文件均不在 PHASE0 §5 承重护栏清单。
- **无承重禁区行**。但 soul_line_sensitive 等效软禁区：`_meta_bool_state` 内所有 `return ..., True`（degradation.py:132/139/140、downtime:39/46/47）即 `parse_failed=True` 的 loud 标记位，收口时**不得改写为静默 default 或吞掉第二位**。

## 字段 10：分层 / 导入环风险

**零越层、零导入环**。证据：
- 收口家 `summary_count_parse.py` 仅 `import math` + `from typing import ...`（:3-4），不 import 任何 degradation 文件 → 反向无引用。
- 两 degradation 文件改为 `from .summary_count_parse import _meta_bool_state`（同包 `summary/` 内的水平 import），`summary_count_parse.py` 不回引它们 → **不构成导入环**。
- 分层方向：均在 `core/services/scheduler/summary/` 同层，无 `core.algorithms→core.services` 或 `core.models→core.services` 的越层（AST 红线）。collapsing 仅在 services 层内部移动一个私有函数，0 AST 违规。

## 字段 11：测试耦合

**动手前的测试迁移**：
- **先建（Batch-1 ROOT）**：新 parity 测试（字段 7 矩阵），断言两路 + 收口单份逐格等价。这是收敛的前置回归网。
- **无需迁移既有断言**：现有 `regression_sp06_no_duplicate_defs.py` 的 TARGETS = `schedule_summary_assembly.py / evaluation.py / calendar_admin.py`（:9-11），**不含本二文件**，只查【单文件内】重复 def，**跨文件逐字两份它抓不到**——收敛既不会让它变红，也不会被它挡，无需改它。
- `test_sp05_path_topology_contract.py:90` 的 `SERVICE_ROOTS_WITHOUT_COMPAT` 列**旧根路径** `core/services/scheduler/schedule_summary_degradation.py`（断言该旧根不存在，文件早已在 `summary/` 子目录），把公共函数抽到 `summary_count_parse.py` **不触发**该断言——只要别在旧根 `core/services/scheduler/` 下建文件即可。
- `regression_frontend_ui_language_polish.py`：文件级引用，不 pin 本函数，收敛无影响。

## 字段 12：前置依赖

1. **Batch-1 ROOT（强前置，本债自带边）**：`PHASE1 §2「R68 → R68-converge | parity 先于收敛」`——parity 测试必须先于收敛落地。这是本债内最硬的顺序门。
2. **无跨债前置**：`fix_invalidation_risk` 明确「无其他债依赖本函数，不会让别的债修法失效」；`planned_deps_hint`「无跨桶同文件边」。
3. **F 门**：收敛 PR 须过现有契约门——sp05 路径拓扑（保旧根不存在）、sp06 单文件重复 def（与本债不相关但不得引入新单文件重复）、AST 分层门（0 越层）。
4. **同批协调（非阻塞）**：Batch-15 与 R69/R70 同批，排程时确认三者各自追加到收口模块的 def 名不撞即可，无强先后。

---

## 对抗核验（第二双眼睛）

核验人独立重 grep，默认怀疑、主动找反例。结论 **PASS**，未发现可推翻档案的反证。

**1. 行号 / 符号回盘（不照抄 old_location）**
- `rg -n "_meta_bool_state" --type py` → 定义 A `schedule_summary_degradation.py:123`、定义 B `schedule_summary_downtime_degradation.py:30`，调用点 369/370（A）+112/115/118（B）逐一命中，**与档案/registry 零漂移**。
- `rg -n "def _meta_bool_state"` 仅这两处定义，无第三份。

**2. 两体真等价（防"看着像其实不一样"）**
- `diff <(sed -n '123,140p' …degradation.py) <(sed -n '30,47p' …downtime_degradation.py)` → 输出 `IDENTICAL`（RC=0）。
- 直读 A 体确认二元组 loud 返回真实存在：`return bool(default), True` 出现在 int 非 0/1 分支、str garbage 分支、其他类型兜底分支（即 :132/:139/:140 等价位）。**档案"严禁压扁二元组、保 parse_failed loud"的依据成立。**

**3. 收口点真实性（防新建第二概念模块）**
- `sed -n '1,12p' …/summary_count_parse.py` → 文件存在，仅 `import math` + `from typing import ...`，零业务 import。
- `rg -n "summary_count_parse"` → 已被 `summary_visible_degradation.py:8`、`run/schedule_summary_contract.py:6` 以 `from ... import parse_summary_count` 导入使用，**确系已存在的本包 parse 统一点，非为本债新建**。符合收口铁律。
- 当前该模块仅 `def parse_summary_count`（:7），无 `_meta_bool_state`——确认尚未迁入（status=planned 属实）。

**4. 导入环 / 分层（AST 红线）**
- `summary_count_parse.py` 不 import 任何 degradation 文件，两 degradation 改为同包水平 `from .summary_count_parse import _meta_bool_state` 不构成环；均在 `core/services/scheduler/summary/` 同层，零越层。**字段 10 成立。**

**5. 测试门真实性**
- `tests/regression_sp06_no_duplicate_defs.py` TARGETS 实为 `summary/schedule_summary_assembly.py`、`core/algorithms/evaluation.py`、`scheduler/calendar_admin.py`（:8-11），**确不含本二文件**，跨文件逐字两份抓不到——收敛不会触红也不被它挡。字段 11 成立。
- `tests/gate_meta/test_sp05_path_topology_contract.py:90` `SERVICE_ROOTS_WITHOUT_COMPAT` 确含旧根 `core/services/scheduler/schedule_summary_degradation.py`（断言旧根不存在）；收口到 `summary/summary_count_parse.py` 不在旧根，不触发。字段 11/12 成立。

**6. 承重 / owner_pending / 误删风险**
- registry `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger="none"`、`owner_pending=false`——**非承重、无需 owner 裁断**。档案给出收口终态修法属合规（非承重债允许收敛）。
- 通读档案修法清单：收口为「提升单份 + 同包 import」，**未混入透传/加形参/压扁二元组/吞错兜底**；反而显式禁止压扁与加日志吞错。**无承重误删风险。**
- `_meta_int_state` 经 `rg` 确认仅 downtime:23 独有（非本债符号），档案"严禁动"标注属实。

**7. 同收口点撞名（B15 同批）**
- registry 查 R69/R70：R69 收 `_op_seq`（文件 `schedule_execution_persistence_guard.py`/`schedule_input_runtime_support.py`），R70 收 `_raise_schedule_empty_result`（`schedule_input_collector.py`/`schedule_service.py`）——**符号与文件均不撞 `_meta_bool_state` / `summary_count_parse.py`**，无并发追加 def 冲突。字段 6 的"非阻塞协调"判断成立。

**8. git 现状**
- `git diff --stat b08162cd -- <两文件>` 输出空（RC=0），自 b08162cd 两文件零改动，与 status=planned 一致。

**反例搜寻结果**：未找到任何能推翻"逐字两份 / 收口点已存在 / 零环 / 二元组 loud 必须保"的反证。唯一需 Batch-15 排程时确认的弱约束（R69/R70 def 名不撞收口模块）已验证当前不冲突。**核验通过。**

---

## 2026-06-10 终态补登（R68 → fixed）

详见 `_registry.json` R68 current_evidence。要点：parity 先建后收敛——全矩阵 16 格×两路径参数化测试先在两份副本上实跑 32 绿（证逐字等价），再把单份 `_meta_bool_state` 提升到既有收口家 `summary_count_parse.py`，两 degradation 文件删本地副本改同包 import；(bool, parse_failed) 二元组完整断言+is 单源断言防回潮。禁碰区全守（_meta_int_state、parse_summary_count 签名、调用点 default 差异）。
