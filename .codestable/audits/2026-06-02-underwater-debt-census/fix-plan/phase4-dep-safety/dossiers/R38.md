# R38 逐债档案 — list_as_dicts 三连结构同形死簇(op_type / operator / part)

> 桶 B12 | 簇 C02 | 病理 P6(结构同形死簇,三处零消费) | 严重度 low | 承重 false | owner_pending false
> 主文件 op_type_repo.py;全簇三文件 op_type_repo.py / operator_repo.py / part_repo.py
> 原始回盘时点 2026-06-05；2026-06-08 B 执行后已补登终态。以下旧行号保留作执行前证据，当前状态以 §0 与 `_registry.json` 为准。

## 0) 2026-06-08 B 执行终态补登

- **状态：fixed。** 三处 `list_as_dicts` 已全部删除：`op_type_repo.py` 旧 :73-74、`operator_repo.py` 旧 :85-86、`part_repo.py` 旧 :71-72。
- **同批约束已满足：** `part_repo.py` 的 R38 份与 R39 `list_unparsed` 同一次 diff 删除，行号二次漂移风险已关闭。
- **残留扫描：** `rg -n "list_unparsed|def list_as_dicts" data/repositories core web tests tools scripts` 无命中。
- **活近亲保留：** 三个 repo 的 `list/create/delete_all` 与引用检查方法仍在；未抽 helper，未新增兜底，未改分层 import。

## 1) 当前真实 file:line(grep 回盘)+ 漂移记录

`rg -n "def list_as_dicts" data/repositories/` 回盘三处 def 行(全绝对路径根 `/Users/lurenxing/Documents/GitHub/----/`）：

| 符号 | old_location(勿信) | 当前真实 file:line | 漂移 | def 体范围 |
|---|---|---|---|---|
| OpTypeRepository.list_as_dicts | op_type_repo.py:73 | `data/repositories/op_type_repo.py:73` | **无漂移** | :73-74(def + 单行 return fetchall) |
| OperatorRepository.list_as_dicts | operator_repo.py:85 | `data/repositories/operator_repo.py:85` | **无漂移** | :85-86 |
| PartRepository.list_as_dicts | part_repo.py:71 | `data/repositories/part_repo.py:71` | **无漂移** | :71-72 |

三处 def 行号与 old_location 逐一吻合,**漂移幅度 = 0**。三文件均不在已知漂移名单。

三处 SQL 实体(已读 def 体核实,**非字面同体**)：
- op_type:73-74 `SELECT op_type_id, name, category FROM OpTypes ORDER BY name`
- operator:85-86 `SELECT operator_id, name, status, remark, team_id FROM Operators ORDER BY operator_id`
- part:71-72 `SELECT part_no, part_name, route_raw FROM Parts ORDER BY part_no`

证据:`data/repositories/op_type_repo.py:73-74`、`operator_repo.py:85-86`、`part_repo.py:71-72`。三处仅 `return self.fetchall(...)` 单行体,无分支无副作用。

零调用回盘(铁律核心):
- `rg -n "list_as_dicts" --type py core/ web/ data/` → 仅返回三条 **def 行**,排除 def 行后 **零调用点**。
- callgraph edges.json(`jq` 过滤)：三处 `list_as_dicts` 只有**出边**(`from:...list_as_dicts → to:base_repo.fetchall`),**零入边**(无任何 `to:...list_as_dicts`)。坐实无生产消费者、无测试消费者。
- `tests/` 目录扫描 `list_as_dicts` → 零命中。

## 2) 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05` 已在 B 执行后补登为 **fixed**。
- current_evidence 已更新为：三处 `list_as_dicts` 全部删除，全仓残留扫描无命中，活近亲仍在。
- 一致性：**一致**。旧正文中的 planned/行号内容仅作为执行前证据保留，不再代表当前代码状态。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6**:结构同形死簇——三处方法签名/体形同(均 `def list_as_dicts(self)->List[Dict[str,Any]]: return self.fetchall(<单条SQL>)`),但 SQL 各异,属「结构同形」非「字面同体」。三处均零消费 = 死代码。
- 严重度 **low**:纯死代码清理,删除不改变任何运行路径。
- 桶 **B12**(home 同),簇 **C02**(干扰簇)。
- 修正 codemap `dup_bodies` 的「同体」标记:三处 SQL 列集与表名不同,**不可当「删一份代表三份」**,记账须为三次独立删除(每处各自已 grep 证零引用)。

## 4) 修法类

**修法类 = 直删(三处一并,记账三次独立删除)**。owner_pending=false,给终态修法。

精确删除:
- `op_type_repo.py:73-74`(def + return),连同其上方一空行不强制(保留即可)。
- `operator_repo.py:85-86`。
- `part_repo.py:71-72`。

为何不收口、不抽 helper:三处零消费(无人调用),抽公共 helper = **新建零消费的活代码**,违反「收口点必须收到已存在统一点」规则(planned_fix_hint 明确禁止为消重抽 helper)。本债无收口点,纯删除。

为何可同批删但记三笔:三处 SQL 列集不同(`op_type_id,name,category` / `operator_id,name,status,remark,team_id` / `part_no,part_name,route_raw`),非逐字副本,各自独立按 grep 证零引用后删;恰好三处都零引用,故同批一次提交,但记账上是三次独立删除——**禁止以「删一份代表三份」的省事心态合并判断**。

灵魂线核查:本修法为纯删除,**不新增任何兜底/静默回退/except吞错/过度防御**,无 P4 风险,符合灵魂线。

## 5) 同文件碰撞债(siblings)

同文件兄弟债 = **R39**(part_repo.py,`list_unparsed`)。其余 op_type_repo.py / operator_repo.py 无其他同文件债,随 R38 簇走、无碰撞。

仅 `part_repo.py` 内 R38 与 R39 物理相邻,需协调：

| 债 | 符号 | 真实行号(回盘) | 与 R38 在 part 份的相对位置 |
|---|---|---|---|
| R39 | `PartRepository.list_unparsed` | `part_repo.py:32`(回盘吻合 deps_hint 的 :32) | **在上方**(:32 < :71) |
| R38 | `PartRepository.list_as_dicts` | `part_repo.py:71` | 在下方 |

**行号位移分析(关键)**:
- 删 R38 的 :71-72(下方)→ 不影响 R39 的 :32(上方行号不动)。
- 删 R39 的 :32-33(上方,list_unparsed 体两行)→ R38 的 :71 会**上移约 2 行 → ~:69**。
- 因此**顺序敏感**:若两债分别独立改 part_repo.py,**R39 先删会使 R38 的目标行漂移**;反之 R38 先删不影响 R39。

**结论(与 deps_hint 一致)**:part_repo.py 的 R38 份与 R39 **必须同批改、同一次提交**(Phase1 边 R38↔R39),由人按符号名(非死记行号)逐个删,杜绝行号二次漂移。两者都是 part_repo.py 内零消费删除,同批无语义冲突,只有行号坐标冲突。dict 键位移:本簇删除的是方法 def,不涉及任何 dict 字面量的键顺序,**无 dict 键位移风险**。

## 6) 同收口点债

R38 修法为**纯直删,无收口点**(不收口到任何统一符号)。因此不存在「收口到同一符号的其他债」,**无同收口点顺序敏感性**。簇 C02 内若有其他 list_as_dicts 类死簇债,各自独立删除,互不收口。

## 7) 收口行为差异及所需 parity 测试

**不适用**：R38 是纯直删而非收口,新旧无两路可比,不存在「边界值逐分支等价」问题,**无需 parity 测试**。

需要的是「删除安全性回归」而非 parity：删除后跑一遍现有用例,确认无 `AttributeError: ...list_as_dicts`(印证确无隐藏调用者)。已通过 rg + callgraph 双重静态证零引用,运行期回归仅作兜底确认。

## 8) 爆炸半径

九字段(基于回盘 + callgraph):

- **生产消费者**:三处 `list_as_dicts` 生产**全零引用**——rg 全仓(core/web/data .py)排除 def 行后零调用;callgraph 三处均**仅出边(→fetchall)、零入边**。
- **测试消费者**:**无**(tests/ 扫描零命中,callgraph 零入边)。
- **契约/拓扑测试**:无。
- **下游联动**:无任何页面/Service/算法引用这三方法,删除**不触发任何运行路径变化**。
- **co_change(组内同改)**:R39(part_repo.py 同文件,行号坐标耦合)。
- **fix_invalidation_risk**:none(删除幂等,无后续债依赖这三符号存在)。
- **lb 共址危险**:none(三 repo 均非承重文件,见 §9)。
- **灾难链(删错/统一错会静默炸什么)**:
  1. 若误把三处当「同体」抽 helper 再删,会留下零消费 helper(活死代码),违收口规则——已规避(纯删)。
  2. 若漏删某处或删错相邻方法(如 part_repo.py 误删 :32 list_unparsed 当成 :71)——靠**按符号名删**而非行号规避;list_unparsed(R39)同样零消费,即便误删也不静默炸生产,但记账会乱,故两债同批由人核符号名逐删。
  3. 删除后若 DB schema 演进新增了对这三表的 dict 化需求,需重新实现——但这是未来需求,非本次回归风险。
- 总评:**爆炸半径 = 零**。三处死代码,删除无任何生产/测试消费者受影响,最大风险仅是 part_repo.py 内与 R39 的行号坐标耦合(已用同批 + 按符号删化解)。

## 9) 承重毗邻标记

- registry `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none`。
- 三 repo 文件均无 LB 标记,文件内未见 ROLE_ADOPTED / scenario 硬钉等承重模式(三 repo 仅基础 CRUD + 引用检查,见 op_type_repo.py:79+ `has_machine_reference`、operator_repo.py:91+ `is_referenced_by_batch_operations`,与本债符号无关）。
- **无承重禁区行**。本债不触及任何承重逻辑,§3 灵魂线/承重铁律不施加额外约束。

## 10) 分层 / 导入环风险

- 修法为**纯删除**,不新增任何 import,**不可能引入越层 import 或导入环**。
- 现有三文件 import 头(回盘)：均为 `from core.models import OpType/Operator/Part` + `from .base_repo import BaseRepository`(operator_repo.py 另有 `from .reference_checks import ...`)。删除 list_as_dicts 不动这些 import。
- 分层红线(core.algorithms→core.services、core.models→core.services 不存在):本债在 data/ 层,删除不触及任何跨层边,**0 AST 违规风险**。

## 11) 测试耦合

- 现有测试对三符号**零引用**(tests/ 扫描零命中、callgraph 零入边)。
- **动手前无需迁移任何测试**。删除后仅需跑一遍现有 data/repositories 相关用例确认无 `AttributeError`(见 §7),无测试需先行改造。

## 12) 前置依赖

- **无 F 门前置**:本债无跨边、无契约拓扑测试、severity low,可任意排期(planned_batch_hint:Batch-13,low,无跨边可任意排期)。
- **唯一硬前置(同批约束)**:part_repo.py 一份必须与 **R39 同批同提交**改 part_repo.py(避免行号二次漂移,Phase1 边 R38↔R39)。op_type_repo.py / operator_repo.py 两份无同文件债,可随 R38 簇自由安排。
- 不依赖任何其他债先做;不阻塞其他债。建议执行顺序:同一提交内,先按符号名定位 R38(part 份)与 R39 两个 def,一并删除,再删 op_type/operator 两份。
- owner_pending=false,无须 owner 裁断,可直接进入执行批次。

## 对抗核验(第二双眼睛)

回盘时点 2026-06-05;核验 agent 独立 rg/grep 重盘,只读不改。

**1) 行号复核(一致)**
- `rg -n "def list_as_dicts" data/repositories/` → op_type_repo.py:73 / part_repo.py:71 / operator_repo.py:85,与档案 §1 逐一吻合,**漂移幅度 = 0**。
- `sed` 验 def 体:三处均为单行 `return self.fetchall(<SQL>)`,SQL 各异(`op_type_id,name,category`/`part_no,part_name,route_raw`/`operator_id,name,status,remark,team_id`),坐实「结构同形非字面同体」,档案 §3「禁止删一份代表三份」结论正确。

**2) 零消费复核(挑战通过)**
- `rg -n "list_as_dicts" --type py core/ web/ data/ tests/` → 仅三条 def 行,排除后**零调用**。
- 动态调用挑战:`rg "getattr.*as_dicts|'list_as_dicts'|\"list_as_dicts\"|as_dicts\("` → 零命中(无 getattr/反射、无字符串拼方法名)。
- 收口分发挑战:`rg "as_dicts" --type py .` → 全仓仅这三处,**不存在任何同名 as_dicts 收口点/动态分发**,印证「无收口点,纯删除」。
- 模板/全文挑战:`rg "list_as_dicts" -g '!docs/' .` → 仅三条 def 行,无模板字符串引用。

**3) 相邻债 R39 复核(一致)**
- `rg -n "def list_unparsed" part_repo.py` → :32(吻合 deps_hint);`rg "list_unparsed" --type py .` → 仅一条 def 行,R39 同为零消费。§5 行号位移分析(R39 在上方,先删 R39 会使 R38 上移 ~2 行,故需同批按符号名删)成立。

**4) 承重/分层复核**
- registry `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none`;三处为单行 fetchall 死叶子,无 ROLE_ADOPTED/scenario 硬钉。**无承重误删风险**。
- 修法为纯删除,不新增 import,不触跨层边,0 AST 违规。无兜底/吞错新增,符合灵魂线。

**5) 现场状态复核**
- `git diff --stat b08162cd -- 三文件` → 空(UNCHANGED),与 status=planned 一致。

**核验结论:PASS。** 行号一致、三处真零引用(含动态/字符串/模板挑战均零反证)、非承重、R39 相邻关系准确、修法(三笔独立直删·禁抽 helper·part 份与 R39 同批)无误删风险。低危纯死叶子债,无需第三方裁断。
