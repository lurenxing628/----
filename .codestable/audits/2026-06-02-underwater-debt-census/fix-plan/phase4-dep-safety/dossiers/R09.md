# R09 逐债档案 · _positive_int 可空正整数助手三处复制（执行车道）

> 本档案只读不改、只产计划。registry old_location 行号不可信，下述 file:line 均为 2026-06-05 当前工作区 rg/grep 回盘。
> owner_pending=true → 仅做依赖/爆炸半径透析，**不给终态修法、不分配执行批次**，待 owner 裁断。

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

R09 = 同名私有助手 `_positive_int` 在执行车道的复制。按符号名 rg 回盘三处定义点：

| 副本 | old_location（不可信） | 当前真实 file:line（回盘） | 漂移 | 副本契约 |
|---|---|---|---|---|
| A 服务层 | `resource_dispatch_execution_service.py:23` | `core/services/scheduler/resource_dispatch_execution_service.py:24`（def，body 25-29） | +1 行 | 原始 `int(value)` |
| B 视图模型 | `web/viewmodels/scheduler_resource_dispatch_execution.py:32` | `web/viewmodels/scheduler_resource_dispatch_execution.py:33`（def，body 34-38） | +1 行 | 原始 `int(value)` |
| C 路由上下文 | `web/routes/.../scheduler_resource_dispatch_execution_routes.py:37`（旧路径） | `web/routes/domains/scheduler/scheduler_resource_dispatch_execution_context.py:28`（def，body 29-33） | **换文件**（routes→context，B01 已搬）+ 行号 37→28 | **已不是字节同体**：包 `parse_positive_execution_int`（core/models/operation_execution_scope.py:9）+ catch ValueError→None |

漂移结论：
- A/B 仅 +1 行小漂移，仍是原始 `int(value)` 字节同体。
- **C 已发生质变**：B01 重构把 routes 里的副本搬到 `scheduler_resource_dispatch_execution_context.py:28`，并改写为委托 `core.models.operation_execution_scope.parse_positive_execution_int` 后 catch `ValueError→None`。原 `_routes.py` 里 `_positive_int` 已 grep=0（已不存在）。
- registry `location_status` 写「三处字节同体 _positive_int」**与现状不符**：当前只有 A、B 字节同体，C 是行为异构的二次封装（见字段 7 反例）。

调用点回盘（grep core/web/data）：
- A：service.py:169(`_op_ids`)、:180/:181(`_matching_row`)。
- B：viewmodels:257、:258(`build_task_card`)、:353（任务列表）。
- C：context.py:96/:97/:107/:258/:271/:272（`_row_matches_feedback_target`、payload/args 取 schedule_id）。

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`；本档案沿用 **planned**。
- `current_evidence_2026_06_05` 自述：三份私有拷贝仍在、批准的新 sink `parse_optional_positive_int` 全仓 grep=0（F3 未建）、无 sink/护栏注释 → 回盘**全部复现**：`rg parse_optional_positive_int` = 0 命中（sink 未创建）；三处 `_positive_int` def 均在。
- **一处与 evidence 措辞不一致须纠**：evidence 与 `location_status` 都说「三处字节同体」，但 C（context.py:28）已被 B01 改成委托 `parse_positive_execution_int` 的异构封装，**非字节同体**。状态 planned 本身一致，但「字节同体」前提已失效（影响修法，见字段 7）。

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 **P5**（重复实现/应收口到单一 sink）；`needs_adversarial=true` 且 `adv_refuted=true` → 真债已确认（非误报）。
- 严重度 **low**（纯重复，无安全不变量寄生在本助手身上——安全闸由别处 raise 守，见字段 8）。
- 桶 **B05**（home=B05）；簇 **C01**（数字解析助手簇）。
- `kind=real`，`load_bearing=false`，`lb_no_touch=null`。

## 字段 4：修法类

**修法类 = 收口到 X（唯一批准的新建例外）。但 owner_pending=true → 待裁，暂不给终态修法。**

- PHASE0 §4 唯一批准在本簇新建收口点：`core/shared`（hint 写 `number_utils`）新建 `parse_optional_positive_int(value)`，契约「垃圾/blank/TypeError→None、≤0→None、绝不 raise」。这是 80 债里**唯一允许新建第二实现的 P5 例外**。
- 承重判定：`load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none` → 三文件均非 LB，故不受「承重仅注释」铁律限制，**允许收口**。
- 灵魂线核对：sink 契约保持「坏值→None 跳过坏行」，**不引入新兜底/静默回退**（这是忠实保留现有 3 副本既有的「跳过坏行」语义，不是新增防御）；不得 except 吞错扩面、不得加形参。
- **严禁**（写进禁区）：
  - 严禁复用 R04 扩出的 `core/shared/number_utils.parse_finite_int`（契约相反：parse_finite_int 对垃圾 **raise**，本助手要 **None**）。
  - 严禁改用 STRICT 变体（`operation_execution_feedback_support.py:161` `_positive_int(value, field)`→raise、`operation_execution_event_repo.py:100` `_required_positive_int`→raise、`scheduler_public_errors.py:167` `_positive_int`→raise）——它们是写入闸门，不在本簇。
  - 严禁删/统一 C（context.py:28）那条委托链当作字节同体直接换 sink（行为不等价，见字段 7）。
- **owner 待裁要点**：①sink 落 `core/shared/number_utils` 还是新模块（hint 给了 number_utils，但该文件现含 raise 语义的 parse_finite_int，同文件放 None 语义同名近义函数有混淆风险，owner 须拍）；②C 副本是否一并收口（C 行为已异构，收口需先消除 float 分支差异，见字段 7）。

## 字段 5：同文件碰撞债（siblings）

`same_file_siblings=[R07, R08]`，外加 interference_edges 标的同文件债：

| sibling | 同文件 | 改哪段 | 桶/病理 | 谁先谁后 | 撞行/键位移风险 |
|---|---|---|---|---|---|
| **R07** | `resource_dispatch_execution_service.py`（=A 副本文件） | P4 改 loud raise（与 `_positive_int` 不同段，R07 在别处兜底点） | B01 | **R07 先 / R09 后**（强串行，phase1_blast 硬边） | R07 改后 A 副本 def 行号（现 24）会下移，R09 迁 A 时须在 R07 落地后的稳定行号上重盘 |
| **R08** | 现 `scheduler_resource_dispatch_execution_routes.py`（routes，**非 C 所在的 context.py**） | P6 删死分支 | B01 | **R08 先 / R09 后** | R08 删分支在 routes，C 副本现已搬到 context.py，**不同文件不直接撞行**；但 SP05 拓扑契约钉 routes 模块路径，R08 删分支不触发，移动/重命名才红 |
| R15/R17 | `operation_execution_feedback_support.py` | 与本债无符号交集（该文件含 STRICT `_positive_int` :161，是禁区） | — | 无直接顺序约束 | 不碰 |
| R20 | `scheduler_resource_dispatch_execution.py`（viewmodels 同名簇文件） | 同 B 副本文件域 | — | owner 须确认 R20 是否动 B 所在文件行号 | 若 R20 改 viewmodels 上半部，B 副本（:33）行号可能位移，R09 迁 B 须重盘 |

碰撞核心：**R09 强串行于 B01-structural（Batch-3）之后**。R07/R08 都在 B01、都改本债同名簇文件，B01 落地后 A/C 所在文件行号会变，R09 必须在 B01 完成后重新 grep 回盘再迁，**严禁照当前行号迁**。

## 字段 6：同收口点债 + 顺序敏感性

- R09 的批准 sink `parse_optional_positive_int` 是**全新点，grep=0**，当前**无其他债收口到它**（独占新 sink）。→ 同收口点债：**无**。
- 反向约束：`co_change_within_group=[R04, R28, R59]` 是 B05 桶内同组协变债，但它们**不共用本 sink**。R04 收口到 `parse_finite_int`（raise 契约），与本债 sink 契约相反 → **严禁两债共用同一函数**（phase1_blast `fix_invalidation_risk` 明列）。
- 顺序敏感性：sink 必须**先建（F3）再迁 3 处**；3 处迁移彼此独立（不同文件、各自 import），无内部顺序约束，但整体须在 R07/R08（B01）之后。

## 字段 7：收口行为差异 + parity 测试（反例）

**关键对抗发现：三副本不逐分支等价，registry「字节同体」前提对 C 失效。**

2026-06-05 纯逻辑 parity 探针（不导入生产代码，复刻三副本 + hint 提议的 sink）跑边界值，差异分支：

| 输入 | A/B 原始 int() | C（parse_positive_execution_int 委托） | hint sink（=A/B） | A==C? | sink==C? |
|---|---|---|---|---|---|
| `5.9`(float) | `5`（int 截断） | **None**（非 int/非 digit text → raise→None） | `5` | ✗ | ✗ |
| `3.0`(float) | `3` | **None** | `3` | ✗ | ✗ |
| `True`(bool) | `1` | **None**（scope fn 显式 reject bool） | `1` | ✗ | ✗ |
| `'5.9'`/`'0x10'`/`'-3'`/`0`/`None`/`''`/`'abc'`/`'٣'`(阿拉伯数字3)/大整数 | 一致 | 一致 | 一致 | ✓ | ✓ |

反例结论：
- **A/B（原始 int）对 float 与 bool 宽松**：`5.9→5`、`True→1`。
- **C（parse_positive_execution_int）对 float/bool 严格拒绝→None**。
- hint 提议的 sink「字节对齐 A/B 的 int(value)」→ 收口 A/B 零漂移✓；但**收口 C 会把 C 的 float/bool→None 行为改成 float→截断、bool→1**，是**静默行为漂移**，违反「忠实收口零漂移」。
- 现实暴露面评估：A/B/C 的 op_id/schedule_id 来源——A 来自可信 DB 行（`row.get`，应为 int）、C 既比对 DB 行又解析用户输入（`request.args.get`/payload，来字符串）。DB 行理论不带 float，但 **C 经由用户输入路径**，`5.9`/`True` 这类异常输入在 C 当前被拒为 None（安全），换 sink 后变 `5`/`1`（放宽），属语义放松——low severity 但**确为可观测行为变化**。

**所需 parity 测试（收口前必须先写、且必须区分两路）**：
1. `test_parity_optional_positive_int_AB`：对 A/B 旧实现 vs sink，遍历 {int, 正/负/零 str, blank, 垃圾 str, 大整数, 阿拉伯数字} 断言逐值相等。
2. `test_parity_optional_positive_int_C_float_bool`：**专钉 C 分歧**——断言 `5.9`、`3.0`、`True` 在「收口 C 前后」是否被接受变化；若 owner 决定收口 C，须先决定取哪条契约（保 C 的严格 None 还是放宽到 A/B 截断），并据此**调整 sink 或保留 C 不动**。
3. 若 owner 决定**C 暂不收口**（只收 A/B）：加注释钉死「context.py:28 因 float/bool 契约差异暂不并入 sink」，避免后续误并。

## 字段 8：爆炸半径（消费者清单 + 灾难链）

**生产消费者**（回盘 file:line）：
- A `resource_dispatch_execution_service.py`：:169(`_op_ids` 跳过坏行)、:180/:181(`_matching_row` 比对可信 DB 行)。
- B `web/viewmodels/scheduler_resource_dispatch_execution.py`：:257/:258(`build_task_card`，注意外层有 `or 0` 兜底)、:353（任务列表项 op_id）。
- C `web/routes/.../scheduler_resource_dispatch_execution_context.py`：:96/:97(`_row_matches...` 比对行)、:107(payload schedule_id)、:258(request.args schedule_id)、:271/:272(target 比对)。

**测试消费者**：
- `tests/gate_meta/test_sp05_path_topology_contract.py:166/516`（钉 routes 模块路径，删函数体不触发，移动/重命名才红）。
- `tests/resource_dispatch/test_resource_dispatch_workbench_lane_contract.py`
- `tests/resource_dispatch/test_resource_dispatch_site_records_frontend_contract.py`
- `tests/resource_dispatch/test_scheduler_dispatch_plan_identity_guard.py`
- `tests/resource_dispatch/test_scheduler_candidate_resource_dispatch_contract.py`

**灾难链（删错/统一错会静默炸什么）**：
1. **误用 parse_finite_int（raise 契约）替本助手** → 遍历可信 DB 行/任务卡渲染时遇任何非正/非整值不再「跳过坏行」而是**抛异常 → 整页任务卡/任务列表渲染崩**（downstream_effect 明列）。这是 R09 最大爆炸风险。
2. **把 C 当字节同体直接换 A/B 式 sink** → C 对 float/bool 由 None 放宽为截断/1 → 用户输入 `5.9` 类异常被当成有效 op_id=5 参与行比对 → `_row_matches_feedback_target` 可能误命中相邻行 → 现场记录写到错的任务上（静默错配，无报错）。低概率但语义错误。
3. **误删 STRICT 变体（:161/:100/:167）当重复** → 写入闸门失守，坏 payload 静默入库。**严禁碰**。
- 安全不变量**不寄生在本助手**：真正的安全 raise 在 routes（抛 SCHEDULE_CONFLICT、ValidationError）与 STRICT 变体上，本助手只负责「读时跳过坏行」，故收口本身不直接削弱安全闸——前提是不误用 raise 版替换。

## 字段 9：承重毗邻标记 + 禁区行

- 本债 `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none` → 三文件**无 LB 毗邻**。
- 但 interference_edges 标 **LB08**（`auto_assign_resource_errors.py` / `scheduler_public_errors.py`）为同簇承重邻居——**不在 R09 三文件内**，R09 不应触碰这两文件。
- **禁区行（绝不在本债动）**：
  - `core/services/scheduler/operation_execution_feedback_support.py:161-168` `_positive_int(value, field)→raise`（STRICT 写入闸）。
  - `data/repositories/operation_execution_event_repo.py:100` `_required_positive_int→raise`。
  - `core/models/scheduler_public_errors.py:167` `_positive_int→raise`。
  - `core/shared/number_utils.py` 内 `parse_finite_int`（R04 sink，raise 契约，禁复用）。
  - R07 在 service.py 的 raise 改造段、R08 在 routes 的删分支段（B01 领地，R09 不碰）。

## 字段 10：分层 / 导入环风险

- sink 落 `core/shared/number_utils`（hint 给定）→ shared 是最底层，被 core.services、web 引用合法，**无越层**。
- 三处迁移后的 import 方向：
  - A `core.services.scheduler → core.shared`：合法（service→shared 向下）。
  - B/C `web.viewmodels / web.routes → core.shared`：合法（web→core.shared 向下）。
- **0 越层风险**：不引入 `core.algorithms→core.services`、`core.models→core.services` 等禁止边；sink 在 shared 不 import 任何 services/web，无导入环。
- 注意 C 现状已 import `core.models.operation_execution_scope.parse_positive_execution_int`（core.models 合法被 web 引用）；若 C 改收口到 core.shared.sink，需同时评估是否还保留对 scope fn 的依赖（若 C 收口则 C 的 import 从 core.models 切到 core.shared，仍合法，但 owner 须确认 C 的 float/bool 契约取舍，见字段 7）。AST 违规预期 = 0。

## 字段 11：测试耦合

动手前先迁/先写：
1. **先写 parity 测试**（字段 7 的 1/2/3 项）——收口前置门，证明 sink 对 A/B 零漂移、对 C 的分歧已被 owner 拍定。
2. `tests/gate_meta/test_sp05_path_topology_contract.py:166/516`：删 `_positive_int` 函数体不触发该契约（它钉模块路径不钉符号），**但不得移动/重命名 routes/context 模块**，否则该契约红。R09 纯删函数体+换 import，安全。
3. 4 个 `regression_*` dispatch 契约测试：迁移后须全绿——它们覆盖任务卡/现场记录/plan_identity/候选资源派工，正是 A/B/C 消费者的回归网。R09 落地后跑这 4 个 + SP05 = 验收基线。
4. 不需要新建独立单测文件给 sink 以外的逻辑——R09 是纯收口，行为应被现有回归网 + 新 parity 测试完全覆盖。

## 字段 12：前置依赖

1. **F3（建 sink）**：`core/shared/number_utils` 新建 `parse_optional_positive_int`（PHASE0 §4 唯一批准新建点）。R09 无 F3 不能动。`parse_optional_positive_int` 当前全仓 grep=0，F3 未做。
2. **B01-structural（Batch-3）必须先落**：R07@B01（service.py 改 raise）、R08@B01（routes 删死分支）都改本债同名簇文件。B01 完成后 A/C 所在文件行号会变 → **R09 强串行排在 Batch-3 之后**（registry `planned_batch_hint`：Batch-6/B05 内、B01-structural 后）。
3. **owner 裁断**（owner_pending=true）：①sink 落点（number_utils 同文件混 raise/None 语义 vs 新模块）；②C（context.py:28）是否一并收口、若收口取哪条 float/bool 契约（字段 7 反例）。owner 未裁前**不进入执行批次**。
4. B01 落地后**重新 grep 回盘** A/B/C 真实行号再迁（严禁照本档当前行号，因 B01 会位移）。

**前置链总结**：owner 裁断 → F3 建 sink → B01(R07/R08) 落地 → 重盘行号 → 写 parity 测试 → 迁 A/B（C 视裁断）→ 跑 SP05 + 4 regression。

## 对抗核验（第二双眼睛）

核验人：独立 rg/grep 回盘 + 源码逐体复核，2026-06-05。结论 **PASS**（找不到反证）。

**重 grep 命令与结果：**
- `rg -n "_positive_int" core/services/scheduler/resource_dispatch_execution_service.py` → def **:24**（body 25-29），调用 :169/:180/:181。**与档案一致**。
- `rg -n "_positive_int" web/viewmodels/scheduler_resource_dispatch_execution.py` → def **:33**（body 34-38），调用 :257/:258/:353。**与档案一致**（:257/:258 外层 `or 0` 兜底已复核存在）。
- `rg -n "_positive_int" .../scheduler_resource_dispatch_execution_context.py` → def **:28**（body 29-33），调用 :96/:97/:107/:258/:271/:272。**与档案一致**。
- `rg -n "_positive_int" .../scheduler_resource_dispatch_execution_routes.py` → **0 命中**；但 `ls` 证 routes 文件仍存在（13351B）——档案「副本已搬 context、原 routes 副本 grep=0」**准确**（是符号搬走非文件删除）。
- `rg -n "parse_optional_positive_int" --type py` → **0 命中**，F3 未建。**与 evidence 一致**。
- `rg -n "def parse_positive_execution_int"` → `core/models/operation_execution_scope.py:9`。读源码确认 :10 `isinstance(value,bool)→raise`、:16 `text.isdigit()` 对 float-str/小数点 → raise。**C 的 float/bool 严格→None 反例独立复现成立**。
- 禁区行复核：`operation_execution_feedback_support.py:161` `_positive_int(value,field)→raise`、`operation_execution_event_repo.py:100` `_required_positive_int→raise`、`scheduler_public_errors.py:167` `_positive_int→raise`、`core/shared/number_utils.py:30` `parse_finite_int(...raise)`——**四处禁区行号全部命中，准确**。

**逐项裁决：**
1. **行号复核：一致**（A:24 / B:33 / C:28 全部对上，含 body 与全部调用点）。
2. **A/B 字节同体：确认**——两份函数体逐字相同（`int(value)`/`except (TypeError,ValueError)→None`/`>0`）。
3. **C 异构、registry「三处字节同体」失效：确认**——C 委托 `parse_positive_execution_int` 后 catch `ValueError→None`，对 float(`5.9→None`)、bool(`True→None`) 严格拒绝，与 A/B（`5.9→5`/`True→1`）分歧。档案对 registry 的纠错成立，**非误判**。
4. **最大爆炸风险（误用 raise 版 parse_finite_int 替本助手→整页渲染崩）：确认成立**，禁区标的准确。
5. **承重误删风险：无**——`load_bearing=false`/`lb_no_touch=null`，三文件非 LB；档案修法是「收口到唯一批准新建 sink」，**未混入对承重邻居（LB08/STRICT 变体）的删除/统一/透传/加形参**，且把 STRICT 三处 + parse_finite_int 明确划入禁区。安全不变量不寄生本助手（由 routes raise + STRICT 变体守）——独立复核成立。
6. **灵魂线：无违规**——sink 契约「坏值→None 跳过坏行」是忠实保留现 3 副本既有语义，非新增兜底/静默回退；C 现状的 catch ValueError→None 也是既有行为，收口不扩面。
7. **分层：0 越层**——sink 落 core/shared（最底层），三处迁移 import 方向均向下，无导入环。AST 违规预期 0。
8. **owner_pending 合规：确认**——档案全程标「待裁，不给终态修法、不分配执行批次」，字段 4/12 反复声明 owner 须裁 ①sink 落点 ②C 是否收口及 float/bool 契约取舍。**无擅自给终态、无擅自分批**。

**唯一可商榷点（不影响 PASS）：** registry `prod_consumers` 仍记旧 routes 行号（routes:109/111/117），而档案字段 8 已更新为 context.py 真实行号（:96/97/107/258/271/272）——档案比 registry 更新、与当前工作区一致，属档案正确纠偏，非档案错误。

**核验结论：PASS。** 无承重误删风险，无擅自终态/分批，C 异构反例独立复现成立，无需升级第三方裁断。
