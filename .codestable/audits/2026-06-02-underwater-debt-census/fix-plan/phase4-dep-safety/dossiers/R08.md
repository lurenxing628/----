# R08 逐债档案 — feedback_write_enabled 休眠开关 + 生产不可达的「保护未开启」死文案

> 主文件: `web/viewmodels/scheduler_resource_dispatch_execution.py`
> 桶 B01 / 病理 P6 / 严重度 low / 簇 C01 / 承重 false / owner_pending **true**
> 本档案只读不改，所有断言附当前工作区 file:line 证据（rg/grep 回盘，不信 old_location）。

---

## 字段 1 — 当前真实 file:line（回盘）+ 漂移记录

按符号名 rg 回盘（`web/viewmodels/scheduler_resource_dispatch_execution.py`）：

| 符号 / 分支 | old_location（不可信） | registry current_evidence | **实际回盘行号** | 漂移 |
|---|---|---|---|---|
| `_FEEDBACK_DISABLED_REASON =` 常量定义 | :24 | :25 | **:25** | 老档 +1 |
| `_fill_actual_disabled_reason` def | — | — | **:224** | — |
| 死分支①: `if not feedback_write_enabled: return _FEEDBACK_DISABLED_REASON` | :226-227 | :227-228 | **:227-228** | 老档 +1 |
| `build_execution_payload` def | — | :367 起 | **:337** | — |
| 死分支②: `elif not feedback_write_enabled: disabled_reason = _FEEDBACK_DISABLED_REASON` | :361-362 | :367-368 | **:367-368** | 老档 +6 |
| 读 `context.get("feedback_write_enabled")`（活）:333→ | :333 | :333 | **:339** | 老档 +6 |
| `build_available_actions` 活路径 `can_write and feedback_write_enabled` | — | :233 | **:234** | — |
| R09 同居体 `_positive_int` def | :32 | :32 | **:33** | 老档 +1 |

证据（rg 回盘）:
- `:25` `_FEEDBACK_DISABLED_REASON = "现场记录保护还没开启，暂不能填写现场记录。"`
- `:225-228`:
  ```py
  if not can_write:
      return _NOT_CURRENT_OFFICIAL_REASON
  if not feedback_write_enabled:
      return _FEEDBACK_DISABLED_REASON     # 死分支①
  ```
- `:365-368`:
  ```py
  if not can_write:
      disabled_reason = _NOT_CURRENT_OFFICIAL_REASON
  elif not feedback_write_enabled:
      disabled_reason = _FEEDBACK_DISABLED_REASON   # 死分支②
  ```

**漂移结论: 有漂移，幅度 +1～+6 行。** 与 registry `location_status: "✅准 — 未漂(committed≡基线)"` 的断言**冲突**：该文件在当前分支 `codex/aps-three-gap-directions` 工作区有未提交改动（`git status` = ` M`），且 `git diff` 显示文件头部新增一行 import（`:23` 引入 `resource_dispatch_execution_tokens`）+ `build_task_card` 体大改（task_key/state_key 重构、删 op_id/schedule_id 键），导致死码下游行号整体下移。死码三处（常量、死分支①②）本身**未被该 diff 触碰**，但其绝对行号已漂。

## 字段 2 — 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = "planned"`。
- 回盘: 死码（常量 :25 + 死分支① :227-228 + 死分支② :367-368）**原样存在，未删**，方案A 未执行。→ **与 status=planned 一致。**
- 偏差: registry 把「文件未漂、committed≡基线」当事实陈述写进 `location_status`，但实际工作区已被**其他 feature 改动**（task_key 重构）抬走行号。这不影响 R08 自身状态（仍是 planned/死码在），但**前置门 F 必须包含「等本文件其它在途改动落定后再动 R08」**（详见字段 12）。

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- **病理 P6**: 休眠开关（dormant switch）+ 生产不可达的死面包屑文案。`feedback_write_enabled` 这个参数在主路径与 `can_write_feedback` **同源恒等**，导致 `(can_write=True, feedback_write_enabled=False)` 组合在所有生产路径不可达；专为该组合写的「现场记录保护还没开启」文案因此真实用户**永不可见**。
- **严重度 low**: 纯死码，不改任何生产行为；删与不删对运行时无差异（不可达分支）。
- **桶 B01**（结构债 / 死码）。**簇 C01**（scheduler-dispatch 执行车道）。
- 不可达性证据（三条生产路径全覆盖）:
  1. 主路径 `get_execution_context`: `:127 can_write_feedback = bool(plan_role_fields.get("can_write_feedback"))` 与 `:130 feedback_write_enabled = bool(plan_role_fields.get("can_write_feedback"))` —— **同一 key、同一 bool()，恒等**。二者要么同 True 要么同 False，永不出现 (True,False)。
  2. 反馈卡路径 `task_card_for_feedback_context`（service :134，默认参 :139 `feedback_write_enabled: bool = True`）: 两个调用点 `web/routes/domains/scheduler/scheduler_resource_dispatch_execution_context.py:229` 与 `:246` 均**硬传 `feedback_write_enabled=True`**；而 `can_write_feedback` 取自 identity（可 True 可 False）。即此路径只可能出现 (T,T) 或 (F,T)，仍无 (T,F)。
  3. 空上下文 `_empty_context`（service :207）: `can_write_feedback=False, feedback_write_enabled=False` —— (F,F)。
  - 三路并集 = {(T,T),(F,F),(F,T)}，**(T,F) 不可达** → 死分支①②恒不命中。

## 字段 4 — 修法类

**修法类 = P6 死码删除（直删）。但 owner_pending=true → 待 owner 裁断，暂不给终态修法、暂不分配执行批次。**

owner 须裁断的核心问题: `feedback_write_enabled` 是否是「未来要上线的『现场记录保护总开关』占位」？若 roadmap（commit `65870e47` resource dispatch execution lane）近期要把该开关接到真实门禁，则它**不是死码而是预埋**，本债应转「补『我是故意的』注释 + 在 service :130 处钉 hypothesis 守卫」而非删。若确认无此规划，方可走删除。

待裁后两条候选（仅记录，不在本轮拍板）:

- **候选A（默认，最小删）**: 删死分支①（viewmodel :227-228）、死分支②（viewmodel :367-368）、死常量（viewmodel :25）。**保留 `feedback_write_enabled` 参数**——它仍被活路径 `:234 fill_enabled = bool(can_write and feedback_write_enabled and ...)` 消费，非纯死参，不能连参数一起删。删后 `_fill_actual_disabled_reason` 只剩 `if not can_write` + 兜底文案；`build_execution_payload` 的 disabled_reason 只剩 `if not can_write`。
  - 关键: 删后**新值与旧值在所有可达组合上逐分支等价**（因 (T,F) 不可达，删的是恒不命中的支），属安全删。
- **候选B（彻底参数收口，重）**: 把 `feedback_write_enabled` 整个并入 `can_write_feedback`。须改签名 4 处：service `task_card_for_feedback_context` 默认参 `:139`、两个调用点 `..._context.py:229/:246` 的 kwarg、test `tests/resource_dispatch/test_resource_dispatch_workbench_lane_contract.py:175`。**风险更高**，无必要，不推荐。

灵魂线检查（铁律4）: 候选A 删的是死分支 + 死文案，**不新增任何兜底/静默回退/except 吞错**；删后 `_fill_actual_disabled_reason` 末行 `return f"当前状态是{status_label}…"` 是既有兜底文案，不是新增。`build_execution_payload` 的 `disabled_reason` 仍由 `if not can_write` 显式赋值，无静默化。✅ 合规。

收口点检查（铁律5）: 候选A 是**删**不是收口，无新建模块。候选B 是「向已存在的 `can_write_feedback` 语义收口」（已存在概念，非新建第二模块）。两者都不触发新 P5。✅

## 字段 5 — 同文件碰撞债

registry `interference_edges` 列了 R09 与 R20 两条 same_file 边。**回盘纠错：**

- **R09**（severity 未变，B05 跨桶；title=「`_positive_int` 可空正整数助手在执行车道三处逐字节复制」）—— **真同文件**。本 viewmodel `:33 def _positive_int(value)` 是其三份副本之一。R09 修法是把三份 `_positive_int` 收口到一个统一助手（并按 R09 唯一获批例外 `parse_optional_positive_int`，垃圾/非正→None）。
  - **改哪段**: R08 改 `:25 / :227-228 / :367-368`（文件尾部偏上区）；R09 改 `:33`（文件头部 def）+ 把 `:257/:258/:353` 三处 `_positive_int(...)` 调用切到收口符号。两者**改的物理区段不重叠**。
  - **谁先谁后**: registry 定 R08 在 Batch-3(B01)、R09 在 Batch-6(B05)，**R08 先、R09 后，串行**。理由是「同文件同改须串行避免撞行号 / dict 键位移」。
  - **会否撞行号 / dict 键位移**: R08 候选A 删 3 行常量 + 4 行死分支（共约 -7 行），全在 `:25` 与 `:367` 区，**位于 R09 的 `:33` def 之后**——R08 删行会使 `:33` 以下所有行号上移，但 R09 的 `_positive_int` def 在 `:33`（R08 删点之上，不受影响），R09 的三个**调用点** `:257/:258/:353` 在 R08 删点上方（`:257/:258` 在 `:367` 之上）也基本不受死分支删除影响（死分支②在 `:367-368`，在 `:353` 之下）。结论：**R08 先删 → R09 的锚点行号轻微上移但不与 R08 删区冲突；反序（R09 先）则 R09 收口后行号变动会让 R08 的 `:367-368` 漂移更大**。故 **R08→R09 串行（B01 先于 B05）是正确顺序**，与 registry 一致。

- **R20** —— **registry 边错误**。回盘 R20 `primary_file = operation_execution_labels.py`（service 层垫片），**不在本文件**。`interference_edges` 把 R20 标 `same_file:true, file:scheduler_resource_dispatch_execution.py` 是误标。**R08 与 R20 无同文件碰撞**，可忽略该边。（已 grep registry 确认 R20.primary_file。）

**同文件碰撞结论: 唯一真碰撞 = R09，按 B01→B05 串行；R20 边作废。**

## 字段 6 — 同收口点债

- **R08 候选A 是删除，不收口到任何统一符号** → 无同收口点债。
- 若走候选B（收口 `feedback_write_enabled → can_write_feedback`）：收口目标 `can_write_feedback` 是已存在概念，但**当前无其它债声明也要收口到 `can_write_feedback`**（registry 中无此边）。故即便走B也无同收口点竞争。
- 顺序敏感性: 仅与 R09 的物理行号串行有关（字段5），与「收口符号竞争」无关。

## 字段 7 — 收口行为差异及所需 parity 测试

**新旧两路在所有可达组合上逐分支等价性分析（候选A）:**

`_fill_actual_disabled_reason(can_write, feedback_write_enabled, status_label)` 真值表:

| (can_write, feedback_write_enabled) | 旧返回 | 新返回（删死分支①后） | 等价? | 可达? |
|---|---|---|---|---|
| (F, *) | `_NOT_CURRENT_OFFICIAL_REASON` | `_NOT_CURRENT_OFFICIAL_REASON` | ✅ | 可达(空ctx/非官方) |
| (T, F) | **`_FEEDBACK_DISABLED_REASON`** | `f"当前状态是{status_label}…"` | ❌**不等价** | **不可达** |
| (T, T) | `f"当前状态是{status_label}…"` | `f"当前状态是{status_label}…"` | ✅ | 可达 |

`build_execution_payload` disabled_reason 同构（死分支② :367-368 对应 (T,F)，亦不可达）。

**唯一不等价格 = (T,F)，且已证三路并集不可达** → 在**可达输入域上新旧完全等价**。这是 P6 死码删除可安全进行的核心依据。

**反例边界（owner 必须确认）**: 若未来 `feedback_write_enabled` 被接到独立门禁（不再与 `can_write_feedback` 同源），(T,F) 立即变可达，删除即引入回归（本该显示「保护未开启」却显示「当前状态是X」）。这正是 owner_pending 的根因——**(T,F) 不可达性依赖 service :127≡:130 同源这一不变式，须钉守卫**。

**所需 parity / 守卫测试（动手时补，本轮不写）:**
1. 不变式守卫（最重要）: 在 `tests/resource_dispatch/test_resource_dispatch_workbench_lane_contract.py` 加一条断言 `get_execution_context` 返回的 `can_write_feedback == feedback_write_enabled`（钉死 service :127≡:130 同源），任何人未来拆开同源 → 测试红 → 阻止 (T,F) 偷偷变可达。
2. 删除前后 parity: 对 (F,*) 与 (T,T) 两可达组合断言 `_fill_actual_disabled_reason` / `build_execution_payload.disabled_reason` 删前删后逐字节一致。
3. **不需要** PIN 死文案 `'现场记录保护'`（全仓 grep 证零字符串断言，删不破任何现存测试）。

## 字段 8 — 爆炸半径

**生产消费者清单（回盘核实）:**
- `core/services/scheduler/resource_dispatch_execution_service.py:127` — `can_write_feedback = bool(plan_role_fields.get("can_write_feedback"))` ✅回盘准
- `core/services/scheduler/resource_dispatch_execution_service.py:130` — `feedback_write_enabled = bool(plan_role_fields.get("can_write_feedback"))`（与 :127 同源，**这是 (T,F) 不可达的源头**）✅ —— registry 写 `:120` **行号错**，实为 :130。
- `core/services/scheduler/resource_dispatch_execution_service.py:139` — `task_card_for_feedback_context(..., feedback_write_enabled: bool = True)` 默认参 ✅ —— registry 写 `:139,165` 中 :165 错，透传在 :160。
- `core/services/scheduler/resource_dispatch_execution_service.py:160` — 反馈卡路径透传（调用方恒传 True）✅
- `core/services/scheduler/resource_dispatch_execution_service.py:214` — `_empty_context` 两者皆 False ✅ —— registry 写 `:204` 行号错，实为 :214（def 在 :207）。
- `web/routes/domains/scheduler/scheduler_resource_dispatch_execution_context.py:229, :237, :246` — **三处硬传 `feedback_write_enabled=True`** ✅ —— **registry 重大错误**: 它写 prod_consumer 是 `..._routes.py:184,192,201`，但回盘 `scheduler_resource_dispatch_execution_routes.py` **零 `feedback_write_enabled` 引用**（:184/:192/:201 是 finish/pause 等 feedback_svc 调用，与本债无关）。真正调用点在 **`..._execution_context.py`** 不是 `..._routes.py`。
- `web/viewmodels/scheduler_resource_dispatch_execution.py:227-228, :367-368`（死分支消费）+ `:234` 活路径 `can_write and feedback_write_enabled` ✅

**测试消费者:**
- `tests/resource_dispatch/test_resource_dispatch_workbench_lane_contract.py:165` `build_task_card(...)` 调用，`:174 can_write_feedback=False, :175 feedback_write_enabled=False` —— 因 can_write=False 在 `:225`/`:235` 双重短路，死分支①②不执行；该测试只断言 part_label/actual_time_label/actual_delta（不触死文案）。删死分支**不破此测试**。
- 全仓 `tests/` grep `现场记录保护`/`_FEEDBACK_DISABLED_REASON`/`FEEDBACK_DISABLED` = **零命中**（已验证，repo-wide rg 只回主文件自身）。

**灾难链（删错/统一错会静默炸什么）:**
1. **若误把 `feedback_write_enabled` 参数连同分支一起删**（候选A 明令保留参数）→ 活路径 `:234 fill_enabled = bool(can_write and feedback_write_enabled and ...)` 与 `build_available_actions` 签名 `:232` 断裂 → 「填写实际」按钮的 enabled 判定塌成只看 can_write → 已完工/非可填状态也亮按钮 → **静默放开误填实际记录**。这是最大爆炸风险。
2. **若 owner 误判，删后未来真接总开关** → (T,F) 变可达 → 该显示「保护未开启」处显示「当前状态是X」误导文案 → 用户被引导去等状态而非等开关 → 软性误导（low）。
3. **若反序先做 R09** → R08 的 :367-368 锚漂移变大，删错相邻行风险升（机械风险，非语义）。

## 字段 9 — 承重毗邻标记

- `load_bearing = false`，`lb_no_touch = null`。
- 本文件（viewmodel）无任何 LB 标记的承重逻辑同居。唯一同文件耦合 = R09 的 `_positive_int`（:33，B05），非承重。
- **无禁区行**（无 load_bearing=true 邻债）。但有一条「事实不变式禁区」需 owner 钉守卫而非物理禁改: service `:127 ≡ :130` 同源关系是 (T,F) 不可达的根，删死码前应在测试钉住，使其成为「故意的契约」而非偶然巧合。

## 字段 10 — 分层 / 导入环风险

- **候选A（删）零导入变化** → 无越层、无导入环。删的是 viewmodel 内部分支与本文件常量，不新增任何 import。
- **候选B（收口）**: 也只改既有 kwarg 名/默认值，不新增模块、不新增 import → 无越层风险。
- 现状分层正确: `web/viewmodels/` → 依赖 `core/models/*`、`core/services/scheduler/resource_dispatch_execution_tokens`（工作区新加的 import，:23），方向 web→core 合法。无 `core.algorithms→core.services` / `core.models→core.services` 之类违规。
- **0 AST 违规**（铁律6）✅：本债任何候选都不引入跨层 import。

## 字段 11 — 测试耦合

**动手前需迁/补的测试:**
- **无需迁移现存测试**: 唯一触及本符号的测试 `tests/resource_dispatch/test_resource_dispatch_workbench_lane_contract.py:165` 走 (F,F) 路径、断言项不含死文案，删死分支后仍绿。
- **需新增（动手时，本轮不写）**: 字段7 列的不变式守卫（service :127≡:130）、(F,*)/(T,T) parity 断言。
- 候选B 额外耦合: 须同步改 `:175 feedback_write_enabled=False` 这行测试 kwarg（删 kwarg）——这是候选B 比候选A 多出的测试改动点，也是不推荐 B 的理由之一。

## 字段 12 — 前置依赖

先做 / 先满足的门，才能动 R08:

1. **F-owner（硬门，owner_pending）**: roadmap owner（commit `65870e47` resource dispatch execution lane）须确认**近期无『现场记录保护总开关』上线规划**。未确认前不删——可能是预埋而非死码。
2. **F-worktree-settle（硬门，本轮新发现）**: 本文件当前工作区有未提交的 task_key/state_key 重构改动（`build_task_card` 大改）。须等该在途 feature 落定（合并/丢弃）后再动 R08，否则 R08 删行会与在途 diff 撞，且行号会再漂。
3. **F-invariant-guard（建议先于删）**: 先在测试钉 service :127≡:130 同源不变式（字段7 守卫1），再删死分支——把「(T,F) 不可达」从偶然变契约。
4. **批次顺序**: R08 ∈ Batch-3(B01-structural)，须**先于** R09 ∈ Batch-6(B05) 动本文件（同文件串行，避免 R09 收口后 :367-368 再漂）。R07（`resource_dispatch_execution_service.py`，同执行车道 service）为协同改组，非硬前置。

**结论**: R08 本身依赖透析完毕，但因 owner_pending=true + 在途改动未落定 + 需先钉守卫，**暂不分配执行批次、暂不给终态修法**；待三门满足后按候选A（保参数、删分支删常量）执行，R08→R09 串行。

---

## 返回摘要

R08 死码删除（P6），owner 待裁。真实行号已漂（+1～+6），与 registry「未漂」断言冲突；registry 的 routes 消费者 :184/192/201 与 R20 同文件边均为误标，已纠正。最大风险=误删参数导致填写按钮门禁塌缩静默放开误填。R09 同文件串行（B01 先于 B05）。

---

## 对抗核验（第二双眼睛）

**核验日期 2026-06-05｜独立 rg/grep 回盘，只读不改｜结论：PASS**

### 重 grep 命令与结果（关键反证）

1. **死码三处行号** — `rg -n "_FEEDBACK_DISABLED_REASON|feedback_write_enabled" web/viewmodels/scheduler_resource_dispatch_execution.py`
   - 常量 `:25`、死分支① `:227-228`、死分支② `:367-368`、def `:224/:337`、活路径 `:234`、`_positive_int :33`、test `:174-175`。**与档案字段1 逐行一致**。

2. **service 同源不变式** — `rg -n "can_write_feedback|feedback_write_enabled" core/services/scheduler/resource_dispatch_execution_service.py`
   - `:127 "can_write_feedback": bool(plan_role_fields.get("can_write_feedback"))`
   - `:130 "feedback_write_enabled": bool(plan_role_fields.get("can_write_feedback"))` —— **同 key 同 bool()，恒等坐实**。
   - `:139 feedback_write_enabled: bool = True`（默认参）、`:160` 透传、`:211/:214 _empty_context 全 False`。registry 的 `:117/:120/:165/:204` 行号全错，档案纠正（:127/:130/:160/:214）正确。

3. **routes vs context 消费者之争** — `rg -n "feedback_write_enabled" web/routes/domains/scheduler/scheduler_resource_dispatch_execution_routes.py`
   - **零命中**。registry 把 prod_consumer 写成 `routes.py:184/192/201` 是**实打实误标**。
   - `rg -n "feedback_write_enabled" ..._execution_context.py` → `:229/:237/:246` 三处**硬传 `feedback_write_enabled=True`**。档案纠正正确。

4. **(T,F) 不可达三路并集复核**：主路径 :127≡:130（同 True/同 False）∪ 反馈卡路径 identity×硬传True ∪ _empty_context(F,F) = {(T,T),(F,F),(F,T)} → **(T,F) 不可达**确认。
   - **额外发现（强化死码判定）**：`build_execution_payload :373 "guardrail_text": "" if can_write else disabled_reason` —— 当 `can_write=True` 时 guardrail_text 恒空，故 :367-368 在 (T,F) 下写入的 `_FEEDBACK_DISABLED_REASON` **即便假设可达，下游输出处也被 can_write 二次短路**。死码有双层护城河，删除更安全。

5. **R20 误标复核** — registry `R20.primary_file = operation_execution_labels.py`（model 同名模块转出垫片），**不在本文件**。interference_edge `same_file:true` 误标确认作废。

6. **零测试 PIN** — `rg -n "现场记录保护|FEEDBACK_DISABLED" tests/` → **零命中**。删死文案不破任何现存断言，坐实。

7. **git diff 实情澄清** — `git diff` 本文件触碰的是 `build_task_card` **输出 dict 键**（`op_id`/`schedule_id` 从输出键移除 + `event_payload`/`execution_result_payload` 改键）+ 头部 +1 import。**死码三处本身未被 diff 触碰**，仅绝对行号被上方 diff 抬移。档案字段1「删 op_id/schedule_id 键」指输出键，与 :257/:258/:353 的 `_positive_int(row.get(...))` **读取**（不同位置）不矛盾，表述自洽。registry `location_status:"未漂/committed≡基线"` 确属误判。

### 承重 / owner_pending 合规审查

- **承重误删风险：无**。`load_bearing=false`、`lb_no_touch=null`，本文件无 LB 邻债。候选A 明令**保留 `feedback_write_enabled` 参数**（防 :234 门禁塌缩），未混入删参数/统一/透传/加形参；候选B 仅记录不拍板。灵魂线：候选A 不新增兜底/静默回退/except 吞错，:229 与 :365 兜底文案均为既有，合规。
- **owner_pending=true 合规**：档案明确「暂不给终态修法、暂不分配执行批次」，A/B 两候选仅记录待裁，未擅自拍板或分批。✅
- **分层**：候选 A/B 均零 import 变化，无越层、无导入环，0 AST 违规。✅

### 争议点
**无**。档案对 registry 三处误标（routes 消费者、R20 同文件边、location 未漂断言）与多处行号偏移的纠正经独立回盘全部坐实；不可达性论证成立且有双层证据。<br>
唯一需 owner 裁断的实质问题（`feedback_write_enabled` 是否为未来「现场记录保护总开关」预埋）属业务规划判断，非档案缺陷——档案已正确标 owner_pending 并要求先钉 :127≡:130 守卫，处理得当。

**核验结论：PASS。**

---

## 2026-06-10 终态补登（G22 收口，R08 → fixed）

详见 `_registry.json` R08 current_evidence 与 PHASE4 计划 §1.4 G22 补登段。要点：R08 按候选 A 删死常量+两死分支、保 feedback_write_enabled 形参（未来总开关参数面零破坏）；R09 把 A/B 两副本收编为 `parse_positive_execution_int` 薄 wrap（C 路同形态），两路 parity 锁定合法面零漂移+严格面 O01 统一；STRICT-4/scope:36-50/persistence_errors:13 全程未碰。可达组合 parity 与收编 parity 均落 `tests/resource_dispatch/test_resource_dispatch_workbench_lane_contract.py`。
