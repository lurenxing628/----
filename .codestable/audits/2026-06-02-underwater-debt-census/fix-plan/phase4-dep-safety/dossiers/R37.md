# R37 逐债档案 · operator_machine_repo.list_links_with_machine_names 死方法

> **✅ 2026-06-08 B 执行补登**：R37/G36 已 fixed。已删除 `data/repositories/operator_machine_repo.py` 中的死方法 `list_links_with_machine_names`；生产/测试源码内该符号零命中。活近亲 `list_links_with_operator_info` 保留并上移到 `operator_machine_repo.py:82`，facade、设备页与 `tests/models_domain/test_query_services.py` 仍继续使用它。

> 债 id: R37 | 桶: B12 | 簇: C01 | 病理: P6 | 严重度: low | 承重: false | owner_pending: false
> 主文件: data/repositories/operator_machine_repo.py
> 本档案原为执行前计划；2026-06-08 已补登执行后终态。未特别标“执行前”的行号，均以执行后当前盘为准。

---

## §1 执行后真实状态（grep 回盘）+ 执行前旧锚

执行后回盘命令 `rg -n "list_links_with_machine_names" data core web tests -g "*.py"`，生产/测试源码 **0 命中**（退出码 1）：

- **死方法 def**：已删除；旧锚为 `data/repositories/operator_machine_repo.py:82-90`，仅作为执行前证据保留，后续禁按旧行号施工。
- **活近亲 def**：当前为 `data/repositories/operator_machine_repo.py:82`
  `    def list_links_with_operator_info(self) -> List[Dict[str, Any]]:`
- **活近亲方法体边界**：当前 `:82`（def）→ `:90`（SQL fetchall 的闭合 `)`）。`:92` 起是下一个活方法 `list_simple_rows_for_machine_operator_sets`。

**执行前旧锚记录**：2026-06-05 回盘时 `list_links_with_machine_names` 位于 `:82-90`，`:92` 为活近亲。2026-06-08 删除死方法后，活近亲上移到 `:82`。

> 注：后续如再进入本文件，必须按符号名重新 `rg`，不得照执行前旧锚 `:82-90` 再删。

## §2 当前状态 + 与 status evidence 是否一致

- 2026-06-08 B 执行后，registry `status_2026_06_05 = fixed`。
- 执行前 current_evidence_2026_06_05 称「git diff b08162cd UNCHANGED，未删」；当时方法仍在 `:82`。
- 执行后 `list_links_with_machine_names` 已删；活近亲 `list_links_with_operator_info` 保留并上移到 `:82`。

**一致。** 状态 fixed 与「死方法已删、活近亲仍在」证据吻合，无矛盾。

## §3 病理 / 严重度 / 桶 / 簇

- **病理 P6**：被 facade 兄弟方法淹没的死查询。同 repo 类 `OperatorMachineRepository` 暴露了一排命名对称的 `list_*` 方法，facade `operator_machine_query_service.py` 代理了其中 5 个（`list_simple_rows` / `list_with_names_by_machine` / `list_with_names_by_operator` / `list_links_with_operator_info` / `list_simple_rows_for_machine_operator_sets`，见 :95-112），**唯独漏掉 `list_links_with_machine_names`**——该方法在 facade 层从未挂出，自然全仓零调用，被对称兄弟「淹没」成死方法。
- **严重度 low**：纯死代码、零引用、零跨边、不承重。删除收益仅清债，无功能影响。
- **桶 B12 / home B12**；**簇 C01**；partition = data-repos；kind = real（非误报死债）。

## §4 修法类

**已直删**（planned_fix_class = 直删；owner_pending = false，2026-06-08 已完成）。

- 已删除 `data/repositories/operator_machine_repo.py` 旧 `:82-90` 整个 `list_links_with_machine_names` 方法（含其 SQL 体），连同方法间孤立空行一并清理。
- 零引用点，无需同步退场任何调用方；执行后源码内该符号 0 命中。
- **非收口债**：本债不收口到任何统一点（病理是死代码而非分叉），故不触发铁律 5 的「收口必须落已存在点」约束，也不新建模块。
- **不承重，但属高误删风险类**：死方法与活近亲 `list_links_with_operator_info` 仅一字之差（machine_names vs operator_info）。执行后活近亲当前在 `:82`，后续禁再按旧行号 `:82-90` 施工。
- 不触灵魂线红线（无兜底/静默/吞错改动，纯删除一个 SELECT 查询方法）。

## §5 同文件碰撞债（siblings）

`same_file_siblings = []`。`operator_machine_repo.py` 在本批 80 债中**仅此一债**。

**结论：同文件零碰撞。** 不存在「谁先谁后、撞行号、dict 键位移」问题。删除旧 `:82-90` 后，下方兄弟整体上移；当前活近亲 `list_links_with_operator_info` 已在 `:82`，`list_simple_rows_for_machine_operator_sets` 已在 `:92`。因无第二条债定位本文件，R37 不再留下待施工锚点。

## §6 同收口点债

本债为**直删**、非收口，无收口点。故不存在「收口到同一符号的其他债」与顺序敏感性问题。

- 旁证：唯一 `machine_names` 后缀的活方法是 **另一个 repo** 的 `machine_downtime_repo.list_active_overlaps_with_machine_names`(:57，被 `report_engine.py:395` 实际调用)，与本债**无符号共享、无文件共享**，不构成收口耦合。仅命名后缀相似，删 R37 时勿误连带它。

## §7 收口行为差异及所需 parity 测试

**不适用（N/A）。** 本债是直删而非收口，无新旧两路、无分支等价问题，故无需 parity 测试。

- 删除自证手段（非 parity，而是「死确认」）：执行后 `rg -n "list_links_with_machine_names" data core web tests -g "*.py"` 为 0 命中 → 证实无遗漏调用方。
- contract/topology 测试：phase1_blast.contract_topology_tests = 「无」，无契约/拓扑测试覆盖此方法。

## §8 爆炸半径（生产消费者 + 测试消费者 + 灾难链）

- **生产消费者：零。** 执行后生产/测试源码内 `list_links_with_machine_names` 0 命中；执行前 callgraph edges.json 中本符号仅有 2 条**出边**（`list_links_with_machine_names → BaseRepository.fetchall`，ambiguous attr），**零入边**（无任何 `to=list_links_with_machine_names`）。facade `operator_machine_query_service.py:95-112` 代理 5 个兄弟方法，确认**未代理本方法**。
- **测试消费者：零。** test_consumers = 无；`tests/models_domain/test_query_services.py:196,224` 调的是活近亲 `list_links_with_operator_info`，与本债无关。
- **co_change_within_group：[]**，无跨边联动。
- **灾难链**：
  1. 正确删除旧 `:82-90` 已完成 → 零生产/测试受影响，安全。
  2. **误删活近亲风险（唯一真实风险）**：`list_links_with_operator_info` 当前在 `:82`，被 `equipment_pages.py:142`（`_group_machine_operator_links(om_q.list_links_with_operator_info())`）+ facade :104-105 + 2 处测试实际依赖。若后续因命名对称（machine_names vs operator_info）再按旧行号误删活的那条，会**静默炸掉设备页的人机联动分组**——非编译期报错而是页面数据缺失，最难察觉。

## §9 承重毗邻标记（同文件 LB + 禁区行）

- `load_bearing = false`，`lb_no_touch = null`，`lb_colocation_danger = none`。
- `operator_machine_repo.py` 非承重文件，同文件无任何 LB 债。
- **禁区行（非承重定义，而是「误删护栏」）**：执行后 `:82-90` 区间的活近亲 `list_links_with_operator_info` 整体为不可触碰区。旧删除范围 `:82-90` 已作废，后续不得再按旧行号施工。

## §10 分层 / 导入环风险

**无。** 直删一个 data 层 repo 方法，不新增任何 import，不引入跨层依赖（不会制造 core.algorithms→core.services 或 core.models→core.services 之类越层），不构成导入环。AST 违规增量为 0。

## §11 测试耦合（动手前先迁哪些测试）

**无需迁移任何测试。** 本方法零测试覆盖（test_consumers = 无）。`tests/models_domain/test_query_services.py` 中的相关断言（当前 `:196/:224`）针对活近亲 `list_links_with_operator_info`，R37 删除后这些测试不受影响、无需改动。已跑一遍 `tests/models_domain/test_query_services.py` 兜底确认活近亲未被误伤。

## §12 前置依赖（先做哪些债 / 哪些 F 门）

**无前置依赖。** planned_deps_hint = 无；co_change = []；`operator_machine_repo.py` 仅此一债。

- 已在 Batch-B/G36 完成（旧 planned_batch_hint = Batch-13，纯死无跨边）。
- 与 R41 的 interference_edge 仅因「同在 `equipment_pages.py`」标 same_file=true、same_symbol=false——但 R37 的直删动的是 `operator_machine_repo.py` 旧 `:82-90`，**根本不碰 `equipment_pages.py`**（R37 在该文件无任何改动行）。故此边对 R37 是**伪干扰**，不构成排序约束；R37 与 R41 可独立推进、互不撞行号。
- 无需任何 F 门作前置。

---

## 对抗核验（第二双眼睛）

核验人独立 rg/grep 回盘，默认怀疑，逐条挑战档案断言。

**重 grep 命令与结果：**

1. `rg -n "list_links_with_machine_names" data core web tests -g "*.py"` → **0 命中**（退出码 1）。死方法已从生产/测试源码退场。
2. `rg -n "def list_links_with_operator_info|list_links_with_operator_info\\(" data core web tests -g "*.py"` → 6 处命中：repo def `:82`、facade `operator_machine_query_service.py:104-105`、`equipment_pages.py:142`、`test_query_services.py:196,224`。**活近亲确实活着**，误删它会静默炸设备页人机联动分组。
3. `sed -n '76,104p' operator_machine_repo.py` → 当前 `:82-90` 为活近亲 `list_links_with_operator_info`，不是 R37 死方法。旧删除锚点已作废。
4. `rg -n "list_links_with_machine_names" core/services/personnel/operator_machine_query_service.py` → **0 命中**，facade 未代理死方法，坐实 P6「被兄弟淹没」病理。
5. `rg -n "list_links_with_machine_names" -g '!*.py'` → **0 命中**，无 JS/HTML/字符串调度等非 py 引用。
6. `git diff -- data/repositories/operator_machine_repo.py` → 只删除 `list_links_with_machine_names` 方法块，`list_links_with_operator_info` 仅作为上下文出现，未被修改。

**语义反例排查（死方法 vs 活近亲是否真不同）：** 死方法 `JOIN Machines m ON m.machine_id ... SELECT ... m.name AS machine_name`；活近亲 `LEFT JOIN Operators o ... SELECT ... o.name AS operator_name, o.status`。**两条 SQL 取数维度截然不同（机器名 vs 操作员信息），绝非可统一的重复**——直删死方法不影响活近亲，无「该统一」陷阱。

**铁律复核：** 非承重（`load_bearing=false`/`lb_no_touch=null`）、非 owner_pending、非收口（死代码病理无统一点，不触铁律5/6）、纯删一个 SELECT 方法不碰灵魂线（无兜底/吞错/透传/加形参）、不引跨层 import（铁律6 增量 0）。R41 interference 经核确为伪干扰（R37 不碰 equipment_pages.py）。

**核验结论：PASS。** R37/G36 已 fixed，死符号源码 0 命中，活近亲当前在 `:82`。后续唯一需守住的是：不得再把执行前旧锚 `:82-90` 当成待删范围。
