# R04 逐债档案 — 三套私有正整数强制器与统一收口点并存

> **2026-06-08 B 执行终态：fixed。** G19 已先完成 R01 死链删除，再把 A 路 `_strict_positive_int` 收口到既有 `parse_required_int(field="op_id", min_value=1, reject_integer_float=True)`；R01 删除后剩余 5 处调用点均同步捕获 `ValidationError`。
> **执行说明：** GF1 前置门已在 `core/shared/strict_parse.py` 落地且默认 `False`；B/C 两个错误路径哨兵只补“故意降级”注释和 parity 测试，继续保持 `auto_assign → 0`、`persistence → None`，未触碰 LB08 文案/正则桥，也未碰 STRICT-4。

> 债 id: R04 | bucket: B05 | severity: low | pathology: P5 | 承重: false | owner_pending: false | cluster: C01
> 主文件: `core/services/scheduler/run/schedule_payload_contract.py`
> 同文件兄弟债: R01 | 同收口符号债: R09(parse_finite_int) | co-change 组: R59/R09/R28
> 标题: 三套私有正整数强制器(raise / →0 / →None)与统一收口点 `parse_finite_int` 并存,契约各异
>
> 本档案只读不改,所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

按符号名 rg 回盘（绝不照抄 old_location）：

| 角色 | 符号 | 真实 file:line（2026-06-05） | old_location | 漂移 |
|---|---|---|---|---|
| **主符号 A**（raise 路） | `_strict_positive_int` | `core/services/scheduler/run/schedule_payload_contract.py:50` | `:50` | **无漂移**（行号准） |
| 哨兵 B（→0 路） | `_positive_int` | `core/services/scheduler/run/auto_assign_resource_errors.py:114` | registry blast 写 `:114` | **无漂移** |
| 哨兵 C（→None 路） | `_positive_int` | `core/services/scheduler/run/schedule_persistence_errors.py:13` | registry blast 写 `:13` | **无漂移** |
| 统一收口点 | `_parse_finite_int` / `parse_required_int` | `core/shared/strict_parse.py:46` / `:81` | blast 提 `:56`（1e-9 行） | **无漂移**（1e-9 行实为 `:56`，准） |

主符号 A 的 6 个生产调用点（均在主文件内，全部回盘确认）：
`:72`、`:149`、`:198`、`:308`、`:324`、`:372`（rg `_strict_positive_int` 命中行号与 registry blast 完全一致，**无漂移**）。

**⚠️ registry 自身文件名笔误**：`R04.all_files` 列了 `payload_contract.py`（无 `schedule_` 前缀），但 `fd payload_contract core/services/scheduler/run/` 证实该文件**不存在**，真实文件名是 `schedule_payload_contract.py`。`prod_consumers` 又提到 `payload_contract:33-47 to_repo_rows`——同样应理解为 `schedule_payload_contract.py`（该文件内 `to_repo_rows` 是 op_id 落库构造点）。下游分析以真实文件名为准。

**结论：R04 全部符号行号零漂移，定位精准。**

## 字段 2：当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_08 = fixed`。
- B 执行一致性：**已闭合。**
  - `_strict_positive_int` 已委托 `parse_required_int(..., reject_integer_float=True)`，不再保留本地手搓解析。
  - GF1 已在 strict_parse 落地且默认 `False`，严格模式测试覆盖“默认仍接受 12.0 / 开关打开拒绝 12.0”。
  - R01 先删后，R04 实际剩 5 个调用点，均已把 `ValidationError` 纳入捕获，脏 op_id 仍转成原来的跳过/错误明细，不外逃炸链。
  - B/C 两哨兵只补注释和 parity，行为保持 `→0` / `→None`。

## 字段 3：病理 / 严重度 / 桶 / 簇

- **病理 P5**（收口/统一类，needs_adversarial=true）。对抗结论 `verdict_kind=load_bearing, refuted=false`——即「三套并存确有真实契约差异，不是裸重复，不能无脑合并」。
- **严重度 low**：三套强制器当前行为自洽且被各自的 except 兜住，没有现行 bug；这是「可维护性/语义重复债」而非功能缺陷。
- **桶 B05**（正整数/解析收口）。**簇 C01**。
- **三套契约差异（坐实，这是 P5 不可裸合并的根因）**：

| 强制器 | 位置 | 非法输入(None/非数/<=0)行为 | float 整值(3.0) | 用途语境 |
|---|---|---|---|---|
| A `_strict_positive_int` | `schedule_payload_contract.py:50` | **raise ValueError** | **raise**（:51 显式拒 float） | op_id 落库闸门，被 6 处 except 兜成 skip |
| B `_positive_int` | `auto_assign_resource_errors.py:114` | **→ 0**（`except Exception`） | 接受（`int(3.0)=3`） | 错误消息解析路径哨兵 |
| C `_positive_int` | `schedule_persistence_errors.py:13` | **→ None**（`except (TypeError,ValueError)`） | 接受 | 持久化错误归一哨兵 |
| 收口点 `_parse_finite_int`/`parse_required_int` | `strict_parse.py:46/:81` | **raise ValidationError** | **接受 3.0→3**（:56 `1e-9` 容差） | 通用入参校验 |

注意：A 比收口点**更严**（A 拒 float 整值，收口点接受 3.0）——这正是 F1 必须先给收口点加 `reject_integer_float` 才能让 A 收口而不放松契约的原因。

## 字段 3：病理 / 严重度 / 桶 / 簇

（待填）

## 字段 4：修法类

**修法类 = 收口（部分）+ 哨兵注释（部分）**，owner_pending=false 可给终态修法。

### 4a. 收口点必须是「已存在」点（铁律 5）
收口目标 = `core/shared/strict_parse.py` 的 `parse_required_int`（:81）——**已存在的统一点**，同 partition 范例 `optimizer_config.py:83` 已在用（`int(parse_required_int(value, field=label, min_value=min_value))`）。**不新建任何模块。**

### 4b. 主符号 A `_strict_positive_int` —— 收口（但有强制前置 F1）
- **不能直接收口**：当前 `parse_required_int` 接受 `3.0→3`（`_parse_finite_int:56` 的 `1e-9` 容差），而 A 在 `:51` 显式拒 float。直接委托会**放松 op_id 契约**。
- **前置 F1**：先给 `_parse_finite_int` 加 `reject_integer_float: bool = False` 严格开关（默认不破坏既有调用者），`min_value` 基建已存在（`_ensure_min_int:28`）。
- F1 落地后，A 函数体改为单行委托：
  `return int(parse_required_int(value, field="op_id", min_value=1, reject_integer_float=True))`
- **⚠️ 灾难前置（见字段 8）**：`ValidationError` 是 `AppError`（`errors.py:97`）**不是 `ValueError` 子类**，会逃逸全部 6 处 `except (TypeError, ValueError)`。收口时**必须同步**把 6 处 except 改为捕 `(TypeError, ValueError, ValidationError)`，否则脏 op_id 由「静默跳过」变「整链崩」。这是收口的最小爆炸面要件，**不可拆分提交**。

### 4c. 两哨兵 B/C —— 仅注释，严禁改 raise（灵魂线红线）
- B `auto_assign_resource_errors.py:114`（→0）、C `schedule_persistence_errors.py:13`（→None）位于**错误处理路径**：它们在「已经出错、正在解析错误消息/归一化错误 id」时运行。改成 raise/ValidationError = 在错误处理里抛二次异常，掩盖主诊断 = **灵魂线红线（兜底/静默不可改 loud，但错误路径的降级是故意的）**。
- 修法仅限：给 B、C 各补一条『我是故意的』中文注释，说明「此处是错误处理路径的降级哨兵，→0/→None 为有意设计，禁止收口到会 raise 的 parse_required_int」；并补 parity 测试钉住 B(非法→0)、C(非法→None) 的边界行为。
- **B/C 不参与 A 的收口，二者与 A 是『同标题不同契约』，不是可合并重复。**

### 4d. owner_pending=false
本债无需 owner 裁断，上述终态修法成立。

## 字段 5：同文件碰撞债（siblings）

主文件 `schedule_payload_contract.py` 内的兄弟债：**R01**（registry `same_file_siblings=["R01"]`）。

### R01 改哪段
R01 = 「死 count/has 链 + SP05:54-55 两符号断言」。其符号链回盘：
- `count_actionable_schedule_rows` @ `:90`
- `has_actionable_schedule_rows` @ `:94`（内部 `count_...() > 0`）
- 两符号在 `__all__` 导出 `:414-415`。
- current_evidence 提的「SP05:54-55」= `_strict_positive_int` 的 float 分支两行（`:51-52` 区，R01/R04 共享同一函数的不同关注点：R04 关注「是否收口」，R01 关注「这两行断言的存活性」）。

### 物理位置关系（会否撞行号 / dict 键位移）
- R04 改区：`:50-56`（`_strict_positive_int` 函数体）+ 6 个调用点的 except（`:73/:150/:199/:309/:325/:373`）。
- R01 改区：`:90-95`（count/has 链）+ `:414-415`（`__all__`）。
- **物理分离**：R04 在 :50-56 与 6 个 except；R01 在 :90+。**不撞行号、无 dict 键位移**（本文件无 dict 字面量被两债同时编辑）。
- **但耦合存在（必须同批次或串行）**：R01 的 `count_actionable_schedule_rows:90`→`_iter_actionable_results:67`→`_strict_positive_int:72`。R04 若收口改了 A 的异常类型而**漏改 :73 的 except**，则 `count_actionable_schedule_rows` 遇脏 op_id 会从「跳过」变「ValidationError 上抛」——R01 的死链判定逻辑被破坏（见字段 8 灾难链）。

### 谁先谁后
- **建议 R04 与 R01 同批次提交（同一 PR），或 R04 先、R01 紧随**。理由：R04 收口动了 `_strict_positive_int` 与 6 处 except 的异常面，而 R01 的核心符号 `count/has` 正依赖这条链的「脏行被跳过」行为。先稳定 R04 的异常契约，R01 再判定死链才不会踩到漂移中的语义。
- registry 跨桶硬边：R04@B05、R01@B14，**B05 动本文件须与 B14 同批次或串行**（planned_deps_hint 明确）。

## 字段 6：同收口点债 + 顺序敏感性

registry `interference_edges` 把 **R09** 标为 `same_symbol=true, sym:parse_finite_int`。**实测后这是「概念同源」标记，不是同一物理收口符号——澄清如下：**

| 维度 | R04（本债） | R09 |
|---|---|---|
| 主文件 | `schedule_payload_contract.py` | `resource_dispatch_execution_service.py` |
| 待收口符号 | `_strict_positive_int`（raise 路） | `_positive_int`（执行车道 3 副本，→None 路） |
| 收口目标 | **已存在** `parse_required_int`（raise 语义，min_value=1） | **新建** `parse_optional_positive_int`（→None 语义，铁律 5 批准的唯一例外，前置 F3） |
| 契约 | 非法 → raise | 非法/≤0/垃圾 → None，**绝不 raise** |

- **关键判断：R04 与 R09 收口到两个不同符号、两套不同契约，不会互相覆盖。** registry 的 `sym:parse_finite_int` 是「都属于正整数解析家族」的概念簇标记（C01），不是物理同符号冲突。`auto_assign_resource_errors.py` 当前 rg `parse_finite_int` 零命中，证实 parse_finite_int 尚未被任何文件 import——它只是 strict_parse 内部 helper，对外门面是 `parse_required_int`/`parse_optional_int`。
- **顺序敏感性**：R04 与 R09 **无收口符号竞争**，可并行。但二者共享同一前置家族——R04 依赖 **F1**（`reject_integer_float`），R09 依赖 **F3**（新建 `parse_optional_positive_int`）。F1 与 F3 是不同 F 门，互不阻塞。
- **co-change 组 R59/R09/R28**：R59 与 R04 **共享 F1 产物**（R59 收口也要 `reject_integer_float`），故 F1 一旦落地，R04/R59 可同批做；R28（number_utils.py）属另一文件，无符号竞争。
- **同收口符号的真正风险点不在 R09，而在「F1 改了 `_parse_finite_int` 这个被 `parse_required_int`/`parse_required_float` 共用的内部 helper」**——见字段 8 的 fix_invalidation 链：F1 加 `reject_integer_float` 形参时**必须默认 False**，否则会波及所有现有 `parse_required_int` 调用者（如 optimizer_config.py:83/150/187），把它们的 `3.0` 输入由「接受」变「raise」。

## 字段 7：收口行为差异 + 所需 parity 测试

收口 = 让 A `_strict_positive_int` 委托 `parse_required_int(min_value=1, reject_integer_float=True)`。逐分支等价性核验（旧 A 路 vs 新委托路）：

| 输入边界 | 旧 A 行为（`:51-55`） | 新委托行为（F1 后） | 等价? | 反例/隐患 |
|---|---|---|---|---|
| `None` | raise **ValueError** | raise **ValidationError** | 行为等价（都 raise），但**异常类型变了** | ⚠️ 6 处 except 只捕 `(TypeError,ValueError)`，ValidationError 逃逸 → 必须同步改 except（字段 4b/8） |
| `True`（bool） | raise ValueError（`:51` 显式拒 bool） | raise ValidationError（`_parse_finite_int:48` 显式拒 bool） | 等价（都拒 bool） | ✅ 收口点也专门拒 bool，无漂移 |
| `3.0`（float 整值） | raise ValueError（`:51` 拒 float） | **F1 前**：接受→3（`:56` 1e-9 容差）；**F1 后 reject_integer_float=True**：raise | 仅 F1 后等价 | **核心反例**：不加 F1 直接收口 = 把 `op_id=3.0` 由「拒绝」变「接受」=放松落库闸门，污染 `to_repo_rows:38 int(row.op_id)` |
| `"5"`（数字字符串） | `int("5")=5` 接受 | `parse_required_int` 经 `_parse_finite_int` `float("5")=5.0`→接受 | 等价 | ✅ |
| `0` / `-1` | raise（`:53 <=0`） | raise（`min_value=1` → `_ensure_min_int:28`） | 等价 | ✅ |
| `""`（空串） | `int("")` 抛 ValueError → raise | `parse_required_int` `is_blank_input("")`→`_raise_blank_required` ValidationError | 行为等价（都 raise），异常类型变 | 同 None：需 except 同步 |
| `5.7`（非整 float） | raise（`:51` 拒 float） | raise ValidationError（`:56` 容差不满足） | 等价 | ✅ |

**所需 parity 测试（动手前必备）**：
1. **A 收口 parity**：对上表每行边界值，断言 `_strict_positive_int(x)` 新旧两路对 `{接受值 / 是否抛 / 抛出异常被 6 处 except 捕获后是否仍 skip}` 三态等价。**重点钉 `3.0` 必 raise、6 处 except 改后仍能 `continue`/返回错误而非崩**。
2. **哨兵 B parity**（`auto_assign:114`）：`_positive_int(None)==0`、`_positive_int("x")==0`、`_positive_int(-3)==0`、`_positive_int(3.0)==3` —— 钉死 →0 契约不被误收口。
3. **哨兵 C parity**（`persistence:13`）：`_positive_int(None) is None`、`_positive_int(0) is None`、`_positive_int(3.0)==3` —— 钉死 →None 契约。
4. **回归保护**：`tests/schedule/service/test_schedule_persistence_reject_empty_actionable_schedule.py` 必跑（走 `count_actionable_schedule_rows` 链，验证脏行仍被跳过而非崩）。

## 字段 8：爆炸半径

### 生产消费者（回盘确认全部在）
主符号 A `_strict_positive_int` 的 6 个 op_id 闸门调用点（全在 `schedule_payload_contract.py`，均被 `except (TypeError, ValueError)` 兜住）：
- `:72`（`_iter_actionable_results`，except→`continue`）
- `:149`（actionable 过滤，except→`continue`）
- `:198`（单结果解析，except→`return None, None, "工序编号不合法"`）
- `:308`（`allowed_op_ids.add`，except→`continue`）
- `:324`（row 校验，except→`return None, ["工序编号不合法"]`）
- `:372`（scheduled_op_ids 解析，except→`errors.append(...); continue`）

下游落库链：A 的产物 op_id 流入 `to_repo_rows:38 int(row.op_id)`（落库）与 `:43 op_id in frozen_op_ids`（lock_status 判定）。**A 是 op_id 进库的语义闸门。**

两哨兵生产消费者（不参与 A 收口，仅注释）：
- B `auto_assign_resource_errors.py:85/180`（→0 哨兵调用点，registry blast 提）
- C `schedule_persistence_errors.py:24/56/77`（→None 哨兵调用点）

### 测试消费者
- `tests/schedule/service/test_schedule_persistence_reject_empty_actionable_schedule.py`（走 schedule_payload_contract 的 count/has 链）
- `tests/scheduler_graph/test_ready_queue.py`

### 灾难链（删错 / 统一错会静默炸什么）
1. **最大风险：异常类型逃逸（核心灾难链）**。`ValidationError` 是 `AppError`（`core/infrastructure/errors.py:97`），**不是 `ValueError` 子类**（`AppError(Exception)` @ `:63`）。若收口 A 改抛 ValidationError 而**漏改任一处 except (TypeError,ValueError)**：脏 op_id 输入下，该处由「静默跳过/字段化」变成「ValidationError 一路上抛」。
   - 经 `count_actionable_schedule_rows:90`→`_iter_actionable_results:67`→A：原本「脏行不计数」变成「整个排程结果统计崩溃」。
   - 经 `has_actionable_schedule_rows:94`（R01 兄弟符号）：原本返回 bool，变成抛异常 → 上游「是否有可落库行」判定崩 → **可能让一次本应正常的排程持久化整体 abort**。这是 low 债踩成 P0 的路径。
2. **闸门放松（不加 F1 直接收口）**：`op_id=3.0` 由「raise→被跳过」变「接受→3 落库」。污染 `to_repo_rows` 的 op_id linkage 与 lock_status，**静默写脏数据**，无报错、难追溯。
3. **哨兵误收口（违灵魂线）**：把 B(→0)/C(→None) 改成 raise = 在错误处理路径抛二次异常，掩盖原始诊断 → 用户看到的报错指向错误位置。
4. **F1 默认值踩错**：`reject_integer_float` 若默认 True，波及所有现有 `parse_required_int` 调用者（optimizer_config.py:83/150/187 的 time_limit/budget），把它们能接受的 `3.0` 变 raise → 配置解析回归。**必须默认 False。**

## 字段 9：承重毗邻标记 + 禁区行

- **R04 自身 load_bearing=false，lb_no_touch=null**——本债主文件 `schedule_payload_contract.py` 非 LB 文件，无承重禁区行约束（registry `lb_colocation_danger: none`）。
- **跨文件毗邻 LB08**：registry interference 标 `LB08 same_file:auto_assign_resource_errors.py`。实测 LB08 的 primary 其实是 `scheduler_public_errors.py`（LEGACY 正则桥），其 `lb_no_touch` 写「禁改被 `auto_assign_resource_errors` 发出的文案而不同步正则」。
  - **对 R04 的约束**：R04 在 `auto_assign_resource_errors.py` 只动哨兵 B `_positive_int:114`（补注释），**不碰任何错误文案串**。B 是数值解析，与 LB08 的「中文文案↔正则桥同生共死」无交集。
  - **禁区行（来自 LB08，R04 须绕开）**：`auto_assign_resource_errors.py` 内一切**错误文案常量 / 前缀 / `AUTO_ASSIGN_RESOURCE_ERROR_PREFIXES` 相关行**（`_auto_assign_error_identity` 系列，约 :126+）——R04 一行都不许动，改了会脱钩 `scheduler_public_errors.py` 的 LEGACY 正则桥。
- **结论：R04 无自身承重禁区；唯一毗邻禁区是 LB08 的文案/正则桥行，R04 的注释修法天然不触及。**

## 字段 10：分层 / 导入环风险

- 收口目标 `parse_required_int` 在 `core/shared/strict_parse.py`，其 import 仅 `core.infrastructure.errors`（`:7`）+ stdlib。`core.shared` 是底层共享层，被 `core.services` 下行依赖**合法**。
- 主文件 `schedule_payload_contract.py:7` **已经 `from core.infrastructure.errors import ValidationError`**——收口后把 except 改为捕 `ValidationError` **零新增 import**，更不会引入新依赖边。
- 若 A 收口需新增 `from core.shared.strict_parse import parse_required_int`：`core.services.scheduler.run → core.shared` 为下行，**不越层、不成环**（同 partition 范例 `optimizer_config.py:12` 已这样 import，AST 干净）。
- **分层红线 0 违规：收口不引入 `core.algorithms→core.services` 或 `core.models→core.services` 越层，不造导入环。** ✅

## 字段 11：测试耦合

动手前先迁/补/确保绿的测试：
1. **先补 parity 测试**（字段 7 的 1-3）：A 收口 parity、B(→0) parity、C(→None) parity。这是收口的安全网，**必须先于改 A**。
2. **回归必跑**：`tests/schedule/service/test_schedule_persistence_reject_empty_actionable_schedule.py`（走 count/has 链，验证脏 op_id 收口后仍被「跳过」而非「崩」——直接验证字段 8 灾难链 1 未发生）。
3. `tests/scheduler_graph/test_ready_queue.py`（registry 列为 test_consumer，确认 op_id 闸门行为未变）。
4. **F1 自身测试**（前置）：`reject_integer_float=True` 时 `3.0→raise`、`=False`（默认）时 `3.0→3` 接受——保护现有 `parse_required_int` 调用者不回归。
5. **与 R01 同批时**：R01 的 count/has 死链测试需与 R04 的 except 改动一起跑，避免异常面漂移期间误判死链。

## 字段 12：前置依赖

**做 R04 前必须先完成（按门）**：
1. **F1（强制前置，blast `fix_invalidation_risk` 标⚠️关键）**：给 `core/shared/strict_parse.py:46 _parse_finite_int` 加 `reject_integer_float: bool = False` 严格开关，并经 `parse_required_int` 透传。`min_value` 基建已存在（`_ensure_min_int:28`），无需新建。**默认 False**，否则波及 optimizer_config.py 等现有调用者。
2. **F1 的 parity/回归测试**先绿（字段 11.4）。

**做 R04 时必须同批/串行（不可拆）**：
- **6 处 except 同步改捕 `ValidationError`**——与 A 收口同一 PR，否则灾难链 1（异常逃逸）必触发。
- **R01@B14**：同文件硬边，B05 动本文件须与 B14 同批次或串行（registry planned_deps_hint）。

**可同批但无强依赖**：
- R59（共享 F1 的 `reject_integer_float` 产物，F1 落地后可同批）。
- R09（收口到**新建** `parse_optional_positive_int`，依赖 **F3** 而非 F1；与 R04 无符号竞争，可并行）。

**批次定位**：Batch-6（B05 正整数/解析收口），F1 之后即可做，与 R01@B14 同批或串行。

---

### 档案完整性自检
- 12 字段齐全 ✅；每条断言附 file:line 证据 ✅
- 只读未改任何生产代码 ✅
- 行号全部 grep 回盘（零漂移，含纠正 registry `payload_contract.py` 文件名笔误）✅
- 承重/灵魂线/收口点/分层红线均已校验 ✅

---

## 对抗核验（第二双眼睛）

核验日期 2026-06-05｜方法：独立重 rg/grep 回盘，主动找反例，不照抄正文。

**重 grep 命令与结果（全部独立复跑）：**
- `rg -n "_strict_positive_int" schedule_payload_contract.py` → 定义 `:50`，调用点 `:72/:149/:198/:308/:324/:372` —— 与正文**完全一致，零漂移**。
- `rg -n "_positive_int" auto_assign_resource_errors.py` → 定义 `:114`，调用点 `:85/:180` ✅；`schedule_persistence_errors.py` → 定义 `:13`，调用点 `:24/:56/:77` ✅。
- `rg -n "_parse_finite_int|parse_required_int|reject_integer_float|_ensure_min_int|1e-9" core/shared/strict_parse.py` → `_ensure_min_int:28`、`_parse_finite_int:46`、`:56 abs(...)>1e-9`、`parse_required_int:81`，`reject_integer_float` **零命中** —— F1 确实缺失，正文准。
- `ls .../run/ | rg payload_contract` → 仅 `schedule_payload_contract.py`，**确认 `payload_contract.py` 不存在**，正文纠正 registry 笔误成立。

**主动找反例的核心 4 点（逐项坐实，未找到反证）：**
1. **A 拒 float**：直读 `:51 if value is None or isinstance(value, bool) or isinstance(value, float): raise ValueError` —— A 确实比收口点严（收口点 `:56` 接受 `3.0→3`）。F1 前置不可省，否则放松 op_id 闸门 —— **证实**。
2. **6 处 except 全是 `(TypeError, ValueError)`**：逐个 sed 读 `:73/:150/:199/:309/:325/:373`，无一例外 —— **证实**。
3. **ValidationError 非 ValueError 子类**：`errors.py:63 class AppError(Exception)` → `:97 class ValidationError(AppError)`，与 `ValueError` 无继承关系。`strict_parse` 抛的全是 ValidationError（`_parse_finite_int:48/:52/:57`、`_ensure_min_int:30`、`_raise_blank_required:17`）—— **异常逃逸灾难链（字段8.1）真实成立，6 处 except 必须同步改捕 ValidationError，不可拆**。
4. **收口点已存在、铁律5满足**：`optimizer_config.py:12 from core.shared.strict_parse import parse_required_int`、`:83 int(parse_required_int(value, field=label, min_value=min_value))` —— 收口目标是已在用的统一点，**不新建模块**，正文准。

**补充边界校验（挑战字段7等价表，未推翻）：**
- `is_blank_input`（`strict_parse.py:12`）= `None or 空白串`。`None`/`""` 走 `_raise_blank_required` raise ValidationError，与旧 A raise ValueError「行为等价、类型变」—— 正文准。
- `0`/`-1` 非空白，走 `_ensure_min_int(min_value=1)` raise；旧 A 走 `:54 <=0` raise —— 等价，正文准。
- 哨兵 B `:114-119` = `int(value or 0)`，`else 0`（→0，`except Exception`）；C `:13-18` = `else None`（→None，`except (TypeError, ValueError)`）—— 两哨兵契约与正文表格完全一致。

**承重/越界审查：**
- R04 自身 `load_bearing=false, lb_no_touch=null` —— **核实无误**。
- 毗邻 LB08：registry 确认 LB08 primary=`scheduler_public_errors.py`（B10/P3/承重），all_files 含 `auto_assign_resource_errors.py`。R04 在该文件只动哨兵 B（`:85 op.seq`、`:180 match.group("seq")` 均纯数值序号解析，与错误文案/正则桥无交集）+ 补注释 —— **天然不触及 LB08 禁区，无承重误删风险**。
- 修法严格分层：A 收口 + B/C **仅注释、明令禁改 raise（灵魂线）**，无删除/统一/透传/加形参越界 —— **合规**。
- owner_pending=false 终态修法成立，无擅自越权。

**档案瑕疵（结构性，不影响实质结论）：**
- 字段标题重复：字段 3 出现两次（第二次"待填"占位）、字段 10/11/12 同样各有一份实填 + 一份末尾"待填"占位残留（正文 :56-58、:216-226）。内容无矛盾、无数字打架，仅排版冗余。建议 owner 合并去重，但**不影响修法正确性**。

**核验结论：PASS**（行号零漂移、灾难链真实、修法合规、无承重误删；唯一问题是字段标题重复的排版冗余）。

## 字段 10：分层 / 导入环风险

（待填）

## 字段 11：测试耦合

（待填）

## 字段 12：前置依赖

（待填）
