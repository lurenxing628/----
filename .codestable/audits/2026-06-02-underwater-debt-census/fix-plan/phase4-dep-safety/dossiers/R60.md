# R60 逐债档案 — plan_id 死面包屑被合同字段表正式承认却全链零 resolver 消费

> **2026-06-08 B 执行终态：fixed。** R60 已随 R42 同一原子动作完成：`_REPORT_CONTEXT_FIELD_NAMES`、`_has_navigation_context`、`_EXPORT_CONTEXT_KEYS` 和两条 `scheduler_workbench_link_query.py` URL emit 均不再包含 `plan_id`；`version` / `plan_role` / `scenario_id` / `back_to` 等真上下文键保留。

> 只读不改产物。本档案不改任何生产代码,仅做依赖/爆炸半径分析与安全批次计划。
> bucket=B01 | severity=low | pathology=P6(死码直删) | load_bearing=false | owner_pending=false | cluster=C01

---

## 1) 当前真实 file:line（grep 回盘）+ 漂移记录

R60 = `plan_id` 死面包屑「被合同字段表正式承认」这一 facet。它没有自己的独立符号,寄生在三处「字段表 / emit 点」上。逐一 rg 回盘:

| 角色 | 符号 / 内容 | old_location(勿信) | **回盘真实 file:line** | 漂移 |
|---|---|---|---|---|
| emit-A | `_append_plan_query` 内 `_append_param(query,"plan_id",...)` | link_query:118 | **web/viewmodels/scheduler_workbench_link_query.py:118** | 无 |
| emit-B | execution_review 分支 `_append_param(query,"plan_id",...)` | link_query:154 | **web/viewmodels/scheduler_workbench_link_query.py:154**(分支条件 :152 `if plan_style=="execution_review"`) | 无 |
| 合同表-1 | `_REPORT_CONTEXT_FIELD_NAMES` 元组首项 `"plan_id"` | navigation_links.py:12 | **web/viewmodels/scheduler_navigation_links.py:12**(元组 :11-28,plan_id 在 :12) | 无 |
| 合同表-1 消费者 | `preserved_report_context_fields` 迭代该表生成隐藏表单 | (未给) | **scheduler_navigation_links.py:186-196**(`for name in _REPORT_CONTEXT_FIELD_NAMES`:190) | 唯一消费者,确认 |
| 判空 | `_has_navigation_context` 内 `"plan_id"` | navigation_links.py:52 | **scheduler_navigation_links.py:52**(判空元组 :48-63) | 无 |
| 合同表-2 | `_EXPORT_CONTEXT_KEYS` 元组首项 `"plan_id"` | export_support.py:14 | **web/routes/reports_export_support.py:14**(元组 :13-29,消费者 `current_report_export_url`:38) | 无 |

**漂移结论:无漂移。** registry 的 `location_status:✅准` 与 phase1_blast 全部对得上;6 处真实行号与 old_location 逐一一致。注意 registry 的 `all_files` 列了 `workbench_links.py`,但 R60 在该文件无 plan_id 字段表/emit 痕迹(`rg plan_id web/viewmodels/scheduler_workbench_links.py` 仅命中 R42 的读/存 `:191 plan_id: Any=None` / `:233 "plan_id":_text(plan_id) or None`,那属 R42 不属 R60)。

## 2) 当前状态 + 与 status evidence 是否一致

- 2026-06-08 B 执行终态：registry `status_2026_06_05 = fixed`；两张字段表、判空集与两个 emit 点中的 `plan_id` 已随 R42 同原子下线。下文 planned 回盘是执行前证据。
- 一致性:✅ 状态与证据自洽,无矛盾。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6 死码直删类**。`plan_id` 被正式写进合同字段表(`_REPORT_CONTEXT_FIELD_NAMES`:12 / `_EXPORT_CONTEXT_KEYS`:14)、落进每条链接 query(:118/:154)与隐藏表单(:186-196),但**全链零 resolver**:版本身份解析全程靠 `version`+`plan_role`+`scenario_id`/`plan_identity`,无任何页面用 `plan_id` 定位方案。
- 严重度 **low**;桶 **B01**(plan_id 下线);簇 **C01**。
- R60 与 R42 实为同一死参数的两个视角:**R42 = 完整「读入→存储→吐链」**;**R60 = 特指它「进了合同字段表 / 被字段表制度化承认」**。R60 是 R42 的子 facet,无独立爆炸面(影响面 ⊂ R42)。

## 4) 修法类

**P6 直删，但 MUST 与 R42 合并为单一动作。** owner_pending=false → 可给终态修法。非承重(load_bearing=false,lb_no_touch=null),无「仅注释」约束。无收口动作(P6 不是 P5),故不涉及新建收口点。

R60 自身要删的 4 处(纯删,不加兜底/不加形参/不透传):
1. `scheduler_navigation_links.py:12` —— 从 `_REPORT_CONTEXT_FIELD_NAMES` 元组删 `"plan_id",` 一行,**保留元组其余 15 项原封不动**。
2. `scheduler_navigation_links.py:52` —— 从 `_has_navigation_context` 判空元组删 `"plan_id",` 一行,**保留 version/date_*/batch_id/resource_*/scenario_id/back_to 等其余判空键**。
3. `web/routes/reports_export_support.py:14` —— 从 `_EXPORT_CONTEXT_KEYS` 元组删 `"plan_id",` 一行,**保留其余 14 项**。
4. emit 点 `link_query:118` 与 `link_query:154` —— **不由 R60 单独删,由 R42 统一下线**(R60 与 R42 共点;若 R60 单删字段表而留 emit,链接里仍挂 plan_id=半截)。

**红线(灵魂线):纯删,绝不加兜底 / 静默回退 / except 吞错 / 过度防御。** P6 非 P4,不涉及 raise 改造。

**禁区(绝不连带删):** 两张元组里的真承重透传键 `back_to / scenario_id / date_from / date_to / start_date / end_date / query_date / period_preset / batch_id / resource_type / resource_id / scope_type / scope_id / machine_id / operator_id`,以及 `_has_navigation_context` 里的 `version / plan_role`(:51/:63,plan_role 判空是 chrome 显隐承重逻辑)。删元组只动 plan_id **一项**。

## 5) 同文件碰撞债（siblings）

`same_file_siblings = ["R42"]`,`interference_edges` 标 R42 为 **same_file + same_symbol**(共 `build_workbench_plan_context` 调用链、共 link_query:118/154)。

- **R42**(读存吐链全删 / B01 同桶):
  - R42 改段:`scheduler_workbench_links.py:191`(`plan_id: Any=None` 形参) + `:233`(`"plan_id":_text(plan_id) or None` dict 写入) + emit `link_query:118/154`(读 `context.get("plan_id")` 回吐)。
  - R60 改段:两张字段表元组 + 判空元组(navigation_links:12/52、export_support:14)。
  - **谁先谁后:无先后——MUST 同一次提交合并执行**(planned_batch_hint「与 R42 合并为单一动作」)。理由:R60 单删字段表而 R42 未删读/存,则 `plan_id` 仍被 `build_workbench_plan_context` 写进 context、仍被 emit 读出 → 变更隐蔽残渣;反之 R42 删读/存而 R60 留字段表,则 `preserved_report_context_fields` 迭代到 `"plan_id"` 时 `source.get("plan_id")` 恒空被跳过(无报错,但留死表项)。两边必须同删。
  - **撞行号 / dict 键位移风险:低。** 两债不在同一文件的同一函数(R42 在 scheduler_workbench_links.py;R60 在 navigation_links.py + export_support.py;仅 link_query:118/154 共享,且那两行 by R42 删)。元组删行只位移本元组后续行号,不影响别文件。**唯一需协调:emit 点 :118/:154 归 R42,R60 别重复删**,否则同点双删冲突。
  - 测试:两债共享同一组断言 `regression_reports_workbench_navigation_contract.py:109/122/126`,**必须同改一次**(见 §11),各自为政会重复改同测试 / 半适配。

## 6) 同收口点债（同符号/同字段表）

R60 无 P5 收口动作,但有「同字段表 / 同 emit 符号」耦合:

- **同 emit 符号** `_append_param(query,"plan_id",...)`:link_query:118 与 :154 是同一参数的两条 emit 路径(普通链 + execution_review 分支)。两点必须同删(by R42),漏一条则该路径链接仍挂 plan_id。**顺序敏感:无,但必须成对。**
- **同字段表概念**(两张承认 plan_id 的合同表):`_REPORT_CONTEXT_FIELD_NAMES`(navigation_links:11)与 `_EXPORT_CONTEXT_KEYS`(export_support:13)是平行的两张表,各被 `preserved_report_context_fields`(隐藏表单)与 `current_report_export_url`(导出 URL)独立消费。**两张都要清,漏一张则隐藏表单 / 导出 URL 仍挂 plan_id**(fix_invalidation_risk ④)。
- 这三处删除彼此**无顺序依赖**(纯删,不互相读),但必须**全清**(原子性 > 顺序性)。
- 同文件邻债 R54 收的是同文件别字段(navigation_links.py 内),不收 plan_id,非同收口点(详见 §8 协调)。

## 7) 收口行为差异 + parity 测试

R60 是**纯删 P6,无新旧两路收口**,故无收口 parity 要求(P5 才需逐分支等价)。但删除前必须坐实「死」证据,等价于一次「删除安全性」证明:

- **删前不变量(必须成立才能删):** `plan_id` 永不被任何 resolver 当方案定位键。证据:
  - `rg '\bplan_id\b' core/ data/` → **0 命中**(全部命中是 `plan_identity` 不同概念)。core/data 零消费。
  - web 层无 `args.get("plan_id")` 作解析键(`rg '\.get\(.plan_id' web/` 仅命中 emit-side context.get + plan_identity_*,无 resolution)。
  - 版本身份解析全程靠 `version`+`plan_role`+`scenario_id`/`plan_identity`(navigation_links 判空 :51 version / :63 plan_role)。
- **删后行为差异:** URL query 与隐藏表单不再出现 `plan_id` 键;`preserved_report_context_fields` 少迭代一项;`current_report_export_url` 少透传一键。因下游零消费,**对页面/解析行为零可观察影响**(纯减少噪声键)。
- **无需 parity 测试(无新旧两路)**,但需:删 contract 测试里的 plan_id 断言(:122/:126,见 §11),并补一条**反向守卫**(可选,low 不强求):断言 URL/hidden 不再含 plan_id,防回潮。

## 8) 爆炸半径（生产消费者 + 测试消费者 + 灾难链）

**生产消费者(全是 emit/承认侧,无 resolution 侧):**
1. `web/viewmodels/scheduler_workbench_link_query.py:118` —— `_append_plan_query` 普通回吐(与 R42 共点,by R42 删)
2. `web/viewmodels/scheduler_workbench_link_query.py:154` —— execution_review 分支回吐(与 R42 共点,by R42 删)
3. `web/viewmodels/scheduler_navigation_links.py:11-28` —— `_REPORT_CONTEXT_FIELD_NAMES` 合同表(plan_id 在 :12)
4. `web/viewmodels/scheduler_navigation_links.py:186-196` —— `preserved_report_context_fields` 迭代字段表生成隐藏表单(字段表唯一消费者)
5. `web/viewmodels/scheduler_navigation_links.py:52` —— `_has_navigation_context` 的 plan_id 判空(仅判存在不解析)
6. `web/routes/reports_export_support.py:13-29` —— `_EXPORT_CONTEXT_KEYS` 导出合同表(plan_id 在 :14,消费者 `current_report_export_url`:38)
- **DEATH PROOF:** core/ + data/ 零 `\bplan_id\b` resolver;web 层无 `args.get("plan_id")` 作解析键。

**测试消费者:**
- 执行前测试 `tests/web_pages/test_reports_workbench_navigation_contract.py:105-127` 曾钉住 `plan_id=PLAN-RPT` 的 URL 与隐藏表单透传；2026-06-08 已改为断言 `plan_id` 不再出现在 query / hidden inputs，同时保留 back_to。

**灾难链(删错 / 半删会静默炸什么):**
- ① **R60 单删、R42 留** → `build_workbench_plan_context` 仍写 `"plan_id"` 进 context、emit 仍读出 → plan_id 仍挂在链接,字段表却不认它 → **隐蔽残渣,变更白做**。
- ② **元组删行连带删邻键**(误删 :13 back_to 或 export 表 scope_*)→ 隐藏表单/导出 URL **丢真承重透传键** → 报表跨页跳转丢上下文(date_range/resource/scope 失传)→ 静默坏页面状态,**high 级回归**。这是本债最大爆炸风险。
- ③ **漏清一张表**(只删 navigation_links 不删 export_support,或反之)→ 隐藏表单 OR 导出 URL 仍挂 plan_id → 半截死表。
- ④ **R54 与 R60 各自改 navigation_links 元组未协调** → 同元组双删,行号位移导致 patch 冲突 / 误删对方目标键。

## 9) 承重毗邻标记（LB + 禁区行）

- R60 触碰的文件 `scheduler_navigation_links.py` / `scheduler_workbench_link_query.py` / `reports_export_support.py` **均非 PHASE0§5 承重护栏文件**(load_bearing=false,lb_no_touch=null)。
- 但 `interference_edges` 标了同文件 LB 邻债:**LB05(同文件 link_query)**、**LB02(同文件 workbench_links)**——R60 不碰 workbench_links,LB02 不相干;LB05 在 link_query,R60 在 link_query 仅共 emit:118/154 且 by R42 删,需确认 LB05 守的不是 :118/:154 段。
- 经 `build_workbench_plan_context` 调用链与 `navigation_context.py`(**LB06 / R56**)耦合:删字段表 plan_id 本身低危,但**禁区行**——绝不连带删同表真上下文承重透传键:
  - `_REPORT_CONTEXT_FIELD_NAMES`(navigation_links:13-27):`back_to / scenario_id / date_from / date_to / start_date / end_date / query_date / period_preset / batch_id / resource_type / resource_id / scope_type / scope_id / machine_id / operator_id`
  - `_has_navigation_context`(navigation_links:51/53-63):`version / plan_role / date_from / date_to / query_date / period_preset / batch_id / resource_type / resource_id / scenario_id / back_to`
  - `_EXPORT_CONTEXT_KEYS`(export_support:15-28):`back_to / date_from / date_to / start_date / end_date / query_date / period_preset / batch_id / resource_type / resource_id / scope_type / scope_id / machine_id / operator_id`
  - **三张表只删 `"plan_id"` 一行,其余原封。**

## 10) 分层 / 导入环风险

- 修法是**纯删元组条目 + 删 emit 行(by R42)**,不新增任何 import,不新建模块 → **零越层风险、零导入环**。
- 现状导入合规:`scheduler_navigation_links.py` 仅 import 同层 `.scheduler_workbench_link_query` / `.scheduler_workbench_links`(web.viewmodels 内)+ stdlib;`reports_export_support.py` import `core.services.*`(web→core,合规方向)。AST 分层红线:删字段表项不动 import 拓扑,**0 AST 违规**。

## 11) 测试耦合（动手前先迁哪些测试）

**动手前必须先迁的测试(与 R42 共享,只迁一次):**
- `tests/web_pages/test_reports_workbench_navigation_contract.py`:
  - `:105 test_report_navigation_keeps_plan_id_and_return_context` —— 该用例同时断言 plan_id(R42/R60 删)与 back_to(承重,保留)。**改法:删 :109 请求 URL 里的 `plan_id=PLAN-RPT` 片段 + 删 :122 `assert query["plan_id"]==["PLAN-RPT"]` + 删 :126 `assert hidden_inputs["plan_id"]==...`,保留 back_to 两条断言**。
  - 改完用例名宜随之去 plan_id(或保留并改注释说明 plan_id 已下线),避免名实不符——但 low,non-blocking。
- **协调:R42 与 R60 共用此组断言 → 由合并执行的那次 PR 一次性迁,禁止两债各改一遍。**
- 迁测试时**保留**该用例对 back_to / 其余承重透传键的断言,只摘 plan_id 相关行。

## 12) 前置依赖（先做哪些债 / 哪些 F 门）

- **MUST 合并依赖:R42 ⇔ R60 同批同提交**(Phase1 边 `R42→R60 MUST 合并`)。无谁先谁后,但缺一即半截:R60 删字段表 + R42 删读/存吐链 + 共删 emit:118/154,**原子一次完成**。批次:B01 plan_id 下线,本桶内第 3 顺位(与 R42 同动作)。
- **同文件协调依赖:R54**——registry hint 称 R54「同文件 navigation_links 相邻」,但回盘 R54 primary_file 实为 `scheduler_reports_workbench.py`(plan-guard 字段投影 3 套手维 key 列表),与 R60 改的 navigation_links 元组**不在同文件**。若 R54 确也触 navigation_links 字段表则需协调行号(先做的一方位移另一方),否则无冲突。**动手前确认 R54 实际改哪张表**(hint 与回盘存在出入,见 §1 备注)。
- **LB 邻债澄清:** interference_edges 标 LB05(同文件 link_query),但回盘 LB05 primary_file 实为 `execution_review.py`(非 link_query),边是文件共置粗匹配的噪声——确认 LB05 守护范围不含 link_query:118/154 emit 段即可放心(plan_id emit 与 execution_review 承重不对称无关)。R56/LB06 已 fixed,navigation_context 耦合侧已稳定,不阻塞。
- **F 门:** 无前置 F 门硬依赖(P6 死码删,不依赖任何先行重构);唯一前置是「与 R42 必须打包」+「迁 contract 测试」。建议门:删后跑 `regression_reports_workbench_navigation_contract.py` 全绿 + `rg '\bplan_id\b' web/ core/ data/` 仅剩 plan_identity(无裸 plan_id 残留)。
- owner_pending=false → 可直接给上述终态修法,无需 owner 裁断。

---

## 返回摘要

R60 是 R42 的「合同字段表承认」子 facet,纯 P6 死码删,与 R42 MUST 合并为单次 plan_id 下线动作。删 3 处字段表/判空里的 `"plan_id"` 行(navigation_links:12/52、export_support:14),emit:118/154 由 R42 删。最大风险=误删元组邻键(back_to/date_*/resource_*/scope_* 承重透传)或半删(R42/R60 只动一边=隐蔽残渣)。死证确凿:core/data 零 plan_id resolver。

---

## 对抗核验（第二双眼睛）

**结论:PASS。** 独立 rg 全量回盘,六处真实行号逐一一致,死证(零 resolver)复现成立,无承重误删风险被放进修法。低危纯死叶子债,快核充分。

**重 grep 命令与结果:**

1. 三文件 plan_id 全量定位 `rg -n "plan_id|_REPORT_CONTEXT_FIELD_NAMES|_has_navigation_context|_EXPORT_CONTEXT_KEYS|_append_param" web/viewmodels/scheduler_navigation_links.py web/routes/reports_export_support.py web/viewmodels/scheduler_workbench_link_query.py`:
   - `scheduler_navigation_links.py:12`(`_REPORT_CONTEXT_FIELD_NAMES` 元组定义 :11,plan_id 在 :12)✅;`:52`(`_has_navigation_context` 判空 plan_id,函数定义 :47)✅;唯一消费者 `preserved_report_context_fields` 在 **:186 定义、:190 迭代** ✅(档案正文 §1 写 :186-196/:190 准;phase1_blast 旧值 :189-198 有 ~3 行漂移,以正文为准)。
   - `reports_export_support.py:14`(`_EXPORT_CONTEXT_KEYS` 元组 :13,plan_id 在 :14,消费者 `current_report_export_url` :36 定义/:38 迭代)✅。
   - `scheduler_workbench_link_query.py:118`(`_append_plan_query` 内,函数 :116)✅;`:154`(execution_review 分支 :152)✅。**两点均 `context.get("plan_id")` 回吐,确归 R42 删,R60 不动。**
2. **死证 1** `rg -n "\bplan_id\b" core/ data/ | rg -v plan_identity` → **0 命中** ✅。
3. **死证 2/3** `rg -n "get\(['\"]plan_id" web/` + `rg -n "\bplan_id\b" web/ | rg -v plan_identity`:web 全层 plan_id 出现处除本债三文件外,另有 `dashboard.py:241`、`navigation_context.py:78`、`reports_page_support.py:104/143`、`scheduler_reports_workbench.py:60/79`(`build_report_context` 形参)、`scheduler_workbench_links.py:191/233`、`dashboard_workbench_context.py:92` —— **逐一阅源全是读入/存储侧**(`_request_arg/_request_text("plan_id")` 塞进 context dict 或喂 `plan_id=` 形参),**无一处把 plan_id 当方案解析键**。方案身份解析实走 `execution_review_plan_identity_error(raw_plan_role, scenario_id)`(reports_page_support.py:396),用 plan_role+scenario_id 不碰 plan_id。这些读入点属 R42 范畴,恰好印证灾难链①(R42 不删读入则 plan_id 仍被读存)→ **强化 R42⇔R60 MUST 同批,不构成反证**。
4. **测试** `rg -n "plan_id|back_to" tests/web_pages/test_reports_workbench_navigation_contract.py`：执行前曾有 `test_report_navigation_keeps_plan_id_and_return_context` 与 plan_id 正向断言；2026-06-08 已重命名/改写为 plan_id 下线断言，back_to 断言继续保留。
5. **R54 冲突澄清(档案 §12 存疑点已坐实)**:R54 registry `all_files` = `[schedule_result_view_context, scheduler_navigation_publish, scheduler_reports_workbench, scheduler_resource_dispatch, scheduler_workbench_links, workbench_links]`,**不含 scheduler_navigation_links.py**;而 `_REPORT_CONTEXT_FIELD_NAMES` 唯一定义/消费均在 navigation_links.py(:11/:190)。R54 是 P5 收口到 `build_workbench_plan_context` 的 plan-guard 字段族(另一张表),**与 R60 删的字段表不在同文件、不同表 → 实际零行号冲突**。档案保守留疑可,实可直接降级为「无 R54 协调依赖」。
6. **LB05 澄清复现**:LB05 primary=`execution_review.py`(非 link_query),interference_edge 标 link_query 为文件共置噪声,档案纠正正确;LB05 守护与 R60 删除点(R60 不动 link_query)无交集。

**承重判定复核:** 三文件均不在承重护栏名单(load_bearing=false,lb_no_touch=null)✅。修法为纯删元组单项,**未混入删除以外的统一/透传/加形参**,禁区行(back_to/scenario_id/date_*/resource_*/scope_*/version/plan_role)清单完整且明令只动 plan_id 一项,**无承重误删风险**。灵魂线:纯删不加兜底,P6 非 P4 无 raise 改造,合规。分层:不动 import 拓扑,0 AST 违规 ✅。owner_pending=false → 给终态修法不越权 ✅。
