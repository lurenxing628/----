# R02 逐债档案 — schedule_graph_dispatch_context.build_first_wave_ready_nodes (test-only re-export 包装器)

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：本 dossier 引用的 `tests/regression_scheduler_candidate_py38_contract.py` 已由 A 的 **P1.1 删除**；py38 语法保护现由仓库级全树扫描 `tools/scan_py38plus_syntax.py`（+ `tests/gate_meta/test_scan_py38plus_syntax.py`）承载，覆盖面 ≥ 原 2-文件硬清单。涉及它的 py38 契约锚点一律改指该全局扫描器。详见 `ANCHOR-DRIFT-2026-06-05-POSTCOMMIT.md §六.3`。


> 本轮只读不改、只产计划。所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。

- 债 id: R02 | 桶: B09 | 簇: C01 | 病理: P6 | 严重度: low
- 承重: false | owner_pending: false | lb_no_touch: null
- 标题(registry): `build_first_wave_ready_nodes` 是 test-only re-export 包装器(兄弟包装器是活的,唯独它没有生产调用方)

---

## 字段 0 — 2026-06-08 B 执行终态补登

- **状态：fixed。** `tests/scheduler_graph/test_graph_dispatch_context.py` 已拆分 import：只有 `build_first_wave_ready_nodes` 改为从 `schedule_graph_resource_matching_context` 的真实实现导入，其余三个符号仍从 `schedule_graph_dispatch_context` 导入。
- **状态：fixed。** `core/services/scheduler/run/schedule_graph_dispatch_context.py` 中的 `build_first_wave_ready_nodes` 转发壳已删除。
- **禁区核对：** 兄弟壳 `build_graph_resource_matching_projection` 保留；`schedule_graph_resource_matching_context.py:28-45` 的 `GraphInputContractError` loud raise 守卫未动。
- **执行裁决：** “壳最晚删”的目的在于防生产 ImportError；R02 生产下游为零，且 Batch-A 计划已给出 test-only 两步安全网，因此按 Batch-A 执行，不延后到 facade/D。

---

## 字段 1 — 当前真实 file:line(回盘)+ 漂移记录

**全路径澄清(纠 registry 简称)**:`all_files` / `current_evidence` / `phase1_blast` 多处写裸名 `dispatch_context.py`,工作区内 **不存在该文件**(`find core/services/scheduler/run -name dispatch_context.py` 为空)。裸名是 `core/services/scheduler/run/schedule_graph_dispatch_context.py` 的简称,下文统一用全名。

回盘符号 → 实际 file:line:

| 符号 | registry 老线索 | 当前实测 | 漂移 |
|---|---|---|---|
| 包装器 `def build_first_wave_ready_nodes` | `schedule_graph_dispatch_context.py:461-475` | `schedule_graph_dispatch_context.py:461`(def),`:468` 懒 import 真 impl,体 461-475,476-477 空行 | **零漂移** ✅ |
| 真 impl `def build_first_wave_ready_nodes` | resource_matching_context.py:48 | `schedule_graph_resource_matching_context.py:48` | 零漂移 ✅ |
| 真 impl 的 `__all__` 导出条目 | :132 | `schedule_graph_resource_matching_context.py:132`(块在 130-134) | 零漂移 ✅ |
| 真 impl 内部调用方 | :115 | `schedule_graph_resource_matching_context.py:115`(走模块本地 def,不绕包装器) | 零漂移 ✅ |
| 兄弟包装器 `build_graph_resource_matching_projection` | :478 | `schedule_graph_dispatch_context.py:478`(def),:489 懒 import | 零漂移 ✅ |
| 测试 import `build_first_wave_ready_nodes` | test_consumers 写 `:11` | `tests/scheduler_graph/test_graph_dispatch_context.py:11`(在 10-15 的 4 符号块内,line10=模块路径) | 零漂移 ✅ |
| 测试调用点 | :55、:66 | `tests/scheduler_graph/test_graph_dispatch_context.py:55`、`:66` | 零漂移 ✅ |

证据:`rg -n build_first_wave_ready_nodes` 命中全部上列;包装器体 458-495 行已逐字读取(:468 `from .schedule_graph_resource_matching_context import build_first_wave_ready_nodes as _impl`,纯转发无逻辑)。

**总判:行号线索全部准确,零漂移。** registry `location_status:✅准` 成立。

## 字段 2 — 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = fixed`。
- B 执行终态：`schedule_graph_dispatch_context.py` 的 `build_first_wave_ready_nodes` 包装器已删除；测试 import 已拆分，`build_first_wave_ready_nodes` 现在直接从 `schedule_graph_resource_matching_context` 导入。
- **一致** ✅：当前代码、字段 0 执行补登、`_registry.json` 的 fixed 状态三者一致。字段 1 的旧行号仅保留为执行前证据，不再代表当前代码状态。

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- 病理 **P6**:test-only re-export 包装器。`build_first_wave_ready_nodes` 真 impl 在 `schedule_graph_resource_matching_context.py:48` 并已挂该模块 `__all__`(:132);dispatch_context.py:461 这层只是一个纯转发壳(:468 懒 import + :470-474 透传 4 个参数),**生产零调用方**(见字段 8)。它存在的唯一理由是历史上测试从 dispatch_context 这个旧门面 import。
- 严重度 **low**:删的是冗余转发层,真计算逻辑 + 灵魂暗线守卫不在此处。
- 桶 **B09**(图死版 + 空包打包簇)/ 簇 **C01** / partition `scheduler-run` / kind `real` / verdict `real_debt_confirmed`(needs_adversarial=true 但 adv_refuted=false,即对抗未推翻其为真债)。

## 字段 4 — 修法类

**修法类 = 直删包装器 + 退测试**(承重=false,非收口,非 raise,非 owner_pending)。这是合法的"直删"类:删除的是死的转发壳,不删任何活逻辑。

精确两步(顺序不可颠倒):

1. **【先】拆分测试 import 块**——不可整块改路径。`tests/scheduler_graph/test_graph_dispatch_context.py:10-15` 是一个 4 符号 import 块:
   - `build_first_wave_ready_nodes`(:11)——要下线的转发壳,**唯独这一个**改为从 `schedule_graph_resource_matching_context` 导入(真 `__all__` home,:132)。
   - `build_graph_resource_matching_projection`(:12)——**保留从 dispatch_context 导入**(它的兄弟包装器 :478 仍是活的,生产消费者 schedule_graph_report.py:180 走它)。
   - `build_predecessor_successor_maps`(:13)——**保留**,真定义就在 `schedule_graph_dispatch_context.py:396`(非包装器)。
   - `graph_score_weights`(:14)——**保留**,真定义在 `schedule_graph_dispatch_context.py:41`。
   故必须把 import 块拆成两段(一段从 resource_matching_context 取 1 个符号,一段从 dispatch_context 取 3 个符号),**严禁整块换路径**(否则后三个符号在 resource_matching_context 里找不到,ImportError)。

2. **【后】删 dispatch_context.py:461-475** 的 `build_first_wave_ready_nodes` 包装器(连同 476-477 空行收尾)。

**禁区**(直删类的硬边界):
- 严禁连删兄弟 `build_graph_resource_matching_projection`(:478-…),它承重(report.py:180 唯一生产消费者)。
- 严禁动 `build_predecessor_successor_maps`(:396)、`graph_score_weights`(:41)、`node_op_id` / `positive_op_id_set`(被 resource_matching_context.py:5 模块级 import)。
- 严禁碰 `schedule_graph_resource_matching_context.py:28-45` 的灵魂暗线守卫(`GraphInputContractError` raise,见字段 9)。
- 不新增任何兜底/静默回退:删壳即删,不留 `try/except ImportError` 之类的"温柔降级"。

## 字段 5 — 同文件碰撞债

**结论:无同文件碰撞债。**

- registry `same_file_siblings = []`(空)。
- registry `interference_edges` 有一条 `{other:R25, why:[file:schedule_graph_dispatch_context.py], same_file:true}`,**但此标注错误**:回盘 R25 的 `primary_file = ready_queue.py`(title=「graph/ready_queue.py 兼容导出 + get_ready_operation_ids」),与 R02 主文件 `schedule_graph_dispatch_context.py` **不是同一文件**。R02 与 R25 无真实同文件交集 → 不会撞行号、不会因 dict 键位移互相破坏。
- 该 same_file 边应视为 registry 误报,Layer2 干扰图重建时建议剔除 R02↔R25 的 same_file 标记(改无边或仅留 same_cluster C01 弱边)。

## 字段 6 — 同收口点债

**R02 不是收口类修法(是直删),无"收口点"概念。** 但需澄清相邻概念:
- 真 impl `schedule_graph_resource_matching_context.py:48` 的 `__all__`(:132)是该符号的唯一权威 home,R02 修法把测试 import 指向它即可,无新建模块、无第二门面。
- 同桶 B09 的 R25/R52/R06 与 R02 `co_change_within_group=[]`(registry phase1_blast),不同文件、不收口到同一符号 → **无同收口点债、无顺序敏感性**。
- Batch-10 内 R02 可任意时点独立落,registry 建议作为"最先落的热身动作"。

## 字段 7 — 收口行为差异及 parity 测试

**非收口,但需 import-equivalence 校验(轻量 parity)。**

包装器 dispatch_context.py:461-475 与真 impl resource_matching_context.py:48 的**调用行为完全等价**:包装器签名(schedule_input + keyword-only nodes/edges/graph_ready_context)逐字复制真 impl,:470-474 原样透传四参,无任何参数变形/默认值改写/异常拦截。故新旧两路对所有边界值(含 graph_ready_context=None、缺 predecessor_op_ids_by_op_id 触发 GraphInputContractError)**逐分支等价,无反例**——因为两路最终命中的是**同一个函数对象**(包装器只是再调一次)。

所需 parity 保障(实为回归保留,非新写差异测试):
- 现有 `test_graph_dispatch_context.py:53`(report_uses_fixed_predecessors_and_node_order)、`:64`(on_uses_graph_ready_context)两个用例,改 import 后必须仍绿——它们直接断言 ready_nodes 顺序与守卫行为,等价于把"真 impl 行为"钉住。
- **无需** None vs raise 类反例测试:守卫(raise GraphInputContractError)在真 impl 路径上,删壳不改它。

## 字段 8 — 爆炸半径(9 维)

- **生产消费者**:**零**。`rg build_first_wave_ready_nodes core/ web/ data/`(排除 impl 与 dispatch_context 自身)结果为空。schedule_graph_report.py 只 import 兄弟 `build_graph_resource_matching_projection`(:19、:180),从不 import 本符号。
- **测试消费者**:仅 `tests/scheduler_graph/test_graph_dispatch_context.py` —— import :11、调用 :55、:66(共 3 处,全在同一文件)。
- **真 impl 内部消费者**:`schedule_graph_resource_matching_context.py:115` 调本模块 def(走本地名,不经包装器),删壳不影响。
- **契约/拓扑测试**:`tests/regression_scheduler_candidate_py38_contract.py:30` 把 `schedule_graph_dispatch_context.py` 列入 banned-import/py38 语法白名单,**只查语法不 pin 导出面**,删一个 def 不会让它红(已读 25-35 行确认是文件路径清单)。
- **co_change_within_group**:[](空)。
- **downstream_effect**:无行为变化——删的是冗余转发层,真 ready 节点计算 + 灵魂守卫全在 impl。
- **fix_invalidation_risk**:none。
- **灾难链(若删错)**:
  - (a) 若**只删壳忘了先改测试 import** → `test_graph_dispatch_context.py:11` 从 dispatch_context 找不到该符号 → **ImportError(响亮失败,非静默)**,整个测试模块收集失败 → CI 红,立即可见,不会静默炸生产。
  - (b) 若**误删兄弟** `build_graph_resource_matching_projection`(:478) → schedule_graph_report.py:180 ImportError → 图调度报表路径炸(这是承重,故列入字段 4 禁区)。
  - (c) 若**整块改测试 import 路径**(把 4 符号都指向 resource_matching_context) → 后三符号在该模块不存在 → ImportError。
  - 三条都是**响亮 ImportError,无静默炸**——这是 low 严重度的根因。

## 字段 9 — 承重毗邻标记

- 本债 `load_bearing=false`、`lb_no_touch=null`。
- **同文件(schedule_graph_dispatch_context.py)毗邻承重**:兄弟包装器 `build_graph_resource_matching_projection`(:478-…)事实承重(report.py:180 唯一生产消费者),虽未被单列为 LB 债,但**删 R02 时它是禁区**——见字段 4。
- **跨文件灵魂守卫(禁区行)**:`schedule_graph_resource_matching_context.py:28-45` 的 `GraphInputContractError` 逻辑——
  - :33 `raise GraphInputContractError("graph_ready_context 缺少 predecessor_op_ids_by_op_id。")`
  - :43 `raise GraphInputContractError(f"graph_ready_context 包含无效 op_id：{raw_op_id!r}")`
  这是灵魂暗线(loud raise,非兜底),R02 修法**碰不到也不许碰**(删的是 dispatch_context 的壳,守卫在 resource_matching_context 的 impl 里)。
- registry `lb_colocation_danger: none` 成立。

## 字段 10 — 分层 / 导入环风险

**纠 phase1_blast 一处不准 + 确认零新增环风险。**

- 当前模块依赖实测:
  - `schedule_graph_resource_matching_context.py:5` **模块级** `from .schedule_graph_dispatch_context import build_predecessor_successor_maps, node_op_id, positive_op_id_set`(resource → dispatch,单向模块级边)。
  - `schedule_graph_dispatch_context.py:468、:489` 仅在**函数体内懒 import** resource_matching_context(dispatch → resource,懒边)。
  - 这是一个**已存在的、故意用懒 import 打破环**的设计:模块级只有 resource→dispatch 一条边,反向靠包装器内懒 import,避免模块加载期成环。
- registry `downstream_effect` 写"移除 dispatch→matching 模块边" **不准确**:dispatch→matching 本就**不是模块级边**(只是函数内懒 import)。删 R02 包装器移除的是 :468 那条**懒 import**,模块级拓扑(resource→dispatch 单边)不变。
- **删 R02 后**:dispatch_context 仍保留 :489(兄弟包装器的懒 import),resource→dispatch 模块级边不变 → **不引入新环、不引入越层 import**。三个文件都在 `core/services/scheduler/run/` 同层,无 core.algorithms→services / core.models→services 之类跨层风险。
- AST 分层红线:R02 修法不新增任何 import 跨层,0 违规。

## 字段 11 — 测试耦合

动手前需迁移的测试 = **仅 1 个文件、3 处**(全在 `tests/scheduler_graph/test_graph_dispatch_context.py`):
1. **先迁 import**(:10-15 块拆分,见字段 4 步骤 1):把 `build_first_wave_ready_nodes`(:11)单拎出来改成 `from core.services.scheduler.run.schedule_graph_resource_matching_context import build_first_wave_ready_nodes`,其余三符号留在原 dispatch_context import。
2. 调用点 :55、:66 **无需改**(符号名不变,只换来源模块)。
- 无其他测试文件耦合本符号(grep 已证实)。
- `tests/regression_scheduler_candidate_py38_contract.py:30` 不 pin 导出面,无需迁。

## 字段 12 — 前置依赖

- **无前置债**:registry `planned_deps_hint = 无桶内 co_change、无跨桶同文件边,完全独立`;回盘确认 R25 不同文件(字段 5),故连 same_file 软依赖也不成立。
- **无前置 F 门**:不依赖其它债先落,Batch-10 内任意时点可单独落,registry 建议作为 Batch-10 **最先落的热身动作**。
- 唯一内部时序约束(非跨债):**测试 import 拆分必须先于删壳**(字段 4),否则 ImportError;但这是单债内的两步顺序,不构成对其它债的依赖。

---

### 核验小结(供 Layer2/Layer4 对账)
- 行号:零漂移,registry location_status ✅准 成立。
- 纠 registry 三处:(1) `dispatch_context.py` 裸名实为 `schedule_graph_dispatch_context.py`;(2) interference_edge R02↔R25 `same_file:true` 错误(R25 主文件是 ready_queue.py,不同文件);(3) `downstream_effect` 称删"dispatch→matching 模块边"不准(该边是懒 import 非模块级,删后模块拓扑不变)。
- 修法类:直删壳 + 退测试,顺序=先拆测试 import 再删壳;承重禁区=兄弟 build_graph_resource_matching_projection + resource_matching_context 灵魂守卫。
- 爆炸半径:生产零消费,测试单文件 3 处,最坏只触发响亮 ImportError,无静默炸。owner_pending=false。

---

## 对抗核验(第二双眼睛)

**核验日期 2026-06-05｜只读不改｜结论:PASS(找不到反证)**

### 重 grep 命令与原始结果

```
rg -n build_first_wave_ready_nodes core/ web/ data/ tests/
```
命中(全部对上档案):
- `tests/scheduler_graph/test_graph_dispatch_context.py:11`(import)、`:53/:55`、`:64/:66`(用例名+2 处调用)
- `core/services/scheduler/run/schedule_graph_dispatch_context.py:461`(壳 def)、`:468`(懒 import 真 impl)
- `core/services/scheduler/run/schedule_graph_resource_matching_context.py:48`(真 impl def)、`:115`(impl 内部走本地 def)、`:132`(`__all__` 条目)

```
find core/services/scheduler/run -name dispatch_context.py   → 空(裸名文件不存在,确为 schedule_graph_dispatch_context.py 简称)
rg -n build_graph_resource_matching_projection core/ web/ data/ tests/
```
- 兄弟壳:`schedule_graph_dispatch_context.py:478`(def)、`:489`(懒 import);真 impl:`schedule_graph_resource_matching_context.py:86`、`:133`(`__all__`)
- 唯一生产消费者:`schedule_graph_report.py:19`(`... as _build_...`)、`:180`(调用)——**经 dispatch_context :478 壳消费,非真 impl**

### 逐项独立挑战结果

| 核验项 | 档案断言 | 独立复核 | 判定 |
|---|---|---|---|
| 壳 file:line | :461 def/:468 懒 import/体 461-475 | 逐字读 455-499:壳体 461-475、:476-477 空行、:478 兄弟,完全吻合 | ✅一致 |
| 转发等价 | :470-474 原样透传 4 参,无变形 | 实读:壳签名(schedule_input + kw-only nodes/edges/graph_ready_context=None)逐字复制真 impl :48-53;:470-474 透传无默认值改写/无 except 拦截 → 命中同一函数对象 | ✅无反例 |
| 生产零消费 | rg 结果为空 | rg 全仓:`build_first_wave_ready_nodes` 仅测试 3 处 + 两 impl 文件自身,生产零 import | ✅一致 |
| import 块不可整块挪 | :13/:14 只在 dispatch_context | 真 impl 模块 `__all__`(:131-134)仅含 `build_first_wave_ready_nodes`+`build_graph_resource_matching_projection`,**不含** build_predecessor_successor_maps/graph_score_weights → 整块挪必 ImportError | ✅档案拆分策略正确 |
| 承重禁区(兄弟壳) | report.py:180 唯一生产消费 | rg 证实:兄弟壳 :478 是 report.py 的真实消费路径,确属禁区,严禁连删 | ✅一致 |
| 灵魂守卫不被碰 | resource_matching_context.py:28-45 raise GraphInputContractError | 实读 :28-45:`:33`/`:43` 两处 loud raise,在真 impl 路径,删壳碰不到 | ✅一致 |
| 分层/导入环 | resource→dispatch 模块级单边;dispatch→resource 仅函数内懒 import | 实读:resource_matching_context.py:5 模块级 import dispatch(单向);dispatch :468/:489 函数内懒 import → 删壳移除的是 :468 懒边,模块拓扑不变,三文件同层 `run/` 无跨层 | ✅一致,0 AST 违规 |

### 纠偏/微调
- 档案字段 8「report.py 从不 import 本符号」表述**准确但可更精确**:report.py 消费的是兄弟壳(dispatch_context :478)而非真 impl,本符号(build_first_wave_ready_nodes)确实零生产引用。不影响任何结论。
- 档案对 registry 三处纠正(裸名简称 / R02↔R25 same_file 误标 / downstream「删模块边」实为懒边)经独立复核**全部成立**:`find` 证实裸名文件不存在;懒 import 性质经实读 :468/:489 确认。

### 终判
- **承重误删风险:无**(load_bearing=false;修法为合法直删壳,禁区已正确圈定兄弟壳 + 灵魂守卫,无删除/统一/透传/加形参混入承重区)。
- **灵魂线:无违规**(无新增兜底/静默回退;删壳即删,档案明确禁止 try/except ImportError 温柔降级)。
- **顺序约束正确**:先拆测试 import、后删壳;否则触发响亮 ImportError(非静默)。
- owner_pending=false,未擅自越权;此为 low 死叶子债,快核充分,PASS。
