# R48 逐债档案 — 整模块迁移残渣 config_adapter（B03 / C01）

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：本 dossier 引用的 `tests/regression_sp06_no_duplicate_defs.py` 已由 A 的 **P1.1（commit `7ca42ca4`）整删**（早于 P6），`NO_CFG_GET_TARGETS` 符号全树零命中。凡『同提交退该文件断言行』的步骤为 **no-op**（无文件可红）；若作白名单守卫引用，须确认该锚点已废或重指。详见 `ANCHOR-DRIFT-2026-06-05-POSTCOMMIT.md §六.3` + `_B_COMPAT_SAFEGUARDS.md §5`。


> 只读不改产物。回盘日 2026-06-05。owner_pending=false。
> R48 与 R45 是 **同一物理文件** `core/algorithms/greedy/config_adapter.py` 的两个叙述视角（R45=死壳，R48=整模块迁移残渣），务必合并为同一次整文件删除提交。

## 1) 当前真实 file:line（回盘）+ 漂移记录

整文件 `core/algorithms/greedy/config_adapter.py`，`wc -l` 实测 **27 行**。三符号 + import 行回盘：

- `CriticalConfigReadResult`（frozen dataclass，`value/missing/error` 三字段）→ `config_adapter.py:10`（字段 :11-13）
- `read_schedule_config_value(config, key)` → `config_adapter.py:16`（:17 调 `ensure_schedule_config_snapshot`，:18 `hasattr` 缺字段判定→:19 返 `missing=True`，:20-23 `try/getattr` 吞 `Exception` 塞 `.error`）
- `read_critical_schedule_config(config, key)` → `config_adapter.py:26`（:27 仅透传 `read_schedule_config_value`）
- import 行：`config_adapter.py:6` `from core.models.schedule_config_runtime import ensure_schedule_config_snapshot`

**漂移记录：**
- registry `old_location_DONT_TRUST` 称「全文件 28 行」+ `current_evidence` 引「read_schedule_config_value:16 / read_critical_schedule_config:26」。实测 **27 行**，符号 :16 / :26 / 类 :10 **全部原样命中**。
- 行号漂移 **幅度=0**（三符号位置精确一致）；唯一偏差是 **行数 28→27 的 +1 误差**（`phase1_blast.location_status` 已自标「⚠️行号已变(轻微)」并修正为 27）。删整文件时此 ±1 行数误差 **无任何影响**。
- **registry 的一处路径失准（沿用 R45 已抓结论）**：`planned_deps_hint`/`lb_colocation_danger` 把替代路径写成 `core/models/schedule_params.py`，实测该路径 **不存在**；真实路径为 `core/algorithms/greedy/schedule_params.py`（与 adapter 同目录）。`_snapshot_attr` 实测在该文件 **:59**（registry `current_evidence` 写 :59 正确，但 `downstream_effect` 又写 :59、`pathology` 写 :59 一致；仅 deps_hint 的目录前缀错）。此漂移不影响删除结论。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`。
- `current_evidence_2026_06_05` 称「文件未删（27 行原样），:16/:26 仍是吞异常塞 .error 的旧门面；替代路径 schedule_params.py 的 _snapshot_attr loud raise 已就位，但旧门面残渣未删、sp06:15 路径未退；git diff b08162cd 对该文件零变更」。
- **回盘逐项一致**：文件存在 27 行、:10/:16/:26 原样、`_snapshot_attr` 在 `schedule_params.py:59` 且为 loud-raise（:62/:64/:68 三处 `raise ValidationError`）已就位、`tests/regression_sp06_no_duplicate_defs.py:15` 仍含该路径成员。状态 `planned` 与现状 **完全吻合**，无假阳。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P3**（registry `pathology=P3`，描述为「整模块迁移残渣」；与 R45 标 P6 死壳是同一文件两种归类视角，不矛盾——P3 强调"重构留下的半截迁移"，P6 强调"无消费方的死壳"，对象同一）。`load_bearing=false`。
- 严重度 **low**。桶 **B03**（home=B03），簇 **C01**，分区 `core-algorithms`。
- 性质：配置校验链路重构（registry 记 `fd4b6a9c` 引入→`e02cb914` 拆除最后调用点）留下的整模块残渣。新路径 `schedule_params.py:59 _snapshot_attr` 用「内联 `getattr` 快照 + 缺字段即 `raise ValidationError`」全面取代了旧门面「把错误塞进 `.error` 字段绕过校验」的反向风格。旧门面三符号生产零引用（`rg` 全仓证实），唯一引用是 sp06 把 **文件路径** 列进 `NO_CFG_GET_TARGETS`（:15）。证据：`config_adapter.py:16-23`（吞异常旧风格）vs `schedule_params.py:62/64/68`（loud raise 新风格）。

## 4) 修法类

**直删 + 退测试**（非收口、非注释、非 raise、非 parity）。owner_pending=false，给终态修法。

- 删除整文件 `core/algorithms/greedy/config_adapter.py`（27 行，三符号全删）。
- **同一提交**从 `tests/regression_sp06_no_duplicate_defs.py:15` 的 `NO_CFG_GET_TARGETS` 列表移除 `REPO_ROOT / "core" / "algorithms" / "greedy" / "config_adapter.py"` 这一行。否则文件删后该测试用例（遍历清单逐个 `path.read_text` 跑 AST 检查）会因路径不存在而抛 `FileNotFoundError`，sp06 直接红。
- **R48 不是独立动作**：与 R45 是同一文件的两种叙述，**合并为单提交**整文件删除，避免半删 / 重复投工。

**收口点说明（重要）**：本债 **不走 P5 收口**。新路径 `_snapshot_attr`（`schedule_params.py:59`）已是收敛后形态，且旧门面零消费方——无任何调用方需要被"收口到统一点"。删除 = 直接消灭残渣，不引入新收口符号、不为已有概念新建第二模块。R09 的 `parse_optional_positive_int` 例外与本债无关。

**灵魂线检查（通过）**：`config_adapter.py:22-23` 的 `except Exception as exc: return CriticalConfigReadResult(None, error=exc)` 是吞异常成 error 字段的静默兜底（灵魂线关注点）。但本债修法是 **整文件删除**，删除即彻底消除该兜底；**不应** 改写为 loud raise（无消费方，改它是无意义投工且超出"只动声明范围"）。删后此反模式从代码库消失，净收益。

**非承重确认**：`load_bearing=false`、`lb_no_touch=null`，不触发承重修法（仅注释 + 绑契约）那条路径。

## 5) 同文件碰撞债

同物理文件 `config_adapter.py` 只有 **R45** 一个兄弟，且 `interference_edges` 标 `same_symbol=true`（共享 `read_critical_schedule_config`）。

- **R45（死壳视角）vs R48（迁移残渣视角）= 同一文件、同一删除动作的两个叙述**。不存在"谁先谁后"——它们 **不是两次改动**，是一次整文件删除 + 一次 sp06:15 退路径。
- **不会撞行号 / dict 键位移**：因为是整文件删除，无"改某段后另一债行号漂移"的问题；两债改的是同一组行（全文件 + sp06:15 同一行）。
- 协调要求：执行时 **只生成一个提交**覆盖 R45+R48，提交信息双引用两债 id。若被拆成两次提交，第二次会面对"文件已不存在"的空操作或重复退测试。
- 兄弟 R45 dossier 已落档（`dossiers/R45.md`），修法类一致（直删+退测试），结论无冲突。

## 6) 同收口点债

**无同收口点债**。本债不收口（见 §4）。

- `same_symbol=true` 的只有 R45，且 R45 同样是删除而非收口——两债 **共享被删符号 `read_critical_schedule_config`**，删除后该符号消失，不存在"多债收口到同一存活符号"的顺序敏感问题。
- 与 config 双栈收敛（LB07 / R71 / R47）**正交**：那条线的收口点是 `ensure_schedule_config_snapshot` / `config_snapshot` 系列（callgraph 显示 adapter `import` 它，方向是 adapter→snapshot）。删 adapter 不改双栈本体，`_snapshot_attr` 已是双栈收敛后的形态。无顺序敏感性。

## 7) 收口行为差异及 parity 测试

**不适用（本债非收口，是删除）**。无需 parity 测试。

- 但记录一处 **行为反差**供执行者警惕：旧门面 `read_schedule_config_value`（:18-23）对「缺字段」返回 `missing=True`、对「读取异常」返回 `error=exc`——**两路都不抛**，把错误塞进结果对象。新路径 `_snapshot_attr`（`schedule_params.py:62/64/68`）对「快照为 None / 缺字段 / 读取失败」**全部 `raise ValidationError`**（loud）。
- 二者语义相反（None-塞-字段 vs raise）。**正因生产零消费方**，这个反差不会被任何调用点感知——删除是安全的。反之，若旧门面还有活调用方，则删除属危险（会从"静默塞 error"突变成"loud raise"），但已 grep 证实零消费方（§8），故不构成阻塞。这是"为何可以直删而非必须 parity"的关键论据。

## 8) 爆炸半径

**生产消费者清单：**
- **零**。`rg "read_schedule_config_value|read_critical_schedule_config|CriticalConfigReadResult" core/ web/ data/` 唯一命中是 `config_adapter.py` 自身。`rg "config_adapter" core/ web/ data/` 在生产代码 **零命中**。
- callgraph（`callgraph_2026-06-05/edges.json`）佐证：涉 config_adapter 的边只有 3 条——① 文件内自调 `read_critical_schedule_config`→`read_schedule_config_value`（kind=local）；② / ③ `read_schedule_config_value`→`ensure_schedule_config_snapshot`（kind=import，方向 adapter 依赖 model/snapshot）。**没有任何 `from=外部模块`→`to=config_adapter` 的入边**，即无人调用它。

**测试消费者：**
- `tests/regression_sp06_no_duplicate_defs.py:15`（仅作为 `NO_CFG_GET_TARGETS` 的 **路径成员**，非 import 该模块）。删文件须同步删此行。
- 已核 `tests/gate_meta/test_sp05_path_topology_contract.py` **未列** config_adapter（SP05 只映射 `config_snapshot` 系列），删此文件 **不撞 SP05 拓扑契约**。

**灾难链（删错 / 漏退会静默炸什么）：**
- 漏退 sp06:15 → `test_sp06_..._cfg_get_fallback_helpers` 读不到文件抛 `FileNotFoundError`，**红测试**（响亮，非静默，易发现）。
- 半删（删文件但留某符号引用 / 或删 R45 不删 R48 视角导致认知混乱重复投工）→ 无运行期静默炸点，因生产零引用。
- **最坏静默风险其实极低**：唯一会"静默炸"的前提是有隐藏动态引用（`getattr`/字符串 import）。已 `rg` 全仓字符串 `"config_adapter"`、三符号名零命中，排除此路。

## 9) 承重毗邻标记 + 禁区行

- 本物理文件 `config_adapter.py` **无任何 LB 符号**，`load_bearing=false`，`lb_no_touch=null`。整文件可删，无禁区行。
- `interference_edges` 列的 LB07 / R33 / R51 三个 `same_file=true` 邻债 **都落在 `core/algorithms/greedy/schedule_params.py`**（非 adapter 文件）——它们与 R48 共享的是 *替代路径文件*，不是 *被删文件*。删 adapter **完全不触碰** schedule_params.py 一个字符，故 LB07（承重）零风险。
- **禁区提醒（针对执行者，非本文件内）**：删 adapter 时 **绝不顺手动** `schedule_params.py`（LB07/R33/R51 居所）；本债改动面严格限定在 `config_adapter.py`（删）+ `regression_sp06_no_duplicate_defs.py:15`（退一行）两处。

## 10) 分层 / 导入环风险

- **零越层、零导入环。** adapter 唯一 import 是 `config_adapter.py:6` `from core.models.schedule_config_runtime import ...`，方向 `core.algorithms → core.models`（合法下游依赖）。
- 删除一个 **只出不进**（无入边）的叶子模块，不可能新增任何 import、不可能形成环。删后 `core.models.schedule_config_runtime` 少一个上游引用者而已，其本体仍被 `schedule_params.py` / `schedule_config_runtime_coercion.py` 使用（`rg` 证实），不会变孤儿。
- 不引入 `core.algorithms→core.services` 或 `core.models→core.services` 等红线越层（删除动作本身只减边）。0 AST 违规。

## 11) 测试耦合

- 动手前 **无需迁移测试**，只需 **退一行**：`tests/regression_sp06_no_duplicate_defs.py:15` 从 `NO_CFG_GET_TARGETS` 删掉 config_adapter 路径成员，与删文件 **同提交**。
- 该测试本身（sp06 守卫）不依赖 adapter 的行为，只把它列入"禁止出现 cfg_get 回退助手"的扫描目标清单——退出清单不削弱守卫对其余 4 个目标（scheduler.py / 3 个 summary 文件）的保护。
- 无单测直接 import config_adapter（`rg` 证实），故不存在"先迁单测再删"的耦合。

## 12) 前置依赖

- **前置债：与 R45 合并执行**（同一文件同一删除）。严格说不是"R45 先于 R48"，而是"R45+R48 = 一次提交"。两者 dossier 均已落档、结论一致，可立即合并落地。
- **前置 F 门**：无强 F 门阻塞。`planned_batch_hint` 标 **Batch-5，独立、零 LB 耦合、最早可落**。唯一软约束是与 config 双栈收敛（LB07/R71/R47）**正交**——无需等双栈收敛完成即可删（`_snapshot_attr` 已是收敛后形态）。
- 执行顺序建议：本债（合并 R45）可作为 C01 簇里 **最早、最低风险**的一刀先落，为后续簇内债清出干扰。无任何债需在它之前完成。
- owner_pending=false，无需 owner 裁断。

## 对抗核验（第二双眼睛）

回盘日 2026-06-05。独立重 grep，默认怀疑，逐项挑战档案——结论 **PASS，无争议**。

**重 grep 命令与结果：**

1. 行号 + 全文回盘 `wc -l core/algorithms/greedy/config_adapter.py` → **27 行**（档案称 27 ✅；registry old_location 称 28，+1 误差，删整文件无影响）。`rg -n "class CriticalConfigReadResult|def read_schedule_config_value|def read_critical_schedule_config" config_adapter.py` → 类 **:10**、`read_schedule_config_value` **:16**、`read_critical_schedule_config` **:26**，三符号 **全部精确命中，漂移幅度=0**。`except Exception` 在 :22（吞异常塞 .error，灵魂线反模式，删除即消除——核实档案 §4.2 修法不改写为 raise 的判断正确，无消费方故无意义投工）。import 行 :6 `from core.models.schedule_config_runtime import ensure_schedule_config_snapshot`，方向 `core.algorithms→core.models` 合法下游，**无越层无环**。

2. 生产零引用 `rg -n "read_schedule_config_value|read_critical_schedule_config|CriticalConfigReadResult" core/ web/ data/` → **唯一命中是 config_adapter.py 自身**（含内部自调）。`rg -n "config_adapter" core/ web/ data/` → **零命中**，排除字符串/动态 import 隐藏引用。生产消费者确认 **零**。

3. 测试耦合 `rg -n "config_adapter|read_schedule_config_value|..." tests/` → 唯一命中 `tests/regression_sp06_no_duplicate_defs.py:15`（`NO_CFG_GET_TARGETS` 路径成员）。`sed -n '72,73p'` 证实该测试对 `NO_CFG_GET_TARGETS` 逐个 `path.read_text(...)`+`ast.parse`——**漏退此行删文件后必抛 `FileNotFoundError` 红测试（响亮、非静默）**，档案 §8 灾难链判断准确。`rg config_adapter tests/gate_meta/test_sp05_path_topology_contract.py` → 零命中，**不撞 SP05 拓扑契约**，档案 §8 确认无误。

4. 替代路径真伪 `ls core/algorithms/greedy/schedule_params.py` **存在**；`ls core/models/schedule_params.py` **不存在**——证实档案 §1 抓的 registry `planned_deps_hint` 目录前缀错（误写 core/models/）这一漂移，真实路径在 greedy/ 同目录。`rg -n "_snapshot_attr|raise ValidationError" schedule_params.py` → `_snapshot_attr` **:59**，loud raise **:62/:64/:68**（缺快照/缺字段/读取失败全部 raise ValidationError），与旧门面「塞 .error 不抛」语义相反——**正因生产零消费方，此语义反差无人感知，直删安全**，档案 §7 关键论据成立。

5. 孤儿检查 `rg -l "schedule_config_runtime|ensure_schedule_config_snapshot" core/ web/ | grep -v config_adapter` → 删 adapter 后 `schedule_config_runtime` 仍有 **11 个上游引用者**（summary 系列 / schedule_params / config_snapshot / coercion 等），**不变孤儿**，档案 §10 正确。

**承重误删风险：无。** `load_bearing=false`、`lb_no_touch=null`、本文件零 LB 符号；`interference_edges` 的 LB07/R33/R51 均落在 schedule_params.py（替代路径文件，非被删文件），删 adapter 不触碰其一字符。修法是「直删整文件 + 退 sp06:15 一行」，非收口/非透传/非加形参/非把删除伪装成无害统一——合规。

**owner_pending=false 核查：** 给出终态修法（直删+退测试）并标 Batch-5 合理，无擅自越权。

**与 R45 协调：** `same_symbol=true` 共享 `read_critical_schedule_config`，二者为同一文件删除的两个叙述视角，须合并单提交——核实无冲突，与 R45 dossier 结论一致。

**核验结论：PASS。** 找不到任何反证：行号零漂移、生产零引用三重证实（符号名+文件名字符串+SP05 排除）、替代路径 loud raise 已就位、删后不留孤儿、非承重、测试耦合仅一行退路径且漏退是响亮红测试。低危纯死叶子债，可与 R45 合并安全直删。
