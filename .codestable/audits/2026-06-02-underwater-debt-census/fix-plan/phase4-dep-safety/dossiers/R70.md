# R70 逐债档案 — `_raise_schedule_empty_result` 逐字两份（死副本直删）

> 桶 B15 · 簇 ISOLATED · 病理 P5 · 严重度 low · 承重 false · owner_pending false
> 主文件 `core/services/scheduler/schedule_service.py`
> 本档案只读不改，仅产计划。所有断言附当前工作区 file:line 证据（已 grep 回盘）。

## §1 当前真实 file:line（回盘）+ 漂移记录

grep 回盘（`rg -n "_raise_schedule_empty_result" core/ web/ data/ tests/`，当前工作区）：

| 角色 | 符号 | registry old_location（勿信） | 实际 file:line（回盘） | 漂移 |
|---|---|---|---|---|
| **死副本（本债主体）** | `def _raise_schedule_empty_result` | `schedule_service.py:46` | `core/services/scheduler/schedule_service.py:46` | **无漂移**（仍 :46，func 体 :46-50，:51 空行）|
| live 定义 | `def _raise_schedule_empty_result` | `schedule_input_collector.py:79` | `core/services/scheduler/run/schedule_input_collector.py:79` | **路径纠偏**：registry old_location 写的是 `schedule_input_collector.py:79`（裸路径），真身在 **`run/` 子目录**下；同名顶层文件 `core/services/scheduler/schedule_input_collector.py` 内**无此符号**（grep 0 命中）。行号 :79 无漂移。 |
| live 调用 | 调用点 ×3 | — | `run/schedule_input_collector.py:173 / :228 / :251` | 无漂移 |
| live 注入 | `raise_schedule_empty_result_fn=` | — | `run/schedule_input_collector.py:356` | 无漂移 |

**关键回盘结论**：仓内存在 **两个** `schedule_input_collector.py`（顶层 `core/services/scheduler/schedule_input_collector.py` 与 `core/services/scheduler/run/schedule_input_collector.py`）。本债的 live 份**只**在 `run/` 那个里；不要被裸文件名误导去改顶层那个（顶层无此符号）。死副本与 live 份函数体**逐字节相同**（已对照 §7）。

证据：
- 死副本 `schedule_service.py:46-50`（grep 在本文件内对 `_raise_schedule_empty_result` 计数=1，即仅 def 行，零调用方）。
- live 份 `run/schedule_input_collector.py:79` 定义 + `:173/:228/:251` 调用 + `:356` 作为 `raise_schedule_empty_result_fn=` 注入下游。

## §2 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = "planned"`，本轮任务卡片 `当前状态: planned`。
- current_evidence 称「计划要求纯删 schedule_service.py:46-50 未执行」「git diff vs b08162cd 该文件零改动」「tests/ 无 `_raise_schedule_empty_result` 单点 grep 守卫测试」。
- **逐项核实，全部一致**：
  - `git diff b08162cd -- core/services/scheduler/schedule_service.py` 输出为空 → 死副本仍在，纯删动作**未执行**，与 planned 一致。
  - 死副本仍占 `:46-50`，与 evidence 一致。
  - `rg -ln "_raise_schedule_empty_result" tests/` 0 命中 → 无符号级 grep 守卫，与 evidence 一致。
- **结论：状态 planned，与 status evidence 100% 一致，无漂移、无幻影。**

## §3 病理 / 严重度 / 桶 / 簇

- **病理 P5**（重复定义 / 待收口）。但 phase1 已纠偏：本债**不是真 twin 框架**——两份中 `schedule_service.py:46` 那份在本文件内**零调用**（死码），只有 `run/schedule_input_collector.py` 那份是 live。所以「逐字两份均 loud raise」的表述掩盖了「一活一死」实情。
- **严重度 low**：两份函数体逐字节相同，且死副本零调用，删它**零行为影响**；即便保留也只是死码，不构成线上分叉。
- **桶 B15**；**簇 ISOLATED**（`same_file_siblings=[]`、`interference_edges=[]`）。
- 病理本质：排产空结果异常装配 `ValidationError(field="排产") + details["reason"]`，理论上「两份分叉会让 reason 装配不一致」，但当前两份**字节相同**，分叉风险=0；删死副本后仓内仅剩 live 单点定义，P5 天然闭合。

## §4 修法类

**修法类：纯删（直删死码），不收口、不新建 helper、不动 live 份。**

具体动作（计划，本轮不执行）：
1. 删除 `core/services/scheduler/schedule_service.py:46-50` 整个死 `_raise_schedule_empty_result` 函数定义（连同其后的空行收口到符合 PEP8 的两空行间距）。
2. **不动** `run/schedule_input_collector.py:79` 一个字节——它是唯一 live 份，删死副本后它成为全仓**天然单点**，R70 即闭合，**无需任何「收口到上游 helper」动作**。
3. **不删** `schedule_service.py:7` 的 `from core.infrastructure.errors import BusinessError, ErrorCode, ValidationError`——证据：`ValidationError` 在同文件 **:217** 仍被 `raise ValidationError("系统正在执行排产，请稍后重试。", field="排产")` 使用（grep 实证），删 import 会导致 `:217` 触发 `NameError`。

**为何不是收口类**：phase1 已证 twin 框架不成立（一活一死）。死副本零调用 → 删它就是删死码，不存在「把两个 live 调用点统一到一个 helper」的收口需求。这是 P5 桶里**最便宜的一种**闭合方式。

**灵魂线核对**：删的是死码，live 份本身已是 loud raise（`raise exc`，无兜底/无静默回退/无 except 吞错），删除动作**不引入任何兜底**，符合「P4/P5 必须 loud raise」灵魂线。

## §5 同文件碰撞债

`same_file_siblings = []` → **无同文件碰撞债**。

- registry `same_file_siblings=[]`、`interference_edges=[]`，本债簇 ISOLATED。
- co_change_within_group 列了 `R68/R69`，但 phase1_blast 与 planned_deps_hint 均明确：R68/R69 与本债只是「同批（Batch-15）」语义，**触不同文件**，无 file 冲突、不撞行号、不引发 dict 键位移。
- 删 `schedule_service.py:46-50` 后该文件其余行号上移 5 行，但本文件内**无其他在册债**引用绝对行号 → 不会撞任何 sibling 的行号锚点。

## §6 同收口点债

**无**。本债是纯删，不收口到任何符号，因此不存在「收口到同一符号的其他债」与顺序敏感性问题。

（对照：live 份 `run/schedule_input_collector.py:79` 已是单点定义，本债**不触碰**它，故也不与任何「收口到 input_collector helper」的债产生顺序耦合。）

## §7 收口行为差异 + parity 测试

**本债不收口 → 原则上无需 parity 测试。** 但为防「删错」自证，记录两份的逐字节等价证据：

死副本 `schedule_service.py:46-50`：
```
def _raise_schedule_empty_result(message: str, *, reason: str) -> None:
    exc = ValidationError(message, field="排产")
    exc.details = dict(exc.details or {})
    exc.details["reason"] = reason
    raise exc
```
live 份 `run/schedule_input_collector.py:79-83`：
```
def _raise_schedule_empty_result(message: str, *, reason: str) -> None:
    exc = ValidationError(message, field="排产")
    exc.details = dict(exc.details or {})
    exc.details["reason"] = reason
    raise exc
```
**逐字节相同**（同签名、同 `field="排产"`、同 `details["reason"]` 装配、同 `raise exc`，且两处 `ValidationError` 均来自 `core.infrastructure.errors`）。

- **边界 parity 反例无意义**：因死副本零调用，删它后无任何代码路径行为改变，None/raise 分支不存在差异。
- **建议（可选、非阻塞）守卫**：补一条 `tests/` 级单点 grep 守卫——断言全仓 `def _raise_schedule_empty_result` 仅 1 处（`run/schedule_input_collector.py`），防回归再生第二份。当前 evidence 已确认无此守卫。这是**加固**而非 parity，删除动作本身不依赖它。

## §8 爆炸半径

**生产消费者（live 份，本债不触碰，仅记录拓扑）：**
- `core/services/scheduler/run/schedule_input_collector.py:173`（调用 live）
- `core/services/scheduler/run/schedule_input_collector.py:228`（调用 live）
- `core/services/scheduler/run/schedule_input_collector.py:251`（调用 live）
- `core/services/scheduler/run/schedule_input_collector.py:356`（`raise_schedule_empty_result_fn=_raise_schedule_empty_result` 注入下游）

**死副本消费者：**
- `core/services/scheduler/schedule_service.py:46` —— **零调用方**（grep 本文件内计数=1=仅 def；无 `__all__`；不被任何 import 引用）。这是本债**唯一要删**的对象。

**测试消费者：**
- 无任何测试以符号名引用 `_raise_schedule_empty_result`（`rg -ln ... tests/` = 0 命中）。
- 空结果行为由**行为级**回归覆盖：`tests/regression_schedule_history_not_created_for_empty_schedule.py`、`tests/regression_schedule_service_empty_reschedulable_rejected.py`（import `ScheduleService`，不 pin 私有符号 → 删死副本不会让它们红）。
- 契约/拓扑测试：`tests/regression_schedule_service_facade_delegation.py` —— grep 实证**不引用** `_raise/empty_result`（仅在 :25 校验 facade 委托签名 `params[1].default is inspect.Signature.empty` 中出现单词 `empty`，与本符号无关）→ 直删死副本不会让它红。

**灾难链分析：**
- **删错（删成 live 份）会炸什么**：若误删 `run/schedule_input_collector.py:79` 而非 `schedule_service.py:46`，则 :173/:228/:251 三处调用与 :356 注入立即 `NameError`，排产空结果路径整体崩。→ **缓解：本债白纸黑字只删 `schedule_service.py:46-50`，禁碰 `run/` 文件。**
- **删带 import 会炸什么**：若顺手删 `schedule_service.py:7` 的 `ValidationError` import，则 :217 的 `raise ValidationError(...)` 触发 `NameError`，「系统正在执行排产」并发拒绝路径崩。→ **缓解：import 行必须保留（§9 禁区）。**
- **静默炸风险**：本债无静默回退面；死副本删除是纯结构动作，无行为分支，不会产生「静默炸」。最坏只是语法/NameError，CI 立即可见。

## §9 承重毗邻标记 + 禁区行

- registry `load_bearing=false`、`lb_no_touch=null`、phase1_blast `lb_colocation_danger="none"`。
- `schedule_service.py` 虽是 facade，但 `_raise_schedule_empty_result` **非 facade 委托面**（facade_delegation 契约不引用它），故本函数所在位置**不在承重清单**。
- 修法是直删，无承重约束触发；**不需要**「补『我是故意的』注释 + 绑契约测试」那套承重处理（那是 load_bearing=true 才走的路）。

**禁区行（删除时严禁碰）：**
- `schedule_service.py:7` —— `from core.infrastructure.errors import BusinessError, ErrorCode, ValidationError`（:217 仍用 ValidationError，删则 NameError）。
- `schedule_service.py:217` —— `raise ValidationError("系统正在执行排产，请稍后重试。", field="排产")`（依赖 :7 import）。
- `run/schedule_input_collector.py:79 / :173 / :228 / :251 / :356` —— 唯一 live 份及其全部消费点，**一个字节都不动**。

## §10 分层 / 导入环风险

- **零新增 import**：纯删动作，不引入任何 import，自然不可能造越层 import 或导入环。
- 现状 `schedule_service.py` 已 `from .run.schedule_input_collector import collect_schedule_run_input`（:19，service→run 子模块，同层内合法），删死副本不改这条、也不新增对 live `_raise_...` 的 import（live 份是 `run/` 内部私有 helper，不对外导出，本债**不**让 service 去 import 它）。
- 不触发 `core.algorithms→core.services` / `core.models→core.services` 越层（本债全程在 `core.services.scheduler` 内）。AST 违规增量 = 0。

## §11 测试耦合

- **动手前需迁移的测试：无。** 无任何测试以符号名 pin `_raise_schedule_empty_result`，删除前无需迁移/改测试。
- 删除后**应跑**（自证零行为影响，非阻塞迁移）：
  - `tests/regression_schedule_service_facade_delegation.py`（确认 facade 委托面未受扰）
  - `tests/regression_schedule_history_not_created_for_empty_schedule.py`
  - `tests/regression_schedule_service_empty_reschedulable_rejected.py`
- **可选加固**：新增「单点 grep 守卫」测试（见 §7），防第二份再生。

## §12 前置依赖

- **零前置债**：簇 ISOLATED，`same_file_siblings=[]`、`interference_edges=[]`、无跨桶同文件边（`cross_bucket_same_file_edges=[]`）。R68/R69 仅「同批」语义、触不同文件，**不构成前置/后置依赖**。
- **F 门**：仅需 F-语法门（删后文件能 import）+ F-分层门（AST 0 违规，本债天然满足）；无需 owner 裁断（`owner_pending=false`）。
- **批次定位**：Batch-15（PHASE1 §4，独立并行），本桶内**最先落**——零前置、零风险、纯删，先落以缩小 B15 面。

## 返回摘要

R70：纯删 `schedule_service.py:46-50` 死副本；live 份 `run/schedule_input_collector.py:79` 不碰；保 `:7` import（:217 仍用 ValidationError）；零前置、零承重、零测试迁移、ISOLATED；状态 planned 与 evidence 一致。

## 对抗核验（第二双眼睛）

核验人：独立对抗 agent（只读不改）。结论 **PASS**——逐条试图找反证，全部落空。

**重 grep 命令与结果（当前工作区，2026-06-05）：**

1. `rg -n "_raise_schedule_empty_result" core/ web/ data/ tests/`
   → 死副本 `schedule_service.py:46`（def）；live `run/schedule_input_collector.py:79`(def)/:173/:228/:251(调用)/:356(注入)。**行号与档案逐项一致，零漂移。** tests/ 0 命中（exit=1）→ 无符号级 pin/守卫，确认无测试迁移。

2. `grep -c "_raise_schedule_empty_result" core/services/scheduler/schedule_service.py` → **1**（仅 def 行）。配合 `grep "from .*schedule_input_collector import"` → :19 仅引 `collect_schedule_run_input`，**从不 import live `_raise`**。双证死副本零调用方，直删零引用成立。

3. `grep -n "_raise_schedule_empty_result" core/services/scheduler/schedule_input_collector.py` → **exit=1（无命中）**；`cat` 该文件（185B）→ 仅 `from .run.schedule_input_collector import ...` 转发壳 + `__all__=["ScheduleRunInput","collect_schedule_run_input"]`（不含本符号）。**「路径纠偏」成立**：registry 裸路径 `schedule_input_collector.py` 会误导到这个无符号的顶层壳，真身在 `run/` 子目录。删错文件方向已被钉死。

4. `diff <(sed -n '46,50p' schedule_service.py) <(sed -n '79,83p' run/schedule_input_collector.py)` → **IDENTICAL**。两份函数体逐字节相同，删死副本零行为差异，§7 等价断言属实。

5. `grep -n "ValidationError" schedule_service.py` → :7(import)/:47(死副本)/:217(并发拒绝 `raise ValidationError("系统正在执行排产…")`，live 路径）。**§9 import 禁区成立**：删死副本后 :217 仍依赖 :7 import，:7 必须保留，否则 NameError。

6. `git diff b08162cd -- core/services/scheduler/schedule_service.py` → **空**。纯删未执行，与 status=planned 100% 一致。

**专项判定：**
- **承重误删风险：无。** registry `load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger="none"`。修法是纯删死码、不收口/不建 helper/不动 live/不加形参/不删 import，未混入任何「看着像该统一」的清理。死副本非 facade 委托面，不在承重清单。
- **owner_pending=false**：档案给了终态修法与批次（Batch-15 内最先落），符合规则——非 owner_pending 债允许给终态修法，无擅断。
- **灵魂线：无违反。** 纯删动作不引入兜底/静默回退/except 吞错；live 份本身 `raise exc`（loud raise），删死副本不改其响度。
- **分层/导入环：增量 0。** 纯删不引入任何 import，全程在 `core.services.scheduler` 内，不触越层。

**唯一可能争议点（非阻塞，记录）**：registry `phase1_blast.planned_fix_class` 仍写「删+收口（… input_collector live 份收口到主链共同上游 single helper）」，与档案 §4「纯删、不收口、不动 live」存在措辞张力。但档案已正确判定 twin 框架不成立（一活一死），且两份字节相同、live 已是单点 helper，**无收口需求**——档案结论正确，registry 旧 hint 属早期未纠偏残留，不影响修法。建议执行时以档案 §4 为准。

**最终：R70 核验 PASS，行号一致，无承重误删风险，无需第三方裁断。**
