# R41 逐债档案 — enum_display.py 中文枚举标签是 enum_normalizers 收口点的第二套私有实现（语义已漂移）

- 债 id: R41 | 桶: B04 | 簇: C01 | 病理: P5（重复实现/未收口）| 严重度: medium
- 承重: false | owner 须裁断: true（owner_pending=true）| 同文件兄弟债: 无（same_file_siblings=[]）
- 分区: web-all | kind: real | needs_adversarial: false | adv_refuted: false

> 铁律执行说明：本档案只读不改，仅产计划。owner_pending=true，故终态修法标注「待 owner 裁断，暂不给终态修法/暂不分配执行批次」，但依赖/爆炸半径已分析透。

---

## 字段 1：当前真实 file:line（grep 回盘）+ 漂移记录

回盘命令：`rg -n "^def (machine_status_zh|operator_status_zh|...)" web/routes/enum_display.py` 实测当前态：

| 符号 | old_location_DONT_TRUST | 当前真实 file:line | 漂移 |
|---|---|---|---|
| machine_status_zh | enum_display.py:13 | `web/routes/enum_display.py:13` | 无 |
| operator_status_zh | enum_display.py:13-79 段内 | `web/routes/enum_display.py:24`（"停用/休假":29） | 无（旧报告未给逐符号行） |
| day_type_zh | 段内 | `web/routes/enum_display.py:33` | 无 |
| batch_status_zh | 段内 | `web/routes/enum_display.py:47` | 无 |
| priority_zh | 段内 | `web/routes/enum_display.py:62` | 无 |
| ready_zh | enum_display.py:79（报告写到 :79） | `web/routes/enum_display.py:73`（`return "未齐套"` 在 :79） | **轻微漂移**：旧报告把范围末尾写成 :79（其实 :79 是 ready_zh 函数体最后一行的 return），ready_zh 的 `def` 实际在 :73。registry.phase1_blast.location_status 已自纠（"末符号是 ready_zh:73 非79"）。 |
| _merge_mode_zh | process_bp.py:16 | `web/routes/process_bp.py:16` | 无 |
| _source_zh | process_bp.py:22-25 | `web/routes/process_bp.py:22`（`else "自制"` 实为 :25 `return "自制"`） | 无 |

收口点（enum_normalizers.py）逐符号实测：
- `operator_status_label` → `core/services/common/enum_normalizers.py:82`（inactive→"停用":87）
- `machine_status_label` → `:124`
- `source_type_label` → `:159`（先经 `normalize_op_type_category`:135）
- `batch_priority_label` → `:209`
- `ready_status_label` → `:220`
- `calendar_day_type_label` → `:231`

**漂移结论：行号整体未漂（git diff vs b08162cd 为空），仅旧报告范围末值 79 vs 真实 def 73 的轻微表述误差，符号全部命中。**

## 字段 2：当前状态 + 与 status evidence 一致性

- registry 状态：`status_2026_06_05 = planned`。
- 一致性核实：✅ 完全一致。`enum_display.py` 顶部仅 `from core.models.enums import ...`（:3-10），**未 import 任何 enum_normalizers / *_label**；`process_bp.py:8` 仅 `from core.models.enums import MergeMode, SourceType`，亦未 import 收口函数。收口动作一步未做 → planned 属实，非 in_progress。
- 灵魂线证据仍在：`ready_zh`:79 `return "未齐套"`（未知/None 静默贴确定态，未治）；`_source_zh`:25 `return "自制"`（外协别名误判，未治）；`operator_status_zh`:29 `"停用/休假"`（未收口为"停用"）。
- 钉死测试仍在：`tests/web_pages/test_enum_display_consistency.py:59-61` 仍断言 `ready_zh("weird"/""/None)=="未齐套"`（计划要求改 loud 暴露，未改）。

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 P5：`enum_display.py` 的 6 个 `*_zh` + `process_bp.py` 的 `_source_zh` 是 `enum_normalizers.py` 已有标签函数的**第二套私有实现**，且文案/坏值口径已漂移（见字段 7）。`calendar_day_type_label` 已在导出路径（`personnel_excel_operator_calendar.py:86`/`scheduler_excel_calendar.py:56`）被采用，而显示路径仍走私有 `day_type_zh` → **同概念双口径实证**。
- 严重度 medium：纯展示层文案，不参与算法/落库；但坏值口径分歧（"未齐套" vs "齐套"）会让前端状态列在边界值上误导用户，故非 low。
- 桶 B04（home=B04，enum 收口桶）；簇 C01（中文标签收口簇）。

## 字段 4：修法类

**类型：收口到 enum_normalizers 已存在标签函数（P5 收口）+ 同步把钉死测试改 loud 暴露。承重=false，可动结构（非仅注释）。**

⚠️ **owner_pending=true → 终态修法待裁，本档案暂不给终态、暂不分配执行批次。** 仅给「若 owner 批准后的候选修法形状」，逐符号：

| 私有符号 | 候选收口点（已存在，不新建） | owner 裁断门 |
|---|---|---|
| `machine_status_zh` | `machine_status_label`（:124） | 无语义差，文案全等，可直接收口 |
| `batch_status_zh` | — **无对应收口点**（enum_normalizers 无 batch_status_label，只有 batch_priority_label）→ **本符号不收口，保留** | n/a |
| `priority_zh` | `batch_priority_label`（:209） | 边界差异见字段7（"weird"→"未知" vs 现也"未知"，但空串口径不同），需 parity |
| `ready_zh` | `ready_status_label`（:220） | **灵魂线**：未知/None/空 由静默"未齐套"改为暴露，且空串口径反转，须 owner 拍板（见字段7反例） |
| `operator_status_zh` | `operator_status_label`（:82） | **语义裁断**："停用/休假"→"停用"（open_questions），须 owner 先定是否可丢"/休假" |
| `day_type_zh` | `calendar_day_type_label`（:231） | 边界差异（"WorkdayX" 透传 vs 现也透传，但空串口径不同），需 parity |
| `_source_zh`(process_bp) | `source_type_label`（:159，先 `normalize_op_type_category`） | **灵魂线**："外协/外"不再误判"自制"，行为变化须 owner 知情 |

收口点合法性核实：以上 6 个 *_label 均为 **enum_normalizers.py 已存在**符号（grep 实测行号在字段1），**不新建任何第二模块、不触发新 P5**。`batch_status_zh` 无收口点故排除（强行新建 batch_status_label = 新 P5，禁止）。

灵魂线红线（铁律4）：
- **绝不**在收口包装里加 `try/except` 吞错或兜底回退。
- ready/source 的「未知值」处理必须走收口点的 `unknown_policy="passthrough"`（已暴露原值/"未知"），**不得**重新贴回确定态"未齐套"/"自制"。
- 测试改法：`test_enum_display_consistency.py:59/:60/:61` 必须改为断言 **loud 暴露**（passthrough 原值或"未知"），**绝不**删了重钉静默口径。

## 字段 5：同文件碰撞债

`same_file_siblings = []` → **本文件（enum_display.py / process_bp.py）无同文件兄弟债**，无 dict 键位移 / 行号互撞风险。

interference_edges 三条均为 `same_file=true` 但**指向其他文件**，非本债主文件：
- `LB04` ⟷ via `enum_normalizers.py`（收口点文件，见字段6——这是同收口点而非同主文件碰撞）。
- `R37` ⟷ via `equipment_pages.py`（R41 的下游消费者文件，R37 改的是 `operator_machine_repo` 死方法，与 R41 改的 enum 标签调用不在同一段；R37 是删死方法，R41 是改 import 源，二者在 equipment_pages.py 互不交叠，但**同文件改动需排序避免 diff 冲突**）。
- `R43` ⟷ via `scheduler_batches.py`（R41 的下游消费者文件，R43 是删 9 个顶层 wrapper 别名残渣；scheduler_batches.py:149-151 调用 `_batch_status_zh/_priority_zh/_ready_zh`，R41 收口后这些调用值不变，R43 删的是 import 路径 wrapper，**理论不交叠**，但同文件需排序）。

→ **结论：本债主文件零同文件兄弟；三条边是「下游消费者文件 / 收口点文件」级耦合，落地时按文件粒度排序即可，不存在同函数体行号互撞。**

## 字段 6：同收口点债 + 顺序敏感性

- **同收口点债 = LB04（via enum_normalizers.py）。** LB04 是承重债（load_bearing=true），治 `normalize_yes_no_wide` 在 core/shared 与归一化矩阵的双实现；它**改的是 enum_normalizers.py 内部依赖的 normalization_matrix 层**（boolean_normalize / normalize_yes_no_wide），与 R41 要收口去的 6 个 *_label 函数**不是同一批符号**。
- **顺序敏感性（registry planned_deps_hint 已钉死）：R41 须在 Batch-1（LB04 等承重注释 + 安全网）之后落。** 原因：LB04 给 enum_normalizers/normalization_matrix 加承重注释 + parity 安全网，R41 收口会大量新增对 enum_normalizers.py 的调用，若 LB04 的承重护栏未先就位，R41 收口可能在无网情况下改动收口点行为基线。
- co_change_within_group=[LB04]，但 registry 标注「同桶，独立」「fix_invalidation_risk=none」——即 LB04 与 R41 改的符号不重叠，先后做都不会让对方失效，仅时序上 R41 必须排在 LB04 之后（护栏先行）。

## 字段 7：收口行为差异 + 所需 parity 测试

收口**不是逐分支等价**，存在多处行为反转。逐符号给反例（旧 `*_zh` → 新 `*_label`）：

**(a) ready_zh → ready_status_label（最危险，空串口径反转）**
- 旧 `ready_zh("")`：`(""or"").strip()==""` → 落到末行 `return "未齐套"`（enum_display.py:79）。
- 新 `ready_status_label("")`：→ `normalize_ready_status_value("")` → `_enum_alias_lookup` 空串走 `return str(default)`（normalization_matrix.py:100-101），default=`ReadyStatus.YES`="yes" → label 返回 **"齐套"**。
- ⚠️ **反例：`ready_zh("") == "未齐套"` 但 `ready_status_label("") == "齐套"` —— 边界值语义直接反转（未齐套↔齐套）！** 这不是"治灵魂线"，是真实文案翻转，**必须 owner 拍板**：空串到底该当"齐套"还是"未齐套"还是暴露"未知"。
- 旧 `ready_zh("weird")`/`ready_zh(None)` → "未齐套"（静默贴确定态，灵魂线病）；新 `ready_status_label("weird")` → passthrough → "weird"；`ready_status_label(None)` → 空串 → default "齐套"。
- **所需 parity 测试**：改 `test_enum_display_consistency.py:59/:60/:61` 为断言 loud 暴露口径，并**新增一条专钉空串反转**的断言（owner 定向后才能写死期望值），防止后续 LLM 又把它改回静默。

**(b) priority_zh → batch_priority_label（空串口径不同）**
- 旧 `priority_zh("")`：`not s` → "普通"（:68）；旧 `priority_zh("weird")` → "未知"（:70）。
- 新 `batch_priority_label("")`：空串→default normal→"普通"（一致）；`batch_priority_label("weird")` → passthrough → "weird"（**旧是"未知"，新是原值透传，文案不同**）。
- ⚠️ **反例：`priority_zh("weird")=="未知"` vs `batch_priority_label("weird")=="weird"`** → 需 parity 钉死，确认前端能接受透传原值。

**(c) day_type_zh → calendar_day_type_label**
- 旧 `day_type_zh("")` → `v or "-"` → **"-"**；新空串→default workday→**"工作日"**。⚠️ **反例：空串 "-" vs "工作日" 反转**。
- 旧 `day_type_zh("WorkdayX")` → "WorkdayX"（透传）；新 passthrough → "WorkdayX"（一致）。
- 旧 `day_type_zh(None)` → "-"；新 None→空串→"工作日"。⚠️ 同 (c) 空串问题。

**(d) operator_status_zh → operator_status_label**
- 旧 inactive → "停用/休假"；新 → "停用"（**丢"/休假"**，open_questions 语义裁断）。
- 旧 `operator_status_zh("")` → "-"；新 → 空串→ `v or "未知"` → "未知"。⚠️ "-" vs "未知"。
- 旧 `operator_status_zh("weird")` → "weird"；新 passthrough normalize 后 `v or "未知"` → "weird"（一致）。

**(e) machine_status_zh → machine_status_label**：active/maintain/inactive 文案全等；旧空串→"-"，新→"未知"（仅空串文案差）。⚠️ "-" vs "未知"。

**(f) _source_zh → source_type_label**
- 旧 `_source_zh(external)`→"外协"，**其它一切**（含 "外协"/"外"/"内部"/未知）→ `return "自制"`（process_bp.py:25，灵魂线病：误判）。
- 新 `source_type_label`：先 normalize（"外协"/"外"→external→"外协"；"自制"/"内"→internal→"自制"；未知→passthrough→"未知"）。⚠️ **反例：`_source_zh("外协")=="自制"`（误判！） vs `source_type_label("外协")=="外协"`（正确）** —— 这是 bug 修复方向，但属行为变化，owner 须知情。

**共性结论：六处收口中，machine_status 几乎等价（仅空串"-"↔"未知"），其余五处在「空串 / 未知值」边界均有文案变化或反转。所有 parity 测试必须覆盖 {合法值, 空串, None, 未知串} 四类，且空串/未知的期望值待 owner 裁断后才能写死。**

## 字段 8：爆炸半径

**生产消费者（prod_consumers，已 grep 复核）：**
- `web/routes/equipment_bp.py:9` import `machine_status_zh,operator_status_zh` → 经 :15-20 薄包装 → `equipment_pages.py:33/51/111/230/276`（设备页/人员关联状态列）。
- `web/routes/personnel_bp.py:9` import `day_type_zh,machine_status_zh,operator_status_zh` → :16-25 包装 → `personnel_pages.py:68/136`、`personnel_detail_context.py:42/70/91`、`personnel_calendar_pages.py:33`（人员页/详情/日历状态列）。
- `web/routes/domains/scheduler/scheduler_bp.py:9` import `batch_status_zh,day_type_zh,priority_zh,ready_zh` → :24-37 包装 → `scheduler_batches.py:149/150/151`、`scheduler_batch_detail.py:249/250/251`、`scheduler_calendar_pages.py:30`（排产批次页 ready/优先级/状态列、日历）。
- `web/routes/process_parts.py:17` import `_merge_mode_zh,_source_zh` → :166/167（传入模板 jinja 函数 `source_zh`/`merge_mode_zh`）、:288（flash 文案）。

**测试消费者（test_consumers）：**
- `tests/web_pages/test_enum_display_consistency.py:4-61` 钉死全部 6 个 `*_zh` 输出，含 :18 'weird'→'weird'、:23 inactive→'停用/休假'、:59/:60/:61 ready 坏值→'未齐套'。收口后这些断言会红，**必须同步迁移（先迁测试，见字段11）**。

**灾难链（删错/统一错会静默炸什么）：**
1. **空串/None 反转链（最危险）**：若直接把 `ready_zh`→`ready_status_label` 而不审空串期望，排产批次页 ready 列对「空 ready 字段」会从"未齐套"翻成"齐套"——**会误导调度员把未齐套批次当齐套放行**，且因纯展示无报错、无测试拦（除非新增 parity），**静默**。day_type 空串从"-"翻"工作日"同理误导日历。
2. **operator "/休假"丢失链**：若 owner 未拍板就丢"/休假"，人员页状态列语义收窄，HR 看不出"休假"与"停用"区别——业务语义损失，无技术报错。
3. **_source_zh 误判修复的反向风险**：当前"外协"被误显示"自制"是已存在 bug；收口修正后前端文案突变，若有下游脚本/截图依赖旧错误文案会错位（低概率）。
4. **batch_status_zh 误收口链**：若有人为图省事新建 `batch_status_label` 收口 → 制造新 P5 第二模块，违铁律5；必须保留 `batch_status_zh` 私有。

## 字段 9：承重毗邻标记 + 禁区行

- `lb_no_touch = null`、`load_bearing=false`：**本债主文件 enum_display.py / process_bp.py 非承重护栏文件**（lb_colocation_danger="none(web/routes/* 非承重护栏文件)"）。本文件内**无禁区行**。
- **毗邻承重提醒**：收口目标 `enum_normalizers.py` 与 LB04 承重债同住一文件（LB04 lb=true）。R41 **只调用** enum_normalizers 的 *_label 函数，**绝不修改** enum_normalizers / normalization_matrix 内部（那是 LB04 的承重范围）。若收口过程中发现需改收口点行为，**禁止直接改 enum_normalizers**，须回 owner / 走 LB04。
- 禁区行（毗邻，绝不动）：`enum_normalizers.py:30-89`（normalize_machine/operator_status + operator_status_label）、`:135-165`（normalize_op_type_category + source_type_label）、`:209-237`（priority/ready/day_type label）—— 这些是收口点本体，R41 只可 import 调用，不可改。

## 字段 10：分层 / 导入环风险

- **方向合法**：收口是 `web/routes/*`（表现层）→ `core/services/common/enum_normalizers`（服务层）的 import。web→core.services 是**已存在的允许方向**（equipment_bp 等已大量 `from core.models.enums import ...`，且导出路径 `*_excel_*.py` 已 import enum_normalizers）。**不引入跨层违规**（无 core.algorithms→core.services、core.models→core.services）。
- **导入环风险：无。** `enum_normalizers.py` 只依赖 `core.models.enums` + `core.services.common.normalization_matrix`，**不反向 import 任何 web/routes**。R41 新增的 `web/routes/enum_display.py → core.services.common.enum_normalizers` 是单向叶子依赖，不会成环。
- AST 违规预期：**0**（铁律6满足）。

## 字段 11：测试耦合（动手前先迁哪些测试）

- **唯一直接耦合测试**：`tests/web_pages/test_enum_display_consistency.py`（行为钉死续命测试）。
- **迁移顺序（先测试后代码，避免红着 merge）**：
  1. 先按 owner 裁断的空串/未知值期望，**改写** `:14-61` 各断言为收口后口径（ready :59/:60/:61 改 loud 暴露；operator :23 由"停用/休假"改"停用"；空串行 :19/:26/:34/:44 由"-"改对应 label 口径如"未知"/"工作日"）。
  2. **绝不删测试重钉静默**——改成断言暴露/原值透传，并补一条「空串反转」专项断言钉防回退。
  3. 代码收口后跑 `pytest tests/web_pages/test_enum_display_consistency.py` 自证。
- 间接耦合：无针对 equipment_pages/personnel_pages/scheduler_batches 页面渲染文案的单测命中这些符号（grep 仅 test_enum_display_consistency 引用 `*_zh`），故页面层无需迁测，但建议人工核验四张页面状态列截图（无自动化网）。

## 字段 12：前置依赖

- **F 门 / 债前置（registry planned_deps_hint 钉死）**：
  1. **必须在 Batch-1 之后**：LB04 等承重债先完成「承重注释 + parity 安全网」，给 enum_normalizers / normalization_matrix 收口点上护栏，R41 才可大批接入。
  2. **owner 语义裁断门（owner_pending=true，硬门控）**：以下三项须 owner 先定，否则不得动手——
     - operator "停用/休假" 是否可收窄为 "停用"（丢"/休假"）；
     - ready 空串/未知/None 的目标文案（"齐套" vs "未齐套" vs 暴露"未知"，注意空串会从"未齐套"反转为"齐套"）；
     - day_type/priority/operator 空串由 "-" 改为 label 默认值（"工作日"/"普通"/"未知"）是否可接受。
  3. 批次归属：registry 建议 **Batch-5**（B03 config 双栈 + B04 enum 收口；与 R45/R47/R48/R71 同批，不同文件群内部冲突自由），**须在 Batch-1 之后**。
- **同文件排序（弱依赖，非阻塞）**：R37（equipment_pages.py）、R43（scheduler_batches.py）与 R41 共享下游消费者文件，落地时按文件粒度排序减少 diff 冲突，但无强先后（不同函数体）。
- fix_invalidation_risk = **none**：R41 与 LB04 改的符号不重叠，先后均不致对方失效，仅护栏时序要求。

---

## 对抗核验（第二双眼睛）

核验人：对抗核验 agent（只读不改）。结论：**PASS**。

**重 grep 命令与结果（当前工作区，git b08162cd 基线）：**
1. `rg -n "^def (machine_status_zh|operator_status_zh|day_type_zh|batch_status_zh|priority_zh|ready_zh)" web/routes/enum_display.py` → 13/24/33/47/62/73，与字段1全等。ready_zh `def`=:73、末行 `return "未齐套"`=:79（`rg -n "齐套" enum_display.py`），证实「旧报告 :79 是函数体末行非 def」表述准确，仅范围末值表述误差，**无符号漂移**。
2. `rg -n "^def (_merge_mode_zh|_source_zh)" web/routes/process_bp.py` → 16/22；`_source_zh` `else` 分支 `return "自制"`=:25。一致。
3. `rg -n "^def .*_label" core/services/common/enum_normalizers.py` → operator_status_label:82 / machine_status_label:124 / source_type_label:159 / batch_priority_label:209 / ready_status_label:220 / calendar_day_type_label:231，6 个收口点行号全等，**均为已存在符号，未新建**。
4. `git diff b08162cd -- enum_display.py process_bp.py` → 空。status=planned 属实。

**独立复核空串反转反例（字段7 最危险点）——确证：**
- `_enum_alias_lookup`（normalization_matrix.py:92-108）：`text == "" → return str(default)`。
- `ready_status_label("")` → default=`ReadyStatus.YES` → **"齐套"**；旧 `ready_zh("")` → :79 `return "未齐套"`。**空串语义反转 未齐套↔齐套 实锤**，owner 裁断门成立。
- `calendar_day_type_label("")` → default WORKDAY → "工作日"；旧 `day_type_zh("")` → :44 `v or "-"` → "-"。反转实锤。
- `_source_zh` `else "自制"` 吞掉"外协"中文别名/未知值（灵魂线误判）实锤。
- 钉死测试 `tests/web_pages/test_enum_display_consistency.py:60/61` 仍为 `ready_zh("")=="未齐套"`、`ready_zh(None)=="未齐套"`（未改），与字段2一致。

**一处需标注的同名歧义（非争议，但建议措辞收紧）：**
- 字段1/字段4/字段8 称「enum_normalizers 无 batch_status_label」。实测 `rg -n "batch_status_label"` 命中 `web/viewmodels/scheduler_batches_page.py:145/156` 与 `scheduler_batches.py:80`——但这是 viewmodel 的**注入参数名**（被喂入 `_batch_status_zh`），**并非 enum_normalizers 的 canonical 收口函数**。enum_normalizers 内 `^def .*_label` 共 9 个，确无 `batch_status_label`，BatchStatus 在 matrix/normalizers 中亦无任何 canonical normalizer。**档案「batch_status_zh 不收口须保留」结论正确**（强行新建 canonical batch_status_label = 新 P5，铁律5），建议把表述改为「无 canonical 收口函数（同名仅为 viewmodel 注入参数）」以消歧。

**铁律逐条核：**
- 铁律3 承重：lb=false、lb_no_touch=null；enum_normalizers 作为 LB04 毗邻承重区被正确标为「只调用不改 + 列禁区行（:30-89/:135-165/:209-237）」。无删除/统一/透传/加形参混入收口点。**承重误删风险=无。**
- 铁律4 灵魂线：字段4/9/11 明确「绝不 try/except 吞错、绝不兜底回退、测试改 loud 暴露而非删了重钉静默」。守住。
- 铁律5 收口点：6 个 *_label 均已存在，未新建第二模块。
- 铁律6 分层：`enum_display.py` 顶部仅 `from core.models.enums import ...`（无 web→core.services 违规之外的环）；`enum_normalizers/normalization_matrix` 无反向 `import web`，**无导入环，AST 0 违规**。
- 铁律7 owner_pending=true：档案明确「暂不给终态修法、暂不分配执行批次」，三项裁断门齐备，Batch-5 仅转述 registry 建议而非自定。**无擅自给终态/分配批次。**

**核验裁定：PASS。行号全一致，反转反例独立复现成立，承重/owner_pending/灵魂线/分层四线均守，无需升级第三方裁断。唯一遗留为 batch_status_label 同名歧义（措辞建议，不影响结论）。**

---

## 2026-06-10 终态补登（O26 选项 C 收口，R41 → fixed）

详见 `_registry.json` R41 current_evidence。要点：5 族收口 enum_normalizers 标准标签（machine/day_type/priority 直接薄包装；ready 包装层补丁空值钉「未齐套」防 RK20 误放行、operator 包装层补丁 inactive 保留「停用/休假」）；batch_status_zh 与 _merge_mode_zh 无 canonical 收口点保留私有+注释；_source_zh 收口 source_type_label 顺手修「外协误判自制」bug。补丁全在 web 包装层，enum_normalizers（LB04 承重毗邻）零修改。钉死测试改 loud 暴露口径+ready 空值反转专项断言+_source_zh 别名修复测试。下游 13+ 页面经 *_bp 薄壳消费 *_zh 函数名不变，零页面改动。
