# LB05 逐债档案 — execution_review 固定正式采用方案的承重不对称（服务层零注释保护）

> **⚠️ 2026-06-08 A 收官路径终态（B 执行前必读）**：`tests/regression_scheduler_workbench_link_guardrails.py` 已由 A 的 **P5.1（commit `1fa076fb`）合并**入 `tests/schedule/route_view/test_scheduler_workbench_links_contract.py`（guard 四态矩阵 / `forbidden_keys` / `can_emit` 逐字段断言完整迁入、未去重）。LB02/LB05/R54 的 guard 锚点重指该合并目标，按 `def test_*guardrail` 系列函数名 `rg` 定位、弃裸行号。详见 `_B_COMPAT_SAFEGUARDS.md §B-2`。


> 只读不改 · grep 回盘 · 承重神圣（仅注释 + 绑契约/parity）
> 主文件: core/services/report/execution_review.py | 桶 B01 | 簇 C01 | 病理 P2 承重 | 严重度 high
> 承重 load_bearing=true | owner_pending=false | lb_no_touch=同 LB02（同一不对称第二 finding）
> 同文件兄弟: LB02 / R62（registry interference 另含 LB06/R14/R42/R60/R61）

---

## 1) 当前真实 file:line（回盘）+ 漂移记录

LB05 与 LB02 是 `execution_review.py` 同一承重不对称的两条独立 finding。LB05 钉的是「五处把 plan_role 硬钉 ROLE_ADOPTED / scenario_id 硬钉 None」的执行现场。逐符号 grep 回盘（文件当前 476 行，git 未修改）：

| 硬钉点（语义） | registry old_location_DONT_TRUST | registry current_evidence 线索 | **当前真实行** | 漂移 |
|---|---|---|---|---|
| `from ... import ROLE_ADOPTED` | — | — | `:9` | — |
| `_list_plan_rows_between(..., plan_role=ROLE_ADOPTED, scenario_id=None)` | `:141` | `:163` | **`:180`+`:181`**（调用在 `_execution_review_plan_rows`） | 老 141→实 180（+39）；今晨 163→实 180（+17） |
| `_list_plan_rows_all(..., plan_role=ROLE_ADOPTED, scenario_id=None)` | — | `:174` | **`:191`+`:192`** | 今晨 174→实 191（+17） |
| `_resolve_plan(v, ROLE_ADOPTED, None)`（execution_review 入口） | — | `:204` | **`:221`** | 今晨 204→实 221（+17） |
| 固定标签 `"plan_role": ROLE_ADOPTED`（返回 dict 键） | — | `:219` | **`:236`** | 今晨 219→实 236（+17） |
| `effective_plan_role=ROLE_ADOPTED`（`_execution_scope` 内 scope 构造） | — | — | **`:58`** | 新发现，registry 未列 |
| `_plan_identity_label` / `_filename_part` 兜底标签 `plan_role_label(ROLE_ADOPTED)` | — | — | `:65` / `:69` | 标签兜底，非主硬钉 |

**漂移结论：有漂移，幅度统一 +17 行**（相对今晨 current_evidence），相对 old_location +39 行。registry blast 字段里写的 `:112/:123/:153/:166-167/:178/:188` 这套行号**与当前态全部对不上**：
- `:112/:113` 现在是 `_list_plan_rows_all` 的 **Protocol 形参声明**（`version`/`plan_role`），不是硬钉调用；
- `:121` 是 `_resolve_plan` 的 **Protocol 声明**；
- `:141` 现在是 `_execution_review_date_bounds` 的 `raw_from` 赋值（与 LB02 共享的"同点 :141"实为日期边界方法首行，**已不再是硬钉行**）；
- `:153` 现在是 `calculations.parse_date(raw_from)`，非 `_resolve_plan`。

→ **registry 的 :112/:123/:153/:166-167/:178/:188 全部失准，必须以本回盘表为准**。真正的五处硬钉是 **:58 / :180-181 / :191-192 / :221 / :236**。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = in_progress`。
- evidence 自述：「契约测试 `regression_execution_review_identity_guardrail.py` 已绑（service/页路由双覆盖），但服务层本地『我是故意的』注释仍未钉回」。
- **回盘一致**：`tests/operation_execution/test_execution_review_identity_guard.py` 存在且覆盖到位（GET 带 `plan_role=baseline_best` :63、`plan_role=future_role` :78、`plan_role=adopted&scenario_id=SCENARIO-RPT` :95、导出拒非正式身份 :162），但服务层 `execution_review.py` 五处硬钉旁 **零中文注释**（已逐行读，:58/:180/:191/:221/:236 上下文均无"故意"说明）。
- 状态 in_progress 名副其实：**护栏（测试 + web forbidden-params）已立，欠的只剩服务层执行现场的可见化注释**。这正是 LB05 这条「服务层文件内零注释保护」finding 的剩余增量工作。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P2 承重护栏**（`load_bearing=true, refuted=false, verdict_kind=load_bearing`）。不变量 = 「计划和现场实际复盘只对账正式采用方案（ROLE_ADOPTED + scenario_id=None），禁止把模拟预览/历史候选方案以『既成事实』身份混入」。
- **不对称在哪**：宿主 `report_plan_helpers.py:29 _resolve_plan / :56 _list_plan_rows_between / :83 _list_plan_rows_all` 全是**完全透传任意 plan_role/scenario_id**（host 不设防）；只有 `execution_review.py` 这五处硬钉把角色钉死。→ 不变量的执行点集中在本文件，**护栏证据却散在别处**（web `scheduler_workbench_link_query.py` forbidden-params + roadmap design.md + 测试），服务层执行现场本身零注释 = 承重不对称。
- 严重度 **high**：误删/误统一会让车间把"模拟预览"当"既成事实"读（数据完整性事故，见 §8 灾难链）。
- 桶 B01 / 簇 C01。

## 4) 修法类（承重=仅注释；收口点须已存在；owner_pending 处理）

**修法类 = 承重「仅注释 + 绑契约/parity」（零删除 / 零统一 / 零透传 / 零加形参）。** owner_pending=false，可给终态修法，但因 load_bearing=true，终态修法被铁律③锁死为只能补注释 + 测试。

具体动作（与 LB02 合并为同一注释动作，避免重复投工）：
1. 在 `ExecutionReviewMixin` 类体顶部（`:136-139` 注释块附近）补一段 **§90 LB-A2「我是故意的」中文注释**，说明不变量全文：「计划和现场实际复盘只对账正式采用方案（adopted/scenario_id=None），禁止为统一四报表签名而放开 plan_role/scenario_id；护栏散落在 web forbidden-params 与 design.md，此处是唯一硬钉执行点」。
2. 在五处硬钉旁各补一行短注：**:58**（scope 构造 effective_plan_role）、**:180-181**、**:191-192**、**:221**、**:236**。
3. 不动任何逻辑、不删 R62 死分支（那是 R62 的活）。

**收口点**：本条**不做收口**（不是 P5）。它是 P2 承重，修法只可见化、不收口、不新建模块。若 §4 里要点名"已存在的统一点"——本不变量当前的多层护栏点已存在且不需新建：service 硬钉（本文件五处）+ web `scheduler_workbench_link_query` forbidden-params + 测试三件套。补注释只是把这些已存在点交叉引用到执行现场。

**未来放开历史正式版本（非本批工作）的 precondition**（registry adv_precondition，照搬并校真）：(1) service 内对非 ROLE_ADOPTED role / 非 None scenario_id **显式 raise ValidationError（loud，禁默默回退 adopted）**；(2) 给下游 scope 聚合加 role/scenario 作用域过滤（注意：registry 写的 `aggregate_states_by_op_ids` **当前已是 raise**，真实可走路径是 `get_execution_state_for_scopes`，见 §8）；(3) 路由入站加 plan_role/scenario_id 闸门并保留 web forbidden-params；(4) 用 `regression_plan_vs_actual_review` / `regression_reports_workbench_navigation_contract` 锁死。

## 5) 同文件碰撞债（各改哪段 / 谁先谁后 / 撞行号或 dict 键位移）

`execution_review.py` 三债共处。各自改区已 grep 确认互不重叠：

| 债 | 改哪段（当前真实行） | 动作 | 与 LB05 关系 |
|---|---|---|---|
| **LB02** | 类体注释块 `:136-139` + 五处硬钉 :58/:180/:191/:221/:236（**与 LB05 完全同点**） | 补 §90 LB-A2 注释（零删/零统一） | **同一注释动作满足两条** |
| **LB05（本条）** | 同上五处硬钉 + 类顶注释 | 补注释 | 本体 |
| **R62** | `_execution_review_row` 返回 dict 三档键 :358-382 + `_resource_pair_payload` :406-417（`return {display,identity,export}` :417）+ 模板 `!=` 死副行 + xlsx `or` 死回退 | **删 no-op / 压三键成单键**（行为不变） | **不同方法、不同行段，无重叠** |

**谁先谁后**：LB02+LB05 注释先落（纯增量、零风险）；R62 后做（删行会下移行号）。

**撞行号 / dict 键位移风险**：
- LB02/LB05 是**纯注释插入**，只会让 R62 区（:358+）整体下移若干行，但 R62 按符号名/`!=` 模式定位（registry 明确"文件已漂，按 `!=` 模式定位"），不受影响。
- 反向：若 R62 先做，会**删掉 :358-382/:406-417 的键 + 上移行号**，使 LB02/LB05 注释要贴的 :236 以下行号变化——但 LB05 五处硬钉全在 :236 及以上，**R62 删的全在 :236 之下**，故 LB05 贴注释点不受 R62 影响。两者行号互不干扰，但**仍建议注释先做**以保证 §90 文案锚点稳定。
- **dict 键位移**：R62 压三档键（:373-375 等）是 R62 独有的返回 dict；LB05 的返回 dict 键 `"plan_role": ROLE_ADOPTED`（:236）在不同 dict（`execution_review` 返回值），两者不共享键空间，无串扰。

## 6) 同收口点债 + 顺序敏感性

**LB05 不收口**（P2 承重，仅注释），故严格意义无"同收口点"。但存在**同符号/同概念协同债**，需同批落齐：

- **同符号 `_resolve_plan` / `execution_review`**：LB02、LB06 都触及（interference same_symbol=true）。`_resolve_plan` 的宿主在 `report_plan_helpers.py:29`（透传任意 role/scenario），LB05 在 :221 用它时硬钉 `ROLE_ADOPTED, None`。
- **LB06（web 路由/导航层强制 adopted）顺序敏感性最高**：registry 反复警告——若 LB06 被「统一从 request 读 plan_role」，会与本服务层 :221/:180/:191 硬钉产生**两层认知错位**；**任一层单独『统一』即打穿纵深**。故 **LB02 / LB05 / LB06 注释必须同批落齐**，不可拆批。
- 顺序：三条注释互不依赖逻辑，可同 PR 落；但**禁止只动一层**（部分落地 = 纵深防御出现单点缺口）。

## 7) 收口行为差异 + 所需 parity 测试（反例）

**本批不收口 → 无新旧两路 parity 需求**（注释不改行为）。本节给出的是「**未来若放开历史正式版本**」时必须先建的 parity 反例（precondition 验证，非本批工作）：

| 输入边界值 | 当前行为（硬钉路） | 未来放开路（loud raise 要求） | 反例风险 |
|---|---|---|---|
| `plan_role=adopted, scenario_id=None` | 正常对账正式方案 | 同 | 等价 |
| `plan_role=baseline_best`（候选方案） | service 层**根本不读该参数**，恒按 adopted（web 层 forbidden-params 在入站已拦/可见化拦截，见 identity_guardrail :63） | **必须 raise ValidationError（loud）** | 若误把硬钉改成「透传 + 默默回退 adopted」=静默吞错（违灵魂线铁律④）：车间以为看的是 baseline_best 实则 adopted，**None vs raise 不等价** |
| `scenario_id=SCENARIO-RPT`（模拟预览） | service 不读，恒 None；web 层 :95 拦截 | **必须 raise** | 误透传会让模拟预览以"既成事实"导出（数据完整性事故） |
| `plan_role=future_role`（未知角色） | service 不读；web :78 不泄露 raw role | **必须 raise，禁回显原值** | 信息泄露 + 静默回退双风险 |

**所需 parity 测试（仅放开时建）**：对 `execution_review(version, plan_role=X)` 逐分支断言——非 adopted/非 None 一律 raise ValidationError，**绝不返回 adopted 结果**（即"None vs raise 不可混"）。当前批只需保证现有 `regression_execution_review_identity_guardrail.py` 的 GET 级负向断言不被破坏即可。

## 8) 爆炸半径（生产消费者 + 测试消费者 + 灾难链）

**生产消费者（回盘校真）**：
- `core/services/report/execution_review.py:247 export_execution_review_xlsx → :257 self.execution_review(...)`（registry 写 :188，**实 :247/:257**，漂移）。
- `web/routes/reports_page_support.py:335 engine.execution_review(...)`（registry 写 :327，**实 :335**，漂移 +8）。
- `web/routes/reports_export_routes.py:116 target_type="execution_review"`（registry 写 :101，**实 :116**，漂移）。
- `core/services/report/report_plan_helpers.py:29/56/83`（宿主 `_resolve_plan`/`_list_plan_rows_*` 完全透传任意 role/scenario → execution_review 五处硬钉是**唯一钉死点，host 不设防**）。
- 写侧对称印证：`operation_execution_feedback_service.py` 写侧硬拒（registry 称 LB01），证明读侧硬钉是对称设计非疏漏。

**⚠ registry 下游声明需更正（重要）**：registry 多处写「`operation_execution_event_repo.py:267 aggregate_states_by_op_ids 喂任何 op_id 都照配」。回盘发现 **:403 `aggregate_states_by_op_ids` 当前已是 `raise _unscoped_execution_read_error()`**（连同 :354/:356/:399/:401/:405 一整组 by_op_id 接口全部 raise）——这是**已落地的 loud-raise 收口**，不再是"照配任意 op_id"的软路径。execution_review 实际走的是 `:206 host.execution_feedback_service.get_execution_state_for_scopes(scopes)`（:80 定义），**scope 维度**（含 effective_plan_role），而非 by_op_id。→ 下游"无法自卫"的旧描述**已部分被 _unscoped raise 补强**；真正的自卫缺口要看 `get_execution_state_for_scopes` 是否按 scope.effective_plan_role 过滤（本债不改它，留作 precondition step2 的核查对象）。

**测试消费者（回盘存在）**：
- `tests/operation_execution/test_execution_review_identity_guard.py`（GET 负向：baseline_best :63 / future_role :78 / scenario :95 / 导出拒非正式 :162 / 历史 adopted 版本可见 :136）。
- `tests/scheduler_analysis/test_plan_vs_actual_review.py`（页 + 导出 service 级行为 + nav 负向）。
- `tests/web_pages/test_reports_workbench_navigation_contract.py`（keeps_formal_plan_role）。
- `tests/regression_scheduler_workbench_link_guardrails.py`（viewmodel link builder 禁 scenario）。

**灾难链（删错 / 统一错会静默炸什么）**：
1. 把 :180/:191/:221 的 `ROLE_ADOPTED/None` 改成「透传 + 默认回退 adopted」→ 候选/模拟方案明细以「计划和现场实际」**正式身份**呈现给车间并可导出 xlsx → **预览冒充既成事实，数据完整性事故**。
2. 为「统一四张报表方法签名」给 `execution_review()` 加 `plan_role/scenario_id` 形参 → 穿透 web forbidden-params 守卫的认知，造成**两层认知错位**（web 层以为已拦，service 层却放行）。
3. 误删返回 dict 的 `"plan_role": ROLE_ADOPTED`（:236）→ 模板/nav 的 keeps_formal_plan_role 断言失锚，下游导航可能拼出带非正式 role 的链接。

## 9) 承重毗邻标记（同文件 LB + 禁区行）

**同文件承重债**：LB02（同一不对称第二 finding，承重）、LB06（registry interference 标 same_file/same_symbol，web 路由层 adopted 强制，承重相关）。`lb_no_touch = 同 LB02（execution_review.py 同一不对称第二 finding，core-svc-domain 分区）`。

**禁区行（绝不删 / 绝不统一 / 绝不加形参透传，只能补注释）**：
- `:58` — `effective_plan_role=ROLE_ADOPTED`（`_execution_scope` scope 构造）
- `:180-181` — `_list_plan_rows_between(plan_role=ROLE_ADOPTED, scenario_id=None)`
- `:191-192` — `_list_plan_rows_all(plan_role=ROLE_ADOPTED, scenario_id=None)`
- `:209-217` — `execution_review()` 签名**刻意不含** `plan_role/scenario_id` 形参（禁止为"统一签名"加形参）
- `:221` — `_resolve_plan(v, ROLE_ADOPTED, None)`
- `:236` — 返回 dict `"plan_role": ROLE_ADOPTED`

**对 R62 的边界令**：R62 清死分支**只能动 :358-382 / :406-417 / 模板 `!=` 副行 / xlsx `or` 回退**，**严禁触碰上述 :58-:236 任何承重硬钉行**（registry lb_colocation_danger 明令）。

## 10) 分层 / 导入环风险

**本批=补注释，零 import 变更 → 零分层/环风险。** 现状校真（execution_review.py imports :1-14）：
- 依赖 `core.infrastructure.errors` / `core.models.*` / `core.services.scheduler.schedule_plan_query_service`（同层 services 内）/ 本包 `.calculations` `.exporters` `.report_number_parsing`。**无越层**（service→model/infra/同层 service 合法；无 core.algorithms→services、无 core.models→services 反向边）。
- 0 AST 违规，符合分层红线铁律⑥。
- **未来放开（precondition）的导入风险预警**：若 step1 在 service 内 raise ValidationError，已 import `core.infrastructure.errors.ValidationError`（:6），**不引新边**；step2 给 scope 聚合加 role/scenario 过滤若需 import plan_role 常量，`ROLE_ADOPTED` 已在 :9 在手，亦不引新边。故即便未来放开也不构成导入环。

## 11) 测试耦合（动手前先迁哪些测试）

**本批=补注释 → 不需迁测试、不需改断言**（注释不改行为，现有契约测试应继续全绿）。动手前只需：
1. **基线跑通**（确认补注释前后行为不变）：
   - `tests/operation_execution/test_execution_review_identity_guard.py`（核心护栏，service+页+导出三覆盖）
   - `tests/scheduler_analysis/test_plan_vs_actual_review.py`
   - `tests/web_pages/test_reports_workbench_navigation_contract.py`
   - `tests/regression_scheduler_workbench_link_guardrails.py`
2. 注释落地后**重跑同四组**，断言无变化即可合批。
3. **无测试需要迁移/删除**。R62 才需迁测试（删 no-op 时模板/xlsx 相关断言），与 LB05 解耦。

**盲区备注**（registry fix_invalidation_risk 称"无 GET 带非 adopted plan_role 断言 service 回退/硬拒"）：回盘发现该盲区**已部分被补上**——identity_guardrail :63/:78/:95/:162 已是 GET 级负向断言。剩余真盲区是「**直接调 service `execution_review(version, plan_role=非adopted)` 的单元级断言**」，但因 service 当前签名根本不收 plan_role 形参（:209-217），该调用在类型层就不可达，盲区影响有限；放开时才需补单元级 parity（见 §7）。

## 12) 前置依赖（先做哪些债 / 哪些 F 门）

**批次定位**：registry `planned_batch_hint = Batch-1（纯增量），单元内排序第 3（与 LB02 合并为同一注释动作）`。

**前置依赖**：
- **无硬前置债**——纯注释、零删除、零结构改，不需先做任何收口/清理。
- **同批必须并行落齐（非前置而是协同，铁律：不可单层落地）**：
  - **LB02**：同点同动作，**一次注释同时满足 LB02+LB05**（避免重复投工）。
  - **LB06**（web 路由/导航层）：必须与 LB02/LB05 **同批**，否则任一层单独"统一"打穿纵深（见 §6）。
- **必须先于**：**R62**（同文件死分支清理）。理由：(a) 注释作为 §90 LB-A2 锚点先立，给 R62 删行时一个"承重禁区已标注"的护栏参照；(b) 避免 R62 删行后 LB02/LB05 文案锚点漂移（虽 §5 证明行号互不干扰，仍以顺序固化降风险）。

**F 门（开工前置闸）**：
- F-门-1：基线四组 regression 全绿（§11 列表），证明补注释前行为正确。
- F-门-2：AST 分层检查 0 违规（本批不引 import，应天然通过）。
- F-门-3：合批后重跑同四组全绿 + `git diff` 确认**仅注释行变化、零逻辑行改动**（承重铁律③自证）。

---

## 对抗核验（第二双眼睛）

核验日期 2026-06-05。只读不改，独立 rg/grep 回盘，默认怀疑找反证。

### A. 行号回盘（独立 grep）
命令：`rg -n "ROLE_ADOPTED|scenario_id|_resolve_plan|effective_plan_role" core/services/report/execution_review.py`
结果：五处硬钉 **:58 / :180-181 / :191-192 / :221 / :236 与档案 §1 回盘完全一致**；import ROLE_ADOPTED 在 :9，execution_review 签名 :209-217 确不含 plan_role/scenario_id 形参，返回 dict `"plan_role": ROLE_ADOPTED` 在 :236。**行号复核：一致。**

### B. ⚠ 反证 1——「文件未 git 修改」失实（争议，非阻断）
档案 §1/§2/§12 三处声称「文件当前 476 行，git 未修改」「diff 仅注释」。`git status --porcelain` 实测为 **`MM`（工作区+暂存均有改动）**。
- `git show HEAD:...` 回盘：**HEAD 原版 407 行，硬钉在 :112/:113/:123/:124/:153/:167**——这恰是 registry old_location/blast 写的那套行号。**registry 行号在 HEAD 是准的**，是工作区 +69 行未提交改动把它推漂到 476 行后才变成档案那套。
- `git diff HEAD` 显示工作区已有**非注释逻辑改动**：新增 `_public_report_datetime()` 函数、`_display_time` 逻辑替换（:385 区）、新增 `effective_plan_role` scope 构造（:58）、新增 `_plan_identity_label`/`_filename_part` 兜底标签（:65/:69）、import 加 `SOURCE_SCHEDULE`。
- **关键缓解**：所有逻辑改动均在 :236 **之下**或为新增点，`git diff HEAD | grep ROLE_ADOPTED` 确认**原有 6 处硬钉的 plan_role/scenario_id=None 值零改动**（仅 `effective_plan_role=ROLE_ADOPTED` 是新增的第 7 处硬钉，方向与不变量一致，非削弱）。档案回盘出的**当前工作区行号准确**，承重不变量未被工作区改动削弱。
- 定性：档案把「正在被并行重构的工作区」误当「稳定的 git 原貌」回盘，是**事实性表述错误**，但不改变 LB05 的修法（仍是纯注释）与承重判断。建议档案 §1 改注「文件工作区有未提交改动，HEAD=407 行硬钉在 :112-167，回盘以当前工作区 476 行为准」。

### C. 下游 raise 反例（档案 §8 纠 registry——核实属实）
命令：`rg -n "aggregate_states_by_op_ids|_unscoped_execution_read_error|get_execution_state_for_scopes" data/repositories/operation_execution_event_repo.py`
结果：**:403 `aggregate_states_by_op_ids ... raise _unscoped_execution_read_error()`**，连同 :354/:356/:399/:401/:405 整组 by_op_id 接口全 raise。execution_review 实走 :206 `get_execution_state_for_scopes`（定义于 feedback_service :80，scope 维度）。**档案对 registry「照配任意 op_id 软路径」的纠正属实，registry phase1_blast/prod_consumers 的下游描述已过时。**

### D. 消费者/宿主回盘
- web 页路由 `reports_page_support.py:335`、xlsx `execution_review.py:257`、export route `reports_export_routes.py:116` 全部回盘一致（档案已纠 registry :327/:188/:101 漂移）。
- 额外发现护栏增厚：`reports_export_routes.py:100 _require_execution_review_adopted_plan()` 入站闸门存在，与 §6 多层纵深一致。
- 宿主透传 `report_plan_helpers.py:29/56/83` 确为完全透传，execution_review 五处为唯一钉死点——承重不对称判断成立。

### E. 承重误删/统一/透传嫌疑排查
档案修法明确「零删/零统一/零透传/零加形参」，:209-217 签名禁加形参列为禁区行（§9），R62 边界令禁触 :58-:236（§9）。**未在档案修法中发现任何删除/统一/合并/透传/加形参动作。承重误删风险：无。** owner_pending=false，档案给了终态修法但被铁律③锁死为仅注释+测试，处理正确。

### F. 分层/收口
本批纯注释零 import 变更，无分层/环风险（§10 属实，imports :1-14 无越层）。LB05 不收口（P2 承重非 P5），无新建模块，符合铁律⑤。

### 结论
PASS（带一处事实性争议：B 节「git 未修改」表述失实，但不影响行号准确性、承重判断与纯注释修法）。无需第三方裁断——争议为档案表述更正级别，承重铁律未被违反。
