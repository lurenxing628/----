# R29 逐债档案 — common number_utils 门面 KEEP/现盘薄委托

> **2026-06-09 台账回写终态：fixed / O20 KEEP。** `core/services/common/number_utils.py` 现盘已不是旧“全量函数体拷贝”，`parse_finite_float` / `parse_finite_int` 仅保留兼容门面并薄委托到 `core.shared.strict_parse`。本轮只回写台账并在代码顶部补 KEEP 注释，不做薄壳化/删除动作。

## 现盘事实

- `core/services/common/number_utils.py` 仍是两个生产消费者的兼容入口。
- 函数体已经委托 `parse_optional_float` / `parse_required_float` / `parse_optional_int` / `parse_required_int`。
- 4 个兄弟模块仍为 re-export 壳；R29 不再按旧“全量函数体拷贝”描述执行。

## 后续边界

- 继续 KEEP 时不得把本模块当死代码删除。
- 若未来要下线 facade，必须先重写对应 facade 契约测试并迁移生产 import。
- 本轮不写任何运行逻辑，只补事实记录与保护性注释。
