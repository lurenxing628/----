# LB02 逐债档案 — execution_review 承重不对称(刻意只复盘 ROLE_ADOPTED)

> 债 id: LB02 | 桶 B01 | 簇 C01 | 病理 P2 | 承重 load_bearing=true | owner_pending=false
> 主文件: core/services/report/execution_review.py
> 同文件兄弟: LB05(同点)、LB06(web 路由层对应)、R62(同文件 :287-348 死分支清理)
> 本档案只读不改,仅产计划。所有断言附当前盘上 file:line。

---

## 1) 当前真实 file:line(grep 回盘)+ 漂移记录

文件总行数 476。`git status` 显示 `MM core/services/report/execution_review.py`(已被本分支修改),所以 registry 的 `location_status:✅准 / 行号与报告一致` 已**过期**——盘上行号相对 registry 整体下移约 +68~+70 行。逐符号回盘:

| 符号(本债承重点) | old_location_DONT_TRUST | current_evidence_2026_06_05 | **盘上真实** | 漂移 |
|---|---|---|---|---|
| `def execution_review(` 签名(刻意不收 plan_role/scenario_id) | :141 | :192 | **:209** | vs141 **+68**;vs192 **+17** |
| `_resolve_plan(v, ROLE_ADOPTED, None)` 调用 | :153 | :204 | **:221** | vs153 **+68** |
| `_execution_review_plan_rows` 内 `plan_role=ROLE_ADOPTED,scenario_id=None`(between 路) | :112/:123 | :163-164 | **:180-181** | vs112 **+68** |
| 同上(all 路) | — | :174-175 | **:191-192** | +68 |
| 返回 dict `"plan_role": ROLE_ADOPTED` | :166-167 | :219 | **:236** | vs166 **+70** |
| `from ... import ROLE_ADOPTED, SOURCE_SCHEDULE` | — | — | **:9** | — |
| `_resolve_plan` Protocol 声明(host 契约) | — | — | **:121** | — |

漂移结论:**有漂移,幅度大(+68~+70 行)且 registry 自标"✅准/未被 git 修改"已失真**——文件本轮被改过(MM)。任何照抄 registry/evidence 行号的修法都会打错位置,必须以本表盘上行号为唯一基准。承重五处硬钉(registry 称 :112/:123/:153/:166-167)在盘上对应 **:180-181 / :191-192 / :221 / :236**,签名禁区在 **:209**。

## 2) 当前状态 + 与 status evidence 是否一致

registry `status_2026_06_05=in_progress`。evidence 称"计划两动作之一已落":(a)请求级负向契约测试 **已存在**——`tests/regression_execution_review_identity_guardrail.py`(173 行,盘上确认),含 `plan_role=baseline_best`(:63)、`plan_role=future_role`(:78)、`plan_role=adopted&scenario_id=SCENARIO-RPT`(:95)三条直接打 `/reports/execution-review` 的页面级拦截断言 + 导出链置空(`_export_links==[]`)+ 资源筛选导出仍只含正式行(:107-133);(b)签名上方 `# 注释` **仍缺**——盘上 :193-208(`_execution_review_rows` 结束到 `def execution_review`)与 :209-211 签名区**零 `#` 注释**,§90 LB-A2"刻意不对称"本地护栏文案未补。

一致性:**与 evidence 一致,in_progress 名副其实**——"测试已落、注释未补"。注意 evidence 把测试文件名写作"...identity_guardrail.py",盘上同名存在 ✅;但 registry 顶层 `phase1_blast.fix_invalidation_risk` 仍写"全仓无 GET /reports/execution-review?plan_role=非adopted 的请求级回归",此句**已被 evidence 推翻/过期**(测试现已存在),档案据盘上为准:**该最大缺口已基本闭合,剩注释一项**。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P2 承重护栏**(load_bearing=true, verdict=load_bearing_confirmed, refuted=false)。
- 表征:同 `ExecutionReviewMixin`/`ReportEngine` 体系下,兄弟报表 overdue/utilization/downtime 的对外方法**全签名带 plan_role+scenario_id 并透传**,唯独 `execution_review`(:209)**故意不收**这两个参数、在 :180-181/:191-192/:221/:236 **硬钉 ROLE_ADOPTED/None**。这是**刻意的承重不对称**:防"模拟预览方案/对比参考方案"的明细以"正式现场复盘"身份呈现给车间并被导出。
- 风险点:护栏理由**散落**在 navigation_context / workbench_links / scheduler_reports_workbench / 多个契约测试,而服务方法本体(:180-236)**零注释**——下一个改动者极易以"统一四张报表签名"之名给 :209 加形参,CI 可能仍绿(见 §8 灾难链)。
- 严重度 **medium**;桶 **B01**;簇 **C01**;_partition: scheduler-plan-identity。

## 4) 修法类

**承重 = 仅注释 + 绑契约/parity 测试**。零删除、零统一、零透传、零加形参(铁律 3)。owner_pending=false,可给终态修法。

终态修法(以**盘上行号**为准,非 registry):
1. 在 `def execution_review(`(**:209**)正上方补 §90 LB-A2 逐字护栏注释:
   ```
   # 本方法故意只复盘 ROLE_ADOPTED,不收/不透传 plan_role/scenario_id——
   # 与 overdue/utilization/downtime 三兄弟的签名不对称是刻意的护栏,不是遗漏。
   # 勿为『统一四张报表方法签名』而加 plan_role/scenario_id 形参:
   # 会让模拟预览/对比参考方案的明细以『正式现场复盘』身份呈现给车间并可导出。
   ```
2. 在三处硬钉旁各补一行短注(同义凝练 `# 硬钉 adopted/None=护栏,勿改形参透传`):
   - `_execution_review_plan_rows` between 路 **:180-181**;
   - all 路 **:191-192**;
   - `host._resolve_plan(v, ROLE_ADOPTED, None)` **:221**;
   - (可选)返回 dict `"plan_role": ROLE_ADOPTED` **:236**。
3. 契约测试:**最大盲区已闭合**(`regression_execution_review_identity_guardrail.py` 已存在,见 §2)。本债**无需新建测试**,只需在落注释后跑该文件 + §11 列表回归确认不回归。

**P5 收口**:本债**不涉及收口**(纯护栏注释,不合并/不统一第二模块)。adopted-only 取数语义已收口在**已存在**的单点 `_resolve_plan`(:121 Protocol / :221 调用),无需新建收口点。灵魂线:本修法不新增任何兜底/静默回退;反而 evidence 测试已验证页面 loud 拦截(返回拦截文案 + 导出链置空),符合 P4 精神。owner_pending=false → 给终态修法。

## 5) 同文件碰撞债(每个 sibling)

同文件 `execution_review.py` 上有 **LB05 / R62** 两条直接 sibling,外加 interference_edges 标 `same_symbol` 的 **LB06**(web 层对应,不在同文件)。

- **LB05(同点,必须合并)**:title=同一承重不对称的第二 finding,_partition 不同(LB02=scheduler-plan-identity,LB05=core-svc-domain),但**改的是同一段**——签名 :209 上方那条护栏注释 + 同三处硬钉(:180-181/:191-192/:221/:236)。**修法合并为同一次注释动作 + 同一组回归**。谁先谁后:**二者一次落地**,不存在先后;registry 单元内排序均为"第 3,与对方合并"。**会否撞行号/dict 键位移**:同插一段 `#` 注释,只新增行、不动 dict 键序,不会位移返回 dict(:225-238)的键。LB02≡LB05 是"同点两 finding",落一次注释即同时满足两条。

- **R62(同文件,后置,Batch-3)**:title=三档标签(display/identity/export)压扁成同值的死分支清理。盘上真实位置 **:358-359 / :361-362 / :373-375 / :380-382**(返回 dict 内三键并列同值)+ **:406-417**(`_resource_pair_payload` 内 `return {display,identity,export}` 三键恒 = display)+ **:438-440**(暂无反馈三键同值),以及 `_actual_resource_identity` :430-431。**还跨文件牵 templates/reports/execution_review.html(死副行 `!= *_label`)与 exporters/xlsx 的 `*_export_label or *_label` 死回退**。
  - 谁先谁后:**LB02/LB05 注释(Batch-1)必须先落,R62(Batch-3)后做**(registry planned_deps + R62.planned_batch 双向确认)。
  - **会否撞行号**:LB02 注释插在 :180-236(签名+硬钉区),R62 改 :358-444(标签区)+ 模板 + xlsx,**两区段不重叠**,但 R62 做时 :358+ 行号会因 LB02 已插注释而再下移——R62 动手前必须**重新 grep 回盘**,不可用本档案行号(本档案行号是"LB02 注释未插"时的盘上态)。
  - **dict 键位移风险**:R62 删的是返回 dict 里 `*_identity_label`/`*_export_label` 两类**键本身**(:358-359/:361-362/:373-375/:380-382),会改变返回 dict 形状 → 模板/xlsx/页面装饰(`decorate_execution_review_rows`)若仍读旧键将 KeyError 或取空。这是 R62 自身的爆炸半径,**与 LB02 注释互不干扰**,但提示 R62 是"三处原子同删"(dict 键 + 模板副行 + xlsx 回退)必须同 commit。
  - **承重禁区警告**:R62 清死分支时**绝不能碰** LB02/LB05 的五处护栏行(:180-181/:191-192/:221/:236/:209 签名),更不能"顺手统一签名"加形参——见 §9。

## 6) 同收口点债 + 顺序敏感性

本债**不做收口**(纯护栏注释)。但"adopted-only 取数"这一**不变量**事实上收敛在两个**已存在**单点,需点名以防他人误把它们当作收口对象去"统一":

- `_resolve_plan(version, plan_role, scenario_id=None)` Protocol(:121)/ 调用(:221)——adopted 由调用方传死 `ROLE_ADOPTED, None` 强制。
- `_execution_review_plan_rows`(:180-181/:191-192)——between/all 两路同样传死。

收口到同一符号(`_resolve_plan` / `execution_review`)的其他债:**LB05(同点)、LB06(web 路由层 `require_execution_review_adopted_plan` 守卫,见 web/routes/reports_export_routes.py:100、reports_page_support.py:396)**。顺序敏感性:

- LB02/LB05 注释先落 → 之后任何人(含 R62 重构者、LB06 web 侧改动者)读到本地"我是故意的"注释,**降低误统一概率**。这是**纯增量、无顺序冲突**。
- LB06(web 层)与本债是"同一护栏的服务/路由两端":服务层(本债)硬钉是**数据层最后一道**,web 层守卫是**入口拦截**。两端注释可并行落,**无强制先后**,但建议本债(数据层)注释明确写"web 层另有 `require_execution_review_adopted_plan` 入口守卫",形成双向交叉引用。

## 7) 收口行为差异 + 所需 parity 测试

本债**不引入新旧两路**(不收口、不删分支),故**无 parity 等价义务**。但必须守住"假如有人违规给 :209 加形参透传"的边界差异,作为契约测试的**反例锚点**(已被现有测试覆盖):

- 反例 1(role):`/reports/execution-review?version=12&plan_role=baseline_best` —— 期望:页面 loud 拦截"计划和现场实际只复盘正式采用方案",导出链置空(`_export_links==[]`)。**非**返回 baseline_best 明细。已由 `regression_execution_review_identity_guardrail.py:59-73` 锁定。
- 反例 2(unknown role):`?plan_role=future_role` —— 期望:拦截 + **不泄漏原始 role 字符串**(`"future_role" not in body`)。已由 :76-88 锁定。
- 反例 3(scenario):`?plan_role=adopted&scenario_id=SCENARIO-RPT` —— 期望:拦截"模拟预览身份",预览批次 `B-RPT` 不出现。已由 :91-104 锁定。
- 反例 4(导出 parity):资源筛选下导出 xlsx 仍只含正式行(`B-OTHER`/`M-OTHER` not in export_text)。已由 :107-133 锁定。

结论:**parity 盲区已闭合**,本债落注释后**无需新建测试**,仅需回归现有四组反例 + §11 列表。

## 8) 爆炸半径(生产消费者 + 测试消费者 + 灾难链)

### 生产消费者(盘上回盘)
- `core/services/report/execution_review.py:257` `export_execution_review_xlsx` → 调 `self.execution_review`(同文件,导出复用同一 service)。
- `web/routes/reports_page_support.py:335` `_execution_review_report` → `engine.execution_review(...)`,**不传** plan_role/scenario_id(:323-343)。页面入口另有 `execution_review_plan_identity_error`(:396)+ `_blocked_execution_review_report`(:351)做身份拦截 = LB06 web 守卫。
- `web/routes/reports_export_routes.py:96` `execution_review_export` → :100 `plan_role,scenario_id = require_execution_review_adopted_plan()` 入口守卫 → :105 `engine.export_execution_review_xlsx`。
- `core/services/report/report_plan_helpers.py:56-106` `_list_plan_rows_between`/`_all` —— **完整透传** plan_role/scenario_id 进 `list_detail_rows`(:60-114)。这正是为何 execution_review 在 :180-192 硬钉是**数据层最后一道把关**:helpers 本身不挡,挡的是 execution_review 传死。
- `core/services/scheduler/schedule_plan_query_service.py:176` `resolve_plan_view`:scenario_id 非空 → :185 `_resolve_scenario_plan` → `source_table=SOURCE_ADJUSTMENT_SCENARIO_ROWS`(import :13)。即 **role/scenario 一旦透传就换取数源表**。
- 导出落地:`core/services/report/exporters/xlsx.py:365` `export_execution_review_xlsx`。

### 测试消费者(盘上回盘)
- `tests/regression_execution_review_identity_guardrail.py`(173 行)—— **本债关键护栏测试**,service+页+导出三层负向覆盖(见 §7)。
- `tests/regression_plan_vs_actual_review.py:269/280/323/330/343` —— execution_review 页+导出行为 + 负向 `/reports/n` not-in。
- `tests/regression_reports_workbench_navigation_contract.py:84` —— 导航保持 adopted。
- `tests/regression_reports_workbench_backlink_contract.py:347/355` —— 非 adopted 报表禁用 execution-review 链 + 负向 not-in。
- `tests/regression_scheduler_workbench_link_guardrails.py:96/121/153/224` —— guard 不可被 enabled flag 覆盖 / 用完整身份 / 拒绝 extra params 注入 scenario。

### 灾难链(删错/统一错会静默炸什么)
若他人以"统一四张报表签名"之名给 `execution_review`(:209)加 `plan_role/scenario_id` 形参并透传:
`report_plan_helpers:56-106` 按 role/scenario 取明细 → `query_service.resolve_plan_view:176` → scenario 非空切 `_resolve_scenario_plan` 换 `source_table=SOURCE_ADJUSTMENT_SCENARIO_ROWS` → **预览/对比方案明细拼进"计划 vs 实际"** → 经 `export_execution_review_xlsx`(:257)导出。而返回 dict(:225-238)**不携带也不拦截 `is_scenario_preview`** → **预览静默冒充正式现场复盘,无 loud failure(违灵魂线)**。
**当前缓解**:此灾难现已被 `regression_execution_review_identity_guardrail.py` 的四组反例**在 CI 层挡住**——若有人加形参,这些断言会红。**残余风险**:测试是请求级(页/导出),若有人**绕过页面、直接在新服务里调透传后的 execution_review**,且不更新该测试,仍可能漏;故 **§90 LB-A2 注释必须钉回 :209**,把"不可加形参"写在改动点本地,而非只靠远端测试。

## 9) 承重毗邻标记(同文件 LB + 禁区行)

同文件 `execution_review.py` 承重债:**LB02(本债)、LB05(同点)、LB06(same_symbol 标记,实体在 web 层)**。R62 是 `real` 非承重,但其清理动作**紧贴**本债禁区,故 R62 动手时必须把以下行视为**不可触碰禁区**(`lb_no_touch`):

**禁区行(盘上真实,任何修法/重构均不得改动其语义或加形参)**:
- **:209** `def execution_review(self, version, *, date_from..., resource_id)` —— **签名故意不收 plan_role/scenario_id**。禁:加这两个形参。
- **:180-181** `_list_plan_rows_between(..., plan_role=ROLE_ADOPTED, scenario_id=None, ...)` —— 禁:改为透传变量。
- **:191-192** `_list_plan_rows_all(..., plan_role=ROLE_ADOPTED, scenario_id=None, ...)` —— 同上。
- **:221** `host._resolve_plan(v, ROLE_ADOPTED, None)` —— 禁:换成 `_resolve_plan(v, plan_role, scenario_id)`。
- **:236** 返回 dict `"plan_role": ROLE_ADOPTED` —— 禁:改为返回请求 role。

合法修法**仅限**:在上述行**上方/旁边补 `#` 注释**(不改任何表达式)。这是承重债唯一允许形态(铁律 3)。

## 10) 分层 / 导入环风险

本债修法 = **纯注释**,**零新增 import,零分层风险**(铁律 6,0 AST 违规)。当前文件 import(:6-14)已全部合规:
- `core.infrastructure.errors`、`core.models.*`(operation_execution_scope / resource_identity / schedule_plan_role)、`core.services.scheduler.schedule_plan_query_service`、相对 `. calculations / .exporters / .report_number_parsing`。
- 无 `core.algorithms→core.services`、无 `core.models→core.services` 越层。
- 注释不引入任何符号,**不可能造越层 import 或导入环**。
- 警示(给 R62/LB06,非本债):若任何人为"统一签名"在本服务里 import 新模块去解析 scenario,可能引 `report`↔`scheduler` 方向的新边——但这正是禁区(§9),本债注释恰为阻止此事。

## 11) 测试耦合(动手前先迁哪些测试)

本债**不删不改产线行为**,故**无需迁移测试,只需回归**。落注释后必跑(盘上确认存在):
1. `tests/regression_execution_review_identity_guardrail.py`(173 行)—— 主护栏,4 组反例必须全绿。
2. `tests/regression_plan_vs_actual_review.py` —— execution_review 页+导出 + 负向 not-in。
3. `tests/regression_scheduler_workbench_link_guardrails.py` —— :96/121/153/224 guard 系列。
4. `tests/regression_reports_workbench_navigation_contract.py:84` + `tests/regression_reports_workbench_backlink_contract.py:347/355`。
注释为 no-op,以上应**零变化全绿**;若任何一条因注释而变色,说明误改了表达式 → 立即回退。

## 12) 前置依赖

- **无 F 门 / 无前置债**即可做(纯增量注释,Batch-1,registry planned_batch=Batch-1 纯增量)。
- **与 LB05 同点合并**:本注释一次动作同时收 LB02+LB05,不需先做对方。
- **下游门控(本债是别人的前置)**:**R62(Batch-3,同文件死分支清理)必须等 LB02/LB05 注释先落**——registry planned_deps + R62.planned_batch 双向确认。R62 动手时本档案行号已下移,须重新 grep 回盘。
- **LB06(web 守卫)** 可并行,无强制先后;建议本债注释交叉引用 web 层 `require_execution_review_adopted_plan`。
- owner_pending=false → **可直接给终态修法并分配执行批次(Batch-1)**,无需 owner 裁断。

---

## 对抗核验(第二双眼睛)

核验人:独立 rg/grep 回盘,只读不改。结论 **PASS**(找不到反证)。

### 重 grep 命令与结果

```
$ rg -n "def execution_review\(|def export_execution_review_xlsx|ROLE_ADOPTED|_resolve_plan|scenario_id=None" core/services/report/execution_review.py
```
- `def execution_review(` → **:209** ✅(签名 :209-218,keyword-only 仅 version/date_from/date_to/batch_id/resource_type/resource_id,**确证不收 plan_role/scenario_id**)
- 四处硬钉:`_list_plan_rows_between` **:180-181**(plan_role=ROLE_ADOPTED, scenario_id=None)、`_list_plan_rows_all` **:191-192**、`host._resolve_plan(v, ROLE_ADOPTED, None)` **:221**、返回 dict `"plan_role": ROLE_ADOPTED` **:236** —— **全部与档案 §1 盘上表一致**
- `_resolve_plan` Protocol 声明 **:121**;`import ROLE_ADOPTED` **:9**;文件总行 476。`git status` 确认 `MM`(本轮被改),漂移 +68~+70 行属实。
- **注释缺失复核**:`sed -n '199,225p'` 显示 :199-208(_execution_review_rows 结尾)与 :209-218(签名)区**零 `#` 注释**,§90 LB-A2 本地护栏文案确未补——in_progress 名副其实。

```
$ ls/wc tests/regression_execution_review_identity_guardrail.py  → 173 行,存在
$ rg -n "baseline_best|future_role|SCENARIO-RPT|_export_links|B-OTHER|def test_" 该文件
```
- 实际有 **6 个测试函数**(档案声称 4 组反例,盘上更充分):baseline_best 拦截 :59、future_role 不泄漏 :76、scenario SCENARIO-RPT 拦截 :91、资源筛选导出只含正式行 :107、历史 adopted 可见 :136、参数化导出拒绝非正式身份 :155-162。**parity 盲区确已闭合,无虚报**。

```
$ rg consumers: execution_review.py:257 / reports_page_support.py:335 / reports_export_routes.py:100
$ rg -n "resolve_plan_view|_resolve_scenario_plan" schedule_plan_query_service.py
```
- 生产消费者三处属实;helpers `report_plan_helpers.py` 确**完整透传** plan_role/scenario_id(:32-100),证 execution_review 硬钉是数据层最后一道把关。
- **灾难链坐实**:`resolve_plan_view`(**:176**)中 scenario_key 非空 → `_resolve_scenario_plan`(:185)**静默切换取数源表**(无 raise),正是"加形参即静默冒充正式复盘"的物理路径,违灵魂线;现被 guardrail 测试在 CI 层挡住,残余风险=绕页面直调,故注释必须钉回 :209。档案 §8 成立。
- **LB06 web 守卫符号核对**:档案写 `require_execution_review_adopted_plan`,盘上确存于 `web/routes/reports_export_routes.py`(import `require_execution_review_adopted_plan as n`,调用点 `:100`,def `execution_review_export` `:96`);页面侧拦截 `execution_review_plan_identity_error`(reports_page_support.py:396)/ `_blocked_execution_review_report`(:351)亦在。LB06 真实存在,无误。

### 逐项裁决
- **行号复核:一致**。§1 盘上表全部命中(:209/:180-181/:191-192/:221/:236/:121/:9),漂移幅度 +68~+70 属实,registry old_location 与 location_status「✅准/未被 git 改」确已失真——档案已正确标红并以盘上为准。
- **承重判断:确认成立**(load_bearing_confirmed)。四张兄弟报表透传 vs execution_review 硬钉的不对称是刻意护栏。
- **承重误删风险:无**。修法全为「补 `#` 注释 + 回归既有测试」,零删除/零统一/零透传/零加形参;明列五处禁区行(:209/:180-181/:191-192/:221/:236)。无「看着该统一」被当无害清理。
- **分层/导入环:0 违规**。import(:6-14)全在同层或下层,注释零新增符号。
- **收口点:不涉收口**(纯护栏注释);adopted-only 不变量本就收敛在已存在单点 `_resolve_plan`(:121/:221),无新建第二模块。
- **owner_pending=false**:终态修法 + Batch-1 分配合规,未越权。
- **同文件协调**:LB05 同点(partition 不同、lb 同 true)可一次注释合并,确认;R62 标签区盘上 :358-382 与护栏区 :180-236 不重叠,后置且禁碰五处禁区,确认。

### 争议点
- **无实质争议**。唯一须留痕的细节:registry 顶层 `phase1_blast.fix_invalidation_risk` 仍写「全仓无请求级回归(最大缺口)」**已过期**——该测试现已存在(173 行),最大盲区已闭合,仅剩注释一项。档案 §2/§7 已正确推翻此过期断言,据盘上为准。此为 registry 自身陈旧,非档案错误,不构成升级三方裁断的理由。

**核验结论:PASS。**
