# R27 逐债档案 — 空 delayed 包 calendar/ 与 batch/:SP05 强制存在却始终未填充

> 本轮只读不改、只产计划。所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。
> 桶 B14 / 病理 P3 / 严重度 low / 簇 ISOLATED / 承重 false / owner_pending false。

## 1) 当前真实 file:line(回盘)+ 漂移记录
R27 不是「一个符号」债，而是「目录/包结构 + 测试自指 pin」债。回盘对象是 4 个空包目录与 SP05 钉死它们的 3 行测试。R27 名义占 **calendar + batch**（dispatch 归 R06，gantt 无独立 id 但同批清除）。

**空包目录(回盘字节数,均为 0):**
- `core/services/scheduler/calendar/__init__.py` — EXISTS, **0 bytes**（目录另含 `__pycache__/`，无任何 `.py` 模块）
- `core/services/scheduler/batch/__init__.py` — EXISTS, **0 bytes**（JSON 旧记 "1字节"，实测 0 字节，微差，以实测为准）
- `core/services/scheduler/dispatch/__init__.py` — EXISTS, **0 bytes**（属 R06，列此供同批参照）
- `core/services/scheduler/gantt/__init__.py` — EXISTS, **0 bytes**（无独立 id，V22 同批扫除）
- 四目录非 `__init__.py` 的 `.py` 模块数均 = **0**（`find -maxdepth 1` 实测），即纯空壳，无任何下沉逻辑。

**SP05 测试自指 pin(回盘行号):**
- `tests/test_sp05_path_topology_contract.py:309` `def test_sp05_service_topology_and_strong_compatibility()`
- `:310` 存在性元组 `("config", "run", "summary", "batch", "dispatch", "gantt", "calendar")` → `:311-313` 断言每个 `package_dir.is_dir()` 且 `(package_dir/"__init__.py").is_file()`
- `:315` delayed-package 循环 `for delayed_package in ("batch", "dispatch", "gantt", "calendar")` → `:316` `_assert_init_has_no_imports(...)`
- 辅助函数 `_assert_init_has_no_imports` 定义于 `:173`，另一处调用在 `:409`（针对 web 空域 `web/routes/domains/<empty_domain>/__init__.py`，**与本债无关**，动元组/循环不会波及）。

**漂移记录:**
- 行号漂移:**无（精准命中）**。registry `current_evidence_2026_06_05` 写 SP05:310 / :315-316，实测 :310 / :315-316 完全吻合。`location_status` 自评「✅准」成立。
- 字节漂移:batch 旧记 1 字节 → 实测 0 字节（无意义微差，不影响修法）。
- 证据漂移(需标注):registry `downstream_effect` 引用 `.limcode/plans/.../05.plan.md:226,230-233`「明确批次逻辑保留顶层」——**当前工作区 `.limcode/plans/` 下无 `05.plan.md`**（实测仅有若干中文命名 `*.plan.md`）。即「保留预留位」这条 roadmap 依据在当前工作区无法按 file:line 回盘；视为「无 active roadmap 占用这 4 包」的弱证据，不足以单独阻断直删，但应在 owner 拍板时复核「日后是否真要分包」。

## 2) 当前状态 + 与 status evidence 一致性
**状态:** `planned`（registry `status_2026_06_05` = planned）。

**与 status evidence 一致性:一致。** registry 标 planned，含义为「应删但未删」。今天回盘:4 空包全部仍在且 0 字节，SP05:310 元组仍含 batch/dispatch/gantt/calendar，:315-316 delayed 循环仍遍历 4 包——即 V22「同提交直删（4 目录 + 2 SP05 站点）」**完全未动手**。代码现状与「planned 未执行」相符,无 in_progress 半截痕迹（目录里没有任何半下沉模块）。verdict = `real_debt_confirmed`（refuted=true 指「对抗质疑被驳回、确认为真债」）。

## 3) 病理 / 严重度 / 桶 / 簇
- **病理 P3** — 搁置脚手架（半截重构）。scheduler 服务包拆分只完成了 `config/run/summary` 三个真包，`batch/calendar`（及 `dispatch/gantt`）预建了空包目录就停手，顶层文件（真实逻辑在 `core/services/scheduler/batch_service.py`、`calendar_engine.py`、`gantt_service.py`、`resource_dispatch_*.py` 等顶层 `.py`）**从未下沉**进这些子包。SP05 反而新增 :310/:315-316 把空壳钉死，保证「不误删、也不误填（__init__ 必须无 import）」——pin 锁住了一个永远空着的脚手架。
- **严重度 low** — 生产零引用（见 §8），不影响运行；纯结构噪声 + 一个自指测试 pin。
- **桶 B14**（home=B14），partition = `scheduler-config-summary-graph`。
- **簇 ISOLATED**，`interference_edges = []`。注意:ISOLATED 指「不与其他簇的债共享收口符号/跨模块耦合」，但 R27 与 R06 是**同批硬绑**（co_change_within_group=[R06]），二者通过同一个 SP05 测试块互相钉死——见 §5。

## 4) 修法类
**修法类:直删（非收口、非 raise、非 parity；承重=false 不限注释）。** owner_pending=false，可给终态修法。

V22「同提交两件事缺一不可」(本债 R27 负责 calendar+batch 那一份):

1. **删空包目录** `core/services/scheduler/calendar/` 与 `core/services/scheduler/batch/`（各仅一个 0 字节 `__init__.py`，外加 calendar 的 `__pycache__/`）。dispatch/ 由 R06 负责删、gantt/ 同批一并删（gantt 无独立 id）。
2. **改 SP05 测试** `tests/test_sp05_path_topology_contract.py`:
   - `:310` 存在性元组从 `("config","run","summary","batch","dispatch","gantt","calendar")` → 仅留 `("config","run","summary")`（移除 batch/dispatch/gantt/calendar 4 项）。
   - **删除整个 :315-316 delayed-package 循环块**（4 包全删后该循环无对象；registry planned_fix_hint 明确「删块比留空元组更干净」）。

**灵魂线合规:** 直删空壳 + 同步摘 pin 是纯减法,不新增任何兜底/静默回退/except 吞错/过度防御。无 P4 raise 诉求（本债病理 P3 非 P4）。

**禁区(直删债也要守的边界):**
- 不得保留空目录而仅清 pin（会让目录变野生未 pin 残渣）；也不得清目录而留 pin（SP05 立即红）。**目录与 pin 必须同一提交原子落地。**
- 不得顺手动 `_assert_init_has_no_imports`（:173）函数体或它在 :409 对 web 空域的调用——那是另一套与本债无关的检查。
- 不得碰 :310 元组里的 `config/run/summary`（真包，有真实下沉模块）。
- 注意:本债 ISOLATED 但跨桶硬绑 R06——**R27 不能单独落地**，须并入 R06 同一提交（见 §5、§12）。

## 5) 同文件碰撞债(siblings)
`same_file_siblings = ["R06"]`。但要分清「同文件」的两层含义:

**(a) 不在同一个 `__init__.py` 文件里。** R27 删的是 `calendar/__init__.py`+`batch/__init__.py`，R06 删的是 `dispatch/__init__.py`，物理上是不同的空文件,删除互不撞行号(各自整目录删除)。

**(b) 真正的碰撞点是同一个测试文件的同一段。** R27 与 R06 都要改 `tests/test_sp05_path_topology_contract.py:310`（同一行元组）和 `:315`（同一行循环），这是真正会撞行号/撞编辑的地方:
- `:310` 元组:R27 摘 batch/calendar/gantt，R06 摘 dispatch。**若两债分两次提交各改 :310,第二次会基于过时行内容产生编辑冲突或漏摘。**
- `:315-316` 循环:R27 与 R06 都想删/改这块。registry 方案是「4 包全删后整块删」——这必须由**单一提交一次性删除**，不能 R27 删一半 R06 删一半。

**协调结论(谁先谁后):**
- **不存在「先后」,只存在「同提交」。** R06 dossier 已产出（`dossiers/R06.md`，11715 字节）并同样声明 co_change=[R27] 硬绑。两债必须在 **V22 这一个提交里一起落地**:同删 4 目录（calendar/batch by R27 + dispatch by R06 + gantt 同批）、同改 :310 元组、同删 :315-316 循环块。
- 字典键位移风险:`:310` 是元组非 dict，无键位移;但元组**元素顺序与个数变化会让 :311-313 的 for-body 自然适配**（循环按元素遍历，删元素即少跑几轮），无需改 body。`:315-316` 循环块整删后,其后的 `:318` `lingering_root_files` 断言等不依赖这两块，行号上移不影响语义。

## 6) 同收口点债
本债是**直删**，不收口到任何统一符号——无「同收口点债」需要排序。

唯一被多债共享的「点」是测试站点 `tests/test_sp05_path_topology_contract.py:310 / :315-316`,它同时被 R27（calendar/batch/gantt）与 R06（dispatch）改写。这不是「收口符号」而是「同一测试块的协同删除」，顺序敏感性已在 §5 处理（必须同提交，不可分次）。

回盘确认 SP05 文件内**没有第二处**钉这 4 个 service 子包名（`rg` scheduler/services 子包名仅命中 :310/:315-316;:96-166、:506-516 那些是 `web.routes.scheduler_*` 路由别名映射，与 core 服务空包无关，不受本债影响）。无隐藏的第三方测试同时依赖这 4 空包。

## 7) 收口行为差异及所需 parity 测试
**不适用——本债是直删，不存在「新旧两路」分支等价问题。**

空包无任何可执行逻辑（0 字节 `__init__.py`，无函数、无导入、无 `__all__`、无副作用），删除后不改变任何运行期行为。不存在 None vs raise 之类边界反例。

唯一的「行为」是 import 语义:删除 `core.services.scheduler.calendar` 包后,任何 `import core.services.scheduler.calendar` 将 `ModuleNotFoundError`。但 §8 已证生产/测试零引用（`from core.services.scheduler.{calendar,batch,dispatch,gantt} import` 全仓 0 命中），故无消费者会观察到这一差异。

**唯一需要的「parity」性质验证 = SP05 自身:** 删目录 + 改 :310/:315-316 后,SP05 整套 `test_sp05_service_topology_and_strong_compatibility` 仍须绿（:318 起的 strong/behavior compat 断言不依赖这 4 包，不受影响）。即「改完测试本身仍通过」是唯一的回归门，而非新旧双路逐分支等价。

## 8) 爆炸半径
**生产消费者(回盘):零。**
- `rg 'from core\.services\.scheduler\.(calendar|batch|dispatch|gantt) import' core/ web/ data/ tests/` → **0 命中**。
- `rg 'scheduler\.(calendar|batch|dispatch|gantt)\b'`（剔除 `*_engine/_service/_detail/_baseline/_topo` 等顶层文件误命中）→ **0 命中**。
- `core/services/scheduler/resource_dispatch_range.py:3` 的 `import calendar` = **Python 标准库 calendar**（无 `core.services.scheduler.` 前缀），经回盘确认非本空包,删空包不影响它。
- callgraph `edges.json` 中无指向这 4 个 `__init__` 的边。

**测试消费者:**
- `tests/test_sp05_path_topology_contract.py:310`（存在性元组含 batch/calendar/dispatch/gantt）
- `tests/test_sp05_path_topology_contract.py:315-316`（4 包 delayed 循环 + `_assert_init_has_no_imports`）
- 仅此一个测试文件钉死，无其他测试引用。

**灾难链(删错/统一错会静默炸什么):**
- 删空包**而漏改 SP05** → :310 元组里 `(package_dir/"__init__.py").is_file()` 立即 `AssertionError`，**响亮失败、非静默**。这是好事:pin 即守卫，误删会被 SP05 当场抓住。
- 改 SP05 **而漏删空包** → 测试仍绿但目录残留为野生未 pin 残渣（结构噪声回流）。这才是隐性风险:无测试兜底,只能靠 review 抓。
- **跨债漏一半最危险（核心灾难链）**:删 calendar/batch(R27) 而漏 dispatch/gantt，或只删一部分却把 :315-316 循环整块删了 → 残留的某个空包失去 `_assert_init_has_no_imports` 守卫，**未来有人往该空包塞 import 不会被任何测试发现**（静默劣化）。故 V22 必须 4 包 + 2 站点一次性原子完成。
- 误把 `config/run/summary`（真包）从 :310 摘掉 → `:321` 起的 strong-compat 别名链找不到真模块，连锁 `AssertionError`。这是为何 §4 把「不碰 config/run/summary」列为禁区。

## 9) 承重毗邻标记
**无承重毗邻。** registry `load_bearing=false`、`lb_no_touch=null`、`phase1_blast.lb_colocation_danger="none"`。

- 4 个空包目录本身 0 字节，无任何 load-bearing 逻辑。
- 同文件兄弟 R06（dispatch 空包）也是 load_bearing=false 的直删债，非承重。
- 改写的 SP05 测试 :310/:315-316 不是承重生产代码,是结构契约 pin。
- **禁区行（即便非承重也不得越界）:**
  - `tests/test_sp05_path_topology_contract.py:173` `_assert_init_has_no_imports` 函数体——不动。
  - `:409` web 空域对该函数的调用——不动（另一套检查）。
  - `:310` 元组中的 `config/run/summary`——保留。
  - `:318` 起 `lingering_root_files`、`:321` 起 strong/behavior compat 断言——不动（行号上移不影响）。

## 10) 分层 / 导入环风险
**无分层/导入环风险。** 本债是纯删除 + 摘 pin，**不引入任何新 import**，自然不可能造越层（core.algorithms→core.services、core.models→core.services）或导入环。

- 删空包是减边，导入图只会缩小不会新增依赖。
- 0 AST 违规风险:无新代码、无新 import 语句。
- 反向确认:这 4 空包当前零被引用（§8），删除后导入图中本就不存在的边继续不存在,拓扑不变。

## 11) 测试耦合
**动手前需同步修改的测试:** 仅 1 个文件、3 行（与 §4 修法步骤 2 完全重合，无额外迁移）。
- `tests/test_sp05_path_topology_contract.py:310`（摘元组元素）
- `tests/test_sp05_path_topology_contract.py:315-316`（删 delayed 循环块）

无需「先迁移再删代码」的两段式操作——因为删的是测试 pin 本身,不是被测的生产符号。测试与生产删除必须**同一提交**（见 §5/§8 灾难链:分次会留 SP05 红或野生残渣）。

无 fixture/conftest 耦合:这 4 空包不被任何 fixture import,无 `conftest.py` 引用（§8 零引用已覆盖 tests/ 目录）。改完后跑 `pytest tests/test_sp05_path_topology_contract.py` 自证整文件绿即可。

## 12) 前置依赖
**硬前置(缺一不可同提交):**
- **R06（B09，dispatch 空包）** — co_change_within_group=[R06]，双向硬绑。R27 **不能单独落地**:删 calendar/batch 而漏 dispatch+gantt（或反之）都留 SP05 红或留野生残渣（§8 灾难链）。两债 + gantt 空包 + SP05:310/:315-316 必须在 **V22 这一个提交**里一起完成。R06 dossier 已产出，可直接联合排批。

**澄清「V22」语义(回盘):**
- `core/infrastructure/migrations/v22.py` **物理文件不存在**于当前工作区。这里的「V22」是 phase 计划里的**批次/提交代号**（= Batch-10 同提交标记），**不是**一个待写的数据库迁移脚本。不要误读为「缺一个 v22 迁移」。本债无任何 DB schema 改动。

**软前置 / 排序约束:**
- 独立于 B01 根（planned_deps_hint 明示「独立于 B01 根」）。
- **早于 B13 facade 删除**（§10.2 排序:本批应在 facade 删除前完成）。
- Low 优先级，但被 R06 配对门控——批次随 R06 走（Phase1 Batch-10 = B09 + R27 已归同批）。

**F 门(批次落地前应过的门):**
- 落地前确认 §1 标注的 `.limcode/.../05.plan.md` 预留位证据漂移已由 owner 复核「日后不再分包」（弱依赖:无 active roadmap 占用这 4 包，但该证据当前工作区缺失,建议口头确认而非阻断）。
- 落地后必过门:`pytest tests/test_sp05_path_topology_contract.py` 全绿（唯一回归门，§7）。

## 对抗核验(第二双眼睛)

**核验日期:** 2026-06-05。**结论:PASS**(低危纯死叶子债,快核路线;独立重 grep 找不到反证)。

**重 grep 命令与结果:**
- 四空包字节/模块数:`for d in calendar batch dispatch gantt; do f=core/services/scheduler/$d/__init__.py; wc -c<$f; find core/services/scheduler/$d -maxdepth 1 -name '*.py' ! -name __init__.py|wc -l; done` → **全部 0 字节、非 `__init__` 模块数全 0**,纯空壳确认。
- SP05 行号回盘:`rg -n 'for delayed_package|_assert_init_has_no_imports|def test_sp05_service_topology|config.*run.*summary' tests/test_sp05_path_topology_contract.py` → `:173` 辅助函数、`:309` 测试函数、`:310` 存在性元组、`:315`/`:316` delayed 循环、`:409` web 空域调用、`:638` 第二处 config/run/summary——**与档案 §1 / §9 逐行精准吻合,行号漂移=无**。
- 生产/测试 import:`rg -n 'from core\.services\.scheduler\.(calendar|batch|dispatch|gantt) import|import core\.services\.scheduler\.(calendar|batch|dispatch|gantt)\b' core/ web/ data/ tests/` → **NO_IMPORT_HITS(0 命中)**,§8 零消费者成立。

**主动找反例(均已澄清,不构成反证):**
- `rg 'scheduler\.(calendar|batch|dispatch|gantt)\b'` 多出 4 处档案未列命中,逐条挑战后**全是误命中**:
  - `core/algorithms/greedy/external_groups.py:70/121` `scheduler.calendar.add_calendar_days(...)` —— 是 `scheduler: Any` 形参的**运行期属性访问**(贪心算法对象的 `.calendar` 属性),非 `core.services.scheduler.calendar` 包 import,删空包不影响。
  - `core/services/scheduler/gantt_service_support.py:75` `scope="scheduler.gantt"` —— 是 DegradationEvent 的**字符串标签**,非 import。
  - `tests/test_greedy_refactor_contracts.py:81` `assert "scheduler.calendar" not in text` —— 是**负向护栏**(禁贪心模块戳私有属性),与空包删除无关、不受波及。
- SP05 `:638` 第二处 `for package_name in ("config","run","summary")` —— **恰好印证**档案 §6「SP05 内无第二处钉这 4 空包」::638 只钉 config/run/summary(真包),不含 batch/dispatch/gantt/calendar,删空包不波及 :638。

**承重/分层/owner_pending 复核:** lb=false、lb_no_touch=null、纯减法无新 import(0 AST 越层风险)、owner_pending=false 给终态修法合规——档案均无误,无「承重误删风险」、无擅自分配批次问题。

**R06 硬绑复核:** `:310`/`:315` 同一行被 R27(calendar/batch/gantt)与 R06(dispatch)共改,必须 V22 同提交原子落地——档案 §5/§12 判断正确,分次会留 SP05 红或编辑冲突。

**核验小结:** R27 = PASS;行号一致;争议=无;承重误删风险=无;不需第三方裁断。
