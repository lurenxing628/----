# R58 逐债档案 — navigation_publish 用未门控 guard_fields 覆盖回 builder 已 gate 的 can_write_feedback(护栏被动削弱)

> **2026-06-08 B 执行终态：fixed。** 本轮只在 `scheduler_navigation_publish.py` 的 `context.update(guard_fields)` 上方补「我是故意的」注释，明确这里保留旧导航上下文语义；真正写入口仍走 `can_emit_feedback_write_urls`。未做 Phase2 剔 `can_write_feedback`，未删整行 `update`，未新增任何温柔兜底。

> 本档案只读不改、只产计划。承重债(load_bearing=true),默认修法仅「补『我是故意的』中文注释 + 绑现有契约测试」。
> 主文件: web/routes/domains/scheduler/scheduler_navigation_publish.py | 簇 C01 | 桶 B01 | 病理 P2 | 严重度 low | owner_pending=false

## 1) 当前真实 file:line(回盘)+ 漂移记录

按符号名独立回盘（不照抄 old_location），证据如下：

- **病灶覆盖语句** `context.update(guard_fields)`：实盘在
  `web/routes/domains/scheduler/scheduler_navigation_publish.py:91`。
  - old_location_DONT_TRUST = `:79-84`；registry 的 current_evidence/phase1_blast 又改口写「覆盖语句在【第86行】」。
  - **实盘 :91**。→ 相对 old_location 漂移约 **+7~12 行**；相对 registry 今日核出的 `:86` 仍 **再漂 +5 行**（registry 自身核错）。
- **承重函数** `_publish_context(plan_resolution, **kwargs)`：实盘 `scheduler_navigation_publish.py:85`（registry 写 :85-93 ✅基本对齐，但内部覆盖行偏了）。
- **guard_fields 来源** `_plan_guard_fields`：实盘 `scheduler_navigation_publish.py:80`，`:82` `return {key: fields.get(key) for key in _PLAN_GUARD_FIELD_NAMES if fields.get(key) is not None}`；`_PLAN_GUARD_FIELD_NAMES` 元组定义 `:12`，含 `can_dispatch`(:22)、`can_write_feedback`(:23)。
- **builder gate** `_feedback_guard_context`：实盘 `web/viewmodels/scheduler_workbench_links.py:149-161`，其中
  - `:157 formal_adopted = plan_role_text == ROLE_ADOPTED and not preview`
  - `:158 can_write = bool(can_write_feedback and formal_adopted)`
  - registry/phase1_blast 写 gate 在 `:145-157`、`can_write` 在 `:154`。**实盘 gate 在 :149-161、can_write 在 :158**，→ workbench_links.py **整体下移约 +4 行**。
- **publish 入口** `publish_workbench_navigation_context`：registry 暗示其在 workbench_links.py，**实盘其实定义在 `web/navigation_context.py:56`**，`:57 context = build_workbench_plan_context(**kwargs)`；真正算 gate 的 `build_workbench_plan_context` 在 `web/viewmodels/scheduler_workbench_links.py:187`，`:214` 调 `_feedback_guard_context`，`:248 "can_write_feedback": can_write`（已门控）。

**漂移结论：有，幅度中等。** 主病灶 +5~12 行（registry 自身今日核出的 :86 都不准，实盘 :91）；builder gate 文件 workbench_links.py 下移约 +4 行。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`。
- 2026-06-08 B 执行终态：registry `status_2026_06_05 = fixed`；`context.update(guard_fields)` 上方已补「我是故意的」注释。下文“零注释 / planned 名副其实”是执行前回盘证据，不再代表当前状态。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P2 + load_bearing=TRUE（护栏被动削弱）。**
- 机理（坐实）：链路 `_publish_context`(:85) → `publish_workbench_navigation_context`(navigation_context.py:56) → `build_workbench_plan_context`(workbench_links.py:187) → `_feedback_guard_context`(:149) 在 `:158` 算出 **门控值** `can_write = can_write_feedback AND formal_adopted`，写进 context 的 `can_write_feedback`(:248)。**随后** `_publish_context:91 context.update(guard_fields)` 用 `_plan_guard_fields`(:82) 从 `plan_role_filter_fields` 抽出的 **未门控原始 `can_write_feedback`** 把门控值**覆盖回去**。
- 后果：`current_workbench_navigation_context()` / 返回 context 里的 `can_write_feedback` 是**非门控真值**（仅反映 plan 自身 can_write_feedback，不再叠加 formal_adopted gate）。
- **严重度 low** 成立：当前**零生产消费**（见 §8）——真正的写链接放行走另一条收口 `can_emit_feedback_write_urls`(workbench_links.py:469)，不读这个 nav context 的 `can_write_feedback`。属**潜伏护栏削弱**，非现网漏洞。
- **桶 B01；簇 C01。**

## 4) 修法类

**类别：P2 / 承重 → 默认【只补『我是故意的』注释 + 绑现有契约测试】(Batch-1，纯增量)。**

- 红线2 钉死：承重债**严禁** 删/合并/统一/透传/加形参/加兜底。本轮**不**执行「剔除 update 里的 can_write_feedback」（那是 Phase2 owner 确认后的可选收敛，归 Batch-4 与 R44/R54 协调，**不在本计划终态修法内动**）。
- 注释贴在 **`scheduler_navigation_publish.py:91 context.update(guard_fields)` 上方**（注意：registry hint 写「贴 :86」，行号已漂，**实盘锚点是 :91**，落地时按符号 `context.update(guard_fields)` 重新定位，勿照抄行号）。注释文案（改编自 planned_fix_hint，钉住 4 点）：
  1. 故意：`context.update` 会用未门控 guard_fields 覆盖 builder 已 gate 的 `can_write_feedback`；
  2. 这是已知护栏削弱（builder 的 `_feedback_guard_context` 只放行 formal_adopted=adopted且非preview 可写）；
  3. 当前**零消费**——真正的写链接放行走 `can_emit_feedback_write_urls`(scheduler_workbench_links.py:469)，非此 nav context；
  4. **勿据此 nav context 的 `can_write_feedback` 渲染写按钮**——会绕过门控；收敛方向=剔除 update 里的 `can_write_feedback`（留待 owner）。
- 绑测试：现有 `tests/web_pages/test_reports_workbench_navigation_contract.py:406 test_scheduler_navigation_publish_keeps_plan_guard_fields` 已 PIN 覆盖语义，注释落地后保持其绿即为承重契约。
- **非 P5 收口**：本债默认动作不收口；若 Phase2 真做收敛，收口点= builder 已存在的 gate（workbench_links.py:149 `_feedback_guard_context` / :469 `can_emit_feedback_write_urls`），不新建模块。

## 5) 同文件碰撞债（siblings）

同文件 `scheduler_navigation_publish.py` 三债硬约束（PHASE0 §3）：R58(B01,本条) / R54(B01,P5收口) / R44(B02)。各自实盘锚点（本轮回盘）：

| sibling | 桶 | 改哪段（实盘锚点） | 与 R58 行号关系 |
|---|---|---|---|
| R44 | B02 | 同文件（registry 未给本债内具体行；属同文件干扰，非同符号） | 不与 :91 同行；但任一先落都会移 :91 行号 |
| R54 | B01 | P5 收口（同文件 + workbench_links.py 双触） | 同上 |

- **谁先谁后**：R58 注释**先落**（planned_deps_hint 明示「R58→R44 边：R58 注释先落」）。R58 只在 `:91` 上方**插入注释行**（纯增量，不改逻辑、不动 dict 键）。
- **撞行号 / dict 键位移风险**：
  - R58 插注释会让 :91 之后所有行**下移**，R44/R54 若按行号编辑会错位 → **三债必须同批或显式串行**，且后落者必须按**符号名**重新定位，禁止照抄行号。
  - R58 **不改 `guard_fields` dict 的键集合**（不增删键、不改 `context.update` 的参数），故不会引发 dict 键位移；R54 是 P5 收口若改 dict 形态需单独评估，与 R58 注释互不破坏。
- 建议顺序：**R58(纯注释,先) → R54(P5收口) → R44(B02)**，每步后按符号重定位。

## 6) 同收口点债

- **本债默认不收口**（仅注释），故本轮无「收口到同一符号」的强耦合。
- 仅在 Phase2 可选收敛场景下，会触及与 builder gate 同一逻辑面：`_feedback_guard_context`(workbench_links.py:149) 与 `can_emit_feedback_write_urls`(:469) 是该护栏的两个**已存在**收口点。
  - R54（同文件 P5 收口，同时触 workbench_links.py）与本债**共享 workbench_links.py 这一护栏面**：interference_edges 标 R54 `same_file: workbench_links.py`。若 R58 走 Phase2 收敛且 R54 也动 workbench_links.py 的护栏，**顺序敏感**——必须 R54 的护栏形态先稳定，R58 才能判断「剔除 update 后是否落到 R54 收口后的 gate」。
  - LB02、R60 也标 `file:workbench_links.py` 同文件干扰边（非同符号），属弱耦合：只要不改 :149-161 / :469 的符号签名即互不影响。
- 顺序敏感性结论：**本轮(注释)无序敏感；Phase2 收敛需排在 R54 之后**。

## 7) 收口行为差异 + 所需 parity 测试

**本轮注释不改行为 → 无 parity 风险。** 以下为 Phase2「剔除 update 里 can_write_feedback」**若做**的差异分析（供 owner 决策，本轮不执行）：

- 新旧两路对边界值是否逐分支等价？**否，存在反例。**
  - 旧路（现状，update 覆盖）：context.can_write_feedback = **raw**（plan 自身 can_write_feedback，不查 formal_adopted）。
  - 新路（剔除 update）：context.can_write_feedback = **builder-gated** = `raw AND (plan_role==adopted AND not preview)`。
  - **反例（两路分歧）**：plan_role=adopted 但 **is_preview=True**，且 plan.can_write_feedback=True →
    - 旧路 = True（护栏被削，错误地"可写"）；新路 = False（gate 生效）。**两路不等价。**
  - **另一反例**：plan_role≠adopted（如 scenario/comparison）且 can_write_feedback=True → 旧路 True，新路 False。
- **契约测试为何对此盲（关键告警）**：现有 `keep_plan_guard_fields` 用例构造 `requested_role/selected_role="adopted"` + `is_preview=False` + `can_write_feedback=True`，此时 **builder-gated≡raw≡True**（`:441/443` 还断言派工链接 disabled 是另一条 `can_dispatch`/superseded gate 的功，非 can_write_feedback）。→ **剔除 update 后该用例仍绿，但绿不代表语义安全**。
- **所需 parity 测试（Phase2 收敛前必补）**：新增「**preview-adopted** 用例」与「**非 adopted 角色** 用例」，断言剔除前后 context.can_write_feedback 在这两个边界上**从 raw 翻为 gated（True→False）**；只有补齐这两个反例分支并明确接受新语义，owner 方可批准剔除。

## 8) 爆炸半径

**生产消费者（调 `_publish_context` / 产出该 nav context 的路由，回盘确认）：**
- `scheduler_navigation_publish.py:109` publish_gantt_navigation_context → 调 `_publish_context`
- `scheduler_navigation_publish.py:137` publish_analysis_navigation_context → 调 `_publish_context`
- `scheduler_navigation_publish.py:168` publish_week_plan_navigation_context → 调 `_publish_context`
- 上游路由：`scheduler_gantt.py`、`scheduler_analysis.py`、`scheduler_week_plan.py` 分别调上述三个 publish（registry prod_consumers 行号 :216/:52/:309 未在本轮逐一回盘，但调用关系成立）。
- `web/viewmodels/scheduler_workbench_link_query.py` 把 `can_write_feedback` 列入 query 字段白名单。

**该 nav context 的 can_write_feedback 当前是否被下游读取传播？（关键缓解）**
- 回盘 `web/viewmodels/scheduler_navigation_links.py`：**grep `can_write_feedback` = 无命中**（"NO can_write_feedback in scheduler_navigation_links.py"）。→ **nav 链接不传播该字段**，故 nav 渲染不依赖被覆盖的脏值。
- 真正的「现场记录写链接」放行走**另一条收口**：`can_emit_feedback_write_urls`(workbench_links.py:469)，其内部 `:470 _is_formal_adopted_context` + `:473 can_dispatch AND can_write_feedback`，消费者：
  - `scheduler_resource_dispatch.py:202 can_write_feedback = can_emit_feedback_write_urls(filters)`
  - `scheduler_reports_workbench.py:361 can_write_feedback = can_emit_feedback_write_urls(row_context)`
  - **这两条不读 nav context**，读的是各自 filters/row_context → 与 R58 病灶解耦。

**测试消费者：**
- `tests/web_pages/test_reports_workbench_navigation_contract.py:406` test_scheduler_navigation_publish_keeps_plan_guard_fields（PIN :439/440/456/457/471/472 三处 context['can_write_feedback']==True for superseded，即 PIN 了被覆盖回的原始值）。
- 同测试 :429/446/462 三个 publish_*_navigation_context 调用点。

**灾难链（删错/统一错会静默炸什么）：**
- 若**误删整行 `context.update(guard_fields)`**（红线禁止）：连带丢失 `can_dispatch` 等**其它 guard 字段**，致 superseded 派工护栏（依赖 can_dispatch + superseded）失效，且 `keep_plan_guard_fields` 测试转红——**响声有**，非静默，但属违规修法。
- 若**只剔 can_write_feedback 一键**（Phase2 可选）：现有契约测试**仍绿**（§7 已证 superseded 用例 builder-gated≡raw），但 **preview-adopted / 非 adopted 路径语义静默翻转**（True→False）→ **静默炸点**：未来若有人据 nav context 渲染写按钮，行为悄悄改变而测试无感。这是最大潜伏风险。

## 9) 承重毗邻标记 + 禁区行

- **R58 自身即 load_bearing=TRUE。** 它守护的护栏 = `_feedback_guard_context` 的 formal_adopted gate（只 adopted+非preview 可写）。
- 同文件/同护栏面承重毗邻：interference_edges 含 **LB02**（`file:workbench_links.py`，承重邻接）。
- **禁区行（绝不删/改/透传/合并）：**
  - `web/routes/domains/scheduler/scheduler_navigation_publish.py:91 context.update(guard_fields)` —— 本轮只允许在其**上方插注释**，绝不删整行、不剔键（剔 can_write_feedback 须 owner Phase2 批准且补反例 parity）。
  - `web/routes/domains/scheduler/scheduler_navigation_publish.py:80-82 _plan_guard_fields` 及 `:12-24 _PLAN_GUARD_FIELD_NAMES` —— 不增删 guard 字段集。
  - `web/viewmodels/scheduler_workbench_links.py:149-161 _feedback_guard_context`（尤 `:157 formal_adopted`、`:158 can_write = bool(...)`）—— builder gate 本体，禁动。
  - `web/viewmodels/scheduler_workbench_links.py:469-473 can_emit_feedback_write_urls` —— 真正写链接收口，禁动。
  - `web/viewmodels/scheduler_workbench_links.py:248 "can_write_feedback": can_write` —— 门控值落点，禁改。

## 10) 分层 / 导入环风险

- 本轮**纯注释，零 import 变更 → 零分层风险、零导入环风险。**
- 现状 import 合规（回盘）：`scheduler_navigation_publish.py` 已从 `web.navigation_context` 取 `publish_workbench_navigation_context`/`set_current_workbench_navigation_context`(:8)、从 `core.services.scheduler.*` 取 `plan_role_filter_fields`(:7)、`ROLE_ADOPTED`(:6)。均 web→core / web→web 同/下层，**无越层**。
- Phase2 若收敛：即便要复用 builder gate，也是 web 层内（navigation_publish 已能访问 workbench_links 的导出），**不会新增 core→web 或 models→services 越层**；分层红线（0 AST 违规）可保持，但仍须复核不引入 `scheduler_navigation_publish ↔ scheduler_workbench_links` 的双向导入环（当前为单向 publish→build，安全）。

## 11) 测试耦合（动手前先迁哪些）

- **本轮注释不需迁测试**——`tests/web_pages/test_reports_workbench_navigation_contract.py:406 keep_plan_guard_fields` 已就位，注释落地后只需**跑它保持绿**作为承重契约自证。
- Phase2 收敛**才**需先动测试：在剔除 update 之前，先向上述契约测试**增补** preview-adopted、非 adopted 两个反例分支（§7），把「True→False 翻转」固化为期望，再改代码。**先迁测试后改码**，顺序不可倒。

## 12) 前置依赖（先做哪些债 / 哪些 F 门）

- **本轮（Batch-1 注释）前置：** 几乎无硬前置（纯增量注释）。唯一协调约束 = **PHASE0 §3 三债同文件**：与 R44(B02)/R54(B01) 必须**同批或显式串行**；R58 注释**先落**（planned_deps_hint）。
- **落地纪律前置：** 因 workbench_links.py 与 navigation_publish.py 当前 `git status` 均为 **M / MM（已漂）**，动手前须先按符号重定位 `context.update(guard_fields)`(:91) 与 `_feedback_guard_context`(:149)，**禁照抄 registry 的 :86/:145**。
- **Phase2 可选收敛前置（owner_pending=false 但收敛仍需 owner 拍板）：** 先 (a) 验证 builder-gated≡raw 的全分支覆盖、(b) 补 §7 两个反例 parity 测试、(c) 排在 **R54(同文件 P5 收口) 之后**、(d) 确认 workbench_links.py 护栏面在 R54 收口后稳定。
- F 门：注释属 ROOT 纯增量，按现有回归门（contract 测试绿）即可放行；无额外 F 门阻塞。

## 对抗核验（第二双眼睛）

核验日期 2026-06-05。只读不改，默认怀疑，独立 rg/grep 回盘，结论如下。

**重 grep 命令与结果（关键证据）：**

1. `rg -n "context\.update\(guard_fields\)" scheduler_navigation_publish.py` → 唯一命中 **:91**。证实病灶实盘在 :91（非 registry 的 :86，非 old_location 的 :79-84）。dossier §1 自报 :91 **复核一致**。
2. `rg -n "_publish_context|_plan_guard_fields|_PLAN_GUARD_FIELD_NAMES|publish_workbench_navigation_context|can_write_feedback|can_dispatch"` → `_PLAN_GUARD_FIELD_NAMES`(:12, 含 can_dispatch :22 / can_write_feedback :23)、`_plan_guard_fields`(:80, return :82)、`_publish_context`(:85)、`guard_fields=_plan_guard_fields`(:86)、`can_write_feedback=guard_fields.get(...)` 传入 builder(:88)、三 publish 调用点 :109/:137/:168。**全部与 §1/§8 一致。**
3. Read :80-93 坐实机理：:88 把 raw can_write_feedback 喂给 builder（builder 内 gate 成 can_write 写到 :248），**:91 `context.update(guard_fields)` 随后用 raw guard_fields 把门控值覆盖回去**——P2 护栏被动削弱 **坐实**。
4. `rg -n "_feedback_guard_context|formal_adopted|can_write = bool|can_emit_feedback_write_urls|build_workbench_plan_context|\"can_write_feedback\": can_write|_is_formal_adopted_context" scheduler_workbench_links.py` → gate def **:149**、`formal_adopted = plan_role_text == ROLE_ADOPTED and not preview` **:157**、`can_write = bool(can_write_feedback and formal_adopted)` **:158**、build_workbench_plan_context **:187**、调 gate **:214**、门控落点 `"can_write_feedback": can_write` **:248**、`can_emit_feedback_write_urls` def **:469** / `_is_formal_adopted_context` **:470**。**全部与 §1/§3/§9 一致**（registry 的 :145-157/:154/:381 三处行号 **错**，dossier 已正确改口并未照抄——回盘印证 dossier 正确）。
5. publish 入口：`web/navigation_context.py:56 def publish_workbench_navigation_context`、`:57 context = build_workbench_plan_context(**kwargs)`。**与 §1 一致。**
6. 下游传播缓解：`rg can_write_feedback web/viewmodels/scheduler_navigation_links.py` → **零命中**（nav 链接不传播脏值）。真正写链接收口 `can_emit_feedback_write_urls` 消费者：`scheduler_resource_dispatch.py:202`、`web/viewmodels/scheduler_reports_workbench.py:361`（dossier 写 `scheduler_reports_workbench.py:361` 省了 `viewmodels/` 目录前缀，文件名/行号正确），两者读 filters/row_context 非 nav context → **与病灶解耦，§8 缓解成立，severity=low 零生产消费 坐实。**
7. 契约测试 Read :406-472：superseded_plan `requested_role=selected_role="adopted"` + `is_preview=False`（:416-417）+ `can_write_feedback=True`(:419)；`expected_guard_fields` 含 `can_write_feedback=True`(:425)；断言为 dict-subset 等价 `{key: ctx[key] for key in expected_guard_fields}==expected_guard_fields`，落在 **:439/440、:456/457、:471/472**——dossier §8 列的这 6 个行号 **复核一致**；**registry 的 :453/468/475 错**（:453=gantt publish 收尾、:468=日期参数、:475=下一个 test def），dossier 未照抄 **正确**。
8. **§7「契约测试盲点」反例独立复证为真**：superseded 用例 adopted+非preview → builder gate `formal_adopted=True` → builder-gated `can_write = True AND True = True ≡ raw`，故 Phase2「剔 update」后该用例仍绿，而 preview-adopted / 非 adopted 路径会静默从 True 翻 False。§7/§8/§9 静默炸点告警 **成立**。
9. 分层：`rg "^from|^import" scheduler_navigation_publish.py` → 仅 `core.infrastructure.errors`、`core.services.scheduler.*`(ROLE_ADOPTED :6、plan_role_filter_fields :7)、`web.navigation_context` :8、`web.request_resource_context` :9。**纯 web→core(下) / web→web(同)，无 core→web 越层；单向 publish→build 无导入环。§10 成立。**
10. git status 复核：navigation_publish.py = **M**、workbench_links.py = **MM**（均已漂）——印证 §12「动手前须按符号重定位、禁照抄 registry 行号」纪律必要。

**承重误删审查（红线2）：** dossier 终态修法 = 仅在 :91 上方插「我是故意的」注释 + 绑现有 `keep_plan_guard_fields` 契约，**未混入任何 删/合并/统一/透传/加形参/加兜底**；明确把「剔 can_write_feedback」划归 Phase2/owner 之外、列禁区行（:91/:80-82/:12-24/:149-161/:469-473/:248）。**承重误删风险=无。**

**owner_pending 审查：** owner_pending=false（非 true），dossier 给终态修法（注释）+ 分配 Batch-1 **合规**；可选收敛已正确标注「须 owner 拍板、排 R54 之后、先补两反例 parity」未擅自分配执行。

**像重复实为护栏复核（C01/承重）：** :91 update 看似冗余覆盖，实为 _plan_guard_fields 一次性写齐 can_dispatch 等多护栏字段的统一落点；DRY「剔 can_write_feedback」会静默翻转 gate 语义 + 误删整行会连带丢 can_dispatch 致 superseded 派工护栏失效——**dossier §8 灾难链分析正确，DRY 引爆风险已识别。**

**唯一非实质瑕疵（不影响结论）：** §8 表述「PIN context['can_write_feedback']==True」，实盘断言是含 can_write_feedback==True 的 dict-subset 等价，非独立标量断言——语义等价，仅措辞不够精确。

**核验结论：PASS。** 全部 file:line 与机理、消费者清单、承重判断、分层、收口点、反例分析独立回盘一致；dossier 还主动纠正了 registry 自身的多处错误行号。无需第三方裁断。
