# R61 逐债档案 — filter_plan_rows_for_report_context 出生即死(实际过滤在 SQL 层，已清理)

> 簇 C01 · 桶 B17 · 病理 P6(出生即死) · 严重度 low · 承重 false · owner_pending false
> 2026-06-08 B 执行终态：已 fixed。`core/services/report/report_context_filters.py` 中旧 `filter_plan_rows_for_report_context` + `_plan_row_matches_batch` + `_plan_row_matches_resource` 死簇已删除；`tests/scheduler_analysis/test_report_context_filters_contract.py` 中三条负向断言已重定向到 `normalize_report_resource_filter`；`filter_downtime_rows_for_report_context` / `_row_text` / `normalize_report_resource_filter` live 链均保留。
> 验证：`PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/scheduler_analysis/test_report_context_filters_contract.py` = **13 passed**；ruff 通过；Python 3.8 语法扫描总发现数 0；`rg` 确认三死符号在 core/web/data/tests 中零命中。

## 字段 1 — 执行前真实 file:line(回盘)+ 执行后终态

死簇三符号(全部在 `core/services/report/report_context_filters.py`,2026-06-05 实测；2026-06-08 已删除):

| 符号 | 老 location(DONT_TRUST) | 实测 file:line | 漂移 |
|---|---|---|---|
| `filter_plan_rows_for_report_context` (def→return) | 160-187 | **:172-187** | def 老标 160 实际 172,**下移 12 行** |
| `_plan_row_matches_batch` (helper) | (混在 160-187) | **:160-161** | 老 location 把 helper 起点当成主函数起点 |
| `_plan_row_matches_resource` (helper) | (混在 160-187) | **:164-169** | 同上 |

执行前证据:`rg -n "^def " report_context_filters.py` → :160 `_plan_row_matches_batch`、:164 `_plan_row_matches_resource`、:172 `filter_plan_rows_for_report_context`。
**漂移结论:有,幅度 +12 行**。老 location 的 `160-187` 实为「helper 起点 → 主函数末尾」的混合区间;真正的主函数 def 在 :172。registry 的 `phase1_blast.location_status` 已正确标注此漂移,本回盘一致。
**执行后终态**：三死符号在 `core/services/report/report_context_filters.py`、`tests/scheduler_analysis/test_report_context_filters_contract.py`、`core/`、`web/`、`data/` 中均零命中；下方 live downtime 簇因删除整体上移，但相对逻辑不变。

死簇内部调用链(:184/:185 证据):
- `filter_plan_rows_for_report_context` :184 调 `_plan_row_matches_batch`、:185 调 `_plan_row_matches_resource`(均见 Read :184-185)。
- 两 helper 经 `rg` 全仓搜,**仅** :184/:185 这一处调用者(死函数自身),无第二供养点。

## 字段 2 — 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = "fixed"`（2026-06-08 B 执行补登）。
- 执行终态:三死符号已删除；测试 import 不再包含 `filter_plan_rows_for_report_context`；原 :66/:75/:81 三处调用已改为 `normalize_report_resource_filter(...)`。
- **一致**:状态 fixed 与「代码和测试均已完成，live 孪生未动」完全吻合。
## 字段 3 — 病理 / 严重度 / 桶 / 簇

- **病理 P6 出生即死**:`filter_plan_rows_for_report_context` 实现了「报表计划行的 Python 端 batch/resource 过滤」,但运行期**零生产引用**——真正的计划行过滤在 SQL 层完成(`data/repositories/schedule_plan_query_repo.py:331,362` 调 `append_detail_filters`,来自 `schedule_resource_sql_filters.py`)。该函数 + 两 helper 仅被合同测试供养,制造「此函数存活」的假信号(实为层级误导:让人以为计划行在 Python 过滤)。
- **严重度 low**:运行期 downstream_effect = 零(死代码)。删除收益纯属去维护成本 + 消除层级误导,无功能风险。
- **桶 B17 / home B17;簇 C01;partition delta-report-core;kind real;load_bearing false**。

## 字段 4 — 修法类

**修法类 = 直删 + 退测试(优选:负向测试重定向)**。非承重(load_bearing=false, lb_no_touch=null),无 owner_pending,可给终态修法。

删除范围(三死符号 + 其间空行):
1. `filter_plan_rows_for_report_context` :172-187(主死函数)。
2. `_plan_row_matches_batch` :160-161(仅供 :184)。
3. `_plan_row_matches_resource` :164-169(仅供 :185)。

**严格保留(禁删,均被真活路径用)**:
- `_row_text` :156 — 被 downtime 孪生链真用:`_schedule_machine_ids` :161、`_filter_downtime_rows_by_machine` :191、`_schedule_overlaps_downtime` :209、`_downtime_row_overlaps_schedule` :223。`rg "_row_text"` 证实删死簇后这些 live 引用仍在 → 必须保留。
- `normalize_report_resource_filter` :119 — 被 :252(downtime 孪生)+ `report_engine.py:146/167/279/383` + `web/request_resource_context.py:39` + `web/routes/reports_request_support.py:55` 真用。**绝对禁删**。
- 整个 `filter_downtime_rows_for_report_context` :244-263 及其全部私有 helper(`_schedule_machine_ids`:160、`_downtime_machine_filter`:164、`_filter_downtime_rows_by_machine`:180、`_parsed_interval`:195、`_schedule_overlaps_downtime`:203、`_downtime_row_overlaps_schedule`:219、`_filter_downtime_rows_by_batch_overlap`:233)— `report_engine.py:406` live 调用。

**灵魂线核对(无违规)**:本修法纯删死码,不新增兜底/静默回退/except 吞错/过度防御;不改 loud 行为。删除后 `normalize_report_resource_filter` 的 loud raise(ValidationError)路径**完全不受影响**(它独立存活)。

**测试处理(关键细节,优于 registry 的「直接退测试」)**:
- :64-70 `test_report_core_filter_rejects_unsupported_resource_type`、:73-84 `test_report_core_filter_rejects_resource_type_without_resource_id` 这两条**负向测试断言的是资源校验拒绝**(ValidationError),而该校验真身在 `normalize_report_resource_filter`(死函数 :180 与 live 孪生 :282 同样调它)。
- 因此推荐**重定向**这两条断言体到直接调 `normalize_report_resource_filter(...)`(kept 符号),而非整删 —— 保住「team 维度拒绝 / machine 缺编号拒绝 / operator 缺编号拒绝」三条校验覆盖,只删 import :8 中的 `filter_plan_rows_for_report_context` 与函数调用外壳。
- registry `current_evidence_2026_06_05` 已点名「(两条负向测试未重定向到 normalize_report_resource_filter)」,本档案据此判定**重定向 > 纯删**,与 registry planned_fix_hint「退测试」不冲突(退的是 import 与调用壳,断言迁到 kept 符号)。
## 字段 5 — 同文件碰撞债

`same_file_siblings = []`(registry)——`report_context_filters.py` 内**无**其他登记债。
`interference_edges` 标的 LB02 / LB05 / R14 三条均 `same_file=true` 但 `file:report_engine.py`,**不是本文件**(本债主文件是 `report_context_filters.py`,这三条是「跨债共享 report_engine.py」的弱边,与本删除不在同一物理文件)。

- **无同文件碰撞**:删 :160-187 这段死簇,不与本文件任何其他债竞争行号。
- 行号位移影响:删除 :160-187(约 28 行,含空行)会使其**下方**所有 def 上移约 28 行(`_schedule_machine_ids` :190 → ~:162 等)。但本文件无其他登记债 → 无 dict 键位移撞车、无 sibling 改段顺序问题。下方 downtime 簇是整块迁移,内部相对引用不变。
- 与 report_engine.py 上的 LB02/LB05/R14:本删除**不触碰** report_engine.py 一字(import :28 只删不到——见字段 11),故对那三条债零影响,无谁先谁后约束。

## 字段 6 — 同收口点债

本债为**直删**类,非收口类 → 无「收口到同一符号」的概念。
唯一与本删除发生符号交集的是 kept 符号 `normalize_report_resource_filter`(死函数 :180 调它、live 孪生 :282 调它)。但本债不收口、不改它,仅在删除死函数时移除 :180 那一处调用。无任何其他债收口到 `normalize_report_resource_filter`(registry 未登记此符号为收口点)。**无顺序敏感性**。

## 字段 7 — 收口行为差异及 parity 测试

本债是**直删死码**,非收口/合并/统一 → **无新旧两路 parity 问题**(没有「旧路 vs 新路」,只有「删掉一条零调用的死路」)。

- 不存在 None vs raise 这类边界分叉:死函数从未被生产调用,删它不改变任何运行期可观测行为。
- 唯一需「等价保住」的是测试侧覆盖:删 :172-187 后,:66/:75/:81 三条负向断言原走 `filter_plan_rows_for_report_context → :180 normalize_report_resource_filter` 抛 ValidationError;重定向后改为**直接**调 `normalize_report_resource_filter`,抛同一 ValidationError(同一函数同一分支)。这不是 parity 测试,是「把断言挂到真身上」——校验逻辑物理同源,无行为差。
- 若选择「纯删测试」而非重定向:则 team 维度拒绝 / 缺编号拒绝 的覆盖会丢失,但该覆盖在 :87-108 的 `normalize_report_resource_filter` 直测、:111+ 的 request 层测试中**部分仍在**(:64-84 的 plan-flavor 入口覆盖会净损)。故**推荐重定向以零覆盖损失**。
## 字段 8 — 爆炸半径

**生产消费者:0**(已 grep 证实)。
- `rg "filter_plan_rows_for_report_context|_plan_row_matches_batch|_plan_row_matches_resource" core/ web/ data/` → 命中**仅** `report_context_filters.py` 自身的 def + 死函数内部 :184/:185 调用。无任何 core/web/data 外部引用。
- `report_engine.py:28` 的 import 行 = `from .report_context_filters import filter_downtime_rows_for_report_context, normalize_report_resource_filter` —— **不含** plan 死函数(只 import 孪生 + normalize)。:406 只调 `filter_downtime_rows_for_report_context`。
- 真实计划行过滤路径(死函数本应做的事,实由 SQL 做):`schedule_plan_query_repo.py:331,362` → `append_detail_filters`(`schedule_resource_sql_filters.py`)。

**测试消费者(4 处,全在同一文件)**:
- `tests/scheduler_analysis/test_report_context_filters_contract.py:8`(import)、`:66`、`:75`、`:81`(三处调用,均在两条负向测试内)。
- 全仓 `rg -ln "filter_plan_rows_for_report_context" tests/` → **仅此一个测试文件**,无第二测试引用。

**灾难链(删错/退错会静默炸什么)**:
1. 若手抖把孪生 `filter_downtime_rows_for_report_context`(:244,report_engine:406 live)一起删 → report_engine import 红、报表 downtime 行过滤直接崩(运行期 ImportError)。**防线**:孪生名仅差 `plan`/`downtime`,删除时按 def 行段(:172-187 + :160-169)精确切,勿按文本前缀模糊删。
2. 若误删共享 `_row_text`(:156)或 `normalize_report_resource_filter`(:119)→ 波及 downtime 全链 + report_engine 4 处 + web 2 处,大面积炸。**防线**:字段 4 已列保留清单。
3. 若删函数但漏退/漏改测试 :8/:66/:75/:81 → 测试 import 即红(collection error),CI 当场拦截(非静默)——属可见失败,危害低。
4. 若「纯删测试」而非重定向 → 静默丢失 team/缺编号 校验在 plan 入口的覆盖,运行期无感但回归网变薄(隐性债)。**防线**:选重定向。

## 字段 9 — 承重毗邻标记

- `lb_colocation_danger = none`:`report_context_filters.py` **不在承重清单**,`load_bearing=false`、`lb_no_touch=null`。
- 本文件内无任何 LB 标记符号,无禁区行。
- 注:`interference_edges` 里的 LB02/LB05 是 report_engine.py 上的承重债,**不在本文件**,本删除不触 report_engine.py(连 import 都不删——见字段 11),故 LB 毗邻风险 = 0。

## 字段 10 — 分层 / 导入环风险

- 本债**纯删**,不新增任何 import,不可能引入越层 import 或导入环。
- 删后 `report_context_filters.py` 仍属 `core/services/report/`,被 `report_engine.py`(同层)、`web/`(上层,合法)引用,方向不变。
- 0 AST 违规风险:无 `core.algorithms→core.services`、无 `core.models→core.services` 新边(本删除不产生任何新边)。
- 删除 :160-187 不影响文件顶部 import(`Dict/Any/List/Iterable/Optional/Tuple`、`parse_dt`、`overlap_seconds`、`ValidationError` 等)——这些被 downtime live 链 + normalize 继续使用,不可顺手清。

## 字段 11 — 测试耦合(动手前先迁哪些测试)

动手顺序(必须同批,否则测试 collection 红):
1. **改 import :7-9**:从 import 块删去 `filter_plan_rows_for_report_context`(:8),保留 `filter_downtime_rows_for_report_context`(:7,live 孪生测试 :16/:33/:44/:56 真用)与 `normalize_report_resource_filter`(:9,:87+ 直测真用)。
2. **重定向 :64-70**:`test_report_core_filter_rejects_unsupported_resource_type` 内 :66 的 `filter_plan_rows_for_report_context([...], resource_type="team", resource_id="T-RPT")` 改为 `normalize_report_resource_filter(resource_type="team", resource_id="T-RPT")`,断言 `match="当前报表暂不支持班组维度筛选"` 不变。
3. **重定向 :73-84**::75/:81 两处 `filter_plan_rows_for_report_context([...], resource_type="machine"/"operator")` 改为 `normalize_report_resource_filter(resource_type="machine")` / `(resource_type="operator")`,断言 `match="缺少设备编号"`/`"缺少人员编号"` 不变。
4. **保留勿动**:downtime 四测(:16/:33/:44/:56)、normalize 冲突直测(:87-108)、request 层测(:111/:129/:149)、overdue/SQL bottom 测(:162/:193/:220)。
- **禁止**删除/改动 :16-59 的 downtime 测试(孪生 live)。

## 字段 12 — 前置依赖

- registry `planned_deps_hint = 无`、`co_change_within_group = []`、`fix_invalidation_risk` 明示「无其他债依赖」。
- **完全独立叶子债**:与本桶 R64/R65/R53 不同文件(它们不在 `report_context_filters.py`),任意先后,无门控。
- `planned_batch_hint = Batch-16(独立叶子)`,Batch-1 之后任意时点可单独成一小提交。
- **唯一硬约束**:删函数 + 改测试**必须同一提交**(原子),否则中间态 CI 红(测试 import 未删而函数已删,或反之)。这是「同批」不是「前置债」。
- 无任何 F 门前置。

---

### 一句话结论
R61 是教科书式的「出生即死」死簇:三符号(:160/:164/:172-187)零生产引用,仅被合同测试供养假信号;直删 + 把两条负向测试**重定向**到真身 `normalize_report_resource_filter` 即可零覆盖损失收口;最大风险是手抖误删一字之差的 live 孪生 `filter_downtime_*` 或共享 helper `_row_text`/`normalize_report_resource_filter`;完全独立,与既有 registry 分析无冲突(仅在「退测试 vs 重定向」上提供更优解,方向一致)。

---

## 对抗核验(第二双眼睛)

**结论:PASS**(2026-06-05 独立 rg/grep 回盘,默认怀疑、找反证未果)。

重盘命令与结果:
- `rg -n "^def |^    def " core/services/report/report_context_filters.py` → `_plan_row_matches_batch`:160、`_plan_row_matches_resource`:164、`filter_plan_rows_for_report_context`:172(主 def);Read :172-187 确认 return 在 :187。**行号复核一致**,漂移 +12 行(老 160→实 172,老 location 把 helper 起点当主函数起点)成立。
- `rg -n "filter_plan_rows_for_report_context|_plan_row_matches_batch|_plan_row_matches_resource" core/ web/ data/` → 仅命中本文件 def + 死函数内 :184/:185 自调,**core/web/data 零外部消费者**,生产引用=0 成立。
- `rg -n "filter_downtime_rows_for_report_context" core/services/report/report_engine.py` → :28 import、:406 调用;import 行实文为 `from .report_context_filters import filter_downtime_rows_for_report_context, normalize_report_resource_filter`,**不含 plan 死函数** → 「连 import 都不删、删除不触 report_engine.py」成立。
- `rg -n "_row_text" report_context_filters.py` → :191/:221/:239/:253(downtime live 链)真用,删死簇后仍被引用,**禁删 `_row_text` 成立**。
- `rg -n "normalize_report_resource_filter" core/ web/` → report_engine :146/167/279/383 + web/request_resource_context.py:39 + web/routes/reports_request_support.py:55 + downtime 份 :282 真用,**绝对禁删成立**。
- Read 死函数 :180 + 测试 :64-84:负向断言(班组拒绝 / 缺设备编号 / 缺人员编号)的 ValidationError 由 `normalize_report_resource_filter` 在过滤前抛出,与 live 孪生 :282 同源 → **重定向零覆盖损失等价性成立**。

**行号复核:一致**(主 def :172-187 / helper :160 / :164 三符号全部对齐)。
**争议点:无**(仅一处微漂移,无害:registry/档案对 reports_request_support 写 :54,实测 import :10、调用 :55,不影响修法)。
**承重误删风险:无**(load_bearing=false、lb_no_touch=null;终态修法为纯删死码,无删除统一/透传/加形参混入承重符号;保留清单 `_row_text`/`normalize_report_resource_filter`/`filter_downtime_*` 已正确列禁删)。
**灵魂线核对:无违规**(纯删,不新增兜底/静默回退/except 吞错;不改 loud raise 路径;normalize 的 ValidationError 独立存活)。
**分层/导入环:无新边**(纯删不新增 import,0 AST 违规)。
**owner_pending=false**:终态修法允许给出,无擅自越权。
**需升级第三方裁:n**。
