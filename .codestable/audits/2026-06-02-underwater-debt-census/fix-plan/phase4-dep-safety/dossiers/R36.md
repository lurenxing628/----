# R36 逐债档案 — batch_operation_repo 两个死查询方法（get_by_op_code + list_by_status）

> 桶 B12 | 病理 P6 | 严重度 low | 簇 ISOLATED | 承重 false | owner_pending false
> 原始档案只读不改，仅产计划。原始断言附 2026-06-05 回盘 file:line 证据；2026-06-08 B 执行后已补登终态。以下旧行号保留作执行前证据，当前状态以 §0 与 `_registry.json` 为准。

## 字段 0：2026-06-08 B 执行终态补登

- **状态：fixed。** `BatchOperationRepository.get_by_op_code` 已删除（旧锚点 `data/repositories/batch_operation_repo.py:25-35`）。
- **状态：fixed。** `BatchOperationRepository.list_by_status` 已删除（旧锚点 `data/repositories/batch_operation_repo.py:50-61`）。
- **残留扫描：** `rg -n "get_by_op_code|list_by_status|['\"]get_by_op_code['\"]|['\"]list_by_status['\"]" core web data tests tools scripts 开发文档/技术债务治理台账.md tools/test_registry_data.py tools/full_test_debt_shards.py pyrightconfig.tools.json` 无命中。
- **活近亲保留：** `get/list_by_batch/create/update/delete_by_batch` 保留；未新增 helper、兜底、异常吞咽或跨层 import。

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

回盘命令：`rg -n "def get_by_op_code|def list_by_status" data/repositories/batch_operation_repo.py`

| 符号 | old_location_DONT_TRUST | 当前真实 file:line | 方法体范围 | 漂移 |
|---|---|---|---|---|
| `get_by_op_code` | `batch_operation_repo.py:25` | `data/repositories/batch_operation_repo.py:25` | `:25-35`（return 在 :35） | **无漂移**（行号一致） |
| `list_by_status` | `batch_operation_repo.py:50` | `data/repositories/batch_operation_repo.py:50` | `:50-61`（return 在 :61） | **无漂移**（行号一致） |

- 证据：`get_by_op_code` def 在 `batch_operation_repo.py:25`，方法体 `:26-35`（SELECT … WHERE op_code = ? → `BatchOperation.from_row(row) if row else None`）。
- 证据：`list_by_status` def 在 `batch_operation_repo.py:50`，方法体 `:51-61`（SELECT … WHERE status = ? ORDER BY created_at DESC → list comprehension）。
- 文件不在漂移名单，`git diff b08162cd -- data/repositories/batch_operation_repo.py` 为空（UNCHANGED 确认）。phase1_blast.location_status「✅准」核实成立。

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05` 已在 B 执行后补登为 **fixed**。
- `current_evidence_2026_06_05` 已更新为：两死方法已删除，全仓残留扫描无命中，活方法保留。
- 旧正文中的 planned/旧行号内容仅作为执行前证据保留，不再代表当前代码状态。

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 **P6**：两个定义后零消费的死查询方法（纯 dead code）。
- 严重度 **low**：纯死方法，不参与任何调用链，留着仅是阅读噪声 + 维护税。
- 桶 **B12**，home B12，partition `data-repos`，kind `real`。
- 簇 **ISOLATED**：`same_file_siblings=[]`，`interference_edges=[]`，`co_change_within_group=[]`。本文件仅此一债。
## 字段 4：修法类

**直删（两方法一并删）**。owner_pending=false，承重=false，给终态修法。

- 删 `batch_operation_repo.py:25-35`（`get_by_op_code` 整方法，含 def 行 + 方法体 + return）。
- 删 `batch_operation_repo.py:50-61`（`list_by_status` 整方法，含 def 行 + 方法体 + return）。
- 两段一并删，删后保留 `get`（:13-23）→ `list_by_batch`（删后顺移）等其余方法不动。
- 非收口债（不涉及 P5 收口点），不涉及 raise（不属灵魂线 P4），不涉及 parity（无新旧两路）。
- 灵魂线检查：直删两个零消费方法，**不新增兜底 / 静默回退 / except 吞错 / 过度防御**，合规。
- 说明（与 planned_fix_hint 一致）：`op_code` 虽是 schema 上的 UNIQUE 业务键，但**从未接线按 code 取行**（生产全靠 `get(op_id)` 按主键取）；`list_by_status` 无人按状态批量取批次工序（生产靠 `list_by_batch` 按批次取）。两方法是「写了备用但永不接线」的死库方法。

## 字段 5：同文件碰撞债

**无**。`same_file_siblings=[]`，`co_change_within_group=[]`。`batch_operation_repo.py` 全文件仅 R36 这一债，无任何同文件兄弟债，不存在「谁先谁后 / 撞行号 / dict 键位移」协调问题。

- 删 :25-35 会使其下方法（`list_by_batch` 起）整体上移 11 行、删 :50-61 再上移 12 行——但因无同文件兄弟债，无任何其他档案引用本文件的行号，行号位移不污染任何其他债。
- 建议删除顺序：**从后往前删**（先删 :50-61 再删 :25-35），避免先删上段导致下段行号漂移。但因两段相互独立、无重叠，顺序不影响正确性，仅是操作便利。

## 字段 6：同收口点债

**无**。R36 是直删债，不收口到任何统一符号；不存在「收口到同一符号的其他债」，无顺序敏感性。
## 字段 7：收口行为差异及所需 parity 测试

**不适用**。本债为直删，非收口，无新旧两路行为对比，无需 parity 测试。

- 唯一需自证的命题是「真死、删后零影响」，由字段 8 的零引用证据 + 删后跑全测试套件（应零失败）兜底，而非 parity。

## 字段 8：爆炸半径

phase1_blast 九字段已给基线，本轮独立回盘复核如下：

**生产消费者：零。** `BatchOperationRepository` 在生产被 5 处实例化（已 rg 全仓核实）：
- `core/services/scheduler/repository_bundle.py:39`（`op_repo=BatchOperationRepository(conn, ...)`）
- `core/services/scheduler/resource_dispatch_actual_record_service.py:67`（`self.batch_operation_repo=...`）
- `core/services/scheduler/operation_execution_feedback_service.py:72`（`self.batch_operation_repo=...`）
- `core/services/scheduler/batch_service.py:34`（`self.batch_op_repo=...`）
- `tests/_scripts_e2e/benchmark_fjsp.py:383`（测试基准，`op_repo=...`）

这些实例上**实际被调的方法**（rg `batch_op_repo\.|batch_operation_repo\.` 全仓）仅：
- `get`（feedback_service:386、resource_dispatch:238）
- `list_by_batch`（batch_copy:32、batch_service:392）
- `create`（batch_copy:58、batch_template_ops:228）
- `delete_by_batch`（batch_template_ops:137）

→ **`get_by_op_code` / `list_by_status` 不在被调集合内**，删之零生产影响。

**测试消费者：零。** rg 全仓 `--type py` 含 `tests/`：两符号各仅命中自己 def 行，无任何测试调用。`contract_topology_tests` = 无（facade delegation / SP05 拓扑测试均不枚举这两方法）。

**动态分发排查：** rg `getattr.*op_code|getattr.*by_status` 命中 20+ 处，全部是 `getattr(op, "op_code", ...)`——读取**操作对象的 op_code 属性**（用于日志 / 错误信息 / input builder），与 repo 方法 `get_by_op_code` 无关；无任何 `getattr(repo, "get_by_op_code")` 式动态调用。

**`__init__` 导出排查：** `data/repositories/__init__.py:74 __all__` 导出的是 `BatchOperationRepository` 类，非方法名；删方法不破坏导出契约。

**灾难链：** `fix_invalidation_risk=none`。最坏情形——若未来有人想按 op_code 业务键取行 / 按状态批量查，需重新加方法；但当前零引用，删除不会静默炸任何东西。直删风险等级：**zero**（本桶最早可落之一）。

## 字段 9：承重毗邻标记

- 本文件 `load_bearing=false`，`lb_no_touch=null`，`lb_colocation_danger=none`（batch_operation_repo.py 非承重文件）。
- rg `load_bearing|lb_no_touch|我是故意的|承重` on 文件 → 零命中，文件内无任何 LB 标记 / 故意注释。
- **无禁区行**。可放心直删。
## 字段 10：分层 / 导入环风险

- **零风险**。直删两方法不新增任何 import；文件现有 import 仅 `core.models.BatchOperation`（:5）+ `.base_repo.BaseRepository`（:7），删方法后这两个 import 仍被其余方法（`get`/`create` 等）使用，无需动 import。
- 不引入跨层 import，不造导入环。分层红线（0 AST 违规，core.algorithms→core.services、core.models→core.services 不存在）不受影响——本债在 data 层，删方法纯收缩，不新增边。

## 字段 11：测试耦合

- **无需迁测试**。`test_consumers=无`（已 rg 证实零引用），无任何测试断言这两方法存在或其行为。
- 动手前无前置测试迁移；动手后跑全套测试（`tests/`）应保持零失败即为自证。

## 字段 12：前置依赖

- **无前置债、无前置 F 门**。`planned_deps_hint=无`，`co_change_within_group=[]`，簇 ISOLATED，可任意排期。
- `planned_batch_hint`：Batch-13，纯死无跨边，本桶最早可落之一，零风险，优先级 low。
- 可独立执行，不阻塞 / 不被阻塞于任何其他 R 债。

## 对抗核验（第二双眼睛）

> 核验日期 2026-06-05｜只读不改｜默认怀疑，找不到反证才 PASS。低危纯死叶子债（severity=low、承重=false、owner_pending=false），走快核：行号 + 直删真零引用 + 测试耦合。

**重 grep 命令与结果：**

1. `rg -n "def get_by_op_code|def list_by_status" data/repositories/batch_operation_repo.py`
   → `:25 def get_by_op_code`、`:50 def list_by_status`。**行号与档案一致，零漂移。** Read 全文复核：`get_by_op_code` 体 `:25-35`（return 在 :35）、`list_by_status` 体 `:50-61`（return 在 :61），方法体范围与档案逐字吻合。
2. `git diff b08162cd -- data/repositories/batch_operation_repo.py` → 空。**文件 UNCHANGED 确认。**
3. `rg -n --type py "get_by_op_code|list_by_status"` → 全仓仅命中两条 def 行自身（`:25`、`:50`），**零非 def 引用**（含 tests/、模板、动态调用）。
4. 动态分发反例排查 `rg -n "getattr.*op_repo|getattr.*operation_repo|getattr.*batch_op|\"get_by_op_code\"|'get_by_op_code'|\"list_by_status\"|'list_by_status'"` → 仅 2 处 `getattr(batch_op, "op_code"/"batch_id"/"seq", ...)`（resource_dispatch_actual_record_service.py:242-243），读的是 **BatchOperation 对象属性**（用于工序名/日志），**与 repo 方法 `get_by_op_code` 无关**；无任何 `getattr(repo, "get_by_op_code")` 式拼方法名调用。档案字段 8 论断坐实。
5. 非 py 引用排查 `rg -n "get_by_op_code|list_by_status" -g '!*.py' -g '!docs/_panorama_data/**'` → **零命中**（模板/前端/json 均无）。
6. 整文件非死反证 `rg -n "\.list_by_batch\(|\.delete_by_batch\(|\.get\("` → 同文件 `list_by_batch`（batch_service:392）、`get`（:119/:170）确有生产调用 → 仅死这两个查询方法，非整文件死；直删后文件存活、`core.models.BatchOperation` + `.base_repo` import 仍被 `get`/`create` 等占用，字段 10「不动 import」成立。

**承重 / owner_pending 核查：** `load_bearing=false`、`lb_no_touch=null`、`owner_pending=false`。文件内 `rg "load_bearing|lb_no_touch|我是故意的|承重"` 零命中，无 LB 标记 / 故意注释 → 非承重，无禁区行，给终态修法合规。修法是纯直删两个零消费方法，未混入统一/透传/加形参，**无承重误删风险**。

**灵魂线核查：** 直删不新增兜底/静默回退/except 吞错/过度防御，不属 P4，不涉 raise，合规。

**结论：PASS。** 行号一致、文件未动、两符号全仓（含测试/动态/非 py）零非 def 引用、整文件非死、非承重无禁区。直删 :50-61 与 :25-35（从后往前）零生产影响，删后跑全测试套件自证即可。无争议，无需第三方裁。
