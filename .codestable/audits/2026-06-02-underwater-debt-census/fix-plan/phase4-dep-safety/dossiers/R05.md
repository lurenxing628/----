# R05 逐债档案 — 派工 scope_type 归一与 operator/machine→列映射绕开 ScheduleResourceFilter 收口点(双轨漂移)

- bucket: B07 | partition: scheduler-dispatch | severity: medium | pathology: P5
- load_bearing: false（但 verdict=load_bearing，refuted=False）| owner_pending: **true**
- cluster: C01 | same_file_siblings: []（registry 内）
- 主文件: core/services/scheduler/resource_dispatch_service.py
- 协同文件: data/repositories/schedule_plan_query_repo.py（收口未达点）/ core/models/schedule_resource_filter.py（收口点）

> 本档只读不改、只产计划。owner_pending=true：以下「修法类」给方向与禁区，**不给终态修法、不分配执行批次**，待 owner 裁断。

## 1) 当前真实 file:line（grep 回盘）+ 漂移记录

回盘均按符号名 rg/grep 命中，不照抄 old_location。

| 符号 | old_location_DONT_TRUST | 当前真实 file:line | 漂移 |
|---|---|---|---|
| `_normalize_scope_type`（服务私写第 N 套校验） | resource_dispatch_service.py:63 | `core/services/scheduler/resource_dispatch_service.py:59-63`（def 在 :59，raise 在 :62，return :63） | **无**（registry location_status 已标「:63 准、行号已变」；def 头实为 :59，本档以 :59-63 为准） |
| `_normalize_scope_type` 消费点 A | 报告 :284 | `resource_dispatch_service.py:269` | **有，-15 行** |
| `_normalize_scope_type` 消费点 B（get_dispatch_payload 内） | 报告 :368 | `resource_dispatch_service.py:353`（def get_dispatch_payload 在 :335） | **有，-15 行** |
| 收口点 `SUPPORTED_SCHEDULE_RESOURCE_TYPES` | — | `core/models/schedule_resource_filter.py:8`（={"machine","operator"}，**无 team**） | 无 |
| 收口点 `ScheduleResourceFilter.column_name` | — | `schedule_resource_filter.py:19-24`（machine→machine_id / operator→operator_id / 其余→""，team 返空串） | 无 |
| 收口点 `normalize_schedule_resource_filter`「类型有 / id 空 → raise」 | collar :65-70 | `schedule_resource_filter.py:65-70`（`if resource_type_text and not resource_id_text: raise`） | **无**（精确命中，证「类型有 id 空被强制 raise」属实） |
| repo 内联映射 `list_dispatch_rows` | schedule_plan_query_repo.py:447-463 | `data/repositories/schedule_plan_query_repo.py:430-469`（方法体）；operator/machine 内联展开在 :451-460、team 双 join 在 :461-463 | **有**：方法体由报告 447-463 → 实盘 430-469；team 双 join 实在 :461-463 |
| repo 服务层调用点 | 报告 :431 | `resource_dispatch_service.py:409`（`self.plan_query_service.list_plan_dispatch_rows_for_resolution(`） | **有**：报告 :424/:431 漂到 :409（注：65870e47 后服务文件 -22/+7 重构，调用点上移） |
| 另一轨 column_name 收口点 | — | `data/repositories/schedule_resource_sql_filters.py:36`（`TRIM(COALESCE(s.{resource_filter.column_name}, '')) = ?`）+ `schedule_plan_query_repo.py:396/:403` | 无 |

**回盘补记（registry 未载、本轮新发现）**：服务文件已被 `65870e47 Implement resource dispatch execution lane` 重构（`git diff b08162cd` = -22/+7），故 collar/repo 两文件仍 byte-identical 基线、但服务文件已变。重构**新增** `_normalize_team_axis`（resource_dispatch_service.py:65-69，校验 team 轴 ∈{operator,machine}），与 `_normalize_scope_type` 紧邻——这是双轨漂移的**第四套本地校验苗头**，但不在 R05 收口范围（team_axis 是「班组下按人/按机展开」的展示轴，非资源类型轴），仅作毗邻提示，不并入修法。

## 2) 当前状态 + 与 status evidence 一致性

- registry status_2026_06_05 = **planned**；current_evidence 称「步1扩接口/步2负向回归/步3字面量收敛全未落地」。
- 回盘**一致**：collar :8 仍无 team、:19-24 column_name 仍单列、:65-70 仍对空 id raise；repo :451-463 仍内联 operator/machine/team 三分支；服务 :59-63 `_normalize_scope_type` 仍本地校验 `{operator,machine,team}` 未委托收口点。
- 唯一与 evidence 文字略偏：evidence 写 collar/repo「均 git diff b08162cd 为空」——回盘确认这两文件确为空 diff；但**服务文件已非空 diff**（evidence 把服务行号视为静态，本档已按真实 :59/:269/:353 修正）。结论：**状态仍是 planned，修法 0 落地**，与 evidence 实质一致，仅行号需以本档回盘为准。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P5 双轨漂移**（verdict=load_bearing，refuted=False）。资源类型→列名**已有**收口点（`column_name` + `normalize_schedule_resource_filter`，用于超期/明细轨：`schedule_resource_sql_filters.py:36` + `schedule_plan_query_repo.py:396`）。但**派工读取链**私写第 N 套：
  1. 服务层 `_normalize_scope_type`（resource_dispatch_service.py:59-63）自校验 `{operator,machine,team}`，未委托收口点；
  2. repo `list_dispatch_rows`（schedule_plan_query_repo.py:451-463）内联 operator/machine 列字面量 + team 双 join。
- 严重度 **medium**：双轨各写一套，改一处忘另一处即口径漂移；但有 smoke（team 活用例）+ 续命测试守护，未到 high。
- 桶 **B07**（资源筛选 collar 域）；簇 **C01**。
- **关键定性**：collar `column_name` 是「单列属性」，**表达不了** team 双 join（`((o.team_id=?) OR (m.team_id=?))`）与「类型有 / id 空 = 全量」两条真实第三轴语义——属「**先扩收口点、再谈统一；严禁反向把现状塞进现有收口点**」，**非可裸删**。

## 4) 修法类（owner_pending：暂不给终态修法）

**owner_pending=true → 待 owner 裁断，本档暂不给终态修法、暂不分配执行批次**；以下只给方向与禁区。

- 修法类 = **「需先扩收口点」(precondition_to_change) → 后收敛字面量 + `_normalize_scope_type`**。三原子小步（顺序不可换）：
  - **步1 扩收口点**：给 `ScheduleResourceFilter` / `normalize_schedule_resource_filter` 加 **team 轴表达力**。`column_name` 单列不够，需新增可产出 `((o.team_id=?) OR (m.team_id=?))` 双 join 谓词 + 参数序列的接口（如 `where_predicate(scope_type, scope_id) → (sql_fragment, params)`）；并**放开「类型有 / id 空 = 全量」语义**（当前 collar :65-70 对空 id 强制 raise，会打断派工「全部人员 / 全部设备」视图，smoke 活用例为证）。空 id 全量分支必须仍产出 `TRIM(COALESCE(...,''))<>''`（保留 repo :454-455/:459-460 现状语义）。
  - **步2 补负向回归**：断言 scope_type=team 只返回该 team、空 id 全量视图可查（守住 :461-463 + :454-460 语义不丢）。
  - **步3 才收敛**：把 repo `list_dispatch_rows` 字面量 + 服务 `_normalize_scope_type` 收敛到扩容后的收口点。
- **P5 收口点（必为已存在点）**：`core/models/schedule_resource_filter.py` 的 `ScheduleResourceFilter` / `normalize_schedule_resource_filter`（已存在，非新建）。**不批准为派工另起第二收口模块**（那将是新 P5）。本债不属 R09 例外（与 parse_optional_positive_int 无关）。
- **灵魂线红线**：步1「放开空 id = 全量」是**显式扩语义**，不是兜底/静默回退——必须在收口点写明「id 空 → 全量视图（故意，派工『全部人员/设备』入口）」中文注释，**不得**用 except 吞错或默认空串静默放行。team 双 join 收敛时若 scope_id 缺失，应**显式走全量分支或 loud raise**，禁止静默返全量坏数据。

## 5) 同文件碰撞债

- **registry same_file_siblings = []**（R05 自身在 registry 内无同文件兄弟）。
- 但 interference_edges 标 **R34**（`same_file:true`，`same_symbol:false`）——同在 `data/repositories/schedule_plan_query_repo.py`？回盘**纠正**：R34 实体方法 `list_dispatch_rows_with_resource_context` 在 **`data/repositories/schedule_repo.py:128-158`**（不在 schedule_plan_query_repo.py）。registry 的 `why:["file:schedule_plan_query_repo.py"]` 与 location 略偏；二者**真正的撞点不是同文件，而是同构 SQL 字面量**（见 §6）。
- 因此 R05 与 R34 **不会撞同一段行号 / 不会引起同文件 dict 键位移**（分属两个 repo 文件）。同文件 R05 改的是 `schedule_plan_query_repo.py:451-463`，与 R34 改的 `schedule_repo.py:151-152` 物理隔离，**无行号碰撞**。

## 6) 同收口点债 + 顺序敏感性

- **共目标收口点债（强前置·跨桶硬边）= R34（B12）**。R34 的 `schedule_repo.py:128-158` 含与本债 **byte-identical 的 team 双 join**：回盘命中 `schedule_repo.py:151`→`((o.team_id = ?) OR (m.team_id = ?))`，:152 `params.extend([scope_id_text, scope_id_text])`，以及 :141-150 同款 operator/machine 内联四分支。
- **顺序敏感（不可逆）**：R05 必须**先**给 collar 扩出 team 双 join 表达力；R34 才能把它的内联 SQL 收敛到同一收口点而**不丢 team 谓词**。
  - 若 **R34 先收敛、R05 未扩 team** → R34 收敛到只有 `column_name` 的收口点 → team 轴谓词凭空消失 → 班组视角**静默返回全量坏数据**，且连带使 R05 后续修法失效。
- **同桶 B07**：与 **R67** 同桶、`co_change_within_group` 列 R67，但**不同收口点、零冲突**（registry 明示）。R67 只需同批次共改，无顺序约束。

## 7) 收口行为差异 + 所需 parity 测试

收口（步3）后，「新路（委托扩容后的 collar）」与「旧路（repo/服务内联）」须对边界值**逐分支等价**，否则即坏数据。逐分支反例：

| 输入 (scope_type, scope_id) | 旧路（现状 repo :451-463 / 服务 :59-63） | 新路（若直塞现状 collar，未扩 team） | 反例风险 |
|---|---|---|---|
| `("operator", "OP1")` | `TRIM(COALESCE(s.operator_id,''))=? ['OP1']` | column_name=operator_id 等价 | 等价 ✅ |
| `("operator", "")` | 全量分支 `<> ''`（:454-455） | collar :65-70 **raise ValidationError**（类型有 id 空） | **None/全量 vs raise**——整页 500，反例 ① |
| `("team", "TEAM-OP")` | team 双 join（:461-463） | collar column_name **返空串**（:24）→ 无谓词 | **全量坏数据**，反例 ② |
| `("team", "")` | 现状**无对应分支**（:461 要求 scope_id），落空 = 全量 | collar 同 raise 或空串 | 行为漂移，反例 ③ |
| `("bad", *)` | 服务 :62 raise；repo 无分支落全量 | collar :59-63 unsupported raise | 等价 ✅（均 raise） |
- **所需 parity 测试（步1/步2 必落）**：
  1. `scope_type=team & scope_id=TEAM-OP` → 仅返回该 team 行（守 :461-463 双 join），对照 smoke `test_scheduler_resource_dispatch_smoke.py:177/190/192/194`。
  2. `scope_type=operator & scope_id=""` → 返回全量（守 :454-455），断言**不 raise**（反例①的回归钉）。
  3. `scope_type=machine & scope_id=""` → 全量（守 :459-460）。
  4. `scope_type=team & scope_id=""` → 明确定义为「全量 or loud raise」其一并断言（消除反例③的未定义行为，由 owner 裁断取哪种）。
  5. `scope_type=bad` → ValidationError（守 `regression_scheduler_resource_dispatch_invalid_query_cleanup.py:340`）。

## 8) 爆炸半径（生产/测试消费者 + 灾难链）

**生产消费者（回盘核实行号）**：
- `core/services/scheduler/resource_dispatch_service.py:269`（调 `_normalize_scope_type`，registry 报 :284，**-15 漂移**）
- `core/services/scheduler/resource_dispatch_service.py:353`（get_dispatch_payload@:335 内调，registry 报 :368，**-15 漂移**）
- `core/services/scheduler/resource_dispatch_execution_service.py:76`（经 `self.dispatch_service._normalize_scope_type`）
- `core/services/scheduler/schedule_plan_query_service.py:399`（`self.repo.list_dispatch_rows(`，调 repo 内联映射；其 def 在 :387，:376 经 facade 转调）
- `core/services/scheduler/resource_dispatch_service.py:409` + `resource_dispatch_execution_service.py:106,144`（调 `list_plan_dispatch_rows_for_resolution`）
- 另一轨 column_name 收口点（不可同时被破坏）：`data/repositories/schedule_resource_sql_filters.py:36` + `schedule_plan_query_repo.py:396/:403`

**测试消费者**：
- `tests/resource_dispatch/test_scheduler_resource_dispatch_invalid_query_cleanup.py:340`（直接调 `svc._normalize_scope_type("bad")`——删/改名即红的续命测试）
- `tests/resource_dispatch/test_scheduler_resource_dispatch_smoke.py:177,190,192,194`（scope_type=team & TEAM-OP 活用例：班组轴 / cross_team_rows=1 / operator_rows=1）
- `tests/scheduler_analysis/test_report_context_filters_contract.py`（collar 别名优先级契约，扩 collar 会波及）
- `tests/schedule/service/test_schedule_service_facade_delegation.py:39`（守兄弟方法 `list_dispatch_rows_with_resource_context`=R34 的 facade 委派返回类型——扩 collar 旁敲此契约）

**三入口下游**：`/scheduler/resource-dispatch` 页面 + `/data` JSON（operator_rows / machine_rows / cross_team_rows）+ `/export`。

**灾难链**：
1. **统一漏 team**（最危险）：把 `_normalize_scope_type` 收敛到只认 column_name 的 collar → team 双 join 谓词消失 → 班组视角**静默返全量坏数据**（无报错，最难发现）或整页 500。
2. **删/改 `_normalize_scope_type` 名**：`regression_..._invalid_query_cleanup.py:340` 直红 + execution_service:76 链断。
3. **强塞空 id 进现状 collar**：collar :65-70 raise → 「全部人员/全部设备」全量视图整页 500（smoke 失败）。

## 9) 承重毗邻标记 + 禁区行

- 收口点文件 `schedule_resource_filter.py` **不在** PHASE0 §5 承重名单，但本债 **verdict=load_bearing（refuted=False）**。
- **禁区行（lb_no_touch，绝不删/改/透传）**：
  - `data/repositories/schedule_plan_query_repo.py:461-463`——team 双 join `((o.team_id = ?) OR (m.team_id = ?))` + `params.extend([scope_id_text, scope_id_text])`。column_name 单列**表达不了**，塌缩 = 班组静默失效。
  - `schedule_plan_query_repo.py:454-455` / `:459-460`——空 id 全量分支 `TRIM(COALESCE(s.operator_id,''))<>''` / `...machine_id...<>''`。塌缩 = 团队·全量视图整页 500。
  - 这三段是 column_name 表达不了的**承重第三轴**；任何修法**只能先扩收口点把它们搬过去并保语义，绝不可裸删/裸合并**。
- 毗邻提示（非禁区、非本债）：`_normalize_team_axis`（resource_dispatch_service.py:65-69）是 65870e47 新增的第四套本地校验苗头，紧邻 `_normalize_scope_type`，收敛时**勿误伤**（它管 team_axis 展示轴，不在 R05 收口范围）。

## 10) 分层 / 导入环风险

- 收口点在 `core/models/`，消费者在 `core/services/scheduler/` 与 `data/repositories/`。`core.services → core.models`、`data → core.models` 均为**已存在合法方向**，扩 collar 接口不引入新边。
- **AST 红线 0 违规要求**：步1 给 collar 加「产出 SQL 谓词字符串 + 参数」接口时，**谓词须为纯字符串/参数序列**，不得让 `core.models` 反向 import `data.repositories` 的 SQL builder（否则 model→data 越层 + 潜在导入环）。collar 只产 fragment 文本，由 repo 拼装——保持 model 不依赖 data。
- 无 `core.algorithms→core.services` / `core.models→core.services` 风险（本债不涉这两层）。



## 11) 测试耦合（动手前先迁哪些）

动手前需先处理/对齐的测试（按收敛步骤）：
- **步1 前先迁**：`tests/resource_dispatch/test_scheduler_resource_dispatch_smoke.py:177-194`——team 全量/cross_team 活用例。扩 collar「类型有 id 空=全量」前，需先把此 smoke 升级为对收口点的断言（防 :65-70 raise 改动后 smoke 假绿）。
- **步2 落地**：新增 §7 列的 5 条 parity 回归（team-only / operator-空-全量 / machine-空-全量 / team-空-未定义裁断 / bad-raise）。
- **步3 收敛时同步迁**：`tests/resource_dispatch/test_scheduler_resource_dispatch_invalid_query_cleanup.py:340`——若 `_normalize_scope_type` 收敛/改委托，此直调点须改为调收口点或保留薄 shim。
- **旁敲必跑**：`tests/schedule/service/test_schedule_service_facade_delegation.py:39`（R34 facade 返回类型契约）+ `tests/scheduler_analysis/test_report_context_filters_contract.py`（collar 别名契约）——扩 collar 后两者须仍绿，作为「没碰坏邻居」的护栏。
- `tests/schedule/service/test_schedule_repository_detail_queries.py`（R34 实体方法的 repo 测试）——R34 收敛阶段才迁，R05 阶段只确认其当前绿，不动。

## 12) 前置依赖（先做哪些债 / F 门）

- **R05 内部顺序（不可换）**：步1 扩收口点 → 步2 补回归 → 步3 才收敛字面量 + `_normalize_scope_type`。
- **跨桶硬边（全局排序）**：**R05（Batch-8 / B07）必须先于 R34（Batch-13 / B12）**。理由见 §6：R05 先扩 collar 的 team 双 join 表达力，R34 才能安全收敛其 `schedule_repo.py:151-152` 的同构 team 双 join；反序 = team 谓词静默丢失。
- **同桶同改（无序）**：R67（B07，co_change 组）同批共改，但不同收口点、零冲突，无前后约束。
- **F 门（owner 裁断点，owner_pending=true）**：
  - F-决策①：collar `column_name`(单列) 之上新增 team 双 join 谓词接口的**形态**（`where_predicate→(sql, params)` vs 扩 `ScheduleResourceFilter` 字段）——待 owner 拍板，本档不定终态。
  - F-决策②：`scope_type=team & scope_id=""`（§7 反例③）的语义——**全量** 还是 **loud raise**，须 owner 选定后才能写 parity 断言。
- **结论**：本条 owner_pending=true → **暂不分配执行批次、暂不给终态修法**；依赖/爆炸半径已分析透，待 owner 裁断 F-决策①② 后方可进 Phase2 落地。
