# R66 逐债档案 — _context_summary 死函数(reports_workbench 私有副本从未调用)

> 本档案只读不改、只产计划。R66:B01 / C01 / P6 / severity=low / load_bearing=false / owner_pending=false。
> 同文件兄弟: R54。主文件: web/viewmodels/scheduler_reports_workbench.py。

## 1) 当前真实 file:line(回盘)+ 漂移记录

按符号 `def _context_summary(context: Dict[str, Any], suffix: str = "")` 在当前工作区 rg 回盘:

- **死函数(R66 本体)**: `web/viewmodels/scheduler_reports_workbench.py:150`(def 行),完整函数体 `150-163`(150 def → 163 `return`),后接两空行 164-165。证据:rg `_context_summary scheduler_reports_workbench.py` → 仅 :150 一处命中,无任何调用点。
- **LIVE 同名异签函数(绝不可误删)**: `web/viewmodels/scheduler_workbench_links.py:258` `def _context_summary(context, target_page, view=None)`,在 `:448` `build_workbench_link` 内被实际调用。签名特征区分:R66 版独有 `suffix` 形参;LIVE 版是 `target_page/view`。

**漂移记录(三处口径打架,以实盘为准)**:
| 来源 | 行号口径 | 实盘 | 漂移 |
|---|---|---|---|
| old_location_DONT_TRUST | 141-154 | 150-163 | +9 行 |
| current_evidence_2026_06_05 | 150-162 (def) | 150-163 | def 行准,尾行 -1(漏算 `return` 行) |
| phase1_blast.location_status | 第144行 / 144-157 | 150 / 150-163 | **-6 行(blast 字段陈旧,误)** |

→ 漂移**有**,幅度 +6~+9 行。registry 内部 blast 字段(144)与今日 evidence(150)自相矛盾;**实盘以 rg 符号定位的 :150 为唯一真相**。修法严禁照任何裸行号删。

## 2) 当前状态 + 与 status evidence 是否一致

状态 = `planned`(未执行)。一致。死函数原样在 :150-163,计划"纯删"(Batch-3)尚未落地。evidence "本文件零调用(仅 :150 定义)" 经独立回盘核实属实(rg 全仓 core/web/data/tests 对 `scheduler_reports_workbench._context_summary` 零引用)。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6**:reports_workbench 私有副本死函数,从未接线(copy-paste 自 workbench_links 的同名函数,但本文件改用 `suffix` 形参后从未被调)。
- 严重度 **low**:纯死代码,无运行期/契约影响。
- 桶 **B01**(home=B01,structural)。簇 **C01**。
- 承重 false,owner_pending false,needs_adversarial false,adv_refuted false。

## 4) 修法类

**纯直删(P6 死代码消除)** — 6 条结构债中风险最低。

- 删除范围:`scheduler_reports_workbench.py:150-163` 整个死 `_context_summary`(含 def 行到 `return` 行)。可一并清掉前置两空行之一(150 上方 148-149 两空行)以保持 PEP8 间距,但不强求。
- **红线 4 已满足**:删前已确认 prod+test 零消费方(§8 证据)。
- **定位铁律**:必须按完整签名 `def _context_summary(context: Dict[str, Any], suffix: str = "")` 定位 —— `suffix` 形参是 reports_workbench 版独有特征。**严禁照报告 141-154 或 blast 字段 144-157 裸行号删**(文件已漂)。
- **误删红线**:绝不动 `scheduler_workbench_links.py:258` 那个签名为 `(context, target_page, view=None)`、在 :448 被调用的 LIVE 同名函数。
- 非承重(load_bearing=false,lb_no_touch=null),无需"我是故意的"注释、无需绑契约/parity 测试(删的是无人调用的死码,无新旧两路可比)。
- owner_pending=false → 给终态修法(纯删)。

## 5) 同文件碰撞债(scheduler_reports_workbench.py)

唯一同文件兄弟 = **R54**(B01,P5 收口,planned)。

- **R54 改哪段**:`_copy_plan_guard_fields`(本文件 `:36`,函数体 36-55)的 13 键手维 mapping,收口/delegate 到 `build_workbench_plan_context`(scheduler_workbench_links.py)。R54 的编辑作用在本文件 **第 36-55 行区间**(R66 死函数 :150 的上方)。
- **R66 改哪段**:删 `:150-163`。
- **谁先谁后**:`planned_batch_hint` 定 R54(Batch-2)先落、R66(Batch-3)后删。
- **会否撞行号**:**会** —— R54 修改/缩减 36-55 区间会让 :150 整体上移或下移。因此 **R66 删除点必须在 R54 落地后用 `def _context_summary(...suffix...)` 符号重新定位**,严禁沿用本档记录的 :150(那是 R54 落地前的快照)。或两者同提交。两者改的是不同符号(`_copy_plan_guard_fields` vs `_context_summary`),无逻辑耦合,纯行号偏移风险。
- 其余同文件 interference_edges(LB02、LB06)在本文件,但 R66 是纯删孤立死函数,与它们不共享符号、不共享行(详见 §9)。

## 6) 同收口点债

R66 是**纯删**,不收口到任何符号,故**无同收口点债**。

注:R54 收口到 `build_workbench_plan_context`(scheduler_workbench_links.py:183-251)/ 真相源 `plan_role_filter_fields`(schedule_result_view_context.py:277),与 R66 删除的死函数不在同一符号、不交叉。R66 不参与任何收口点的顺序敏感性。

## 7) 收口行为差异及所需 parity 测试

不适用。R66 是纯删死函数,**无收口**(无新旧两路、无边界值分支等价问题)。无 parity 测试需求。
唯一需要的是删前再确认零消费(已在 §8 给出 rg 证据),删后跑一次全量回归确认无 ImportError/NameError。

## 8) 爆炸半径

**生产消费者**:**(无)**。
- rg `_context_summary` 全仓 → reports_workbench 内仅 :150 定义,零调用。
- 模块导入方 `web/routes/reports_page_support.py:26-38` 的 `from ... import (...)` 名单逐项核:`ReportPresentationValueError / build_report_context / build_report_limitations / build_report_page_links / build_reports_index_workbench / decorate_delay_diagnosis_context / decorate_downtime_rows / decorate_execution_review_rows / decorate_overdue_rows / decorate_utilization_rows / downtime_empty_message / downtime_summary` —— **不含 `_context_summary`**。
- `__all__`(:431-444)同样**不含 `_context_summary`**(私有 `_` 前缀)。

**测试消费者**:**(无)**。
- `tests/schedule/route_view/test_scheduler_historical_plan_label_contract.py:10` 仅 import `build_report_context`。
- `tests/web_pages/test_web_silent_fallback_contract.py:229` `import ... as reports_vm` 仅用 `decorate_utilization_rows`/`ReportPresentationValueError`(:231-235);`:314` 仅 `build_report_context, decorate_execution_review_rows`。**均不触 `_context_summary`**。

**下游效应**:纯死代码,删除无任何运行期/契约影响。

**灾难链(删错/统一错会静默炸什么)**:
- 唯一真实灾难 = **误删 LIVE 同名函数** `scheduler_workbench_links.py:258`(被 :448 `build_workbench_link` 调用)。若按裸行号/IDE 全局 rename 误删它,会让所有 workbench link 卡片的 `context_summary` 文案静默丢失或 `NameError`。**防护:严格按 `suffix` 形参签名定位本文件 :150,绝不跨文件批量操作。**
- 次要风险:R54 先落后行号漂移,若仍按 :150 删 → 可能删到错误行(R54 把代码上推后 :150 已不是该函数)。**防护:删除前实时 rg 重定位。**

## 9) 承重毗邻标记

- **本文件无 LB**(load_bearing=false,lb_no_touch=null;blast.lb_colocation_danger 确认"本文件无 LB")。
- interference_edges 中 LB02、LB06 标 `same_file=true`,但 `same_symbol=false` —— 它们与 R66 死函数(:150-163)不共享符号、不共享行。R66 删除范围 150-163 是孤立死函数,**禁区行 = 无**(本文件无承重红线落在 R66 删除窗口内)。
- 跨文件兄弟 R42 在 `scheduler_workbench_links.py`,与 R66 不共改(R66 不动该文件,仅"读它确认 LIVE 函数别误删")。

## 10) 分层 / 导入环风险

- R66 是**减法**(删一个本文件内部死函数),不新增任何 import。**0 越层风险、0 导入环风险**。
- 不引入 core.algorithms→core.services / core.models→core.services 等违规(本债在 web/viewmodels 层,且不加 import)。
- AST 分层检查:删除后本文件 import 集合不变,F 门 0 AST 违规不受影响。

## 11) 测试耦合

- 动手前**无需迁任何测试**(零测试引用 `_context_summary`,§8 已证)。
- 删除后建议跑:`tests/web_pages/test_web_silent_fallback_contract.py`、`tests/schedule/route_view/test_scheduler_historical_plan_label_contract.py`(两个 import 本模块的测试)做冒烟,确认无 ImportError/NameError。无需新增测试。

## 12) 前置依赖

- **同文件 R54 先落(Batch-2)→ R66 后删(Batch-3)**,或两者同提交。理由:R54 改 :36-55 会漂移 R66 的 :150;R66 删除点必须在 R54 落地后用符号重新定位(§5)。
- **F 门**:无特殊门(纯死代码删除,无契约/parity/AST 新增)。常规删后跑 §11 两个回归即可。
- 本债不阻塞任何其他债;不被 R54 之外的任何债阻塞。owner_pending=false,可直接进 Batch-3 执行。

## 对抗核验(第二双眼睛)

**结论: PASS**(低危纯死叶子债,快核 + 误删红线深核)。独立重 grep 完全坐实档案,未找到反证。

重跑命令与结果:
1. `rg -n '_context_summary' web/viewmodels/scheduler_reports_workbench.py` → **仅 :150 一处**(def 行),零调用。坐实"本文件零消费"。
2. `rg -n '_context_summary' web/viewmodels/scheduler_workbench_links.py` → **:258 def(签名 `(context, target_page, view=None)`)+ :448 调用点**。坐实 LIVE 同名异签函数真实存在且被调,签名特征(`target_page/view` vs R66 版独有 `suffix`)可区分,误删红线成立。
3. `rg -n '_context_summary' core/ web/ data/ tests/` → 全仓仅上述 3 处命中,**core/data/tests 零引用**。坐实生产+测试零消费(§8 属实)。
4. `Read scheduler_reports_workbench.py:146-167` → 死函数体精确 **:150(def)→ :163(return)**,后接两空行 164-165,:166 为下一函数 `build_downtime_report_link`。**删除范围 150-163 准确**,无截断/越界。
5. `rg -n '__all__'` + 区间核 → __all__(:431起)**不含** `_context_summary`(私有 `_` 前缀)。
6. `rg -n '_context_summary' web/routes/reports_page_support.py` → **零命中**,导入方未导出该符号。
7. `rg -n 'def _copy_plan_guard_fields'`(R54 目标符号)→ **:36**,确在 R66 死函数 :150 **上方**;R54 落地后 :150 会下移,故"删 R66 前按 `suffix` 签名符号重定位"判断正确,且二者改不同符号无逻辑耦合(纯行号偏移)。

逐项挑战结果:
- **真实行号**: 一致。实盘 :150-163,与档案 §1 完全吻合。registry `old_location`(141-154)、`phase1_blast.location_status`(144)、`planned_fix_hint`(:254 LIVE)均为陈旧/误值——档案已正确标红"三处口径打架,以 rg 符号为唯一真相"并采用实盘 :150,核验认同。
- **消费者清单**: 一致。prod/test/__all__/导入方四路独立回盘全零,无遗漏调用点。
- **承重判断**: 认同 load_bearing=false / lb_no_touch=null。本文件无 LB(blast 字段确认),死函数 :150-163 孤立、不与任何 LB 共享符号或行,禁区行=无。
- **分层/导入环**: 认同。纯减法删本文件内部死函数,不新增 import,0 越层 / 0 导入环。
- **收口点**: 认同"无收口"。R66 纯删不收口到任何符号,不参与 P5 收口顺序敏感性。无 DRY 静默引爆面。
- **owner_pending**: =false,档案给终态修法(纯删)+ 批次(Batch-3),符合规则,无擅自越权。

**承重误删风险: 无**(本债非承重;档案修法为纯直删,未混入统一/透传/加形参/合并;唯一外部误删风险=跨文件误删 workbench_links:258 LIVE 函数,档案已用 `suffix` 签名定位 + 禁止跨文件批量操作充分防护)。

**需升级第三方裁: 否。**
