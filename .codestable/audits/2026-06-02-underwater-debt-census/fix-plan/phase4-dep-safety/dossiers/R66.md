# R66 逐债档案 — _context_summary 死函数(reports_workbench 私有副本从未调用)

> **2026-06-09 B 执行终态：fixed。** R66:B01 / C01 / P6 / severity=low / load_bearing=false / owner_pending=false。
> 同文件兄弟: R54。主文件: web/viewmodels/scheduler_reports_workbench.py。

## 1) 当前真实 file:line + 漂移记录

执行前按符号 `def _context_summary(context: Dict[str, Any], suffix: str = "")` 重 rg 回盘:

- **死函数(R66 本体)**: 删除前位于 `web/viewmodels/scheduler_reports_workbench.py:139-152`。该函数只在本文件有 def,无任何调用点;本次已删除。
- **删后证据**: `rg -n "_context_summary" web/viewmodels/scheduler_reports_workbench.py` 零命中。
- **LIVE 同名异签函数(绝不可误删)**: `web/viewmodels/scheduler_workbench_links.py:210` `def _context_summary(context, target_page, view=None)`,仍在 `:399` 被 `build_workbench_link` 实际调用。签名特征区分:R66 版独有 `suffix` 形参;LIVE 版是 `target_page/view`。

**漂移记录(历史口径只作证据,以执行前实盘 :139-152 为准)**:

| 来源 | 行号口径 | 执行前实盘 | 处置 |
|---|---|---|---|
| old_location_DONT_TRUST | 141-154 | 139-152 | 旧行号不可信 |
| 2026-06-05 dossier | 150-163 | 139-152 | R54/G01/G02 等前序提交后再次漂移 |
| phase1_blast.location_status | 144-157 | 139-152 | 旧 blast 字段陈旧 |

## 2) 当前状态 + 与 status evidence 是否一致

状态 = `fixed`。R66/G03 已按计划纯删,删后 reports_workbench 内 `_context_summary` 零命中;生产/测试代码区仅剩 workbench_links 的 LIVE 同名异签函数 def+调用。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6**:reports_workbench 私有副本死函数,从未接线(copy-paste 自 workbench_links 的同名函数,但本文件改用 `suffix` 形参后从未被调)。
- 严重度 **low**:纯死代码,无运行期/契约影响。
- 桶 **B01**(home=B01,structural)。簇 **C01 / NAV-PLANID / G03**。
- 承重 false,owner_pending false,needs_adversarial false,adv_refuted false。

## 4) 修法类

**已执行:纯直删(P6 死代码消除)**。

- 删除范围:`scheduler_reports_workbench.py:139-152` 整个死 `_context_summary`(含 def 行到 `return` 行)。
- **红线 4 已满足**:删前已确认 prod+test 零消费方(§8 证据)。
- **定位铁律已执行**:按完整签名 `def _context_summary(context: Dict[str, Any], suffix: str = "")` 定位,未按旧报告裸行号删。
- **误删红线已守住**:未动 `scheduler_workbench_links.py:210` 那个签名为 `(context, target_page, view=None)`、在 :399 被调用的 LIVE 同名函数。
- 非承重(load_bearing=false,lb_no_touch=null),无需"我是故意的"注释、无需绑契约/parity 测试(删的是无人调用的死码,无新旧两路可比)。

## 5) 同文件碰撞债(scheduler_reports_workbench.py)

唯一同文件兄弟 = **R54**(B01,P5 收口,已 fixed)。

- R54 已先于 R66 落地,因此 R66 删除前按 `suffix` 签名重新定位到当前 :139-152。
- 两者改的是不同符号(`build_workbench_plan_context` guard 投影面 vs `_context_summary` 死函数),无逻辑耦合,仅有历史行号偏移风险。
- 其余同文件 interference_edges(LB02、LB06)与 R66 死函数不共享符号、不共享行。R66 删除窗口内无承重禁区。

## 6) 同收口点债

R66 是**纯删**,不收口到任何符号,故**无同收口点债**。

## 7) 收口行为差异及所需 parity 测试

不适用。R66 是纯删死函数,无收口、无新旧两路、无边界值分支等价问题。无需 parity 测试。

## 8) 爆炸半径

**生产消费者**:**(无)**。

- 删前 `rg _context_summary web/viewmodels/scheduler_reports_workbench.py` 仅命中 def;删后零命中。
- 模块导入方 `web/routes/reports_page_support.py` 的 import 名单不含 `_context_summary`。
- `scheduler_reports_workbench.py` 的 `__all__` 不含 `_context_summary`。

**测试消费者**:**(无)**。

- `tests/schedule/route_view/test_scheduler_historical_plan_label_contract.py` 仅 import `build_report_context`。
- `tests/web_pages/test_web_silent_fallback_contract.py` 仅消费 `decorate_utilization_rows` / `ReportPresentationValueError` / `build_report_context` / `decorate_execution_review_rows`,不触 `_context_summary`。

**下游效应**:纯死代码,删除无任何运行期/契约影响。

**灾难链防护**:

- 唯一真实灾难是误删 LIVE 同名函数 `scheduler_workbench_links.py:210`。本次只删 reports_workbench 的 `suffix` 版死函数,未跨文件批量删除。

## 9) 承重毗邻标记

- **本文件无 LB**(load_bearing=false,lb_no_touch=null)。
- interference_edges 中 LB02、LB06 标 `same_file=true`,但 `same_symbol=false`;它们与 R66 删除范围不共享符号、不共享行。
- 跨文件兄弟 R42 在 `scheduler_workbench_links.py`,与 R66 不共改;本次仅只读确认 LIVE 函数别误删。

## 10) 分层 / 导入环风险

- R66 是**减法**(删一个本文件内部死函数),不新增任何 import。**0 越层风险、0 导入环风险**。
- 不引入 core→flask、model→data、data→core 等反向 import 风险。

## 11) 测试耦合

- 无需迁移或新增测试。
- 本簇验证锚点:`tests/web_pages/test_web_silent_fallback_contract.py`、`tests/schedule/route_view/test_scheduler_historical_plan_label_contract.py`。
- 常规门禁:`ruff` + `tools/scan_py38plus_syntax.py --fail-on-hit web/viewmodels/scheduler_reports_workbench.py`。

## 12) 前置依赖

- **R54 已 fixed,R66 后删前已按符号重定位。**
- **F 门**:无特殊门(纯死代码删除,无契约/parity/AST 新增)。
- 本债不阻塞任何其他债;不被 R54 之外的任何债阻塞。owner_pending=false。

## 对抗核验(第二双眼睛)

2026-06-09 R66 recon 三路结论均为 PASS:

1. 生产调用链:reports_workbench `suffix` 版 `_context_summary` 删除前仅 def 零调用;workbench_links LIVE 同名函数在 :210/:399,不可误删。
2. 测试锚点:两个直接 import reports_workbench 的测试足以覆盖模块 import/公开函数无 NameError;R66 不需要 full gate 证明。
3. 文档/registry: R66 是 NAV-PLANID / AS-3 / G03 单债叶子;R54 前置已满足;owner_pending=false;允许范围就是纯删 :139-152。

**需升级第三方裁: 否。**
