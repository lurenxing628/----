# R49 逐债档案 — 5 处死模块别名 `_due_exclusive` / `_parse_due_date`

> 只读不改产物。bucket=B06 home=B06 cluster=C01 severity=low pathology=P6 partition=core-algorithms load_bearing=false owner_pending=false
> 主文件 dispatch_rules.py;全债跨 3 文件 dispatch_rules.py / evaluation.py / ortools_bottleneck.py
> 回盘时间 2026-06-05;工作区 git status 三文件均干净(未改)。

## 字段 1 — 当前真实 file:line(回盘)+ 漂移记录

`rg -n '_due_exclusive\s*=' / '_parse_due_date\s*='` 在 core/ 回盘,5 处死别名当前真实坐标:

| # | 符号 | 真实 file:line(2026-06-05) | old_location_DONT_TRUST | 漂移 |
|---|------|---------------------------|-------------------------|------|
| 1 | `_due_exclusive = due_exclusive` | `core/algorithms/dispatch_rules.py:25` | dispatch_rules.py:25 | **无漂移**(0 行) |
| 2 | `_parse_due_date = parse_date` | `core/algorithms/evaluation.py:40` | evaluation.py:40-41 | **无漂移** |
| 3 | `_due_exclusive = due_exclusive` | `core/algorithms/evaluation.py:41` | evaluation.py:40-41 | **无漂移** |
| 4 | `_parse_due_date = parse_date` | `core/algorithms/ortools_bottleneck.py:24` | ortools_bottleneck.py:24-25 | **无漂移** |
| 5 | `_due_exclusive = due_exclusive` | `core/algorithms/ortools_bottleneck.py:25` | ortools_bottleneck.py:24-25 | **无漂移** |

证据:`rg -n '_due_exclusive' core/` 仅命中三处赋值(:25/:41/:25)+ 1 处无关 `batch_due_exclusive = due_exclusive(...)`(evaluation.py:257,是调用本体非别名);`rg -n '_parse_due_date\b' core/` 命中两处别名赋值(:40/:24)外,另有 `sgs_scoring.py:34/:223` —— 见字段 3 同名陷阱。三别名右值来源真实存在:dispatch_rules.py:9 / evaluation.py:9 / ortools_bottleneck.py:19 均 `from ...date_parsers import due_exclusive[, parse_date]`。

**漂移结论:5/5 全部零漂移,registry 行号线索本次全准。**

## 字段 2 — 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`;本档案不推进,保持 planned。
- `current_evidence_2026_06_05` 与回盘**完全一致**:5 处死别名全在原样,三文件 git status 空(已 `git status --short` 证实)。
- evidence 中「活近亲 evaluation.py:26 `_parse_due_date_state` 仍在,未误删」回盘成立:`evaluation.py:26 def _parse_due_date_state(value)` + `:230` 被调用,是独立活函数,**不在删除清单**。

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- 病理 **P6**(机械改造残渣 / 直删类):c8a5a49c「日期解析收敛」把模块切到共享 `core/algorithms/greedy/date_parsers` 后,同提交内调用点已全改裸名(`due_exclusive(...)`/`parse_date(...)`),这 5 行私有别名是兼容垫片,**出生即零消费**。
- 严重度 **low**;桶/home **B06**;簇 **C01**(死模块别名簇)。
- ⚠ **同名陷阱(本债最大误删风险)**:`core/algorithms/greedy/dispatch/sgs_scoring.py:34 def _parse_due_date(value, *, strict_mode=False)` 是**另一个真实的本地函数**(带 strict_mode 形参,:223 被调用,且该文件 :12 自己 `from ..date_parsers import parse_date`),与本债的 `_parse_due_date = parse_date` 别名**只是重名,毫无关系**。删除时必须按 file:line 精确定点,严禁按符号名全局删 —— 否则会误铲 sgs_scoring 的活函数。同理 evaluation.py:26 `_parse_due_date_state` 重前缀但是不同符号,不动。
## 字段 4 — 修法类

**直删(纯删 5 行别名,无任何替换 / 收口 / 透传)。** 非承重(load_bearing=false, lb_no_touch=null),不走「仅注释」路径。

精确删除 5 行(按 file:line 定点,**不可按符号名全局删**,见字段 3 同名陷阱):
- `dispatch_rules.py:25` `_due_exclusive = due_exclusive`
- `evaluation.py:40` `_parse_due_date = parse_date`
- `evaluation.py:41` `_due_exclusive = due_exclusive`
- `ortools_bottleneck.py:24` `_parse_due_date = parse_date`
- `ortools_bottleneck.py:25` `_due_exclusive = due_exclusive`

import 行**保留不动**:三文件仍有真实调用 `due_exclusive(...)` / `parse_date(...)`(evaluation.py:257/:36、ortools:138/140),删别名不会让 import 变死。
非 P5 收口债(本债是删垫片不是建收口);非 P4(无静默兜底,纯死代码);不触灵魂线 —— 删的是「定义后从不被读」的死赋值,既不引入也不移除任何兜底/回退/except 行为。

## 字段 5 — 同文件碰撞债(siblings)

dispatch_rules.py 内三债同文件,回盘真实坐标:

| 债 | 桶 | 符号 / 改动段(真实行) | 与 R49 行关系 |
|----|----|----------------------|---------------|
| **R49** | B06 | 删 `:25` `_due_exclusive` 单行 | 本债 |
| **R51** | B06 | 删 `:28 def parse_dispatch_rule(...)`(+ 续命测试) | 在 R49(:25)**之下** |
| **R50** | B05 | 删 `:112 def mean_positive(...)` 整函数 + 顶部 `import statistics`(:4) | 在 R49(:25)**之下** |

碰撞分析:
- **删除方向自上而下,行号互撞规避**:R49 删 :25 会让 :28(R51)、:112(R50)整体上移,且 R50 还要动顶部 :4 `import statistics`。三债**须同批(Batch-7)在同一次编辑里处理**,以「行号快照在编辑前一次性取、删除从大行号往小行号删 / 或同 patch 一次性删」避免「删了 :25 后 :28/:112 漂移导致后续按旧行号误删」。
- **谁先谁后(同批内推荐顺序)**:R49(零风险、纯死代码、先落清场,registry hint「本桶内第 1」)→ R51(medium,删宽容解析器,需连退续命测试,灵魂线红线)→ R50(B05,需判 `import statistics` 是否仅 mean_positive 独用)。但因须同 patch,本质是「一次性原子删 3 处 + 改 1 import」,顺序仅为人工 review 心智顺序,不是运行期依赖序。
- **dict 键位移**:三债删的都是「模块级 def / 顶层赋值」,非 dict 字面量条目,**无 dict 键位移风险**。
- R49 与 R51 同桶 B06、R50 跨桶 B05,但 registry `same_file_siblings` 与 `co_change_within_group` 一致列 R50/R51,共识无冲突。

## 字段 6 — 同收口点债

**无。** 本债是「删死垫片」而非「收口到某统一符号」。这 5 个别名右值确实指向共享 `date_parsers.due_exclusive` / `parse_date`,但删除动作不向任何收口点聚合调用,纯减行。不存在「多债收口同一符号的顺序敏感性」。(对照:evaluation/ortools 两处与 dispatch_rules 那处之间也无收口关系,是 3 个独立文件各自的死垫片。)

## 字段 7 — 收口行为差异及所需 parity 测试

**不适用(直删类,无新旧两路)。** 删除 5 行死赋值不改变任何运行期分支:
- 这 5 个别名「定义后 0 读取」(字段 8 已 grep 证实),删除前后程序的执行路径**逐字节不变**。
- 无 None-vs-raise 之类边界分歧需 parity 覆盖。
- 唯一需「证不退」的不是行为 parity,而是**编译 / 导入不破**:删后三文件仍能 import(import 行保留、调用本体未动),跑一次现有 `tests/regression_due_exclusive_*` 即可自证未误伤本体。详见字段 11。
## 字段 8 — 爆炸半径

- **生产消费者:零。** `rg -n '_due_exclusive' core/` 去掉 3 处赋值与无关 `batch_due_exclusive` 后**无任何读取**;`rg -n '_parse_due_date\b' core/` 去掉两处别名赋值后,剩下的全是 sgs_scoring 的同名独立函数(字段 3),**不消费本债别名**。调用点全用裸名(evaluation.py:257 `due_exclusive(...)`、:36 `parse_date(...)`;ortools:138/140;dispatch_rules 本文件内裸用)。
- **测试消费者:零。** `rg '_due_exclusive|_parse_due_date' tests/`(排除 sgs)命中的全是 `due_exclusive(...)` 本体断言(regression_due_exclusive_consistency.py:47、regression_due_exclusive_guard_contract.py:25-26),测的是共享函数本体,**不引用 `_` 前缀别名**,删别名不会变红。
- **灾难链(删错 / 误删会静默炸什么)**:
  1. 若**按符号名全局删** `_parse_due_date` → 误铲 `sgs_scoring.py:34` 活函数 → `:223` `_parse_due_date(...)` 调用 NameError,派工评分链炸(**最高风险,定点删可完全规避**)。
  2. 若误删 evaluation.py:26 `_parse_due_date_state`(重前缀) → :230 调用炸,评估链 NameError。
  3. 同批误把 import 行(:9/:19)一起删 → 本体 `due_exclusive(...)` / `parse_date(...)` 调用 NameError。
  - 三条均为**编译期 / 启动期 NameError(loud,非静默)**,定点删 + 跑测试即兜住,不存在静默错算。

## 字段 9 — 承重毗邻标记

- 本债 load_bearing=false,lb_no_touch=null,phase1_blast.lb_colocation_danger=**none**。
- 三文件内 `rg '故意|load_bearing|承重|parity'` **零命中**,无 LB 注释、无承重毗邻债。
- **禁区行(虽非 LB,仍列为「定点删保护边界」)**:
  - `dispatch_rules.py:9` `from .greedy.date_parsers import due_exclusive`(保留)
  - `evaluation.py:9` import、`evaluation.py:26 _parse_due_date_state`、`:257 batch_due_exclusive=due_exclusive(...)`(全保留)
  - `ortools_bottleneck.py:19` import、`:138/:140` 调用(保留)
  - `sgs_scoring.py:34/:223` `_parse_due_date`(**重名活函数,绝对不动**)
  - `dispatch_rules.py:4 import statistics`(R50 的事,R49 不碰)

## 字段 10 — 分层 / 导入环风险

**零风险。** 纯删 5 行,**不新增任何 import**,不调整 import 拓扑 → 不可能引入越层 import 或导入环(分层红线 #6 满足:0 AST 违规,core.algorithms 内部不向 core.services 反向依赖)。被删行的右值 `due_exclusive`/`parse_date` 来自同 partition 的 `core.algorithms.greedy.date_parsers`,删后该 import 因仍有真实调用而存活,亦不产生孤儿 import。

## 字段 11 — 测试耦合

- **动手前无需迁移任何测试**(测试消费者为零,字段 8 已证)。
- 删后**自证手段**:跑 `tests/regression_due_exclusive_consistency.py` + `tests/regression_due_exclusive_guard_contract.py`(测 `due_exclusive` 本体 parity),再做一次三文件 import 冒烟(`python -c "import core.algorithms.dispatch_rules, core.algorithms.evaluation, core.algorithms.ortools_bottleneck, core.algorithms.greedy.dispatch.sgs_scoring"`)确认未误伤同名活函数。
- `tests/regression_aps_three_gap_docs_quality_gate.py:73` 仅把 consistency 测试列入清单,不直接依赖别名。
- contract_topology_tests = none(registry),无契约拓扑测试需同步。

## 字段 12 — 前置依赖

- **无硬前置债 / 无前置 F 门**(registry `planned_deps_hint`:无前置/后置硬序)。
- 唯一编排约束是**同文件同批**:R49 / R50 / R51 须在 dispatch_rules.py 上**同一原子编辑**完成,避免行号互撞(字段 5)。evaluation.py / ortools_bottleneck.py 两处与 dispatch_rules 完全独立,可单独删,无序约束。
- 建议 Batch-7(B06)内 R49 先落(零风险清场),但因须同 patch,实为「一次性删 dispatch_rules.py 三处 + 改 R50 的 import statistics」,R49 不被任何债阻塞,也不阻塞他债。
- owner_pending=false:本债已可给终态修法(直删),无需 owner 裁断。

## 对抗核验(第二双眼睛)

核验时间 2026-06-05;独立重 grep 回盘,默认怀疑、主动找反例,无一反例成立 → **PASS**。

**重跑命令与结果:**
- `rg -n '_due_exclusive' core/` → 3 处赋值(dispatch_rules.py:25 / ortools_bottleneck.py:25 / evaluation.py:41)+ 无关本体 evaluation.py:257/258/261 `batch_due_exclusive = due_exclusive(...)`。
- `rg -n '_parse_due_date\b' core/` → 2 处别名赋值(evaluation.py:40 / ortools_bottleneck.py:24)+ **同名活函数** sgs_scoring.py:34(def，带 strict_mode)、:223(调用)。
- `rg -n '\b_due_exclusive\s*\(|\b_parse_due_date\s*\(' core/`(排除 sgs / _parse_due_date_state)→ **零调用点**，证实 5 别名定义后 0 读取。
- import 来源回盘:dispatch_rules.py:9 / evaluation.py:9 / ortools_bottleneck.py:19 三处 `from ...date_parsers import due_exclusive[, parse_date]` 均真实存在；裸名调用 dispatch_rules.py:67、ortools_bottleneck.py:138/140、evaluation.py:257 仍在 → 删别名后 import 不变死，无孤儿。
- `rg '__all__'` 三文件 → 无 re-export 暴露，别名未经 `__all__` 外泄。
- tests 回盘:命中全是 `due_exclusive(...)` 本体断言（regression_due_exclusive_consistency.py:47、guard_contract.py:25-26），无 `_` 前缀别名引用 → 删别名不会变红。
- `git status --short` 三文件 + sgs_scoring.py → 全空。

**逐项裁决:**
- 行号复核:**一致**（5/5 零漂移，registry hint 全准）。
- 同名陷阱:**复核成立**——sgs_scoring.py:34 `_parse_due_date(strict_mode=...)` 与 evaluation.py:26 `_parse_due_date_state` 均独立活函数，定点按 file:line 删可完全规避，档案标红正确。
- 承重误删风险:**无**（lb=false，lb_no_touch=null，纯删死赋值，不触灵魂线/分层红线，不动 import 拓扑）。
- owner_pending=false:档案给终态修法(直删)未越权。
- 唯一可商榷(非争议):字段 5「删除方向自上而下…整体上移」措辞，但同段已正确给出「删除从大行号往小行号删 / 或同 patch 一次性删」，自洽无误，不影响结论。

**核验结论:PASS（低危纯死叶子，直删 5 行真零引用，同名陷阱已锁定）。**
