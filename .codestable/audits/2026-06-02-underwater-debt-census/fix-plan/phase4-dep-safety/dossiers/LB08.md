# LB08 逐债档案 · legacy 错误串正则反解桥(承重)

> **2026-06-08 B 执行终态：fixed。** G40a/LB08 已由 `fae8829b` 落地 ROOT 护栏：`core/models/scheduler_public_errors.py` 的 `LEGACY_PUBLIC_PATTERNS` 上方已有承重注释，说明这些正则是老中文错误串到公开错误码的反解桥，改 `internal_operation` / `resource_validation` 中文模板时必须同步这里。
> **执行说明：** 下文保留的是动手前的只读证据和安全顺序；现盘正则桥、`legacy_public_error_message`、`infer_legacy_public_code`、`make_public_error`、`__all__` 均未删未统一未透传，既有 `tests/schedule/route_view/test_scheduler_user_visible_messages.py` 继续作为合同测试。R46 已在该护栏存在后删除私有死别名。

> 债 id: LB08 | bucket B10 | severity medium | pathology P3 | partition core-models
> kind: load_bearing(**承重 true**) | needs_adversarial: true | owner_pending: **false(O35 已裁；历史字段曾为 true，执行已 fixed)**
> primary_file: core/models/scheduler_public_errors.py | cluster C01 | siblings [R46]
> 本档案主体保留执行前只读证据和安全顺序；当前状态以顶部 fixed 横幅和字段 2 为准。承重铁律仍然有效:修法仅「补注释 + 绑契约测试」,绝不删/合并/统一/透传正则桥。

---

## 1) 执行前 file:line(grep 回盘)+ 漂移记录（历史快照）

以下为 2026-06-05 执行前按符号名 `rg -n` 回盘的历史快照；2026-06-08 ROOT 护栏和 R46 删除落地后，现盘行号已变化，后续只能按符号重新定位:

| 符号 | old_location_DONT_TRUST | 回盘实际 file:line | 漂移 |
|---|---|---|---|
| `LEGACY_PUBLIC_PATTERNS` | :62-103 | core/models/scheduler_public_errors.py:62 | 0 |
| `_LEGACY_CODE_PREFIXES` | (未单列) | :94 | 0(≡ blast.location_status) |
| `make_public_error` | (结构化产出) | :175 | 0 |
| `legacy_public_error_message` | :218-290 | :218 | 0 |
| `infer_legacy_public_code` | :284 | :284 | 0 |
| `public_error_message_from_detail` | (同文件消费) | :261 | 0 |
| `build_public_error_records` | (同文件消费) | :321 | 0 |
| `__all__` 中 LEGACY 导出 | :342/:345/:346 | `__all__` 始 :340, `"LEGACY_PUBLIC_PATTERNS"` :342 | 0 |

回盘命令:`rg -n "LEGACY_PUBLIC_PATTERNS|_LEGACY_CODE_PREFIXES|def legacy_public_error_message|def infer_legacy_public_code|def make_public_error|def public_error_message_from_detail|def build_public_error_records|__all__" core/models/scheduler_public_errors.py`

**历史注释插入点核实**:`:61` 在 2026-06-05 执行前为空行,`:62` 即 `LEGACY_PUBLIC_PATTERNS: Tuple[Pattern[str], ...] = (`。**现盘状态已变化**：2026-06-08 已由 `fae8829b` 在该位置补入承重护栏注释，当前注释位于 `scheduler_public_errors.py:62`，`LEGACY_PUBLIC_PATTERNS` 下移到 `:63`。
> 结论:本节只保留执行前定位依据，不再代表当前待办状态；现盘以字段 2 的 fixed 结论为准。

## 2) 当前状态 + 与 status evidence 是否一致

- status_2026_06_08 = **fixed**。
- B 执行一致性：**已闭合。** 原 planned 证据里缺少的承重护栏注释已经存在；注释按 O35 裁定指向 `internal_operation` / `resource_validation` 这两个真实中文模板产出点，没有沿用旧 planned 草稿里把 `auto_assign_resource_errors` 当产出方的误指。
- 合同一致性：`tests/schedule/route_view/test_scheduler_user_visible_messages.py` 仍覆盖 legacy 中文串反解和 `build_public_error_records` 公共错误体；本轮不新建测试文件、不删测试。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P3(承重 load_bearing=true)**:legacy 正则反解桥——`legacy_public_error_message`(:218)把**已渲染的中文错误串**用 `LEGACY_PUBLIC_PATTERNS`(:62) `fullmatch` 脱敏归类,`infer_legacy_public_code`(:284)用 `_LEGACY_CODE_PREFIXES`(:94)把中文串反解回 code;与下方结构化体制 `make_public_error`(:175,直接带 code 产出)**两代并存**。属「产出↔解析桥」,**非双栈等价实现**(故无 parity 前置)。
- **严重度 medium**:呈现层降级风险,不触算法/数据正确性(见 §8 灾难链)。
- **桶 B10**(home=B10) | **簇 C01** | partition core-models。

## 4) 修法类(承重=仅注释;已执行)

**修法类 = 补注释 + 绑契约测试(承重,零删除/统一/透传)，已执行 fixed。**

- **动作1·钉注释**:已在 `LEGACY_PUBLIC_PATTERNS` 定义行上方补入承重护栏，钉死这组正则把**已渲染中文串反解回 code**，且改 `internal_operation` / `resource_validation` 中文模板时必须同步这里。
- **动作2·绑契约**:绑定**已存在**的 `tests/schedule/route_view/test_scheduler_user_visible_messages.py`(:680 参数化 + :696/:722/:744/:759 `build_public_error_records` 系列)作续命断言,确保「改 legacy 文案不同步正则 → 测试红」。该测试**已存在**,本动作未新建测试文件。
- **不是 P5 收口债**:LB08 修法为承重注释,不涉及收口到任何统一点;无新建模块。
- **owner 裁定结果**:O35 已裁按实证更正产出点指向，不贴 planned 草稿里把 `auto_assign_resource_errors` 当产出方的旧文案；该裁定已随 ROOT 护栏执行。

## 5) 同文件碰撞债(sibling 各改哪段、谁先谁后、撞行号/dict 键位移)

同文件兄弟债 = **R46**(`same_file_siblings=[R46]`,co_change_within_group=[R46])。

- **R46 改哪段**:直删 `core/models/scheduler_public_errors.py:162-164` 三行——`:162` 误导注释 `# Backward-compatible private alias…`、`:163` `def _safe_identifier(...)`、`:164` `return public_safe_identifier(...)`(回盘 `rg` 确认:`_safe_identifier` 仅 :163/:164,全仓零生产/测试/动态引用,死别名)。
- **LB08 改哪段**:仅在 `:61` 空行处**插入**注释块(在 `:62` 上方)。
- **是否撞行号**:**不直接撞**——LB08 插入点(:61/:62)在 R46 删除段(:162-164)**上方**,两段不重叠。
  - 但**单向位移**:LB08 注释插入会把 :62 以下所有行(含 R46 的 :162-164)整体**下移 N 行**;R46 删除会把 :164 以下所有行(含 `make_public_error`:175 / `legacy_public_error_message`:218 / `infer_legacy_public_code`:284 / `__all__`:340)**上移 3 行**。两者作用于不同行段,互不抵消,但都会令对方的「绝对行号」失效。
  - **无 dict 键位移**:两债均不改 `LEGACY_PUBLIC_PATTERNS` tuple 顺序、不改 `_LEGACY_CODE_PREFIXES` 键序。
- **谁先谁后(Phase1 边 LB08→R46)**:**LB08 注释必须先落(或同提交先于 R46 删行)**。理由:R46 删行属结构动作,LB08 注释是承重护栏,需先钉死「哪段是承重不可碰」,R46 才能在有护栏保护下安全删 :162-164。R46 的禁区清单已含「正则桥 :62/:94/:218/:284 + make_public_error :175 一律不碰」,与 LB08 承重边界完全咬合。
  - 批次:LB08 注释 → Batch-1(ROOT 增量);R46 删除 → Batch-11。LB08 先于 R46。
- **跨桶同文件冲突**:`cross_bucket_same_file_edges=[]`,无。

## 6) 同收口点债(收口到同一符号的其他债 + 顺序敏感性)

- LB08 **不收口**(承重注释),不收到任何统一符号。
- 同文件其他债 R46 亦不收口(纯删死别名)。
- 故**无同收口点债、无收口顺序敏感性**。唯一顺序约束是 §5 的 LB08(注释)先于 R46(删行)的「承重先于动同文件」边,属结构保护顺序,非收口顺序。

## 7) 收口行为差异 + 所需 parity 测试

- LB08 **本轮不收口**(承重,仅注释),故**无新旧两路 parity 等价验证需求**;无收口行为差异。
- 终态(注释中声明的、未来的「让老路径也走 make_public_error 端到端带 code 后删正则」)**才会**产生 parity 需求,但那是另立债的未来动作,不在本轮 owner_pending 终态修法内。
- **续命契约(非 parity,是耦合断言)**:`tests/schedule/route_view/test_scheduler_user_visible_messages.py:678 test_legacy_error_with_sensitive_tail_is_generic`(parametrize 4 条 raw,钉死 legacy 模板字面 `自制工序未补全设备或人员，无法排产：工序 B001_05`,回盘见 :639/:646/:672-675)。该断言验证「legacy 中文文案 ↔ 正则 fullmatch」强耦合:
  - **边界反例(供未来收口 parity 用)**:若改 legacy 模板一个字而不同步 `LEGACY_PUBLIC_PATTERNS` → `fullmatch` 失配 → `legacy_public_error_message` 返回通用文案「排产执行遇到问题…」(降级,非 raise,**静默**),`infer_legacy_public_code` 返回空 code。新旧两路对此边界**不等价**:老路径(正则桥)= 静默降级;终态路径(make_public_error 端到端)= 直接带 code 无需反解。这正是承重不可轻删的根据。

## 8) 爆炸半径(生产消费者 + 测试消费者 + 灾难链)

**生产消费者(回盘确认)**:
- `core/services/scheduler/run/auto_assign_resource_errors.py:6`(import `infer_legacy_public_code, legacy_public_error_message`)、`:42`(`legacy_public_error_message`)、`:142`(`legacy_public_error_message`)、`:157`(`infer_legacy_public_code`)。
- `web/viewmodels/scheduler_summary_display.py:13`(import `legacy_public_error_message`)、`:63`(调用)。
- 同文件内部消费:`:276` `public_error_message_from_detail` → `legacy_public_error_message`;`:325/:327/:336` `build_public_error_records` → legacy + infer + make_public_error 三者。

**真·产出点(grep 回盘新增,registry 未列)**:中文模板字面**不**由上述消费者产出,而由算法层 `core/algorithms/greedy/internal_operation.py`(如 `自制工序…无法排产：工序 {op_code}`、`…本次无法自动补齐设备和人员：工序 {op_code}`)与 `core/algorithms/greedy/dispatch/resource_validation.py` 产出;`auto_assign_resource_errors.py` 仅持有前缀字面做反解匹配。→ 直接修正 open_questions#1。

**测试消费者(回盘确认)**:
- `tests/schedule/route_view/test_scheduler_user_visible_messages.py:678`(`test_legacy_error_with_sensitive_tail_is_generic`,4 条 parametrize)。
- `:695/:721/:743/:758`(`build_public_error_records` 系列,走 legacy_public_error_message)。registry 标 :694,实测 def 在 :695(参数化装饰器与 def 一行差),属精确级微差,不影响绑定。

**灾难链(承重为何不可碰)**:
1. **删错正则桥** → `legacy_public_error_message` 对一切 legacy 中文串 fullmatch 失配 → 全部降级通用文案「排产执行遇到问题…」+ `infer_legacy_public_code` 返回空 → 用户可见错误**丢失归类与具体工序信息**,且**静默**(无 raise、无降级标记),P4 灵魂线红区(静默回退)。
2. **改文案不同步正则**(`fix_invalidation_risk`) → 同上单条静默失配 → `regression_scheduler_user_visible_messages` 转红(若测试到位)。
3. **影响域 = 呈现层**(用户可见错误脱敏+归类),**不触算法/数据**;故 severity medium 而非 high。
4. LB08 本轮动作(纯注释)**零爆炸**——不改任何运行时行为,只增护栏文本。

## 9) 承重毗邻标记(同文件 LB + 禁区行)

`core/models/scheduler_public_errors.py` 本体即 LB08 承重(`lb_colocation_danger`)。**禁区行(动同文件 R46 时绝不可碰)**:
- `:62` `LEGACY_PUBLIC_PATTERNS`(正则桥定义,本债护栏锚点)
- `:94` `_LEGACY_CODE_PREFIXES`(code 反解前缀表)
- `:175` `make_public_error`(结构化产出,两代之新代)
- `:218` `legacy_public_error_message`(反解主函数)
- `:284` `infer_legacy_public_code`(反解 code)
- `:142` `public_safe_identifier`(活,8 处生产消费,R46 删别名时保留)
- `:340-` `__all__`(LEGACY 导出 :342/:345/:346 不可动)

`lb_no_touch`:禁删正则桥;禁改被算法层发出、被 auto_assign_resource_errors 反解的文案而不同步正则;注释须钉「文案与正则同生共死」。R46 允许删的只有 :162-164 死别名,**与上述禁区零重叠**。

## 10) 分层 / 导入环风险

- `core/models/scheduler_public_errors.py` 仅 import `re` + `typing`(回盘 `rg "^from |^import "` 证 :1/:3/:4),**纯 leaf model,零跨层 import**。
- LB08 修法为注释,**不新增任何 import**,故对分层红线(无 core.algorithms→core.services / core.models→core.services)**零影响**,0 AST 违规风险。
- 导入方向:`core.services.scheduler.run.auto_assign_resource_errors` 与 `web.viewmodels` 单向 import 本 model(:6 / :13),model 不反向 import 它们,**无导入环**。注释不改导入,环风险不变(无)。

## 11) 测试耦合(动手前先迁哪些测试)

- **无需迁移测试**——LB08 是绑定**已存在**测试,不是新建/搬迁。
- 执行前已确认目标测试就绪；现盘目标测试为 `tests/schedule/route_view/test_scheduler_user_visible_messages.py`(:680 + :696/:722/:744/:759)，继续绑死 legacy 模板字面与正则。
- 本轮验证已裸跑该测试文件，52 passed，证明注释和 R46 删除未改变用户可见错误合同。

## 12) 前置依赖(先做哪些债 / F 门)

- **无 parity 前置**:LB08 非双栈等价实现(是产出↔解析桥),无 LB07/R71 类前置(planned_deps_hint 明示)。
- **本债自身是前置**:LB08 注释是 **R46 的前置**(Phase1 边 LB08→R46『承重先于动同文件』)——该前置已满足，R46 已在护栏存在后删除。
- **批次**:LB08 注释 ∈ 全局 ROOT **G40a**，R46 删除 ∈ Batch-B **G40**；两者均已 fixed。
- **F 门**:本债不依赖任何 F 门；owner_pending 裁断已由 O35 闭合。

## open_questions(已由 O35 裁定闭合)

1. **注释逐字文案对产出点的指向有误(已裁:按实证更正)**:planned_fix_hint 草拟注释写「改任何被 **auto_assign_resource_errors** 发出的错误文案,都必须同步更新这里的 pattern」。但 §8 grep 回盘证明:
   - `auto_assign_resource_errors.py` 是 legacy 串的**消费/反解方**(import + 调用 `legacy_public_error_message`/`infer_legacy_public_code`),它仅持有前缀字面做匹配;
   - 真正**产出**这些渲染中文模板的是 `core/algorithms/greedy/internal_operation.py` 与 `core/algorithms/greedy/dispatch/resource_validation.py`。
   - O35 已裁按实证更正；现盘注释已指向 `internal_operation/resource_validation`，未贴 auto_assign 消费方旧草稿。
2. **是否纳入本轮执行**:已纳入 ROOT/G40a 并 fixed；R46 已在该前置满足后执行。

## 对抗核验(第二双眼睛，执行前历史快照)

核验时间 2026-06-05,只读不改,独立重 grep 回盘,默认怀疑。以下命中是**执行前历史快照**，不代表当前行号；当前状态以顶部 fixed 横幅和字段 2 为准。

**重 grep 命令与结果**:
- `rg -n "LEGACY_PUBLIC_PATTERNS|_LEGACY_CODE_PREFIXES|def legacy_public_error_message|def infer_legacy_public_code|def make_public_error|def public_error_message_from_detail|def build_public_error_records|^__all__|def public_safe_identifier" core/models/scheduler_public_errors.py` → 执行前 LEGACY_PUBLIC_PATTERNS:62 / _LEGACY_CODE_PREFIXES:94 / public_safe_identifier:142 / make_public_error:175 / legacy_public_error_message:218 / public_error_message_from_detail:261 / infer_legacy_public_code:284 / build_public_error_records:321 / __all__:340(LEGACY 导出 :342)。执行后这些符号仍保留，但行号已随注释和 R46 删除变化。
- `awk 'NR=59..62'` → 执行前 :61 确为空行,:62 为 `LEGACY_PUBLIC_PATTERNS: ...=(`;其上无承重护栏注释。该缺口已由 `fae8829b` 补齐。
- `rg -nw "_safe_identifier" --glob '*.py' .`(R46 死别名零引用核实)→ 执行前全仓仅 4 命中:`scheduler_public_errors.py:163`(R46 待删定义本身)+ `core/infrastructure/migrations/v4_sanitizers.py:37/121/122`。执行后 models 层命中已消失，v4_sanitizers 同名独立函数保留。
- `rg -n ... core/services/scheduler/run/auto_assign_resource_errors.py web/viewmodels/scheduler_summary_display.py` → 生产消费者 auto_assign:6/42/142/157、summary_display:13/63 全部命中,与 §8 一致。
- `rg -n "test_legacy_error_with_sensitive_tail_is_generic|build_public_error_records|自制工序未补全设备或人员，无法排产：工序 B001_05" tests/schedule/route_view/test_scheduler_user_visible_messages.py` → :678 test 定义、:639/646/672-675 模板字面、:695/721/743/758 build_public_error_records 系列全部命中。续命契约确实**已存在**,绑定可行;:694→:695 微差(parametrize 装饰器与 def 一行差)如档案所述。
- **产出点指向争议复核(主动找反例)**:`rg -n "自制工序未补全设备或人员|本次无法自动补齐设备和人员|缺少自动派工所需工种信息|没有找到可用的设备和人员组合" core/algorithms/greedy/internal_operation.py core/algorithms/greedy/dispatch/resource_validation.py` → internal_operation:119/148/150/154、resource_validation:86 **才是中文模板真产出点(f-string 拼接)**;`auto_assign_resource_errors.py:11` 仅持有前缀字面 `"自制工序缺少自动派工所需工种信息，无法自动分配：工序 "` 做**反解匹配**,非产出。**open_questions#1 成立**——planned_fix_hint 注释草稿指向 auto_assign 确属错位。
- 分层:`sed -n '1,6p'` → 仅 `import re` + `from typing import ...`,纯 leaf model,零跨层 import。§10 成立。

**承重误删核查**:档案修法严格为「补注释 + 绑已存在 regression_scheduler_user_visible_messages 续命断言」,通篇**无删除/统一/合并/透传/加形参**;R46 删除段(:162-164 死别名)与正则桥禁区(:62/:94/:175/:218/:284)**零重叠**,经回盘确认。**承重误删风险:无。**

**owner_pending 核查**:执行前档案没有擅自给终态；执行时已按 O35 裁定落地，当前状态 fixed。合规。

**未发现反证。结论:PASS。** 行号一致、消费者清单一致、承重判断正确、收口判断(本债不收口,承重注释)正确、产出点争议复核成立(档案已自行记入 open_questions#1,非遗漏)。无需第三方裁断(争议点档案已主动暴露并留 owner)。
