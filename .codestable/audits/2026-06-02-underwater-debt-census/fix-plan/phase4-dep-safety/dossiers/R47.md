# R47 逐债档案 · 死参数 raw_value（_record_blank_choice_degradation 收了不用）

> 桶 B03 / 病理 P6 / 严重度 trivial / 承重 false / owner_pending false
> 簇 C01 / 同文件兄弟 LB07, R71 / planned_batch Batch-5（随 R71 双栈同删，承重注释 Batch-1 之后）
> 本档案只读不改、只产计划。所有断言附当前代码 file:line 证据。

## 字段 1：当前真实 file:line（grep 回盘 + 漂移记录）

死参数 `raw_value` 是**双栈对称**存在（model 侧 + service 孪生），不是单边。逐点回盘：

**Model 栈：`core/models/schedule_config_runtime_coercion.py`**
- 定义：`def _record_blank_choice_degradation(` @ **:83**（registry old_loc 写 83-98 → 实测 :83-:98，**无漂移**）
- 死形参：`raw_value: Any,` @ **:88**（函数体 :91-98 只读 `field_name`/`fallback`/`scope` 调 `collector.add(code="blank_required" ...)`，**从不读 raw_value**——:96 消息只插 `{fallback}`，:97 `sample=None`）
- 调用点①：`_record_blank_choice_degradation(` @ **:155**，死实参 `raw_value=raw_value,` @ **:159**（在 `_choice_with_degradation` 的 `if text == ""` 分支内）
- 调用点②：`_record_blank_choice_degradation(` @ **:210**，死实参 `raw_value=raw_value,` @ **:214**（在 `_yes_no_with_degradation` 的 `if text == ""` 分支内）

**Service 孪生栈：`core/services/scheduler/config/config_field_coercion.py`**
- 定义：`def _record_blank_choice_degradation(` @ **:63**，死形参 `raw_value: Any,` @ **:68**（注意：service 侧形参名是 `field` 不是 `field_name`；函数体 :71-78 同样不读 raw_value）
- 调用点③：`_record_blank_choice_degradation(collector, scope=scope, field=field, raw_value=raw_value, fallback=fallback_text)` @ **:158**（**单行内联**，死实参就在本行）
- 调用点④：`_record_blank_choice_degradation(` @ **:207**，死实参 `raw_value=raw_value,` @ **:211**

**漂移总结：基本无漂移。** registry `phase1_blast.prod_consumers` 写 model :155/:210（定义起始行）准确；写「死实参」时口径含混（说 :155 而非 :159），但定位到该调用块即命中，属可接受口径差。共 **6 处编辑点**（2 形参 + 4 实参），**0 行号偏差影响修法**。

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = "planned"`；`current_evidence_2026_06_05` 与今日回盘**完全一致**：两栈对称死参数均未删，4 调用点未改。
- `phase1_blast.location_status = "✅准"`，且已在 evidence 里**自行纠正了原始报告的错误**（原报告 why_debt 声称「服务侧孪生甚至没有这个参数」，实测 service :68 同样有 `raw_value` 且同样死）。本档案核实：纠正成立，**确为双栈对称死参，非单边**。无新增不一致。

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 **P6**（签名死参数 / 接口冗余）：`raw_value` 被声明并由 4 处调用方传入，函数体从不读取。纯签名冗余，徒增调用噪音与双栈锁步面积。
- 严重度 **trivial**；**无运行期行为变化**——删形参 + 改实参不改任何降级消息、降级结果、返回值（:96 消息只用 `fallback`，与 `raw_value` 无关）。
- 桶 **B03**；分区 core-models；kind real（真债非误报）。
- 簇 **C01**；needs_adversarial=false，adv_refuted=false。

## 字段 4：修法类

**直删（删死形参 + 改 4 调用点实参）· 两栈对称同改。** 非收口（无需收口到统一点）、非 raise、非承重仅注释（本债 load_bearing=false）。

具体 6 处编辑（**唯一允许动的行**）：
1. 删 model `coercion.py:88` 形参 `raw_value: Any,`
2. 删 model `coercion.py:159` 实参 `raw_value=raw_value,`
3. 删 model `coercion.py:214` 实参 `raw_value=raw_value,`
4. 删 service `config_field_coercion.py:68` 形参 `raw_value: Any,`
5. 删 service `config_field_coercion.py:158` 内联实参 `raw_value=raw_value,`（保留行内其余参数）
6. 删 service `config_field_coercion.py:211` 实参 `raw_value=raw_value,`

**铁律 4（灵魂线）防呆：** 函数体内 `collector.add(code="blank_required" ...)` 这条**有意可观测降级记录整条不动**。blank 降级不是兜底/静默回退——它是 strict_mode=false 下「按默认值处理」的显式 degradation 上报；删死参数时**绝不能把这条 add 一起删**，否则把可观测降级变成静默，踩灵魂线。

**为什么必须两栈对称同删（铁律 5 方向）：** 若只删 model 侧（:88）而留 service 孪生（:68），会加大 model↔service 的逐字分叉，与 R71/LB07 的收敛方向相悖，**反造新锁步差**。R47 本身不新建收口点，但其修法必须服从同文件双栈收敛的节奏。

## 字段 5：同文件碰撞债（sibling 协调）

同文件兄弟（interference_edges same_file=true）：**LB07、R71、LB04、R33**（后两条为同文件干扰边）。逐个协调：

- **R71（双栈逐字复制函数群 = LB-B2 配料表，LB=false）**：与 R47 **同向同批**。R71 收敛的正是 model↔service 这组逐字复制函数，`_record_blank_choice_degradation` 是其配料之一。**顺序：R71 与 R47 同批（Batch-5）一起做**，避免锁步差。撞行号风险：R71 若整体收敛/合并双栈函数，会大幅移动本债 6 个编辑点的行号——所以**不能 R47 先单独删完再让 R71 改**，否则 R71 工作面变化；应在同一批内由同一只手统筹（先删死参再收敛，或收敛时顺手删）。dict 键位移：本债不涉 dict 字面量，无键位移风险。
- **LB07（ScheduleConfigSnapshot 整栈双实现，LB=true）**：承重。LB07 的修法是「补『我是故意的』注释 + 绑 parity」，**先于** R47 动同文件（承重注释 Batch-1 落地后才动 Batch-5）。LB07 不删不改函数体，与 R47 编辑点不重叠，但二者落在同一组承重文件上，**顺序：LB07 承重注释先 → 再 R71+R47 同批**。
- **LB04（normalize_yes_no_wide 双实现，LB=true）**：在 `core/shared/boolean_normalize.py`，**不在** coercion.py / config_field_coercion.py 本体，仅因调用链关联被标 same_file 干扰边。R47 6 个编辑点**不触及** normalize_yes_no_wide（model :230 / service 的 to_yes_no 调用均在 `text==""` 分支之外）。无直接碰撞，但 LB04 是承重，R47 不得顺手碰。
- **R33（re-export facade 残渣，LB=false）**：在 `core/services/common/compat_parse.py` 等，与 R47 编辑点无重叠，仅同分区干扰边。无碰撞。

## 字段 6：同收口点债 + 顺序敏感性

R47 **不收口到任何统一符号**（修法是直删死参，非透传/合并）。`_record_blank_choice_degradation` 本身是 model/service 各一份的孪生函数——R47 不把两份合一（合一是 R71 的活），只是各自删掉死参。

- 与 R71 的关系：R71 才是「把双栈逐字复制函数收敛」的债。R47 删的死参是 R71 收敛对象的内部细节。**顺序敏感：R71 与 R47 必须同批**，否则 R71 收敛后行号/签名变化使 R47 编辑点漂移。建议执行序：在 Batch-5 内 **先删两栈死参（R47）→ 再收敛双栈（R71）**，因为删参后两份函数签名更接近、收敛 parity 比对更干净。
- 无其他债收口到 `_record_blank_choice_degradation`。

## 字段 7：收口行为差异 + parity 测试

R47 是直删，非收口，**不存在新旧两路分支等价问题**——删死参数对 `_record_blank_choice_degradation` 是纯无副作用变换（编译期签名收缩，运行期产出逐字节相同）。

但因落在双栈收敛节奏内，仍需 parity 守门**确保删参后两栈降级行为不漂移**。已存在的现成守门测试（无需新建）：
- `tests/config/test_config_validator_preset_degradation.py::test_config_validator_preset_relaxed_blank_choice_and_yesno_emit_ln` —— 覆盖 relaxed 模式下 blank choice + yesno 的降级上报（service 侧路径）
- `tests/config/test_schedule_config_snapshot_optional_guard.py::test_build_schedule_config_snapshot_relaxed_explicit_blank_choice_and_yesno_emit_ln` —— 覆盖 model 侧 snapshot 构建的 blank/yesno 降级

边界值核对（删参前后必须等价，反例自查）：
- `raw_value=""`（空字符串）→ `text==""` 命中 blank 分支 → `collector.add(blank_required, message="…没有填写…")`。删参后消息不变（消息不引用 raw_value）。✅
- `raw_value=None` → :150/:203 `text=""` → 同上 blank 分支。删参后行为不变。✅
- `raw_value="  "`（纯空格）→ `.strip()` 后 `text==""` → blank 分支。✅
- **不会发生 None vs raise 这类分歧**——因为删的是「从不被读的形参」，无任何分支依赖它。这正是 P6 trivial 的本质：零行为面。

## 字段 8：爆炸半径（消费者清单 + 灾难链）

**生产消费者（4 个调用点，全部在两栈内部，无外部跨模块调用）：**
- `core/models/schedule_config_runtime_coercion.py:155`（_choice_with_degradation 内，:159 传 raw_value）
- `core/models/schedule_config_runtime_coercion.py:210`（_yes_no_with_degradation 内，:214 传 raw_value）
- `core/services/scheduler/config/config_field_coercion.py:158`（_choice_with_degradation 内联）
- `core/services/scheduler/config/config_field_coercion.py:207`（_yes_no_with_degradation 内，:211 传 raw_value）

rg 全仓确认：`_record_blank_choice_degradation` **无任何模块外引用、无测试直接 import 调用**（test_consumers=[]）。该函数是模块私有（`_` 前缀），调用面完全闭合在两文件内。

**测试消费者：** 直接调本函数 = **0**。间接经 blank 降级路径覆盖 = 上述 2 条 regression（见字段 7/11）。

**downstream_effect：** blast_radius = **无**。删形参 + 改实参不改任何消息 / 降级结果 / 返回值。

**灾难链（删错 / 漏改会静默炸什么）：**
1. **漏改某个调用点**（删了形参但留某处 `raw_value=raw_value`）→ Python 立刻 `TypeError: unexpected keyword argument 'raw_value'`，**loud 失败、不静默**——此类错被现成 regression 测试或导入即抓。低危。
2. **只删 model 不删 service（或反之）**→ 不报错，但加大双栈分叉，制造新锁步差，与 R71/LB07 收敛相悖。这是**最大隐患**：静默地让两栈再次背离。必须两栈对称同删。
3. **误删函数体 `collector.add(blank_required)`**（把降级记录当死代码一起删）→ blank 配置不再上报降级 = **静默吞掉可观测降级**，踩灵魂线、且会让上述 2 条 regression 红。中危，靠字段 4 防呆 + 现成测试兜住。

## 字段 9：承重毗邻标记 + 禁区行

⚠️ **R47 落在承重文件 coercion.py 上（LB07 = ScheduleConfigSnapshot 整栈双实现，load_bearing=true）。** 虽然 R47 本债 load_bearing=false，但同文件毗邻 LB07，删参时必须避开 LB07 列明的禁区。

**禁区行（绝不能碰，回盘后的真实行号）：**
- `coercion.py:470` —— `graph_downstream_weight` 承重置零（LB07 lb_no_touch 明列）。
- `coercion.py:153` —— `_choice_with_degradation` 中 `if strict_mode and text == "": raise ValidationError("…不能为空")` **strict loud raise**（registry 禁区写 :152-153，实测 raise 实体在 **:153**，概念命中、行号 +1 漂移）。
- `coercion.py:208` —— `_yes_no_with_degradation` 中同款 strict loud raise（registry 笼统写 :152-153，实测第二处在 **:208**）。
- `coercion.py:73-80` —— `_handle_missing_value` 的 `MISSING_POLICY_ERROR raise`（LB07 lb_no_touch 明列）。
- service 侧对称禁区：`config_field_coercion.py:156`、`:205` 的两处 strict loud raise 同样不碰。

**R47 只动：** model `:88` 形参、`:159`/`:214` 实参；service `:68` 形参、`:158`/`:211` 实参。这 6 行**全部在 `if text == ""` 的非 strict 降级分支内**，与上述 loud raise 行（在 `if strict_mode and text == ""` 分支）**物理相邻但语义隔离**，删除时务必逐行核对不要误伤紧邻的 raise。

## 字段 10：分层 / 导入环风险

**无越层 / 无导入环风险（铁律 6）。** R47 是删形参，**不新增任何 import**：
- model 栈在 `core/models/`，service 栈在 `core/services/scheduler/config/`，两栈各删各的私有函数死参，互不新增引用。
- 不触碰 LB04（normalize_yes_no_wide）——后者才是分层承重（core.shared 供下层），R47 不改其调用指向，故不会造 core.models→core.services 越层或导入环。
- AST 违规面 = 0（无新 import 边）。

## 字段 11：测试耦合（动手前先迁哪些）

**无需迁移任何测试。** 原因：
- 0 测试直接 import / 调用 `_record_blank_choice_degradation`（私有函数，test_consumers=[]）。
- 删死参不改函数签名以外的任何行为，现成 2 条 regression 测试**无需修改即可继续作为 parity 守门**：
  - `tests/config/test_config_validator_preset_degradation.py`
  - `tests/config/test_schedule_config_snapshot_optional_guard.py`
- 动手前**先跑**上述 2 条（取绿基线）→ 删 6 行 → 再跑 → 必须仍绿且降级消息逐字节相同。
- 若与 R71 同批收敛，R71 自身可能引入双栈 parity 比对测试；R47 的死参删除应在 R71 的 parity 测试**建立之后或同步**进行，确保收敛 diff 干净。

## 字段 12：前置依赖

执行序（依 planned_deps_hint，回盘确认）：
1. **LB07 承重注释先落（Batch-1）** —— 同文件承重边，「我是故意的」注释 + parity 守护先到位，再动同文件其他债（承重先于动）。
2. **R71 与 R47 同批（Batch-5）** —— 双栈对称收敛，co_change_within_group=[LB07, R71]。R47 不得脱离 R71 单独提前删（否则 R71 收敛时本债编辑点行号漂移、且单边删造新锁步差）。建议批内执行序：**先删两栈死参（R47）→ 再收敛双栈（R71）**，删参后两份签名更接近、parity 比对更干净。
3. **F 门**：动手前需现成 2 条 degradation regression 取绿基线（见字段 11）；删后 6 行须复跑全绿 + 降级消息逐字节不变。
4. owner_pending=false → 本档案给出终态修法（直删两栈对称），无需等 owner 裁断。

**前置债清单：LB07（承重注释，先）→ 与 R71 同批（同时）。** 无其他硬前置。

## 对抗核验（第二双眼睛）

核验人：对抗核验 agent（只读不改）。结论：**PASS（带 1 处必修标识符争议，不影响修法本体）**。

**重 grep 命令与结果（全部独立回盘当前工作区）：**
1. `rg -n "_record_blank_choice_degradation" core/models/schedule_config_runtime_coercion.py` → 定义 :83、调用 :155、:210。**与档案一致**。
2. `rg -n "_record_blank_choice_degradation" core/services/scheduler/config/config_field_coercion.py` → 定义 :63、调用 :158、:207。**与档案一致**。
3. `rg -n "def _record_blank_choice_degradation" --glob '*.py'` → 仅 model:83 + service:63 两份，**确为双栈对称、无第三份**。
4. 全仓引用（排除 docs）= 上述 6 行，**无任何模块外调用、无测试 import**，test_consumers=[] 成立。
5. 读 model :83-217 + service :63-217 函数体：两栈 `_record_blank_choice_degradation` 形参 raw_value（model:88 / service:68）**确实从不被读**——`collector.add` 的 message 只插 `{label}`/`{fallback}`、`sample=None`。**死参断言成立**。
   - 反例自查：外层 `_choice_with_degradation` / `_yes_no_with_degradation` 的 raw_value 形参是**活的**（用于 `text=str(raw_value)`、`_record_invalid_choice_degradation` 的 message 引用 raw_value）。R47 只删 `_record_blank_choice_degradation` 这一个函数的死参，不碰外层活形参、不碰 `_record_invalid_choice_degradation`（活）。档案区分清晰，**无误删活参风险**。
6. `rg -n "raise ValidationError|graph_downstream_weight|MISSING_POLICY_ERROR"` 两栈 → 禁区行复核：model strict raise 在 :153/:208（档案 +1 行漂移记录准确）、graph_downstream :470、MISSING raise :72；service strict raise :156/:205。**全部命中，承重禁区定位准确**。6 个编辑点（model :88/:159/:214、service :68/:158/:211）全在 `if text==""`（非 strict）分支，与 raise 行物理隔离，**不会误伤 loud raise**。

**承重误删风险：无。** 本债 load_bearing=false、lb_no_touch=null；修法为直删两栈对称死参，未混入对承重邻接 LB07 禁区（:470/:153/:208/:72）的任何编辑；明确保留 `collector.add(blank_required)` 降级记录不动（不踩灵魂线）。owner_pending=false，档案给终态修法合规。分层：删形参不引入任何 import，AST 违规面=0，无越层/导入环。

**争议点（必修，trivial 级，不影响修法本体）：字段 7/11 引用的 parity 守门测试方法名写错后缀。**
- 档案写 `test_config_validator_preset_relaxed_blank_choice_and_yesno_emit_ln` → **真实方法名** `test_config_validator_preset_relaxed_blank_choice_and_yesno_emit_blank_required` @ `tests/config/test_config_validator_preset_degradation.py:210`。
- 档案写 `test_build_schedule_config_snapshot_relaxed_explicit_blank_choice_and_yesno_emit_ln` → **真实方法名** `test_build_schedule_config_snapshot_relaxed_explicit_blank_choice_and_yesno_emit_blank_required` @ `tests/config/test_schedule_config_snapshot_optional_guard.py:107`。
- 即档案把尾巴 `_emit_blank_required` 误截成 `_emit_ln`。两测试文件**真实存在**、覆盖路径**正确**（:210 触发空白值 `"   "`/`None`→blank 分支，断言 `blank_required` 计数=4，正是删参后须守住的 parity）。但若执行者照档案 `pytest -k ..._emit_ln` 取基线，会**匹配不到测试、误判无守门**。需把档案字段 7/11 的方法名后缀更正为 `_emit_blank_required`。

**结论：行号 12 字段核实一致；唯一争议为 2 处 parity 测试方法名后缀笔误（_emit_ln→_emit_blank_required），属可独立修正的标识符错误，不动摇「直删两栈对称死参」修法本体，不需升级第三方裁断。**
