# R23 逐债档案 — `_normalize_role` 字节级重复，无 plan_role 归一收口点

> 主文件: `core/models/schedule_plan_role.py`；干扰簇 C01；桶 B02（身份族）；病理 P5；严重度 low；承重 false；owner_pending false
> 本档案原始证据来自 2026-06-05 回盘；2026-06-08 B 执行后已补登终态，以下“当前”均以本分支现盘为准。

---

## 1) 当前真实 file:line（回盘）+ 漂移记录

按符号名 `rg -n "_normalize_role" core/ web/ data/` 回盘，R23 已落最小收口路径：model 侧保留唯一实现，service 侧改为 import 同一对象；registry 里的 `schedule_plan_query_service.py:28-30` 是执行前旧线索，不再是当前锚点。

| 角色 | 符号 | registry 老线索 | 当前实际 file:line | 漂移 |
|---|---|---|---|---|
| 定义 A（model 侧） | `def _normalize_role` | `schedule_plan_role.py:21-23` | `core/models/schedule_plan_role.py:21`（体 :22-23） | **无** |
| 定义 B（service 侧旧重复体） | `def _normalize_role` | `schedule_plan_query_service.py:28-30` | **已删除**；service 现于 `core/services/scheduler/schedule_plan_query_service.py:17` import model `_normalize_role` | **B 已收口** |
| 调用 A | `_normalize_role(role)` | — | `core/models/schedule_plan_role.py:52`（`plan_role_label` 内） | — |
| 调用 B | `_normalize_role(role)` | — | `core/services/scheduler/schedule_plan_query_service.py:102`（`resolve_plan` 入口） | R23 后上移 4 行 |
| 第三变体（带校验，raise） | `def normalize_plan_role` | `view_context.py:65` | `core/services/scheduler/schedule_result_view_context.py:65` | **无** |

**执行前字节级重复实证**（两处函数体逐字符相同，现已收口）：
- model `schedule_plan_role.py:21-23`：`text = str(role or "").strip()` / `return text or ROLE_ADOPTED`
- service 旧体 `schedule_plan_query_service.py:29-30`：`text = str(role or "").strip()` / `return text or ROLE_ADOPTED`；当前本地体已删除，service 复用 model 对象。

**结论：R23 已完成。** 当前 `schedule_plan_query_service.py` 在 :17 导入 `_normalize_role`，:102 使用同一对象；旧 :28-30 本地重复体不存在。

## 2) 当前状态 + 与 status evidence 是否一致

- 原始 registry `status_2026_06_05` 曾为 **planned**，该状态已被 B 执行终态取代。
- 当前回盘佐证：service 侧已 `from core.models.schedule_plan_role import _normalize_role`；旧本地定义已删除；`tests/candidate/test_scheduler_candidate_plan_query_contract.py:69` 已新增单源守卫，断言 service 与 model 引用同一 `_normalize_role` 对象。
- **状态：fixed in B（提交内补登）。**

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P5（重复/缺收口点）已清理**：`_normalize_role`（空→adopted 的归一逻辑）原先在 model 与 service 两处字节级重复；当前 service 已收口到 model 侧既有实现。
- **严重度 low**：纯结构重复，今日两版行为完全一致，无现网行为差；唯一风险是「改一处漏另一处」的未来漂移。
- **桶 B02（scheduler-plan-identity 身份族）/ home B02**。
- **簇 C01**：当前权威 `_registry.json` 已把 R23 的 `interference_edges` 清空；R22/R34 只作为历史弱邻接背景留在本档说明，非机器排序边、非批次门控。
- 注意：本债病理涉及**三个**plan_role 归一变体——model `_normalize_role`(:21)、service 当前 import 的同一 `_normalize_role`(:17/:102)、view_context `normalize_plan_role`(:65)。前两者静默归一（无校验），第三者带 `VALID_PLAN_ROLES` 校验并 `raise ValidationError`（见 §7）。本债的收口范围仅限**前两者的字节级重复**，第三者语义不同，不在合并范围。

## 4) 修法类 — **收口（dedup 到单一实现）**

承重 false、owner_pending false，故给终态修法。

**收口点（必须是已存在点）= `core.models.schedule_plan_role._normalize_role`（当前 `schedule_plan_role.py:21`）。** 这是已存在的符号，**不新建模块**（满足收口铁律：不为已有概念建第二模块）。

已采用合法落法 (a)：`schedule_plan_query_service` 从 `core.models.schedule_plan_role` import 既有 `_normalize_role`，删除 service 本地旧 `:28-30` 重复体，`resolve_plan` 调用名保持 `_normalize_role`。

**禁区 / 灵魂线红线（不得触碰）：**
- 绝不动 `resolve_plan` 的双段语义（`schedule_plan_query_service.py:101-104`）：归一后 `if requested_role not in VALID_PLAN_ROLES: raise ValueError(...)` 的「先静默归一再 loud raise」两段式必须原样保留——收口只换 `_normalize_role` 的来源，不改其「空→adopted、非空原样返回」契约。
- 绝不把 service 引用的 `_normalize_role` 改成 view_context 的 `normalize_plan_role`(:65)：后者**带校验会 raise**，:102 当前依赖「先得到归一值、再由 :103 自己判 VALID 抛 ValueError」，换成 :65 会把抛错点前移、改成 ValidationError，属行为变更（见 §7）。
- 不新增兜底 / except 吞错 / 静默回退：本债本就纯 dedup，不引入任何防御逻辑。

## 5) 同文件碰撞债

registry `same_file_siblings` = **空**（主文件 `schedule_plan_role.py` 无同文件兄弟债）。当前权威 `_registry.json` 中 R23 的 `interference_edges=[]`；以下两条仅是历史弱邻接说明，不能当机器边使用：

- **R22**（历史弱邻接 why=`file:view_context.py`）：R22 落在 `schedule_result_view_context.py`。本债不碰 view_context（只动 model + query_service），与 R22 零碰撞；本债病理虽提及 view_context 的第三变体 `normalize_plan_role`(:65)，但修法范围**不含**它，故 R22 与本债可独立先后任意落。
- **R34**（历史弱邻接 why=`file:schedule_plan_query_service.py`）：R34 与本债同住 `schedule_plan_query_service.py`，但权威 registry 不再保留 R23↔R34 机器边。本债最小落法已在 import tuple 增加 `_normalize_role` 一行，同时删除 service 本地重复块（`def` + 两行函数体 + 分隔空行），因此其后锚点**净上移 4 行**（实盘 `get_plan_time_span_for_resolution:210→206`），不是旧口径的 3 行；dataclass 字段无 dict 键位移，纯行号位移。**协调建议：R34 后续按符号重 rg 定位**，避免 patch 命中漂移行。

## 6) 同收口点债

收口点 `_normalize_role`（model :21）目前**无其它债收口至此**（`co_change_within_group` 为空，`prod_consumers` 为本文件 :52 与 service :102 两处调用）。
- 顺序敏感性：无其它债排队收口到此符号，本债是该收口点的**唯一动作**，无「谁先谁后」依赖。
- 唯一需留意的是：view_context `normalize_plan_role`(:65) 是**另一个**概念变体（带校验），**不应**在本债内被一并收口到 `_normalize_role`——那会丢失校验语义（见 §7）。若未来要统一三变体，属新的 P5 决策，需 owner 裁断，不在本档案范围。

## 7) 收口行为差异及所需 parity 测试

**执行前两路（model :21 vs service 旧 :28）字节级一致，收口后逐分支等价、无反例：**
- 输入 `None` → `str(None or "").strip()` = `""` → `"" or ROLE_ADOPTED` = `"adopted"`（两路同）。
- 输入 `""` / `"   "` → `.strip()` = `""` → `"adopted"`（两路同）。
- 输入 `"baseline_best"` → `.strip()` 原样 → 返回 `"baseline_best"`（两路同，**不校验是否在 VALID**，由调用方 :107 后续判）。
- 输入非字符串（如 `0`）→ `str(0 or "")` = `""` → `"adopted"`（两路同）。

故 (a)/(b) 任一收口对 model :21 实现的 dedup **不需要新增 parity 守卫即等价**，行为零变化。

**但必须显式排除「误收口到 view_context :65」的反例（这是本债最易踩的灾难）：**
- `normalize_plan_role`(:65) 对 `"bogus"` 这类非 VALID 值会 `raise ValidationError`；而 `_normalize_role` 对 `"bogus"` **静默返回 `"bogus"`**。
- 在 `resolve_plan` 入口（:102→:103），调用方依赖 `_normalize_role` 的「静默返回」，再由 :103 `if requested_role not in VALID_PLAN_ROLES: raise ValueError(...)` 抛**ValueError**。若误收口到 :65，抛错类型会从 ValueError 变 ValidationError、抛错位置前移——**反例：role=`"bogus"` → 正确路径抛 ValueError、错误路径抛 ValidationError**，属行为变更。
- **已补守卫**：`tests/candidate/test_scheduler_candidate_plan_query_contract.py:69` 断言 `core.models.schedule_plan_role._normalize_role is core.services.scheduler.schedule_plan_query_service._normalize_role`，钉住「单一来源」防未来再分叉。
- **非阻塞澄清**：`baseline_best/critical_best + SOURCE_SCHEDULE` 的“同源比较态”是现有 required 契约保护的合法只读身份，不是 R23 的坏 role 值。它由 `tests/schedule/route_view/test_scheduler_plan_identity_evidence_contract.py`、`tests/gantt/test_scheduler_candidate_gantt_plan_role_contract.py`、`tests/resource_dispatch/test_scheduler_candidate_resource_dispatch_contract.py` 等用例钉住：可保留对比身份，但 `can_write_feedback/can_dispatch` 为 false，逾期标记使用 adopted 历史。R23 只清理 `_normalize_role` 重复，不得顺手把该同源比较态改成 loud reject。

## 8) 爆炸半径

- **生产消费者（prod_consumers，2 处自调用，无外部调用方）**：
  - `core/models/schedule_plan_role.py:52`（`plan_role_label` 内调本模块 `_normalize_role`）。
  - `core/services/scheduler/schedule_plan_query_service.py:102`（`resolve_plan` 入口调从 model import 的 `_normalize_role`）。
  - 跨模块导入私有符号：`schedule_plan_query_service.py:17` 是当前唯一新增 import 边，方向 service→model 合法。
- **测试消费者**：`tests/candidate/test_scheduler_candidate_plan_query_contract.py:69` 直接钉住单一来源；`tests/candidate/test_scheduler_candidate_plan_query_contract.py` 与 `tests/schedule/route_view/test_scheduler_plan_identity_evidence_contract.py` 的 `resolve_plan` 用例继续间接覆盖行为。
- **灾难链（删错 / 统一错会静默炸什么）**：
  1. 若未来误把 service 引用改到 view_context :65（带校验变体）→ `resolve_plan("bogus")` 抛错类型由 ValueError 变 ValidationError → 上游 catch ValueError 的调用方静默漏接 → **错误角色被当合法值放行**（身份族最怕的静默错身份）。
  2. 若 (b) 改名 `normalize_role_or_adopted` 时**漏改 model :52 的调用**→ NameError，但属 loud 失败、即时暴露，非静默。
  3. 若未来改名却漏改 :102 调用名 → 同样 NameError loud 失败。
  - 综合：本债无静默炸点（唯一静默风险来自「误收口到带校验的 :65」，已在 §4 禁区 + §7 反例双重拦截）。
- `fix_invalidation_risk` = none（registry）：纯 dedup，不使任何其它债修法失效。

## 9) 承重毗邻标记

- 本债 `load_bearing=false`、`lb_no_touch=null`。
- registry `lb_colocation_danger` = none：`schedule_plan_role.py` 与 `schedule_plan_query_service.py` 均**不在**承重护栏文件名单（承重的是 `schedule_plan_identity_builder.py`，是 query_service 在 :25 import 的**另一**文件，本债不触碰其内容）。
- **禁区行**：虽非承重，仍守 §4 红线——`schedule_plan_query_service.py:101-104`（`resolve_plan` 的归一→loud raise 双段）为不可改语义区，仅可改 `_normalize_role` 的**符号来源**，不可改其行为。

## 10) 分层 / 导入环风险

- 收口方向 **service → model**（`schedule_plan_query_service` import `core.models.schedule_plan_role`）= **合法层向**，与现有 :9-21 的 `from core.models.schedule_plan_role import (...)` 同向（service 本就已 import 该 model 模块的多个公开符号）。
- **零导入环实证**：`rg "^(from|import)" core/models/schedule_plan_role.py` 仅 `from __future__ import annotations` 与 `from typing import Optional` → model 侧**不 import 任何 service**；`rg "schedule_plan_query_service" core/models/` 为空 → model 层不反向依赖 service。故 service→model 新增（或复用）import 边**不可能成环**。
- 选 (a) 直接 import 私有 `_normalize_role` 不违反分层（仍是 service→model）；选 (b) 提公开名更符合「公开表面」惯例。
- **0 AST 违规**：不引入 `core.algorithms→core.services`、`core.models→core.services` 等不存在的跨层 import。

## 11) 测试耦合

- 动手前**无测试需先迁**：原先 `rg "_normalize_role|normalize_role_or_adopted" tests/` 为空，无测试直接钉该私有符号。
- 间接覆盖 `resolve_plan` 归一行为的回归用例（不锁实现位置，dedup 不会变红，无需预迁）：
  - `tests/candidate/test_scheduler_candidate_plan_query_contract.py`
  - `tests/schedule/route_view/test_scheduler_plan_identity_evidence_contract.py`
- 已新增一条单源守卫测试（见 §7），断言 model 与 service 引用同一 `_normalize_role` 对象，钉死单一来源。
- 回归门：已跑上述 resolve_plan 契约测试 + view_context 相关 `tests/schedule/summary/test_schedule_result_view_context.py`（确认未误动 :65）。

## 12) 前置依赖

- registry `planned_deps_hint` = **none**；`co_change_within_group` 空；`fix_invalidation_risk` none。
- **无前置债**：可独立最先落，是 B02 桶（Batch-4）内零风险第一刀；`planned_batch_hint` 明确「Batch-1 之后任意时点可落、作本桶热身」。
- **唯一坐标提醒**（非 registry 边、非硬依赖）：R34 后续若读取 `schedule_plan_query_service.py` 锚点，须知道本债最小落法让下游锚点净上移 4 行（`get_plan_time_span_for_resolution:210→206`），执行时必须按符号重 rg，禁套旧 3 行漂移口径。与 R22（view_context.py）无序约束。
- 门禁前提：仅需 Batch-1（基线/AST 门）通过即可动手，无需等其它身份族债。

---

## 对抗核验（第二双眼睛）

> 核验人：独立对抗 agent；2026-06-05 初核，2026-06-08 B 执行后按现盘补登；只读不改；默认怀疑、找不到反证才 PASS。

**重 grep 命令与结果（全部独立回盘，未信 registry 行号）：**

```
rg -n "_normalize_role" core/ web/ data/
→ core/models/schedule_plan_role.py:21 (def) / :52 (调用)
→ core/services/scheduler/schedule_plan_query_service.py:17 (import) / :102 (调用)

rg -n "normalize_plan_role" core/ web/ data/
→ core/services/scheduler/schedule_result_view_context.py:65 (def) / :74 (调用)
→ resource_dispatch_service.py:32 (import) / :271 / :355
→ resource_dispatch_execution_service.py:16 (import) / :77

rg -n "normalize_role_or_adopted" core/ web/ data/ tests/   → 空 (exit 1)
rg -n "_normalize_role|normalize_role_or_adopted" tests/    → tests/candidate/test_scheduler_candidate_plan_query_contract.py:69-73 单源守卫
rg -n "from core.models.schedule_plan_role import" …        → query_service import tuple 已含私有 _normalize_role，方向 service→model 合法
git diff HEAD^ -- core/services/scheduler/schedule_plan_query_service.py → 旧 :28-30 本地重复体删除，新增 :17 import
git diff HEAD^ -- tests/candidate/test_scheduler_candidate_plan_query_contract.py → 新增 :69-73 单源守卫
```

**逐项裁决：**
- **行号复核：已补登到 B 终态。** model :21（体22-23）保留唯一实现；service 旧 :28-30 本地重复体已删；service :17 import model `_normalize_role`，:102 调用；view_context:65 第三变体未动。
- **收口点真实存在：是。** `core.models.schedule_plan_role._normalize_role`@:21 为已存在符号；service 在 :9-21 已 `from core.models.schedule_plan_role import (...)`，收口 service→model 为合法层向、不新建模块、不可能成环（model 仅 import typing）。
- **resolve_plan 双段语义复核：属实。** :102 `requested_role = _normalize_role(role)`（静默归一）→ :103-104 `if not in VALID_PLAN_ROLES: raise ValueError(...)`（loud raise）。收口仅换 :102 符号来源、不动该双段，结论成立。
- **误收口反例复核：成立。** view_context `normalize_plan_role`@:65-70 确带 `VALID_PLAN_ROLES` 校验并 `raise ValidationError`，与 `_normalize_role` 的静默返回语义不同。§4 禁区 + §7 反例拦截有效。
- **承重误删风险：无。** lb=false、lb_no_touch=null；非删除/透传/加形参，是合法 dedup 到已存在收口点。
- **owner_pending 复核：无擅自越权。** owner_pending=false，给终态修法合规。

**对档案的一处补强（非纠错，未与原文矛盾）：** §6 提到 view_context 第三变体时仅以 view_context 内部口吻描述，实测 `normalize_plan_role` 另有 **3 个生产外部消费者**（`resource_dispatch_service.py:271/:355`、`resource_dispatch_execution_service.py:77`）+ 2 处 import。这进一步加重「绝不可在本债内误统一三变体」的爆炸面，与 §4/§6/§7 结论同向。建议 owner 若未来启动「三变体统一」P5 时，把这 3 个消费者纳入爆炸半径评估。

**核验结论：PASS。** B 终态已完成 dedup：旧重复体删除、service 复用 model 真相源、单源守卫已补、坏值仍由 `resolve_plan` loud `ValueError`；无承重误删风险，无 owner 越权，无需第三方裁断。
