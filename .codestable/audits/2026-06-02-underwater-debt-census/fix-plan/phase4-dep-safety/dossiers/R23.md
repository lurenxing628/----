# R23 逐债档案 — `_normalize_role` 字节级重复，无 plan_role 归一收口点

> 主文件: `core/models/schedule_plan_role.py`；干扰簇 C01；桶 B02（身份族）；病理 P5；严重度 low；承重 false；owner_pending false
> 本档案只读不改，所有断言附当前工作区 file:line 证据（2026-06-05 回盘）。

---

## 1) 当前真实 file:line（回盘）+ 漂移记录

按符号名 `rg -n "_normalize_role" core/ web/ data/` 回盘，四处全部命中，行号与 registry `current_evidence_2026_06_05` / `phase1_blast.location_status` 完全一致：

| 角色 | 符号 | registry 老线索 | 当前实际 file:line | 漂移 |
|---|---|---|---|---|
| 定义 A（model 侧） | `def _normalize_role` | `schedule_plan_role.py:21-23` | `core/models/schedule_plan_role.py:21`（体 :22-23） | **无** |
| 定义 B（service 侧，重复体） | `def _normalize_role` | `schedule_plan_query_service.py:28-30` | `core/services/scheduler/schedule_plan_query_service.py:28`（体 :29-30） | **无** |
| 调用 A | `_normalize_role(role)` | — | `core/models/schedule_plan_role.py:52`（`plan_role_label` 内） | — |
| 调用 B | `_normalize_role(role)` | — | `core/services/scheduler/schedule_plan_query_service.py:106`（`resolve_plan` 入口） | — |
| 第三变体（带校验，raise） | `def normalize_plan_role` | `view_context.py:65` | `core/services/scheduler/schedule_result_view_context.py:65` | **无** |

**字节级重复实证**（两处函数体逐字符相同）：
- model `schedule_plan_role.py:21-23`：`text = str(role or "").strip()` / `return text or ROLE_ADOPTED`
- service `schedule_plan_query_service.py:29-30`：`text = str(role or "").strip()` / `return text or ROLE_ADOPTED`

**结论：零漂移。** `git diff core/services/scheduler/schedule_plan_query_service.py` 当前为空（已落盘/已 staged），:28-30 这段未被工作树改动；registry 所述「工作树漂移落在 :86 build_plan_identity 调用区」对本债无影响。

## 2) 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05` = **planned**；`current_evidence` 明确「未动手」。
- 回盘佐证「未动手」：两处定义仍并存且字节相同；`rg "normalize_role_or_adopted"` 全空（公开名未提出）；`rg "_normalize_role" tests/` 全空（无单源守卫）；service 侧未 `from core.models.schedule_plan_role import _normalize_role`（`rg` 跨模块导入私有符号为空）。
- **状态一致：planned，与 evidence 无矛盾。**

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P5（重复/缺收口点）**：`_normalize_role`（空→adopted 的归一逻辑）在 model 与 service 两处字节级重复，全仓**无 plan_role 归一的统一收口点**。`enum_normalizers` / `normalization_matrix` 经 grep 实证不含 plan_role 逻辑（registry `pathology` 字段已实证）。
- **严重度 low**：纯结构重复，今日两版行为完全一致，无现网行为差；唯一风险是「改一处漏另一处」的未来漂移。
- **桶 B02（scheduler-plan-identity 身份族）/ home B02**。
- **簇 C01**：通过 `interference_edges` 与 R22（同在 view_context.py）、R34（同在 query_service.py）成同文件邻接，但 `same_symbol=false`。
- 注意：本债病理涉及**三个**plan_role 归一变体——model `_normalize_role`(:21)、service `_normalize_role`(:28)、view_context `normalize_plan_role`(:65)。前两者静默归一（无校验），第三者带 `VALID_PLAN_ROLES` 校验并 `raise ValidationError`（见 §7）。本债的收口范围仅限**前两者的字节级重复**，第三者语义不同，不在合并范围。

## 4) 修法类 — **收口（dedup 到单一实现）**

承重 false、owner_pending false，故给终态修法。

**收口点（必须是已存在点）= `core.models.schedule_plan_role._normalize_role`（当前 `schedule_plan_role.py:21`）。** 这是已存在的符号，**不新建模块**（满足收口铁律：不为已有概念建第二模块）。

两种合法落法（二选一，无新建例外）：
- **(a) 最小改动**：`schedule_plan_query_service` `from core.models.schedule_plan_role import _normalize_role`，删 service 本地 `:28-30`，:106 调用名不变。
- **(b) 更干净（推荐）**：把 model 侧 `_normalize_role` 提为公开名 `normalize_role_or_adopted`、加进 `schedule_plan_role.py:79` 的 `__all__`，service import 公开名。其余调用点（model :52、service :106）随之改名。

**禁区 / 灵魂线红线（不得触碰）：**
- 绝不动 `resolve_plan` 的双段语义（`schedule_plan_query_service.py:105-108`）：归一后 `if requested_role not in VALID_PLAN_ROLES: raise ValueError(...)` 的「先静默归一再 loud raise」两段式必须原样保留——收口只换 `_normalize_role` 的来源，不改其「空→adopted、非空原样返回」契约。
- 绝不把 service `_normalize_role`(:28) 改成 view_context 的 `normalize_plan_role`(:65)：后者**带校验会 raise**，:106 当前依赖「先得到归一值、再由 :107 自己判 VALID 抛 ValueError」，换成 :65 会把抛错点前移、改成 ValidationError，属行为变更（见 §7）。
- 不新增兜底 / except 吞错 / 静默回退：本债本就纯 dedup，不引入任何防御逻辑。

## 5) 同文件碰撞债

registry `same_file_siblings` = **空**（主文件 `schedule_plan_role.py` 无同文件兄弟债）。`interference_edges` 给出两条跨文件邻接（`same_symbol=false`）：

- **R22**（边 why=`file:view_context.py`）：R22 落在 `schedule_result_view_context.py`。本债**若选 (a) 不碰 view_context**（只动 model + query_service），与 R22 零碰撞；本债病理虽提及 view_context 的第三变体 `normalize_plan_role`(:65)，但修法范围**不含**它，故 R22 与本债可独立先后任意落。
- **R34**（边 why=`file:schedule_plan_query_service.py`）：R34 与本债同住 `schedule_plan_query_service.py`。本债在该文件**只删 :28-30 三行 + 改 :106 一个调用名**（若选 (b)）。删 :28-30 会使其后所有行号**上移 3 行**（dataclass 字段无 dict 键位移，纯行号位移）。**协调建议：本债先落（删行）→ 再落 R34**，让 R34 基于删后行号定位，避免 R34 patch 命中漂移行。若 R34 先落，本债需基于 R34 改后的工作树重新回盘 :28 位置（成本极低，因 :28-30 在文件顶部、R34 大概率动文件中下部）。

## 6) 同收口点债

收口点 `_normalize_role`（model :21）目前**无其它债收口至此**（`co_change_within_group` 为空，`prod_consumers` 仅本文件 :52 与 service :106 两处自调用）。
- 顺序敏感性：无其它债排队收口到此符号，本债是该收口点的**唯一动作**，无「谁先谁后」依赖。
- 唯一需留意的是：view_context `normalize_plan_role`(:65) 是**另一个**概念变体（带校验），**不应**在本债内被一并收口到 `_normalize_role`——那会丢失校验语义（见 §7）。若未来要统一三变体，属新的 P5 决策，需 owner 裁断，不在本档案范围。

## 7) 收口行为差异及所需 parity 测试

**收口的两路（model :21 vs service :28）今日字节级一致，逐分支等价、无反例：**
- 输入 `None` → `str(None or "").strip()` = `""` → `"" or ROLE_ADOPTED` = `"adopted"`（两路同）。
- 输入 `""` / `"   "` → `.strip()` = `""` → `"adopted"`（两路同）。
- 输入 `"baseline_best"` → `.strip()` 原样 → 返回 `"baseline_best"`（两路同，**不校验是否在 VALID**，由调用方 :107 后续判）。
- 输入非字符串（如 `0`）→ `str(0 or "")` = `""` → `"adopted"`（两路同）。

故 (a)/(b) 任一收口对 model :21 实现的 dedup **不需要新增 parity 守卫即等价**，行为零变化。

**但必须显式排除「误收口到 view_context :65」的反例（这是本债最易踩的灾难）：**
- `normalize_plan_role`(:65) 对 `"bogus"` 这类非 VALID 值会 `raise ValidationError`；而 `_normalize_role` 对 `"bogus"` **静默返回 `"bogus"`**。
- 在 `resolve_plan` 入口（:106→:107），调用方依赖 `_normalize_role` 的「静默返回」，再由 :107 `if requested_role not in VALID_PLAN_ROLES: raise ValueError(...)` 抛**ValueError**。若误收口到 :65，抛错类型会从 ValueError 变 ValidationError、抛错位置前移——**反例：role=`"bogus"` → 旧路抛 ValueError、新路抛 ValidationError**，属行为变更。
- **所需守卫（建议补，非强制）**：在 `tests/` 增一条单源守卫——断言 `core.models.schedule_plan_role._normalize_role is core.services.scheduler.schedule_plan_query_service._normalize_role`（选 (a) 时为 `is`；选 (b) 改名后断言 service 引用的就是 model 公开符号），钉住「单一来源」防未来再分叉。当前 `rg tests/` 实证此守卫**不存在**。

## 8) 爆炸半径

- **生产消费者（prod_consumers，2 处自调用，无外部调用方）**：
  - `core/models/schedule_plan_role.py:52`（`plan_role_label` 内调本模块 `_normalize_role`）。
  - `core/services/scheduler/schedule_plan_query_service.py:106`（`resolve_plan` 入口调本模块 `_normalize_role`）。
  - 跨模块导入私有符号：`rg "from core.models.schedule_plan_role import ... _normalize_role"` **当前为空** → 没有任何外部模块已 import 此私有符号，收口 (a) 是「新增第一条 import 边」，零现存破坏面。
- **测试消费者**：`test_consumers` = 空；`rg "_normalize_role" tests/` 空。行为变化只会被 `tests/candidate/test_scheduler_candidate_plan_query_contract.py`、`tests/schedule/route_view/test_scheduler_plan_identity_evidence_contract.py` 的 `resolve_plan` 用例**间接**捕获，且它们不锁实现位置 → dedup 不会变红（registry `contract_topology_tests` 已实证）。
- **灾难链（删错 / 统一错会静默炸什么）**：
  1. 若 dedup 时**误把 service :28 收口到 view_context :65**（带校验变体）→ `resolve_plan("bogus")` 抛错类型由 ValueError 变 ValidationError → 上游 catch ValueError 的调用方静默漏接 → **错误角色被当合法值放行**（身份族最怕的静默错身份）。
  2. 若 (b) 改名 `normalize_role_or_adopted` 时**漏改 model :52 的调用**→ NameError，但属 loud 失败、即时暴露，非静默。
  3. 删 service :28-30 后**忘删/忘改 :106 调用名**（选 (b) 时）→ 同样 NameError loud 失败。
  - 综合：本债无静默炸点（唯一静默风险来自「误收口到带校验的 :65」，已在 §4 禁区 + §7 反例双重拦截）。
- `fix_invalidation_risk` = none（registry）：纯 dedup，不使任何其它债修法失效。

## 9) 承重毗邻标记

- 本债 `load_bearing=false`、`lb_no_touch=null`。
- registry `lb_colocation_danger` = none：`schedule_plan_role.py` 与 `schedule_plan_query_service.py` 均**不在**承重护栏文件名单（承重的是 `schedule_plan_identity_builder.py`，是 query_service 在 :25 import 的**另一**文件，本债不触碰其内容）。
- **禁区行**：虽非承重，仍守 §4 红线——`schedule_plan_query_service.py:105-108`（`resolve_plan` 的归一→loud raise 双段）为不可改语义区，仅可改 :106 调用的**符号来源**，不可改其行为。

## 10) 分层 / 导入环风险

- 收口方向 **service → model**（`schedule_plan_query_service` import `core.models.schedule_plan_role`）= **合法层向**，与现有 :9-20 已有的 `from core.models.schedule_plan_role import (...)` 同向（service 本就已 import 该 model 模块的多个公开符号）。
- **零导入环实证**：`rg "^(from|import)" core/models/schedule_plan_role.py` 仅 `from __future__ import annotations` 与 `from typing import Optional` → model 侧**不 import 任何 service**；`rg "schedule_plan_query_service" core/models/` 为空 → model 层不反向依赖 service。故 service→model 新增（或复用）import 边**不可能成环**。
- 选 (a) 直接 import 私有 `_normalize_role` 不违反分层（仍是 service→model）；选 (b) 提公开名更符合「公开表面」惯例。
- **0 AST 违规**：不引入 `core.algorithms→core.services`、`core.models→core.services` 等不存在的跨层 import。

## 11) 测试耦合

- 动手前**无测试需先迁**：`rg "_normalize_role|normalize_role_or_adopted" tests/` 为空，无测试直接钉该私有符号。
- 间接覆盖 `resolve_plan` 归一行为的回归用例（不锁实现位置，dedup 不会变红，无需预迁）：
  - `tests/candidate/test_scheduler_candidate_plan_query_contract.py`
  - `tests/schedule/route_view/test_scheduler_plan_identity_evidence_contract.py`
- 建议**新增**（非前置、收口后补）：一条单源守卫测试（见 §7），断言 model 与 service 引用同一 `_normalize_role` 对象，钉死单一来源。
- 回归门：落后跑上述两条 resolve_plan 契约测试 + view_context 相关 `tests/schedule/summary/test_schedule_result_view_context.py`（确认未误动 :65）即可。

## 12) 前置依赖

- registry `planned_deps_hint` = **none**；`co_change_within_group` 空；`fix_invalidation_risk` none。
- **无前置债**：可独立最先落，是 B02 桶（Batch-4）内零风险第一刀；`planned_batch_hint` 明确「Batch-1 之后任意时点可落、作本桶热身」。
- **唯一软排序建议**（非硬依赖）：与同文件兄弟 **R34**（同住 query_service.py）协调时，本债**先落**（删 :28-30 三行）可让 R34 基于删后行号定位，避免行号漂移；反之亦可，仅需重新回盘。与 R22（view_context.py）无序约束。
- 门禁前提：仅需 Batch-1（基线/AST 门）通过即可动手，无需等其它身份族债。

---

## 对抗核验（第二双眼睛）

> 核验人：独立对抗 agent；2026-06-05；只读不改；默认怀疑、找不到反证才 PASS。

**重 grep 命令与结果（全部独立回盘，未信 registry 行号）：**

```
rg -n "_normalize_role" core/ web/ data/
→ core/models/schedule_plan_role.py:21 (def) / :52 (调用)
→ core/services/scheduler/schedule_plan_query_service.py:28 (def) / :106 (调用)

rg -n "normalize_plan_role" core/ web/ data/
→ core/services/scheduler/schedule_result_view_context.py:65 (def) / :74 (调用)
→ resource_dispatch_service.py:32 (import) / :271 / :355
→ resource_dispatch_execution_service.py:16 (import) / :77

rg -n "normalize_role_or_adopted" core/ web/ data/ tests/   → 空 (exit 1)
rg -n "_normalize_role|normalize_role_or_adopted" tests/    → 空 (exit 1)
rg -n "from core.models.schedule_plan_role import" …        → 25 处全为公开符号(VALID_PLAN_ROLES/ROLE_ADOPTED/SOURCE_*/plan_role_label 等)，无一 import 私有 _normalize_role
git diff core/services/scheduler/schedule_plan_query_service.py → 空（:28-30 未被工作树改动）
git diff core/models/schedule_plan_role.py → 仅 :53 一行（plan_role_label 的兜底文案改"未知方案身份"，与本债 :21-23 无关）
```

**逐项裁决：**
- **行号复核：一致。** model :21（体22-23）、service :28（体29-30）、model调用:52、service调用:106、view_context:65 五处全准，零漂移。Read 实证两处函数体逐字符相同。
- **收口点真实存在：是。** `core.models.schedule_plan_role._normalize_role`@:21 为已存在符号；service 在 :9 已 `from core.models.schedule_plan_role import (...)`，收口 service→model 为合法层向、不新建模块、不可能成环（model 仅 import typing）。
- **resolve_plan 双段语义复核：属实。** :106 `requested_role = _normalize_role(role)`（静默归一）→ :107-108 `if not in VALID_PLAN_ROLES: raise ValueError(...)`（loud raise）。收口仅换 :106 符号来源、不动该双段，结论成立。
- **误收口反例复核：成立。** view_context `normalize_plan_role`@:65-70 确带 `VALID_PLAN_ROLES` 校验并 `raise ValidationError`，与 `_normalize_role` 的静默返回语义不同。§4 禁区 + §7 反例拦截有效。
- **承重误删风险：无。** lb=false、lb_no_touch=null；非删除/透传/加形参，是合法 dedup 到已存在收口点。
- **owner_pending 复核：无擅自越权。** owner_pending=false，给终态修法合规。

**对档案的一处补强（非纠错，未与原文矛盾）：** §6 提到 view_context 第三变体时仅以 view_context 内部口吻描述，实测 `normalize_plan_role` 另有 **3 个生产外部消费者**（`resource_dispatch_service.py:271/:355`、`resource_dispatch_execution_service.py:77`）+ 2 处 import。这进一步加重「绝不可在本债内误统一三变体」的爆炸面，与 §4/§6/§7 结论同向。建议 owner 若未来启动「三变体统一」P5 时，把这 3 个消费者纳入爆炸半径评估。

**核验结论：PASS。** 行号、字节级重复、收口点存在性、收口等价性、误收口反例、分层/导入环、测试零钉死均独立复证；无承重误删风险，无 owner 越权，无需第三方裁断。
