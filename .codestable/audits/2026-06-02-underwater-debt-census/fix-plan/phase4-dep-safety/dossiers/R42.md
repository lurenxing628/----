# R42 逐债档案 — plan_id 死面包屑全链下线（依赖分析与安全批次计划）

> 本档案只读不改、只产计划。grep 回盘日期 2026-06-05。债 id R42 / 桶 B01 / 簇 C01 / 病理 P6（死码直删类）/ 严重度 medium / 承重 false / owner_pending false。

## 1) 当前真实 file:line（回盘）+ 漂移记录

按符号名 `\bplan_id\b` 全仓回盘，逐点对账 registry 声称值：

| 角色 | 文件 | 符号 | registry 声称 | 实际回盘 | 漂移 |
|---|---|---|---|---|---|
| 读入(scheduler) | `web/navigation_context.py` | `plan_id=_request_arg("plan_id")` | :77/:86 | **:78** | 轻微(±1~8行)。registry 双口径(current_evidence:77、old_location:86)均不准 |
| 读入(reports) | `web/routes/reports_page_support.py` | `plan_id=_request_text("plan_id")` (build_report_context 内) | :98/:104 | **:104** | current_evidence:104 ✅准；old_location:97/98 偏 |
| 读入(reports底部) | `web/routes/reports_page_support.py` | `build_report_context(plan_id=...)` | :135/:143 | **:143** | current_evidence:143 ✅准；old_location:134/135 偏 |
| 存储形参 | `web/viewmodels/scheduler_workbench_links.py` | `build_workbench_plan_context(... plan_id ...)` | :187/:190 形参 | 形参 **:191**，函数定义头 **:187** | 形参实为:191（:187 是 `def` 行） |
| 存储 dict | `web/viewmodels/scheduler_workbench_links.py` | `"plan_id": _text(plan_id) or None` | :229/:232 | **:233** | 轻微(+1~4行) |
| 透传形参 | `web/viewmodels/scheduler_reports_workbench.py` | `build_report_context(... plan_id ...)` | :60/:61 | 形参 **:60**，透传 `plan_id=plan_id` **:79** | :60 ✅准；透传实为:79（registry 写:76 偏） |
| 回吐URL(常规) | `web/viewmodels/scheduler_workbench_link_query.py` | `_append_plan_query` → `_append_param(query,"plan_id",...)` | :118 | **:118** | ✅完全准 |
| 回吐URL(execution_review) | `web/viewmodels/scheduler_workbench_link_query.py` | `_append_target_plan_query` 内 | :154 | **:154** | ✅完全准 |
| 字段表白名单 | `web/viewmodels/scheduler_navigation_links.py` | `"plan_id"` ×2 | :12/:52 | **:12 / :52** | ✅完全准 |
| 导出字段表 | `web/routes/reports_export_support.py` | `_EXPORT_CONTEXT_KEYS` 含 `"plan_id"` | :14 | **:14**（表头 :13） | ✅准 |
| 读入(dashboard) | `web/routes/dashboard.py` | `"plan_id": _request_arg("plan_id")` | :172 | **:241** | ⚠️**大漂移 +69 行**。registry:172 完全失准；实际在 :241 的 context dict 内 |
| 透传(dashboard_workbench) | `web/viewmodels/dashboard_workbench.py` | `plan_id=_text(filters.get("plan_id"))` | :121 | **0 命中** | ⚠️**该点已不存在**。registry phase1_blast/planned_fix 反复引 :121，回盘 `\bplan_id\b` 在该文件 0 命中——此消费点要么已被前序改动删除，要么从来是误记。**修法清单不得再含此点** |

死码证明回盘：`git grep -nP '\bplan_id\b' -- core/ data/` = **0 命中**（registry 声称的 143 个 substring 均为 `plan_identity`/`plan_idempotency`/`_plan_idempotency_keys`，与本债无关）。✅死面包屑结论成立——plan_id 全程不进任何 resolver/query/计算。

**漂移总结**：有漂移。轻微漂移多处（±1~8 行，整体未做大改）；**两处实质漂移**：dashboard.py :172→:241（+69 行，registry 严重失准）、dashboard_workbench.py:121 消费点**整点消失**（修法范围须收缩）。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05` = **planned**；本债今天回盘后仍 planned（全链 plan_id 读入/存储/回吐/字段表/契约测试三断言**全部原样在位**，无任何下线动作发生）。✅状态与 evidence 一致。
- 但 evidence 内**两条事实已过期**：①dashboard.py 行号(:172)失准；②dashboard_workbench.py:121 消费点已消失。状态标签正确，证据需更正。
## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P6（死码直删类）**：plan_id 从 request 读入 → 存进工作台上下文 dict（`scheduler_workbench_links.py:233`）→ 回吐进所有导航/导出 URL 与隐藏表单（`scheduler_workbench_link_query.py:118/154`），却从不进任何 resolver/query/版本身份解析。版本身份全程靠 `version + plan_role + scenario_id`（见 `navigation_context.py:73-80`）。
- **对照同行 back_to（真消费）**：back_to 确实驱动跳转（contract 测试 `:123/:127/:239/:292` 均断言 back_to 回跳目标真实生效），plan_id 无任何等价消费 = 真死面包屑。
- **严重度 medium**：无功能依赖、误删风险低；唯一痛点是误导维护者 + 被在途 roadmap 制度化（items.yaml:289）供养。
- **桶 B01 / 簇 C01**：B01 内第 3 顺位（与 R60 同动作）。

## 4) 修法类

**P6 纯直删（与 R60 合并为一次 plan_id 下线，rebase 在 R54 新签名之后）**。无收口点（这是直删债，不是收口债）。修法清单（已按 §1 回盘修正行号、剔除消失点）：

删读入：`navigation_context.py:78`、`reports_page_support.py:104` 与 `:143`、`dashboard.py:241`（**注意非 :172**）。
删存储：`scheduler_workbench_links.py:191`（形参）+ `:233`（dict 键）。
删透传：`scheduler_reports_workbench.py:60`（形参）+ `:79`（透传实参）。
删回吐：`scheduler_workbench_link_query.py:118`（`_append_plan_query` 内一行）+ `:154`（`_append_target_plan_query` execution_review 分支内一行）。
删字段表：`scheduler_navigation_links.py:12` 与 `:52`、`reports_export_support.py:14`（由 R60 处置字段表承认面，二者 MUST 同删）。
**剔除**：~~`dashboard_workbench.py:121`~~（§1 已证消失，不在清单）。
同步退契约测试三断言（§11）。同步删 roadmap 制度化（§4 注：仅 items.yaml:289 一处真实存在，详见 §8）。

**灵魂线红线**：纯删，不加任何兜底/静默回退/except 吞错/过度防御。删 plan_id 形参不得改默认值掩盖、不得保留空壳键透传。删前已 grep 证 back_to（真消费）不在删除链内、不受影响。
**承重红线**：见 §9 —— 删 `navigation_context.py:78` 那一行时，**绝不碰** 同函数 :79（`plan_role if ... else ROLE_ADOPTED` 强制）与 :80（scenario_id）——但回盘发现 R56/R57/LB06 已 fixed，护栏现态见 §9。
## 5) 同文件碰撞债

> ⚠️**重大回盘发现**：registry 把 R56/R57/LB06 列为 R42 的活跃同文件兄弟，但今天回盘 `status_2026_06_05`：**R56=fixed、R57=fixed、LB06=fixed**。只有 **R60 仍 planned**。任务交底里的「同文件兄弟债 [LB06,R56,R57,R60]」中三条已落地，碰撞协调面大幅收窄。

| sibling | 文件/段 | 状态 | 改什么 | 谁先谁后 | 撞行号/键位移风险 |
|---|---|---|---|---|---|
| **R60** | `scheduler_workbench_link_query.py:118/154` + `navigation_links.py:12/52` + `export_support.py:14` 字段表 | **planned** | R60 = R42 的「字段表承认面」，共享同一组 URL-emit 点 | **必须同批次同动作**（B01 内合并为单次 plan_id 下线） | **高**：只删 R42 的读入/存储而留 R60 字段表 = 留半截残渣（新 P3）；二者 emit 点重叠（link_query:118/154 既是 R42 回吐也是 R60 承认面），分两次删会撞键位移 |
| **R56** | `navigation_context.py:79`（plan_role 强制 ROLE_ADOPTED 护栏，已补注释/契约） | **fixed** | 已落地 | — | 删 R42 的 :78 时**仍绝不碰** :79；R56 已 fixed 意味护栏注释/测试已就位，删 plan_id 行更安全但不得连带改护栏 |
| **R57** | `navigation_context.py` fallback 双构建路径 raw plan_role/scenario_id 信任 | **fixed** | 已落地 | — | 已收敛；删 plan_id 不触及 plan_role/scenario_id 解析路径 |
| **LB06** | `reports_page_support.py` execution-review 写死 plan_role='adopted'/scenario_id=None 承重护栏（已补注释） | **fixed** | 已落地 | — | 删 `reports_page_support.py:104/:143` 的 plan_id 形参时**绝不碰** build_report_context 内的 adopted/scenario_id 强制分支 |

**协调结论**：R42 唯一须严格同批次的活跃兄弟是 **R60**（强 co-change，详见 §6/§8）。R56/R57/LB06 已 fixed → 退化为「禁区行」约束（§9），不再是顺序协调对象。

## 6) 同收口点债

本债是 **P6 直删，无收口点**——不存在「收口到同一符号」的语义。但有**同符号 co-change**（registry interference_edge `R60 same_symbol=true, sym:build_workbench_plan_context`）：

- R42 删 `build_workbench_plan_context` 的 plan_id 形参(:191)+dict 键(:233)；R60 删该函数下游字段表对 plan_id 的承认。二者操作同一数据流的两端。
- **顺序敏感**：必须**先合并成一次提交/一个批次**，不存在「先 R42 后 R60」的安全中间态——任何中间态都遗留半截 plan_id（要么键存着没人 emit，要么 emit 引用已删的键）。
- **R54 同符号但非同收口**：R54 在 `build_workbench_plan_context` **加** guard 字段（`guardrail_text`/`guardrail_reason_type`，回盘已见于 :209-210，疑似部分已落但 status 仍 planned）。R42 删 plan_id 形参会让 R54 的形参列表 rebase。顺序：**R54 先（Batch-2 guard 收口）→ R42/R60 后（Batch-3）rebase 在 R54 新签名上**。
## 7) 收口行为差异及所需 parity 测试

本债是**直删**，不是收口（无新旧两路分支等价问题）。但删除会改变**外部可观测的 URL/隐藏表单形态**，需「删除前后行为 parity」保障：

- **删除前**：导航/导出 URL 携带 `plan_id=PLAN-RPT`（contract 测试 :109 fixture / :122 query 断言 / :126 hidden_inputs 断言）。
- **删除后**：URL 不再含 plan_id，**但 version/plan_role/scenario_id/back_to 必须逐字不变**。
- **所需 parity 断言**：删除后跑 contract 测试，须确认——①`query["plan_id"]` 键消失（不是变空、不是 None 字符串）；②`query["back_to"]`、`query["plan_role"]`、`query["version"]` 与删除前**完全一致**（防止误删波及相邻 `_append_param` 调用，link_query:117/119/120 三行紧邻 plan_id 行）；③hidden_inputs 同理只少 plan_id。
- **反例（必须排除）**：删 `_append_param(query,"plan_id",...)` 时若错删整段或改了相邻 version/plan_role 行 → URL 丢身份参数 → 版本解析静默错位（version 是真消费者）。这是本债**唯一灾难链**（详见 §8）。
- execution_review 分支（link_query:151-157）有独立 plan_id emit（:154），与常规分支(:118)**两处都要删**，parity 测试须覆盖 execution_review 与常规两种 plan_style。

## 8) 爆炸半径

**生产消费者清单（回盘后真实清单，已剔除消失点）**：
1. `web/navigation_context.py:78`（scheduler 链读入）
2. `web/routes/reports_page_support.py:104` + `:143`（reports 链读入 ×2）
3. `web/routes/dashboard.py:241`（dashboard 链读入，**registry:172 失准**）
4. `web/viewmodels/scheduler_workbench_links.py:191`（形参）+ `:233`（dict 存储）
5. `web/viewmodels/scheduler_reports_workbench.py:60`（形参）+ `:79`（透传）
6. `web/viewmodels/scheduler_workbench_link_query.py:118` + `:154`（URL 回吐 ×2）
7. `web/viewmodels/scheduler_navigation_links.py:12` + `:52`（字段表白名单 ×2，R60 承认面）
8. `web/routes/reports_export_support.py:14`（导出字段表，R60 承认面）
9. ~~`web/viewmodels/dashboard_workbench.py:121`~~ **已消失，不在半径内**

**测试消费者**：`tests/web_pages/test_reports_workbench_navigation_contract.py` 三断言：:109(fixture URL 含 plan_id) / :122(query 断言) / :126(hidden_inputs 断言)。全仓 plan_id 测试足迹**仅此 1 文件 3 行**，不退则 CI 红。

**制度化（roadmap）⚠️回盘订正**：registry 声称 3 处（items.yaml:289 + acceptance.md:52 + checklist.yaml:82）。回盘实况：
- ✅`.codestable/roadmap/aps-frontend-workbench/aps-frontend-workbench-items.yaml:289` **真实存在**（"…保留 plan_id 与 back_to"）。
- ❌`acceptance.md:52` —— 该 roadmap 目录下 **acceptance.md 内 0 个 plan_id**（grep 0 命中）。
- ❌`checklist.yaml:82` —— 该 roadmap 目录下**根本没有 checklist 文件**（`*checklist*` no matches）。
- **结论**：制度化只 **1 处真实**（items.yaml:289），registry 另 2 处是幻觉/已删。修法只需同步删 items.yaml:289 一行（且需先与在途 workbench roadmap owner 对齐，因触碰 in-progress feature 契约）。

**灾难链（删错/统一错静默炸什么）**：
- **C1（最危）**：删 link_query:118/154 的 plan_id 行时误伤相邻 `_append_param(version/plan_role/scenario_id)`（紧邻同函数）→ 导航 URL 丢真身份参数 → version 解析静默错位、报表打开错版本。**静默**，无报错。
- **C2**：只删 R42（读入/存储/回吐）而留 R60 字段表 → 半截 plan_id 残渣（字段表承认一个无人写入的键）→ 新 P3 债。
- **C3**：不退 contract 三断言 → CI 红（显式失败，非静默，风险可控）。
- **C4**：删 items.yaml:289 制度化项失败 → 未来 LLM 看 roadmap 重新供养 plan_id，本次删了下次长回来（失忆债）。
## 9) 承重毗邻标记

R42 本身 `load_bearing=false`、`lb_no_touch=null`。但**双承重同居**：
- `navigation_context.py` 同居 R56（plan_role 强制护栏，已 fixed）、R57（已 fixed）。
- `reports_page_support.py` 同居 LB06（execution-review 写死 plan_role='adopted'/scenario_id=None 承重护栏，已 fixed）。

**禁区行（删 plan_id 时绝不顺手碰）**：
- `web/navigation_context.py:79` —— `plan_role=plan_role if plan_role in VALID_PLAN_ROLES else ROLE_ADOPTED`（R56 承重，is_execution_review/adopted 强制语义）。
- `web/navigation_context.py:80` —— `scenario_id=scenario_id`（与上同函数 `current_workbench_navigation_context`，承重数据面）。
- `web/routes/reports_page_support.py` build_report_context 内的 execution-review → plan_role='adopted'/scenario_id=None 强制分支（LB06 承重）。
- PHASE0§5 明示：plan_id 死面包屑清理**不得碰** execution_review 强制 adopted 分支。删 :78 那一行 `plan_id=_request_arg(...)` 在语法上独立于 :79/:80（是 build_workbench_plan_context 的不同关键字实参），删它不影响护栏——但**严禁**借机「顺手统一/简化」相邻护栏行。

## 10) 分层 / 导入环风险

**零越层风险**。本债全在 web 层内部（web/navigation_context、web/routes/*、web/viewmodels/*），是**删除**操作不是新增 import。删除只减少 web 层内部数据流，不引入任何 `core.algorithms→core.services`、`core.models→core.services` 越层，也不新建模块（无新收口点）。AST 违规 0。无导入环风险（删除不可能制造环）。

## 11) 测试耦合

动手前先迁/退的测试（唯一耦合点）：
- `tests/web_pages/test_reports_workbench_navigation_contract.py:109`（fixture URL 去掉 `plan_id=PLAN-RPT` 片段，保留 `version`/`plan_role=adopted`/`back_to`）。
- `:122`（删 `assert query["plan_id"] == ["PLAN-RPT"]`，**保留** :123 back_to 断言）。
- `:126`（删 `assert hidden_inputs["plan_id"] == ["PLAN-RPT"]`，**保留** :127 back_to 断言）。
- 顺序：**先改测试（去 plan_id 断言、留 back_to）→ 再删生产代码 → 跑测试自证**（绿=parity 成立）。若先删代码后改测试，中间态 CI 红。
- 不得新增「plan_id 仍为 None」之类的兜底断言（灵魂线）。

## 12) 前置依赖

执行批次：**Batch-3**（B01 plan_id 下线 + 死码清理）。前置门：
1. **R54 先行（Batch-2 guard 收口）**：R54 在 `build_workbench_plan_context` 加 guard 字段，R42 删该函数 plan_id 形参须 **rebase 在 R54 新签名之后**，否则形参列表互撞。
2. **R60 同批次**（强 co-change，MUST 同删；二者实为同一 plan_id 死面包屑两面，共享 link_query:118/154 + 字段表）。
3. **F 门**：与在途 `aps-frontend-workbench` roadmap owner 对齐 items.yaml:289 制度化项的下线（触碰 in-progress feature 契约），并确认 contract 测试三断言退法被接受。
4. R56/R57/LB06 **已 fixed**，不再是前置（其护栏已就位，只作禁区约束）。

**owner_pending=false** → 给终态修法（上述 §4），并分配执行批次 Batch-3（与 R60 合并、rebase 在 R54 后）。

## 对抗核验（第二双眼睛）

核验日期 2026-06-05。只读不改，独立重 grep 回盘，默认怀疑。

**重 grep 命令与结果**：

1. `rg -nP '\bplan_id\b'` 逐文件回盘 → 全部**与档案 §1/§8 一致**：navigation_context.py:78、reports_page_support.py:104/:143、scheduler_workbench_links.py:191（形参）/:233（dict）、scheduler_reports_workbench.py:60（形参）/:79（透传）、scheduler_workbench_link_query.py:118/:154、scheduler_navigation_links.py:12/:52、reports_export_support.py:14、dashboard.py:241。**0 处不一致**。
2. `rg -nP 'plan_id|filters\.get' web/viewmodels/dashboard_workbench.py` → 文件**真实存在**（18689 字节）但 plan_id **0 命中**。证实档案剔除消失点正确，registry phase1_blast/planned_fix 反复引的 `:121` 是幻觉/已被前序改动删除。**修法清单不含此点，正确**。
3. `git grep -nP '\bplan_id\b' -- core/ data/` = **0 命中**。死面包屑结论独立证实。
4. C1 灾难链**亲眼复核**：`sed -n '116,121p' link_query.py` 显示 `_append_plan_query` 内 `version`(:117)→`plan_id`(:118)→`plan_role`(:119)→`scenario_id`(:120) 四行紧邻，plan_id 被三个真消费身份参数前后夹住；`_append_target_plan_query` execution_review 分支(:151-155)同构。**误删波及相邻行→URL 丢身份参数→version 静默错位**的风险链确凿，§7/§8 的 parity 断言要求成立且必要。
5. 承重护栏复核 `sed -n '78,80p' navigation_context.py`：:79 = `plan_role=plan_role if plan_role in VALID_PLAN_ROLES else ROLE_ADOPTED`、:80 = `scenario_id=scenario_id`，二者与 :78 plan_id 行是同一 `build_workbench_plan_context(...)` 调用的**不同关键字实参**，语法独立。删 plan_id 不触护栏，§9 禁区约束正确且充分。
6. R54 guard 字段复核 `sed -n '185,210p' scheduler_workbench_links.py`：`guardrail_text=""`、`guardrail_reason_type=""` 形参**真实在位**（:209-210 区段）。R42 删 plan_id 形参须 rebase 在 R54 新签名后，§6/§12 顺序约束正确。
7. roadmap 制度化复核：`items.yaml:289` 真实存在（"…保留 plan_id 与 back_to"）；`ls .../aps-frontend-workbench/ | grep checklist` = **无 checklist 文件**；该目录 acceptance.md 内 plan_id **0 命中**。档案 §8 订正 registry「3 处」为「仅 1 处真实」**正确**。
8. 契约测试复核：`tests/web_pages/test_reports_workbench_navigation_contract.py` 的 :109/:122/:126 plan_id 三断言与 :123/:127 back_to 断言**均在位**，§11 退法（去 plan_id 留 back_to）正确。

**承重误删风险审查**：R42 本身 lb=false / lb_no_touch=null。修法为 **P6 纯直删**，逐条复核——无删除统一/透传/加形参的越界，无把护栏「顺手统一」的清理，明确列出禁区行（navigation_context.py:79/:80、reports_page_support 的 LB06 adopted/scenario 强制分支）。灵魂线红线（纯删不加兜底/静默回退/except 吞错）符合。**承重误删风险：无**（前提是执行时严守 §9 禁区行 + §7 parity 断言）。

**owner_pending 审查**：owner_pending=false，档案给终态修法 + 分配 Batch-3，**合规**（无擅自越权）。

**核验结论**：PASS。行号 0 处漂移（与档案完全一致）；死面包屑、消失点剔除、C1 灾难链、承重护栏、R54/R60 co-change、roadmap 幻觉订正全部独立证实。无需第三方裁断。

