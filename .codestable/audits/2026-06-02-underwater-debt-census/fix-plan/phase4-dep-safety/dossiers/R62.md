# R62 逐债档案 — execution_review 三档身份(display/identity/export)压扁成同值 + 模板!=死副行 + xlsx or 死回退

> 桶 B01 / 簇 C01 / 病理 P3 / 严重度 low / 承重 false / owner_pending false
> 主文件 core/services/report/execution_review.py（+ templates/reports/execution_review.html、core/services/report/exporters/xlsx.py）
> 本档只读不改，仅产计划。所有行号为 2026-06-05 当前工作区 grep 回盘值。

## 1) 当前真实 file:line（回盘）+ 漂移记录

R62 是三处消费方共病的「三档身份压扁」债，回盘三个文件：

**(a) execution_review.py — payload 收口源 + 四组 *_label 三连**
- `_resource_pair_payload` 定义：**:406**（registry 老线索 337-348 → 实际 406-417，**漂 +69 行**）。:417 `return {"display_label": display, "identity_label": display, "export_label": display}` 三键恒等于 display（病灶收口源）。
- exception 簇（dict 装配）：**:357-362**（老线索 287-292 → 实际 357-362，**漂 +70 行**）
  - :357 `exception_affected_machine_label` = affected_machine_label
  - :358 `exception_affected_machine_identity_label` = affected_machine_label（同值）
  - :359 `exception_affected_machine_export_label` = affected_machine_label（同值）
  - :360-362 operator 同构三连
  - 上游：:320 `affected_machine_label = self._exception_value_label(affected_machine.display_label, ...)`，:326 operator 同。
- planned 簇：**:373-375**（老线索 304-305 → 实际 373-375，**漂 +69 行**），三键全 = `planned_resource["display_label"]`。
- actual 簇：**:380-382**（老线索 311-312 → 实际 380-382，**漂 +69 行**），三键全 = `actual_resource["display_label"]`。
- payload 消费者：:308 `planned_resource = self._planned_resource_identity(row)`、:309 `actual_resource = self._actual_resource_identity(...)`；:420 `_planned_resource_identity`、:435 `_actual_resource_identity`、:425 `_state_resource_identity`。

**(b) templates/reports/execution_review.html — `!=` 死副行**（4 行）
- :138 machine、:139 operator、:142 planned、:143 actual，模式 `{% if r.X_identity_label and r.X_identity_label != r.X_label %}<div class="text-meta">...`（registry 老线索 131-136/138-143 → 实际 138-143，**模板已漂，须按 `!=` 模式定位非裸行号**）。三键恒等故 `!=` 恒 False，text-meta div 永不渲染。

**(c) xlsx.py — `or` 死回退**（4 行）
- :410 machine、:411 operator、:414 planned、:415 actual，模式 `row.get("X_export_label") or row.get("X_label")`（registry 老线索 410-415 → 实际 410-415，**未漂**）。export_label 恒等于 label 故 `or` 永不回退。

**漂移结论：有，幅度大。** execution_review.py 全体 +69~+70 行（registry current_evidence 自承 location_status「✅准」是基于 committed≡基线的旧快照，实际工作区已漂——文件 git 状态 `MM`，已被本分支改动顶下去）。xlsx.py 无漂。模板已漂但 `!=` 模式行实测仍在 :138-143。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`。
- 一致 ✅。三处 no-op 残骸全部仍在（payload :417 三键恒等、模板 :138-143 `!=` 死副行、xlsx :410-415 `or` 死回退实测均存在），Batch-3 三处原子同删未执行，与 planned 状态吻合。
- registry current_evidence 注「有新测试，但针对 adopted-only 拦截即 LB02/05/06，非 R62 三档收敛」——已独立核实属实（见 §11），新测试与 R62 收口面无交集。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P3 半截收敛 / 死分支未清**：三档身份系统（display/identity/export）被设计成可承载「展示名 / 完整身份 / 导出名」三种变体，但实现把三键全压成同一 `display` 值（payload :417、四组三连），导致下游模板 `!=` 分支与 xlsx `or` 回退三处全沦为 no-op 死代码。API 仍宣称有 identity/export 变体却纯空转。
- 严重度 **low**：运行期零风险（死分支恒不触发，结果稳定正确）。真正代价是**演进误导**——后续若要恢复真实三档身份，无法区分「有意压扁」还是 bug 残留。
- 桶 **B01**（structural / delta-report-core 分区）。簇 **C01**。
- **非灵魂线债**：三处死分支属「半截重构残骸」，不涉及兜底/静默回退语义债（注意区分：xlsx `or` 与模板 `!=` 在语义上是「展示降级」而非「错误吞噬」，删除它们不触犯灵魂线红线，反而是清理 P3 残骸）。

## 4) 修法类

**修法类 = P3 半截收敛（删 no-op，非删行为）。承重=false，无 lb_no_touch，不需 owner 裁断。**

默认方向 = **收掉已掏空的三档残骸**（三档身份本已被压扁为同值，删除不改变任何观测行为）。三处消费方必须**原子同改**：

- **(a) execution_review.py 收口源**：
  - `_resource_pair_payload`:417 `return {"display_label": display, "identity_label": display, "export_label": display}` → 收成单键 `return {"display_label": display}`。
  - dict 装配三连：
    - exception 簇 :357-362 → 保留 `exception_affected_machine_label` / `exception_affected_operator_label`，删 `*_identity_label` / `*_export_label` 两键。
    - planned 簇 :373-375 → 保留 `planned_resource_label`，删 `planned_resource_identity_label` / `planned_resource_export_label`。
    - actual 簇 :380-382 → 保留 `actual_resource_label`，删 `actual_resource_identity_label` / `actual_resource_export_label`。
  - `_actual_resource_identity`:437-440 的 no-feedback 早退字典也含三键（`display_label`/`identity_label`/`export_label`），随 payload 一并收成单键。
- **(b) 模板 execution_review.html :138-143**：删 `{% if X_identity_label and X_identity_label != X_label %}...{% endif %}` 整段死副行，`title="{{ X_identity_label }}"` 改回 `title="{{ X_label }}"`（identity_label 键已删）。**按 `!=` 模式定位，勿用裸行号**（模板会随 B01 其它债动模板而漂）。
- **(c) xlsx.py :410-415**：`row.get("X_export_label") or row.get("X_label")` → `row.get("X_label")`（export_label 键已删，留 `or` 会取到 None）。

**收口点说明**：本债不新建任何模块/符号；`_resource_pair_payload`（:406）本就是已存在的唯一收口源，三处消费方收敛到它即可。**不触犯收口红线**（无新建第二模块）。

**反向兜底**：若 Phase2 owner 明确要恢复真三档身份（identity≠display、export≠display），则改为反向补齐——但需求证有此规划文档，当前无证据，故默认收敛方向。

## 5) 同文件碰撞债（siblings）

execution_review.py 同文件兄弟：**LB02、LB05**（registry interference_edges 另列 LB06，亦同文件）。三者均为 `same_file=true, same_symbol=false`。

- **LB02 / LB05（承重 high，forced-adopted 护栏）**：
  - 改哪段：LB02/LB05 动 ROLE_ADOPTED 强制段（回盘实证在 :58 `effective_plan_role=ROLE_ADOPTED`、:65、:69、:180-181 `plan_role=ROLE_ADOPTED, scenario_id=None`、:191-192、:221、:236）。R62 动 `_resource_pair_payload`（:406-417）+ dict 三连（:357-382）。**两者在不同方法、不同行段，无行重叠。**
  - 谁先谁后：**LB02/LB05 注释先落（Batch-1）**，R62 后动（Batch-3）。原因：承重神圣——承重债先补「我是故意的」注释 + 绑 parity，把护栏行钉死后，R62 再清三档死分支才安全。
  - 会否撞行号 / dict 键位移：**会移行号但不撞段**。LB02/LB05 补注释会在 :58-236 区间插入注释行，把 R62 的 :357-417 整体下推（R62 自身又因 :358-359/361-362/374-375/381-382 删键而**上移**，dict 键位移会回缩）。**因此 R62 必须在 LB02/LB05 落定后按 `!=`/符号名重新 grep 回盘，绝不照抄本档行号。**
- **LB06**：同文件，亦 adopted-only 拦截类，与 R62 收口面无交集；顺序同 LB02/LB05（承重/护栏类先落）。

## 6) 同收口点债 + 顺序敏感性

- R62 收口源 = `_resource_pair_payload`（:406）。registry interference_edges 全部标 `same_symbol=false`——**没有其它债收口到 `_resource_pair_payload` 或同符号**。LB02/LB05/LB06 收口在 ROLE_ADOPTED 段，是不同符号。
- 故 R62 在 `_resource_pair_payload` 这个收口点上**独占**，无「同收口点债」竞争。
- 顺序敏感性仅来自同文件行号位移（见 §5），非同收口点语义竞争。R62 三处消费方（payload / 模板 / xlsx）之间是**强原子耦合**：必须同一批次同删，任一不同步即产生新半截残骸（registry fix_invalidation_risk 已述）。

## 7) 收口行为差异 + 所需 parity 测试

**收口行为差异：零。** 这是 R62 安全的根本——三键当前恒等：

- payload :417 三键 = 同一 `display`。
- 模板 :138-143 `!=` 恒 False（identity==label），text-meta div 永不渲染；删副行后渲染输出**逐字节不变**。
- xlsx :410-415 `or` 永不回退（export_label 非空且==label），删 `or` 后取值**完全相同**。

**逐分支等价性分析（反例搜索）：**
- 边界 `identity_label is None`？→ 不可能。payload :417 三键由同一 `display` 赋值，`display` 在 :409-416 五分支（machine&op / machine / op / else empty_label）全产生非空 str，identity 恒非空且恒==label。无 None vs raise 反例。
- exception 簇 :358-359 = `affected_machine_label`（来自 :320 `_exception_value_label(... display_label ...)`），identity/export 同源同值，无差异。
- actual 无 feedback 早退 :437-440 三键 = "暂无现场反馈"，亦恒等。
- **结论：新旧两路对所有边界值逐分支等价，无反例。**

**所需 parity 测试（建议在动手前补，作为收口安全网）：**
1. **报表 dict 快照 parity**：构造一行（含/不含 feedback、含/不含 exception、空资源/单资源/双资源 5 种 display 分支），断言收口后 dict 不再含 `*_identity_label`/`*_export_label` 键，且 `*_label` 值与收口前完全一致。
2. **xlsx 导出 parity**：同输入跑 xlsx 导出，断言收口前后对应单元格字符串逐格相等。
3. **模板渲染 parity**：渲染 execution_review.html，断言收口前后 HTML 输出无 text-meta 差异（本就恒不渲染，应零 diff）。
> 注：当前 tests/ 对 R62 收口面（execution_review 报表的无前缀 `exception_affected_machine_identity_label` 等键）**零断言**，故无现成测试可借；上述 parity 需新写。

## 8) 爆炸半径（生产/测试消费者 + 灾难链）

**生产消费者（回盘核实）：**
- execution_review.py 内部：:308/309 调 payload，dict 装配 :357-382（自产自销，4 组键全在本文件内生成）。
- templates/reports/execution_review.html :138/139/142/143 — `*_identity_label` 仅用于 `!=` 死副行 + title；`*_label` 用于主显示。删 identity 键后须同改 title。
- core/services/report/exporters/xlsx.py :410/411/414/415 — `*_export_label or *_label`，export 键删后落到 label。
- **爆炸面闭合**：rg 全仓 `planned_resource_identity_label`/`actual_resource_identity_label`/`*_export_label` 的生产消费者**仅这 3 个文件**，无 web/ 路由或其它服务读取这些无前缀键。

**关键消歧（避免误判）：** tests/ 与 core/ 大量出现的 `latest_exception_affected_machine_identity_label`（带 `latest_` 前缀，定义在 `core/models/operation_execution_state.py:45`，被 resource_dispatch_*、operation_execution_state 等消费，断言值="M2 二号设备"真身份）**不是 R62 收口面**。R62 收的是 execution_review 报表 dict 里**无前缀**的 `exception_affected_machine_identity_label`（:358，恒=display 压扁值）。两者键名空间不同、来源不同。**删 R62 三档键绝不触及 state 层 `latest_*_identity_label` 真身份字段。**

**测试消费者：** R62 收口面**零断言**（git grep 证 tests/ 无任何处断言 execution_review 报表的 `planned_resource_identity_label`/`*_export_label`/`_resource_pair_payload` 的 display/identity/export 差异）。registry phase1_blast.test_consumers「无」对 R62 收口面**正确**（registry 措辞需限定为 execution_review 无前缀键，已在本档澄清）。

**契约/拓扑测试：** 无。regression_plan_vs_actual_review.py 不 PIN 这些键的 identity/export 变体差异。

**灾难链（删错 / 统一错会静默炸什么）：**
- **链 A（三处不原子）：** 只删 payload 三键、漏删 xlsx `or` → xlsx `row.get("X_export_label")` 取 None，`or row.get("X_label")` 仍回退 → 看似无害，但留下「键已死、回退仍在」的新半截残骸（registry fix_invalidation_risk 核心）。只删 payload、漏删模板 `!=` → 模板 `r.X_identity_label` 取 None，`{% if None and ... %}` 仍 False 不炸，但残骸留存。**全是静默债务累积，非运行崩溃。**
- **链 B（删错对象）：** 若误把 state 层 `latest_*_identity_label`（真身份）当 R62 残骸删 → **真炸**：resource_dispatch viewmodel 契约测试（regression_resource_dispatch_viewmodel_public_output_contract.py:68/71、test_resource_dispatch_viewmodel.py:239 断言 `counterpart_resource_identity_label`="MC001 数控车床1"）+ operation_execution 系列断言全红。§8 消歧即为防此。
- **链 C（碰护栏）：** 若改 R62 时手滑动 :58-236 ROLE_ADOPTED 强制段 → 破坏 LB02/LB05「只复盘正式采用方案」语义，复盘可能错读 scenario 草稿。**绝对禁区。**

## 9) 承重毗邻标记 + 禁区行

- R62 本身：load_bearing=false，lb_no_touch=null。R62 三档收口动作非承重。
- **同文件承重毗邻：高危同居。** execution_review.py 同时含 LB02/LB05/LB06 的 forced-adopted 护栏。
- **禁区行（R62 修改绝不可碰，回盘实证）：**
  - :58 `effective_plan_role=ROLE_ADOPTED`
  - :65 / :69（plan_role_label(ROLE_ADOPTED) 回退默认）
  - :99-100 / :113-114（`plan_role`/`scenario_id` 形参签名——**绝不给这些签名加形参或改默认**）
  - :121 `_resolve_plan(self, version, plan_role, scenario_id=None)`
  - :180-181 / :191-192 `plan_role=ROLE_ADOPTED, scenario_id=None`（强制采用方案）
  - :221 `host._resolve_plan(v, ROLE_ADOPTED, None)`
  - :236 `"plan_role": ROLE_ADOPTED`
  - registry planned_deps_hint 列的旧硬钉行（:112/123/153/141/166-167）已漂，实际钉在上述行；**修法只能补「我是故意的」注释 + 绑契约/parity，不删/不统一/不透传/不加形参。**

## 10) 分层 / 导入环风险

- **零分层风险。** R62 是删键 + 删模板副行 + 删 xlsx 回退，**不新增任何 import**。
- 收口源 `_resource_pair_payload` 已存在于 core/services/report/execution_review.py，无需新建模块，无新跨层 import。
- 三处文件分层：execution_review.py（core.services.report）、xlsx.py（core.services.report.exporters）、html（templates）——同层或模板层，删键不引入 core.algorithms→core.services 或 core.models→core.services 越层。
- 无导入环风险（纯删除操作）。AST 违规预期 0。

## 11) 测试耦合（动手前先迁哪些测试）

- **R62 收口面无现成测试可迁**：tests/ 对 execution_review 报表的无前缀 `*_identity_label`/`*_export_label` 零断言。
- **不可误迁的测试（属 state 层 / resource_dispatch，与 R62 无关，动手时勿碰）：**
  - tests/regression_operation_execution_exception_feedback.py:108/111/121/124（断 `latest_*_identity_label` 真身份）
  - tests/regression_operation_execution_state_flow.py:61/64
  - tests/regression_operation_execution_event_foundation.py:178/181
  - tests/regression_resource_dispatch_viewmodel_public_output_contract.py:68/71、tests/test_resource_dispatch_viewmodel.py:239（`counterpart_resource_identity_label`）
- **动手前应新写**（见 §7）：execution_review 报表 dict / xlsx / 模板三套 parity 快照，作为收口安全网。
- registry 提到的「新测试针对 adopted-only 拦截（LB02/05/06）」属 LB 系债的护栏测试，与 R62 收口面无交集，不迁。

## 12) 前置依赖

1. **LB02 / LB05（同文件承重 high，PHASE0 §2）先落 Batch-1 注释**：承重神圣，护栏行先补「我是故意的」注释 + 绑契约/parity 钉死后，R62 才动同文件。LB02→R62 有依赖边（承重先于动同文件）。LB06 同理。
2. **行号重新回盘门**：LB02/LB05/LB06 落定后，:357-417 区间整体位移，R62 动手前**必须按符号名 `_resource_pair_payload` + `!=` 模式重新 grep**，绝不照抄本档行号。
3. **B01 其它动模板的债先后协调**：模板 :138-143 会随其它 B01 债动 execution_review.html 而漂，R62 删 `!=` 副行须按 `!=` 模式定位。
4. **parity 测试门**：建议 §7 三套 parity 测试先落地（红→绿基线），再做收口删除，确保零行为差异可自证。
5. 批次定位：**Batch-3 B01-structural**（registry planned_batch_hint）。三处消费方原子同删为一个不可拆原子操作。

---

### 回盘附证（关键 file:line）
- `_resource_pair_payload` 收口源三键恒等：core/services/report/execution_review.py:417
- exception 三连：:357-362；planned 三连：:373-375；actual 三连：:380-382；actual 无 feedback 早退三键：:437-440
- 模板 `!=` 死副行：templates/reports/execution_review.html:138,139,142,143
- xlsx `or` 死回退：core/services/report/exporters/xlsx.py:410,411,414,415
- 禁区（LB02/LB05 护栏）：execution_review.py:58,65,69,99-100,113-114,121,180-181,191-192,221,236
- 消歧（state 层真身份，非 R62）：core/models/operation_execution_state.py:45

---

## 对抗核验（第二双眼睛）

> 核验人：独立 rg/grep 回盘 agent，2026-06-05，工作区当前态（execution_review.py git=MM）。默认怀疑，找反例。

**结论：PASS。** 档案行号、消费者清单、承重判断、收口等价性、消歧均独立复核通过，未找到反证。

### 重 grep 命令与结果
1. `rg -n "_resource_pair_payload|return \{\"display_label\"" core/services/report/execution_review.py`
   → 定义 **:406**，三键 return **:417 `{"display_label": display, "identity_label": display, "export_label": display}`**，两处调用 :423/:444。**与档案 :406/:417 一致。**
2. `rg -n "<四组 *_identity_label/*_export_label 无前缀>" execution_review.py`
   → exception 簇 **:357-362**、planned 簇 **:373-375**、actual 簇 **:380-382**（Read :350-384 实证整块）。**与档案完全一致。**
3. `rg -n "identity_label.*!=" templates/reports/execution_review.html`
   → 死副行实测 **:138/139/142/143**，模式 `{% if r.X_identity_label and r.X_identity_label != r.X_label %}`。**与档案一致（按 != 模式定位，非裸行号）。**
4. xlsx `or` 死回退（全仓 -t py 扫）→ `core/services/report/exporters/xlsx.py:410/411/414/415` `row.get("X_export_label") or row.get("X_label")`。**未漂，与档案一致。**
5. 爆炸面闭合：全仓 `rg -t py -t html` 无前缀 identity/export 键 → 消费者**仅 execution_review.py / 模板 / xlsx 三处**，无 web/ 路由或其它服务读取。**爆炸面闭合，与档案 §8 一致。**

### 反例搜索（主动找反证，均落空）
- **收口面零断言（验真）**：`rg tests/` `planned_resource_identity_label` / `actual_resource_export_label` / `_resource_pair_payload` / 无前缀 `exception_affected_machine_identity_label`（剔除 latest_）→ **全 0 命中**；`regression_plan_vs_actual_review.py` 不含 identity_label/export_label。**registry test_consumers「无」对 R62 收口面属实。**
- **等价性根（验真）**：:357-359 三键全 = `affected_machine_label`（:320 `_exception_value_label` 产出，同源同值）；payload :407-417 五分支全产非空 str，identity 恒==label，无 None/raise 反例；actual 早退 :437-441 三键恒="暂无现场反馈"。**逐分支等价，删 `or`/`!=`/三键后输出逐字节不变。**
- **删 xlsx `or` 等价性**：export_label 键删后 `row.get` 返 None，`or` 回退到 label == 直接 `row.get("X_label")`。等价成立。

### 承重 / 消歧 / 分层复核
- **承重误删风险：无。** R62 本身 lb=false、lb_no_touch=null。禁区行 ROLE_ADOPTED 实测在 **:58/65/69/121/180-181/191-192/221/236**（`rg ROLE_ADOPTED`），与 R62 收口段 :357-417 **不同方法、零行重叠**，档案 §9 禁区清单准确。修法为「删 no-op 死分支」非删行为，未混入透传/加形参/统一护栏。
- **消歧成立（关键防误删）**：state 层 `latest_exception_affected_machine_identity_label`（operation_execution_state.py:45/:50）+ `counterpart_resource_identity_label` 被 7+ 文件 PIN（data/repositories、resource_dispatch_rows、resource_dispatch_execution_enrichment、4 个 regression/test）。**带 latest_/counterpart_ 前缀，键名空间与 R62 无前缀键完全分离。删 R62 不触及真身份字段。** 档案 §8 灾难链 B 防护正确。
- **分层：零风险。** 纯删键/删模板副行/删 xlsx 回退，不新增 import，无新建模块，无跨层。AST 违规预期 0。

### 微小偏差（不影响修法）
- 档案 §1/§6 称 actual 无 feedback 早退三键在「:437-440」，实测三键内容在 **:438-440**、闭合 `}` 在 :441（块整体 :437-441）。属边界表述，指代同一块，不影响「随 payload 一并收单键」的修法。
- registry 自身 location_status 标「✅准（核心）」基于 committed≡基线旧快照，**实际工作区已漂 +69~70 行**（payload 337→406、return 348→417、exception 287→357…）。档案已正确纠正并标「按符号名重 grep」门，registry 旧行号不可信判断成立。

### owner_pending 复核
- owner_pending=false，档案未越权——给出了默认收敛终态修法且未擅自跳过 owner 裁断（本就无需裁断）。批次定位 Batch-3、LB02/LB05 Batch-1 注释先落的前置依赖合理。**无越权。**

**最终：PASS。行号一致（仅 actual 早退块边界 :437-440↔:437-441 微差）。无承重误删风险。无需升级第三方裁断。**
