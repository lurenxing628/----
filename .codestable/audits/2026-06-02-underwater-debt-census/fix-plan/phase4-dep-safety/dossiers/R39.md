# R39 逐债档案 · part_repo.list_unparsed 死方法

> 原始档案只读不改、只产计划。原始回盘时间锚点 2026-06-05；2026-06-08 B 执行后已补登终态。以下旧行号保留作执行前证据，当前状态以 §0 与 `_registry.json` 为准。

## §0 2026-06-08 B 执行终态补登

- **状态：fixed。** `PartRepository.list_unparsed` 已删除（旧锚点 `data/repositories/part_repo.py:32-33`）。
- **同批约束已满足：** R39 与 R38 的 `part_repo.py` 份同一次 diff 删除，R39 删除导致下方行号上移的风险已关闭。
- **残留扫描：** `rg -n "list_unparsed|def list_as_dicts" data/repositories core web tests tools scripts` 无命中。
- **活真相源保留：** `PartRepository.list` 仍在 `data/repositories/part_repo.py:20`，真实调用方继续走该活方法；未新增 helper、兜底或导入。

## §八 12 字段

### 1) 当前真实 file:line（回盘）+ 漂移记录
- 符号 `PartRepository.list_unparsed` → **`data/repositories/part_repo.py:32`**（def 行），方法体 `:33 return self.list(route_parsed="no")`。
- 被封装的活方法 `PartRepository.list` → `data/repositories/part_repo.py:20`（带 `route_parsed: Optional[str]=None` 形参，`:21` 分支）。
- **漂移：无。** `old_location_DONT_TRUST` = `part_repo.py:32`，rg 回盘命中 `part_repo.py:32`，逐字一致（漂移幅度 0 行）。文件不在已知漂移名单，registry `location_status` 标 ✅准 属实。
- 证据：`rg -n "list_unparsed" --type py` 全仓唯一命中即 `part_repo.py:32` def 行（见字段 8）。

### 2) 当前状态 + 与 status evidence 是否一致
- registry `status_2026_06_05` 已在 B 执行后补登为 **fixed**。
- 与 `current_evidence_2026_06_05` 一致：旧 `:32-33` 整方法已删除，`PartRepository.list` 活方法未动，全仓残留扫描无命中。
- 旧正文中的 planned/行号内容仅作为执行前证据保留，不再代表当前代码状态。

### 3) 病理 / 严重度 / 桶 / 簇
- 病理 **P6**（纯死方法：零消费，含测试均为零引用）。
- 严重度 **low**。
- 桶 **B12**（home=B12）。
- 簇 **C02**。
- partition=data-repos，kind=real（真债非误报），load_bearing=**false**，needs_adversarial=false / adv_refuted=false（不需对抗，已是确定性死码）。

### 4) 修法类
- **直删**（planned_fix_class=直删）。删 `data/repositories/part_repo.py:32-33` 整个 `list_unparsed` 方法（def 行 + 唯一一行方法体）。
- 内部转调的 `self.list(route_parsed="no")` 走的是活方法 `list(:20)`，不受影响——删的是包装壳，被包者继续存活。
- 承重=false 且 `lb_no_touch=null`，不触发「仅注释」铁律；无收口需求（非 P5，删除即终态，不需要把谁收口到统一点）。
- owner_pending=**false**：可直接给终态修法，无需待裁。
- 灵魂线检查：纯删除，不新增兜底/静默回退/except 吞错；不改 loud raise（本方法无错误路径，仅一行透传调用）。符合 P4 灵魂线（本条非 P4，无需改 raise）。

### 5) 同文件碰撞债
唯一同文件兄弟：**R38**（`PartRepository.list_as_dicts`，回盘 `data/repositories/part_repo.py:71`）。
- **各改哪段**：R39 删 `:32-33`；R38 动 `:71-72`（`list_as_dicts`，SELECT 仅 part_no/part_name/route_raw）。两段物理不相邻（中间隔 `create/update/delete/delete_all` 共 `:35-69`）。
- **谁先谁后**：无强序——两者都是 data-repo 内独立方法，无相互调用（callgraph 无 R38↔R39 边）。但 **必须同批改同一 part_repo.py**（registry 硬约束 planned_deps_hint「同文件同改·硬」/ Phase1 边 R38↔R39）。
- **会否撞行号/dict 键位移**：R39 删 `:32-33` 会使其下方所有行号上移 2 行（`create` 从 `:35`→`:33`，`list_as_dicts` 从 `:71`→`:69`）。**若 R39 先合入而 R38 的 patch 基于旧行号 `:71`，将打偏**。这正是 registry 要求二者同批同提交的原因——避免行号二次漂移。无 dict 键位移问题（删的是方法，不涉字典字面量重排）。
- 协调结论：R38、R39 **同 PR 同 commit**；patch 以同一基线一次性生成，或按「先删 R39（行号上移）再以新行号定位 R38」的顺序应用 hunk。

### 6) 同收口点债
- **无**。R39 是直删类，不收口到任何统一符号。
- 它内部转调的 `list(:20)` 是被复用的活方法，但 R39 删除后不改变 `list` 的任何契约（少了一个调用者，且该调用者本就零外部引用）。
- 全仓其他 repo 有同名 `list_as_dicts`（operator_repo:85 / op_type_repo:73），但那是 R38 域、与 R39 的 `list_unparsed` 不同符号、不构成同收口点。R39 无顺序敏感性。

### 7) 收口行为差异及所需 parity 测试
- **不适用**。R39 是直删非收口，不存在「新旧两路对边界值是否逐分支等价」的问题——删除一个零调用者不会让任何现存调用方走到不同分支。
- 唯一可设想的边界：若将来有人复活「查未解析零件」需求，应直接调 `list(route_parsed="no")`（活方法），语义与被删的 `list_unparsed` 逐字等价（`list_unparsed` 体仅 `return self.list(route_parsed="no")`）。本轮无此调用者，故无 parity 测试义务。
- 无需新增/迁移任何 parity 测试。

### 8) 爆炸半径（9 维）
- **生产消费者**：**零**。`rg -n "list_unparsed" --type py | grep -v "def list_unparsed"` 退出码 1（无匹配）；全仓 .py 仅 `part_repo.py:32` def 行。
- **测试消费者**：**零**。`rg -n "list_unparsed" tests/` 退出码 1（无匹配）。
- **callgraph 出入边**：入边 0（无人调它）；出边 1 条 `PartRepository.list_unparsed --self--> PartRepository.list`（其自身调用 `list`，删除后此出边一并消失，不影响 `list` 的其他入边）。证据：edges.json 唯一命中即此 self 边，`ambiguous=false`。
- **downstream_effect**：可直接删，零引用点（registry 一致）。
- **fix_invalidation_risk**：none——前提是与 R38 同批改 part_repo.py（否则 R38 patch 因行号上移失效，见字段 5）。
- **灾难链（删错/统一错会静默炸什么）**：本条几乎无灾难链。最坏情形只有两个：
  1. 删 `:32-33` 时手抖多删一行碰到 `list(:20-30)` 或 `create(:35)`——会破坏活方法，但这是机械误删而非语义陷阱，diff review/单测即捕获。
  2. R39 单独先合、R38 未同批 → R38 行号漂移打偏（非静默炸 R39，炸的是 R38 的应用）。
  - 无静默吞错风险（删的不是 except/兜底，是一行透传）。
- **lb_colocation_danger**：none（part_repo.py 非承重文件）。
- **contract/topology 测试**：无。
- 综合：爆炸半径接近 0，是 80 债中最安全的一档（low + P6 + 零引用 + 直删）。

### 9) 承重毗邻标记
- 本债 load_bearing=**false**，`lb_no_touch=null`。
- 同文件 `part_repo.py` 全 73 行，无任何承重方法标记（registry `lb_colocation_danger=none`）。同文件兄弟 R38 亦非承重。
- **禁区行：无**（非承重文件，无 lb_no_touch 列表）。删除 `:32-33` 不毗邻任何承重逻辑。

### 10) 分层 / 导入环风险
- **无风险**。R39 是纯删除，不新增任何 import。
- 当前 `part_repo.py` 导入面（回盘 `:1-7`）：`from __future__`、`typing`、`from core.models import Part`（data→core.models，合法）、`from .base_repo import BaseRepository`（同层）。**不存在 core.services 反向 import**，删除 `list_unparsed` 不可能引入越层 import 或导入环（删除只减边）。
- 0 AST 违规风险（分层红线满足）。

### 11) 测试耦合
- **无测试需迁移**。`rg -n "list_unparsed" tests/` 退出码 1，零测试引用本方法。
- 动手前不需迁任何测试；删除后亦不会有测试变红（因无测试触达此方法）。

### 12) 前置依赖
- **无前置债、无前置 F 门**（registry「无其他依赖」）。
- 唯一硬约束是**与 R38 同批同提交 part_repo.py**（避免行号二次漂移，见字段 5），这是「并发/同批」约束而非「先后」依赖——R38 不必先于 R39 完成，二者同 commit 即可。
- 批次：Batch-13，与 R38 同 part_repo.py 子批同提交；优先级 low；无跨边，可任意排期。

## 对抗核验（第二双眼睛）

回盘时间 2026-06-05，独立 rg/grep，默认怀疑、主动找反例，结论 **PASS**。

- **行号复核：一致**。`rg -n "list_unparsed" --type py` 全仓唯一命中 `data/repositories/part_repo.py:32`（def 行），方法体 `:33 return self.list(route_parsed="no")`，与档案/registry old_location 逐字一致，漂移 0 行。
- **零引用复核（含反例搜索）**：
  - `rg -n "list_unparsed"`（全仓任意文件类型，不限 --py）→ 仅 `part_repo.py:32` def 行，退出码 0 仅 1 命中。
  - `rg -n "list_unparsed" tests/` → 退出码 1（无测试引用）。
  - 反射/字符串调用嫌疑 `rg -n "getattr\(.*list_unparsed|['\"]list_unparsed['\"]"` → 退出码 1（无动态调用，排除 getattr 漏网）。
  - 结论：生产 0、测试 0、反射 0，纯死叶子坐实。
- **删壳不孤立活方法（反例验证 list 仍被多方消费）**：`rg -n "\.list\(" --type py | rg -i part` 显示 `core/services/process/part_service.py:105 self.part_repo.list(route_parsed=route_parsed)`、`part_excel_adapter.py:8`、`web/routes/process_parts.py:64/378` 等多个活调用者。删 `list_unparsed` 只移除一条 self→list 出边，`list(:20)` 入边充足、继续存活，无误伤。
- **承重判断复核**：load_bearing=false、lb_no_touch=null 属实；同文件 part_repo.py 无承重标记。**承重误删风险：无**。修法为纯直删，未混入统一/透传/加形参；不属「像重复实为护栏」族（list_unparsed 仅一行透传，非边界守卫）。
- **分层/导入环复核**：`rg -n "core\.services|core\.algorithms" data/repositories/part_repo.py` → 退出码 1，无反向越层 import；删除只减边，0 AST 违规风险坐实。
- **灵魂线复核**：删的是一行透传 `return self.list(...)`，非 except/兜底/静默回退，不触碰 loud raise，符合 P4 灵魂线（本条非 P4）。
- **owner_pending 复核**：registry owner_pending=false，档案给终态修法（直删）合规，未越权。
- **唯一争议外坑（与档案一致，非 R39 本体风险）**：R38（list_as_dicts:71 同 part_repo.py）须同 commit；删 R39 致下方行号上移 2 行，R38 patch 若基于旧行号会打偏。属同批协调约束，已在字段 5/12 记全。

核验结论：行号一致、零引用三重坐实、活方法独立存活、非承重非收口非 owner_pending、分层灵魂线均无违规 → **PASS**，无需第三方裁。

## 返回摘要
R39 | 真实行号: list_unparsed→data/repositories/part_repo.py:32 (体:33→self.list(route_parsed="no")) | 漂移: 无(0行) | 修法类: 直删 :32-33 整方法,被包活方法 list(:20)不动 | 同文件兄弟需协调: R38(list_as_dicts:71)同 part_repo.py 同批同提交,删 R39 致下方行号上移2行,R38 patch 须同基线避免打偏 | 最大爆炸风险: 近 0(零引用含测试);唯一坑=R38 不同批则其行号漂移打偏 | 与既有分析冲突: 无 | 前置: 无债依赖/无 F 门,仅与 R38 同 commit | owner_pending: n
