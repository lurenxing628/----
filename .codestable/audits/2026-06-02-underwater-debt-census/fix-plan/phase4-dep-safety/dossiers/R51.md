# R51 逐债档案 · parse_dispatch_rule / parse_strategy 宽容解析器(生产零引用 + 违灵魂线静默回退)

> 桶 B06 | 簇 C01 | 严重度 medium | 病理 P3 | 承重 false | owner_pending false
> 主文件 core/algorithms/dispatch_rules.py + core/algorithms/sort_strategies.py
> 本档案只读不改,仅依赖分析与安全批次计划。
> ✅ 2026-06-08 B 执行核销：R51/G24 已 fixed。`parse_dispatch_rule` / `parse_strategy` 两个宽容解析器已删除；两份 case-insensitive 续命测试已整体退场，未迁移、未保留 `unknown -> default` 静默回退断言。以下旧行号保留为执行前证据，后续勿重复施工。

## 1) 执行前真实 file:line(grep 回盘)+ 漂移记录

> 2026-06-08 执行后说明：本节旧行号仅作历史留档；当前两个宽容解析器与两份续命测试均已删除。

R51 是「双符号债」:同条债覆盖两个不同文件里的两个宽容解析器。

| 符号 | old_location(勿信) | 当前回盘真实位置 | 兜底体(违灵魂行) | 漂移 |
|---|---|---|---|---|
| `parse_dispatch_rule` | dispatch_rules.py:28 | `core/algorithms/dispatch_rules.py:28` def;`:34-35` `except ValueError: return default` | `:33` `return DispatchRule(str(value).strip().lower())`,`:34-35` 静默回退 default | **无**(0 行) |
| `parse_strategy` | sort_strategies.py:161 | `core/algorithms/sort_strategies.py:161` def;`:172-173` `except Exception: return default` | `:170` 空串回退 default + `:172-173` 宽 `except Exception` 回退 default | **无**(0 行) |

证据(逐行):
- dispatch_rules.py:28 `def parse_dispatch_rule(value: Any, default: DispatchRule = DispatchRule.SLACK) -> DispatchRule:`;:33 `return DispatchRule(str(value).strip().lower())`;:34 `except ValueError:`;:35 `return default`。
- sort_strategies.py:161 `def parse_strategy(value: Any, default: SortStrategy = SortStrategy.PRIORITY_FIRST) -> SortStrategy:`;:168-170 `s = str(value or "").strip().lower()` / `if not s: return default`;:171 `return SortStrategy(s)`;:172 `except Exception:`;:173 `return default`(比 dispatch 更宽——裸 `except Exception` 吞一切)。

结论:两符号 file:line 与 old_location 逐字命中,**漂移 0**。这是本批中少见的「行号还准」债——但仍按铁律独立回盘,不照抄。

## 2) 当前状态 + 与 status evidence 一致性

- registry status_2026_06_05 = **fixed**（2026-06-08 B 执行核销）。
- 执行结果：`parse_dispatch_rule`、`parse_strategy` 已删除；两份续命测试已整体删除。
- 一致性：执行前 planned 证据曾完全一致；当前本档只保留历史回盘和禁区提醒，后续不再表示待施工。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P3**(生产零引用)叠加 **灵魂线违例**:两解析器把「非法/未知字符串→静默回退默认枚举」固化为 API。这正是被严格收口取代的旧宽容入口,留着既是死壳(P3),其语义又直接违反灵魂线 #1(坏数据禁止静默兜底)。
- 严重度 **medium**:本体生产 0 blast(无人调),但危险面是「复活」——一旦有人误把它当规范解析器复用,会把已铲除的 P4 静默兜底重新引回派工/排序参数入口。
- 桶 **B06**;簇 **C01**;分区 core-algorithms;kind real。

## 4) 修法类:**直删 + 连退续命测试**(非承重,可删)

承重 false、lb_no_touch 空 → 不受「仅注释」约束。2026-06-08 已按以下历史清单执行：
1. 删 `dispatch_rules.py:28-35` 整个 `parse_dispatch_rule`(含 except→default 兜底体)。
2. 删 `sort_strategies.py:161-173` 整个 `parse_strategy`(含 `except Exception`→default 兜底体)。
3. **整体退场**(非保留) `tests/resource_dispatch/test_dispatch_rule_case_insensitive.py` 与 `tests/algorithm/test_sort_strategy_case_insensitive.py`。

灵魂线红线(本债核心,**严禁**违反):
- 这两测试 :25 钉死的 `parse_*("unknown", default=X) == X` 正是灵魂线禁止的「坏值静默回退默认枚举」。**严禁保留它们续命**——保留=把 P4 兜底语义钉成契约。必须随解析器一起退。
- 不得「保留函数只改 except→raise」:该函数生产零引用,改 raise 也只是养一个无人调的死壳;且严格入口已在收口点(§6)做了 raise,无需在此重复。直删是正解。
- 不得新增任何兜底/透传/形参/合并。

owner_pending=false → 给终态修法(如上),无需待裁。
## 5) 同文件碰撞债(siblings)

R51 跨两文件,碰撞只发生在 `dispatch_rules.py`(parse_strategy 所在的 sort_strategies.py 内无同文件兄弟债)。

dispatch_rules.py 当前函数布局(回盘):
- :28 `parse_dispatch_rule`(**R51 本条**,文件首个函数)
- :39 `class DispatchInputs`
- :55 `build_dispatch_key` / :73 内嵌 `_safe_positive`(**R49**)
- :112 `mean_positive`(**R50**,B05)

碰撞分析:
- **R49**(same_file,dispatch_rules.py):各改不同函数(R51 删 :28-35,R49 改 :55/:73 区段)。**但 R51 删的是文件首函数,删除后 :39 以下全体上移 7~8 行,R49 的真实行号必随之偏移**。→ 须同批改、改后立即按符号名重新回盘 R49 行号,严禁跨批靠绝对行号定位。
- **R50**(same_file,B05,mean_positive:112):同上,R51 删除导致 :112 上移。registry planned_deps_hint 明示「dispatch_rules.py:28 与 R49:25、R50:112 同文件须同批删避免行号互撞」。但 R50 在 **Batch-6** 收敛、R51 在 **Batch-7**——若 R50 先改完,Batch-7 时 dispatch_rules.py 已稳定,R51 再删 :28 不会回踩 R50(R50 已落地);风险仅在「同批未协调时按旧绝对行号盲删」。
- 谁先谁后:**R50(Batch-6)→ R49+R51(Batch-7 同批)**。R49 与 R51 必须同批且按符号名定位,不撞 dict 键(本债无 dict,纯函数删除,无键位移问题)。
- parse_strategy 一侧(sort_strategies.py:161)独立,无同文件兄弟,删除只影响其下方(:161 是文件末尾函数,删除几乎不影响他人)。

## 6) 同收口点债

R51 **本身不收口到任何统一点**——它是「删除」债,不是「收口」债。但其语义被以下**已存在的严格收口点**取代(这正是它可删的前提):

- string→DispatchRule 严格入口:`core/algorithms/greedy/schedule_params.py:337` `_resolve_dispatch_rule_enum` → :344 `require_choice(...)` → :346 `DispatchRule(rule_key)`。
- string→SortStrategy 严格入口:`schedule_params.py:266` `_resolve_strategy` → :275 `require_choice(...)` → :277 `SortStrategy(strategy_key)`。
- 另一严格入口:`core/services/scheduler/run/optimizer_config.py:166` `SortStrategy(require_choice(...))`、:189 `require_choice(... dispatch_rule ...)`。
- `require_choice`(optimizer_config.py:64-69)对非法值 **loud raise** `ValidationError("…请返回排产参数页重新选择。", field=…)`——符合灵魂线(P4 该 raise)。

顺序敏感性:R51 删除**不触碰**这些收口点,纯减法。无收口顺序耦合。唯一前提是「确认严格收口点已在位」(已回盘在位,见上 file:line)——删 R51 不会让任何严格入口失去依赖。

## 7) 收口行为差异及 parity 测试

R51 是**直删**,不是收口/合并,**不需要新旧两路 parity 测试**——没有「替换某条路」,只有「移除一条无人调的旧路」。但必须验证「旧路与严格收口点的行为差异」以证明删除安全:

| 输入 | 旧宽容路(parse_*,被删) | 严格收口路(require_choice→Enum(),保留) | 差异 |
|---|---|---|---|
| 合法 "CR" / "WEIGHTED" | 返回对应枚举 | 返回对应枚举 | 等价 |
| 大小写/空白 " atc " | strip().lower() 后命中 → 枚举 | require_choice 先 `str().strip().lower()`(optimizer_config.py:66)后查表 → 命中 | 等价 |
| 未知值 "unknown" | **静默返回 default**(违灵魂) | **raise ValidationError**(loud) | **反例:None/default vs raise** |
| 空串 "" / None | parse_strategy:170 静默 default | require_choice text="" 不在集合 → raise | **反例:default vs raise** |

→ 行为**故意不等价**:旧路兜底正是要铲的 P4。所以**不写 parity 测试钉等价**(钉了反而把兜底锁死)。验收方式改为:删除后跑全量回归确认生产路径(走严格入口)不受影响 + grep 确认零残留引用。两续命测试 :25 的兜底断言**必须随删除整体退场**,不得迁移、不得保留。

## 8) 爆炸半径(生产消费者 + 测试消费者 + 灾难链)

9 字段(phase1_blast)回盘核实:
- **prod_consumers:零**(已 grep 证实):`rg parse_dispatch_rule|parse_strategy core/ web/ data/ scripts/` 仅命中两测试 + 两定义自身,生产代码 0 调用;`core/algorithms/__init__.py __all__`(:17-24)**不导出**二者(实测 __all__ 仅含 SortStrategy/StrategyFactory/BatchForSort/GreedyScheduler/ScheduleResult/ScheduleSummary)。
- **test_consumers**:
  - `tests/resource_dispatch/test_dispatch_rule_case_insensitive.py:18` import,:20-25 断言(:25 兜底)。
  - `tests/algorithm/test_sort_strategy_case_insensitive.py:18` import,:20-25 断言(:25 兜底)。
- **downstream_effect**:代码本身生产 0 blast。
- **灾难链**:
  1. **删错(误删严格入口)**:若手滑删到 require_choice / schedule_params 的 `DispatchRule(rule_key)`,生产 string→enum 会丢失校验 → 静默 KeyError/ValueError 上抛或非法枚举入排产,炸派工/排序参数解析。**禁区:只删 parse_* 两函数,绝不碰 :39 以下 DispatchInputs/build_dispatch_key/mean_positive,绝不碰 schedule_params.py/optimizer_config.py。**
  2. **不删而保留(复活风险)**:留着死壳 → 未来有人 `from core.algorithms.dispatch_rules import parse_dispatch_rule` 复用 → 把「非法静默回退默认枚举」重新引回入口,灵魂线 P4 复发,且静默不报错最难发现。这是本债真正的危险面。
  3. **删函数但留测试**:测试 import 失败 → CI 红;或更糟,有人为救 CI 把兜底语义迁回别处。**所以测试必须连删。**

## 9) 承重毗邻标记(同文件 LB + 禁区行)

- lb_no_touch = **null**,load_bearing = **false** → R51 两符号本身非承重。
- lb_colocation_danger = **none**:
  - dispatch_rules.py 内无 LB 债(siblings R49/R50 均 false)。
  - sort_strategies.py 内无 LB 债。
  - interference_edges 提到 **LB07** 同 `schedule_params.py`——但那是收口点所在文件,R51 **不改 schedule_params.py**,故 LB07 与本债删除互不接触(只在「确认收口点在位」时只读它,不写)。
- **禁区行(绝不触碰)**:
  - `core/algorithms/dispatch_rules.py:39` 起 `DispatchInputs` / `:55 build_dispatch_key`(R49)/ `:112 mean_positive`(R50)——非本债。
  - `core/algorithms/greedy/schedule_params.py` 全文(收口点 :266-277/:337-346,只读)。
  - `core/services/scheduler/run/optimizer_config.py` 全文(require_choice :64-69、:166/:189,只读)。

## 10) 分层 / 导入环风险

- R51 是**纯删除**,不新增任何 import → **不可能引入越层或导入环**。
- 现状导入方向核实:`schedule_params.py:13-14` `from ..dispatch_rules import DispatchRule` / `from ..sort_strategies import SortStrategy`——均 core.algorithms 内部,合规。删 parse_* 不影响这些 enum import(parse_* 与 enum 同模块,删函数不删 enum)。
- 收口点在 `core/algorithms/greedy/`(同 algorithms 子树)与 `core/services/scheduler/run/`(services 调 algorithms 的 enum,正向),无 core.algorithms→core.services 反向越层。
- 分层红线:0 AST 违规,删除不引入新边。**合规**。

## 11) 测试耦合(动手前先迁哪些测试)

- 唯二耦合测试:`tests/resource_dispatch/test_dispatch_rule_case_insensitive.py`、`tests/algorithm/test_sort_strategy_case_insensitive.py`。
- **处置 = 整体退场,不是迁移**:它们 :25 钉的兜底语义是要铲的对象,迁移=续命=违灵魂。删函数同 PR 删这两文件。
- 删前确认:`rg parse_dispatch_rule|parse_strategy tests/` 仅命中这两文件(已核实),无其它测试依赖 → 退场后无遗留红。
- 其它测试(走严格收口 require_choice 的)不受影响,无需迁。

## 12) 前置依赖(先做哪些债 / 哪些 F 门)

- **硬前置已闭合**：无硬前置；严格收口点仅只读确认，未写 `schedule_params.py` / `optimizer_config.py`。
- **软序已闭合**：R51 已随 R49/R50 同一 G24 原子 diff 执行，避免了 `dispatch_rules.py` 行号互撞。
- **验证口径**：本簇按窄测、ruff、py38 扫描、import 冒烟、残留 `rg` 和子代理复审收口；整批全门禁留到 Batch-B go-no-go。

## 2026-06-08 执行留档

- 已删：`parse_dispatch_rule`、`parse_strategy`、两份 case-insensitive 续命测试文件。
- 已保留：`DispatchRule`、`SortStrategy`、`DispatchInputs`、`build_dispatch_key`、严格入口 `schedule_params.py` / `optimizer_config.py`。
- 残留检查口径：`parse_dispatch_rule` / `parse_strategy` 在生产/测试 Python 源内应零命中；不得用新测试迁回 `unknown -> default` 静默回退。

## 执行前对抗核验(历史第二双眼睛)

> 以下为 2026-06-05 执行前的只读核验记录，用来说明为什么当时允许直删；2026-06-08 执行后，两个宽容解析器和两份续命测试已不存在，勿当成当前代码事实。

独立重 grep(只读,未照抄档案),结论 **PASS**。

重盘命令与结果:
- `rg -n "def parse_dispatch_rule" core/algorithms/dispatch_rules.py` → **:28**(逐字命中,0 漂移);`rg -n "def parse_strategy" core/algorithms/sort_strategies.py` → **:161**(逐字命中,0 漂移)。
- 函数体 `sed` 复读:dispatch_rules.py:33 `return DispatchRule(str(value).strip().lower())` / :34 `except ValueError:` / :35 `return default`;sort_strategies.py:170 `if not s: return default` / :172 `except Exception:` / :173 `return default`。与 §1 完全一致。
- 全仓引用 `rg "parse_dispatch_rule|parse_strategy"`(排除 docs/_panorama_data):**生产 0 调用**。命中仅 = 两定义自身 + 两测试(`tests/resource_dispatch/test_dispatch_rule_case_insensitive.py:18/20-25`、`tests/algorithm/test_sort_strategy_case_insensitive.py:18/20-25`)+ `audit/2026-03/` 历史审计 csv/json/md(纯文档记录,非代码依赖,删除不影响)。`prod_consumers=0` 证实。
- `core/algorithms/__init__.py` 实测 `__all__` 仅 SortStrategy/StrategyFactory/BatchForSort/GreedyScheduler/ScheduleResult/ScheduleSummary,**不导出** parse_*。证实。
- **收口点真实性核验(主动找反例)**:`require_choice`(optimizer_config.py:64-69)体内 `text = str(value or "").strip().lower()` 后 `if text not in set(valid_values): raise ValidationError(...)` —— **strip/lower 标准化与旧路等价,但未知/空值 loud raise 而非静默 default**,符合灵魂线。`schedule_params.py:_require_choice`(:71-78)同样标准化 + 对空串/未知双重 raise。收口点 :277 `SortStrategy(strategy_key)`、:346 `DispatchRule(rule_key)`、optimizer_config.py:166/189 全部在位。§7 反例表(未知值/空串=旧静默 default vs 收口 loud raise,故意不等价)成立 → 「不写 parity 钉等价、测试连退场」判断正确。
- 档案微瑕(不影响结论):§6 引 `_resolve_strategy` 为 `:266`,实测 `:265`(差 1 行,符号命中);`_resolve_dispatch_rule_enum :337` 实测命中。属软引用,非本债待删行。
- 执行前 siblings 重盘:R49 `build_dispatch_key:55` / `_safe_positive:73`、R50 `mean_positive:112` 全命中 → §5「R51 删 :28-35 致下方上移、须与 R49 同批 Batch-7 改后按符号名重盘」成立。
- 历史状态：当时两文件 + 两测试 `git status --porcelain` **空**(未动工),与当时 status=planned 一致；2026-06-08 已 fixed 后不再代表当前工作区。

执行前判定:
- **行号复核:一致**(parse_dispatch_rule:28 / parse_strategy:161 逐字命中,0 漂移)。
- **承重误删风险:无**。lb_no_touch=null、load_bearing=false,修法为纯物理删除,**无删除/统一/透传/加形参混入**,禁区行(:39 以下 DispatchInputs/build_dispatch_key/mean_positive + schedule_params.py/optimizer_config.py 全文只读)已明列。
- **灵魂线:合规**。直删不新增兜底;收口点已是 loud raise,无需在此重复改 raise;测试连退场避免把 P4 兜底钉成契约。
- **owner_pending=false 核验**:给终态修法(直删 + 退测试)未违规,无擅自分配越权。
- **分层**:纯删除不新增 import,0 越层,合规。
- 争议点:无。无需升级第三方裁。
