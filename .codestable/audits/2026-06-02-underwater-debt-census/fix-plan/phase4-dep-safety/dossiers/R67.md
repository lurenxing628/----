# R67 逐债档案 — 资源筛选6个请求参数名清单在三处各抄一遍

> 干扰簇 C01 | 桶 B07 | 病理 P3(维护面重复) | 严重度 low | 承重 false | owner_pending **true**
> 本档案只读不改,只产计划。所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。

---

## 字段 1 — 当前真实 file:line(grep 回盘 + 漂移记录)

`old_location_DONT_TRUST` = `web/request_resource_context.py:20-28 + web/routes/reports_request_support.py:53-61 + web/routes/reports_export_support.py:13-29`。逐符号 rg 回盘:

| 符号 / 抄写点 | 老行号(勿信) | **当前真实 file:line** | 漂移 |
|---|---|---|---|
| 共享常量 `REPORT_RESOURCE_FILTER_ARG_KEYS` | — | **全仓 NOT FOUND**(`rg REPORT_RESOURCE_FILTER_ARG_KEYS web/ core/ data/` 空) | n/a,确认不存在 |
| 抄点①`_request_resource_values()` 6 键字典 | :20-28 | `web/request_resource_context.py:20`(def),6 键体 `:22-27` | 几乎无(±1) |
| 抄点②`request_resource_filter()` 6 位/键参 | :53-61 | `web/routes/reports_request_support.py:54`(def),6 实参 `:56-61` | 几乎无(±1) |
| 抄点③`_EXPORT_CONTEXT_KEYS` 15 键元组 | :13-29 | `web/routes/reports_export_support.py:13`(元组首),6 资源键**尾块** `:23-28` | 几乎无 |
| 抄点④`_REPORT_CONTEXT_FIELD_NAMES` 16 键元组 | (registry 未列老行号) | `web/viewmodels/scheduler_navigation_links.py:11`(元组首),6 资源键**尾块** `:22-27` | n/a(本档案新登记的第 4 处) |
| 收口点 `normalize_report_resource_filter` | — | `core/services/report/report_context_filters.py:119`(def),6 关键字参 `:120-126` | 准 |

**漂移结论:无显著漂移**(全部 ±1 行内)。registry 的 `location_status` 自评「✅准」属实。
唯一新增事实:registry current_evidence 已正确把**第 4 处** scheduler_navigation_links.py 纳入,但 `old_location_DONT_TRUST` / `all_files` 只列了前 3 个文件 —— **第 4 处不在 all_files**,修法时若漏它则仍是半截去重(见字段 4 警示)。

## 字段 2 — 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05` = **planned**。
- 与 evidence 一致性:**一致**。current_evidence 称「抽常量去重未开始」「`REPORT_RESOURCE_FILTER_ARG_KEYS` 全仓 grep 为空」——本轮独立回盘复现:常量确不存在,4 处仍逐键手抄。无任何 in_progress 痕迹(唯一有 diff 的 reports_request_support.py 改的是无关的 `execution_review_plan_identity_error` 守卫,`request_resource_filter()` 体 `:54-61` 未动 —— 回盘确认该函数仍是手抄 6 实参,与 evidence 描述吻合)。
- 状态判定:**planned 属实,未开工。**

## 字段 3 — 病理 / 严重度 / 桶 / 簇

- **病理 P3 维护面重复**:同一份「6 个资源筛选请求参数别名清单」(`resource_type / resource_id / scope_type / scope_id / machine_id / operator_id`)在 4 处各手抄一遍 —— ①喂收口点 `normalize_report_resource_filter`(抄点①②)、②拼导出 URL(抄点③)、③拷贝导航上下文字段(抄点④)。新增或改名一个别名,要同步 4 处,漏一处即"半截":某入口少认一个资源 key,筛选/导出**静默缺失**(不报错、不崩、数据不坏)。
- **严重度 low**:纯维护面隐患,blast=low(字段 8)。无运行期正确性损伤,只在"未来某次改别名漏同步"时才显形。
- **桶 B07** / home B07;**簇 C01**;`kind=real`(非幻觉);`needs_adversarial=false`(无须对抗,病理直白)。

## 字段 4 — 修法类(owner_pending:暂不给终态修法)

> ⚠️ **owner_pending = true:待 owner 裁断,本档案暂不给终态修法、暂不分配执行批次。** 以下为依赖透视下的"候选修法形状",非拍板方案。

- **修法类 = 收口到 X(抽单一常量,多处复用)** —— 注意:此处的"收口"指**抽一份别名键清单常量**作为单一真相源,**不是新建/改动收口点本身**。
- **P5 收口?否。** 真正的归一收口点 `normalize_report_resource_filter`(`report_context_filters.py:119`)**未被任何抄点绕过**(抄点①②均 import 并调用它;抄点③④只是拷 URL/上下文字段,不做归一)。故本债**不动收口点函数体/签名**,只把"6 个键名"这份重复字面量收敛。
- **候选常量 home(收口到已存在点)**:放在收口点同模块 `core/services/report/report_context_filters.py`,定义
  `REPORT_RESOURCE_FILTER_ARG_KEYS = ("resource_type","resource_id","scope_type","scope_id","machine_id","operator_id")`,
  使其与该模块 `:120-126` 的 6 个关键字参**同生共死**(改名一处即触双改,这正是去重要点)。**这是已存在模块,不算新建第二模块(非新 P5)。**
- **owner 须裁断的真问题**(为何 owner_pending):
  1. **第 4 处该不该并入?** 抄点④ `_REPORT_CONTEXT_FIELD_NAMES`(16 键)、抄点③ `_EXPORT_CONTEXT_KEYS`(15 键)都是 **superset**,6 个资源键只是其**尾部连续块**。把常量塞进去要做**元组拼接**(`(...前缀键..., *REPORT_RESOURCE_FILTER_ARG_KEYS)`),不能整体替换。owner 需裁:为去重 6 个键而让两份 superset 元组依赖一个外部常量,是否划算 / 是否反而降低可读性(superset 本就含非资源键)。倾向:**抄点①②**(纯 6 键,语义=喂收口点)收编明确;**抄点③④**(superset)收编收益小、耦合 navigation,**可只收①②、③④保持现状或仅加注释**。
  2. 常量是 `tuple` 还是 `frozenset`:抄点③④用顺序(URL 拼接/字段遍历顺序),抄点①②不依赖顺序 —— 须 tuple 保序。
- **铁律自检**:本修法不删除生产路径(灵魂线 OK)、不加兜底/静默回退/except 吞错(P3 非 P4,无 raise 改造需求)、不新建第二收口模块(常量入已存在 home)。✅

### 禁区(即便最终动手)
- **禁动** `normalize_report_resource_filter` 的签名与 6 个关键字参顺序/默认值(`report_context_filters.py:120-126`)—— 契约测试钉死(字段 7)。
- **禁改** 抄点③④ superset 元组里**非资源键**(`plan_id/back_to/scenario_id/date_*/batch_id` 等)的成员与顺序 —— 那是 R42(plan_id 死面包屑)的地盘 + navigation URL 契约。
- **禁把** 4 处的"读取方式"统一(抄点①读 `_request_arg`、②读 `request.args.get`、③读 `request.args.get` 带 `_has_text` 过滤、④是字段名拷贝)——本债只统一**键名清单**,不统一读取逻辑(那是越界)。

## 字段 5 — 同文件碰撞债

- `same_file_siblings = []` —— **R67 在其主文件 `request_resource_context.py` 内无同文件兄弟债**。
- 但 `interference_edges` 有 1 条 **same_file 边 → R42**,因为两债共享**第三个文件** `web/routes/reports_export_support.py`:
  - **R42**(plan_id 死面包屑)primary=`navigation_context.py`,all_files 含 `reports_export_support.py`。R42 关注 `plan_id` 这个 key 的"读入→存上下文→回吐 URL 却不进 resolver"。
  - **R67** 在 `reports_export_support.py` 只碰 `_EXPORT_CONTEXT_KEYS` 元组的**资源键尾块 `:23-28`**;R42 碰的是 `plan_id`(元组首 `:14`)及其消费链。
  - **撞行号/dict 键位移风险:低但非零。** 两债都可能编辑同一个 `_EXPORT_CONTEXT_KEYS` 元组(`:13-29`):R67 若把资源键替换为 `*REPORT_RESOURCE_FILTER_ARG_KEYS` 展开,会改动元组**字面布局**;R42 若删除 `plan_id` 成员,也改动同元组。**两者改的是元组的不同段**(R42 改首/中,R67 改尾),不改对方的键,但**会撞同一处 diff hunk**。
  - **先后建议**:谁先落都行(零语义冲突),但**避免并行编辑同一元组**。若 R42 要删 `plan_id`,先让其落定,R67 再基于新布局抽尾块;反之亦可。`fix_invalidation_risk=none`(registry)在"语义"层成立,但"diff 层"建议串行同文件编辑。
- 同桶 **R05**:同桶 B07、**不同收口点、不同文件**,`co_change_within_group=[R05]` 但 `fix_invalidation_risk=none` → **可完全并行,无行号/键位冲突**。

## 字段 6 — 同收口点债

- R67 的"常量 home"若落在 `report_context_filters.py`,该模块也是收口点 `normalize_report_resource_filter` 所在。**但 R67 不改该函数**,只**新增**一个模块级常量 + `__all__` 导出。
- **同收口点其他债**:本轮 registry 未标出另一条"也要改 `normalize_report_resource_filter` 函数体"的债与 R67 在同符号上碰撞(`interference_edges` 仅 same_file→R42,`same_symbol=false`)。故**无同收口点顺序敏感性**。
- 顺序敏感性结论:**无**。R67 是"新增常量并让 4 处 import",不与任何改 collar 行为的债排队。

## 字段 7 — 收口行为差异及所需 parity 测试

- 本债**不改任何运行期行为**(纯字面量去重:把 4 份手抄键名换成 import 同一常量;键名集合与顺序逐字不变)。理论上**无行为差异、无新旧两路 parity 问题**。
- 但仍须 **回归护栏**(防"抽常量时手滑漏键/改名/改序"):
  1. **既有契约测试必须保持绿**:`tests/scheduler_analysis/test_report_context_filters_contract.py`(收口点别名优先级契约,`:89-103` 覆盖 `machine`+`operator_id`、`machine`+`machine_id` 等边界)——它钉死收口点签名/语义,R67 不动收口点故应原样通过。**反例守卫**:若抽常量时误把 `machine_id`↔`operator_id` 顺序调换或漏一键,抄点①②喂给收口点的 `**values` 仍按 dict key 匹配关键字参,漏键 → 该别名永远传 `None` → 该契约用例(如 `machine_id=M-OTHER` 优先级)**会红**,可捕获。
  2. **新增轻量等价测试(建议)**:断言 `set(REPORT_RESOURCE_FILTER_ARG_KEYS) == {收口点 6 个关键字参名}`,且 `_EXPORT_CONTEXT_KEYS` / `_REPORT_CONTEXT_FIELD_NAMES`(若收编)**仍各自含且仅含这 6 个资源键**(superset 其余键不变)。这是"键清单单一真相源"契约,正是去重要落的护栏。
  3. **边界反例**:抄点①用 `{k: _request_arg(k) for k in KEYS}`,抄点②用位置+关键字混传(`resource_type`/`resource_id` 走位置参,其余走关键字)。若把抄点②也改成 `**{k:... for k in KEYS}`,行为等价(收口点前两参非 keyword-only);但若 KEYS 顺序变,抄点②的位置参语义不会错(因走 dict 解包按名匹配)——**故顺序对①②无害,只对③④(URL/字段遍历序)有害**。parity 重点放在③④的遍历序不变。

## 字段 8 — 爆炸半径

**生产消费者(直接读 R67 抄写点产出的下游)**:
- 抄点①`_request_resource_values` → `request_report_resource_context()`(`request_resource_context.py:35`,喂 `normalize_report_resource_filter` 于 `:39`)。其下游消费者:
  - `web/navigation_context.py:55`
  - `web/routes/dashboard.py:169`
  - `web/routes/domains/scheduler/scheduler_week_plan_query.py:22`
  - `web/routes/domains/scheduler/scheduler_analysis.py:26`
  - `web/routes/domains/scheduler/scheduler_navigation_publish.py:48`
- 抄点②`request_resource_filter()`(`reports_request_support.py:54`)→ `web/routes/reports_page_support.py:59,371`、`web/routes/reports_export_routes.py:32,60,100,128`。
- 抄点③`_EXPORT_CONTEXT_KEYS` → `current_report_export_url()`(`reports_export_support.py:38` 遍历)拼导出 URL。
- 抄点④`_REPORT_CONTEXT_FIELD_NAMES` → `scheduler_navigation_links.py:190` 遍历拷字段。

**测试消费者**:
- `tests/scheduler_analysis/test_report_context_filters_contract.py:89-103`(收口点别名优先级契约)。

**灾难链(删错/统一错会静默炸什么)**:
- **静默缺 key**:抽常量时漏一个键(如漏 `scope_id`)→ 受影响入口(报表页筛选 / 导出 URL / 导出路由 / 周计划查询)对该资源维度**静默不筛**,返回的行不报错但范围错。**不损坏数据**,blast=**low**。
- **顺序错(仅③④)**:若把③④的资源键顺序打乱 → 导出 URL query 参数顺序变 / 导航字段拷贝顺序变 —— URL 语义不依赖参数顺序,字段拷贝按 name 取值也不依赖顺序,**实际无害**,但可能让 URL 字符串断言型测试(若有)红。
- **最大风险点**:不是删,而是**半截去重**——只把 4 处中的 1~2 处换成常量、其余仍手抄,反而制造"看起来已统一实则仍分裂"的假象,未来改别名更易漏。故 owner 若批准动手,要么**全收**(①②必收,③④酌情),要么**全不收只加注释**,忌只收一半。

## 字段 9 — 承重毗邻标记

- `load_bearing = false`、`lb_no_touch = null`、`lb_colocation_danger = "none"`(registry phase1_blast)。
- 4 个抄写文件 + 收口点模块**均无 load_bearing=true 的毗邻债**钉在同行附近。
- **禁区行**(非承重禁区,而是契约/越界禁区,重申字段 4):
  - `core/services/report/report_context_filters.py:119-127`(收口点签名,禁动)。
  - `reports_export_support.py:14-22`(`_EXPORT_CONTEXT_KEYS` 非资源键前缀)、`scheduler_navigation_links.py:12-21`(`_REPORT_CONTEXT_FIELD_NAMES` 非资源键前缀)——R42 / navigation 契约地盘,禁改成员与序。
- 承重风险:**无**。

## 字段 10 — 分层 / 导入环风险

候选常量 home = `core/services/report/report_context_filters.py`(core.services 层)。各抄写点引入它的越层/环分析:
- 抄点① `web/request_resource_context.py:7` **已 import** `from core.services.report.report_context_filters import normalize_report_resource_filter` —— 加一个同模块常量名 **零新增依赖、零环**(`web → core.services`,合法下行)。
- 抄点② `web/routes/reports_request_support.py:10` **同上已 import 该模块** —— 零新增依赖。
- 抄点③ `web/routes/reports_export_support.py` 当前**未 import** 该模块,但已 import `core.services.report.*` 其它符号(`:8-10`)—— 新增 `from core.services.report.report_context_filters import REPORT_RESOURCE_FILTER_ARG_KEYS` 仍是 `web → core.services` 合法下行,**无环**(report_context_filters 不反向 import web)。
- 抄点④ `web/viewmodels/scheduler_navigation_links.py` 当前只 import 同层 `web.viewmodels.*`(`:6-7`),**无 core import**。若收编它,会新增 `web.viewmodels → core.services.report` 边。需查该方向是否已被分层规则允许(viewmodels 通常允许读 core.services)——**这是 owner 收编第 4 处前要过的 AST 门**;若不确定,**第 4 处保持现状/仅注释**,完全规避此边。
- 收口点模块自身 import 链:`core.infrastructure.errors` + `core.models.schedule_resource_filter` + 同包 `.calculation_helpers`(`:5-11`)—— 全是合法下行/同层,**新增一个纯字面量常量不引入任何新 import,绝无环**。
- **分层红线(0 AST 违规)结论**:①②③ 安全;④ 须先验 `web.viewmodels → core.services` 边是否合规(若已有同类边则安全)。

## 字段 11 — 测试耦合

- **动手前需关注/可能要迁的测试**:
  - `tests/scheduler_analysis/test_report_context_filters_contract.py`(`:89-103` 等)—— 收口点契约。R67 不改收口点,**无须迁移**,只须保持其绿(回归护栏,见字段 7)。
  - 若有针对**导出 URL 字符串**或**导航字段集合**的断言型测试(本轮未在 registry test_consumers 列出,grep 亦未见专测 `_EXPORT_CONTEXT_KEYS`/`_REPORT_CONTEXT_FIELD_NAMES` 的用例)——收编③④前应 grep 确认,有则需保证键序/集合不变。
- **建议新增**(非迁移):字段 7.2 的"键清单单一真相源"等价断言测试,与常量同提交。
- 测试耦合度:**低**。R67 不触碰被测的运行期语义,测试只作"抽常量没改坏键集"的守门。

## 字段 12 — 前置依赖

- **F 门**:
  - **AST/分层门**:仅当 owner 决定收编第 4 处(scheduler_navigation_links.py)时,须先确认 `web.viewmodels → core.services.report` 边合规(字段 10);只收①②③ 则无此门。
- **债前置**:
  - **无硬前置**。`planned_deps_hint`:与 R05 同桶不同收口点零冲突,**可完全并行**;`cross_bucket_same_file_edges=[]`;`planned_batch_hint`=Batch-8(B07)桶内优先级 2,**Batch-1 之后任意时点可落**,无前置/被前置约束。
  - **软排序(同文件 diff,非语义)**:与 **R42** 共享 `reports_export_support.py` 的 `_EXPORT_CONTEXT_KEYS` 元组。建议**避免与 R42 并行编辑该元组**;若 R42 计划删 `plan_id` 成员,串行(任一先)以免 diff hunk 互撞(字段 5)。语义层 `fix_invalidation_risk=none`。
- **owner 裁断前置(本债特有,owner_pending=true)**:
  1. 是否收编第 4 处(superset + 跨层边)?
  2. ③④两个 superset 元组用"前缀键 + `*常量`展开"是否可读 / 是否值得?
  倾向建议(供 owner 参考,非拍板):**①②必收(纯 6 键、语义=喂收口点、零新增依赖);③④可只加"我是故意维持本地清单"注释或暂不收**,以最小爆炸半径落地 low 债。

---

## 对抗核验(第二双眼睛)

**核验结论:PASS**(2026-06-05,独立重 grep,未发现反证)。

### 重 grep 命令与结果
1. `rg -n "REPORT_RESOURCE_FILTER_ARG_KEYS" web/ core/ data/ tests/` → exit=1(空)。**共享常量确不存在**,4 处仍手抄,planned 属实。
2. 抄点① `web/request_resource_context.py:22-27` 6 键字典 + `:7` import 收口点 + `:39` 调用 `normalize_report_resource_filter(**values)` —— **行号逐一命中,一致**。
3. 抄点② `reports_request_support.py:56-61` 6 实参(`:54` def,`:55` 调收口点)+ `:10` import `normalize_report_resource_filter` —— **一致;抄点②确已 import 收口模块**(档案字段10断言正确)。
4. 抄点③ `reports_export_support.py:23-28` 资源键尾块(`_EXPORT_CONTEXT_KEYS` 元组 `:13-29`,首键 `plan_id` 为 R42 地盘)—— **一致;该文件本次 grep 未 import `normalize_report_resource_filter`**,印证档案字段10「抄点③当前未 import 收口模块」。
5. 抄点④ `web/viewmodels/scheduler_navigation_links.py:22-27` 资源键尾块(`_REPORT_CONTEXT_FIELD_NAMES` 元组 `:11` 首)—— **一致**。
6. 收口点 `report_context_filters.py:119` def + `:120-126` 6 关键字参(`resource_type, resource_id, *, scope_type, scope_id, machine_id, operator_id`)—— **签名逐字命中,一致**。
7. `rg "normalize_report_resource_filter" reports_export_support.py scheduler_navigation_links.py` → exit=1 → **抄点③④确不调用收口点(只拼 URL/拷字段)**,证实「收口点未被任何抄点绕过」「本债非 P5」成立。
8. 契约测试 `tests/scheduler_analysis/test_report_context_filters_contract.py:89-103` → 实测覆盖 `machine_id=M-OTHER`「设备编号冲突」、`scope_type/scope_id`「资源筛选类型/编号冲突」等 raises 用例 —— **行号与语义吻合**,可作"抽常量漏键即红"的回归护栏。

### 争议/补强点(均不翻案,记录供 owner 参考)
- **【补强,非反例】R42 共享文件不止一个**:独立查 registry,R42 的 `all_files` 同时含 `reports_export_support.py` **与** `scheduler_navigation_links.py`(抄点③④两处)。档案字段5只点了"共享第三个文件 reports_export_support.py",**实际 R67↔R42 同文件碰撞面覆盖抄点③④两个 superset 元组所在文件**。结论不变(两债改不同键、零语义冲突),但**串行避 diff hunk 互撞的范围应同时覆盖 `scheduler_navigation_links.py`**,不止 `reports_export_support.py`。
- **【证实,降不确定性】分层边已有先例**:`web.viewmodels → core.services` 边**已存在合法先例** `web/viewmodels/scheduler_resource_dispatch_execution.py:23 from core.services.scheduler...`。故 owner 若收编抄点④,新增 `web.viewmodels → core.services.report` 边**有同向先例,过 AST 门概率高**(档案字段10留作保守 owner 裁断,合理)。
- **半截去重风险属实**:第 4 处 `scheduler_navigation_links.py` 确**不在** registry `all_files`(只列前 3 文件)。档案字段1/字段8「忌只收一半」的警示**成立且重要**——动手前须把第 4 处一并纳入决策范围。

### 铁律自检
- 承重:`load_bearing=false`、`lb_no_touch=null`、`lb_colocation_danger=none` —— **无承重误删风险**。
- owner_pending=true:档案字段4 明确标注「暂不给终态修法、暂不分配执行批次」,只给"候选修法形状"+ 透析 2 个 owner 裁断问题(是否收编第4处 superset / tuple 可读性)—— **未擅自拍板,合规**。
- 灵魂线/兜底:本债 P3 纯字面量去重,候选修法不删生产路径、不加兜底/静默回退/except 吞错、不新建第二收口模块(常量入已存在 home `report_context_filters.py`)—— **合规**。
- 分层红线:①②③ 零新增依赖;④ 新增边有先例 —— **0 AST 违规可达**。

**核验人结论:行号全部一致,承重/owner_pending/分层/收口等价判断均经独立 grep 复现,无须升级第三方裁断。唯一补强:R42 同文件碰撞面应扩到 `scheduler_navigation_links.py`。**

---
*回盘时间 2026-06-05;本档案仅依据当前工作区 grep 实证,不照抄 registry 老行号。所有 file:line 已逐符号 rg 验证。*
