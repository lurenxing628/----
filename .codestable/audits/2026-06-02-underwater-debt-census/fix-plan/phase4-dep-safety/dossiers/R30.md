# R30 逐债档案 — compat 解析「日期」分支死切片（生产零消费，仅测试续命）

> 本档案只读不改、只产计划。所有断言均附 2026-06-05 当前工作区 grep 回盘证据。
> 桶 B06 · 簇 C01 · 病理 P6（死分支/直删类）· 严重度 low · 承重 false · owner_pending false

---

## 字段 1：当前真实 file:line（回盘）+ 漂移记录

R30 是「整条 date/datetime 切片」，不是单符号，涉及两个文件的 5 处实现 + 3 个常量。逐符号 rg 回盘（2026-06-05 当前工作区）：

| 符号 | old_location_DONT_TRUST | 实际 file:line（grep 回盘） | 漂移 |
|---|---|---|---|
| `parse_compat_date`（def） | compat_parse.py:198 | `core/shared/compat_parse.py:198` | **无漂移** |
| `parse_compat_date` 内 VALUE_DATE 分支 | — | `core/shared/compat_parse.py:207`（`_resolve_compat_policy(field, expected_kind=VALUE_DATE)`）、`:208`（parser 选择）、`:213`（`_date_fallback`） | 准 |
| `_date_fallback`（def） | — | `core/shared/compat_parse.py:143-150` | 准 |
| date 专用 import `parse_optional_date` / `parse_required_date` | — | `core/shared/compat_parse.py:11` / `:14` | 准 |
| `due_date` FieldPolicy | value_policies.py:180 | `core/shared/value_policies.py:179-188`（field= 在 :180，value_kind=VALUE_DATE 在 :183） | 准（块体 179-188） |
| `start_time` FieldPolicy | — | `core/shared/value_policies.py:189-198`（field= 在 :190，READ_FILTER_ONLY 在 :192，VALUE_DATETIME 在 :193） | 准 |
| `end_time` FieldPolicy | — | `core/shared/value_policies.py:199-208`（field= 在 :200，READ_FILTER_ONLY 在 :202，VALUE_DATETIME 在 :203） | 准 |
| `READ_FILTER_ONLY` 常量 | :12 | `core/shared/value_policies.py:12` | **无漂移** |
| `VALUE_DATE` 常量 | :16 | `core/shared/value_policies.py:16` | **无漂移** |
| `VALUE_DATETIME` 常量 | :17 | `core/shared/value_policies.py:17` | **无漂移** |

漂移结论：**全部无漂移或仅块体边界微调**。registry 的 location_status 标 ✅准，回盘一致。
注意：registry planned_fix_hint 把 `_date_fallback` 标为「:143-150」、`parse_compat_date` 标为「:198-215」、三 FieldPolicy 标为「:179-208」，回盘后块体精确落点：`due_date`=179-188、`start_time`=189-198、`end_time`=199-208（registry 的 179-208 是三块合并区间，准确）。

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05` = **planned**。
- current_evidence 声称「原样未动，两文件 git status 空」。回盘核对：`parse_compat_date`@:198、`_date_fallback`@:143、import@:11/:14、三 FieldPolicy@:179-208、三常量@:12/:16/:17 **全部在位且与 evidence 描述逐项吻合**。
- 一致性结论：**与 status evidence 完全一致，未动**。无 in_progress 痕迹。

## 字段 3：病理 / 严重度 / 桶 / 簇

- 病理 **P6（死分支，直删类）**：`parse_compat_date` 全仓生产调用 = 0（见字段 8 回盘）；`due_date`/`start_time`/`end_time` 三策略仅被 `parse_compat_date` 的 `_resolve_compat_policy(...VALUE_DATE)` 与契约测试触达，生产读侧时间留痕走 `_sched_display_utils.py:84 record_bad_time_row`（直接 `parse_dt` + 跳过，绕开 value_policies）。
- 严重度 **low**：死代码，删除不改变任何生产行为；唯一风险是测试与 facade re-export 的连带断裂。
- 桶 **B06**（core-infra-shared 收敛）；home=B06。
- 簇 **C01**：与 R29 / R31 / R33 共享 compat_parse.py / value_policies.py / field_parse.py，全 same_file 无 same_symbol。
- 灵魂线核对：R30 是**直删**死分支，不新增兜底/静默回退/except 吞错；删除后 `due_date` 的兼容回退路径整体消失，而生产本就不走它 → 无「降级标记丢失」风险。无需 P4 式 loud raise 改造。

## 字段 4：修法类

**修法类 = 直删 + 退测试（P6 死分支删除）**。承重=false，故允许删；非收口、非注释、非 raise、非 parity。owner_pending=false，给终态修法。

终态删除清单（grep 实证每项删后无生产引用）：

1. `core/shared/compat_parse.py`：
   - 删 `parse_compat_date`（:198-215，整函数）；
   - 删 `_date_fallback`（:143-150，整函数）；
   - 删 import 行 `parse_optional_date`（:11）与 `parse_required_date`（:14）——**仅这两名在本文件只被 date 路径用**（字段 8 已 grep 证实本文件内 date import 命中只在 :150/:208）；**严禁误删同 import 块的 `parse_optional_float`/`parse_required_float`/`parse_optional_int`/`parse_required_int`/`is_blank_input`**，它们被存活的 `parse_compat_float`/`parse_compat_int` 用（:165/:168/:188/:191 等）。
2. `core/shared/value_policies.py`：
   - 删 `due_date` FieldPolicy（:179-188）、`start_time`（:189-198）、`end_time`（:199-208）；
   - 删常量 `READ_FILTER_ONLY`（:12）、`VALUE_DATE`（:16）、`VALUE_DATETIME`（:17）——删 `VALUE_DATE` 的前提是 `parse_compat_date` 与 `due_date` 策略已先删（`VALUE_DATE` 仅这两处 + facade re-export 用，见字段 8）。

**收口点：本条不是收口债（P6 直删），不收口到任何统一点。** 不新建任何模块。

退测试（动手范围内必须同步退，否则红，见字段 11）：
- `tests/models_domain/test_compat_parse_emits_degradation.py`：退 `parse_compat_date` import（:18）与 due_date 用例（:39 及其断言、events/codes 期望里属于 date 的项）；
- `tests/config/test_value_policies_matrix_contract.py`：退 `READ_FILTER_ONLY` import（:20）、`expected_fields` 里 `due_date`/`start_time`/`end_time` 三项（:46-48）、due_date 断言块（:89-91）、start_time/end_time 断言块（:92-95）；
- `tests/config/test_config_service_component_contract.py`：退 R30 直接相关的 `parse_compat_date` 身份断言（:411）——**此行属 R33 步骤2 范围，须在 R33 同步删，不由 R30 单独动**（见字段 5 顺序约束）。

## 字段 5：同文件碰撞债

同文件兄弟（registry same_file_siblings + interference_edges same_file=true）：

| sibling | 落点文件 | 改什么段 | 与 R30 撞不撞 |
|---|---|---|---|
| **R33** | compat_parse.py / field_parse.py / 两 regression + value_policies.py | R33 删 `core/services/common/compat_parse.py` re-export 空壳（:3-7 import、:9-13 `__all__`）+ R33 身份断言 `config_service_component_contract.py:411` | **强耦合同链，撞**。详见下方硬顺序。 |
| **R29** | compat_parse.py / field_parse.py / value_policies.py | 与 R30 same_file 不 same_symbol | 同文件，但 R29 改的不是 date 符号；若 R29 也在 value_policies.py 增删块体，会与 R30 删 :12/:16/:17 常量 + :179-208 策略**抢同一文件行号位移**——需串行或一次性提交避免 patch 冲突。 |
| **R31** | compat_parse.py / value_policies.py | R31@B13 与 facade 相关（services.common.value_policies re-export） | 同文件。R31 跨桶 @B13；R30 删 `VALUE_DATE`/`VALUE_DATETIME`/`READ_FILTER_ONLY` 会使 `core/services/common/value_policies.py:6-8 import` + `:24-26 __all__` 三项失效（ImportError）→ **必须与 R31/B13 facade 协调同步删 re-export 三项**，否则 import 即崩。 |

**R33↔R30 硬顺序（registry planned_deps_hint 实证）**：
1. **R33 步骤1**：先把测试 import 从 `core.services.common.compat_parse` 迁到 `core.shared.compat_parse`（`regression_compat_parse_emits_degradation.py:18` 当前经 services.common 壳 import）。
2. **R30**：删 shared 的 `parse_compat_date`/`_date_fallback`/date import + 三 FieldPolicy + 三常量 + 退 date 用例。
3. **R33 步骤2**：删 services.common 壳 re-export + 删身份断言 `config_service_component_contract.py:411`。
违序后果：若 R30 先删 `parse_compat_date` 而 R33 步骤1 未迁 import → 降级测试与身份断言红（:18 import 失败、:411 `service_compat_parse.parse_compat_date` AttributeError）。
**dict 键位移注意**：value_policies 的 `_FIELD_POLICIES` 是 tuple，删三块后 `FIELD_POLICIES_BY_FIELD`（:211 推导）键集少 due_date/start_time/end_time——任何 R29/R31 若硬编码该 dict 顺序/长度会受影响（当前矩阵契约 `len(policies)==len(fields)` :87 会同步少 3，无序号硬编码风险）。

## 字段 6：同收口点债

R30 是 **P6 直删，不收口到任何符号**，故无「收口到同一统一点」的债。
但存在「**反向收口锚点**」需澄清：`VALUE_DATE`/`VALUE_DATETIME`/`READ_FILTER_ONLY` 三常量是 value_policies 的公共词汇表，被 `core/services/common/value_policies.py` facade 全量 re-export（:5-18 import、:21-37 `__all__`）。删这三常量等于改 facade 公共表面——顺序敏感：**必须 facade re-export（R31/B13）与 shared 删除（R30）同一批次或 facade 先收，否则 services.common 模块导入即 ImportError**，连带打挂任何 `from core.services.common.value_policies import ...` 的存活消费者（含矩阵契约测试 :18 经 services.common 取 READ_FILTER_ONLY）。

## 字段 7：收口行为差异及所需 parity 测试

R30 **不收口、不统一新旧两路**，是纯删除，故**无 parity 等价义务**——没有「保留路 vs 删除路」需逐分支对齐的情形。
唯一需守的是「删除前确认死」的**零消费证明**（已在字段 8 grep 实证），而非 parity。
反例澄清（为何不是 parity 债）：若误把它当「收口到 float/int 统一解析」会出错——`parse_compat_date` 的边界语义（`"2026-02-31"` 非法日期 → 走 `_date_fallback` → `due_date.compat_default=None` → 返回 None）与 float/int 分支的回退值/原因码完全不同（date 用 `invalid_due_date`，float 用 `invalid_number`），二者**不可统一**。结论：维持直删，不引入任何合并。

## 字段 8：爆炸半径（9 维）

1. **生产消费者清单**：`parse_compat_date` 生产调用 = **0**（rg 全仓：`core/`/`web/`/`data/`/`scripts/` 无任何 `parse_compat_date(` 调用形态）。唯一引用是 `core/services/common/compat_parse.py:4` re-export（= R33 的壳）。对照活跃分支：`parse_compat_float`/`parse_compat_int` 经 `core/shared/field_parse.py:6` 活跃消费（证实 float/int 活、date 死）。
2. **`due_date`/`start_time`/`end_time` 策略生产消费者**：0。生产读侧坏时间留痕走 `core/services/scheduler/_sched_display_utils.py:76-84`（`parse_dt(row.get("start_time"))` + `record_bad_time_row`），**直接解析、绕开 value_policies 三策略**——证实三策略生产无连线。
3. **三常量消费者**：`VALUE_DATE` 仅 `compat_parse.py:20/:207` + `value_policies.py:16/:183` + facade re-export；`VALUE_DATETIME` 仅 `value_policies.py:17/:193/:203` + facade；`READ_FILTER_ONLY` 仅 `value_policies.py:12/:192/:202` + facade + **测试 `regression_value_policies_matrix_contract.py:20/:94/:95`**。删后均无生产孤儿，但 facade 三 re-export 与测试三断言需同步退。
4. **测试消费者**：
   - `tests/models_domain/test_compat_parse_emits_degradation.py:18`（import）、`:39`（调用 `parse_compat_date`）；
   - `tests/config/test_value_policies_matrix_contract.py:20`（import READ_FILTER_ONLY）、`:46-48`（expected_fields 三项）、`:89-95`（due_date/start_time/end_time 断言）；
   - `tests/config/test_config_service_component_contract.py:411`（`parse_compat_date` 身份断言，R33 范围）+ `:395`（import shared value_policies，可能触达三常量）。
5. **灾难链 · 删错**：若删了 import 块里 float/int 的名字（误伤 :12/:13/:15/:16 等同块行）→ 存活的 `parse_compat_float`/`parse_compat_int` NameError → `field_parse.py` 链崩 → 排产输入解析全挂。**禁误删行已在字段 4 列明**。
6. **灾难链 · 顺序错**：R30 先删 shared 实现而 R33 步骤1 未迁测试 import → 降级测试 + 身份断言红（非静默，CI 即可见，非灾难级）。
7. **灾难链 · facade 漏改**：删三常量但未同步 `core/services/common/value_policies.py:6-8/:24-26` → 该模块 import 即 ImportError → **打挂所有 `from core.services.common.value_policies import` 的存活消费者**（含矩阵契约测试经此取 READ_FILTER_ONLY），这是本条**最大爆炸点**：一处漏改、连带打挂 facade 全表面。
8. **静默炸风险**：低。删 date 死分支不影响任何生产数据流；坏日期生产走 `record_bad_time_row` 独立路径，不依赖被删策略 → 无「悄悄丢降级事件」风险。
9. **co_change_within_group**：R33、R15、R29（registry phase1_blast）。R31@B13 facade 须协调（跨桶）。

## 字段 9：承重毗邻标记

- `core/shared/compat_parse.py`：**非承重**（load_bearing=false，lb_no_touch=null，lb_colocation_danger=none）。
- `core/shared/value_policies.py`：**非承重**，但**跨桶**——`due_date` 策略族在 B06（R33），re-export facade 在 B13（R31）。
- **禁区行**：本条非承重，无「我是故意的」注释义务；但删除范围**禁外溢**到以下存活行（误删即崩，等同禁区）：
  - `compat_parse.py:9-10/:12-13/:15-17`（import 块里 float/int/is_blank_input 名字）；
  - `compat_parse.py:153-195`（`parse_compat_float`/`parse_compat_int` 函数体）、:129-140（`_float_fallback`/`_int_fallback`）；
  - `value_policies.py:6-11`（WRITE_* 常量）、:14-15（VALUE_FLOAT/VALUE_INT）、:22-40（FieldPolicy 类）、:43-178（float/int 的 13 个 FieldPolicy）、:211-227（推导 dict + 三个查询函数）。

## 字段 10：分层 / 导入环风险

- **越层 import 风险：0**。R30 是删除动作，不新增任何 import。回盘核：`core/shared/compat_parse.py`、`value_policies.py`、`field_parse.py` 均**未 import `core.services`**（rg 实证 NONE，clean）。删除只会减少 import，不会引入 `core.shared→core.services` 越层。
- **导入环风险：0**。当前依赖方向 `core.services.common.* → core.shared.*`（facade 单向下依赖），R30 删 shared 叶子，方向不变；R33 删 services.common 壳同样单向缩小，无环。
- 分层红线（0 AST 违规）：删除不会触发 `core.algorithms→core.services` 或 `core.models→core.services` 等禁连——本条全程在 `core.shared` 与 `core.services.common`/`tests` 内，无跨这两条红线的 import。

## 字段 11：测试耦合（动手前先迁哪些）

动手前必须先处理（否则红）：
1. **R33 步骤1 先行**：把 `tests/models_domain/test_compat_parse_emits_degradation.py:18` 的 `from core.services.common.compat_parse import parse_compat_date, parse_compat_float` 中 import 路径迁到 `core.shared.compat_parse`（或在 R30 删 due_date 用例时连 import 一并退）。这是顺序硬约束的根。
2. **退降级测试 date 用例**：`regression_compat_parse_emits_degradation.py:39` 的 `parse_compat_date("2026-02-31"...)` 调用 + 其在 events/codes 期望列表中的 date 项（需读 :43+ 的 codes 断言确认 date 对应项后精确退，避免误删 float/int 项）。
3. **退矩阵契约**：`regression_value_policies_matrix_contract.py` 的 `READ_FILTER_ONLY` import（:20）、`expected_fields` 三项（:46-48）、due_date 断言（:89-91）、start_time/end_time 断言（:92-95）、并核对 `len(policies)==len(fields)`（:87）随策略数 -3 自洽。
4. **R33 步骤2 退身份断言**：`regression_config_service_component_contract.py:411`（`parse_compat_date` 身份）——R33 范围，R30 不单独动。
原则：**删生产符号与退对应测试同批提交**，不留「实现已删、测试仍 import」的红窗口。

## 字段 12：前置依赖（先做哪些债 / 哪些 F 门）

硬前置（缺一即红或炸）：
1. **R33 步骤1（迁测试 import 到 core.shared）必须先于 R30**（registry planned_deps_hint 硬顺序）。
2. **R31 / B13 facade 收敛须与 R30 协调**：删 `VALUE_DATE`/`VALUE_DATETIME`/`READ_FILTER_ONLY` 三常量前，`core/services/common/value_policies.py` 的 re-export（:6-8/:24-26）须同批次删或 facade 先收，否则 ImportError 打挂 facade 全表面（字段 8 第 7 点，本条最大爆炸点）。registry 注「B13 facade 删除晚于 B05/B06 收敛（PHASE0 §10.2）」——意味 facade re-export 的删除时点要卡在 R30 删 shared 常量的同一窗口。
3. **R29 同文件协调**：若 R29 也改 value_policies.py / compat_parse.py 块体，与 R30 串行或同提交，避免 patch 行号位移冲突（字段 5）。
4. **F 门**：动手后须过 ① AST 分层校验（0 违规，本条不引入越层，应稳过）；② `regression_compat_parse_emits_degradation`、`regression_value_policies_matrix_contract`、`regression_config_service_component_contract` 三测试退改后全绿；③ 全仓 `rg parse_compat_date` 仅剩 0 命中（R33 步骤2 后）确认死切片彻底下线。

批次定位（registry planned_batch_hint）：Batch-7（B06），桶内第 3，与 R33 配对，在 R49/R51 之后、R15 之前或并行；须在 R33 步骤1 之后启动。

---

## 对抗核验（第二双眼睛）

核验人：对抗核验 agent（只读不改）。日期 2026-06-05。结论 **PASS**（独立 grep 回盘未找到任何反证）。

**重 grep 命令与结果**：
1. `rg -n "parse_compat_date" --type py` → 全仓仅 4 处：`core/shared/compat_parse.py:198`(def)、`core/services/common/compat_parse.py:4/:10`(R33 壳 re-export)、`tests/models_domain/test_compat_parse_emits_degradation.py:18/:39`、`tests/config/test_config_service_component_contract.py:411`(身份断言)。**生产调用形态 = 0,死分支属实**。
2. `rg -n "^def parse_compat_date|^def _date_fallback" core/shared/compat_parse.py` → 198 / 143,**零漂移**,与档案逐项一致。
3. `rg -n "READ_FILTER_ONLY|VALUE_DATE|VALUE_DATETIME" core/shared/value_policies.py` → 常量 :12/:16/:17,三 date 策略 due_date(:180/:183)/start_time(:190/:192)/end_time(:200/:202),**逐项一致**。
4. `rg -n "\bVALUE_DATE\b|\bVALUE_DATETIME\b|\bREAD_FILTER_ONLY\b" --type py` → 消费者仅 shared 定义+facade re-export(`core/services/common/value_policies.py:6-8/:24-26`)+矩阵契约测试(:20/:94/:95),**零生产孤儿**,facade 爆炸点真实。
5. `rg -n "parse_optional_date|parse_required_date" --type py` → 关键校验：这两名定义在 `strict_parse`,在 `compat_parse.py` 内仅 date 路径用(:150/:208),但全仓有 `core/algorithms/ordering.py:60/:64`、`sgs_scoring.py:35`、`tests/models_domain/test_strict_parse_blank_required.py`、`core/services/common/strict_parse.py` 等大量独立活跃消费者。**档案字段4「删 import 行:11/:14」指的是删 compat_parse.py 文件内的 import 那两行(该文件删 date 函数后不再用),不是删 strict_parse 定义——措辞精确,无越界删除风险**。
6. `rg -n "import core.services|from core.services" core/shared/{compat_parse,value_policies,field_parse}.py` → NONE(clean),删除不引入越层,0 AST 违规属实。
7. 身份断言 `config_service_component_contract.py:411` = `service_compat_parse.parse_compat_date is shared_compat_parse.parse_compat_date`,在 R30 删 parse_compat_date 后必失效 → R33 步骤2 须同步删,顺序约束正确。
8. `_sched_display_utils.py:76-84` 确认用 `parse_dt` 直接解析坏时间,绕开 value_policies 三策略,降级留痕走 `record_bad_time_row`——绕开路径属实,删三策略无「悄悄丢降级事件」风险。

**逐项裁决**：
- 行号复核：**一致**（5 处实现 + 3 常量 + 3 策略 + 4 测试锚点全部零漂移；档案字段1的块体边界 179-188/189-198/199-208 经 sed 复核精确）。
- 承重误删风险：**无**。本条非承重(load_bearing=false, lb_no_touch=null),修法是纯直删+退测试,核对档案未混入透传/合并/统一/加形参/兜底；同 import 块 float/int/is_blank_input 名字与 :153-195 存活函数体已被档案列入禁误删禁区(字段4/字段9),正确。
- 灵魂线：**干净**。直删死分支不新增兜底/静默回退/except 吞错；字段7对「date 与 float/int 回退语义不可统一(invalid_due_date vs invalid_number)、故不是 parity 债」的反例澄清成立,无误把死分支当「该统一」处理。
- owner_pending=false：档案已给终态修法,合规(非 owner_pending 债允许给终态)。
- 最大爆炸点(facade 漏改→ImportError 打挂全表面)与 R33 强耦合硬顺序(R33步1迁import→R30删实现→R33步2删壳+:411)均经 grep 复核属实,前置依赖刻画准确。

**争议点：无**。需升级第三方裁：否。

---

## 2026-06-10 终态补登（G23 收口，R30 → fixed）

详见 `_registry.json` R30 current_evidence。要点：与 R33 同 commit 原子收口（壳与 shared 三常量同窗删除，无 facade 残 import 窗口）。`core/shared/compat_parse.py` 删 `parse_compat_date`（旧 :198-215）+`_date_fallback`（旧 :143-150）+date 专用 import（`parse_optional_date`/`parse_required_date`/`VALUE_DATE`），`datetime` 名删后零引用故 import 收为只剩 `date`（现盘 :54 `_format_fallback_value` 的 isinstance 仍引用）；`core/shared/value_policies.py` 删三常量（READ_FILTER_ONLY/VALUE_DATE/VALUE_DATETIME）+三 FieldPolicy（due_date/start_time/end_time）。**WRITE_NOT_APPLICABLE 删 start/end_time 后变零引用，按本档案字段 9 与 R31 档案字段 9 的「:6-11 WRITE_* 禁删区」裁定保留不扩大，登记为后续微债候选；同窗一并登记：`_compat_message` prefixes 内 `invalid_due_date`/`bad_time_row_skipped` 两键经 compat 路径已不可达（两码生产仍由 schedule_summary_degradation/_sched_display_utils 硬编码直发、不经 _compat_message），及 `write_mode==WRITE_OPTIONAL` 双分支在剩余 13 策略全为 WRITE_REQUIRED 后当前永假（机制保留）。** 退测试：emits_degradation 删 due_date 用例+invalid_due_date 期望；matrix_contract 删三字段期望与断言。禁误删区全守（float/int import 名、:153-195 存活函数、:6/:7/:8 活常量）。验证：config+models_domain 259 passed、fitness+resource_dispatch 137 passed、ruff 全过。
