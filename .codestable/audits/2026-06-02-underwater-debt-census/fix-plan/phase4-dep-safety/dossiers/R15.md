# R15 逐债档案 - datetime 解析私造 3 份绕过收口点 + 语义漂移

> 桶 B06(scheduler-exec-diag) · 病理 P5(语义漂移) · 严重度 medium · 承重 false · 簇 C01 · owner_pending=**false**
> 主文件 `core/services/scheduler/execution_fact_provider.py`; 另涉 `operation_execution_feedback_support.py`、`data/repositories/operation_execution_state_builder.py`
> **2026-06-09 执行终态**: R15 已 fixed。O03 已裁: provider 合法空时间继续本地 `return None`; 坏时间必须 loud raise。实现已按分支级收口到已有真相源 `core.models.operation_execution_event.parse_operation_event_time`, 未新建模块, 未新增兜底, 未改 support/state_builder 既有语义。

---

## 1) 当前真实锚点

所有行号均为 2026-06-09 按符号名重新 `rg` 的现盘结果, 旧 file:line 不再可信。

| 处 | 符号 | 当前 file:line | R15 终态 |
|----|----|----|----|
| provider | `_parse_execution_time` | `core/services/scheduler/execution_fact_provider.py:89` | 空值 `None/""/" "` 仍返回 `None`; 非空交给 `parse_operation_event_time`; 坏值 raise |
| provider 调用 | `_fact_from_state` | `core/services/scheduler/execution_fact_provider.py:56-57` | `actual_start_time`/`actual_end_time` 继续经 `_parse_execution_time` |
| support | `_parse_feedback_datetime` | `core/services/scheduler/operation_execution_feedback_support.py:222` | 维持 required+raise, 保公开错误文案 |
| state_builder | `_parse_time` | `data/repositories/operation_execution_state_builder.py:37` | 空值 `None/""/" "` 仍返回 `None`; 坏值 raise |
| 真相源 | `parse_operation_event_time` | `core/models/operation_execution_event.py:75` | 既有事件时间解析入口, 不新建模块 |

## 2) 已落地改动

- provider 删除本地 `replace` + `strptime` 循环私造解析。
- provider 新增同模块既有 import: `parse_operation_event_time`。
- provider 保留合法空时间短路: `None`、空串、纯空白仍是 `None`, 避免把“任务还没开始/还没结束”误炸成错误。
- provider 非空值统一走 `parse_operation_event_time`; `"not-a-date"`、非法日期、`0`、`False` 这类坏值不再静默吞成 `None`, 而是 loud raise。
- support/state_builder 没有被本轮改动: support 仍保 required+raise 和中文错误文案; state_builder 仍保 optional 空值语义并对坏值 raise。

## 3) owner 裁定与旧计划偏差

- O03 已裁: R15 provider 坏值方向为 loud raise; 合法空时间仍本地 `return None`。
- 旧 planned_fix_hint 写过 `core/shared/strict_parse`, 但现盘 support/state_builder 已先收口到 `core.models.operation_execution_event.parse_operation_event_time`。本轮遵循 owner 裁定和既成事实, provider 也收到这个已有真相源。
- 这不是新建收口点, 也不是整体 delegate。整体 delegate 会让空值进入 `parse_operation_event_time` 并 raise, 已被计划列为爆点 #7, 本轮显式避开。

## 4) 测试与守卫

本轮新增/扩充 `tests/operation_execution/test_operation_execution_scope_read_contract.py` 中的 provider parity:

- `test_execution_fact_provider_keeps_blank_actual_times_optional`: 钉住 `None`、`""`、`" "` 均返回 `None`。
- `test_execution_fact_provider_parses_valid_actual_times_like_operation_event_time`: 钉住合法字符串、`/`/`T` 归一化和 `datetime` 输入与真相源一致。
- `test_execution_fact_provider_rejects_bad_actual_time_loudly`: 钉住 `"not-a-date"`、非法日期、`0`、`False` 走 `ValueError`。

已跑过的 R15 本地验证:

- `.venv/bin/python -m pytest tests/operation_execution/test_operation_execution_scope_read_contract.py -p no:cacheprovider`
- `.venv/bin/python -m ruff check --force-exclude -- core/services/scheduler/execution_fact_provider.py tests/operation_execution/test_operation_execution_scope_read_contract.py`
- `.venv/bin/python -m pytest tests/operation_execution/test_operation_execution_scope_read_contract.py tests/operation_execution/test_scheduler_reschedule_execution_facts.py tests/operation_execution/test_operation_execution_event_time_contract.py -p no:cacheprovider`
- `.venv/bin/python -m pytest tests/gate_meta/test_architecture_fitness.py::test_no_circular_service_dependencies tests/gate_meta/test_architecture_fitness.py::test_no_new_local_parse_helpers -p no:cacheprovider`
- `.venv/bin/python tools/scan_py38plus_syntax.py --fail-on-hit core/services/scheduler/execution_fact_provider.py tests/operation_execution/test_operation_execution_scope_read_contract.py`
- `jq empty _registry.json _registry_index.json`
- `git diff --check` for the R15 commit path list

后续提交前仍须完成 Sub Agent 对抗复审闭环; 不得把旁路 dirty 文件混进 R15 commit。

## 5) 语义禁区

- 禁把 provider 空时间改成 required+raise; 空 actual_start/end 是合法现场态。
- 禁给 provider 加 `try/except` 把坏值再吞回 `None`; R15 的收口目标就是消掉静默坏值。
- 禁改 support 的 `_parse_feedback_datetime` 为 None 回退; 写侧坏反馈时间必须拦住并保持公开消息契约。
- 禁把三处一刀切成同一 required/optional 语义; 三处业务含义本来不同。

## 6) 分层与导入环

- provider 位于 `core.services`, 新增依赖仍是既有 `core.models.operation_execution_event`, 属合法方向。
- 未引入 `core -> flask`、`model -> data`、`data -> core` 反向环。
- 未碰 `STRICT-4`、未碰 `docs/_panorama_data`、未碰 LB01 承重点。

## 7) 同文件兄弟与收口状态

R15 是 provider 链第一段, 当前已 fixed。R19 与 R13 也已 fixed, 同文件串行链已收口:

- R19 已 fixed: `_positive_op_ids` 已委托 `positive_op_ids`，`execution_snapshot.py:42` 排序事实承重与 positive_op_ids 黄金用例已落。
- R13 已 fixed: owner 已确认单机无仓库外读者，已删除 `ExecutionFact.last_event_schedule_*` 字段、`_fact_from_state` 的 `latest` 形参/实参、旧字段赋值与孤儿 `_latest_events_by_scope`。
- G09 provider 链已完成；后续只剩 G10 的 R18 repo stub 护栏。

---

## 对抗核验要求

R15 提交前的对抗审核重点:

1. provider 是否只对空值本地短路, 非空是否真正委托 `parse_operation_event_time`。
2. 坏值是否 loud raise, 没有新兜底/日志降级/静默 None。
3. support/state_builder 语义是否未被本轮误改。
4. registry、index、C-EXEC-FACT、PHASE plan 是否都表达 R15/R19/R13 fixed，且没有把 R18 提前收口。
