# R59 逐债档案 — parse_report_nonnegative_int 手搓正则绕过 strict_parse 收口点

- 桶/home: B05 / B05 · partition: delta-report-core
- severity: low · pathology: P5(语义漂移) · load_bearing: false · owner_pending: false
- cluster: ISOLATED · same_file_siblings: [] · interference_edges: []
- 主文件全路径: `core/services/report/report_number_parsing.py`

---

## 字段 1) 当前真实 file:line(回盘)+ 漂移记录

按符号名在当前工作区 rg 回盘（`core/services/report/report_number_parsing.py`）：

| 符号 | registry old_location（勿信） | 当前真实行号 | 漂移 |
|---|---|---|---|
| `_INT_TEXT_PATTERN = re.compile(r"^[+-]?\d+$")` | :9 | **:9** | 0（准） |
| `_parse_plain_report_int(...)` def | :54-70 | **:54-70** | 0（准） |
| `parse_report_nonnegative_int(...)` def | :73-90 | **:73-90** | 0（准） |
| 私有正则命中点 `_INT_TEXT_PATTERN.fullmatch` | — | **:68** | — |
| `parse_report_nonnegative_int` 委派 `_parse_plain_report_int` | — | **:81-87** | — |
| 兄弟 `parse_report_int` 委派 `parse_required_int` | :48-51 | **:48-49**（try/except :48-51） | ±0 |
| 兄弟 `parse_report_float` 委派 `parse_required_float` | :12 | **:12-27**（委派点 :25） | ±0 |

**主文件行号全部零漂移**，registry 的 `location_status: ✅准` 经独立回盘成立。

下游/收口/测试侧的真实行号（rg 回盘，与 registry hint 对照）：

- 收口点 `parse_required_int` 真实位置：`core/shared/strict_parse.py:81`（签名 `(value, *, field, min_value=None)`，**无 reject_integer_float 形参**）；其内核 `_parse_finite_int` 在 `:46-58`，关键宽松点 `:50 parsed=float(value)` + `:56 abs(parsed-int)>1e-9` → 整值 float（`'2000.0'`/`3.0`）被接受成 int。**registry 引用 strict_parse:46-58 / :56 经回盘准确。**
- prod 消费者 `report_engine.py`：import `:29`、`_export_nonnegative_int` `:71-72`、三档调用 `:81`(导出行数)/`:82`(direct_max)/`:86`(stream_max)。**与 registry :29/72/81/82/86 一致。**
- prod 消费者 `reports_export_support.py`：import `:10`、包装函数 `report_nonnegative_int` `:44-45`、调用 `:56`/`:80`。**与 registry :10/45/44 一致。**
- 测试消费者 `tests/web_pages/test_web_silent_fallback_contract.py`：**registry hint 的 :48/61/64/67/70 已严重漂移**，真实断言在 **:241(`"1.5"`)/:244(`"-1"`)/:247(`"1.0"`)/:250(`1.0`)**，全部 `pytest.raises(ValidationError, match="导出行数")`。**漂移幅度约 +180~190 行**（registry 的 phase1_blast.test_consumers 行号是旧版整文件偏移）。修复时务必以 :241-250 为准，勿照抄旧行号。

## 字段 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`；`current_evidence_2026_06_05` 描述「:9 私有正则 + :54 `_parse_plain_report_int` 拒 '2000.0' 仍原样；未收口到 `parse_required_int(reject_integer_float)` 且 F1 不存在；无『刻意拒整值 float』注释」。
- 独立回盘结论：**完全一致**。代码确实仍走私有正则路径（:68 `_INT_TEXT_PATTERN.fullmatch`），未收口；F1（`reject_integer_float`）在 `core/`、`web/` 全仓 `rg` 命中 **0 处**，确认未落地；文件内无任何「故意拒整值 float」中文注释。状态 `planned` 与现态吻合，无偏差。

## 字段 3) 病理 / 严重度 / 桶 / 簇

- **病理 P5（语义漂移收口缺失）**：同文件三兄弟 `parse_report_float`(:25 委派 `parse_required_float`)、`parse_report_int`(:49 委派 `parse_required_int`)均收口到 `core.shared.strict_parse` 统一点；**唯独** `parse_report_nonnegative_int` 经私有 `_parse_plain_report_int` + 手搓正则 `_INT_TEXT_PATTERN`（:9）走旁路。两条路径对「整值 float」语义不一致：私有正则拒 `'2000.0'`/`2000.0`（:70 raise），统一点 `parse_required_int` 接受（float()→int）。这是「同一个解析意图被实现成两套语义」的典型 P5。
- **严重度 low**：该差异当前**被续命测试钉死**（:247/:250 要求 `'1.0'`/`1.0` raise），即「拒整值 float」恰是导出行数字段**当前想要的**行为；私有正则虽是旁路，但语义上更严格、无静默错值流出。风险面仅限「实现重复 + 与收口点漂移」，无生产数据正确性事故。
- **桶 B05**（delta-report-core）；**簇 ISOLATED**：`same_file_siblings=[]`、`interference_edges=[]`，独立文件、无同簇并发改动方。

## 字段 4) 修法类

**修法类 = 收口（P5）+ 强前置依赖 R04 的 F1；F1 未落地前退化为「注释 + parity 测试钉死差异，暂不收口」。**

- 非承重（load_bearing=false、lb_no_touch=null、phase1_blast.lb_colocation_danger=none），不受「仅注释」铁律限制，可实施收口。
- **收口点必须是已存在的统一点 `core.shared.strict_parse.parse_required_int`（strict_parse.py:81）**——这是兄弟 `parse_report_int`(:49) 已收口到的同一符号，**不新建第二模块**，符合铁律 5。
- **终态修法（需 R04 F1 先落地）**：把 `parse_report_nonnegative_int`(:73-90) 改为委派——
  `number = parse_required_int(value, field=label, min_value=0, reject_integer_float=True)`，
  包在与 `parse_report_int`:48-51 同款 `try/except ValidationError → 友好消息(label/source_label)` 模式里；**保留 blank 短路**：现 `_parse_plain_report_int`:62-63 对 blank `return blank_default`，而 `parse_required_int` 对 blank `raise`（strict_parse:82-83 `_raise_blank_required`），故委派前必须先 `if is_blank_input(value): return int(blank_default)` 短路（照搬 `parse_report_int`:44-47 的 blank_default 处理，但 nonnegative 版 blank_default 必有默认 0，不会 raise）。然后**删除 `_parse_plain_report_int`(:54-70) 与 `_INT_TEXT_PATTERN`(:9)**（含 `import re`:3 若再无其他用途——回盘确认全文件仅 :9 用 re，删后可一并删 :3）。
- **灵魂线核对**：终态委派会 `loud raise ValidationError`，不新增兜底/静默回退/except 吞错；`min_value=0` 由统一点强制（替代现 :88-89 的 `number<0` 二次判定），无降级标记需求。✅ 不违反 P4 灵魂线。
- **F1 未落地前的临时态（当前应停在此）**：保留私有正则路径不动，仅在 `_parse_plain_report_int` 上方/`_INT_TEXT_PATTERN` 旁补一行「我是故意的」中文注释（说明：导出行数字段刻意拒「2000.0」整值 float，区别于收口点的 float()→int 宽松语义），并补 parity 测试（见字段 7）钉死差异。**不裸收口**——裸收口会撞自己的续命测试 :247/:250。

## 字段 5) 同文件碰撞债

- registry `same_file_siblings = []`、`interference_edges = []`：**本文件无同桶/同簇其他债**。
- 文件内非债兄弟（**勿顺手改**）：
  - `parse_report_float`(:12-27)、`parse_optional_report_float`(:30-33)、`parse_report_int`(:36-51)：已正确收口，非债。
  - ⚠️ **`parse_report_int`(:36) 当前也经 `parse_required_int` 接受 `'2000.0'`**（统一点宽松），registry `planned_deps_hint` 明确：「它是别的 field 语义，**不在本债范围，勿顺手改**」。R59 只动 nonnegative 一支，禁动 `parse_report_int`。
- **行号/dict 键位移碰撞**：删 `_parse_plain_report_int`(:54-70) 后，`parse_report_nonnegative_int` 上移约 17 行；但本文件无 dict 字面量、无同文件其他债排队，**无键位移、无与兄弟债撞行号风险**（兄弟债集合为空）。唯一需同步的是 `__all__`(:93-98) 不变（删的是私有函数，不在导出表），收口后 `parse_report_nonnegative_int` 仍在 `__all__:97`。

## 字段 6) 同收口点债 + 顺序敏感性

- **收口到同一符号 `parse_required_int`(strict_parse:81) 的其他债**：R04 本身（统一三套私有正整数强制器到 `parse_required_int`/F1）。R59 与 R04 是 **co-change 组**（registry phase1_blast.co_change_within_group = [R04, R09, R28]）。
- **顺序敏感性（强）**：**R04 的 F1 必须先于 R59**。F1 = 给 `strict_parse.parse_required_int` 增加 `reject_integer_float` 严格模式（拒整值 float）。原因见字段 7 反例：F1 未落地时，`parse_required_int` 接受 `1.0`/`'1.0'`→1，而 R59 的续命测试 :247/:250 要求二者 raise。**R59 不等 F1 直接收口 = 自违续命测试，绿转红。**
- R09(`parse_optional_positive_int`)、R28 同属该组但**不同符号/不同收口形态**，与 R59 无直接顺序耦合，仅同批协调（Batch-6/B05）。R59 对它们无前后依赖，唯一硬锁是 F1。

## 字段 7) 收口行为差异及 parity 测试

逐边界值对比「旧路（私有正则 `_parse_plain_report_int`）」 vs 「裸收口到现 `parse_required_int`（F1 前）」 vs 「F1 后收口 `reject_integer_float=True`」：

| 输入 | 旧路（现态，:54-70/:88-89） | 裸收口现 `parse_required_int`（F1 前） | F1 后 `reject_integer_float=True` | 续命测试期望 |
|---|---|---|---|---|
| `"2000"` / `2000` | 2000 | 2000 | 2000 | —（接受） |
| `"-1"` | raise(:88-89 `<0`) | raise(`min_value=0`) | raise(`min_value=0`) | :244 raise ✅ |
| `"1.5"` | raise(:70 正则不匹配) | raise（float 非整值 :56） | raise | :241 raise ✅ |
| **`"1.0"`** | **raise**(:68 正则 `^[+-]?\d+$` 不含 `.`) | **2000?→1（接受!）** float('1.0')=1.0,:56 整值通过 | **raise**(reject_integer_float) | **:247 raise** ⚠️ |
| **`1.0`**（float） | **raise**(:66 非 int / :64 非 bool / :68 非 str → :70) | **1（接受!）** | **raise** | **:250 raise** ⚠️ |
| 空串 / blank | `blank_default`(0)(:62-63) | raise（strict_parse:82-83 blank→raise） | raise → 故收口须保留 blank 短路 | 未覆盖（须 parity 守住） |
| `True`(bool) | raise(:64-65) | raise（strict_parse:47-48 拒 bool） | raise | —（一致） |

**反例（违约点）**：`'1.0'` 与 `1.0` 在「裸收口现 `parse_required_int`」下由 **raise 变成返回 1**——直接违反 `regression_web_silent_fallback_contract.py:247/:250`，灵魂线续命测试由绿变红。**这是 R59 与 R04 互锁的根因。**

**所需 parity 测试（F1 落地前，临时态钉死差异）**：在 contract 测试旁补一组断言，显式声明「导出行数刻意拒整值 float」的语义边界——
- `parse_report_nonnegative_int("1.0", ...)` raise（已由 :247 覆盖，维持）
- `parse_report_nonnegative_int(1.0, ...)` raise（已由 :250 覆盖，维持）
- 新增对照：`parse_report_int("2000.0", ...)` **接受 2000**（钉死「兄弟 field 语义不同、不在本债范围」，防后续 agent 误统一）
- F1 落地、R59 收口后：上述 :247/:250 保持 raise（由 `reject_integer_float=True` 兑现），并补 blank→`blank_default` 短路测试（`parse_report_nonnegative_int("", blank_default=5)`==5）守住短路未被收口吞掉。

## 字段 8) 爆炸半径

**生产消费者（rg 回盘）：**
1. `core/services/report/report_engine.py`：import `:29`；`_export_nonnegative_int`(:71-72) 封装；三处档位决策 `:81 导出行数`、`:82 direct_max`、`:86 stream_max`。→ 这三档是**导出行数/直连最大/流式最大**的阈值决策，决定 direct / stream / reject 导出通道选择。
2. `web/routes/reports_export_support.py`：import `:10`；`report_nonnegative_int`(:44-45) 二次封装（label/source_label 固定为「报表导出数据」）；调用 `:56`（estimated_rows）、`:80`（row_count 落库）。→ web 导出表单整数参数入口。

**测试消费者：** `tests/web_pages/test_web_silent_fallback_contract.py:241/244/247/250`（钉死 `'1.5'`/`'-1'`/`'1.0'`/`1.0` 全 raise，灵魂线续命）；间接 `:252-254` `ReportEngine._build_export_decision("-1")` 也走该链。

**灾难链（删错/统一错会静默炸什么）：**
- **裸收口（不等 F1）** → `'1.0'`/`1.0` 由 raise 变接受 1 → 续命测试 :247/:250 变红（**显性失败，会被 CI 拦下，非静默**）——这是「测试保护到位」的良性炸法。
- **更危险的静默炸法**：若**误删续命测试**或**误把 `reject_integer_float` 设 False/漏传** → 导出行数字段开始接受 `2000.0`、`1e9`(float) 等，进入 `_export_nonnegative_int` 档位决策 → 导出通道阈值被 float 污染（虽整值等价，但语义边界松动，未来 `2000.5` 类输入若上游放宽即静默落入）→ web 导出参数静默接受非预期格式。**关键：本债的「拒整值 float」是导出阈值的有意收紧，统一错（放宽）会让阈值校验静默失守。**
- 删 `_parse_plain_report_int`/`_INT_TEXT_PATTERN` 时若**漏删 blank 短路逻辑** → blank 输入由 `return blank_default(0)` 变 `raise` → 导出行数空值场景（estimated_rows 缺省）从「容错降级到 0」变「直接报错」，web 导出流程中断。**故 blank 短路是删 `_parse_plain_report_int` 时必须迁出保留的语义。**

## 字段 9) 承重毗邻标记 + 禁区行

- 本债 `load_bearing = false`、`lb_no_touch = null`、phase1_blast `lb_colocation_danger = none`。
- 全文件 rg 扫描无 load_bearing 标记/「我是故意的」承重注释；**同文件无承重债毗邻**。
- **禁区行（非承重意义上的「勿动」）**：
  - `parse_report_int`(:36-51)、`parse_report_float`(:12-27)、`parse_optional_report_float`(:30-33)——**别的 field 语义，禁改**（registry 明令：`parse_report_int` 接受 `'2000.0'` 是有意的，不在本债范围）。
  - `__all__`(:93-98) 导出表——删的是私有 `_parse_plain_report_int`，不在表内，**禁动表项**。
  - `core/shared/strict_parse.py` 的 `parse_required_int`(:81)/`_parse_finite_int`(:46-58)——R59 **只消费**该收口点，**不改其实现**；`reject_integer_float` 形参的新增属 **R04/F1 的范围，R59 禁越界改 strict_parse**。

## 字段 10) 分层 / 导入环风险

- `report_number_parsing.py` 现有 import（rg :1-7）：`core.infrastructure.errors`、`core.shared.strict_parse`。收口到 `parse_required_int` **不新增任何 import**（该模块已 import `parse_required_int` 在 :7），故**零新增跨层依赖**。
- 层级合规：`core.services.report → core.shared`（strict_parse）+ `core.infrastructure.errors`，均为「services 向下依赖 shared/infrastructure」的合法方向。**无 `core.algorithms→core.services` / `core.models→core.services` 越层**（铁律 6）。`strict_parse.py` 仅 import `math` + `core.infrastructure.errors`，不回指 services，**无导入环**。
- 删 `_parse_plain_report_int` 后若 `import re`(:3) 失去唯一用途（:9 是全文件唯一 re 用法），可一并删 :3——**降低**依赖面，无新增风险。
- **0 AST 违规**：收口方向与现有兄弟一致，不引入任何新跨层边。

## 字段 11) 测试耦合

**动手前需对齐/迁移的测试：**
1. `tests/web_pages/test_web_silent_fallback_contract.py:241-250`（+ :252-254 间接）——**灵魂线续命测试，不可删、不可放宽**。F1 落地前维持现绿（私有正则天然满足）；F1+收口后必须仍绿（靠 `reject_integer_float=True` 兑现 `'1.0'`/`1.0` raise）。这是收口正确性的**唯一回归闸**。
2. 收口前**先补 parity 测试**（字段 7 列表）：钉死 `'1.0'`/`1.0` raise + blank→blank_default 短路 + 对照 `parse_report_int("2000.0")` 接受。先补 parity → 再收口 → parity 全绿才算等价迁移。
3. 无独立针对 `_parse_plain_report_int` 的单测（私有函数，rg 无直接引用于 tests/）——删除它无孤儿测试需清理。
4. 顺序：F1（R04）落地并自带其 strict_parse 测试 → R59 补 parity → R59 收口 → 跑 contract + parity 全绿。

## 字段 12) 前置依赖

- **硬前置（互锁，必须先做）：R04 的 F1** = 给 `core.shared.strict_parse.parse_required_int` 增加 `reject_integer_float`（拒整值 float）严格模式。**F1 未落地，R59 不得裸收口**（会违反续命测试 :247/:250，字段 6/7 已证）。F1 前 R59 只能停在「注释 + parity 钉死差异」临时态。
- **F 门**：依赖 R04 提供的 strict_parse 严格模式门（registry `planned_batch_hint`：「桶内须排在 R04 的 F1 之后(互锁)」）。
- **批次**：Batch-6（B05），独立文件，F1 落地后即可执行；co-change 组 R04/R09/R28 同批协调但与 R59 无额外顺序耦合。
- **无同文件跨桶边**（独立文件、兄弟债集合空），除 F1 外无其他债前置。
- owner_pending = false → 可给终态修法（已给，见字段 4），但终态执行须等 F1。

---

### 一致性自检
- registry 主文件行号零漂移；测试消费者行号 registry hint 失准（+~180 行），本档已以真实 :241-250 更正。
- 与既有分析（registry phase1_blast / planned_fix_hint）**无冲突**：互锁、收口点、blank 短路保留、删 `_parse_plain_report_int` 等结论一致；本档补充了 F1 缺位的独立验证（全仓 0 命中 `reject_integer_float`）与测试行号漂移修正。

---

## 对抗核验（第二双眼睛）

独立 rg/grep 回盘 + 运行时反例实证，逐条挑战本档结论。结论：**PASS**。

**重 grep 命令与结果：**
1. `rg -n "_INT_TEXT_PATTERN|_parse_plain_report_int|def parse_report_nonnegative_int" core/` →
   `_INT_TEXT_PATTERN` :9（定义）/:68（fullmatch 命中）；`_parse_plain_report_int` :54（def）/:81（委派点）；`parse_report_nonnegative_int` :73。**主文件行号零漂移，与本档字段1表格完全一致。** Read 全文复核 :9/:54-70/:73-90 逐行成立，`number<0` 二次判定在 :88-89，blank 短路在 :62-63，`__all__` 在 :93-98。
2. 收口点 `rg -n "def parse_required_int|def _parse_finite_int" core/shared/strict_parse.py` → `parse_required_int` **strict_parse.py:81**，签名 `(value, *, field, min_value=None)` —— **确无 reject_integer_float 形参**；`_parse_finite_int`:46-58，宽松点 `:50 float(value)` + `:56 abs(parsed-int)>1e-9 raise`。Read 复核成立。**收口点真实存在且为兄弟 `parse_report_int`:49 已收口的同一符号，不新建模块——符合铁律5。**
3. F1 存在性 `rg -n "reject_integer_float" core/ web/ tests/` → **0 命中**。F1 确未落地，硬前置/互锁成立。
4. 测试消费者 `rg -n "report_nonnegative_int|导出行数" tests/web_pages/test_web_silent_fallback_contract.py` → 断言真实在 **:240-241(`"1.5"`)/:243-244(`"-1"`)/:246-247(`"1.0"`)/:249-250(`1.0`)**，全 `pytest.raises(ValidationError, match="导出行数")`。**registry hint :48/61/64/67/70 确已漂移约 +190 行，本档以 :241-250 更正准确。**
5. 生产消费者 `rg` 回盘：`report_engine.py` import :29 / `_export_nonnegative_int` :71-72 / 三档调用 :81/:82/:86；`reports_export_support.py` import :10 / 包装 `report_nonnegative_int` :44-45 / 调用 :56/:80。**与本档字段8、registry prod_consumers 全部一致。**
6. 分层 `rg "^from|^import" 两文件` → `report_number_parsing` 仅依赖 `core.infrastructure.errors`+`core.shared.strict_parse`（`parse_required_int` 已在 :7 import，**收口零新增 import**）；`strict_parse` 仅 `math/datetime/core.infrastructure.errors`，**不回指 services，无导入环、无越层**（铁律6 ✅）。
7. 孤儿测试 `rg "_parse_plain_report_int" tests/` → 0 命中，删私有函数无孤儿测试需清理。

**运行时反例实证（python3 实跑，主动找反例验「裸收口会炸」）：**
- 现 `parse_required_int('1.0')=1`、`(1.0)=1`、`('2000.0')=2000`（接受）；`('1.5')`/`('-1')` raise。
- 现私有正则路 `parse_report_nonnegative_int('1.0')`/`(1.0)`/`('1.5')`/`('-1')` 全 **RAISE**；`('')` → **0**（blank 短路）。
- → **完全证实**：裸收口（F1 前）会让 `'1.0'`/`1.0` 由 raise 变接受 1，直撞续命测试 :247/:250（**显性失败，CI 可拦，非静默**）；blank `''` 现返回 0、若收口漏迁短路则 strict_parse 对 blank raise——**字段6/7/8 的互锁论断、blank 短路必保留论断经实证成立。**

**承重 / 越界 / owner_pending 审查：**
- 非承重（load_bearing=false、lb_no_touch=null、lb_colocation_danger=none）；全文件无「我是故意的」承重注释。
- 修法 = 收口到已存在点 + 删私有函数 + 保留 blank 短路 + 补 parity；**未混入越界改 `strict_parse`（reject_integer_float 明确划归 R04/F1）、未顺手改别 field 语义的 `parse_report_int`、未动 `__all__` 表项**。无新建模块/无加形参到 R59 范围符号/无透传/无静默兜底；终态委派 loud raise，不违 P4 灵魂线。**承重误删风险：无。**
- owner_pending=false：给终态修法合规，且本档正确要求终态执行须等 F1、F1 前停在「注释+parity 钉死差异」临时态，**未擅自裸收口、未越分配执行批次（Batch-6/B05 与既有 hint 一致）**。

**唯一可记微瑕（不影响结论）**：字段7表格 `"1.0"` 行「裸收口」列写作「`2000?→1（接受!）`」，`2000?` 为笔迹噪声，正解即「1（接受!）」，已由上方反例实证澄清。

**核验结论：PASS**——行号一致（主文件零漂移，测试行号本档已更正且复核准确）、收口点真实存在、互锁/blank/分层经独立回盘与运行时反例双重证实，无承重误删风险，无擅自终态/批次越权。无需第三方裁断。

