# R33 逐债档案 — 三壳 re-export facade 残渣(compat_parse/field_parse/value_policies)

> 桶 B06 | 簇 C01 | 严重度 low | 病理 P3(needs_adversarial,已 refuted=real_debt) | 承重 false | owner_pending false
> 主文件: `core/services/common/compat_parse.py`(+ field_parse.py / value_policies.py 同类)
> 同文件兄弟债: R30、R31

## 1) 当前真实 file:line(grep 回盘)+ 漂移记录

R33 不是单符号债,是「三个纯 re-export 壳文件整体」债。逐文件回盘(2026-06-05 当前工作区):

| 壳文件 | 当前真实位置 | def/class 计数 | re-export 源 |
|---|---|---|---|
| `core/services/common/compat_parse.py` | :3-7 `from core.shared.compat_parse import (parse_compat_date, parse_compat_float, parse_compat_int)`;:9-13 `__all__` | **0**(实测纯壳) | core.shared.compat_parse |
| `core/services/common/field_parse.py` | :3 `from core.shared.field_parse import parse_field_float, parse_field_int`;:5 `__all__` | **0** | core.shared.field_parse |
| `core/services/common/value_policies.py` | :3-19 `from core.shared.value_policies import (...13 个符号...)`;:21- `__all__` | **0** | core.shared.value_policies |

测试侧续命点(回盘):
- `tests/regression_compat_parse_emits_degradation.py:18` `from core.services.common.compat_parse import parse_compat_date, parse_compat_float`(经壳)
- `tests/regression_value_policies_matrix_contract.py:18` `from core.services.common.value_policies import (...)`(经壳)
- `tests/regression_config_service_component_contract.py`:
  - `:13-20` `_SERVICE_COMMON_NEUTRAL_HELPERS` 元组含 `:14 compat_parse`、`:16 field_parse`、`:19 value_policies`(还含 `:15 degradation` 为 R29 反例,不可连带删)
  - `:355` `imported.startswith(_SERVICE_COMMON_NEUTRAL_HELPERS)`(run 层 FORBIDDEN 白名单)
  - `:393-399` `test_services_common_value_policies_reexports_shared_identity`(is-identity 身份断言)
  - `:402-411` `test_services_common_parse_core_reexports_shared_identity`(:410 parse_compat_float is、:411 parse_compat_date is)

**漂移记录: 基本无漂移。** registry `current_evidence` 给的 compat_parse.py:3-13 / 测试 :18 / config_contract :14/:403/:411 全部与回盘逐行吻合。仅一处需澄清:registry `phase1_blast.contract_topology_tests` 写身份断言在 `:393/402`,回盘确认 :393(value_policies)与 :402(parse_core)是两个测试函数的 `def` 行,身份 `is` 断言体在 :397-399 与 :410-411,**函数定义行号准、断言体行号需 +若干**(微漂,不影响定位)。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`。
- 回盘一致: 三壳全在且仍纯 re-export(def=0),「步骤1 迁 import」未做(两测试仍 import `core.services.common.*`),身份断言与白名单元组条目均在原位。
- 结论: **状态与证据完全一致,planned 未动。** 无 in_progress 痕迹。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P3**(needs_adversarial=true,已对抗 → `verdict.verdict_kind=real_debt`,`refuted=true`): 三壳是历史 `core.services.common.*` → `core.shared.*` 迁移留下的兼容层残渣,生产侧已全部直连 `core.shared`,壳零生产消费,**仅测试续命**。结论可删,但删前必须先迁测试覆盖(保灵魂线行为测试不连带删光)。
- **严重度 low**: 纯死代码清理,删错不污染运行期(无人调用)。风险只在「删序错 → 测试红」与「误删 R29 反例 / 误碰 core.shared」。
- **桶 B06** / **home B06**(`core-svc-domain` 分区)。
- **簇 C01**: 与 R29(同 common/ family **反例**,有意保留)、R30(同链 date 切片)、R31(同 value_policies 壳)同簇。

## 4) 修法类

**修法类 = 直删 + 退测试(三步,严格保灵魂线测试覆盖,不连测删光)。** 非承重(load_bearing=false、lb_no_touch=null),不受「仅注释」约束。owner_pending=false,可给终态修法。

> 灵魂线红线: 本债是「删兼容壳」,纯减法,**不新增任何兜底/静默回退/except吞错**。迁 import 只是把测试指向真承重点,不改测试断言逻辑。无 P4 raise 改造需求(P3 死代码债)。

三步(顺序神圣,见 §12 与 §5):
- **步骤1(最先,为 R30 解锁)——迁行为测试 import 到承重点 `core.shared`:**
  - `tests/regression_compat_parse_emits_degradation.py:18`: `from core.services.common.compat_parse` → `from core.shared.compat_parse`
  - `tests/regression_value_policies_matrix_contract.py:18`: `from core.services.common.value_policies` → `from core.shared.value_policies`
  - 理由: 这两测试断言的「降级发射 / 策略矩阵」**行为承重点在 core.shared,不在壳**。平移 import 后覆盖无损,且 R30 删 `core.shared` 实现时仍受这两测试保护。
- **步骤2(R30 删实现后收尾)——退身份续命断言:**
  - 删 `tests/regression_config_service_component_contract.py:13-20` 元组中 `:14 compat_parse`、`:16 field_parse`、`:19 value_policies` 三条(保留 `:15 degradation` = R29 反例);
  - 删 `:393-399` value_policies 身份测试、`:402-411` parse_core 身份测试中针对三壳的断言(注意 `:411 parse_compat_date is` 在 R30 删 `parse_compat_date` 后**已失效**,须由 R33 同步删,但 R30 自身链已声明此协调)。
- **步骤3(最后)——删三 .py 壳文件**(compat_parse.py / field_parse.py / value_policies.py)。

**无 P5 收口点新建**: 本债是删壳,不收口、不建第二模块。承重点 `core.shared.{compat_parse,field_parse,value_policies}` 已存在(回盘 def/class 计数 14/6/4),生产已直连,不需新增收口。

## 5) 同文件碰撞债(sibling 协调)

同文件兄弟债 R30、R31 —— 二者均与 R33 在 `compat_parse.py` / `value_policies.py` 壳 + 同批测试上接触。

### R30(B06,planned,lb=false)——「compat 日期分支整条下线」
- **R30 改哪段**: 删 `core/shared/compat_parse.py` 的 `parse_compat_date(:198-215)` + `_date_fallback(:143-150)` + 顶部相关 import;删 `core/shared/value_policies.py` 的 due_date/start_time/end_time 三个日期策略 + VALUE_DATE/DATETIME 相关。**R30 动的是 `core.shared` 真实现**。
- **R33 改哪段**: 删 `core.services.common` 三壳 + 退测试。**R33 动的是壳 + 测试**。
- **谁先谁后(硬顺序,registry deps_hint 明示)**:
  1. **R33 步骤1**(迁两测试 import 到 core.shared)**最先**;
  2. **R30**(删 `parse_compat_date` 实现 + 退 date 用例);
  3. **R33 步骤2/3**(删身份断言含 `:411`、删壳)收尾。
- **撞行号/键位移**: 二者都改 `tests/regression_config_service_component_contract.py`。R33 删 `:14/:16/:19` 元组条目 + `:393-411` 断言,R30 退 date 相关用例。**若同批 PR 必须协调:先 R33 步骤1(仅改 :18 两行,不碰 config_contract)→ R30 改实现+退用例 → R33 删 config_contract 断言+壳。** `:411 parse_compat_date is` 这一行是双方交界: R30 删实现后它必然失效,约定由 R33 步骤2 删除(R30 链也已声明)。**绝不能 R30 先删 `parse_compat_date` 而 R33 测试 import 未迁 → 测试红。**

### R31(B13,planned,lb=false)——「WRITE_INTERNAL_ONLY 写模式常量」
- **R31 改哪段**: 源侧 `core/shared/value_policies.py:9 WRITE_INTERNAL_ONLY = "write_internal_only"`(无任何 FieldPolicy 用它,定义+re-export+白名单齐全但实际死)。
- **R33 接触面**: `WRITE_INTERNAL_ONLY` 在 **services.common 壳侧仅是 re-export**(`core/services/common/value_policies.py:11` import 行 + `:29` `__all__` 条目)。
- **吞并关系(registry deps_hint 明示)**: R33 删整个 `value_policies.py` 壳,会**连带吞掉 :11/:29 这两处 WRITE_INTERNAL_ONLY 的壳侧 re-export**。R31 须改的「源侧」`core/shared/value_policies.py:9` 由 **R31 自己处置,R33 不碰 core.shared**。
- **顺序**: 二者无强硬序,但**若 R33 先删壳,R31 在 services.common 侧无残留可改**(已被吞);R31 只需专注 core.shared 源侧。建议 R33 删壳 与 R31 删源 同批或 R33 先,避免 R31 改了壳侧又被 R33 删一次的无效改动。

## 6) 同收口点债

**本债无收口动作(删壳,非收口)。** 因此不存在「收口到同一符号的其他债 + 顺序敏感性」意义下的同收口点债。

唯一与「收口点」沾边的语义: 三壳承重点 `core.shared.{compat_parse,field_parse,value_policies}` 是生产已统一直连的既有点。R30 在该承重点上做「删 date 实现」、R31 在该承重点 `value_policies.py:9` 做「删 WRITE_INTERNAL_ONLY」—— 它们是**对同一承重模块的并行减法**,顺序敏感性已在 §5 覆盖(R33步骤1 必须夹在 R30 删实现之前)。R33 自身只删壳,不向承重点写入任何东西。

## 7) 收口行为差异 + parity 测试

**不适用收口 parity。** 三壳是纯 `from core.shared import ...` re-export(def=0,无任何本地实现/包装/默认值),壳侧符号与 `core.shared` 源侧是**同一对象**(`is` 身份相等,见 config_contract `:397-399`/`:410-411` 现有断言)。因此「新旧两路对边界值是否逐分支等价」在 R33 范围内**恒真**——删壳后调用方(测试)直接 import core.shared,得到的就是原对象,无行为差异、无 None vs raise 反例可言。

唯一需 parity 保护的是 **R30 删 `parse_compat_date` 实现** 时的行为退场——但那是 R30 的边界,不是 R33。R33 的职责仅是: 在 R30 删实现前,确保 `tests/regression_compat_parse_emits_degradation.py`(降级发射行为)与 `tests/regression_value_policies_matrix_contract.py`(策略矩阵行为)的 import 已迁到 core.shared,使这两份**行为 parity 测试**继续覆盖承重点。即 R33 步骤1 是在「为 R30 的 parity 保护让路」,自身不引入 parity 缺口。

## 8) 爆炸半径(生产/测试消费者 + 灾难链)

### 生产消费者: **零(已 grep 实证)**
`rg "core\.services\.common\.(compat_parse|field_parse|value_policies)" core/ web/ data/ scripts/ run/` → **0 命中**。全仓引用三壳的**只有 3 个测试文件**(下列)。生产直连 `core.shared.*`(如 `core.shared.field_parse` 被 greedy/external_groups、schedule_config_runtime_coercion、run/schedule_input_builder 等),与三壳无关。
→ **删三壳生产运行期零影响。**

### 测试消费者(全部,3 个文件):
1. `tests/regression_config_service_component_contract.py`: `:13-20`(`_SERVICE_COMMON_NEUTRAL_HELPERS` 元组含三者+degradation)、`:355`(run 层 FORBIDDEN 白名单 startswith)、`:393-399`(value_policies 身份)、`:402-411`(parse_core 身份,:411=parse_compat_date is)
2. `tests/regression_compat_parse_emits_degradation.py:18`(经壳 import,降级发射行为)
3. `tests/regression_value_policies_matrix_contract.py:18`(经壳 import,策略矩阵行为)

### 灾难链(删错/统一错会静默炸什么):
- **链A(删序错 → 测试红,非静默)**: 若先删壳/先删 `:411` 而未迁 `:18` 两测试 import → `ImportError` / 身份断言失败,**立即红、不静默**。这是「响声债」,可控。
- **链B(误删 R29 反例 → 静默破坏意图)**: `_SERVICE_COMMON_NEUTRAL_HELPERS` `:15` 的 `degradation` 是 **R29 有意保留的反例**(同 family 但不删)。若删元组时连带删 `:15` → `degradation` 壳的「中性 helper 白名单 / 身份续命」约束丢失,**这是真正的静默风险**——degradation 壳若日后被误删将无测试拦截。**禁区: 删元组只删 :14/:16/:19,死保 :15。**
- **链C(误碰 core.shared → 炸生产)**: R33 只删壳。**若手滑删到 `core.shared.{compat_parse,field_parse,value_policies}` → 生产直连方(schedule_input_builder/external_groups 等)全炸**。这是 R33 最大爆炸风险,但属「越界操作」,靠 §9 禁区行 + 「R33 不碰 core.shared」铁律拦。
- **链D(与 R30 交界 :411)**: 见 §5,R30 删 `parse_compat_date` 后 `:411` 必失效,须 R33 同步删——漏删则测试红(响声,非静默)。

## 9) 承重毗邻标记 + 禁区行

- registry `lb_no_touch = null`、`load_bearing = false`、`phase1_blast.lb_colocation_danger = "none"`。**R33 三壳本身无承重物毗邻。**
- 但 interference_edges 显示同簇/同文件链上有承重邻居(LB04/LB07,在 `schedule_config_runtime_coercion.py`/`schedule_params.py`)——这些文件 **R33 完全不碰**,仅说明簇 C01 内有承重物,与 R33 删壳动作无交集。

### 禁区行(R33 绝不可触碰):
1. **`core/shared/compat_parse.py` 全文**(14 个 def/class,生产承重点)——尤其 R30 负责的 `parse_compat_date(:198-215)` 由 R30 删,R33 不碰。
2. **`core/shared/field_parse.py` 全文**(6 个 def/class,被 greedy/external_groups/schedule_input_builder 直连)。
3. **`core/shared/value_policies.py` 全文**(4 个 def/class)——尤其 `:9 WRITE_INTERNAL_ONLY` 归 R31 处置,R33 不碰。
4. **`tests/regression_config_service_component_contract.py:15`**(`degradation` 元组条目 = R29 有意保留反例)——删元组时**死保此行**。
5. **`:355` FORBIDDEN 白名单逻辑** 与 **`:375-380` degradation 身份测试**(R29 反例配套)——R33 不动。

## 10) 分层 / 导入环风险

- **删壳是纯减法,不引入任何新 import,因此 0 越层、0 新增导入环。**
- 现状三壳的 import 方向: `core.services.common.*` → `core.shared.*`(高层 services 依赖低层 shared),**合法下沉,无违规**(回盘确认三壳仅 import `core.shared`,无 `core.services` 反向 import)。
- 测试 import 迁移(步骤1): `tests/* → core.shared.*` 取代 `tests/* → core.services.common.*`。测试层 import 任意生产层均合法,**不触发分层红线**(0 AST 违规,不引入 core.algorithms→core.services / core.models→core.services)。
- 删壳后 `core.services.common` 包内少 3 个文件,不影响其余成员(degradation/external_groups/number_utils/strict_parse 等)的导入拓扑。**无导入环风险。**

## 11) 测试耦合(动手前先迁哪些测试)

**先迁 2 份行为测试 import(步骤1,最先,且独立于 config_contract):**
1. `tests/regression_compat_parse_emits_degradation.py:18` → 改指 `core.shared.compat_parse`(覆盖降级发射行为,承重点在 core.shared)。
2. `tests/regression_value_policies_matrix_contract.py:18` → 改指 `core.shared.value_policies`(覆盖策略矩阵行为)。
   - 这两步只改 import 行,断言逻辑不动,迁完单独可跑绿(此时壳仍在,只是测试不再经壳)。

**后退 1 份身份续命测试(步骤2,须在 R30 删实现后):**
3. `tests/regression_config_service_component_contract.py`: 删 `:14/:16/:19` 元组三条(保 `:15`)、删 `:393-399` 与 `:402-411` 针对三壳的身份断言(`:411` 与 R30 协调)。

**删壳(步骤3)前的测试自证**: 迁完步骤1+2 后,全量跑 `pytest tests/regression_compat_parse_emits_degradation.py tests/regression_value_policies_matrix_contract.py tests/regression_config_service_component_contract.py` 应全绿,再删三 .py。删后再跑一遍确认无 import 残留。

## 12) 前置依赖(先做哪些债 / 哪些 F 门才能做本条)

**链内硬顺序(R33↔R30 强耦合,registry deps_hint 明示):**
```
R33 步骤1 (迁 2 测试 import 到 core.shared)   ← 必须最先,为 R30 解锁
        ↓
R30      (删 core.shared.parse_compat_date 实现 + 退 date 用例)
        ↓
R33 步骤2 (删 config_contract 身份断言含 :411 + 退元组三条)
        ↓
R33 步骤3 (删三 .py 壳文件)
```
- **R33 步骤1 是整条链的解锁前置**(若 R30 先删 `parse_compat_date` 而测试 import 未迁 = 红)。
- **R30 是 R33 步骤2/3 的前置**(`:411 parse_compat_date is` 须等 R30 删实现后才删)。
- **R31 弱协调**: R33 删 value_policies 壳会吞并 R31 在 services.common 侧的 WRITE_INTERNAL_ONLY re-export;R31 处置 core.shared 源侧(`:9`)。建议 R33 删壳 与 R31 删源同批或 R33 先,避免 R31 无效改壳侧。

**批次门(registry planned_batch_hint):**
- **Batch-7(B06)**,本桶内第 3(与 R30 配对)。
- **整体晚于 B05/B06 收敛**(PHASE0 §10.2):三壳删除排在 B05/B06 桶其余债收敛之后,避免在桶仍在动时拆兼容层。
- **R29 不是前置而是禁区伴随**: R29(degradation)同 common/ family 但为有意保留反例,做 R33 时**只需保证不连带删它**(见 §9 禁区行 4/5),无先后依赖。

**F 门**: 无专门 F 门前置;只需 B05/B06 桶收敛 + R33 步骤1 先行 + R30 删实现到位。owner_pending=false,可直接进 Batch-7 排期。

## 对抗核验(第二双眼睛)

核验时间 2026-06-05,只读不改,独立重 grep 回盘。结论 **PASS**(low/非承重/非 owner_pending 死叶子债,深核而非快核——因与 R30/R31 强耦合且触及 R29 禁区)。

**重 grep 命令与结果:**
1. 三壳全文 `cat -n core/services/common/{compat_parse,field_parse,value_policies}.py`:def/class **全=0**(纯 re-export),import 行号 compat_parse :3-7/:9-13、field_parse :3/:5、value_policies import :3-19 + `__all__` :21-37,WRITE_INTERNAL_ONLY 在 import :11 / `__all__` :29。**与档案 §1/§5 全吻合。**
2. 生产引用 `rg "core\.services\.common\.(compat_parse|field_parse|value_policies)" core/ web/ data/ scripts/ run/` → **0 命中**;全仓(含 tests)仅 5 行命中,分布在 3 个测试文件(emits_degradation:18、matrix_contract:18、config_contract:14/:16/:19)。**生产零消费证实,与 §8 一致。**
3. `tests/regression_config_service_component_contract.py` 实读:元组 :14 compat_parse / **:15 degradation** / :16 field_parse / :19 value_policies;白名单 startswith 在 :355;身份测试 `def` 行 :393(value_policies)/:402(parse_core),断言体 :397-399、:410-411(:411=`parse_compat_date is`)。**行号一致;档案 §1 已自陈"断言体行号比 def 行略偏后(微漂)"——属如实标注,不构成错误。**
4. 两行为测试 import 实读:emits_degradation:18 `from core.services.common.compat_parse import parse_compat_date, parse_compat_float`、matrix_contract:18 `from core.services.common.value_policies import (...)`。**步骤1 未做证实(planned 未动),与 §2 一致。**
5. 承重点 `core/shared/{compat_parse,field_parse,value_policies}.py` def/class = **14/6/4**,与档案 §57 报数一致,生产直连存在。

**承重判断复核:** load_bearing=false、lb_no_touch=null 属实。三壳 def=0 无任何本地实现,删壳生产零影响。**修法为纯减法(删壳+退/迁测试),无删除/统一/透传/加形参混入承重物**;禁区行(core.shared 三模块全文 + degradation :15)清晰。**承重误删风险:无**(R33 自身不碰承重点;唯一爆炸面"手滑删 core.shared"属越界,靠 §9 禁区拦)。

**分层/导入环复核:** 三壳仅 `core.services.* → core.shared.*` 合法下沉,删壳纯减法 0 新增 import、0 越层、0 新环。属实。

**owner_pending 复核:** =false,档案给终态修法 + Batch-7 排期合规,无擅自越权。

**发现一处定性措辞瑕疵(不影响修法,需读者知悉):** 档案 §3/§9/§12 多处称 `core/services/common/degradation.py` 为"反例**壳**/同 family 反例壳"。实测该文件**不是壳,是真实现模块**(385B,含 DegradationCollector/DegradationEvent 等),被 **20+ 处生产**直连(resource_sheet_builder、gantt_*、schedule_summary_degradation、web/bootstrap/plugins 等)。它之所以是 R29 的"有意保留反例",是因为它**和三壳同处 common/ 目录但性质相反——是真承重实现,绝不能当兼容壳删**。修法结论完全不变(死保元组 :15、删元组只动 :14/:16/:19),但"反例壳"应更正为"反例(真承重实现,非壳)",以免后续执行者误判 degradation 也是可删的 re-export 残渣。建议执行批次落地前由 dossier owner 顺手修正该措辞,不阻断 PASS。

**核验结论: PASS。** 行号一致(身份断言体微漂已被档案如实标注);生产零消费、R29 禁区(degradation 真承重)、R30/R31 协调顺序均经独立回盘证实。无承重误删风险,无需第三方裁断。
