# R10 逐债档案 — get_latest_version_or_1 零生产消费死方法 + 名实矛盾

> 主文件: core/services/scheduler/gantt_service.py | 桶 B14 | 簇 C01 | 严重度 low | 承重 false | owner_pending n
> 标题: get_latest_version_or_1 零生产消费的死方法,且名字与实现自相矛盾
> 本档只读不改、只产计划。

## 1) 当前真实 file:line(grep 回盘)+ 漂移记录
回盘符号 `get_latest_version_or_1`,全仓 rg(排除 docs/_panorama_data)只命中 2 处,皆为 `def`,无任何调用点:
- **死方法定义**: `core/services/scheduler/gantt_service.py:60` `def get_latest_version_or_1(self) -> int:`,体 `:61` `v = int(self.history_repo.get_latest_version() or 0)`,`:62` `return v if v > 0 else 0`。
  - 老行号 `:60-62` → 实际 `:60-62`。**漂移: 无**(零位移,位置精准)。
- **测试镜像 stub**: `tests/candidate/test_scheduler_week_plan_summary_observability.py:59` `def get_latest_version_or_1(self):`,体 `:60` `return 3`(在 `_GanttServiceStub` 内)。
  - 老行号 `:59-60` → 实际 `:59-60`。**漂移: 无**。
- **活方法(禁触)**: `core/services/scheduler/gantt_service.py:64` `def resolve_version(self, value: Any) -> VersionResolution:`,体 `:65-69` 调 `history_repo.get_latest_version()` + `resolve_version_or_latest(...)`。registry hint `:64` → 实际 `:64`,**漂移: 无**。
- 证据: rg `get_latest_version_or_1` 仅 2 def 命中;`rg get_latest_version_or_1 core/ web/ data/ | rg -v "def "` → EXIT=1(零调用);callgraph edges.json 中 `"to":"...get_latest_version_or_1"` 计数 = **0**(零入边),仅 2 条出边(它自己调 `ScheduleHistoryRepository.get_latest_version`)。

## 2) 当前状态 + 与 status evidence 一致性
- registry `status_2026_06_05 = planned`(计划直删,尚未执行)。
- `current_evidence_2026_06_05`: 死方法 + stub 仍在,直删未执行,`resolve_version:64` 未触碰。
- **一致**: gantt_service.py:60-62 死方法、:62 `return v if v>0 else 0`(名实矛盾)、stub:59 全部现场核到,与 evidence 逐项吻合。`resolve_version` 活方法在 :64 未动。状态 planned 准确。

## 3) 病理 / 严重度 / 桶 / 簇
- 病理 **P6**(零真实 caller 死方法 + 命名误导):零生产入边(callgraph 0 入边、rg 0 调用);名字承诺 `_or_1`(版本兜底为 1),实现却 `return v if v>0 else 0`——版本无效时返回 **0** 而非 1,名实自相矛盾,误导未来读者以为有"至少 1 版"语义。
- 严重度 **low**(死代码,无运行期影响;唯一危害是认知债)。
- 桶 **B14**(home=B14);簇 **C01**;partition `scheduler-gantt`;kind=real。
## 4) 修法类
**直删(非收口、非承重)**。owner_pending=false,可给终态:
- (1) 删 `gantt_service.py:60-62` 整个 `get_latest_version_or_1` 方法(含 `:60` def、`:61` 取版本、`:62` `return v if v>0 else 0`)。
- (2) 删测试镜像 stub `tests/candidate/test_scheduler_week_plan_summary_observability.py:59-60`(`def get_latest_version_or_1(self): return 3`)——仅为镜像 GanttService 表面而存在,通读全测试从不被 invoke(rg 仅命中其 def,无调用)。
- **禁区**: 不得触碰 `gantt_service.py:64 resolve_version`(活的版本解析路径,被周计划/详情链使用),它与死方法共享 `history_repo.get_latest_version()` 仅是巧合,语义独立。
- **灵魂线核查**: 本修法是纯删除,不新增兜底/静默回退/except 吞错/过度防御;无 P4 loud-raise 诉求。删除后 `return v if v>0 else 0` 的误导语义随之消失,不残留任何"or_1 返回 1"幻觉。
- **非 P5**: 本债不收口到任何统一点,纯删孤儿。

## 5) 同文件碰撞债(sibling)
同 `gantt_service.py` 兄弟债(interference 全为 same_file=true、same_symbol=false):
- **R55**(B08, medium, planned, 唯一显式 `same_file_siblings`): 落点 `critical_chain_for_plan_detail_filter`,registry hint :375 → **实际 :384**(import 别名在 :21,调用在 :384;**漂移 +9 行**)。R55 修法是"在 :384 区域补 scope=filtered/full 标记"(补注释 + 标记,非裸删)。
  - **改不同区域**(R10 删头部 :60-62 / R55 改中段 :384),物理不重叠。
  - **谁先谁后**: 推荐 **R10 先删**(删上方 :60-62 共 3 行 → R55 的 :384 整体上移 3 行至 ~:381;但 R55 按符号名定位不靠行号,不受影响)。反之 R55 先改(在 :384 区只增不删 → 不影响 :60-62)。**任意顺序皆安全,无行号撞车致命性**。
  - **必须同批或串行**: planned_deps_hint 已把 R10/R55 归 Batch-9(B08 SCC-GANTT)同批,避免两 PR 改同文件冲突。
- **R11 / R12 / R21 / R34 / R63**(interference same_file): 均与 R10 共居 gantt_service.py 但 same_symbol=false,各自落点不同区域。R10 删 :60-62 在文件头部(类定义早段),与这些债的落点(critical_chain/view_context/week_plan 等中后段)无行号交叠。仅需同批避免合并冲突,无语义碰撞。
- **dict 键位移风险**: R10 删的是一个方法定义,不涉及任何 dict 字面量/键序;无键位移风险。

## 6) 同收口点债
- **无**。R10 是直删孤儿方法,不收口到任何统一符号;它内部调的 `history_repo.get_latest_version()` 也被 `resolve_version` 独立调用,但删除死方法不改变该 repo 方法的任何调用面。
- 顺序敏感性: 无同收口点债,故无收口顺序约束。仅有 §5 的同文件合并冲突约束。

## 7) 收口行为差异 + parity 测试
- **不适用**(本债为直删,非收口/统一)。无新旧两路等价性问题。
- 唯一需验证的"行为不变"断言: 删除后无任何生产路径行为改变——已由"零入边(callgraph 0、rg 0 调用)"证明。**无需新增 parity 测试**;反需删除测试 stub(§4 步骤2)。
- 反例提示(供怀疑者复核): 若误删 :64 `resolve_version` 而非 :60 死方法,会静默炸周计划版本解析(见 §8 灾难链)——二者名字相近(均含 version),删时须按行号 :60-62 精确锚定,勿按模糊符号匹配。
## 8) 爆炸半径
- **生产消费者**: **零**。`rg get_latest_version_or_1 core/ web/ data/ | rg -v "def "` → EXIT=1(无调用);callgraph `"to":"...get_latest_version_or_1"` 入边 = 0。周计划路由 `scheduler_week_plan.py` 不触碰它(rg 在该文件 0 命中 get_latest_version_or_1/resolve_week_range/get_week_plan_rows)。Flask 路由/registry 已排除。
- **测试消费者**: 仅 `tests/candidate/test_scheduler_week_plan_summary_observability.py:59`(`_GanttServiceStub` 内镜像 def,通读测试从不被 invoke——rg 在该文件仅命中其 def,无调用点)。
- **下游影响**: 删方法 + 删 stub:59,无生产影响(零 caller);消除 "or_1 返回 0/1" 命名误导。
- **灾难链(删错/统一错静默炸什么)**:
  1. 若误删 `:64 resolve_version`(相邻活方法,名字也含 version)→ 静默炸周计划/详情的版本解析路径(`resolve_version_or_latest` 链断),版本回退逻辑失效。**防御: 按 :60-62 行号精确删,删后立即 grep `def resolve_version` 确认 :64 仍在**。
  2. 若只删生产 def 而漏删 stub:59 → 测试 stub 残留死镜像 API,无运行报错但留认知债(下次有人以为生产仍有此方法)。**防御: 两处同 PR 删**。
  3. 若漏删生产 def 只删 stub → 死方法继续误导,病未除。

## 9) 承重毗邻标记
- 本债 load_bearing=false,lb_no_touch=null,`lb_colocation_danger=none`。
- 同文件 gantt_service.py 兄弟债(R55/R11/R12/R21/R34/R63)经 registry 标记均无 load_bearing,无 lb_no_touch 禁区落在 :60-62 邻域。
- **禁区行**: 无承重禁区。唯一"勿动"行是 **:64-69 `resolve_version`(活方法,非承重但删错即炸版本解析)**——非 LB 禁区,属操作精度禁区,已在 §8 灾难链列明。

## 10) 分层 / 导入环风险
- 纯删除,**不新增任何 import**,故 0 新增越层风险、0 新增导入环。
- 现状 imports(gantt_service.py:1-37)合规: `core.infrastructure.errors`、`data.repositories`、同包 `.gantt_*` 相对导入,无 core.algorithms→core.services 或 core.models→core.services 越层。删死方法后该清单不变。
- AST 违规: 0(删除操作不可能引入跨层 import)。

## 11) 测试耦合
- 动手前/同步需迁移的测试: **删** `tests/candidate/test_scheduler_week_plan_summary_observability.py:59-60` 的 stub 镜像 def(非迁移,是删除——它从不被调用,删后该测试文件其余逻辑不受影响,因为 `_GanttServiceStub` 的实际被调方法是 `resolve_week_range`/`get_week_plan_rows`)。
- 删 stub 后建议跑该 regression 文件一遍,确认 stub 其余 API(resolve_week_range/get_week_plan_rows/resolve_version 若有)未被牵连。
- 无需新增测试(死代码删除,零行为变更)。

## 12) 前置依赖
- **批次**: Batch-9(B08 SCC-GANTT),与 R55 同批(planned_batch_hint/deps_hint 已锁定)。自成簇 C01,不阻塞其他桶。Low 优先级。
- **同文件协调**: 与 R55(及同文件 R11/R12/R21/R34/R63)须**同批次或串行**,避免同文件并发 PR 合并冲突。R10/R55 改不同区域,任意顺序安全(§5),但不可同时两 PR 并发改 gantt_service.py。
- **F 门**: 无特殊架构门。删除前唯一前置确认 = grep 复核 `:64 resolve_version` 未被波及(§8 防御1)。
- **owner_pending**: false — 无需 owner 裁断,可直接给终态修法 + 分配 Batch-9。

## 对抗核验(第二双眼睛)
独立重 grep(cwd=仓库根),逐项挑战档案,未找到反证:

1. **行号复核——一致**。`rg -n get_latest_version_or_1 --glob '!docs/_panorama_data/**'` → 仅 2 命中:`gantt_service.py:60 def get_latest_version_or_1`、`tests/candidate/test_scheduler_week_plan_summary_observability.py:59 def`。`sed -n '55,75p' gantt_service.py` 现场:死方法 :60 def / :61 `v=int(...or 0)` / :62 `return v if v>0 else 0`,活方法 `resolve_version` 在 :64。stub 现场 :59 def / :60 `return 3`。三处零漂移,与档案 §1 完全吻合。名实矛盾(`_or_1` 却返回 0)现场确认。
2. **零调用——确认**。`rg -n get_latest_version_or_1 core/ web/ data/ tests/ | rg -v "def "` → EXIT=1。属性调用形式 `rg -n "\.get_latest_version_or_1\s*\("` 全仓 → EXIT=1(0 命中)。`getattr/hasattr/__dict__` 反射式隐藏调用 → 无。死方法判定成立。
3. **stub 真死——确认**。`_GanttServiceStub` 在 regression 文件实际被断言的是 `resolve_calls/row_calls`(:141-142,对应 resolve_week_range/get_week_plan_rows),从不 invoke get_latest_version_or_1。删 stub:59-60 安全,不牵连其余测试。
4. **最大风险点 resolve_version——活方法,删错即炸,但物理隔离**。`rg -n "\.resolve_version\s*\(" --glob '!tests/**'` → 唯一真实消费 `web/routes/domains/scheduler/scheduler_gantt.py:187 svc.resolve_version(request.args.get("version"))`。`rg -n "def resolve_version"` 全仓仅 `gantt_service.py:64` 一处(+ version_resolution.py:39 的 `resolve_version_or_latest` 是不同符号),无同名混淆。它在 :64,与死方法 :60-62 物理隔离;档案 §8 防御1「按行号精删 + 删后 grep 复核 :64 在」必要且充分。
5. **灵魂线/承重/分层——干净**。纯删除,不新增兜底/import/except/过度防御,无 P4 loud-raise 诉求;非收口(无统一点)、非承重(lb=false、lb_no_touch=null)、0 越层。无承重误删风险,无 owner_pending 越权(owner_pending=false 且档案未擅自越权)。

**核验结论: PASS**。行号一致,死叶子+stub 直删零引用证据双证(rg EXIT=1 + 属性调用 0 命中 + callgraph 0 入边),唯一操作精度禁区 resolve_version:64 已锁定且物理隔离。无需第三方裁断。
