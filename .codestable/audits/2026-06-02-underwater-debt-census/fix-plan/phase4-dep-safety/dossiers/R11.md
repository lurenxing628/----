# R11 逐债档案 — _normalize_critical_chain_result 第二份私有实现（关键链结果归一）

> 只读不改产物。所有断言附当前工作区 file:line 证据。owner_pending=true → 终态修法与执行批次「待 owner 裁断」，本档只把依赖/爆炸半径分析透。
> 桶 B08 / 病理 P5 / 簇 C01 / 严重度 low / 承重 false / lb_no_touch null。
> R11 ≡ R63（同一动作），planned_fix_hint 明确「不单独投工，与 R63 合并为单个 work item」。

---

## 字段 1：当前真实 file:line（grep 回盘）+ 漂移记录

`rg -n "def _normalize_critical_chain_result" core/ web/ data/` 在全仓恰好命中 **2 处**，与 registry「repo 内恰好 2 处」吻合：

| 符号 | registry old_location_DONT_TRUST | 当前真实 file:line | 漂移 |
|---|---|---|---|
| `_normalize_critical_chain_result`（module 级，第一份） | `gantt_service_support.py:32-51` | `core/services/scheduler/gantt_service_support.py:32-51` | **无漂移**（32-51 准） |
| `_normalize_critical_chain_result`（`@staticmethod`，第二份） | current_evidence 称 provider.py:113 / phase1_blast 称 104-128 | `core/services/scheduler/gantt_critical_chain_provider.py:113-137` | **轻微漂移**：def 头确在 :113；blast 写「104-128」是含 `_copy_critical_chain_result`(:104-110) 的连读块，实际归一体 :113-137 |

调用点与出口（registry prod_consumers 行号 vs 实际）：

| 锚点 | registry 行号 | 当前真实 file:line | 漂移 |
|---|---|---|---|
| support 路径自调 `_normalize`（在 `critical_chain_for_plan_detail_filter` 内） | `gantt_service_support.py:58` | `gantt_service_support.py:58`（def 头 :54） | **无** |
| provider 路径自调 `_normalize`（在 `get_critical_chain` 内） | `gantt_critical_chain_provider.py:171` | `gantt_critical_chain_provider.py:180`（def 头 :143） | **漂移 +9 行**（registry 171 不准） |
| 出口汇入：support/provider 两路在 gantt_service.py 的分叉点 | `gantt_service.py:375/377` | `gantt_service.py:384`（support）/ `:386`（provider，`get_critical_chain`） | **漂移**：375/377 是 `build_tasks` 块；真实分叉在 :384/:386 |
| 白名单收敛 `_public_critical_chain` | `gantt_contract.py:89`（调用处）/ :19（def） | def `gantt_contract.py:19`；调用 `gantt_contract.py:89` | **无** |
| §4 候选公共落点 `gantt_critical_chain.py` 现有 helper | — | `_empty_result:54` / `_unavailable_result:71`；**无** `_normalize_critical_chain_result` | 吻合 registry「无统一公共 helper」 |

**漂移结论**：第一份（support:32-51）行号完全准；第二份 def 头准（:113）、blast 区间标注与归一体范围有澄清差异；两处下游调用行号（provider:171→180、service:375/377→384/386）有 +9~+9 漂移，照抄会指错块。

## 字段 2：当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`；`adv_refuted=false`，`verdict.refuted=true`、`verdict.bucket=real_debt_confirmed`、`verdict_kind=real_debt` → 已经过对抗核验、坐实为真债。
- 与 current_evidence 一致：两份 `_normalize` 实测仍各自存活（support:32 + provider:113），未抽取，故仍 `planned` 合理。
- registry 自带 location_status 已澄清「R11 称该文件『新增未提交(A)』已过时」。本轮独立验证：`git cat-file -e b08162cd:core/services/scheduler/gantt_service_support.py` → **EXISTS_IN_b08162cd**，即 b08162cd 已含此文件，「A 未提交」说法过时，R11 与 R63「已提交坐实」一致。**结论：状态 planned 与现态一致，无矛盾。**

## 字段 3：病理 / 严重度 / 桶 / 簇

- **病理 P5**：第二份私有实现（关键链结果归一）。同一段归一逻辑被逐字复制成两份：support 模块级 :32-51 + provider 静态方法 :113-137。两份都活：
  - support 路径：`critical_chain_for_plan_detail_filter`（:54）→ 有 `detail_filters` 时走（gantt_service.py:384）。
  - provider 路径：`get_critical_chain`（:143）→ 无 filter 时走（gantt_service.py:386）。
  - 两路在 gantt_service.py 汇合后透到 build_gantt_contract → `gantt_contract.py:19 _public_critical_chain` 白名单（:89 调用）。
- **严重度 low**：当前两份字段语义近似等价（见字段 7），现态前端无可见差异；风险是「未来分叉」而非「当前已分叉」。
- **桶 B08；簇 C01。** kind=real，load_bearing=false，needs_adversarial=true 且未被推翻（real_debt）。
- **R11 ≡ R63**：registry 多处明示二者是同一动作（同一对 `_normalize` 副本），不可当两条债分别投工。

## 字段 4：修法类

**修法类 = 收口到 X（去重抽单份）**，但 **owner_pending=true → 待 owner 裁断，暂不给终态修法、暂不分配执行批次**。以下为「若获批」的方向性分析，不作为终态指令。

- **唯一允许的收口点（必须是已存在点）**：`core/services/scheduler/gantt_critical_chain.py`。该文件已是关键链 compute 的公共模块（已存在 `_empty_result:54` / `_unavailable_result:71` / `compute_critical_chain_from_rows`，被 support:7 与 provider 双双 import），把 `_normalize_critical_chain_result` 抽成它的一个公开/半公开 helper，**符合铁律 5「收口到已存在的统一点」**，不构成「为已有概念新建第二模块=新 P5」。**绝不可新建 normalize 专用模块。**
- support（:58）与 provider（:180）两路改为调用该单份 helper。
- **红线（铁律 4/5）**：
  - 绝不把 `critical_chain_for_plan_detail_filter` 简化为 `return raw`（phase1_blast.lb_colocation_danger 明示：raw `compute_critical_chain_from_rows` 缺 `available`/`edge_type_stats` 默认，归一调用对 support 路径承重，`collect_gantt_degradation_events:70` 与白名单 `_public_critical_chain` 都依赖归一后的 `available`/`reason_code`）。
  - 不得借机加兜底/静默回退/except 吞错（本债为 P5 去重，不是 P4，无需改 loud raise，但也不得引入新兜底）。
- **R11 ≡ R63**：本债不单独投工。修法、收口点、红线以 R63 条目为准；R11/R63 合并为单个 work item 一次落地。

## 字段 5：同文件碰撞债（siblings R55 / R63）

same_file_siblings = [R55, R63]。两者均与本债跨 `gantt_service_support.py` / `gantt_service.py` 同文件碰撞（interference_edges 标 same_file=true、same_symbol=false）。

- **R63（= R11，同一动作）**：不是「碰撞」而是「同一债」。registry planned_deps_hint：「R11≡R63（Phase1 纠偏#1）… 合并为单动作，该动作必须早于 R12+R55」。**顺序：R11/R63 作为同一 work item 一并改，谁先无意义（本就一次落）。** 风险：若被误派成两人各改一份副本 → 两人撞改同两文件（support.py + provider.py），且可能一人删 support 副本、一人删 provider 副本造成调用悬空。**必须合并派工。**
- **R55（加 scope 字段，同文件 gantt_service_support.py / gantt_service.py）**：R55 要往关键链 payload 增 `scope` 维度。**谁先**：phase1_blast.fix_invalidation_risk 明示「R11/R63 必须早于 R12+R55」——若先做 R55 在两份副本各加 `scope`，则 R11 抽取时要合并两份带 `scope` 的副本，等于复活本债的根因。**R11/R63 先、R55 后。** 撞行号风险：R55 大概率改 `_normalize` 返回 dict 的键集（:42-51 / :126-137），与 R11 抽取后的单份 helper **同段同 dict 键位**，若并行会硬撞；串行（R11 先）则 R55 只改单份，反而省冲突。
- **dict 键位移风险**：R11 抽取不改键集（保持 ids/edges/makespan_end/edge_type_stats/edge_count/available/reason/reason_code 八键原样），故对快照测试 `regression_gantt_contract_snapshot.py:122` 的 subset 检查不会变红；但 R55 加键后该 subset 检查仍宽松（加键不红，见字段 8）。

## 字段 6：同收口点债 + 顺序敏感性

- **收口到同一符号/同一落点的债**：仅 R63（≡R11，同一动作，收口到 `gantt_critical_chain.py` 单份 `_normalize`）。无第三条债声明收口到这个符号。
- **顺序敏感（registry co_change_within_group = [R63, R12, R55]）**：
  - **R11/R63（单动作）必须最先**（Batch-9 work item 最先落，planned_batch_hint）。
  - **R12（加 `dropped_count`）紧随其后**：fix_invalidation_risk 明示「若不先统一，R12 加 dropped_count、R55 加 scope 会分别改两份副本=复活本债」。R12/R55 都依赖本债先收口。
  - 链：**R11≡R63 → 再 R12 → 再 R55**（R12/R55 间相对顺序由各自档案定，但都晚于 R11/R63）。
  - 顺序敏感性高：倒序做（先 R12/R55 后 R11）= 直接复活 P5，且让 R11 的抽取工作量翻倍（要合并已被 R12/R55 改花的两份副本）。

## 字段 7：收口行为差异 + 所需 parity 测试

**逐分支比对两份当前实现（support:32-51 vs provider:113-137）——必须在收口前证逐分支等价，否则去重会引入静默行为变化：**

| 行为点 | support 第一份 (:32-51) | provider 第二份 (:113-137) | 等价? |
|---|---|---|---|
| 非 dict 入参 | `raw={}`（:33-34） | `raw={}`（:114-115） | ✅ |
| `available` 非 bool 默认 | `is_available=True`（:36） | `is_available=True`（:119-120） | ✅ |
| `reason` 提取 | `_text(raw.get("reason"))`（:37，`_text`=`str(x or "").strip()`） | `str(raw.get("reason") or "").strip()`（:121） | ✅ 同义 |
| `reason_code` 提取 | `_text(raw.get("reason_code") or raw.get("reason"))`（:38） | `str(raw.get("reason_code") or raw.get("reason") or "").strip()`（:122） | ✅ 同义（support `_text` 内部 `x or ""` 已等价于多重 `or`） |
| available 时清 reason/code | :39-41 清空 | :123-125 清空 | ✅ |
| 八键返回（ids/edges/makespan_end/edge_type_stats/edge_count/available/reason/reason_code） | :42-51 | :126-137 | ✅ 逐键同默认（edge_type_stats 四键 `process/machine/operator/unknown` 默认一致；edge_count `int(... or 0)`；reason_code `or ("unknown" if not is_available else "")` 一致） |
| `available` 返回类型 | `bool(is_available)`（:48，**显式 bool() 包裹**） | `is_available`（:134，**裸值**） | ⚠️ **微差**：support 显式 `bool()`，provider 裸传。当前两路 `is_available` 已是 bool（前面分支保证），故运行时等价；但若收口取 provider 那份（无 `bool()`），且未来有人改前置使 `is_available` 漏成非 bool，support 路径会丢掉防御。**收口时应保留 `bool(is_available)` 那份的显式包裹（取 support 写法或合并时补上）。** |

**反例边界值（parity 测试必须覆盖）**：
1. `raw=None` / `raw=123`（非 dict）→ 两路均应得「available=True、八键齐、ids/edges 空、edge_type_stats 四键 0」。
2. `raw={"available": False, "reason": "rows_load_exception"}`（provider :177 真实会产此 raw）→ 两路 `reason_code` 应为 `"rows_load_exception"`、`available=False`。
3. `raw={"available": "yes"}`（available 非 bool）→ 两路应 fallback `is_available=True`、清 reason；验证 `bool()` 包裹差异不致分叉。
4. `raw={"available": False}` 无 reason → 两路 `reason_code` 应为 `"unknown"`（:50 / :136 fallback）。
5. `raw={"edge_type_stats": {"process": 5}}`（缺键）→ **当前两份都用 `dict(raw.get("edge_type_stats") or {...四键})`：注意 raw 给了非空 dict 时，`or` 短路保留 raw 的 `{"process":5}`，四键默认不补**——两份此分支同行为（都不补 machine/operator/unknown），属同一既有行为，收口须保持。

**所需 parity 测试**：新增 `tests/regression_gantt_critical_chain_normalize_parity.py`（或并入 R63 测试），对上述 5 类边界值断言「收口后单份 helper 的输出 == 收口前 support 旧实现输出 == provider 旧实现输出」，逐键 assert。**重点钉死 `available` 的 `bool()` 包裹差异**（反例 3）。

## 字段 8：爆炸半径（生产/测试消费者 + 灾难链）

**生产消费者（回盘核实）**：
- `core/services/scheduler/gantt_service_support.py:58`：`critical_chain_for_plan_detail_filter` 自调 `_normalize`（support 路径）。
- `core/services/scheduler/gantt_critical_chain_provider.py:180`：`get_critical_chain` 自调 `self._normalize_critical_chain_result(raw)`（registry 写 :171，**实际 :180**）。
- 出口汇入：`gantt_service.py:384`（support 分叉）/ `:386`（provider 分叉，`get_critical_chain`）→ 同 build_gantt_contract 流 → `gantt_contract.py:89` 调 `_public_critical_chain`（def :19）白名单收敛 → 前端。
- 下游消费归一后字段：`collect_gantt_degradation_events`（support.py:61，:70-71 读 `critical_chain.get("available")`/`reason_code`/`reason`）；provider 缓存层 `_copy_critical_chain_result`(:104)、`_critical_chain_cacheable`(:140 读 `available`)。
- 前端契约：`makespan`/`reason_code`/`edge_type_stats` 透到 JS `gantt_contract.js` / `gantt_legend.js`（downstream_effect）。

**测试消费者（回盘核实，registry 给的行号未逐一重测，按符号追）**：
- `tests/regression_gantt_critical_chain_provider.py`（get_critical_chain × 多 + monkeypatch `compute_critical_chain_from_rows`）。
- `tests/regression_gantt_critical_chain_unavailable.py`（compute_from_rows + provider.get_critical_chain，available:False 降级契约）。
- `tests/regression_gantt_contract_snapshot.py`（critical_chain 字段集快照；**:122 用 subset 检查 `required_top_keys - set(data.keys())`，加键不会红** → 字段分叉无法被该测试捕获，是漏网点）。
- `tests/regression_gantt_task_detail_panel_contract.py`（`compute_critical_chain_from_rows` 公开标签）。

**灾难链**：
1. **删错（去重时只保留一份、误删 support 那份并把 `critical_chain_for_plan_detail_filter` 改 `return raw`）** → support 路径（有 detail_filters 的甘特）payload 缺 `available`/`edge_type_stats` 默认 → `collect_gantt_degradation_events:70` 对缺键 raw 判 `available is False` 失效 → 降级事件该报不报（**静默**，灵魂线违规），且白名单 `_public_critical_chain` 收到无 `available` 键的 dict 走 `return raw`（:40 else 分支），前端拿到非归一 payload。
2. **统一错（收口取 provider 那份、丢 support 的 `bool(is_available)` 包裹）** → 若未来 `available` 上游变非 bool，support 路径丢防御 → 前端 `available` 字段类型漂移，JS 真值判断分叉（**静默**）。
3. **只改一份（不去重就给 R12/R55 加字段）** → filtered(support) 与 unfiltered(provider) 两路 payload 字段不一致 → 前端契约在「有无 detail_filter」两条路径下分叉，且快照测试 :122 subset 检查抓不到（漏网）→ 用户在筛选/不筛选甘特看到不同关键链字段集（**静默契约分叉**，这正是本债存在的核心危害）。

## 字段 9：承重毗邻标记 + 禁区行

- 本债 `load_bearing=false`、`lb_no_touch=null` → 本债自身**非承重**，修法不受「仅注释」约束（可去重）。
- **但 phase1_blast.lb_colocation_danger 标出毗邻承重行为**：归一调用本身对 support 路径承重。涉及文件均**非 §5 承重清单文件**，但以下行为对调用承重，**抽取时禁区（绝不可触碰/绕过）**：
  - `gantt_service_support.py:58` `return _normalize_critical_chain_result(raw)`——**禁改为 `return raw`**。raw（`compute_critical_chain_from_rows` 的产物）缺 `available`/`edge_type_stats` 四键默认，归一是 support 路径唯一补默认点。
  - `gantt_service_support.py:70-71`（`collect_gantt_degradation_events` 读 `available`/`reason_code`）——依赖归一后字段，禁让其读到未归一 raw。
  - `gantt_critical_chain_provider.py:180` `computed = self._normalize_critical_chain_result(raw)`——禁删归一、禁直接缓存 raw（:183 `_critical_chain_cacheable` 读 `available`）。
  - `gantt_contract.py:19-40` `_public_critical_chain`——白名单依赖归一后的 `available is False` 分支（:22）；禁让其裸收 raw。
- **同文件无 §5 承重债**（lb_no_touch=null，siblings R55/R63 也非承重）。

## 字段 10：分层 / 导入环风险

- **零新增越层 import**：收口落点 `gantt_critical_chain.py` 已被两路 import——`gantt_service_support.py:7 from .gantt_critical_chain import compute_critical_chain_from_rows`、`gantt_critical_chain_provider.py:19 from .gantt_critical_chain import compute_critical_chain, compute_critical_chain_from_rows`。把 `_normalize` 抽到该文件并复用，**只是在两条已存在的 import 边上多取一个符号，不新增任何 import 边、不引入新模块依赖**。
- **无导入环**：`gantt_critical_chain.py` 当前只 import `._sched_utils`、`.gantt_task_labels`（:6-7），不反向 import support/provider。抽取后它仍不 import 上层，**不构成环**（support/provider → gantt_critical_chain 单向）。
- **分层红线（铁律 6）**：全程在 `core/services/scheduler/` 同层平移，无 `core.algorithms→core.services` / `core.models→core.services` 跨层风险。**0 AST 违规预期。**

## 字段 11：测试耦合（动手前先迁哪些测试）

去重前应先就位/迁移的测试（避免抽取后失去回归网）：
1. **先建 parity 测试**（字段 7 的 `regression_gantt_critical_chain_normalize_parity`，或并入 R63）——抽取前先钉死「旧 support / 旧 provider / 新单份」三方逐键等价，含 `available` 的 `bool()` 差异反例。这是抽取的安全网，**必须先于改代码**。
2. `tests/regression_gantt_critical_chain_provider.py`：provider monkeypatch `compute_critical_chain_from_rows` 的桩仍指 `gantt_critical_chain` 模块符号——抽取后若 helper 同模块，monkeypatch 路径不变，但需确认测试不直接断言 `_normalize` 是 staticmethod（若有则迁为调用单份 helper）。
3. `tests/regression_gantt_critical_chain_unavailable.py`：available:False 降级契约——抽取后 support/provider 两路应同走单份 helper，此测试覆盖反例 2/4。
4. `tests/regression_gantt_contract_snapshot.py:122`：**注意其 subset 检查抓不到字段分叉**（漏网），不能依赖它做去重正确性证明；正确性靠 parity 测试。
5. `tests/regression_gantt_task_detail_panel_contract.py`：`compute_critical_chain_from_rows` 公开标签——抽取不动 compute，预期不受影响，回归即可。

## 字段 12：前置依赖

- **R11 ≡ R63**：二者是同一动作，**不存在「R11 依赖 R63」的先后**，而是「合并为单个 work item 一次落地」。Phase3 排期禁拆两条派工（否则重复投入 + 两人撞改 support.py/provider.py 同两处）。
- **本债（R11/R63 单动作）是 R12 与 R55 的前置**（co_change_within_group=[R63,R12,R55]，fix_invalidation_risk）：必须**早于 R12（加 dropped_count）、R55（加 scope）**。倒序=复活本 P5。
- **F 门**：动手前需有字段 7 的 parity 测试绿（F-test 门），以及 `core/services/scheduler/` 分层 AST 检查（铁律 6，0 越层）作为收口后的放行门。
- **owner_pending=true**：以上前置与顺序分析照常给出，但**终态修法落地与执行批次分配待 owner 裁断**，本档不锁定 Batch-9 为最终批次（planned_batch_hint 仅参考）。

---

## 对抗核验（第二双眼睛）

只读不改，独立重 grep 回盘。结论：**PASS（真债坐实，行号已被档案纠正且与现态一致）**。

**重跑命令与结果**：
- `rg -n "def _normalize_critical_chain_result" core/ web/ data/ tests/` → 恰好 2 命中：`gantt_service_support.py:32` + `gantt_critical_chain_provider.py:113`，与 registry「repo 内恰好 2 处」吻合。
- 两份实体逐行比对（`sed -n '32,52p' support` / `sed -n '113,137p' provider`）：八键返回逐键同默认；`available` 微差**复核成立**——support `bool(is_available)`（:48 显式包裹）vs provider 裸 `is_available`（:134），当前两路前置分支已保证 bool 故运行时等价，收口须保留 support 的 `bool()` 包裹。档案字段 7 判断准确。
- 调用点回盘：support 自调确在 `:58`（registry/档案一致）；provider 自调实际在 `:180`（registry 写 `:171`，**漂移 +9**，档案纠正正确）；service 分叉实际 `gantt_service.py:384`（support）/`:386`（provider，registry 写 375/377 实为 build_tasks 块，档案纠正正确）；白名单 `_public_critical_chain` def `:19`/调用 `:89`（无漂移）。
- 收口点核实：`gantt_critical_chain.py` 确为已存在公共模块（`_empty_result:54`/`_unavailable_result:71`/`compute_critical_chain_from_rows:337`），**无** `_normalize_critical_chain_result`。收口到此=「收口到已存在统一点」，**不违铁律 5**（非新建第二模块）。
- 分层/环核实：`gantt_critical_chain.py` 仅 `from ._sched_utils`/`from .gantt_task_labels`（:6-7），不反向 import support/provider；support:7、provider:19 已 import 它。抽取**不新增 import 边、不构成环、0 越层**——铁律 6 通过，档案字段 10 成立。
- 承重核实：本债 `load_bearing=false`/`lb_no_touch=null` 自身非承重；但白名单 else 分支 `gantt_contract.py:40 return raw`（当 `available is not False`）确实存在——证实档案灾难链 1「support 改 return raw → 缺 available 键 dict 走 raw 漏归一」的依据真实。**禁区行（:58 禁改 return raw、:70-71 依赖归一字段、:180 禁删归一、:19-40 白名单依赖归一）全部回盘成立。**

**与档案的微瑕（不影响 PASS）**：
- 字段 7 反例 2 写 provider :177 产 `raw={"available": False, "reason": "rows_load_exception"}`，实测 :177 还多给了 `"reason_code": "rows_load_exception"`（reason_code 也填）。属反例描述简化，不改 parity 等价结论。
- snapshot 测试 subset 检查实测在 `:122 missing_top = required_top_keys - set(data.keys())`，确只查**顶层缺键**，加键/内层字段分叉均不红——档案「漏网点」判断成立（且实际比档案所述更宽，连 top-level 加键都不抓）。
- parity 测试 `tests/regression_gantt_critical_chain_normalize_parity.py` 当前**不存在**，与档案「需新建」一致。

**owner_pending 合规核查**：档案三处（行 3/48/147）明确「待 owner 裁断、暂不给终态修法、暂不分配执行批次、不锁定 Batch-9」，字段 4 全程标「若获批」方向性分析。**无擅自给终态修法、无擅自锁批次，合规。**

**承重误删风险：无**（档案未混入删除/统一/透传/加形参的越界修法；红线明确禁 `return raw`，且禁区行均回盘真实）。

**核验签字**：PASS | 行号一致（含档案已纠正的 +9 漂移与 service 分叉）| owner_pending 合规 | 无需第三方裁断。
