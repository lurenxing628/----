# R25 逐债档案 — graph/ready_queue.py 兼容导出 service 垫片

> 本档案只读不改，只产计划。债 id: R25 | bucket B09 | 严重度 medium | 病理 P3 | 簇 C01 | 承重 false | owner_pending false
> **2026-06-08 B 执行补登：已 fixed。** O07 已裁 R52 走方向 B（保留全量扫描差分 oracle），所以 R25 service 垫片同步保留；本轮只补「我是故意的」注释，不删垫片、不退模块枚举、不补 roadmap 下线备忘。下文旧“删垫片”路线仅作为未来若重启方向 A 的历史风险证据，不是当前执行计划。

## 1) 当前真实 file:line（回盘）+ 漂移记录

R25 = service 层兼容导出垫片 + 它转手的底层 `get_ready_operation_ids`「未接线并行实现」。grep 回盘（rg core/ web/ data/ tests/）：

- **service 垫片本体** `core/services/scheduler/graph/ready_queue.py`：2026-06-08 补注释后实测 **13 行 re-export**。
  - `:1-7` docstring「Compatibility export…so SGS does not import the scheduler service layer」+「我是故意的」KEEP 注释。
  - `:11` `from core.algorithms.greedy.dispatch.ready_queue import ReadyQueueContractError, get_ready_operation_ids`
  - `:13` `__all__ = ["ReadyQueueContractError", "get_ready_operation_ids"]`
  - 漂移：old_location_DONT_TRUST 写 `:1-12`，执行前实测全文件 11 行；2026-06-08 G39 补注释后为 13 行（:11 import / :13 __all__），语义仍是纯 re-export 垫片。
- **底层真实 impl** `core/algorithms/greedy/dispatch/ready_queue.py:103` `def get_ready_operation_ids(...)`，补注释后 `:142` `__all__`（这是 **R52** 的本体范围；R25/R52 same_symbol）。
- **被它「取代」的 LIVE 路径**：`core/algorithms/greedy/dispatch/sgs_graph.py:33` `def _prepare_graph_ready_state(...)`，由 `core/algorithms/greedy/dispatch/sgs.py:16,23 from .sgs_graph import (... _prepare_graph_ready_state)`，`sgs.py:201` 调用。
  - **关键漂移纠错**：registry phase1_blast 把 LIVE SGS 写成「走 sgs.py:16,23」「sgs_graph.py 取代」，但**这两个文件实际在 `core/algorithms/greedy/dispatch/`，不在 `core/services/scheduler/graph/`**。planned_fix_hint 末句也只说「sgs_graph._prepare_graph_ready_state」未带路径，易误读为同目录。本档案钉死：取代者是**算法层** sgs_graph，不是 service 层。
- **三处测试枚举/引用**（rg 实测行号，补注释后已漂移）：
  - `tests/scheduler_graph/test_ready_queue.py:18` `from core.services.scheduler.graph.ready_queue import ...`（**经垫片** import；旧方向 A 删垫片这行会变红）。
  - `tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py:29` 字面量 `"core.services.scheduler.graph.ready_queue"`（懒加载模块名枚举数组，`:20-34` 一串）。
  - `tests/scheduler_graph/test_metrics_topology.py:142` 字面量 `"core.services.scheduler.graph.ready_queue"`。

## 2) 当前状态 + 与 status evidence 是否一致

- registry 2026-06-08 已补登为 `status_2026_06_05 = fixed`。
- 与现状**一致**：垫片仍在磁盘（补注释后 13 行）、两处测试枚举仍含模块名、test_ready_queue.py:18 仍经垫片 import；这是 O07 方向 B 的有意保留，不是漏删。
- verdict：原 `real_debt_confirmed` 用于说明“未认账时确实像债”；当前修法不是删除，而是用注释把隐性债转成显性差分/兼容测试地基。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P3** — 「被取代的未接线 service 垫片」。双层冗余：(a) service 层 `ready_queue.py` 是纯转手桥，零生产消费者（见 §8）；(b) 它转手的底层 `get_ready_operation_ids` 在 LIVE 图 SGS 路径上**根本没被调**——`sgs_graph._prepare_graph_ready_state`（sgs_graph.py:33）走自己的 `_initialize_graph_ready_frontier`（:53 区）构造 ready frontier，全程不碰 `get_ready_operation_ids`（rg 实证：sgs_graph.py 内无 `get_ready_operation_ids` 字样）。故底层 impl 是「并行实现」，service 垫片是「桥到并行实现的桥」。
- **严重度 medium** — 不影响排产产出（downstream_effect=无），仅多一层 service 兼容面 + 一份未接线算法实现，属可维护性/失忆债，非正确性债。
- **桶 B09**（home B09）；**簇 C01**；partition `scheduler-config-summary-graph`；kind=real。
- 历史失忆点（current_evidence）：networkx roadmap 只有 PR-5 原设计文把 shim 描述为「有意兼容导出」，曾缺「已被 sgs_graph 取代、shim 应一并下线」的更新备忘。O07 已否决下线路线，当前用 KEEP 注释把“有意兼容导出 + 差分 oracle”写回代码和台账，不再补 shim 下线备忘。

## 4) 修法类

**修法类 = KEEP + 认账注释**（承重 false，无 lb_no_touch；O07 裁定保留）。

R25 只负责 **service 垫片这一层**（底层 impl 属 R52）。本轮终态：

1. **保留** `core/services/scheduler/graph/ready_queue.py`（补注释后 13 行垫片）。
2. 在垫片 docstring 里补「我是故意的」说明：它随 R52 全量扫描 oracle 保留给兼容/差分测试，不是生产 SGS 的 live ready queue 路径。
3. **不退** `tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py` 与 `tests/scheduler_graph/test_metrics_topology.py` 的模块枚举；这些枚举继续证明垫片可懒加载且不拉入上层图依赖。
4. **不补** “shim 下线” roadmap 备忘，因为下线路线已被 O07 否决。

**灵魂线合规**：只补注释，不新增任何兜底/静默回退/except 吞错/过度防御，无形参改动。无 P5 收口动作。

## 5) 同文件碰撞债（siblings）

`same_file_siblings = [R52]`。R25 与 R52 是**同一逻辑的 shell + body**，必须协调：

- **R52（同 symbol `get_ready_operation_ids`、底层 impl）**：R52 处理 `core/algorithms/greedy/dispatch/ready_queue.py:103-142` 的真实实现与导出（被 sgs_graph 取代的并行实现本体）。R25 处理 service 层垫片 + 它转手的导出面。
  - **当前改哪段**：R25 保留 `core/services/scheduler/graph/ready_queue.py`，只补 docstring 认账注释；R52 保留算法层 `ready_queue.py` 本体，只补差分 oracle 注释；`tests/scheduler_graph/test_ready_queue.py` 继续经 service 垫片 import。
  - **旧方向 A 才会改哪段**：删 R25 垫片整文件 + 退两处枚举；R52 删/降级算法层本体 + 迁 `tests/scheduler_graph/test_ready_queue.py`。
  - **谁先谁后**：当前 O07 KEEP 路线已闭合，不再删垫片、不迁 test_ready_queue.py。历史方向 A 若未来重启，仍必须先迁/同步迁 R52 测试再删 R25 垫片；否则 test_ready_queue.py:18 import 立即变红。
  - **撞行号/dict 键位移**：当前 KEEP 路线只补注释，R25 与 R52 两文件物理隔离，不撞行号、无 dict 键位移。旧方向 A 中，R25 删除的是独立文件、两处枚举是独立测试文件，仍不与 R52 算法层文件发生物理撞行。
  - **结论**：R25 与 R52 当前已在同一 G39 单元按 KEEP 注释闭合；旧方向 A 若未来重启，仍必须同一提交避免中间态红测试。
- 其余 interference_edges（R02@schedule_graph_dispatch_context.py、R50@sgs.py）是 **same_file=true 但 same_symbol=false**，且这两文件 **R25 完全不动**（R25 不 import schedule_graph_dispatch_context，不动 sgs.py）。故 R02/R50 与 R25 **无真实碰撞**，仅簇内 C01 共现，调度时不需与本条争行号。

## 6) 同收口点债

**R25 当前修法是 KEEP + 认账注释，不是收口**——不向任何统一符号收口，故严格意义上无「同收口点债」。

唯一 symbol 关联是 R52（same_symbol `get_ready_operation_ids`），但二者是同一概念的两层（shell+body）一起保留，不是「都收口到某第三点」。当前已按 §5 的 KEEP 路线闭合。无其他债收口到 `get_ready_operation_ids` 或 `core.services.scheduler.graph.ready_queue`（rg + callgraph 实证：该 service 路径零生产 import 边）。

## 7) 收口行为差异及 parity 测试

**不收口 → 无新旧两路 parity 问题。** R25 当前只补注释，不合并/不统一/不透传两套行为，无边界值分支差异（如 None vs raise）需对照。

当前行为不变证明很直接：service 垫片仍从算法层转发同一符号，`test_ready_queue.py` 仍按原 import 路径运行，断言全部沿用。若未来重启旧方向 A，才需要把消费者改为直接 import 算法层并证明同一符号、同一对象。

## 8) 爆炸半径

**生产消费者：零（已 grep + callgraph 双证实）。**
- rg `scheduler.graph.ready_queue` 于 core/ web/ data/（排除垫片自身）→ **空**。无任何生产文件 import 该 service 垫片。
- LIVE 图 SGS 走 `sgs.py:201 _prepare_graph_ready_state`（算法层），`schedule_graph_dispatch_context.py` 不 import ready_queue。
- callgraph `edges.json` 中 `scheduler.graph.ready_queue` 边数 = **0**（grep -c 实证）；`ready_queue.py::get_ready_operation_ids` 等条目均指**算法层**文件，与 service 垫片无入边。

**测试消费者（3 处，全部已定位，当前 KEEP 路线继续保留）：**
- `tests/scheduler_graph/test_ready_queue.py:18`（**经垫片** import，当前保留；旧方向 A 删垫片会红）。
- `tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py:29`（模块名枚举，当前保留；旧方向 A 删垫片才必摘名）。
- `tests/scheduler_graph/test_metrics_topology.py:142`（同上）。

**历史方向 A 灾难链（当前不执行）：**
- 若未来删垫片且漏摘 lazy_runtime/metrics_topology 枚举 → lazy_runtime 会在 `importlib.import_module("core.services.scheduler.graph.ready_queue")` 处抛 ModuleNotFoundError，测试**红**（loud，非静默，可立即发现）；metrics_topology 可能只留下悬空字符串。
- 若未来删垫片但 R52 未迁 test_ready_queue.py → :18 import 红（loud）。
- **无排产产出影响**：SGS 不调此垫片也不调底层 helper，当前 KEEP 只补注释，不改变任何 schedule 结果。
- 唯一「静默」风险点：若**误把算法层** `core.algorithms.greedy.dispatch.ready_queue`（R52 本体/真实 impl）当成 service 垫片删，会连带删掉差分 oracle 覆盖；当前 O07 KEEP 通过“两个文件都不删”规避这个风险。

## 9) 承重毗邻标记

- R25 自身 `load_bearing=false`，`lb_no_touch=null`。
- phase1_blast `lb_colocation_danger=none`：同文件/同提交单元（R25+R52）**无任何承重债毗邻**。
- 2026-06-08 已补登：service 垫片与算法层 ready_queue.py 均已新增 `我是故意的` 认账注释。
- **当前禁区**：不删 service 垫片、不删算法层 full-scan oracle；认账注释就是本轮闭合动作，不是删除授权。

## 10) 分层 / 导入环风险

- 当前 KEEP 路线不新增 import，不改变现有导入方向。
- 现状边：service 垫片 `:11` 从 `core.services.scheduler.graph` → `core.algorithms.greedy.dispatch`（service→algorithms 是**合法向下**方向），本轮保留该边给兼容/差分测试。
- R25 本轮只补注释，故 **0 AST 违规**，不触碰分层红线（无 core.algorithms→core.services、无 core.models→core.services）。
- 导入环风险：none——未新增任何边。

## 11) 旧方向 A 测试耦合（当前不执行）

当前 O07 KEEP 路线不删垫片，所以下列测试迁移不执行。若未来重启方向 A，动手删垫片**之前/同步**必须处理 3 处测试：

1. `tests/scheduler_graph/test_ready_queue.py:18`——把 import 从 `core.services.scheduler.graph.ready_queue` 改指算法层 `core.algorithms.greedy.dispatch.ready_queue`（或随 R52 一并删/迁）。这是**唯一行为性测试**（当前 helper/用例按符号重 rg，禁信旧裸行号），归属 R52 迁移决定其去留；R25 不得在它未迁时先删垫片。
2. `tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py:29`——删字面量行。该数组是 `for` 迭代，断言仅 `networkx_loaded==False`，删一行不影响其余断言、无 count/index 依赖。
3. `tests/scheduler_graph/test_metrics_topology.py:142`——删字面量行（同性质）。

迁移顺序铁律：**测试先就绪 → 再删垫片**，否则任一处 import/枚举变红（loud failure，可发现，非静默）。

## 12) 前置依赖（先做哪些债 / 哪些 F 门）

- **co_change 强约束已按 KEEP 路线闭合：R52（底层 impl）与 R25（shell）同批补注释，不执行“同提交删”路线。**
- **批次提示**：registry 已补登为 **Batch-B/G39 已 fixed**。
- **阶段门**：O07 KEEP 注释闭合后，R52/R25 不再阻塞自身删除路线；仍作为 R26 facade 最晚约束的“桶已收敛”事实。
- **无 load_bearing 前置**（lb_colocation_danger=none），无 adv_precondition（=null），无需先证伪某假设。
- 簇内 C01 邻债 R02/R50 与本条无文件/符号碰撞（§5），**不构成前置**，可并行调度。

## 对抗核验（第二双眼睛）

核验结论：**PASS（带 1 处次要措辞瑕疵，非阻断）**。独立重 grep/callgraph 回盘，主动找反例，未推翻档案任何修法结论。

**重 grep 命令与结果：**
- `nl -ba core/services/scheduler/graph/ready_queue.py; wc -l` → 2026-06-08 补注释后实测 **13 行** re-export，`:11` import / `:13` `__all__`，docstring 自述桥意图并新增 KEEP 注释。
- `rg -n "def get_ready_operation_ids|__all__|class ReadyQueueContractError" core/algorithms/greedy/dispatch/ready_queue.py` → `class:10` / `def get_ready_operation_ids:103` / `__all__:142`。与档案/registry 一致。
- `rg -n "_prepare_graph_ready_state|from .sgs_graph|get_ready_operation_ids" core/algorithms/greedy/dispatch/sgs.py` → `:16 from .sgs_graph import (` / `:23 _prepare_graph_ready_state` / `:201` 调用。`sgs_graph.py:33 def _prepare_graph_ready_state`。LIVE 路径行号全准。
- `rg -n "get_ready_operation_ids" core/algorithms/greedy/dispatch/sgs_graph.py` → **空**。证实 sgs_graph 不碰底层 helper，「并行实现/未接线」判断成立。
- `rg -n "services\.scheduler\.graph\.ready_queue" core/ web/ data/`（排除垫片自身）→ **空**。**生产零消费者证实**。
- callgraph `callgraph_fresh/edges.json`：`ready_queue` 相关 11 条边**全是算法层文件内部 local 边 + 2 条 ambiguous name 误匹配（出边）**，无任何指向 service 垫片 `core.services.scheduler.graph.ready_queue` 的入边。印证 §8「service 路径零 import 边」。
- 三处测试枚举行号补注释后为 `test_ready_queue.py:18` / `lazy_runtime:29` / `metrics_topology:142`，均已按当前盘回盘。test_ready_queue.py 实调点按符号重 rg 复核，差分 helper 仍经 `_full_scan_ready_ids` 调用。
- 承重标记历史核验已过期：2026-06-08 已在 service 垫片与算法层 ready_queue.py 补「我是故意的」认账注释。lb=false / lb_no_touch=null 仍成立；当前禁区是保留 service 垫片与算法层 full-scan oracle，不再把“可整删”作为执行结论。

**承重误删风险：无。** lb=false；2026-06-08 已补「我是故意的」标记，档案当前修法为 KEEP + 认账注释，无统一/透传/加形参/兜底，灵魂线合规。分层方向 service→algorithms 为合法向下，本轮未新增导入边，0 AST 违规。owner_pending=false，给终态修法合规。

**1 处次要措辞瑕疵（不阻断、不改修法）：** 档案 §8 第一条灾难链曾把 metrics_topology 与 lazy_runtime 两个模块枚举并列描述为「漏摘任一→`importlib.import_module` 抛 ModuleNotFoundError 变红」。当前盘已校准为 `metrics_topology:142` 与 `lazy_runtime:29`，且两者失败模式**不同**：
- `lazy_runtime:29` 处于 `modules=[...]` + `for module_name in modules: import_module(module_name)`，删垫片后漏摘 → **确实 loud 红**。
- `metrics_topology:142` 处于 `forbidden_modules=[...]`，该测试**只 pop sys.modules + `import_module("...metrics")` 自身**，对 forbidden 列表项**不做 import_module**。删垫片后漏摘 → **不会变红**，只留一个悬空模块名（断言更宽松，无害静默残留）。
两处**都应摘名**（§4 步骤②正确），只是失败模式一为 loud、一为静默残留——修法动作与正确性结论均不受影响，仅 §8 灾难链表述需将 metrics_topology 一支由「ModuleNotFoundError 红」更正为「悬空残留、不红」。

**phase1_blast 路径纠错复核：** 档案 §1/§3 已正确钉死 sgs.py/sgs_graph.py 在 `core/algorithms/greedy/dispatch/`（算法层）而非 registry phase1_blast 误标的 service 目录——重 grep 证实文件确在算法层，档案纠错成立。

**需升级第三方裁：n**（医级措辞瑕疵已在本节就地更正，修法结论无争议）。
