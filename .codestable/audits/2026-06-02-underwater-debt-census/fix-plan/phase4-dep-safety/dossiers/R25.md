# R25 逐债档案 — graph/ready_queue.py 兼容导出 service 垫片

> 本档案只读不改，只产计划。债 id: R25 | bucket B09 | 严重度 medium | 病理 P3 | 簇 C01 | 承重 false | owner_pending false

## 1) 当前真实 file:line（回盘）+ 漂移记录

R25 = service 层兼容导出垫片 + 它转手的底层 `get_ready_operation_ids`「未接线并行实现」。grep 回盘（rg core/ web/ data/ tests/）：

- **service 垫片本体** `core/services/scheduler/graph/ready_queue.py`：实测 **11 行纯 re-export**。
  - `:1-5` docstring「Compatibility export…so SGS does not import the scheduler service layer」（自述桥意图）。
  - `:9` `from core.algorithms.greedy.dispatch.ready_queue import ReadyQueueContractError, get_ready_operation_ids`
  - `:11` `__all__ = ["ReadyQueueContractError", "get_ready_operation_ids"]`
  - 漂移：**无**。old_location_DONT_TRUST 写 `:1-12`，实测全文件 11 行（:9 import / :11 __all__），与 current_evidence_2026_06_05「仍是 11 行纯 re-export（:9 import + :11 __all__）」逐字吻合。location_status=✅准。
- **底层真实 impl** `core/algorithms/greedy/dispatch/ready_queue.py:103` `def get_ready_operation_ids(...)`，`:138` `__all__`（这是 **R52** 的本体范围；R25/R52 same_symbol）。
- **被它「取代」的 LIVE 路径**：`core/algorithms/greedy/dispatch/sgs_graph.py:33` `def _prepare_graph_ready_state(...)`，由 `core/algorithms/greedy/dispatch/sgs.py:16,23 from .sgs_graph import (... _prepare_graph_ready_state)`，`sgs.py:201` 调用。
  - **关键漂移纠错**：registry phase1_blast 把 LIVE SGS 写成「走 sgs.py:16,23」「sgs_graph.py 取代」，但**这两个文件实际在 `core/algorithms/greedy/dispatch/`，不在 `core/services/scheduler/graph/`**。planned_fix_hint 末句也只说「sgs_graph._prepare_graph_ready_state」未带路径，易误读为同目录。本档案钉死：取代者是**算法层** sgs_graph，不是 service 层。
- **三处测试枚举/引用**（rg 实测行号，全部=registry 标注，零漂移）：
  - `tests/scheduler_graph/test_ready_queue.py:16` `from core.services.scheduler.graph.ready_queue import ...`（**经垫片** import；删垫片这行变红）。
  - `tests/regression_scheduler_graph_lazy_runtime_contract.py:27` 字面量 `"core.services.scheduler.graph.ready_queue"`（懒加载模块名枚举数组，:19-31 一串）。
  - `tests/scheduler_graph/test_metrics_topology.py:140` 字面量 `"core.services.scheduler.graph.ready_queue"`。

## 2) 当前状态 + 与 status evidence 是否一致

- registry `status_2026_06_05 = planned`。
- 与现状**一致**：垫片仍在磁盘（11 行未删）、两处测试枚举仍含模块名、test_ready_queue.py:16 仍经垫片 import。无任何已动手痕迹，确为 planned 而非 in_progress。
- verdict：`real_debt_confirmed`，`refuted=true`（指对抗已确认是真债，非误报）。needs_adversarial=true 且 adv_refuted=false，但 adv_precondition=null（无前置假设需先证）。

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P3** — 「被取代的未接线 service 垫片」。双层冗余：(a) service 层 `ready_queue.py` 是纯转手桥，零生产消费者（见 §8）；(b) 它转手的底层 `get_ready_operation_ids` 在 LIVE 图 SGS 路径上**根本没被调**——`sgs_graph._prepare_graph_ready_state`（sgs_graph.py:33）走自己的 `_initialize_graph_ready_frontier`（:53 区）构造 ready frontier，全程不碰 `get_ready_operation_ids`（rg 实证：sgs_graph.py 内无 `get_ready_operation_ids` 字样）。故底层 impl 是「并行实现」，service 垫片是「桥到并行实现的桥」。
- **严重度 medium** — 不影响排产产出（downstream_effect=无），仅多一层 service 兼容面 + 一份未接线算法实现，属可维护性/失忆债，非正确性债。
- **桶 B09**（home B09）；**簇 C01**；partition `scheduler-config-summary-graph`；kind=real。
- 失忆点（current_evidence）：networkx roadmap 只有 PR-5 原设计文把 shim 描述为「有意兼容导出」，**缺**「已被 sgs_graph 取代、shim 应一并下线」的更新备忘——这正是 P3 的「失忆」成因，planned_fix_hint ③ 要补这句。

## 4) 修法类

**修法类 = 直删 + 退测试枚举 + roadmap 备忘**（承重 false，无 lb_no_touch，可删）。owner_pending=false，给终态修法。

R25 只负责 **service 垫片这一层**（底层 impl 的删除属 R52）。三步（与 R52 **同一提交单元**）：

1. **删整文件** `core/services/scheduler/graph/ready_queue.py`（11 行垫片）。
2. **退两处懒加载/拓扑测试枚举**：从 `tests/regression_scheduler_graph_lazy_runtime_contract.py:27` 与 `tests/scheduler_graph/test_metrics_topology.py:140` 的模块名字面量数组里摘掉 `"core.services.scheduler.graph.ready_queue"`（两处都是单行字面量，删行即可）。
3. **补 roadmap 备忘**：networkx items.yaml PR-5 notes 区补一句「ready_queue helper 已被 sgs_graph._prepare_graph_ready_state 取代、service 层兼容 shim 一并下线」（治 §3 失忆点）。注意路径是**算法层** sgs_graph，备忘措辞别误标到 service 目录。

**灵魂线合规**：纯删除 + 删测试断言行 + 文档备忘，不新增任何兜底/静默回退/except 吞错/过度防御，无形参改动。无 P5 收口动作（这是删，不是收口），故不涉及「收口到已存在点」约束。

## 5) 同文件碰撞债（siblings）

`same_file_siblings = [R52]`。R25 与 R52 是**同一逻辑的 shell + body**，必须协调：

- **R52（同 symbol `get_ready_operation_ids`、底层 impl）**：R52 处理 `core/algorithms/greedy/dispatch/ready_queue.py:103-138` 的真实实现（被 sgs_graph 取代的并行实现本体）。R25 处理 service 层垫片 + 它转手的导出面。
  - **改哪段**：R25 删 `core/services/scheduler/graph/ready_queue.py` 整文件 + 退两处枚举；R52 删/降级算法层 `ready_queue.py` 本体 + 迁 `tests/scheduler_graph/test_ready_queue.py`（该测试直接调 :31/:80/:193/:210/:221/:239 处的 `get_ready_operation_ids`）。
  - **谁先谁后**：**R52 的 test 迁移必须先于/同步于 R25 删垫片**。test_ready_queue.py:16 经 service 垫片 import；若先删垫片而 test 未迁/未删，:16 import 立即变红。registry planned_deps_hint 明列此隐含前置。
  - **撞行号/dict 键位移**：**不撞**。R25 删的是独立文件（整删，无行号交错）；R25 退的两处枚举是 service 层测试文件，R52 动的是算法层文件与 test_ready_queue.py，物理不重叠。lazy_runtime 枚举数组是 `for` 迭代、非 index/count 断言（实证 :24 `for module_name in modules` + :36 仅断言 `networkx_loaded==False`），删一行不引发位移。
  - **结论**：R25 与 R52 **同一提交单元**（co_change），原子提交，避免中间态红测试。
- 其余 interference_edges（R02@schedule_graph_dispatch_context.py、R50@sgs.py）是 **same_file=true 但 same_symbol=false**，且这两文件 **R25 完全不动**（R25 不 import schedule_graph_dispatch_context，不动 sgs.py）。故 R02/R50 与 R25 **无真实碰撞**，仅簇内 C01 共现，调度时不需与本条争行号。

## 6) 同收口点债

**R25 修法是删除，不是收口**——不向任何统一符号收口，故严格意义上无「同收口点债」。

唯一 symbol 关联是 R52（same_symbol `get_ready_operation_ids`），但二者是删同一概念的两层（shell+body），不是「都收口到某第三点」。顺序敏感性已在 §5 锁定：**R52 test 迁移 → 删两层**，原子同提交。无其他债收口到 `get_ready_operation_ids` 或 `core.services.scheduler.graph.ready_queue`（rg + callgraph 实证：该 service 路径零 import 边）。

## 7) 收口行为差异及 parity 测试

**不收口 → 无新旧两路 parity 问题。** R25 是删一层纯 re-export 垫片，不合并/不统一/不透传两套行为，无边界值分支差异（如 None vs raise）需对照。

唯一需「行为不变」证明的是：删 service 垫片后，凡仍需 `get_ready_operation_ids`/`ReadyQueueContractError` 的消费者（仅 test_ready_queue.py，且属 R52 迁移范围）应改为直接 import 算法层 `core.algorithms.greedy.dispatch.ready_queue`——**同一符号、同一对象**，无行为差异（垫片 :9 本就是直转算法层）。故无需新增 parity 测试；R52 迁移后 test_ready_queue.py 原 6 处调用（:31/:80/:193/:210/:221/:239）的断言全部沿用即为等价证明。

## 8) 爆炸半径

**生产消费者：零（已 grep + callgraph 双证实）。**
- rg `scheduler.graph.ready_queue` 于 core/ web/ data/（排除垫片自身）→ **空**。无任何生产文件 import 该 service 垫片。
- LIVE 图 SGS 走 `sgs.py:201 _prepare_graph_ready_state`（算法层），`schedule_graph_dispatch_context.py` 不 import ready_queue。
- callgraph `edges.json` 中 `scheduler.graph.ready_queue` 边数 = **0**（grep -c 实证）；`ready_queue.py::get_ready_operation_ids` 等条目均指**算法层**文件，与 service 垫片无入边。

**测试消费者（3 处，全部已定位）：**
- `tests/scheduler_graph/test_ready_queue.py:16`（**经垫片** import，删垫片→红，属 R52 迁移范围）。
- `tests/regression_scheduler_graph_lazy_runtime_contract.py:27`（模块名枚举，删垫片必摘名）。
- `tests/scheduler_graph/test_metrics_topology.py:140`（同上）。

**灾难链（删错/漏改静默炸什么）：**
- 漏 §4 步骤②任一枚举 → lazy_runtime/metrics_topology 在 `importlib.import_module("core.services.scheduler.graph.ready_queue")` 处抛 ModuleNotFoundError，测试**红**（loud，非静默，可立即发现）——不是静默炸，属安全失败。
- 删垫片但 R52 未迁 test_ready_queue.py → :16 import 红（loud）。
- **无排产产出影响**：SGS 不调此垫片也不调底层 helper，删除不改变任何 schedule 结果（downstream_effect=无）。fix_invalidation_risk=none（属 B13 facade 删除，但此垫片本身就是 B09 产物，须晚于 B09 收敛——见 §12）。
- 唯一「静默」风险点：若**误把算法层** `core.algorithms.greedy.dispatch.ready_queue`（R52 本体/真实 impl）当成 service 垫片删，会连带删掉 R52 范围、且可能波及其他对算法层的引用——故 §4 严格限定 R25 只删 service 路径文件，算法层删除交 R52。

## 9) 承重毗邻标记

- R25 自身 `load_bearing=false`，`lb_no_touch=null`。
- phase1_blast `lb_colocation_danger=none`：同文件/同提交单元（R25+R52）**无任何承重债毗邻**。
- rg 实证：service 垫片与算法层 ready_queue.py **均无** `我是故意的`/`load_bearing`/`承重`/`intentional` 标记。
- **无禁区行**——可整文件删除，无需「补故意注释 + 绑契约」式承重保护。

## 10) 分层 / 导入环风险

- **删除只移除一条 import 边，绝不可能新增越层 import 或导入环。**
- 现状边：service 垫片 `:9` 从 `core.services.scheduler.graph` → `core.algorithms.greedy.dispatch`（service→algorithms 是**合法向下**方向）。删掉它，该边消失，分层只会更干净。
- R25 不引入任何新 import（纯删 + 删测试行 + 改 yaml 备忘），故 **0 AST 违规**，不触碰分层红线（无 core.algorithms→core.services、无 core.models→core.services）。
- 导入环风险：none——删除不可能制造环。

## 11) 测试耦合（动手前先迁哪些测试）

动手删垫片**之前/同步**必须处理 3 处测试：

1. `tests/scheduler_graph/test_ready_queue.py:16`——把 import 从 `core.services.scheduler.graph.ready_queue` 改指算法层 `core.algorithms.greedy.dispatch.ready_queue`（或随 R52 一并删/迁）。这是**唯一行为性测试**（:31/:80/:193/:210/:221/:239 六处实调），归属 R52 迁移决定其去留；R25 不得在它未迁时先删垫片。
2. `tests/regression_scheduler_graph_lazy_runtime_contract.py:27`——删字面量行。该数组是 `for` 迭代（:24），断言仅 `networkx_loaded==False`（:36），删一行不影响其余断言、无 count/index 依赖。
3. `tests/scheduler_graph/test_metrics_topology.py:140`——删字面量行（同性质）。

迁移顺序铁律：**测试先就绪 → 再删垫片**，否则任一处 import/枚举变红（loud failure，可发现，非静默）。

## 12) 前置依赖（先做哪些债 / 哪些 F 门）

- **co_change 强约束：R52（同 symbol、底层 impl）** —— R25（shell）与 R52（body）**必须同一提交删**。R52 内含 `tests/scheduler_graph/test_ready_queue.py` 的测试迁移，该迁移**必须先于/同步于** R25 删垫片，否则 :16 import 红。
- **批次提示**：registry planned_batch_hint = **Batch-10（PHASE1 §4）**，与 R52 同提交单元，须**晚于 test 迁移**。
- **阶段门**：fix_invalidation_risk 注「属 B13 facade 删除，须**晚于 B09 收敛**」——但此垫片本身即 B09 产物，故本条在 B09 桶内收尾即可，无需等独立 B13 阶段；调度时确保 B09 簇（含 C01 相关收敛）已稳定。
- **无 load_bearing 前置**（lb_colocation_danger=none），无 adv_precondition（=null），无需先证伪某假设。
- 簇内 C01 邻债 R02/R50 与本条无文件/符号碰撞（§5），**不构成前置**，可并行调度。

## 对抗核验（第二双眼睛）

核验结论：**PASS（带 1 处次要措辞瑕疵，非阻断）**。独立重 grep/callgraph 回盘，主动找反例，未推翻档案任何修法结论。

**重 grep 命令与结果：**
- `nl -ba core/services/scheduler/graph/ready_queue.py; wc -l` → 实测 **11 行**纯 re-export，`:9` import / `:11` `__all__`，docstring 自述桥意图。与档案 §1 逐字吻合，**行号零漂移**。
- `rg -n "def get_ready_operation_ids|__all__|class ReadyQueueContractError" core/algorithms/greedy/dispatch/ready_queue.py` → `class:10` / `def get_ready_operation_ids:103` / `__all__:138`。与档案/registry 一致。
- `rg -n "_prepare_graph_ready_state|from .sgs_graph|get_ready_operation_ids" core/algorithms/greedy/dispatch/sgs.py` → `:16 from .sgs_graph import (` / `:23 _prepare_graph_ready_state` / `:201` 调用。`sgs_graph.py:33 def _prepare_graph_ready_state`。LIVE 路径行号全准。
- `rg -n "get_ready_operation_ids" core/algorithms/greedy/dispatch/sgs_graph.py` → **空**。证实 sgs_graph 不碰底层 helper，「并行实现/未接线」判断成立。
- `rg -n "services\.scheduler\.graph\.ready_queue" core/ web/ data/`（排除垫片自身）→ **空**。**生产零消费者证实**。
- callgraph `callgraph_fresh/edges.json`：`ready_queue` 相关 11 条边**全是算法层文件内部 local 边 + 2 条 ambiguous name 误匹配（出边）**，无任何指向 service 垫片 `core.services.scheduler.graph.ready_queue` 的入边。印证 §8「service 路径零 import 边」。
- 三处测试枚举行号 `test_ready_queue.py:16` / `lazy_runtime:27` / `metrics_topology:140` 全部命中，**零漂移**。test_ready_queue.py 实调点 `:31/:80/:193/:210/:221/:239` 复核命中（另有 :159-266 多处 ReadyQueueContractError 断言）。
- 承重标记 `rg "我是故意的|load_bearing|承重|intentional"` 于两文件 → **空**。lb=false / lb_no_touch=null **证实**，无禁区行，可整删。

**承重误删风险：无。** lb=false 且无任何承重标记；档案修法为纯删 + 删测试行 + yaml 备忘，无统一/透传/加形参/兜底，灵魂线合规。分层方向 service→algorithms 为合法向下，删除只减一条边，0 AST 违规。owner_pending=false，给终态修法合规。

**1 处次要措辞瑕疵（不阻断、不改修法）：** 档案 §8 第一条灾难链把 `metrics_topology:140` 与 `lazy_runtime:27` 并列描述为「漏摘任一→`importlib.import_module` 抛 ModuleNotFoundError 变红」。实测两者失败模式**不同**：
- `lazy_runtime:27` 处于 `:18 modules=[...]` + `:33 for module_name in modules: import_module(module_name)`，删垫片后漏摘 → **确实 loud 红**（档案对）。
- `metrics_topology:140` 处于 `:135 forbidden_modules=[...]`，该测试**只 pop sys.modules + `import_module("...metrics")` 自身**，对 forbidden 列表项**不做 import_module**（rg 实证 `:54/:148` 仅 import metrics）。删垫片后漏摘 → **不会变红**，只留一个悬空模块名（断言更宽松，无害静默残留）。
两处**都应摘名**（§4 步骤②正确），只是失败模式一为 loud、一为静默残留——修法动作与正确性结论均不受影响，仅 §8 灾难链表述需将 metrics_topology 一支由「ModuleNotFoundError 红」更正为「悬空残留、不红」。

**phase1_blast 路径纠错复核：** 档案 §1/§3 已正确钉死 sgs.py/sgs_graph.py 在 `core/algorithms/greedy/dispatch/`（算法层）而非 registry phase1_blast 误标的 service 目录——重 grep 证实文件确在算法层，档案纠错成立。

**需升级第三方裁：n**（医级措辞瑕疵已在本节就地更正，修法结论无争议）。
