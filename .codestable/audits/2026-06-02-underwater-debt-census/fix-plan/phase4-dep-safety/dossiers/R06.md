# R06 逐债档案 — 空包 core/services/scheduler/dispatch/ 零引用残渣

> 本轮只读不改、只产计划。所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。

## 1) 当前真实 file:line(回盘)+ 漂移记录

R06 不是单点符号，而是「一个空包目录 + 两处测试守卫站点」三处真实证据，按符号/路径逐处回盘：

| 实体 | old_location_DONT_TRUST | 回盘真实位置(2026-06-05) | 漂移 |
|---|---|---|---|
| 空包本体 | `core/services/scheduler/dispatch/__init__.py` | `core/services/scheduler/dispatch/__init__.py` 实测 **0 字节**，目录内仅此一个文件，无兄弟模块(`find` 证实) | 无 |
| SP05 存在性元组 | SP05:310 | `tests/test_sp05_path_topology_contract.py:310`，元组 `("config","run","summary","batch","dispatch","gantt","calendar")`，:311-313 断言每个 `package_dir.is_dir()` 且 `__init__.py is_file()` | **无漂移**(:310 命中) |
| SP05 delayed 循环 | SP05:315-316 | `tests/test_sp05_path_topology_contract.py:315` 循环 `for delayed_package in ("batch","dispatch","gantt","calendar"):`，:316 调 `_assert_init_has_no_imports(REPO_ROOT / "core/services/scheduler" / delayed_package / "__init__.py")` | **无漂移**(:315-316 命中) |
| `_assert_init_has_no_imports` 定义 | (未给) | `tests/test_sp05_path_topology_contract.py:173-176`，AST 解析断言 `imports == []` | n/a |

证据：`wc -c core/services/scheduler/dispatch/__init__.py` = 0；`Read` SP05:309-316 与 :173-176 原文。**registry 行号线索本次 100% 命中，零漂移**。

## 2) 当前状态 + 与 status evidence 一致性

- registry 状态：`status_2026_06_05 = planned`。
- 回盘核对：空包未删(0 字节仍在)、SP05:310 元组仍含 batch/dispatch/gantt/calendar、:315-316 循环仍枚举四包——与 `current_evidence_2026_06_05` 描述**逐字一致**。
- 结论：状态 `planned` 与现状一致，**无矛盾**。`location_status` 标 ✅准 亦核实(目录无兄弟模块、唯一来源提交 `24f4c93e "Implement SP05 path topology guardrails"`，`git log -- dispatch/` 证实)。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P3**：无关提交(图拓扑护栏 24f4c93e)误建的空包脚手架，命名撞「dispatch 派工域」却**零内容、零引用**，制造「scheduler 还有 dispatch 子包」的假象。注意：真正的派工实现在 `resource_dispatch*` 路由与 `greedy/dispatch`、`schedule_graph_dispatch` 等处，本空包与之无关。
- 严重度 **low**；桶/home **B09**(scheduler-dispatch partition)；簇 **ISOLATED**(`interference_edges = []`)。
- 承重 `load_bearing=false`，`lb_no_touch=null`，`owner_pending=false`。
## 4) 修法类

**直删**(`planned_fix_class = 直删`)。承重=false、零引用、零运行期影响，符合直删前提。`owner_pending=false`，给终态修法：

1. 删除目录 `core/services/scheduler/dispatch/`(连同 0 字节 `__init__.py`，目录内别无他物，`find` 证实)。
2. 改 `tests/test_sp05_path_topology_contract.py:310` 存在性元组：从 `("config","run","summary","batch","dispatch","gantt","calendar")` 摘掉 `dispatch`(R06 仅负责 dispatch；batch/calendar 由 R27 摘、gantt 随同提交一并摘)。**四包全摘后该元组留 `("config","run","summary")`**。
3. 改 `:315-316` delayed 循环：从 `("batch","dispatch","gantt","calendar")` 摘掉 `dispatch`；四包全摘后整个循环块无遍历对象，**删整块**(:315-316)比留空元组更干净(R27 fix_hint 同此判断)。

注意：R06 的修法**绝不能单独落**——SP05:310/315 是「断言四包必须存在」的守卫，单删 dispatch 目录而不改测试 → SP05 `package_dir.is_dir()` 立即红。这是「删生产必同步改契约测试」的硬绑定(非违反只读：测试改在 phase2 一并落，本档案只规划)。

## 5) 同文件碰撞债(siblings)

`same_file_siblings = ["R27"]`，但**这是 basename 撞名、非物理同文件**——已核实：

- R06 主文件 = `core/services/scheduler/dispatch/__init__.py`；
- R27 主文件 = `core/services/scheduler/calendar/__init__.py` + `core/services/scheduler/batch/__init__.py`(R27 title：「空 delayed 包 calendar/ 与 batch/」)。
- 三者 basename 均为 `__init__.py`，故 registry 标了 same_file_siblings，但它们是**不同目录下的不同物理文件**，删 dispatch/ 与删 calendar/、batch/ **互不撞行号**(各自独立目录，删目录不引起对方文件偏移)。
- 真正的耦合在 §6(同收口点)：两条债**改的是同一份 SP05 测试的同两行**(:310 元组、:315 循环)，dict/元组键位移风险落在那里，不在生产文件。
- 协调结论：R06 摘 `dispatch`、R27 摘 `batch`+`calendar`，gantt 无独立 id 须由二者之一带走 → **必须同一提交**，否则任一先落都会让另一条 sibling 面对一个「四包元组已被改成三元组/二元组」的中间态，再改时撞键。

## 6) 同收口点债 + 顺序敏感性

收口点(共享改写站点) = `tests/test_sp05_path_topology_contract.py` 的 **:310 存在性元组** 与 **:315 delayed 循环元组**。收口到此两站点的债：

- **R06**(本条)：从两元组摘 `dispatch`。
- **R27**：从两元组摘 `batch`、`calendar`。
- **gantt 空包**(无独立 id)：从两元组摘 `gantt`，由 R06/R27 同提交带走(V22/SP05 守卫扫全 4 包，漏一个则该包目录删了但测试仍断言其存在 → 红)。

**顺序敏感性：高，但只有一个安全顺序——「四包同一原子提交」**。理由：:310 元组与 :315 循环是「列举型断言」，逐条增量摘会产生中间态(如先摘 dispatch 留 `("config","run","summary","batch","gantt","calendar")` + 目录已删)，此时若 R27 后续 patch 仍按「七元组/四元组」做 old_string 匹配会失配或误删。最干净：**R06+R27+gantt 三方一次性把 :310 改成 `("config","run","summary")`、:315-316 整块删除**，无中间态、无键位移歧义。这正是 `planned_deps_hint` 所述「PHASE1 §4 已把 R27 拉进 Batch-10 与本桶同落，Phase 3 排序必须把 R06↔R27 串进同一批次/同一提交」。
## 7) 收口行为差异 + parity 测试

**本条是直删，不是「新旧两路收口」**，故无运行期行为分支需做 parity。唯一的「行为差异」在测试守卫语义层面：

- 旧守卫语义：SP05 断言「scheduler 下有 batch/dispatch/gantt/calendar 四个空 delayed 包，且其 `__init__.py` 无 import」(:310 存在 + :315 无导入)。
- 新语义：删除该断言，scheduler 下不再要求这四个空包存在。

需补的不是 parity 测试，而是**确保 SP05 改后仍能跑过且语义自洽**：
- 改后 :310 元组应只剩 `("config","run","summary")`——这三个是真实非空业务包(须保留断言保护)。须验证这三包确实存在且为目录(改 patch 时勿误删 config/run/summary)。
- :315-316 整块删除后，文件无悬空引用 `_assert_init_has_no_imports`(该函数 :173 在 :409 web 域循环仍被调用，**不可连函数定义一起删**——这是删块时的禁区，见 §11)。
- 反例守卫：若有人误把 :310 改成空元组 `()`，循环 `for ... in ():` 不报错但断言全失效，静默放过任何回归 → patch review 须确认留下的是 `("config","run","summary")` 而非空。

## 8) 爆炸半径(phase1_blast 九字段核对)

- **生产消费者**：`grep scheduler.dispatch / scheduler/dispatch`(排除 resource_dispatch、greedy/dispatch、schedule_graph_dispatch、dispatch_execution、dispatch_lane)= **0 命中**，已在 core/ web/ data/ 全域 rg 证实。零 import、零运行期影响(`downstream_effect=无运行期影响`)。
- **测试消费者**：仅 `tests/test_sp05_path_topology_contract.py:310`(存在性元组)、`:315-316`(无导入循环)两站点。无其他测试触及该空包。
- **fix_invalidation_risk = none**：删空包不会让任何其他债的修法失效(ISOLATED 簇，无 interference_edges)。
- **灾难链**：
  1. *删错/漏改链*：删 dispatch/ 目录但漏改 SP05:310 → `package_dir.is_dir()` 断言红(**fail loud，非静默**，安全)。
  2. *只改测试不删目录链*：改 SP05 但忘删目录 → 无害(目录变成无人断言的死空包，回到 P3 原状，不炸)。
  3. *误删 config/run/summary 链*：patch :310 元组时若手滑删掉真实业务包名 → 这三包失去存在性保护，但因它们仍被实际 import，运行期会立即 ImportError(fail loud)。
  4. *最危险——误删 `_assert_init_has_no_imports` 函数定义链*：删 :315-316 循环块时若连 :173-176 函数定义一起删 → :409 web 域 `for empty_domain` 循环调用该函数处 NameError，SP05 整文件崩。**这是本条唯一「删块」型隐患，须圈定只删 :315-316 两行，保留 :173 定义**。
- 总评：爆炸半径极小，所有失败路径均 fail loud，无静默炸点。

## 9) 承重毗邻标记

- `lb_colocation_danger = none`。`load_bearing=false`，`lb_no_touch=null`。
- 物理毗邻：`dispatch/` 目录孤立(无兄弟模块)；SP05 测试文件中 :310/:315 周边无承重符号(均为护栏断言)。
- **禁区行(本条无承重，但列出删块时不可越界的行)**：
  - `tests/test_sp05_path_topology_contract.py:173-176` `_assert_init_has_no_imports` 定义——**不可删**(:409 仍调用)。
  - SP05:318-326 `SERVICE_ROOTS_WITHOUT_COMPAT` / `SERVICE_STRONG_COMPAT_MODULES` 断言段——与本条无关，**不可顺手动**。
  - :310 元组中的 `"config","run","summary"`——真实业务包，**不可摘**，只摘 dispatch(及 R27/gantt 的 batch/calendar/gantt)。
## 10) 分层 / 导入环风险

- **零风险**。本条是「删空包 + 删测试断言」，不新增任何 import，不可能造越层(core.algorithms→core.services / core.models→core.services)或导入环。
- 反向核对：被删的 `dispatch/__init__.py` 本身 0 字节、无 import；SP05 改动只删元组项与循环块，不引入新模块引用。
- AST 违规计数：本条贡献 **0**。

## 11) 测试耦合(动手前先迁/先核哪些测试)

- 唯一耦合测试：`tests/test_sp05_path_topology_contract.py`，函数 `test_sp05_service_topology_and_strong_compatibility`(:309 起)。删生产空包**必须同提交**改其 :310 元组 + 删 :315-316 循环块，否则该测试红。
- **共享函数保护(已核实，禁区)**：`_assert_init_has_no_imports`(:173-176)同时被 `:316`(本条要删的 delayed 循环)与 `:409`(`test_sp05_route_topology_and_compatibility_matrix` 的 web 域循环，针对 `web/routes/domains/{scheduler,process,personnel,equipment,system}/__init__.py`)调用。删 :315-316 时**只删调用语句、保留 :173 定义**，否则 :409 NameError。
- 无需「迁移」测试，只需「同步修改」该一处守卫；无其他测试 import 或引用 `scheduler.dispatch`。

## 12) 前置依赖

- **跨桶硬约束(本桶唯一)**：R06(B09) ↔ R27(B14) 必须**同一提交**落地，且把无独立 id 的 `gantt` 空包一并带走(V22/SP05 守卫扫全 4 包)。`planned_deps_hint` 已声明 PHASE1 §4 将 R27 拉进 **Batch-10** 与本桶同落，**Phase 3 排序必须把 R06↔R27 串进同一批次/同一提交**。
- 顺序：无「先做某债再做本条」的单向依赖；R06 与 R27、gantt **并发原子落**(见 §6 顺序敏感性，只有「四包同提交」一个安全顺序)。
- **F 门**：本条无 owner 裁断需求(`owner_pending=false`)；可执行前提是确认 R27 与本条编入同一 Batch-10 提交清单。无其他前置债。
- owner_pending：**false** — 终态修法已给(直删 + SP05 同提交摘项/删块)。

## 对抗核验(第二双眼睛)

核验人:对抗核验 agent(只读不改)。日期 2026-06-05。结论 **PASS**,无反证。

重 grep/命令与结果:
- `wc -c core/services/scheduler/dispatch/__init__.py` = **0 字节**;`find core/services/scheduler/dispatch -type f` 仅返回该一个文件 → 空包本体核实,目录无兄弟模块。
- `rg -n "scheduler\.dispatch\b|scheduler/dispatch\b" core/ web/ data/ | rg -v "resource_dispatch|greedy|schedule_graph|dispatch_execution|dispatch_lane"` = **0命中**;`from core.services.scheduler.dispatch` = 0命中 → 生产零引用核实(排除项穷尽,无漏网同名)。
- `grep -n` 精确定位(sed 行号会串位,以 grep 为准):元组在 **:310** `("config","run","summary","batch","dispatch","gantt","calendar")`、delayed 循环在 **:315**、`_assert_init_has_no_imports` 调用在 **:316**、定义在 **:173**、web 域复用调用在 **:409**(`for empty_domain in ("scheduler","process","personnel","equipment","system")`)。**registry/档案行号 100% 命中,零漂移。**
- `wc -c` 三兄弟空包:batch/calendar/gantt `__init__.py` 均 **0 字节** → R27(batch+calendar)+gantt(无独立id) 协调对象成立,「四包同提交」约束有现实基础。
- `git log --oneline -- core/services/scheduler/dispatch/` 唯一提交 **24f4c93e "Implement SP05 path topology guardrails"** → P3「无关提交误建」核实。

独立挑战项:
- **承重判断**:load_bearing=false 与 registry 一致;档案修法为直删,**未混入透传/加形参/统一/合并**,符合非承重直删前提,无承重误删风险。
- **owner_pending**:false 与 registry 一致;档案未擅自分配执行批次(Batch-10 系 registry 自带 planned_batch_hint,非档案杜撰),无越权。
- **最危险删块隐患复核**:`_assert_init_has_no_imports`(:173)被 :316 与 :409 双调用,**实测 :409 仍在 web 域循环调用** → 档案「删 :315-316 调用块、保留 :173 定义」的禁区圈定正确;若连定义删则 :409 NameError,档案已标红,核实无误。
- **灵魂线/分层**:本条删空包+删测试断言,不新增 import、不加兜底/静默回退,0 AST 违规 → 核实。
- **收口点真实存在**:SP05:310/:315 两站点为已存在的统一守卫,非新建第二模块 → 核实。

唯一提示(非争议、不降级):§4 步骤②文字说「R06 仅摘 dispatch、batch/calendar 由 R27 摘、gantt 随同提交」,与 §6/§12「四包必须同一原子提交、最干净是一次性改成 ("config","run","summary")」表述一致,无矛盾;执行期严守「四包同提交」单一安全顺序即可。

核验结论:**PASS**。行号一致,承重误删风险=无,无需升级第三方裁断。
