# R35 逐债档案 — schedule_repo.list_between 纯死方法（全仓零引用）

> 簇 C01 · 桶 B12 · 病理 P6 · 严重度 low · 承重 false · owner_pending false
> 同文件兄弟: R34 · 主文件: data/repositories/schedule_repo.py

---

## 1) 当前真实 file:line（回盘）+ 漂移记录

- 符号 `ScheduleRepository.list_between` → **data/repositories/schedule_repo.py:61**（def 行）。
  - 方法体边界：**:61-69**（:61 def，:62 sql，:63 params，:64-66 version 分支，:67 ORDER BY，:68 fetchall，:69 return；:70 空行后即 :71 下一方法 `list_version_rows_by_op_ids_start_range`）。
- **漂移：无。** old_location_DONT_TRUST=`schedule_repo.py:61`，回盘实际同为 :61，幅度 0 行。registry 自评 location_status「✅准，文件不在漂移名单」与回盘一致。
- 全仓 grep 回盘（`rg -n "list_between" -g '*.py' data/ core/ web/ tests/ scripts/` 及全仓 `.`）：**仅 1 处命中 = :61 定义行本身**，零调用、零测试引用。
- callgraph 交叉验证（docs/_panorama_data/callgraph_2026-06-05/edges.json）：`list_between` 仅以 `from`（出边，调 base_repo.fetchall）出现，**无任何 `to:...list_between` 入边**——零调用者，与「纯死方法」一致。
- git 验证：`git diff b08162cd -- schedule_repo.py` 中 list_between 段 **UNCHANGED**（未删，仍在）。

## 2) 当前状态 + 与 status evidence 是否一致

- registry status_2026_06_05 = **planned**。
- 与 current_evidence_2026_06_05（「:61 整方法体 :61-69 仍在；全仓零调用零测试；git diff UNCHANGED 未删」）**完全一致**。本轮回盘逐条坐实，无偏差。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P6 纯死方法**（全仓零引用，含测试）。
- 严重度 **low**：死代码无行为面，不影响生产；唯一成本是阅读噪声 + 与 R34 同文件的行号耦合。
- 桶 **B12**（home=B12）；分区 partition=`data-repos`。
- 簇 **C01**；interference_edges 仅一条 → R34（same_file=true, same_symbol=false）。

## 4) 修法类

- **直删**。删除 schedule_repo.py:61-69 整个 `list_between` 方法（连同 :70 不删，保留方法间空行；实操删 :61-69，:60 末空行与 :70 空行二取一保留以免出现连续双空行）。
- 无任何引用点需同步退场（§8 已证零调用零测试零 callgraph 入边）。
- 不属于收口/raise/parity/注释类：非承重（load_bearing=false，lb_no_touch=null），无收口点（adv_precondition=null），不涉灵魂线（无兜底/except 改动）。
- owner_pending=false → 给终态修法（直删），无需挂起待裁。

## 5) 同文件碰撞债（sibling 协调）

**唯一兄弟：R34（同 schedule_repo.py，硬协调边 R34↔R35）。**

- R34 实质（核 R34 registry 后修正）：标题写「三死方法」，但 fix_hint/evidence 显示这三个方法（`get_version_time_span` :36、`list_overlapping_with_details` :114、`list_dispatch_rows_with_resource_context` :128）是 **plan-role 迁移残渣**，有「活孪生」在 data/repositories/schedule_plan_query_repo.py（:257/:311/:430，带 source_table+candidate_id+scenario_id+未知 source raise），且**仍被测试/类型契约续命**：facade_delegation.py:31/36/39-40 三条 get_type_hints 断言、detail_queries.py 六个用例（:168/:198/:218/:225/:240/:247/:254/:268/:274/:288）、gantt_critical_chain_unavailable.py:59 monkeypatch、benchmark_fjsp.py:503 调 get_version_time_span。R34 要删这三段 **并同步退四件套测试**——比 R35 复杂得多。
- **行号撞位（核心风险）**：R34 删 :36-59 / :114-126 / :128-158 三段。R35 的 `list_between` :61-69 **正好夹在 R34 第一段（止于 :59）与第二段（起于 :114）之间**。
  - 谁先谁后无功能依赖（R35 直删与 R34 删互不引用），但**必须同提交（同 schedule_repo.py 子批）**：
    - 若 R34 先删 :36-59 → list_between 行号从 :61 上移 ~24 行至 ~:37；若 R35 先删 :61-69 → R34 第二段 :114/:128 上移 9 行。任一先落地都让对方的行号定位失效（二次漂移），且 R34 的 facade delegation 契约断言按符号名而非行号定位、相对安全，但 detail_queries 用例与人工 review 易因行号漂移误删错段。
  - **结论：R34 与 R35 同提交，删除前以符号名（非行号）定位每段**，删完一次性跑 tests 验证。
- dict 键位移：本文件无 dict 字面量结构依赖顺序，仅方法定义，删方法不引发 dict 键位移问题。

## 6) 同收口点债

- **无。** R35 是直删，不收口到任何统一符号。
- 需澄清：R35 的 `list_between` 与 R34 三方法不共享收口点——R34 的「活孪生」是 schedule_plan_query_repo 的版本-only 查询，`list_between` 是按 [start,end] 时间窗+可选 version 的区间查询，**语义不同、无孪生、无候选承接者**，故 R35 不进入 R34 的收口/repoint 链，纯退场即可。
- 顺序敏感性：仅来自 §5 的同文件行号耦合，与收口无关。

## 7) 收口行为差异及所需 parity 测试

- **不适用（直删，非收口）。** 无新旧两路对照，无边界值分支等价问题，无需 parity 测试。
- 唯一需做的「验证」是 **删后回归不破**：删除后跑全量 import + 现有 tests，确认无 `AttributeError: list_between` 或类型契约失败（facade_delegation.py 当前不枚举 list_between，见 §8/§11，故无契约断言会断）。

## 8) 爆炸半径

- **生产消费者：0。** rg 全仓 `.py`（data/core/web/scripts）除 :61 定义行外 0 命中；callgraph 0 入边。
- **测试消费者：0。** tests/ 下 0 命中（与 R34 的方法不同——R34 三方法在 detail_queries/facade_delegation/benchmark 有大量测试引用，R35 完全没有）。
- **contract/topology 测试：0。** facade delegation 测试（regression_schedule_service_facade_delegation.py）只对 R34 的三个方法做 get_type_hints 断言，**不枚举 list_between**；SP05 不涉本符号。
- downstream_effect = **零爆炸半径**，可直接删。
- 灾难链分析：
  - 「删错」风险——唯一现实风险是**删到相邻方法**（:59 上方 get_version_time_span 末尾、:71 下方 list_version_rows_by_op_ids_start_range 开头）。若与 R34 同批改且按行号而非符号定位，R34 删段后行号漂移可能误伤 list_between 或反之。→ 缓解：同提交 + 按符号名定位（§5）。
  - 「统一错」风险——不适用（无收口）。
  - 删 list_between 本身不会静默炸任何东西：无运行期引用，删后任何 `.list_between(` 调用本就不存在，不会出现「以为删了死代码其实有动态调用」——已用 grep + callgraph 双证零调用。

## 9) 承重毗邻标记

- 本债 load_bearing=false，lb_no_touch=null。
- **同文件无承重债**：schedule_repo.py 非承重文件（registry lb_colocation_danger=`none(schedule_repo.py 非承重文件)`）；兄弟 R34 亦 load_bearing 非承重（纯删死方法）。
- **禁区行：无。** 整个 schedule_repo.py 无 LB 标记，删 :61-69 不触任何承重禁区。

## 10) 分层 / 导入环风险

- **零风险。** 直删一个方法不新增任何 import，不引入跨层 import（不碰 core.algorithms→core.services、core.models→core.services 等红线），不构成导入环。
- schedule_repo.py 现有 import（:5 `from core.models import Schedule`、:7-9 本包相对导入）均不受删 list_between 影响。

## 11) 测试耦合

- **无需先迁任何测试。** R35 的 list_between 零测试引用（§8），直删前不必动测试。
- 注意：**不要误迁 R34 的测试**——facade_delegation.py / detail_queries.py / gantt_critical_chain_unavailable.py / benchmark_fjsp.py 的引用全部指向 R34 的三方法，与 list_between 无关，属 R34 工作范围。R35 不碰这些测试文件。

## 12) 前置依赖

- **同文件硬绑：与 R34 同提交（同 schedule_repo.py 子批，planned_batch_hint=Batch-13）。** 这不是「先后」依赖而是「同批」约束——避免任一先删致行号二次漂移（§5）。
- **无功能/F 门前置**：R35 本身无依赖、零爆炸半径，可任意排期，仅被 R34 的同文件约束拉到同批。
- registry deps_hint 提到的 R05/B07（Batch-8）前置是挂在 **R34** 上的软约束（R34 的 list_dispatch_rows_with_resource_context 与 collar/team join 相关），**与 R35 的 list_between 无关**；R35 不继承该前置，仅因同文件随 R34 批次走。
- 执行顺序建议：R34 与 R35 在同一 commit 内，删除时**全部按符号名定位**，删完一次性跑 import + tests。

---
## 返回摘要

R35（list_between）= 纯死方法直删，零爆炸半径，唯一约束是与 R34 同 schedule_repo.py 同提交防行号二次漂移（list_between :61-69 夹在 R34 删段 :59 与 :114 之间）。与既有分析无冲突，但澄清两点：R34 实为「带活孪生的迁移残渣+四件套测试退场」而非简单死方法；R35 不共享 R34 收口链、不继承 R05/B07 前置。owner_pending=n。

---

## 对抗核验（第二双眼睛）

> 核验时间 2026-06-05 · 只读不改 · 默认怀疑找反证 · 结论 **PASS**

**重 grep / Read 命令与结果（独立回盘，未信档案行号）：**

1. `rg -n "list_between" -g '*.py' .`（全仓 data/core/web/tests/scripts）→ **仅 1 处命中**：`data/repositories/schedule_repo.py:61` def 行本身。零调用、零测试、零脚本、零 web 引用。
2. `rg -n "\.list_between\s*\(" -g '*.py' .`（调用形态）→ **0 命中**，确认无任何 `.list_between(` 调用点（含动态/方法引用）。
3. `rg -n "def list_between" data/repositories/schedule_repo.py` → **:61**，与档案一致。
4. Read schedule_repo.py:30-128 实读方法体边界：:61 def → :62 sql → :63 params → :64-66 version 分支 → :67 ORDER BY → :68 fetchall → :69 return；:70 空行；:71 即下一方法 `list_version_rows_by_op_ids_start_range`。**方法体 :61-69 与档案 §1 逐行吻合**。

**逐项挑战结论：**

- **真实行号**：一致（:61，漂移 0，与 old_location 同值）。无二次漂移。
- **调用者/消费者清单**：一致——生产 0 / 测试 0 / contract-topology 0，独立 grep 双形态（裸名+调用形态）均坐实零引用，直删成立。
- **承重判断**：确认非承重（load_bearing=false, lb_no_touch=null）。修法为「直删」死方法，**未混入删除护栏/统一/透传/加形参**——这是真死叶子，非「看着像该统一」的护栏误删。承重误删风险=**无**。
- **owner_pending**：false。档案给终态修法（直删）+批次（Batch-13 随 R34）合规，未对挂起债擅自定终态。
- **灵魂线**：直删一方法，不新增兜底/静默回退/except 吞错，不涉 P4 raise，无违规。
- **分层/导入环**：删方法不新增任何 import，不碰跨层红线，零风险（实读 import 区未受影响）。
- **同文件协调（R34↔R35）**：独立确认 list_between :61-69 上接 :59（`get_version_time_span` 末尾 `return {...}`）、下接 :71（另一方法 def），确实夹在 R34 删段 :36-59 与 :114 之间——档案「同提交+按符号名定位防行号二次漂移」的硬约束判断准确。
- **收口点**：档案声明「无收口（纯直删）」属实，list_between（时间窗+可选 version 区间查询）与 R34 三方法（版本-only 查询，活孪生在 schedule_plan_query_repo）语义不同、无承接者，不进 R34 收口链——核实无误。

**未发现任何反证。** 结论 PASS，无争议点，无需第三方裁断。
