# R12 逐债档案 · 关键链 _build_nodes 静默丢坏时间行无 partial/缺口信号

> 本档案只读不改、只产计划。所有断言附当前工作区 file:line 证据(2026-06-05 回盘)。

## 1) 当前真实 file:line(grep 回盘)+ 漂移记录

按符号名独立 rg 回盘（**不照抄 old_location**），主病点 + 全调用链：

| 符号 | old_location_DONT_TRUST | 回盘真实 file:line | 漂移 |
|---|---|---|---|
| 丢行过滤 `if not st or not et or not (st < et): continue` | gantt_critical_chain.py:84 | `core/services/scheduler/gantt_critical_chain.py:84`（在 `_build_nodes` 内） | **无**（0 行） |
| `_build_nodes` | — | `gantt_critical_chain.py:79` | 与 evidence :79 一致 |
| `_build_process_prev`（坏行删后受害排序） | — | `gantt_critical_chain.py:103`，排序在 `:114` | evidence 说 :114 ✅ |
| `_sink_id`（`max(...)`） | — | `gantt_critical_chain.py:261`，`max` 在 `:262` | evidence 说 :262 ✅ |
| `_compute_critical_chain_from_loaded_rows`（注入点） | — | `gantt_critical_chain.py:312`，return dict 在 `:328-334` | evidence 说 :312-334 ✅ |
| `compute_critical_chain_from_rows`（出口 A） | — | `gantt_critical_chain.py:337`，try/except 在 `:338-341` | evidence 说 :337 ✅ |
| `compute_critical_chain`（出口 B，role==adopted） | — | `gantt_critical_chain.py:344`，try/except 在 `:352-359` | evidence 说 :344 ✅ |

**结论：本债 file:line 几乎零漂移**，registry `location_status: ✅准` 成立。全文件 `rg "partial|dropped|skipped|degraded|bad_time"` → **NONE_FOUND**，信号缺口属实。

## 2) 当前状态 + 与 status evidence 一致性

- `status_2026_06_05 = planned`，`verdict.refuted = true`（已对抗核验为真债）。
- evidence「:84 仍是裸 continue，全文件无可观测键，计划留痕未落地」与回盘**完全一致**：:84 确为裸 `continue`，无任何 dropped/partial 键。
- 状态一致 ✅，无矛盾。本债尚未动工（planned）。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P4（灵魂线缺口）**：`_build_nodes` 在 :84 静默剔除「时间字段缺失/start≥end」的坏行，但结果 dict（:328-334）不含任何降级标记，经归一层 `available` 默认 `True`（provider :120 `is_available = True`）。
- 危害链：坏行被悄悄剔除 → 真正瓶颈工序可能被排除出关键链 → 链路结论被污染，**用户看到 available:True 以为完整**。这是「静默丢数据却报成功」的典型灵魂线违反。
- 严重度 **low**：因丢行分支近死（见 §8，Schedule 表 NOT NULL）；但灵魂线合规为**必做**（低投入）。
- 桶 **B08**，簇 **C01**，分区 scheduler-gantt，承重 **false**。

## 4) 修法类

**修法类 = 补可观测降级标记（P4 正确修法）**。`owner_pending=false`，可给终态修法。

P4 灵魂线铁律：**严禁加兜底/静默回退**，正确修法是「补 loud 信号或可观测降级标记」。本债选**可观测降级标记**（坏行可韧性容忍，不必 raise 整链）。具体：

1. **保留 :84 过滤本身**（这是「丢一行坏数据仍出有用链」的刻意韧性，删它=过度激进，见 §8 灾难链）。
2. **照搬兄弟范式**：`gantt_tasks.py:192-196` 的 `DegradationCollector` + `_record_bad_time_row(collector, scope=..., row=...)`（helper 在 `_sched_display_utils.py:84-92`，code=`bad_time_row_skipped`）。在 `_build_nodes`(:79-100) 线程化一个 collector，:84 命中时记一笔。
3. **注入结果**：`_compute_critical_chain_from_loaded_rows`(:312-334) 在 return dict 加 `dropped_count: int` + `critical_chain_partial: bool(=dropped_count>0)`。
4. **【关键】穿透三道白名单**，否则在归一层被静默剥离 = 换地方自欺：
   - `_normalize_critical_chain_result`（provider :112-137，**显式白名单 dict，当前仅 11 键**）；
   - `gantt_service_support.py:32-51`（出口）；
   - `gantt_contract.py:19-40`（契约→JS）。

> 注：scope 名应取关键链专属（如 `gantt.critical_chain`），勿与 gantt_tasks 的 `gantt.tasks` 混；二者写入同一 `bad_time_row_skipped` code 但 collector 实例不同（见 §6）。

**禁区行（绝不动）**：
- :84 `if not st or not et or not (st < et): continue` —— 删/改条件即破坏韧性，触发 §8 TypeError 灾难链。
- :114 排序、:262 `max` —— 删 :84 后 None 流入这两处即 TypeError。
- :338-341 / :352-359 try/except —— 是 P4 灵魂线**单独债（R55 等）**的领地，本债不碰（详见 §6 顺序敏感）。

## 5) 同文件碰撞债

`same_file_siblings = []` —— **gantt_critical_chain.py 内本债无同文件兄弟**。`interference_edges` 中标 `same_file:true` 的 R10/R11/R21/R34/R63 全部 `same_symbol:false` 且其 why 指向 **gantt_service.py**（不是本债所在的 gantt_critical_chain.py），属「同分区不同文件」噪声边，不会撞行号/dict 键位移。

唯一与本债**真实共址**的是 **R55**（其 why 同时含 `gantt_critical_chain.py` 与 `gantt_service.py`）：
- R55 是 gantt_critical_chain.py 内 try/except 静默吞错的 P4 债（领地为 :338-341 / :352-359 出口 try/except）；本债领地为 :79-100/:312-334 注入点 + 三白名单。**两者文件内行段不重叠**，但**共用同一出口归一与三道白名单**（见 §6）。
- 撞行号风险：低。本债在 :84/:328-334 加键，R55 在 :338-359 改 except 行为，行段隔开 >300 行；唯一交汇是 result dict 与 normalize 白名单的**键集合**——需协调键名不冲突（dropped_count/critical_chain_partial vs reason_code）。
- 顺序：**宜同批**（registry「与 R55 同批」），见 §6/§12。

## 6) 同收口点债（收口到同一符号 + 顺序敏感性）

本债不新建收口点，而是**注入到既有出口归一 + 三道白名单**。收到同一组白名单/归一的其他债：

- **R55（P4，gantt_critical_chain.py + gantt_service.py）**：改出口 try/except 的静默吞错。与本债共用 `_normalize_critical_chain_result`(provider :112-137) 与三白名单。**顺序敏感**：若 R55 把 `:341/:355/:359` 的 `_unavailable_result(...)` 改为带 reason_code 的 loud 路径、同时调整 normalize，本债的新键须与 R55 调整后的 dict 形状对齐；**建议 R55 先定 normalize 形状，本债再往里加 2 键**，或二者一次性同 PR 改 normalize（推荐，避免两轮改同函数）。
- **R11/R63（B 类重复债）**：registry 明示「两份 `_normalize` 须先统一为单份」——回盘确认 `_normalize_critical_chain_result` 确在 **provider(:112) 与 gantt_service_support.py(:32-51 段) 两处**重复存在。**强顺序前置**：若 R11/R63 未先合并，本债加 `dropped_count` 要改两份副本 = 复活 R11/R63。
- 顺序结论：**R11/R63（统一 normalize）→ 本债 + R55（同批往单份 normalize 加键/改吞错）**。

## 7) 收口行为差异 + parity 测试

本债**不是收口合并两路逻辑**，而是「新增可观测键」，因此无「新旧两路是否逐分支等价」的传统 parity 风险。但有**白名单穿透的等价性**要测：

- **核心反例（必测）**：构造 rows 含 N 条坏时间行（start_time=None / end_time<start_time）+ M 条好行。
  - 旧行为：`dropped_count` 不存在，`available=True`，用户无感。
  - 新行为：`dropped_count==N`，`critical_chain_partial==True`，`available` 仍可为 True（链路仍出，只是标 partial）。
  - **parity 断言**：新键存在**不改变** `ids`/`edges`/`makespan_end`/`available` 的既有取值（保留 :84 过滤 ⇒ 链路计算完全不变，只多两个旁路标记）。这是「补可观测不改行为」的核心证明。
- **白名单穿透等价**：同一结果分别经 provider :104(`_copy`) / :112(`_normalize`) / support:32-51 / contract:19-40 后，`dropped_count`/`critical_chain_partial` **均不被剥离**（当前 normalize 是显式白名单，不加键则被吞 → 必须断言穿透）。
- **边界**：全坏行（nodes 为空）走 :314 `_empty_result()` 早返回 —— 需确认 `dropped_count` 在空 nodes 路径也被计入（:84 在 :313 `_build_nodes` 内，空 nodes 时 dropped_count 应=坏行数，但当前 :315 `_empty_result()` 不带该键 ⇒ **修法须让 _empty_result 也带 dropped_count**，否则「全是坏行」这一最该报警的场景反而无信号，自相矛盾）。这是修法时最易漏的反例。

## 7) 收口行为差异 + parity 测试
（待填）

## 8) 爆炸半径（9 字段）

**生产消费者清单**（回盘确认）：
- 主点 `gantt_critical_chain.py:84` 在 `_build_nodes`(:79) → 经 `_compute_critical_chain_from_loaded_rows`(:312)。
- 出口 A `compute_critical_chain_from_rows`(:337)：被 `gantt_service_support.py:57` + `gantt_critical_chain_provider.py:179` 调用。
- 出口 B `compute_critical_chain`(:344)：被 `gantt_critical_chain_provider.py:164`（role==adopted）调用。
- 三道白名单：`gantt_service_support.py:32-51` / `gantt_critical_chain_provider.py:104-128`(`_copy` :104 + `_normalize` :112) / `gantt_contract.py:19-40`(`_public_critical_chain`)。

**测试消费者**（回盘）：
- `tests/regression_gantt_critical_chain_unavailable.py:215-248`：边级时间异常 raise ValueError「时间字段缺失」的**政策契约**——坐实「边级已立『不静默』政策、而节点级 :84 仍静默」是已立政策的缺口（本债的政策依据）。
- `tests/regression_gantt_critical_outline_sync.py:1842`：`degradation_counters` 的 `bad_time_row_skipped` 计数（兄弟范式已有计数测试，本债可复用同 code）。
- `tests/regression_gantt_contract_snapshot.py:143-184`：关键链字段集快照（:122 是 subset 检查，:144/:178 仅查已知键存在 ⇒ **加 dropped_count/critical_chain_partial 新键不会红，安全**）。
- `tests/regression_gantt_critical_chain_provider.py`：`compute_from_rows` monkeypatch。

**灾难链（删错/统一错会静默炸什么）**：
1. **裸删 :84 过滤** → start/end=None 流入 :114 `items.sort(key=...x.get("start"))` 与 :262 `max(..., key=lambda x:(x.get("end"),...))` → `None < datetime` **TypeError** → 被 :340/:341（出口 A）或 :358/:359（出口 B）try/except 接住 → 整链降为 `available:False`。**= 把「丢一行仍出链」回归成「一行坏数据全链不可用」的功能回归 + 过度激进。必须保留过滤。**
2. **新键不穿三白名单** → 在 `_normalize`(provider :112 / support :32，**均为显式白名单 dict**)或 contract（:23-28 available=False 仅回 6 键）被静默剥离 → 前端永远收不到 partial 信号 = **换地方自欺**，债没修反而更隐蔽。
3. **只改一份 _normalize**（provider 或 support）→ 另一路（如 `critical_chain_for_plan_detail_filter` support:55 走 support 版）丢键 → 同一概念两种输出 = 复活 R11/R63 漂移债。

**fix_invalidation_risk**：依赖 R11/R63 先把两份 _normalize 合一；否则改两副本。源头优先级：Schedule 表 `start_time/end_time DATETIME NOT NULL`（`schema.sql:272-273`，引擎写入）⇒ 丢行分支**近死**；真正触发点是人工 scenario 调整 —— 修法投入低、优先级桶内偏低，但灵魂线合规必做。

## 9) 承重毗邻标记

- 本债 `load_bearing=false`，`lb_no_touch=null`。**非 §5 承重文件**，无承重禁区行。
- 但 registry `lb_colocation_danger` 提醒：**:84 过滤本身是「丢坏数据仍出有用链」的刻意韧性**，虽非 LB 标记，修法仍须「丢行+留痕」而非删除（已在 §4 禁区行覆盖）。
- 同文件无其他 LB 债（R55 同址但亦非承重）。

## 10) 分层 / 导入环风险

- 修法照搬 `DegradationCollector`（来自 `core/services/common/degradation`）——**已是 core.services 内同层依赖**，gantt_tasks.py:10 / gantt_service_support.py:5 / _sched_display_utils.py:6 均已 import，无新增越层。
- helper `record_bad_time_row` 在 `core/services/scheduler/_sched_display_utils.py:84`，与 gantt_critical_chain.py 同包（scheduler/），**同层同包，无导入环**。
- 不引入 core.algorithms→core.services、core.models→core.services 等禁止边。**0 AST 违规风险**。

## 11) 测试耦合（动手前先迁哪些测试）

动手前**无需迁移现有测试**（新增键对既有断言安全）；但须**新增/扩展**以下，且与改动同 PR：
1. 扩 `tests/regression_gantt_contract_snapshot.py:143-184`：把 `dropped_count`/`critical_chain_partial` 纳入字段集快照（当前 subset 检查不会因新键红，但应主动锁定新键存在，否则后续被剥离也不报警）。
2. 新增 parity 测试（§7）：坏行+好行混合 ⇒ 断言 `dropped_count==N`、`critical_chain_partial==True`、且 `ids/edges/available` 与无坏行基线**等价**。
3. 新增「全坏行」边界（§7）：确认 `_empty_result()`(:54) 路径也带 `dropped_count`（修法须改 :54/:315，否则最该报警的场景反而无信号）。
4. 复用 `regression_gantt_critical_outline_sync.py:1842` 的 `bad_time_row_skipped` 计数范式（兄弟测试已存在）。
5. 穿透测试：经 provider :112 / support :32 / contract :19 三路后断言新键不被剥离。

> 与 R55 同批时，R55 会动 :338-359 try/except，可能影响 `compute_critical_chain_from_rows` 的异常路径测试（provider monkeypatch 测试）——同批改时一并核对。

## 12) 前置依赖（先做哪些债 / 哪些 F 门）

- **强前置 R11 + R63**：先把 provider:112 与 support:32 两份逐字重复的 `_normalize_critical_chain_result` 统一为单份。回盘确认两份确为重复（且 contract:19 是第三道）。**否则本债加键要改两副本 = 复活 R11/R63**。这是 Phase1 已标的 `R11→R12` 收口前置边。
- **同批 R55**（同址 + 共用三白名单 + 同 P4 桶 B08）：宜与本债一次性改 normalize 形状，避免两轮改同函数。registry：「内部次序在 R11/R63 之后，与 R55 同批（Batch-9）」。
- **F 门**：无跨层/契约破坏门槛；contract_topology_tests（snapshot subset + unavailable 政策契约）当前对新键安全，无需先开 F 门。
- **建议先确认触发点**：因 Schedule NOT NULL（schema.sql:272-273）使丢行分支近死，动手前可先确认人工 scenario 调整写入是否已校验 `st<et`——若已校验，本债更接近纯灵魂线合规留痕（投入更低）。
- **次序总结**：`R11/R63（合并 normalize）` → `本债 R12 + R55（同批往单份 normalize 加键/改吞错）`。

---
### 证据索引（当前工作区 2026-06-05 回盘）
- 主病点：`core/services/scheduler/gantt_critical_chain.py:84`（`_build_nodes` :79-100，裸 `continue`）
- 注入点：同文件 :312-334（`_compute_critical_chain_from_loaded_rows` return dict）；空路径 :54 `_empty_result` / :315
- 受害：同文件 :114 sort / :262 `max` / :338-341 + :352-359 try/except
- 兄弟范式：`gantt_tasks.py:192-196`（DegradationCollector + `_record_bad_time_row`）；helper `_sched_display_utils.py:84-92`（code `bad_time_row_skipped`）
- 三白名单：`gantt_critical_chain_provider.py:104`(_copy)/:112(_normalize 显式 11 键)；`gantt_service_support.py:32-51`（_normalize 重复）;`gantt_contract.py:19-40`（available=False 仅回 6 键 :23-28，available=True :39 透传）
- 源头近死：`schema.sql:272-273` Schedule.start_time/end_time DATETIME NOT NULL
- 政策依据：`tests/regression_gantt_critical_chain_unavailable.py:215-248`（边级已立「不静默」政策）

---

## 对抗核验（第二双眼睛，2026-06-05 独立 rg 回盘）

**核验结论：PASS（真债成立，修法类正确，无承重误删风险，无需第三方裁断）。**

### 重 grep 命令与结果（独立回盘，未照抄档案）

1. 主病点 + 受害点 + 注入点行号：
   `rg -n "if not st or not et|def _build_nodes|\.sort\(|def _sink_id|max\(|def _compute_critical_chain_from_loaded_rows|def compute_critical_chain|def _empty_result" gantt_critical_chain.py`
   → :84 过滤 ✅；_build_nodes :79 ✅；sort :114 ✅；_sink_id :261 / max :262 ✅；注入点 :312 ✅；出口 A :337 / 出口 B :344 ✅；_empty_result :54 ✅。**全部一致，0 漂移。**
2. 全文件可观测键：`rg -n "partial|dropped|skipped|degraded|bad_time" gantt_critical_chain.py` → **NONE_FOUND**，信号缺口属实。
3. 三白名单 + 兄弟范式：`rg -n "_normalize_critical_chain_result|_copy|_public_critical_chain|DegradationCollector|record_bad_time_row|bad_time_row_skipped"` 跨 provider/support/contract/gantt_tasks/_sched_display_utils → 全部坐实。
4. 分层：`rg "^from|^import" gantt_critical_chain.py | rg -i "algorithm|core\.models\.|core\.services"` → 无可疑越层；DegradationCollector 经 `core.services.common.degradation` 再导 `core.shared.degradation`，同层/下沉，**0 AST 违规**。
5. schema：`rg -n "start_time|end_time" schema.sql | rg "NOT NULL"` → :46-47/:175-176/:272-273/:434-435 全 NOT NULL。

### 独立读源确认的承重/收口反例验证

- **provider `_normalize`(:113-137) 与 support `_normalize`(:32-51) 均为显式重建白名单 dict**（逐字段 `raw.get(...)` 重新拼 dict，键集固定 9 键），新键 `dropped_count/critical_chain_partial` **确会被静默剥离** ⇒ 「穿三白名单否则换地方自欺」**成立**。且两份 `_normalize` 回盘确为重复 ⇒ **R11/R63 强前置成立**（不先合并则改两副本）。
- **`_empty_result`(:54-64) 含 `available:True` 且无 dropped_count** ⇒ §7「全坏行最该报警却反而无信号」自相矛盾反例**成立**，修法须改 :54。
- **裸删 :84 灾难链成立**：:84 删后 None 流入 :114 sort / :262 `max`（`None < datetime` → TypeError）→ 被 :340-341（出口 A）/ :354-359（出口 B）try/except 接住 → 整链 `available:False` 功能回归。**:84 过滤=刻意韧性，禁删判定正确。**
- 修法类 = 补可观测降级标记（DegradationCollector 范式），**无删除/统一/透传/加形参混入**，P4 严禁兜底已在 §4 明列；owner_pending=false 给终态修法合规。

### 与档案的细微出入（不影响结论，记录漂移）

1. **contract `_public_critical_chain`(:19-40) 非严格白名单**：仅 available=False 分支收 5-6 键（:23-28），available=True 分支 :40 `return raw` **全透传**。R12 的 partial-but-available 主场景在 contract 层新键**会透过**，真正卡口只有 provider/support 两份 `_normalize`。档案把 contract 列为「第三道白名单」略夸大，但「必须穿 _normalize」的核心判断正确（卡口是 _normalize 不是 contract）。**不构成争议。**
2. **schema 行号**：档案/registry 引 `schema.sql:272-273`(ScheduleCandidateRows) 与 `240-241` 来佐证「Schedule 表 NOT NULL」，但 **Schedule 表本体的 NOT NULL 在 :175-176**；:272-273 实为候选行表。结论（丢行分支近死）跨所有时间行表（:46/:175/:272/:434 全 NOT NULL）稳健成立，仅**证据指针落到了邻表**，属轻微 file:line 不精确，非结论错误。
3. **出口 B try/except 行段**：档案 §4/§8 写 :352-359，实际是 `:352-355`(repo) + `:356-359`(calc) 两段 try。范围一致，不影响 R55 协调判断。

### 同批/前置复核
same_file_siblings=[] 成立（同文件唯一共址为 R55，行段 >300 行不重叠，共用 _normalize+白名单宜同批）。前置 R11/R63（合并 _normalize）→ R12+R55（Batch-9）次序成立。owner_pending=false 复核无误（未擅自越权分配批次，批次为 registry 既定 hint）。
