# R72 逐债档案 · _get_plan_role_arg 在 gantt 与 week_plan 路由各一份(N1 家族)

> 桶 B02 / 病理 P5 / 严重度 low / 簇 C01 / 承重 false / owner_pending **true**
> 主文件 scheduler_gantt.py(+ scheduler_week_plan.py) · 状态 planned · 只读产档,未动生产代码

---

## 1) 当前真实 file:line(回盘)+ 漂移记录

按符号名 `rg "_get_plan_role_arg"` 全工作区回盘(2026-06-05),**不照抄 old_location**:

| 符号 | old_location_DONT_TRUST | current_evidence 线索 | 本轮实测 file:line | 漂移 |
|---|---|---|---|---|
| `def _get_plan_role_arg()` (gantt 副本) | scheduler_gantt.py:135 | :136 | `web/routes/domains/scheduler/scheduler_gantt.py:136` | **+1 行**(135→136) |
| `def _get_plan_role_arg()` (week_plan 副本) | scheduler_week_plan.py:60 | :65 | `web/routes/domains/scheduler/scheduler_week_plan.py:67` | **+7 行**(60→67),且比 evidence 的 65 又多 2 行 |

两份**逐字相同**(已 sed 比对):
```python
def _get_plan_role_arg() -> Optional[str]:
    raw = request.args.get("plan_role")
    if raw is None:
        return None
    text = str(raw).strip()
    return text or None
```
读 `request.args["plan_role"]` → strip → `text or None`(空串归 None)。无校验、无 raise、无兜底默认值。

生产消费者(call 点)回盘,**week_plan 的两处调用点也漂了**:
- `scheduler_gantt.py:181`(blast 记 :180,**+1**)
- `scheduler_gantt.py:325`(blast 记 :324,**+1**)
- `scheduler_week_plan.py:288`(blast 记 :273,**+15**)
- `scheduler_week_plan.py:375`(blast 记 :360,**+15**)

> 结论:blast.location_status 标"✅准/行号未变"在 gantt 侧大致成立(+1 可视为同义)、但 **week_plan 侧已明显漂移(def +7、call +15)**,说明 scheduler_week_plan.py 在 2026-06-05 当天又被改过(文件 mtime 06-05 02:06,正是 R55/resource_dispatch 邻边在动)。落地修法前**必须以本档行号为准重新回盘**,blast 的 :60/:273/:360 已失真。

## 2) 当前状态 + 与 status evidence 一致性

- registry `status_2026_06_05 = planned`;current_evidence 明确"未提到 web 公共模块,未动手"。
- 本轮实测:两副本仍**字节级各一份**,`scheduler_utils.py` 既无 `get_plan_role_arg`、也未 `import request`,两路由均未 `from .scheduler_utils import` 该 getter。`tests/` 内 `rg get_plan_role_arg` 为空。
- **一致**:状态 planned 与"仍双份、无守卫、未收口"的证据吻合。无 in_progress 迹象。

## 3) 病理 / 严重度 / 桶 / 簇

- 病理 **P5(N1 家族 / N1-dup)**:同一段 plan_role 取参逻辑在 gantt/week_plan 两路由各一份,佐证 web 层 plan_role 取参**缺单一收口点**。
- 严重度 **low**:逻辑极简(6 行纯取参)、零分支差异、无生产事故面;只是"散多处"的可维护性债。
- 桶 **B02(身份族)**,home B02;簇 **C01**。
- 旁注:同文件 `_get_int_arg` 亦双份(`scheduler_gantt.py:34` ↔ `scheduler_week_plan.py:57`),同 N1-dup 模式,但**属另一符号、不在本债 R72 scope**(blast.pathology 已声明)。`_get_scenario_id_arg` 同样双份(gantt:144 ↔ week_plan 紧邻),亦同模式不在 scope。

## 4) 修法类(owner_pending → 暂不给终态)

**修法类 = 收口(P5 dedup,收到已存在点)。承重 false,无注释/raise 约束。**

> **owner_pending=true → 待 owner 裁断,本档暂不给终态修法、暂不分配执行批次。** 下方为依赖/爆炸分析所需的"候选收口形态",非终态指令。

候选收口点(**已存在,非新建**):`web/routes/domains/scheduler/scheduler_utils.py`(2249B,5月24日定稿,§10/Phase1 指定的"scheduler 路由取参 helper 落点")。形态:
1. 在 scheduler_utils.py 提一份 `get_plan_role_arg()`(建议提为公开名,去掉前导下划线);
2. scheduler_utils.py **需新增 `from flask import request`**(现仅 `from flask import g`);
3. gantt / week_plan 改 `from .scheduler_utils import get_plan_role_arg`,删各自本地 :136 / :67 副本。

**【分层红线 — 硬约束,owner 裁断也不可越】** 此 getter 读 `flask.request`,**绝不能**收口到 `core/services` 或 `core/shared`——core 不依赖 flask,放进去 = 跨层 import + 破"AST 0 违规"。这与 R44 的 `selected_plan_role` 收口到 core 是**两个不同落点**:R72 是请求层取参(core 无对应孪生),R44 是已 resolve 后的角色推导(core 有同名收口点)。

owner 需裁断的开放问:① 收口名公开化(`get_plan_role_arg`)还是保留 `_` 私有? ② 是否与 R44 共识"web/core 各落各点、不硬塞同一模块"(见 §6 / planned_deps_hint open_questions)。

## 5) 同文件碰撞债(siblings)

`same_file_siblings = []`(registry 显式空)。但 interference_edges 列了 4 条 same_file 干扰边(非真 sibling,跨桶同文件):

| 干扰债 | 共享文件 | 改哪段 | 与 R72 撞行号/dict 键位移风险 | 谁先谁后 |
|---|---|---|---|---|
| **R21** | scheduler_gantt.py | gantt_plan_query shim 死兼容,改的是 query 层 import 与 shim 函数,**不在 R72 的 def/call 区** | 低:R72 删的是 :136 def + :181/:325 call,R21 动的是别处;但**同文件改 = 行号联动**,谁后做谁须重新回盘 | 无硬序,但建议同批或显式串行 |
| **R55** | scheduler_gantt.py | filtered 关键链 scope 标记,属 gantt_service.py 主体 + gantt 路由调用,**与 R72 取参区不重叠** | 低,同上(同文件行号联动) | 无硬序 |
| **R10** | gantt_service.py(经由 week_plan 关联) | 死方法删除,**不同文件** | 无直接撞 | 无序 |
| **R44** | scheduler_gantt.py + scheduler_week_plan.py | web 层 plan_role getter(selected_plan_role),与 R72 **同属 N1 家族、同两文件** | **中**:R44 主改 scheduler_navigation_publish.py,但 co_change 触及两路由的 import 块;若两债各自往两路由顶部加/删 import,**import 块行号互相位移** | 见 §6,无硬序但须协调 |

**关键提醒(week_plan.py 已是漂移热点)**:scheduler_week_plan.py 当天被多债触碰(R44/R55/resource_dispatch),本债 def 已 +7、call 已 +15 漂移。**任何落地前以实时 `rg` 为准,registry/blast 行号一律重新回盘**。

## 6) 同收口点债

R72 的候选收口点是 **web 层 `scheduler_utils.py`**。检索"收口到同一符号"的其他债:

- **R44**(`selected_plan_role`):**co_change_within_group=[R44]**,同 N1 家族 web 层 plan_role getter。**但落点不同**——R72→web `scheduler_utils.get_plan_role_arg`(请求层取参),R44→core `schedule_result_view_context.selected_plan_role`(已 resolve 角色)。registry 双向确认"落点不同、无硬序"。
- **顺序敏感性**:无硬序。唯一耦合点是"**不要各建各的 web 层 plan_role helper**"——这正是 N1"散多处"病根。若 owner 裁定二者都需 web 侧 helper,须共识同一落点;但 deps_hint 又警告"也不要硬塞同一落点"(R44 本质在 core)。→ **协调项而非排序项**,交 owner。
- scheduler_utils.py 现有消费者(7 处 import:week_plan/excel_batches/excel_calendar_rows/excel_calendar/run/gantt_adjustments/calendar_pages)均 import 的是 `_current_scheduler_operator` / `_normalize_*`,**与 plan_role getter 无符号冲突**,新增 `get_plan_role_arg` 不撞既有名。

## 7) 收口行为差异 + parity 测试

两副本**已逐字相同**(§1 sed 比对证),收口是"两份字节相同 → 收成一份"的纯 dedup,**新旧无行为差**。逐分支等价性核对:

| 输入 | gantt 副本 | week_plan 副本 | 收口后 | 等价? |
|---|---|---|---|---|
| `plan_role` 不在 query | `None` | `None` | `None` | ✅ |
| `plan_role=""`(空串) | `text or None`→`None` | 同 | `None` | ✅ |
| `plan_role="  "`(纯空白) | strip→`""`→`None` | 同 | `None` | ✅ |
| `plan_role="ADOPTED"` | `"ADOPTED"` | `"ADOPTED"` | `"ADOPTED"` | ✅ |
| `plan_role=" x "` | strip→`"x"` | 同 | `"x"` | ✅ |

**无反例**(不存在 None-vs-raise 这类边界分叉)。因此 parity 测试**非强制前置**,但建议补一条**最小守卫测试**钉住收口后行为(当前 tests/ 对该符号 0 守卫,blast.contract_topology_tests 为空):对 `get_plan_role_arg` 在 4 个边界值(缺参/空串/空白/正常)上断言返回,防后续有人误加"默认 ROLE_ADOPTED 兜底"破坏空串→None 语义(那会引入灵魂线违规的静默兜底)。

> **灵魂线检查**:收口**不得**顺手给 plan_role 加默认值/兜底回退/合法性校验吞错。空串→None 是当前真实语义,必须原样保留;若发现 None 下游会炸,应是下游 loud raise,不在本债 scope。

## 8) 爆炸半径

**生产消费者(4 处 call,本轮回盘行号)**:
- `web/routes/domains/scheduler/scheduler_gantt.py:181`(gantt 页 `plan_role = _get_plan_role_arg()`)
- `web/routes/domains/scheduler/scheduler_gantt.py:325`(gantt 数据/JSON 路由)
- `web/routes/domains/scheduler/scheduler_week_plan.py:288`(`week_plan_page`)
- `web/routes/domains/scheduler/scheduler_week_plan.py:375`(week_plan 数据路由)

**测试消费者**:`[]`——tests/ 对 `_get_plan_role_arg` / `get_plan_role_arg` **零守卫**(rg 实测为空)。

**灾难链(收口/删错会静默炸什么)**:
1. **删早于改 call**:删掉某文件本地 def 但漏改该文件的 import,→ `NameError` at request time(loud,易发现)。
2. **import 漏加 request**:把 getter 提到 scheduler_utils.py 却忘 `from flask import request`,→ 运行期 `NameError: request`,只在该路由被命中时炸(latent,但 loud)。**这是本债最易踩的坑**(scheduler_utils 现仅 import g)。
3. **静默改语义**:收口时若有人"顺手"把空串→None 改成空串→`""` 或加默认角色,→ 下游 plan_role 选择逻辑收到非 None 空值,**可能静默选错计划角色**(low 债升级为正确性 bug)。→ §7 parity 测试正为防此。
4. **整体爆炸面**:仅限 gantt/week_plan 两路由的 plan_role 入口取值,**不触碰 core、不触碰算法层**,blast 半径小。

## 9) 承重毗邻标记 + 禁区行

- `load_bearing=false`,`lb_no_touch=null`,blast.lb_colocation_danger = **none**。
- scheduler_gantt.py / scheduler_week_plan.py / scheduler_utils.py **均非承重文件**,同文件无 LB 邻债。
- **禁区行**:无(本债无承重约束)。修法不受"仅注释"限制,可正常收口+删副本。

## 10) 分层 / 导入环风险

- **越层风险(关键)**:getter 读 `flask.request`,收口落点**必须留在 web 层**(`web/routes/domains/scheduler/scheduler_utils.py`)。**绝不可**收到 core/services 或 core/shared——会产生 `core → flask` 跨层 import,破 AST 0 违规红线。本债与 R44(收 core)落点天然分离,正因如此。
- **导入环风险**:**无新增环**。week_plan.py 已 `from .scheduler_utils import _current_scheduler_operator`(实测 :47),即 `scheduler_week_plan → scheduler_utils` 的 import 方向**已存在**;新增 `get_plan_role_arg` 走同一条边,不产生新环。gantt.py 当前未 import scheduler_utils,新增 `from .scheduler_utils import get_plan_role_arg` 是**新边但同方向**(route → utils,utils 不反向 import route),不成环。scheduler_utils.py 现有 import(excel_utils/normalizers/core.excel_service)均不指向两路由,无回边。
- 新增 `from flask import request` 在 scheduler_utils.py 不引入跨层(flask 是 web 层合法依赖)。

## 11) 测试耦合

- **动手前需迁移的测试:无**(tests/ 对该符号 0 守卫,无契约/拓扑测试钉它)。
- 收口**不破坏**任何现有测试(无消费者)。
- 建议(非阻塞)随收口**新增**一条最小守卫测试(见 §7),挂在 web 路由测试或新建 `tests/.../test_scheduler_utils.py`,断言 `get_plan_role_arg` 4 边界值;此测试应**在收口后**写(钉新公开符号),不是前置迁移。

## 12) 前置依赖

- **无硬性前置债**:R72 叶子级、弱耦合,blast.fix_invalidation_risk = "无直接失效"。
- **软协调(非排序)**:
  - 与 **R44** 协调"web/core 各落各点、不各建各的 plan_role helper"(见 §6),但二者无硬序,可任一先落。
  - 与 **R21 / R55**(同 scheduler_gantt.py)、R44(同两路由)存在**同文件行号联动**:谁后做谁须重新 `rg` 回盘行号,**不可信任本档/registry 的静态行号**(week_plan.py 已证当天高频漂移)。建议 R72 与同文件邻债**同批或显式串行**,避免交叉位移返工。
- **F 门**:落地后须过 **AST 分层门(0 违规)**——确认收口未引入 `core→flask` 越层;过 **import 环检测**——确认 gantt→scheduler_utils 新边未成环。两门均预期通过(见 §10 分析)。
- **owner 裁断门(本债特有)**:owner_pending=true → 收口名公开化与"是否与 R44 共识落点"两问须 owner 拍板后,方可进入终态修法/批次分配。本档止于依赖+爆炸分析。

---

### 批次定位(planned_batch_hint 透传,非终态)
Batch-4(B02 身份族),叶子级、弱耦合,Batch-1 之后可较晚落;建议紧随 R44 之后一起处理 N1 家族以统一命名。**最终批次待 owner 裁断后定。**

---

## 对抗核验(第二双眼睛)

核验时间 2026-06-05,只读不改,默认怀疑。重 grep 命令与结果:

- `rg -n "_get_plan_role_arg" web/`:def `scheduler_gantt.py:136` / `scheduler_week_plan.py:67`;call gantt:181、325、week_plan:288、375。**与档案 §1/§8 全部一致**(包括档案已自行更正 week_plan def 60→67、call→288/375 的漂移)。`rg get_plan_role_arg tests/` 空(exit 1)→ tests 0 守卫成立。
- `diff <(sed -n '136,141p' gantt) <(sed -n '67,72p' week_plan)`:空 → **字节级相同**确认,纯 dedup、无行为差成立。§7 逐分支等价表无反例。
- `ls scheduler_utils.py`:存在(2249B,5月24日),`rg "from flask" utils` 仅 `from flask import g`,`rg request utils` 空(exit 1)→ **收口点真实已存在(非新建,非 R09 例外,合规)** 且确实缺 `request` import(本债最易踩坑点,档案 §4/§8 已标红,成立)。
- 导入环/分层:`rg "from .scheduler_utils import" 两路由` → week_plan:47 已 import(同方向边已存在,不成环);utils 不反向 import 两路由(exit 1);`rg "request.args.get\(.plan_role|def get_plan_role_arg" core/` 空(exit 1)→ **core 无孪生**,R72(web 请求层)与 R44(core resolve 层)落点天然分离成立,收口到 core 会越层(`core→flask`)成立。
- **owner_pending 越界检查**:档案 §4 明示"暂不给终态修法、暂不分配执行批次",§12 列 owner 裁断门,批次定位标"非终态"。**未擅自给终态/分配批次**,合规。
- **灵魂线检查**:档案 §7/§8 钉住"空串→None 不得被顺手加默认值/兜底",无新增静默回退,合规。
- **承重检查**:lb=false、lb_no_touch=null、无 LB 邻债,修法类未混入对承重债的删除/统一/透传。

**结论:PASS。** 行号、字节级相同、收口点存在性、分层、导入环、owner_pending 边界、灵魂线均无反证。低危纯 dedup 债,落地前唯一硬动作 = 以实时 `rg` 重盘 week_plan.py 行号(当天高频漂移热点,档案已反复强调)+ 收口时务必补 `from flask import request`。无需升级第三方裁断。

---

## 2026-06-10 终态补登（G29 收口，R72 → fixed）

详见 `_registry.json` R72 current_evidence 与 PHASE4 计划 §1.4 G29 补登段。要点：公开名 `get_plan_role_arg` 落 `scheduler_utils.py`（补 `from flask import g, request`），gantt/week_plan 删本地副本改共享，4 调用点零行为变化；空白→None 无 adopted 兜底；守卫测试 `tests/web_pages/test_scheduler_plan_role_arg_contract.py` 新建并双注册；分层红线守住（request 取参留 web 层，与 R44 core 侧各落各点）。
