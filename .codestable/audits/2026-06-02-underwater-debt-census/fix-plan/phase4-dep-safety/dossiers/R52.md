# R52 逐债档案 — ready_queue.get_ready_operation_ids 全量扫描版

> 簇 C01 | 桶 B09 | 病理 P3 | 严重度 medium | 承重 false | owner_pending **false**
> 同文件兄弟债: R25(service 垫片)
> **2026-06-08 B 执行补登：已 fixed，owner_pending=false。** O07 已裁方向 B：保留全量扫描版作为 incremental-vs-fullscan 差分 oracle；本轮只补「我是故意的」注释，不删 impl、不删 R25 垫片、不新建 `tests/scheduler_graph/test_sgs_graph_ready.py`。下文方向 A 删除路线仅作为历史风险证据，不是当前执行计划。

---

## 1) 当前真实 file:line(回盘)+ 漂移记录

按符号名独立 grep 回盘(2026-06-05),**未照抄 old_location**:

| 符号 | old_location(勿信) | 当前真实 file:line | 漂移 |
|---|---|---|---|
| `def get_ready_operation_ids` | `ready_queue.py:103` | `core/algorithms/greedy/dispatch/ready_queue.py:103` | **0 行,无漂移** |
| `__all__` | — | 同文件补注释后 `:142` `__all__ = ["ReadyQueueContractError", "get_ready_operation_ids"]` | G39 注释使其下移 |
| `class ReadyQueueContractError` | — | 同文件 `:10`(ValueError 子类) | 无漂移 |
| R25 再导出垫片 | `core/services/scheduler/graph/ready_queue.py` | 同路径补注释后 `:11` import,`:13` `__all__` | 2026-06-08 G39 补 KEEP 注释后全文件 13 行,语义仍是纯转发 |

`current_evidence_2026_06_05` 标的 impl 已随 G39 注释扩到 `:103-139`，`__all__` 下移到 `:142`。**location_status ✅准，行号须按当前盘使用。**

生产消费者回盘:`rg "get_ready_operation_ids" core/ web/ data/` 仅命中两处——impl 定义/导出(`:103`/`:142`)与 R25 垫片（补注释后 `:11`/`:13`）。**生产零直接业务调用,证实。** `greedy/dispatch/__init__.py` 不导出该符号(planned_fix_hint 所述,本轮未复核 __init__ 行号但符号级 rg 已证生产无调用)。

## 2) 当前状态 + 与 status evidence 一致性

- registry 2026-06-08 已补登为 `status_2026_06_05 = fixed`;`owner_pending = false`。
- 与 current_evidence 一致:impl 仍在、生产零调用、迁移目标文件 `tests/scheduler_graph/test_sgs_graph_ready.py` 仍不存在，`test_ready_queue.py:18` 仍经 R25 service 垫片 import；这些都是 O07 KEEP 终态，不是未动手。
- **结论:状态 fixed 与现状完全一致；owner 决策门已裁方向 B。**

## 3) 病理 / 严重度 / 桶 / 簇

- **病理 P3(双实现 / 平行抽象残留)**:同"就绪集合计算"存在两套实现。
  - 死/退役版:`ready_queue.py:103 get_ready_operation_ids` 全量扫描(每次遍历全集判前置)。
  - LIVE 生产版:`sgs_graph.py` 增量 Kahn 前沿(`_initialize_graph_ready_frontier`,`:85` 起,入度递减),git `84812566` 已切换。
- **非纯死残渣**:全量版被 `test_ready_queue.py:273/:290 test_incremental_ready_queue_matches_full_scan_*` 当作 **incremental-vs-fullscan 差分 oracle**(`:278/281/284/287/299` 5 处 `_incremental == _full_scan == 字面量`)。这是它唯一存活价值。
- 严重度 **medium**:无生产风险(零业务调用),但删错会静默丢失差分交叉校验覆盖。簇 **C01**(与 R25 同文件同符号绑死)。

## 4) 修法类 — **O07 已裁方向 B：KEEP + 认账注释**

承重 false、lb_no_touch null,**不受承重铁律约束**。O07 已裁方向 B，当前终态是把隐性“像死函数”的全量扫描版转为显性差分 oracle。

当前执行内容：

1. 不删 `core/algorithms/greedy/dispatch/ready_queue.py:get_ready_operation_ids`。
2. 不删 R25 service 垫片。
3. 不新建 `tests/scheduler_graph/test_sgs_graph_ready.py`，不迁 `test_ready_queue.py`。
4. 在 impl 与差分 test 处**显式标注「我是故意的：作为 incremental-vs-fullscan 差分 oracle 保留」**，把隐性死函数转为显式 oracle。

灵魂线核验:两方向均不新增兜底/静默回退/except 吞错;impl 内 `ReadyQueueContractError` 抛错链是 loud raise(`:16/20/22/...`),保留即可,不需改 raise(本债病理 P3 非 P4)。

## 5) 同文件碰撞债(sibling 协调)

唯一 sibling = **R25**(`core/services/scheduler/graph/ready_queue.py` service 再导出垫片)。interference_edges 标记 `same_file=true` + `same_symbol=true`,五条 why(ready_queue.py / sgs_graph.py / test_metrics_topology.py / test_ready_queue.py / sym:get_ready_operation_ids)。

- **co_change 强绑定**:R52(impl)与 R25(垫片)**必须同提交**。R25 垫片补注释后 13 行,核心仍是 `from ...algorithms...ready_queue import ...` 转发;历史方向 A 里 impl 一删,垫片 import 即 ImportError。
- **谁先谁后**:O07 已裁方向 B，R25 垫片随 impl 同批保留并补注释。历史方向 A 若未来重启，仍要求 R25 垫片随 impl 同删(同提交)，并先处理测试迁移。
- **撞行号 / dict 键位移**:R52 impl 在独立文件 `ready_queue.py`(algorithm 层),R25 在 `services/.../ready_queue.py`(service 层),**不同文件,不撞行号**;无 dict 字面量共享,无键位移风险。同文件兄弟仅是"同一逻辑符号跨两文件",物理上隔离。

## 6) 同收口点债

- **本债不收口到任何统一符号**(方向 A 是删平行实现,方向 B 是加注释)。registry `planned_deps_hint` 明确"无 load_bearing colocation"。
- 唯一与之共享符号 `get_ready_operation_ids` 的是 R25,已在 §5 处理。
- **顺序敏感性**:无第三方债收口到此符号。仅 R52→R25 的删除顺序敏感(impl 先于垫片或同提交),已在 §5 覆盖。

## 7) 收口行为差异及所需 parity 测试

本债非"收口到统一函数",而是"删一套平行实现 / 或保留作 oracle",但仍需 parity 论证 **全量版 vs 增量版是否逐分支等价**(方向 A 删全量版前必须证两路对边界值等价,否则丢覆盖=丢真覆盖):

- **校验合同 parity**(downstream_effect 要求):删前须确认 `sgs_graph.py:20-160` 的 `ValidationError(field="graph_ready_context")` 校验语义**已覆盖且不弱于** `ready_queue.py:10-100` 的 `ReadyQueueContractError` 合同点。实测对照:
  - 坏 op_id(非正整数):全量版 `_normalize_op_id`(`:14-22`)raise ReadyQueueContractError;LIVE 版 `_graph_ready_op_id_set`(`:20-30`)raise ValidationError "必须是 op_id 集合"。**两路均 raise,LIVE 异常类型不同(ValidationError vs ReadyQueueContractError)——这是反例:边界值"传入图对象"两路都 raise 但异常类不同,parity 测试须断言"均拒绝",不能断言同类型。**
  - 待排∩固定冲突:全量版 `:83` raise;LIVE 版 `:56-58` raise "待排工序不能同时是固定/已完成工序"(当前由 `test_graph_ready_state_rejects_schedulable_fixed_overlap` 与 `test_graph_ready_blocking_rejects_fixed_successor_conflict` 覆盖)。**LIVE 等价或更严。**
  - 未知 link scope(前置/后继不在域内):LIVE 版 `_validate_graph_ready_links`,当前由 `test_graph_ready_state_rejects_unknown_link_scope` 覆盖(含后继 999 不在域)。全量版 `_validate_predecessor_scope`(`:59-72`)仅校前置,**LIVE 版多校后继 → 更严。**
- **所需 parity 测试(方向 A)**:迁移前先跑现有 5 处差分 oracle 字面量断言确认两路当前等价(基线);迁移后这些断言变为对 LIVE 路径的字面量期望(`[1]/[2,3]/[3]/[4]/[2]`),需保留对 `test_graph_ready_state_*` 三条 ValidationError 契约的覆盖。**反例提醒:None 输入——LIVE 版 `_prepare_graph_ready_state(:38)` 对 None 直接 `return None`(不 raise),全量版无 None 入口语义;此分支不可直接 cross-check,parity 测试须分别针对两路写。**

## 8) 爆炸半径(phase1_blast 9 字段核对 + 回盘)

- **生产消费者**:`[]` 空。回盘证实 `rg get_ready_operation_ids core/ web/ data/` 仅命中 impl 定义与 R25 垫片,**零业务调用**。删除/改造对生产路径零影响。
- **测试消费者**(回盘逐行核对,与 registry 一致):
  - `tests/scheduler_graph/test_ready_queue.py:18`(import 经 R25 垫片)、`_full_scan_ready_ids` helper 与差分 oracle 仍调用全量版；具体断言锚点按符号重 rg。
  - 间接(经 R25 垫片):`tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py:29`("core.services.scheduler.graph.ready_queue" 字符串断言)、`tests/scheduler_graph/test_metrics_topology.py:142`(同字符串)。**这两处是模块路径断言,当前 KEEP 路线保留；旧方向 A 删 R25 垫片时须同步改。**
- **灾难链(删错 / 收错会静默炸什么)**:
  1. 若 **直接删 impl 不先迁移**:`test_ready_queue.py` 当前 import 行变红 → 整文件用例全废 → **LIVE sgs_graph 行为覆盖一次性蒸发**(灾难,planned_fix_hint 明令"绝不可裸删整 test 文件")。
  2. 若 **删 R25 垫片不改 lazy_runtime_contract/metrics_topology 的字符串断言**:这两处模块路径校验失败(但属 import 拓扑断言,非静默——会 loud fail)。
  3. 若 **方向 B 保留但不加注释**:下次审计/雷达再判死残渣,语义债复活(失忆债)。
- fix_invalidation_risk: **none**(registry)。lb_colocation_danger: **none**(ReadyQueueContractError 抛错链全在死函数内,生产无人捕获,删不影响任何生产 catch)。

## 9) 承重毗邻标记

- 本债 `load_bearing=false`,`lb_no_touch=null`。
- 同文件(`core/algorithms/greedy/dispatch/ready_queue.py`)**无其他 LB 债毗邻**;唯一 sibling R25 在 service 层另一文件,也非承重。
- **无禁区行**(无承重约束)。`ReadyQueueContractError` 抛错链(`:10-100`)虽是契约 raise,但 lb_colocation_danger=none——生产无 catch,删除不破坏任何承重捕获点。

## 10) 分层 / 导入环风险

- 现状分层正确:impl 在 `core.algorithms`(底层),R25 垫片在 `core.services`,通过 service→algorithm **单向依赖**(垫片注释明言"so SGS does not import the scheduler service layer")。
- **方向 A(删)**:移除依赖边,**降低**耦合,**不引入越层 import,不造导入环**。
- **方向 B(保留 + 注释)**:不改 import 结构,**零分层风险**。
- 若误让 test 从 service 层改 import algorithm 层(方向 A 迁移时),仍是 test→algorithm,合法。**0 AST 违规风险,不触碰 core.algorithms→core.services / core.models→core.services 红线。**

## 11) 旧方向 A 测试耦合（当前不执行）

当前 O07 KEEP 路线不删 impl，不迁测试。方向 A 若未来重启，才触发下列测试迁移(动手第一步,先于删 impl):
1. **新建** `tests/scheduler_graph/test_sgs_graph_ready.py`(当前不存在,实测确认)。
2. 从 `test_ready_queue.py` 迁出:
   - 三条唯一 LIVE ValidationError 契约:`test_graph_ready_state_rejects_schedulable_fixed_overlap`、`test_graph_ready_state_rejects_unknown_link_scope`、`test_graph_ready_blocking_rejects_fixed_successor_conflict`。
   - ~23 个 sgs_graph LIVE-only 用例(`from core.algorithms.greedy.dispatch.sgs_graph import` 那批,`:7`)。
   - 5 处差分 oracle 字面量断言(`:278/281/284/287/299`)改为对 LIVE 路径字面量期望。
3. 同步改模块路径字符串断言:`tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py:29`、`tests/scheduler_graph/test_metrics_topology.py:142`(删 R25 垫片时)。
方向 B:无需迁移,仅在 test、impl 与 R25 service 垫片加 oracle/兼容用途注释。

## 12) 前置依赖(先做哪些债 / 哪些 F 门)

- **F 门(决策门)已关闭**:O07 已裁方向 B 保留 oracle。
- **co_change**:R25 已与 R52 同批补注释；不执行同提交删。
- **批次提示**:registry 已补登为 Batch-B/G39 已 fixed。
- **删前硬校验(方向 A)**:当前不删；若未来重启方向 A，仍必须先确认 `sgs_graph.py:20-160` 的 graph_ready_context 校验语义已覆盖/不弱于全量版合同点(§7 已逐分支核对:LIVE 等价或更严,但异常类型不同 → parity 测试须断言"均拒绝"而非"同类型")。

---

## 对抗核验(第二双眼睛)

核验日期 2026-06-05,只读不改,独立重 grep 回盘,默认怀疑。

**重跑命令与结果**:
1. `rg -n "def get_ready_operation_ids|^__all__|class ReadyQueueContractError" core/algorithms/greedy/dispatch/ready_queue.py` → `:10` ReadyQueueContractError(ValueError)、`:103` def、`:142` __all__。**与档案 §1 当前锚点一致。**
2. `rg -n "get_ready_operation_ids" core/ web/ data/` → 仅 4 命中:impl 定义(`:103`/`:142`)+ R25 垫片 import(`core/services/scheduler/graph/ready_queue.py:11`)+ 垫片 `__all__`(`:13`)。**生产零业务调用证实。**
3. `cat -n core/services/scheduler/graph/ready_queue.py` → 补注释后全文 13 行,`:11` 纯转发 import、注释明言"so SGS does not import the scheduler service layer"并补 KEEP 注释。**R25 垫片纯转发、co_change KEEP 已闭合,确认。**
4. `ls tests/scheduler_graph/test_sgs_graph_ready.py` → NOT EXIST。**迁移目标文件确不存在,方向 A 须新建,确认。**
5. `rg -n "get_ready_operation_ids|from core" tests/scheduler_graph/test_ready_queue.py` → `:18`(经 R25 垫片 import)，`_full_scan_ready_ids` helper 与差分 oracle 仍在。**测试消费者清单与 §8/§11 一致。**
6. `rg -n "core.services.scheduler.graph.ready_queue" tests/scheduler_graph/test_scheduler_graph_lazy_runtime_contract.py tests/scheduler_graph/test_metrics_topology.py` → `:29` / `:142` 模块路径字符串断言两处命中。**当前 KEEP 路线保留；旧方向 A 删 R25 垫片时须同步改。**
7. `__init__.py:16 __all__ = ["dispatch_batch_order","dispatch_sgs"]` → 不导出 ready 符号,**与 §1 末/registry prod_consumers 一致。**
8. **parity 反例独立复核(最关键)**:全量版 `ready_queue.py:16-100` 全程 `raise ReadyQueueContractError`(ValueError 子类);LIVE 版 `sgs_graph.py:22-173` 全程 `raise ValidationError(field="graph_ready_context")`。"传图对象"两路均拒但异常类不同(全量 `:32` vs LIVE `:26`);`None` 输入 LIVE `:38-39 return None` 不 raise、全量版无 None 入口。**档案 §7 如实标注"parity 须断'均拒绝'非同类型、None 分支不可 cross-check",未把异类异常当无害统一抹平,无承重误删/统一引爆隐患。**

**判定**:
- 行号:全部一致,0 漂移。location_status ✅准 成立。
- 承重:lb=false、lb_no_touch=null,无禁区行;ReadyQueueContractError 抛错链全在死函数内、生产无 catch(lb_colocation_danger=none 确认)。**无承重误删风险。**
- owner_pending 历史核验：2026-06-05 时档案明确不给终态修法、不进批次,仅透方向 A/B 与依赖/爆炸半径。2026-06-08 已由 O07 裁为 KEEP 并补登 fixed。
- 分层:impl(algorithms)←垫片(services)单向依赖,方向 A 删边降耦合不造环,0 AST 风险确认。
- 灵魂线:两方向均不新增兜底/静默回退/except 吞错(P3 非 P4,无需改 raise),确认。

**核验结论:PASS。** 找不到反证。2026-06-08 补登：owner 决策门已由 O07 裁为方向 B 保留 oracle，本轮已按该裁定 fixed。
