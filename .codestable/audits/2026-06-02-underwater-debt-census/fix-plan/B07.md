# B07 · 资源筛选collar

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：B07 是「资源筛选双轨漂移」桶,2 条债共享同一个收口点族(ScheduleResourceFilter/normalize_schedule_resource_filter),但性质天差地别:R05 是 verdict=load_bearing 的「先扩收口点才能收敛」硬骨头(派工读取链的 team 双 join + 空 id 全量语义是 column_name 单列属性表达不了的真实第三轴,塌缩=班组静默返全量坏数据或整页 500);R67 是纯维护面的 P3 字面量去重(6 个请求参数别名清单在 3-4 处各抄一遍),零数据风险。

整桶策略=「R05 先扩后收、R67 随手抽常量」,二者互不冲突可并行,但 R05 是全局排序的关键前置(必须先于 B12 的 R34 落地,否则 R34 收敛 list_dispatch_rows_with_resource_context 到 column_name 时会丢 team 谓词)。

内部排序:R05 优先级 1(承重+跨桶前置,投工大、风险高、分 3 个原子小步:扩收口点→补回归→才收敛);R67 优先级 2(独立叶子、low/trivial、纯增量抽常量,Batch-1 之后任意时点可落,甚至与 R05 并行)。

铁律提醒:R05 绝不允许「把 _normalize_scope_type 直接换成 normalize_schedule_resource_filter」的塌缩式修法——那会丢 team 轴(整页 500)或丢空 id 全量分支;且因 verdict=load_bearing+refuted=False,本桶在 Phase2 是「补回归+扩接口的计划」,真正的字面量收敛收尾留到收口点扩容验证通过后。R67 必须先认清 4 处不是「同一个 6-key 纯清单」——其中 2 处(request_resource_context/reports_request_support)是纯 6 键,2 处(reports_export_support/scheduler_navigation_links)是把 6 键嵌在更大 superset 元组里,naive「整元组替换」会压扁 superset 丢 plan_id/日期键。

**内部排序**：R05 优先级 1、R67 优先级 2,二者零冲突可并行,但 R05 是关键路径(跨桶前置 B12 R34)。R05 必须分 3 原子小步严格串行:(步1)给 ScheduleResourceFilter/normalize_schedule_resource_filter 扩 team 双 join 谓词接口 + 放开『类型有/id 空=全量』语义 → (步2)先补 team≠全量 + 空 id 可查的负向回归(纯增量钉反例)→ (步3)收口点扩容验证绿后,才收敛 repo :451-463 与 schedule_repo :141-153(R34)的 operator/machine 字面量及 _normalize_scope_type 委托。本轮 Phase2 计划重心在步1+步2(纯增量、零数据风险),步3 字面量收敛收尾。R67 三步无序(抽常量→4 处 import 改造→补单一来源契约),随时可落甚至与 R05 步1并行。全局:整桶 Batch-8 必须先于 Batch-13(B12 R34 依赖 R05 扩好 team 轴)。

### R05 · P5(双轨漂移)但 adversarial verdict=load_beari · sev=medium · load_bearing=True

- **当前位置(已复核)**：⚠️部分行号已变(已回盘逐一复核,文件除 schedule_plan_query_service.py 外均未 git 修改、提交态≡基线 b08162cd):(1) `_normalize_scope_type` ✅准 `core/services/scheduler/resource_dispatch_service.py:63-67`(报告写 :63,精确;:65 校验集 {operator,machine,team} 是 collar {machine,operator} 的 SUPERSET,另有独立的 `_normalize_team_axis:69-73` 校验 {operator,machine} 班组内轴)。(2) 收口点 `core/models/schedule_resource_filter.py` 全 ✅:`:8` SUPPORTED_SCHEDULE_RESOURCE_TYPES={machine,operator}、`:18-24` column_name(machine→machine_id/operator→operator_id/其余含 team→'' 空串)、`:43-71` normalize 对『有类型无 id』(:65-70 raise 缺少资源编号)/『不支持类型含 team』(:59-64 raise 只支持设备或人员)/『有 id 无类型』(:53-58 raise)三路 loud raise。(3) ⚠️repo 内联映射:报告写 `schedule_plan_query_repo.py:447-463`,实盘 `list_dispatch_rows` 方法体 `:430-469`,scope_type_text/scope_id_text 解析 :447-448,operator 字面量 `s.operator_id` 含空 id 全量分支 `<> ''` 在 :451-455,machine 字面量 `s.machine_id` 含空 id 全量分支在 :456-460,**team 双 join `((o.team_id = ?) OR (m.team_id = ?))` 在 :461-463**(Phase1 已纠为 430-469/451-463,本次实证一致)。(4) 服务调用点 ⚠️报告写 :431,实盘 `get_dispatch_payload` 调 `list_plan_dispatch_rows_for_resolution` 在 `resource_dispatch_service.py:424`(Phase1 已纠)。(5) 另一轨(超期/明细)✅:`schedule_plan_query_repo.py:391-406`(list_overdue_base_rows)经 `overdue_resource_filter`→`schedule_resource_sql_filters.py:36` 用 `resource_filter.column_name`,坐实双轨对 operator/machine 列字面量各写一套。(6) query_service 中继(漂移文件,已复核):`schedule_plan_query_service.py:387-407` list_plan_dispatch_rows_for_resolution 在 :387、repo 调用在 :399、透传 scope_type/scope_id 在 :406-407。team 双 join 依赖的 o/m 别名只在 `schedule_detail_query.py:70-75`(include_team_context=True 分支,LEFT JOIN Machines m / Operators o)存在,column_name 单列属性结构上表达不了。
- **具体修法**：【三步分段、本轮只产计划不改码;真正字面量收敛收尾在收口点扩容+回归绿后】。步骤1=扩收口点(给 ScheduleResourceFilter/normalize_schedule_resource_filter 加 team 轴表达力):column_name 单列属性不够,新增一个能产出 `((o.team_id = ?) OR (m.team_id = ?))` 双 join 谓词 + 参数序列的接口(如 where_predicate(scope_type, scope_id)→(sql_fragment, params)),并放开『类型有、id 空=全量』语义(当前 collar :65-70 对空 id 强制 raise,会打断派工的『全部人员/全部设备』视图,smoke :125/133 证该空 id 全量视图是活的)。注意空 id 全量必须产出 `TRIM(COALESCE(s.operator_id,'')) <> ''` 这类显式 WHERE 而非『不加谓词』——后者=灵魂线静默返全量。步骤2=先补回归断言(钉死反例,纯增量):①scope_type=team&team_id=X 只返回该 team 的行而非全量(直接打 list_dispatch_rows/data 端点对比 team=X vs 无谓词的行数差);②全部人员/全部设备空 id 视图仍可查且不 raise。步骤3=仅在 collar 同时覆盖 team 双 join + 空 id 全量后,才把 `schedule_plan_query_repo.py:451-463` 的 s.operator_id/s.machine_id 字面量与 team 三分支、以及 `schedule_repo.py:141-153`(R34 territory)收敛到 collar 新接口,并评估 `_normalize_scope_type:63-67` 是否委托给扩容后的 collar(保留 team + team_axis 两套语义)。承重逐字注释钉在 `schedule_plan_query_repo.py:461` team 双 join 上方:『# 故意保留:team(班组)轴是 ((o.team_id) OR (m.team_id)) 双 join,column_name 单列属性表达不了,塌缩到收口点会让班组筛选静默返全量(坏数据)或整页 500;空 id 分支 :454-455/:459-460 的 `<> \'\'` 是"类型有/id 空=全量"承重语义,收口点扩容覆盖前严禁删。』
- **爆炸半径**：炸到生产:`/scheduler/resource-dispatch` 页面 + `/data` JSON(operator_rows/machine_rows/cross_team_rows) + `/export` 三入口;生产消费方 resource_dispatch_service.py:284/368/424、resource_dispatch_execution_service.py:65/95/142、schedule_plan_query_service.py:376/387/399。炸到测试:tests/test_scheduler_resource_dispatch_smoke.py:177-211(team 活用例 operator_rows==1/machine_rows==0/cross_team_rows==1)+ :120-135(空 id『全部人员』活视图);tests/regression_scheduler_resource_dispatch_invalid_query_cleanup.py:341(直调 svc._normalize_scope_type('bad'));tests/regression_report_context_filters_contract.py(collar 别名优先级);tests/regression_schedule_service_facade_delegation.py:39(守 R34 兄弟方法返回类型)。
- **跨债依赖**：【强前置·跨桶硬边 R05→R34(B12)】:R34 删/收敛 `schedule_repo.py:128-158 list_dispatch_rows_with_resource_context`——该方法 :151-153 有与 repo 完全相同的内联 team 双 join。R05 必须先给 collar 扩 team 双 join,R34 收敛到 column_name 才不会丢 team 谓词(否则 team 静默返全量坏数据,R05 修法也连带失效)。故全局排序 Batch-8(B07) 必须先于 Batch-13(B12 的 R34)。【同桶】与 R67 同桶但不同收口点、零冲突,可并行。【无同文件跨桶边】(cross_bucket_same_file_edges=[])。
- **必补回归测试**：必补(纯增量,Phase2 这步先落):①新增 scope_type=team&team_id=X『只返回该 team 行、行数 < 全量』的负向回归(钉死静默丢谓词反例);②空 id『全部人员/全部设备』视图仍可查不 raise 的回归;③scope_type=team 与 scope_type=operator/machine 在同数据集下结果集互斥的契约。必保持绿:smoke :177-211 team 全套、:120-135 空 id 全量、invalid_query_cleanup:341 直调、report_context_filters_contract collar 别名、facade_delegation:39 R34 兄弟返回类型。扩 collar 后须有契约断言『column_name 仍对 team 返 ''(不被误改成单列)』+『新双 join 接口对 team 产出含两个 ? 占位与两个 team_id 参数』。
- **误修风险与防呆**：未来 LLM 最可能的 3 种改错:(1)『统一』把 _normalize_scope_type:63-67 直接换成 normalize_schedule_resource_filter——丢 team 轴(collar :59-64 对 team raise)→班组视角整页 500;(2)强让 repo.list_dispatch_rows 复用 column_name 而漏 team 分支——column_name('team')=='' 但 has_filter('team')==True,WHERE 子句被静默剥离→班组返全量坏数据(踩灵魂线静默兜底);(3)扩容时把空 id 全量做成『不加任何谓词』而非显式 `<> ''`——同样静默返全量。本地防呆=①承重逐字注释钉在 :461 team 双 join 与 :454-455/:459-460 空 id 分支上方;②先落的负向回归(team≠全量、空 id 可查)在塌缩动作下立刻红;③collar 契约测试钉死 column_name('team')=='' 不变 + 新双 join 接口产双占位双参数。绝不允许为『统一好看』给 collar 的 missing-id raise 加兜底默认值(应是显式全量语义的新参数/新方法,不是吞掉 raise)。
- **落地批次/优先级**：Batch-8(B07 资源筛选 collar,team-extend 先行),桶内优先级 1(承重 + 跨桶前置)。全局必须先于 Batch-13(B12 R34)。投工分 3 原子小步(扩接口→补回归→才收敛字面量),本轮 Phase2 计划聚焦前两步(纯增量、零数据风险),第三步字面量收敛留待收口点扩容验证通过。

### R67 · P3 维护面重复:报表资源筛选的 6 个请求参数别名清单(resource_ty · sev=low · load_bearing=False

- **当前位置(已复核)**：✅准(3 处报告点名全在,文件均干净=基线 b08162cd)+ ⚠️发现疑似第 4 处(报告/pack 漏列,R54-style 教训):(1)✅`web/request_resource_context.py:20-28` `_request_resource_values()` 返回 6 键 dict(resource_type/resource_id/scope_type/scope_id/machine_id/operator_id),:39 喂收口点 normalize_report_resource_filter(**values)。(2)✅`web/routes/reports_request_support.py:53-61` `request_resource_filter()` 同 6 键逐个 request.args.get 喂收口点(:54-60)。(3)✅`web/routes/reports_export_support.py:13-29` `_EXPORT_CONTEXT_KEYS` 是 15 键元组,把同样的 6 资源键嵌在 :23-28(其余是 plan_id/back_to/日期/batch_id),:38-40 拼导出 URL。(4)⚠️**疑似第 4 处**`web/viewmodels/scheduler_navigation_links.py:11-28` `_REPORT_CONTEXT_FIELD_NAMES` 是 16 键 passthrough 元组,把同样的 6 资源键嵌在 :22-27(其余是 plan_id/back_to/scenario_id/日期/batch_id)。收口点 ✅`core/services/report/report_context_filters.py:119-148` normalize_report_resource_filter 未被绕过(非 P5,关键字参数恰为这 6 个名)。【明确排除·不是 R67 同概念,勿动】:`scheduler_resource_dispatch_query.py:12-30`(_SCOPE_ARG_KEYS/_EXPORT_ARG_KEYS 用 scope_id/operator_id/machine_id/team_id/team_axis 派工轴词汇,属 R05 族)、`scheduler_workbench_link_query.py`(动态拼 query 元组,非字面清单)。
- **具体修法**：P5 收口?否——收口点(normalize_report_resource_filter)没被绕,**不动收口点本身**。修法=抽单一别名键清单常量到唯一 home,多处 import 复用。具体:在收口点同模块 `core/services/report/report_context_filters.py` 定义 `REPORT_RESOURCE_FILTER_ARG_KEYS = ("resource_type","resource_id","scope_type","scope_id","machine_id","operator_id")`(与收口点的 6 个关键字参数同生共死,改名一处即触双改),并导出。然后:①`request_resource_context.py:20-28` 改成 `{k: _request_arg(k) for k in REPORT_RESOURCE_FILTER_ARG_KEYS}`;②`reports_request_support.py:53-61` 改成 `normalize_report_resource_filter(**{k: request.args.get(k) for k in REPORT_RESOURCE_FILTER_ARG_KEYS})`;③`reports_export_support.py:13-29` 的 superset 用解包组合 `_EXPORT_CONTEXT_KEYS = ("plan_id","back_to","date_from",...,"batch_id", *REPORT_RESOURCE_FILTER_ARG_KEYS)`,保留非资源键不变;④(若 owner 裁定第 4 处入账)`scheduler_navigation_links.py:11-28` 同样 superset 解包组合。注意 ②③ 是 superset 嵌入,**不能整元组替换**,只能把其中 6 资源键段替换为解包常量。分层:web→core 导入(web/viewmodels/routes import core.services.report),是正常允许方向,无 AST 越层(request_resource_context.py:7 已经 import 该模块)。
- **爆炸半径**：仅维护面(blast=low,不损坏数据)。下游消费的是函数 request_report_resource_context/request_resource_filter/current_report_export_url 而非这份 key 清单,故不受抽常量影响:request_report_resource_context 被 navigation_context.py:55/dashboard.py:169/scheduler_week_plan_query.py:22/scheduler_analysis.py:26/scheduler_navigation_publish.py:48 消费;request_resource_filter 被 reports_page_support.py:59,371/reports_export_routes.py:32,60,100,128 消费——全部无感。
- **跨债依赖**：【同桶】与 R05 同桶不同收口点、零冲突,可完全并行(fix_invalidation_risk=none)。【无跨桶同文件边】(cross_bucket_same_file_edges=[])。【弱关联】第 4 处 scheduler_navigation_links.py 属 SCC-NAV 文件群边缘,但 _REPORT_CONTEXT_FIELD_NAMES 与 B01 的 plan-guard 字段无关(纯 passthrough 别名),抽常量不碰 build_workbench_plan_context/guard 字段,与 B01 不冲突。
- **必补回归测试**：必保持绿:tests/regression_report_context_filters_contract.py:87-105(收口点别名优先级契约:machine+operator_id 冲突 raise、machine+machine_id 冲突 raise、resource vs scope 别名归一)——因常量与收口点 6 个关键字参数绑定,改常量若漏改收口点签名此测试即红。建议补一条轻量契约:断言 `set(REPORT_RESOURCE_FILTER_ARG_KEYS) == set(inspect.signature(normalize_report_resource_filter).parameters) - {位置参数白名单}` 或直接断言常量恰等于这 6 个名,钉死『常量=收口点入参清单单一真相源』。
- **误修风险与防呆**：未来 LLM 最可能改错:(1)把 reports_export_support._EXPORT_CONTEXT_KEYS / scheduler_navigation_links._REPORT_CONTEXT_FIELD_NAMES 这两个 **superset** 元组整体替换成 6 键常量——会压扁丢掉 plan_id/back_to/日期/scenario_id/batch_id,导出 URL/导航 passthrough 漏键;正确是只替换其中 6 资源键段(解包 *REPORT_RESOURCE_FILTER_ARG_KEYS)。(2)只改报告点名的 3 处、漏掉第 4 处 scheduler_navigation_links.py:22-27——重蹈 R54『漏第 4 套手维列表』覆辙,债残留。(3)误把 scheduler_resource_dispatch_query.py 的派工轴 _SCOPE_ARG_KEYS(scope_id/operator_id/machine_id/team_id)也当成同一清单合并——词汇不同(派工轴含 team_id 无 resource_type),合并会污染两个概念。本地防呆=常量与收口点同模块同生共死 + 上述契约断言钉死单一来源 + 修时 grep 全仓 `"scope_type"` 字面量复核所有嵌入点(本计划已 grep 出 4 处真+2 处排除)。
- **落地批次/优先级**：Batch-8(B07),桶内优先级 2(独立叶子、low/纯增量抽常量)。Batch-1 之后任意时点可落,可与 R05 并行,无任何前置/被前置约束。

**本单元待 owner 裁断**：
- R67 第 4 处入账与否需 owner 裁断:`web/viewmodels/scheduler_navigation_links.py:11-28` 的 16 键 passthrough 元组 _REPORT_CONTEXT_FIELD_NAMES 把同样 6 个资源别名键嵌在 :22-27,报告/pack 只点名 3 处(漏列,与 R54『漏第 4 套手维列表』同型)。它是『报表资源别名清单』的第 4 处硬编码,但身份是 nav passthrough superset(混 plan_id/scenario_id/日期),可能被视为不同概念。裁断:R67 收口范围是否纳入第 4 处?纳入则需 superset 解包改造(不可整元组替换)。倾向纳入(同一份『哪些 key 是资源别名』知识,漏改即半截)。
- R67 常量落点需 owner 确认:推荐放在收口点同模块 core/services/report/report_context_filters.py(与 normalize_report_resource_filter 的 6 个关键字参数绑定、同生共死)。备选=新建一个 web 层小常量模块。前者更优(单一真相源贴着收口点),但需确认不违反红线 3『为已有概念新建第二模块』——抽『常量』不是『新建第二收口模块』,R67 本身非 P5(收口点没被绕),应不触红线,请 owner 确认此判断。
- R05 收口点扩 team 轴的接口形态需 owner 拍板:column_name 单列属性表达不了 team 双 join,需新增『谓词生成』接口(产出 (sql_fragment, params))。这是给 ScheduleResourceFilter 加方法、还是新建 build_resource_where_predicate 函数?需确认不被误判为『为已有概念新建第二模块=制造新 P5』——本债 precondition_to_change 明确批准『扩容收口点』,应属合法扩容而非新建第二点,但接口落点(model 内方法 vs model 同模块函数)请 owner 定,避免与分层红线冲突(谓词生成属 model 层纯逻辑,不引入 data/service 依赖)。
- R05 步3 字面量收敛是否本轮 Phase2 落地、还是只留计划:因 verdict=load_bearing+refuted=False+本轮『只读只产计划绝不改生产代码』,步1扩接口/步3收敛都属改生产码,严格说本轮全部只产计划。但步2补回归是纯增量测试——owner 是否允许本轮先把负向回归(team≠全量、空 id 可查)作为安全网先行落地(对齐 Batch-1『纯增量测试网先铺』精神),还是连测试也只列计划?
