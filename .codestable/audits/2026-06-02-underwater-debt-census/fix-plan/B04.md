# B04 · boolean承重 + enum第二份

> 逐债细颗粒度修复计划。复核基线 b08162cd；每条 file:line 落地前回盘复核。承重=补注释+绑测试；P5=收口已存在点；P6=grep证零引用直删；P4=raise/可观测。

**本单元策略**：本桶两条债同属"枚举/布尔归一被实现成第二套"，但红线相反，必须分开走，绝不能当成同类一锅端。

LB04（boolean_normalize 双实现）= 分层承重，load_bearing=true，唯一合法动作=补"我是故意的"注释 + 新建一条把两套 wide 归一绑死的契约测试，零删除/零合并。我已回盘逐行核对 boolean_normalize.py:33 与 normalization_matrix.py:168 确为语义全等（同 wide 别名集、同 是/否 短路、同 None→default、同空串→NO、同 default/passthrough/raise/no 四分支）。报告引以为据的 layer_edges.json 在盘上【不存在】（与 PHASE1 §0.3 R29 授权链断裂同病），故我直接 grep 重证了分层不变量：core/models 零 import core.services、core/shared 零 import core.models、matrix.py:5 反向 import core.models.enums.YesNo——三条共同坐实"删 shared 改指 services 必造成 core.models→core.services 越层 + 导入环"，承重判定成立。PHASE1 FINDING-ERROR 复核为真：core/algorithms 全仓零 to_yes_no/boolean_normalize 消费，报告"greedy 经 number_utils 引用"是假，真消费方全在 core.services.scheduler + core.models.coercion。tests/ 全仓零绑定 = 债本身。LB04 落 Batch-1（纯增量 ROOT）。

R41（enum_display + process_bp 中文标签第二套）= 真债 P5，NOT load_bearing，收口到【已存在】enum_normalizers 标签函数。但这条收口不是机械 1:1：(a) 三处语义已漂移，其中两处踩灵魂线（ready_zh 把未知/None 静默贴成确定态"未齐套"、_source_zh 不归一导致"外协/外"误判"自制"），收口方向正确且能顺手治灵魂线，但 test_enum_display_consistency.py:59/61/23 的钉死断言必须改成 loud 暴露而【不是】删了重钉静默兜底；(b) 8 个符号里只有 6 个有收口点（machine/operator/ready/priority/day_type/source），batch_status_zh 与 _merge_mode_zh 在 enum_normalizers 里【没有对应 label】，给它们新建 = 制造新 P5（红线3禁止），必须显式划出 R41 范围之外；(c) 个别符号收口反而引入新静默默认（day_type_zh('')='-' 收口后变 calendar_day_type_label('')='工作日'），需逐符号判语义、不能一刀切删。R41 落 Batch-5，受 owner 语义裁断门控。

内部排序：LB04 先（Batch-1，低风险纯增量，且其契约测试范式是安全网）；R41 后（Batch-5，须等 owner 拍板"哪个语义对"才能动）。两债文件不重叠、cross_bucket_same_file_edges=[]，互不撞行号。

**内部排序**：LB04 先、R41 后。理由：(1) LB04 是 Batch-1 纯增量 ROOT（补注释+契约测试，零生产逻辑改动，零风险），且它建立的 parity 契约测试范式是后续安全网；(2) R41 是 Batch-5 行为收口，会改页面文案+测试钉死断言，且强依赖 owner 先裁断'哪个语义对'（停用/休假 vs 停用、ready 未知口径、day_type 空串默认），未拍板不能动手。两债文件不重叠、无同文件边，技术上可并行，但落地顺序上 LB04 无门槛先落、R41 待裁断后落。

### LB04 · P5（承重，load_bearing=true，_adv_refuted=fal · sev=medium · load_bearing=True

- **当前位置(已复核)**：✅准 core/shared/boolean_normalize.py:33 def normalize_yes_no_wide（盘上 Read 确认）；对照实现 core/services/common/normalization_matrix.py:168 def normalize_yes_no_wide_value ✅准；core/services/common/enum_normalizers.py:172 normalize_yes_no_wide 是薄 shim（delegate 到 matrix:168，非真第三实现）✅准。逐行同义已盘上核实：wide 真集 boolean_normalize.py:7 ('y','yes','true','1','on') == matrix.py:40 _YES_NO_TRUE_NARROW+('on',)；wide 假集 :8 ('n','no','false','0','off','') == matrix.py:41 _YES_NO_FALSE_NARROW+('off','')；None→default（:42-43 vs :177-178）、空串→NO（:48-49 vs :181-182）、是/否短路（:50/52 vs :183/185）、default/passthrough/raise/no 四分支（:55-62 vs :188-195）全等。两文件 git diff vs b08162cd 未漂移，行号准。
- **具体修法**：唯一合法动作=注释+契约测试，零删除/零合并/零透传。(1) 把 §90 LB-B1（report-sections/90-load-bearing.md:95-98）已起草的'我是故意的'注释逐字钉在 core/shared/boolean_normalize.py:33 def 上方：'# 这不是 normalization_matrix.normalize_yes_no_wide_value 的冗余副本：/ # core.models/core.algorithms 在下层只能调本函数，删它改指 core.services 会撞分层红线。/ # 两套必须保持语义等价——靠 regression 契约测试绑定，勿单边修改别名集/枚举值。'（注：注释提到 core.algorithms 是分层前瞻论据，盘上 core.algorithms 当前零消费，见 open_questions，建议保留原文但 owner 可裁是否收窄）。(2) 新建契约测试 tests/regression_boolean_normalize_wide_parity_contract.py（纯增量）：同时 import core.shared.boolean_normalize.normalize_yes_no_wide 与 core.services.common.normalization_matrix.normalize_yes_no_wide_value，断言二者在全 wide 别名集（y/yes/true/1/on、n/no/false/0/off/''、是/否）× None × 未知值（'weird'/'外'/'2'）× 全 unknown_policy（default/passthrough/raise/no/空串）× default 参数变体 × 自定义 true_aliases/false_aliases 上逐一同值；unknown_policy='raise' 分支断言两者同抛 ValueError。测试 docstring 写明'两套故意并存=分层承重，单边改别名集/枚举值即触发本测试红灯'，防未来 LLM 读测试时'简化'掉它。
- **爆炸半径**：改动面=零生产逻辑（仅注释1处+新测试1个文件）。绑定的两套实现的活消费方（仅作测试覆盖参考，不改）：shared 侧 core/models/schedule_config_runtime_coercion.py:6 import（用于 :192/:201/:230）+ core/services/scheduler/number_utils.py:5 import→:17 to_yes_no 包装→下游 resource_pool_builder.py:89/280/340、config/config_field_coercion.py、run/optimizer_config.py:146、run/schedule_input_collector.py、run/freeze_window.py；matrix 侧（经 enum_normalizers:172 shim）core/plugins/manager.py:11/20、core/services/system/system_config_service.py:8/31、core/services/personnel/operator_machine_query_service.py:6/50。语义面：auto_assign/freeze_window/ortools 开关、is_primary 主操作工、plugin enabled、system_config。当前测试零引用（这正是债）。
- **跨债依赖**：co_change=R41（同桶但完全独立：不同文件、无收口冲突、可并行）。无 cross_bucket_same_file_edges。fix_invalidation_risk=none（纯增量，不影响他债）。反向风险：若被错误执行成'删 shared 改指 services'，会连带击穿 R29（core.services.common/number_utils 同层）的分层前提。LB04 是 Batch-1 全局 ROOT 安全网的一部分，应在任何收敛/删除动作前落地。
- **必补回归测试**：新建 regression_boolean_normalize_wide_parity_contract.py，断言两套 wide 归一在 全wide别名集×是/否×None×未知值×全unknown_policy(default/passthrough/raise/no)×default变体×自定义aliases 上逐一同值（YesNo 枚举值或任一别名集单边改动→红）。验收：现有契约全绿 + 新 parity 契约绿 + git diff 仅注释+新测试（零生产逻辑改动）。
- **误修风险与防呆**：最高频误修=未来 LLM 见'两份逐字同义实现'判 DRY 违规→删 boolean_normalize、把 core/models/schedule_config_runtime_coercion.py:6 改指 core.services.common.normalization_matrix→立刻 core.models→core.services 越层 + 导入环（matrix.py:5 反向 import core.models.enums.YesNo），击穿 AST 0 违规。第二误修='统一方向'搞反，把 matrix 当源删 boolean_normalize 逻辑（唯一合法消重=matrix.normalize_yes_no_wide_value 反向 delegate 到下层 boolean_normalize，services→shared 合法下行，保 shared 为唯一源）。第三误修=只在一侧别名集加新别名（如 matrix 加但 shared 没加）→静默漂移。防呆：注释钉死 boolean_normalize.py:33 + 契约测试（单边改即红）+ 测试 docstring 解释为何双存（防 LLM'简化'测试）。绝不能借'核对注释'发现 core.algorithms 当前无消费就反推注释错、进而删模块。
- **落地批次/优先级**：Batch-1（承重注释+契约/parity 测试网，全局 ROOT，纯增量零结构）。与 LB01/LB02/LB05/LB06/LB07/LB08/LB03/R56/R58/R03 同批，互不改同一行段，可并行。是后续所有收敛/删除动作的前置安全网之一。

### R41 · P5（needs_adversarial=false；非承重） · sev=medium · load_bearing=False

- **当前位置(已复核)**：✅准（与 PHASE1 §0 纠偏一致，且 git diff vs b08162cd 为空=未漂移，基线行号准）。web/routes/enum_display.py：machine_status_zh:13 / operator_status_zh:24 / day_type_zh:33 / batch_status_zh:47 / priority_zh:62 / ready_zh:73（报告写'13-79'是区间，末符号 ready_zh 起 :73 非 :79，符号头全核实）。web/routes/process_bp.py：_merge_mode_zh:16 / _source_zh:22（报告 :16-25 准）。收口点 core/services/common/enum_normalizers.py 盘上确认：operator_status_label:82 / machine_status_label:124 / source_type_label:159 / batch_priority_label:209 / ready_status_label:220 / calendar_day_type_label:231 均在。⚠️关键盘上发现：batch_status_zh 与 _merge_mode_zh 在 enum_normalizers【无对应 label】（grep batch_status_label/MergeMode label = 全仓 NONE）。
- **具体修法**：收口到 enum_normalizers 已存在标签函数（仅限有收口点的 6 符号）+ 同步把钉死测试改 loud 暴露。按符号：(1) operator_status_zh→operator_status_label，⚠️含语义裁断（'停用/休假'→'停用'，见 open_questions，须 owner 先定）；(2) ready_zh→ready_status_label，治灵魂线：未知/None 由静默'未齐套'改为暴露（passthrough 原值/'未知'），test :59/:60/:61 改成断言 loud 暴露而【绝不】删了重钉静默；(3) _source_zh→source_type_label（先经 normalize_op_type_category），治灵魂线：'外协/外'不再误判'自制'；(4) machine_status_zh→machine_status_label（未知 '-'→'未知'，且补中文别名归一）；(5) priority_zh→batch_priority_label（未知 '未知'→原值暴露）；(6) day_type_zh→calendar_day_type_label，⚠️注意空串收口后由 '-' 变 '工作日'（canonical 引入静默默认），需 owner 裁是否保留空串→'-' 分支（见 open_questions）。收口落点建议在 enum_display.py 源头让 7 函数 delegate 到 canonical（单文件改动最干净），消费方经 *_bp.py 下划线包装器（_operator_status_zh 等）自动透传。【明确划出范围外】batch_status_zh + _merge_mode_zh 无收口点，保持独立，不得为它们新建 label（=新 P5，红线3）。
- **爆炸半径**：生产消费方（已盘上复核）：equipment_bp.py:9→equipment_pages.py:33/51/111/230/276；personnel_bp.py:9→personnel_pages.py:68/136、personnel_detail_context.py:42/70/91、personnel_calendar_pages.py:33；scheduler_bp.py:9（经 _priority_zh/_ready_zh/_batch_status_zh/_day_type_zh 包装）→scheduler_batches.py:149/150/151、scheduler_batch_detail.py:249/250/251、scheduler_calendar_pages.py:30；process_bp.py:16/22→process_parts.py:17/166/167/288。收口后页面状态列文案变化：operator 停用/休假→停用、ready 未知/None 未齐套→未知/原值、source 未归一别名 自制→正确归一、day_type 空 -→工作日。同概念双口径实证：material.py:124/131 已用 canonical ready_status_label，scheduler_batches.py:150 用 ready_zh（同一齐套状态两套函数）；导出路径（personnel_excel_operators.py:347 operator_status_label、scheduler_excel_calendar.py:56 calendar_day_type_label）已收口、显示路径未收口。测试：test_enum_display_consistency.py:14-61（唯一钉死/续命测试，全仓仅此一处 pin enum_display）。lb_colocation=none（web/routes/* 非承重文件）。
- **跨债依赖**：co_change=LB04（同桶，独立）。无 cross_bucket_same_file_edges。须在 Batch-1（LB04 等承重注释+安全网）之后落。fix_invalidation_risk=none。受 owner 语义裁断门控（见 open_questions）才能动手。
- **必补回归测试**：改写 test_enum_display_consistency.py：:23/:24 operator inactive 期望值按 owner 裁断更新；:59/:60/:61 ready 未知/'weird'/None 改为断言 loud 暴露（'未知'或原值暴露），【绝不】重钉静默'未齐套'。新增负向契约：ready_zh('weird') 不再静默'未齐套'、_source_zh('外协') 归一为外协而非误判'自制'。新增防再漂移契约：对有收口点的符号断言 enum_display.<fn>(x)==<canonical_label>(x)（收口后两者不可再分叉）；无收口点的 batch_status_zh/_merge_mode_zh 不纳入此契约（保持独立）。验收：坏 ready/operator/source 值改为 loud 暴露断言绿（符合灵魂线）。
- **误修风险与防呆**：误修1（灵魂线）：未来 LLM 见两套标签'统一'但选错语义源——以为 enum_display 静默兜底是对的，把 ready 未知重钉成'未齐套'，复发自欺。防呆=测试 :59/:61 改 loud 暴露 + 注释说明未知 ready 不得冒充确定态'未齐套'。误修2（红线3）：给 batch_status_zh/_merge_mode_zh 新建 enum_normalizers label 凑'统一'=制造新 P5。防呆=计划显式标注此二符号无收口点、范围外。误修3：盲删 enum_display 全量改指 canonical，未经产品确认就改页面文案（停用/休假→停用）=用户可见回归。防呆=owner 先裁语义（open_questions）。误修4：'对齐'时改 canonical operator_status_label '停用'→'停用/休假' 去迁就 enum_display，会连带改导出（personnel_excel_operators.py:347），炸更宽。防呆=owner 明确哪个是 canonical UI 文案。误修5：day_type_zh 收口时连空串分支一起收，引入 ''→'工作日' 静默默认（比原 '-' 更不诚实）。防呆=逐符号判语义、空串分支单独裁。
- **落地批次/优先级**：Batch-5（B03 config 双栈+adapter+B04 enum 收口；与 R45/R47/R48/R71 同批，不同文件群内部冲突自由）。须在 Batch-1（LB04 等）之后。⚠️注意 PHASE1 §4 Batch-5 标题写'B03+B04收敛'，但 B04 的 LB04 实为 Batch-1 纯增量、Batch-5 的 B04 动作仅 R41。R41 动手前须先过 owner 语义裁断门。

**本单元待 owner 裁断**：
- R41 语义裁断1（operator inactive）：enum_display operator_status_zh 把 INACTIVE 显示为'停用/休假'，收口点 operator_status_label 显示为'停用'。收口=页面文案改'停用/休假'→'停用'；反向对齐 canonical→'停用/休假'会连带改导出（personnel_excel_operators.py:347）。哪个是正式 UI 文案？须 owner 拍板才能定收口方向。
- R41 语义裁断2（ready 未知/None）：enum_display ready_zh 把未知/None/'weird' 静默贴成确定态'未齐套'（灵魂线自欺）；收口点 ready_status_label 对未知 passthrough 原值、None→'未知'。收口方向（暴露未知）符合灵魂线，但 test_enum_display_consistency.py:59/61 钉死了静默'未齐套'，须确认改成 loud 暴露断言（而非删了重钉）。owner 确认未知 ready 的正确呈现口径（'未知' or 原值暴露）。
- R41 语义裁断3（_source_zh）：process_bp._source_zh 不调 normalize_op_type_category，'外协/外/EXTERNAL大写' 等未归一别名会落 else 误判'自制'（灵魂线）。收口到 source_type_label 可治。确认收口（owner 一般应同意，因这是明确的坏标签 bug）。
- R41 范围外二符号：batch_status_zh（enum_display:47）与 _merge_mode_zh（process_bp:16）在 enum_normalizers 无对应 label（盘上 grep batch_status_label/MergeMode label=全仓 NONE）。给它们新建 label = 新 P5（红线3禁止）。默认=此二符号保持独立、划出 R41 收口范围；除非 owner 明确批准新建 batch_status_label/merge_mode_label 作为受控例外。
- R41 语义裁断4（day_type 空串）：day_type_zh('')='-'，收口点 calendar_day_type_label('')→normalize default=WORKDAY→'工作日'（canonical 自带的静默默认，比 '-' 更不诚实）。收口 day_type 时空串分支要不要跟随？owner 裁：保留 ''→'-'（不收空串分支）或接受 ''→'工作日'。
- LB04 注释文案：§90 LB-B1 起草的注释提到 'core.models/core.algorithms 在下层只能调本函数'，但盘上 grep 证 core/algorithms 当前【零】to_yes_no/boolean_normalize 消费（PHASE1 FINDING-ERROR 已实证），活消费方只有 core.models.coercion + core.services.scheduler。注释作为分层前瞻论据仍成立（algorithms 若用也只能用 shared），但保留原文有'未来 LLM 核对注释→发现无 algo 消费→误判注释错→删模块'的风险。owner 裁：逐字保留（红线2要求复用文案）还是收窄为 'core.models'？
- LB04 邻接微债（非本桶）：enum_normalizers.py:168-169 自带一份 _YES_NO_TRUE_WIDE/_YES_NO_FALSE_WIDE 元组，但 :172 shim 只 delegate 到 matrix、不使用这两元组=第三份死字面副本。不在 B04 范围（LB04 只补注释+测试），但与 LB04 同概念。是否另立 trivial 死码清理？默认=不在本计划处置，flag 给 owner，绝不扩 LB04 范围。
