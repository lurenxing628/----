# 2026-07-19 盲区 6 维度扫描终稿（blindspot-sweep-ultra）

## 背景与方法

承接同日 full-defect-sweep 批评家提名的 5 个盲区维度（该轮 14 维度全在源码语义层），本轮切 6 维度：进程互斥(proc-mutex)、备份完整性(backup-integrity)、内存增长(mem-growth)、壳生命周期(shell-lifecycle)、frozen 打包一致性(frozen-packaging)、报表聚合正确性(report-aggregates)。方法同前：维度探子(硬预算 grep-first)→去重→双镜头对抗核实(镜头A危害链怀疑者/镜头B裁决登记核对者)→分歧仲裁→批评家。首跑 find:proc-mutex 因 API 断流失败，经 TaskStop+resumeFromRunId 缓存续跑完成；2 条 shell-lifecycle 候选的核实/仲裁代理再次 API 断流，改由主会话单独补派对抗核实代理完成终审。

## 终局结论

**探子 6/6 维度产出原始 15 条，去重 14 条；终局确认 14 条（1 high / 8 med / 5 low），驳回 2 条。** 其中 10 条由 workflow 双镜头核实+仲裁定案，2 条 shell-lifecycle 由主会话补派核实定案（一条自 high 降档 low，一条维持 low），2 条为补位批评家新增、批评家自核+主代理抽查决定性证据后定案（B13 med / B14 low）。

## 确认清单（按严重度排序，前 10 条为 workflow 定案）

### B01 [high] [backup-integrity] core/infrastructure/backup.py:512-527; core/services/system/maintenance/cleanup_task.py:99-109; core/services/system/system_maintenance_service.py:102-139; core/services/system/maintenance/backup_task.py:149-178; web/routes/system_backup.py:285-289

**备份轮换修剪(keep_days)无任何『至少保留 N 份』保底,且自动清理不以当轮备份成功为前提,可把仅存的好备份全部删光**

- 危害链：单机离线软件停机超过 keep_days(默认7天,春节/假期/产线检修停机是常态)后开机:所有现存备份 mtime 均早于 cutoff。最坏链:停机期间断电/坏道致主库损坏 → 开机后步骤1自动备份因 integrity_check 失败或打不开而失败(异常被吞,返回 False) → 步骤2自动清理照常执行,把全部旧备份(含唯一好备份)按 mtime 删除(单轮 MAX_BACKUP_DELETE_PER_RUN 限速,几轮后删光) → 用户手里只剩一个损坏主库和空备份目录,数据永久丢失,无任何可恢复路径。次坏链:主库健康时,开机第一轮备份成功后清理立即把历史备份链清零只剩当轮1份,若当前库已有未察觉的逻辑性损坏(误删除数据),可回滚的历史版本同时消失。用户点手动『清理』按钮同样触发。这正是任务书点名的『keep_days 把唯一好备份删掉』:修剪逻辑缺一个『至少保留最新 N 份完好备份』的下限。
- 证据：cleanup_old_backups (backup.py:513,525-526): `cutoff = datetime.now() - timedelta(days=self.keep_days)` → `if file_time < cutoff: remove_fixed_file(filepath...)`,纯按 mtime 删,零保底。自动路径 cleanup_backups_with_limit (cleanup_task.py:99-100,107-109) 同样 `if mtime < cutoff: candidates.append(...)` 后逐个删,只有单轮限速 max_delete,多轮照样删光。调度顺序 (system_maintenance_service.py:102-139):步骤1自动备份、步骤2自动清理是两个独立 due 判定的任务,清理不检查步骤1是否成功;而 maybe_run_auto_backup (backup_task.py:149-173) 备份失败时 `except Exception: ... return False` 吞掉异常继续走,清理照跑。对比同文件日志清理有 min_keep_logs 保底(system_maintenance_service.py:151),备份清理没有对称保护。手动『清理』按钮 (system_backup.py:285-289) 走同一 cleanup_old_backups,同样无保底。且 before_restore/pre_migration 快照文件名同为 aps_backup_*.db 前缀,一并被剪。
- 核实结论（双镜头）：A:逐环核实全部成立,危害链无断点。(1) 两条修剪路径均纯按 mtime 删且零保底:cleanup_old_backups (core/infrastructure/backup.py:513,525-526) 与 cleanup_backups_with_limit (core/services/system/maintenance/cleanup_task.py:73,99-100,107-109,max_delete=200 仅单轮限速,多轮删光);对比日志清理有 MIN_KEEP_LOGS=50 保底 (system_maintenance_service.py:71、cleanup_task.py:141-142),备份清理无对称保护。(2) 调度上步骤1备份/步骤2清理是独立 due 判定 (system_maintenance_service.py:102-139),清理不以备份成功为前提;maybe_run_auto_backup 全异常捕获后 return False (backup_task.py:149-178),run_if_due 忽略之继续清理。(3) 最坏链可达:全仓唯一 integrity_check 在 backup() 内 (backup.py:370-378),启动无完整性门禁,断电致部分损坏的主库可正常服务请求→run_if_due 挂请求钩子 (web/bootstrap/factory.py:402) 触发→备份因 integrity_check 失败→清理照删,7天保留期文件数<200 单轮删光,唯一好备份永久丢失。(4) before_restore/before_migrate/exit 安全快照同用 aps_backup_ 前缀 (backup.py:343,472; migration_runner.py:186; factory.py:139),一并被剪。(5) 手动清理按钮 (web/routes/system_backup.py:285-289) 无条件可用、同样无保底。唯一减损因素:auto_backup_cleanup_enabled 默认 "no" (system_config_service.py:155,179),自动路径需用户开启——但该开关设计即为开启用,keep_days 最低可设 1 天 (MIN_KEEP_DAYS=1),假期停机+断电损坏是本单机离线产品的现实常态场景,且手动按钮不受开关限制。影响为不可恢复的数据永久丢失,维持 high。 | B:未立案、未裁决、非修复中，代码事实属实。逐项核实：(1) 账本 debt_status_2026-07-19.json 唯一备份债是 R32(backup.py:333 integrity_check 执行失败仍升格正式备份,low/planned)——那是'坏备份被升格'，与本候选'无保留下限把好备份删光'是不同缺陷，账本对 cleanup_old_backups/keep_days 保底零登记。(2) 2026-07-19-full-defect-sweep D01-D19 无一覆盖此问题；D15 核实文中'纯按天龄修剪不按数量轮换'只是驳回另一子危害(exit备份按数量顶掉)的事实陈述,非 by-design 裁决；且该报告'下一轮盲区维度'第2条明确承认'备份/恢复/迁移的数据完整性…本轮切法无家可归'，本候选 dim=backup-integrity 正是该未覆盖维度。(3) 无护栏注释、无契约测试锁'无保底'行为(测试仅锁 symlink 安全与 detail 格式)；2026-06-14 finding-01 是已修复的 symlink 问题。(4) 代码现状核实无误且5个文件均不在并行修复改动中：backup.py:513-526 纯 mtime cutoff 零保底；cleanup_task.py:73-107 同型仅 max_delete 单轮限速；system_maintenance_service.py:102-139 备份/清理独立 due 判定,清理不以备份成功为前提,而 :151 日志清理有 min_keep_logs 对称保底；backup_task.py:149-178 备份异常捕获后 return False 流程继续(注:有 error oplog+job_state 留痕,'吞掉异常'措辞略过重,但'失败不阻断清理'成立)；system_backup.py:285-289 手动清理同路径；backup.py:343/:518 证实 before_restore 快照同 aps_backup_ 前缀被一并剪。定级说明：最坏链(停机>keep_days+主库损坏+备份失败+清理照跑→唯一好备份删光,数据永久丢失)终点是不可恢复数据丢失,是单机离线软件最高级用户灾难；减轻因素为两个自动开关默认 no 需用户显式开启,但手动'清理'按钮无需开关即触发同样无保底删除,故维持 high。

### B02 [med] [proc-mutex] web/bootstrap/launcher_contracts.py:239-255

**陈旧 runtime 锁清理存在读后删 TOCTOU:并发双启动可互删对方新锁,双实例同时上线写同一 SQLite**

- 危害链：前置状态:上次进程崩溃/断电留下 pid 已死的陈旧锁(现场真实场景)。用户双击启动器两次或启动器重试,进程 A、B 几乎同时进入 acquire_runtime_lock:A 在第244行读到陈旧锁→判死→第254行删除→第二轮 create 成功持锁返回;B 稍慢,在 A 重建新锁【之后】才执行自己的第254行删除——把 A 刚写好的新锁当陈旧锁删掉,随后 B 第二轮 create 也成功。结果 A、B 都自认持有 runtime 锁,双开防线(entrypoint.py:222-224 注释声称'锁被占的第二实例在触库前即退出')被绕过:两个 Flask 进程各起一个端口、各弹一个窗口,同时对同一 aps.db 做 ensure_schema 迁移和业务写入,退出时各自跑 atexit 备份;两实例内存缓存互不可见,用户在两个窗口分别改排产计划会互相覆盖丢数据。锁文件里只记着 B 的 pid,A 成为无锁运行的幽灵实例,--runtime-stop 也只能停到 B。
- 证据：acquire_runtime_lock 循环体(launcher_contracts.py:239-254):`if _create_new_runtime_lock(lock_path, payload): return payload` 失败后 `existing_result = read_runtime_lock_result(state_dir)` → `_raise_if_runtime_lock_active(existing, ...)` → `_remove_stale_runtime_lock(lock_path)`。其中 _remove_stale_runtime_lock(217-223) 是无条件 `remove_fixed_file(lock_path)`,删除前不重读、不比对锁文件内容是否仍是刚才判定为陈旧的那份 payload。
- 核实结论（双镜头）：A:TOCTOU 属实且危害链完整:launcher_contracts.py:244 读锁→:253 判陈旧→:254 _remove_stale_runtime_lock(:217-223)无条件删除,删除前不重读不比对 payload;O_EXCL 创建本身原子(safe_files.py:321),漏洞只在陈旧清理这一环。该锁是唯一双开防线且先于 create_app/ensure_schema/atexit 备份(entrypoint.py:222-228,注释明确承诺第二实例触库前退出),上游无命名互斥体等其他串行化;获取后无所有权复核(锁重读仅存在于 stop/release 链,launcher_stop.py:238-256),release_runtime_lock 的 pid 比对(launcher_contracts.py:293-294)使被删锁的 A 成为幽灵实例、--runtime-stop 只能停 B,与候选描述一致。窗口非纳秒级理论值:read→delete 之间 Win7 走 tasklist 子进程(launcher_processes.py:39-47),典型 100-500ms,第二进程在此时长内进入即可命中坏交错。severity 由 high 修正为 med:触发需"崩溃遗留死 pid 陈旧锁"+"两次启动亚秒级同时进入"的合取(现实中正相关但单用户桌面发生率低),且最常见路径均安全——活锁第二实例被拒、崩溃后单次重启正常接管、双方读到 MISSING 时 O_EXCL 输家读到赢家活锁后退出。修复方向:删除前重读比对 payload 或 rename-后验证。 | B:未登记未裁决的新问题。(1)账本 debt_status_2026-07-19.json 对 launcher_contracts/runtime_lock/TOCTOU 零命中;(2)full-defect-sweep D01-D19 不覆盖:D02 是 PS 解码致活锁误判死(判定输入错)、D04 是锁序后置(拿锁前做 DB 副作用),本候选是删陈旧锁前不重读复核(删除无守卫),三者修复互不覆盖;concurrency 维度唯一确认项是 D05 static_versioning;驳回清单 4 条无关;并行修复 WP 清单不含此项且 launcher_contracts.py 工作区无改动、近 3 次提交无此修复。(3)无 by-design 裁决:2026-04-30 phase5 启动链专项只裁决坏锁(空/不可读/格式坏)失败闭合不当 stale 清理,未裁决合法陈旧锁的读后删竞态;_remove_stale_runtime_lock(launcher_contracts.py:217-223)无护栏注释;tests/app_runtime/test_shared_runtime_state.py 只锁单进程顺序回收合同无并发测试。(4)代码事实属实:acquire_runtime_lock(239-255)读后无条件删,create 虽用 O_EXCL 原子创建但挡不住 B 删 A 的新锁;加重项——读(244)到删(254)之间要经 _pid_state→Windows 上 subprocess 跑 tasklist(launcher_processes.py:39-64,timeout 8s、实际数百 ms),竞态窗口是子进程调用级而非微秒级,崩溃遗留死锁+双击双启动场景交错可达。severity 从 high 收敛为 med:双实例同写 SQLite/互删/幽灵实例的终态危害成立,但需"崩溃遗留陈旧锁+近同时双启动+不利交错"三重复合条件,按同批 D04 复合条件定 med 的量尺执行。

### B03 [med] [proc-mutex] web/bootstrap/entrypoint.py:209 (+ web/bootstrap/launcher_paths.py:106-110, config.py:30)

**runtime 锁命名空间锚定日志目录而 DB 锚定 APS_DB_PATH/BASE_DIR:两者环境变量分叉时互斥完全失效,双实例确定性同库**

- 危害链：冻结交付机上两种确定性触发:(1) 现场排查时给第二个快捷方式设了 APS_LOG_DIR=D:\tmp(想把日志引出去),第一个实例锁在 ProgramData\APS\shared-data\logs,第二个锁在 D:\tmp——两把锁互不可见,create_fixed_file_exclusive 都成功,两实例同时对同一 BASE_DIR\db\aps.db 跑迁移和业务写入;(2) 两份 exe 拷贝通过 APS_DB_PATH 指向同一共享库文件,锁分别落在各自 runtime 目录,同样双实例同库。与候选1的毫秒级竞态不同,这条只要 env 配置分叉就 100% 复现且长期共存:两窗口并行写排产、双份 atexit 退出备份交错、runtime contract(host/port 文件)各写一份导致 stop/launcher 只认其中一个实例。
- 证据：entrypoint.py:209 `lock_scope_target = prelaunch_log_dir if is_frozen else runtime_dir`;launcher_paths.py:106-110 `resolve_prelaunch_log_dir` 首先取 `os.environ.get("APS_LOG_DIR")`,无则回退 shared-data/logs;而 config.py:30 `DATABASE_PATH = os.environ.get("APS_DB_PATH") or os.path.join(BASE_DIR, "db", "aps.db")`。锁的身份由日志目录决定,被保护资源(数据库)的身份由另一组独立变量决定,二者没有绑定关系。entrypoint.py:207-208 注释只修复了源码运行分叉,冻结分发路径仍以 APS_LOG_DIR 为锁域。
- 核实结论（双镜头）：A:成立但降档。代码事实全部核实:entrypoint.py:209 冻结路径锁域=resolve_prelaunch_log_dir(launcher_paths.py:106-110,APS_LOG_DIR 优先);DB 真实锚点是 factory.py:75 _apply_runtime_config(APS_DB_PATH or resolve_shared_data_root/db/aps.db),候选引用的 config.py:30 只是旧 Config 默认值,但"锁身份与 DB 身份由两条独立 env 链决定、无绑定"结构性成立。锁 payload(launcher_contracts.py:161-169)不含 db_path,分叉后两锁不同目录互不可见,create_fixed_file_exclusive 双双成功。entrypoint.py:222-223 自证该锁是 create_app 触库(迁移/退出备份注册)前唯一双开防线;pick_port(launcher_network.py:200-231)自动换空闲端口,第二实例可长驻,危害链(并发迁移、双实例同库写、双 atexit 备份、contract 分裂致 stop 只停一个)每环可达,确定性无竞态。entrypoint.py:207-208 注释显示同类分叉已在源码路径修过,冻结路径缺口现存。降为 med 的原因:默认配置下锁与 DB 同源于 shared-data root、互斥有效,触发必须人为让两次启动的 APS_LOG_DIR/APS_SHARED_DATA_ROOT/APS_DB_PATH 分叉(现场排查设日志目录是现实场景但非自发);且本地盘 SQLite 文件锁挡住最底层页损坏,实际危害是应用层一致性与运维合同失效而非必然库损毁。根因修法:锁命名空间应从被保护资源(DB 路径)派生或在锁 payload/位置上绑定 db 身份,而非锚定日志目录。 | B:未登记未裁决的新问题。(1) 账本 debt_status_2026-07-19.json 对 APS_LOG_DIR/lock_scope/锁命名空间零命中;(2) full-defect-sweep 的 D04/D15 复合链是锁"时序"缺陷且 WP1 修复已落地(entrypoint.py:222-237 锁已前移),但 :209 lock_scope_target = prelaunch_log_dir if is_frozen else runtime_dir 原样保留,本候选的"命名空间分叉"不在修复范围;该 sweep 自己在 index.md:197 把"进程级互斥与跨进程文件竞争"列为本轮盲区,恰证未覆盖;(3) 无护栏注释(entrypoint.py:207-208 只裁决源码运行路径)、无契约测试锁冻结态锁域与 DB 绑定;(4) 代码事实逐条属实:launcher_paths.py:106-110 APS_LOG_DIR 优先决定锁域,config.py:30/13 DB 由 APS_DB_PATH/exe 目录决定,互斥实体是 lock_scope_target 目录下排他文件(launcher_contracts.py:174),db_path 不参与锁身份。唯一修正:危害场景(2)(双 exe 拷贝+APS_DB_PATH 共享库)需 shared-root 回退到各自 exe 目录才成立(注册表/ProgramData 已配置时两拷贝共享锁域);但场景(1)(第二快捷方式设 APS_LOG_DIR 引日志)无条件确定性双实例同库,维持 high。

### B04 [med] [proc-mutex] core/infrastructure/backup.py:149-187 (自愈判定), backup.py:77-107 (_pid_exists)

**维护窗锁文件自愈存在三个死区:ts 缺失/损坏、pid 缺失、PID 复用,任一命中则备份/恢复/迁移永久报'维护中'需人工删锁**

- 危害链：现场断电/崩溃恰发生在维护窗内(restore_flow 或 migrate 期间,web/routes/system_backup_actions.py:26、migration_runner.py:71)后:(a) 锁文件为空或 ts token 被截断/字节损坏(断电后半写文件的典型形态)→ ts=None → age_seconds=None → 自愈永不触发;(b) pid token 缺失 → _pid_exists(None) 按存活处理,同样永不自愈;(c) 锁内容完好但重启后该 PID 被任意其它进程复用(Windows 小 PID 空间重启后极常见)→ OpenProcess 成功 → 判存活 → 永不自愈。三种死区结果相同:is_maintenance_window_active 恒 True,此后所有备份/恢复/升级迁移全部抛 MaintenanceWindowError'数据库正在维护/恢复中,请稍后重试',用户反复重试无效,退出自动备份也被拒;必须现场人工找到并删除 .maintenance 锁文件才能恢复——而这恰发生在刚经历断电、最需要恢复/备份的时刻。
- 证据：_apply_maintenance_lock_age(149-156):`ts = state.get("ts"); if not isinstance(ts, datetime): return`——age_seconds 只来自锁文件里的 ts= token,无文件 mtime 兜底;_should_auto_heal_lock(175-187):`if age_seconds is None: return False` 且 `return not _pid_exists(state.get("pid"))`;_pid_exists(77-107):pid 解析失败或 `pid_int <= 0` 一律 `return True`(视为存活),Windows 分支仅 OpenProcess 查 pid 存活,不核对进程身份(exe/启动时间)。
- 核实结论（双镜头）：A:三个死区全部在现行代码复核成立:(a) backup.py:149-156 age_seconds 仅来自锁文件 ts token、无 mtime 兜底,空文件/截断/decode 失败(115-125 吞 read_error 返回 raw=None)均 age=None,175-180 直接 return False 永不自愈;(b) pid token 缺失/解析失败→state[pid]=None(128-138),_pid_exists(None) 因 int(None or 0)=0≤0 按存活返回 True(77-83);(c) Windows 分支(84-96)仅 OpenProcess 查 PID 存在、err=5 也按存活、不核对进程身份,重启后 PID 复用即永判存活。全仓确认锁文件删除仅有 maintenance_window finally(313)和自愈路径(200),无启动清扫、无管理端清锁入口,死区内不打任何日志(209 静默 return True)。危害链比候选更宽:web/bootstrap/factory.py:360 在 before-request 钩子对每个非白名单请求(白名单仅 /static、/system/health、/system/runtime/shutdown,factory.py:351)查维护锁,命中死区→全站 503「系统正在维护中」,不只备份/恢复/迁移被拒;退出自动备份(factory.py:141-143)和启动迁移(factory.py:325→migration_runner.py:71→backup.py:277 busy 检查)同被阻断,唯一恢复手段是现场人工删 .maintenance.lock 且无任何提示。触发现实:锁写入(backup.py:282)无 fsync,退出自动备份在每次关机运行,工控机关机时断电半写正是死区(a)形态;死区(c)只需锁完好+重启后小 PID 被系统服务复用,Win7 离线交付下均为现实场景。severity 维持 med:危害面大(全站 503+无诊断线索)但需崩溃恰落在维护窗内+三死区之一,概率窗口窄且(c)有自解可能。小修正:死区(b)物理概率低于(a)(c)——payload 顺序 pid= action= ts=(282),截断先丢 ts,pid 单独损坏而 ts 完好较罕见,但代码路径真实。修复方向:age 兜底用锁文件 mtime、pid 不可解析时按可疑而非存活处理即可闭死区。 | B:未登记未裁决的新问题。(1)账本零命中:debt_status_2026-07-19.json 中 backup.py 仅有 :333 完整性检查吞错一条(不同位置不同形态),维护锁/自愈/stale 全文无条目。(2)D01-D19 不覆盖:full-defect-sweep 的 D04+D15 复合链针对 entrypoint 锁序+atexit 退出备份(运行时单实例锁),与 .maintenance.lock 自愈死区是不同问题;且该报告'下一轮盲区维度'#1 明列 backup.py:26 _MAINT_MUTEX、#4 明列'崩溃后锁文件残留能否再启动'为待扫提名——反向证明本区域此前未立案。(3)无护栏:tests/calendar_maintenance/test_maintenance_window_mutex.py 只锁正路径自愈(完好锁+死pid)和读取异常 fail-closed,未锁三死区为 by-design;backup.py 无裁决注释。(4)代码事实全部属实:149-156 无 mtime 兜底、179-180 age None 不自愈、77-83 pid 缺失按存活、84-98 Windows 无进程身份核对;加重项:锁写入(237-244/282)无 fsync,断电后零字节锁文件是 NTFS 现实形态;:219 报错不含锁路径用户无从自查;危害端点 migration_runner.py:71、system_backup_actions.py:26 确认。维持 med:PID 复用死区不需文件损坏、Win7 重启后现实可达,后果是崩溃后最需要恢复时备份/恢复/迁移永久报'维护中'需人工删锁;不升 high 因常见形态(锁完好+pid死亡未复用)300秒可自愈,死区需崩溃恰在维护窗内的复合条件。

### B05 [med] [backup-integrity] core/infrastructure/backup.py:454-484 (restore), backup.py:392-416 (_copy_db_file); web/routes/system_backup_actions.py:66-85

**restore 恢复前/后都不对备份文件做 PRAGMA integrity_check,页可读但内容损坏的备份会通过『结构校验』落地为主库**

- 危害链：备份文件在创建后损坏(磁盘坏道/位衰减,或用户从U盘拷回时截断但页结构仍可解析,单机 Win7 老硬件场景概率不低) → 用户执行恢复 → stat 通过、backup API 把损坏页原样复制进主库、ensure_schema 只查表列结构也通过 → 页面明确提示『已从备份恢复并完成结构校验』(system_backup_actions.py:48),用户按手册认定完整成功 → 在损坏库上继续录入数天生产排产数据 → 之后某次查询报 database disk image is malformed 或静默返回错数;此时虽有 before_restore 快照可救回恢复前状态,但恢复后新录入的全部数据随回滚丢失,需人工救场。修复成本低:restore 复制前对备份源库跑一次 PRAGMA integrity_check,与创建侧对称即可。
- 证据：restore() (backup.py:455-466) 对备份文件只做 `stat_regular_file(backup_path)`(存在性+普通文件检查),随后 _copy_db_file (backup.py:398-404) 直接 `_connect_existing_sqlite_file(source_path)` + `source.backup(dest)`——SQLite backup API 按页原样复制,页无校验和,payload 级损坏不会报错。恢复后的校验 ensure_schema_func (system_backup_actions.py:67-71 → database.py:184 ensure_schema) 只做 schema 版本+表/列结构契约检查,database.py 全文无 integrity_check。对比创建侧 backup() 落地前有严格 `PRAGMA integrity_check` 且失败即 raise 不落地 (backup.py:370-378)——恢复侧不对称缺失:创建时的检查只能证明『当时是好的』,不能证明恢复时还是好的。
- 核实结论（双镜头）：终审采信镜头A,驳回镜头B的"已登记勿重报"。(1)代码事实双方无争议且已亲验:restore()仅stat(backup.py:456),_copy_db_file惰性URI连接(backup.py:62-63)+source.backup按页盲拷(backup.py:398-404),全core+web仅创建侧一处integrity_check(backup.py:370,当前两分支均raise,R32主项已修),ensure_schema仅结构校验,成功文案system_backup_actions.py:48直达用户;同函数内restore():472对自建快照经backup()做了完整性检查、唯独不检真正被恢复的文件,不对称属实。(2)镜头B的登记依据经逐字核对不成立为"同一已登记缺口":账本R32(debt_status_2026-07-19.json:523-530)的title/location/evidence全部只覆盖创建侧backup.py:333告警问题,恢复侧只字未提——按本工作流自身去重标准(排除对象是已裁决账本债与by-design裁决,位置+形态匹配,D05/D07先例"同函数不同形态可报"、D01先例"账本明示的新路径可报"),候选与R32是不同位置不同形态;09-5审计正文里的"全程无integrity复检"明确限定在auto-rollback消费before_restore快照一条链,爆炸半径明写仅"restore自动回滚、migrate_with_backup回滚"两条路径,不含候选主打的用户选定备份的主恢复路径;处置建议第3条是"可后续"的独立加固,其立论("旧版遗留未校验备份")随创建侧已改raise而逐步失效,而候选的位衰减/拷贝截断向量恰好证明该检查独立必要——这是实质新增的威胁模型,非同一缺口的重新论证。(3)加重的账本孤儿化风险:R32证据已过时(仍描述旧warning代码)、主项已修,复查时极可能整条关闭并静默丢掉恢复侧子项;确认本候选并交叉引用R32/09-5-建议3,才能保证该缺口获得正确处置。(4)severity=med采纳镜头A已核实的量级:三道闸门(stat/无校验和页拷贝/仅结构的ensure_schema)对payload损坏全放行,成功文案强化误信;不升high因触发概率低、before_restore快照兜底恢复前状态、头页级损坏会在backup API连接/起拷时报错走既有回滚分支;不降low因命中后果是静默错数或恢复后数天新数据随回滚丢失,且修复成本一次PRAGMA调用量级。处置建议:与R32/09-5建议3合并推进而非另立孤项——在_copy_db_file补integrity复检可同时覆盖主恢复与自动回滚两条路径,migration_backup.restore_db_file_from_backup(shutil.copy2,:55)需同步补检;并更新R32账本状态(主项已修、恢复侧子项未实施)。

### B06 [med] [shell-lifecycle] assets/启动_排产系统_Chrome.bat:550-569 (:lock_is_active), 触发分支 :315-349

**bat 端锁活性判定只查 tasklist PID 存在、不比对进程映像名——崩溃/断电残留锁的 PID 被任意进程复用后,启动器直接拒启并要求用户手工清理运行时信号**

- 危害链：输入/状态:上一次运行以崩溃/断电/关机硬杀结束(atexit 不跑,候选1 已证明这是常态),%APS_LOG_DIR% 残留 aps_runtime.lock(owner=本人,pid=旧值)+host/port 文件;重启后 Windows PID 以 4 递增在低段密集复用,旧 PID 被 svchost/chrome 等任意进程占用即命中。结果:双击快捷方式被 exit 9 拒启,提示"关闭现有实例"——但并没有实例可关;唯一恢复手段是用户(或运维)进 ProgramData\APS\shared-data\logs 手删 aps_runtime.lock 等信号文件=现场事故需人工救。修复面明确:bat 解析 tasklist CSV 时已拿到映像名字段,与 APP_EXE 文件名比对即可把 PID 复用识别为非活跃。
- 证据：:551 `tasklist /FI "PID eq !LOCK_PID!" /NH /FO CSV` 后,:560-568 只用 `findstr /C:",\"!LOCK_PID!\","` 匹配 PID 列即置 LOCK_ACTIVE=1,CSV 第一列的映像名(可与 APP_EXE 比对)被完全忽略。命中后 :315 分支:同 owner(:322 残留锁 owner=当前用户)→ :321/:378 从残留 aps_host.txt/aps_port.txt 取到端点 → :341 probe_health 必失败(应用根本没起)→ :348 `block_uncertain lock_active_health_failed` → :243-248 exit /b 9:"无法确认现有实例归属…请先关闭现有实例或清理运行时信号后重试"。对照:Python 端 acquire_runtime_lock 对同一场景有 exe_path 比对自愈(launcher_contracts.py:153-157 _pid_matches_contract 为 False 即判非活跃并 :254 清除失效锁),但 bat 在 `start APP_EXE`(:175)之前就拒启退出,永远走不到 Python 的自愈逻辑。
- 核实结论（双镜头）：A:逐环核实成立。(1)代码事实：assets/启动_排产系统_Chrome.bat:539-571 :lock_is_active 在 :551 tasklist CSV 后仅用 :563 行首引号 + :565 `,"PID",` 列匹配即置 LOCK_ACTIVE=1，映像名列从未与 APP_EXE 比对；且 :read_lock_file(:477-485) 只读 owner/pid，锁文件里 Python 写入的 exe_path(launcher_contracts.py:161-169) 被忽略。(2)危害链可达：残留锁 owner 格式两端一致(bat:16-18 vs launcher_paths.py:31-48，:322 /I 比较)→同 owner 分支→:341-348 probe_health 必失败(:440 严格校验 app=='aps')→block_uncertain lock_active_health_failed→:243-248 exit /b 9；即使 host/port 文件缺失也经 :330-337 同样 exit 9，链条比候选更宽。bat 在 :164-166 拦截后永远到不了 :175 start APP_EXE，Python 自愈(launcher_contracts.py:153-157 _pid_matches_contract + :254 _remove_stale_runtime_lock，挂在 web/bootstrap/entrypoint.py:228/321 应用启动路径)此场景不可达；bat 全文从不删 LOCK_FILE。(3)触发现实：断电/硬杀不跑 release_runtime_lock，锁残留 ProgramData 持久目录，重启后低段 PID 4 递增密集复用是 Win7 长期交付现场的概率必然事件；提示"关闭现有实例"误导用户(无实例可关)。(4)severity=med 恰当：启动硬拒需人工删锁救场，但触发概率性、无数据损坏、存在旁路(直接双击 APP_EXE 触发 Python 自愈/再重启可能换 PID 自解，只是用户不可发现)。修复面确凿：bat 解析 tasklist CSV 时已拿到映像名字段，与 APP_EXE 文件名(或锁文件 exe_path)比对即可。候选证据小瑕疵：":378 取端点"属 LOCK_ACTIVE=0 路径，本链只经 :321，不影响成立。 | B:未登记未裁决的新问题。代码事实全部属实：bat :lock_is_active(约:539-571)仅用 findstr 匹配 tasklist CSV 的 PID 列即置 LOCK_ACTIVE=1，映像名未与 APP_EXE 比对；触发链 :164 try_reuse_existing→同 owner+残留 host/port→probe_health 必失败→block_uncertain lock_active_health_failed→:243-248 exit 9，发生在 :175 start APP_EXE 之前，launcher_contracts.py:153-157 的 exe_path 自愈(:254 清失效锁)永远走不到。登记核对零命中：账本 debt_status_2026-07-19.json 对 Chrome.bat/lock_is_active/LOCK_PID/aps_runtime.lock 无条目；full-defect-sweep D01-D17 无 bat 锁活性项(D02/D04 是 Python 侧不同文件不同机制，相邻形态不算)；git status 显示 bat 未被并行修复触及；:lock_is_active 附近无护栏注释、tests 无契约锁定。且 roadmap runtime-launcher-stop-contract notes 明写 Python 侧"pid 身份通过 CIM/WMI ExecutablePath 确认"，证明 PID+映像身份比对是项目既定标准，bat 侧 PID-only 是漏配套非 by-design。severity=med 恰当：触发需崩溃残留+PID 复用复合条件但现实可达，后果为拒启且需手删 ProgramData 信号文件人工救场，无数据破坏；修复面明确(tasklist CSV 已含映像名字段)。

### B07 [med] [frozen-packaging] build_win7_onedir.bat:72-86,97-111 (对照 web/routes/domains/scheduler/scheduler_route_registrar.py:5-21,31)

**bat hidden-import 清单落后 registrar:缺 scheduler_week_plan_print,冻结 exe 启动即死**

- 危害链：打包机按现状跑 build_win7_onedir.bat → PyInstaller 不打包 scheduler_week_plan_print → Win7 现场双击 exe → create_app → _register_all_blueprints (factory.py:260-261) → register_scheduler_routes → ModuleNotFoundError: web.routes.domains.scheduler.scheduler_week_plan_print → create_app 抛异常,entrypoint 写 launch_error 后退出,整个交付 exe 无法启动;源码态测试全绿(sp05 反而断言该模块必须在列表里),只有真到 Windows 打包机跑 validate_dist_exe.py 才会暴露,若跳过 validate 直接交付即客户现场 DOA。且缺同步 gate 意味着每次新增路由模块都会重现同类 DOA。
- 证据：registrar 用 importlib.import_module(f".{module_name}", __package__) (scheduler_route_registrar.py:31, 变量名 PyInstaller 静态分析不可见) 动态导入 _ROUTE_MODULES 共 15 个模块,其中 "scheduler_week_plan_print" (registrar.py:20) 于 2026-06-12 commit 19b17629 加入;build_win7_onedir.bat 两个分支的 --hidden-import 各只列 14 个 scheduler 模块(72-86 行/97-111 行),没有 web.routes.domains.scheduler.scheduler_week_plan_print,且该模块全仓无任何静态 import(grep 仅 registrar 列表与测试提及)。bat 最后一次更新是 2026-06-23 (0270ab19) 仍未同步。tests/gate_meta/test_sp05_path_topology_contract.py:442 只锁 registrar 集合、test_win7_networkx_package_contract.py:14-18 只锁 networkx,无任何 gate 对账 bat 与 _ROUTE_MODULES。
- 核实结论（双镜头）：终审确认缺陷成立。亲验五项关键证据全部属实：(1) scheduler_route_registrar.py:5-21 列 15 个路由模块含 scheduler_week_plan_print(:20)，:31 用 f-string 变量走 importlib.import_module，PyInstaller 静态分析不可见；(2) build_win7_onedir.bat 两分支(72-86/97-111)各仅 14 个 scheduler hidden-import，确缺该模块，且全仓无 .spec、bat 无 collect-submodules，hidden-import 清单是唯一冻结收集机制；(3) 该模块名全仓仅出现在 registrar 元组与两个测试文件，交付代码零静态 import，无旁路可入包；(4) 启动链成立：app.py create_app → factory.py:486 _register_all_blueprints → :251 register_scheduler_routes → 冻结态 ModuleNotFoundError 即 exe 启动即死(候选引用 factory.py:260-261 行号漂移，实为 243/251/486，不影响结论)；(5) git 史吻合：模块 2026-06-12 由 19b17629 新增，bat 最后改于 2026-06-23 0270ab19 未同步。护栏缺口亦属实：sp05 只锁 registrar 集合(源码态全绿反而掩盖冻结缺口)，win7 gate 只锁 networkx，无 gate 对账 bat 与 _ROUTE_MODULES，同类 DOA 会随新增路由模块复发。severity=med：validate_dist_exe.py 会冷启动 exe 并打 /system/health，按流程可在打包机拦截，无数据损坏；但跳过 validate 直接交付即客户现场 DOA。镜头 A 为 null 无有效反证，镜头 B 结论与我的独立核验一致。

### B08 [med] [frozen-packaging] core/services/scheduler/__init__.py:18-41 (触发点 web/bootstrap/request_services.py:14-28, 静态锚 web/bootstrap/factory.py:44)

**lazy _EXPORTS __getattr__ 动态导入对 PyInstaller 不可见,7+ 服务模块不进冻结包,exe 启动即死**

- 危害链：冻结态启动链:app.py → entrypoint → factory 导入 → request_services.py:14 触发包 __getattr__("BatchService") → import_module(".batch_service") → 模块不在 bundle → ModuleNotFoundError → 应用启动即失败,exe DOA;源码态磁盘上有 .py 文件所以 import_module 永远成功、测试全绿,是纯冻结态独有分叉。该 lazy 化自 2026-04-25 (41ad409c) 引入且 bat 从未补 hidden-import,当前 HEAD 打出的 Win7 包必死;与候选1相互独立(修一个仍死于另一个)。
- 证据：__init__.py:35-41: `module_name = _EXPORTS.get(name); value = getattr(import_module(module_name, __name__), name)` —— import_module 实参是变量,PyInstaller 4.10 无法追踪;`from core.services.scheduler import BatchService` 这种 from-import 中 fromlist 是类名而非子模块名,modulegraph 不会把 .batch_service 收进包。全仓 grep 确认 batch_service/gantt_service/schedule_service/gantt_adjustment_draft_service/gantt_adjustment_scenario_service/gantt_adjustment_publish_service/resource_dispatch_actual_record_service 均无任何静态 import 语句(仅 _EXPORTS 映射),bat --hidden-import 也未列;core/services/scheduler/config/__init__.py:18-25 同模式覆盖 .config.config_service。request_services.py:14-28 在模块导入期一次性 from-import 全部 13 个符号,而 factory.py:44 静态 import request_services。
- 核实结论（双镜头）：A:成立。核心机制经现场核实全部属实:core/services/scheduler/__init__.py:35-41 用变量实参 import_module 做 lazy 导出,PyInstaller 4.10 modulegraph 无法追踪;41ad409c(2026-04-25)恰好删掉了旧版 eager 静态导入(git show 确认旧版有 from .batch_service import BatchService 等)——那是这些子模块进冻结包的唯一锚,删后 build_win7_onedir.bat:72-86 从未补 hidden-import(只列 web.routes.domains.scheduler.* 和 networkx),也无 .spec/collect_submodules。全仓 grep 确认 batch_service/gantt_service/schedule_service/gantt_adjustment_draft|scenario|publish_service/resource_dispatch_actual_record_service 在 app 可达代码零静态 import,resource_dispatch_service/execution_service/validation_service 整簇仅被缺失模块引用随之缺失。危害链静态可达:app.py main → web/bootstrap/entrypoint.py:14 → factory.py:44 → request_services.py:14-28 模块导入期触发 __getattr__ → import_module('.batch_service') → 冻结包内无此模块 → ModuleNotFoundError → exe 启动即死;源码态/测试永远通过,纯冻结态分叉。factory.py:56 的 _PYINSTALLER_IMPORT_ANCHORS 只锚 web 路由,证明团队知晓此坑但漏了 core。一处子论断有误但不影响主链:config_service 不会缺失(config/__init__.py:6-8 TYPE_CHECKING 导入会被 modulegraph 收,且 calendar_admin.py:19 有静态导入)。severity 维持 med:确定性交付阻断(当前 HEAD 打的 Win7 包必 DOA),但 validate_dist_exe.py 冷启动验收会在出厂前抓到、app.py 预启动日志失败响亮,不是静默到客户现场,也无数据损坏,故不升 high。 | B:未登记未裁决的新问题。账本 debt_status_2026-07-19.json 对 scheduler/__init__/import_module/hidden-import 零命中;full-defect-sweep D01-D19 不含此条,且 index.md:201 明文把"frozen 打包一致性"列为下一轮盲区("14 维度全在源码语义层,没人问冻结态等价性"),是官方承认的未扫维度。代码事实全部属实且比候选更宽:__init__.py:35-41 变量实参 import_module 确认;request_services.py:14-28 导入期 from-import 13 符号、factory.py:43 静态锚确认;build_win7_onedir.bat:72-86 hidden-import 仅列 web.routes.domains.scheduler.*+networkx;穷举 grep 确认 13 个映射中 10 个服务模块(候选称 7 个)生产代码零静态 import,冻结包缺失后首个 BatchService from-import 即 ModuleNotFoundError,exe 启动链断在 factory 导入期。bat 已为 routes 动态导入补过 hidden-import 却从未为 41ad409c 的服务层 lazy 化同步,佐证无其他收集机制兜底。两处修正不动摇主结论:(1) config/__init__.py 有 TYPE_CHECKING 静态 import,PyInstaller 字节码分析可见,该子包大概率不受影响,候选此子claim存疑;(2) validate_dist_exe.py 冷启动验收(手动步骤,bat:120 仅提示)会在交付验收时抓到 DOA,把"静默交付死包"降为"验收必失败",故维持 med 不升 high——当前 HEAD 打不出可用 Win7 包但不会静默流到用户手里,修复也简单(补 hidden-import 或静态 re-export)。局限:macOS 无法实跑 PyInstaller 4.10 验证,结论基于 modulegraph 已知语义+全仓 import 穷举。

### B09 [low] [mem-growth] web/public_token_registry.py:54-59 + web/routes/domains/scheduler/scheduler_batch_detail.py:418-424

**op-update token 每次渲染逐工序无去重签发,长命登记表靠 5000 上限淘汰,真实使用中打断早先页面的保存入口并丢弃用户已填表单**

- 危害链：危害链:排产员上午打开批次详情页,每个工序的保存表单 action URL 内嵌当次签发的一次性 token → 当天自己/同事继续浏览大批次页面,scheduler-op-update-v1 scope 内活跃 token 超过 5000,_cleanup_scope 按 created_at 把上午签发的 token 全部淘汰(远早于 12h TTL) → 用户回到上午页面改完机台/操作工/工时点"保存" → resolve_public_token 查不到 token 抛 ValidationError → scheduler_ops.py:44-51 flash"工序保存入口已失效,请刷新页面后重试"并 redirect,POST 表单里已填写的修改全部丢弃,必须刷新后逐项重填;方案预览 plan_context 链接同理提前失效。内存本身受 cap+TTL 封顶在约几 MB(5000×~500B×2 scope),不是无界泄漏,但"每请求向长命对象追加+无值去重"使上限淘汰在正常单日使用中可达,cap 把内存增长换成了功能断链。修复方向:按 (scope,value) 去重复用未过期 token,或把 op-update token 改为短 TTL 并将上限淘汰改为拒发+日志告警,避免静默吞掉旧页面入口。
- 证据：scheduler_batch_detail.py:418-423 每次渲染批次详情页对每个工序新签发一个 token(无按 op_id/值去重):`for index, op in enumerate(view_ops, ...): operation_update_actions[form_key] = url_for("scheduler.update_op_by_token", token=operation_update_token(int(op.get("id") or 0)), ...)`;登记表挂在 app.extensions(进程长命对象),TTL 12h、`_MAX_SCOPE_TOKENS = 5000`,public_token_registry.py:54-59 超限时按 created_at 淘汰最旧 token:`ordered = sorted(store.items(), key=lambda item: float(item[1].get("created_at") or 0)); for token, _entry in ordered[:overflow]: store.pop(token, None)`。增长速率假设:每次打开/刷新批次详情页新增"工序数"个 token——典型 20 工序批次、一天浏览/刷新 100 次页面 ≈ 2000 token/天滞留 12h;大批次(100+ 工序)或反复刷新时半天内即越过 5000 上限进入持续淘汰。plan-context scope(scheduler_plan_context_token.py:25)同一机制同族。
- 核实结论（双镜头）：A:代码事实全部核实成立:public_token_registry.py:56-59 确实在 scope 超 5000 时按 created_at 淘汰最旧 token(无视其仍在 12h TTL 内);issue_public_token(62-82)无 (scope,value) 去重;scheduler_batch_detail.py:418-425 每次渲染逐工序签发且 grep 确认是 scheduler-op-update-v1 唯一签发点;resolve_public_token(85-102)成功时不 pop,token 全部滞留到 TTL/淘汰,堆积前提比候选描述更强(候选称"一次性 token"有误,实为可重复使用);scheduler_ops.py:44-51 token miss 时 flash+redirect 丢弃 POST 表单。危害链机械可达:保存 POST 会 redirect 回 batch_detail 重签全部工序 token,100+ 工序批次一个编辑会话(~50 次渲染)即越 5000 上限,旧标签页的保存入口被提前作废。但 severity 虚高,降 med→low,理由:(1) 触发是尾部场景——典型 20 工序批次需 12h 内约 250 次渲染才越限(候选自己的算例 2000/天也未过 5000),只有大批次+重度编辑日+越限后仍拿 stale tab 提交三条件叠加才受害;(2) 危害量级小且可恢复——templates/scheduler/batch_detail.html:133-266 确认每工序独立 form,一次丢失仅该工序 2 个下拉+2 个数值,flash 明确提示"请刷新页面后重试",非静默损坏、非崩溃;(3) 内存被 cap+TTL 封顶几 MB,dim 标 mem-growth 名不副实,实质是功能断链而非内存增长;(4) plan-context 同族(scheduler_plan_context_token.py:25)只影响 GET 链接,刷新即恢复,无数据丢失。修复方向(按 (scope,value) 去重复用未过期 token)成立且成本低,值得立案但优先级不高。 | B:代码事实全部核实属实(web/public_token_registry.py:25-26,54-59,62-82 无值去重签发+5000上限按created_at淘汰;scheduler_batch_detail.py:418-425 每次渲染逐工序新签发;scheduler_ops.py 失效即 flash+redirect 丢表单)。账本 debt_status_2026-07-19.json 零 token 条目未登记;full-defect-sweep D01-D19 终局账不含此项,仅在 index.md:199 '下一轮盲区维度'第3条被提名为内存增长族待扫成员——是已知盲区非已立案,已立案在修的是 gantt 关键链缓存(相邻形态不算)。finding-11 裁决只覆盖裸 scenario_id 回退 by-design,不覆盖签发去重/淘汰行为;无护栏注释或契约测试锁定淘汰行为。唯一小误:token 非一次性(resolve 成功不 pop,TTL 内可复用),不影响核心危害链。危害需重度使用(单 scope 12h 超 5000 token ≈ 250 次 20 工序页渲染)且失败有明确报错可恢复,表单丢失是核心排产工作流的真实功能断链,med 定级合理。

### B10 [low] [report-aggregates] core/services/report/utilization.py:86,92 + core/services/report/report_engine.py:386-398

**零产能报表窗口下利用率整列静默 None,无任何降级留痕或提示消息**

- 危害链：现实触发:用户选整周节假日或全零效率日的报表窗口(空计划周/零产能日,calculations.py:66-77 逐日累加得 total=0),但窗口内仍有排产行(排产可跨节假日执行)。此时每台设备/每个人员行 hours 有值、任务数有值,而利用率整列 None——网页 JSON 与导出 Excel 的"利用率(%)"整列空白,且 report_degraded=False、降级消息为空。使用者会把空白误读为"没数据/导出坏了"或"负荷为 0",与同函数中坏时间行必须降级留痕的契约不对称(同样是丢指标却无留痕)。不会崩溃、数据本体不损坏,属健壮性/可解释性缺口:分母为零的答案是"静默空白"而不是"带原因的降级"。
- 证据：utilization.py:86 `util = (h / cap_hours) if cap_hours > 0 else None`; utilization.py:92 `"utilization": round(util, 4) if util is not None else None`; report_engine.py:386-388 `cap_hours = calculations.capacity_hours(self.calendar, sd, ed)` / `if cap_hours <= 0: cap_hours = 0.0` — 归零动作发生在 degradation_collector(390 行才创建)之前,collector 只收 bad_time_row,零容量这一分支不进 report_degradation_payload,report_degradation_message 为空。exporters/xlsx.py:249/264 把 `_utilization_percent(None)` 写成空白单元格。
- 核实结论（双镜头）：A:成立,维持 low,但触发面比候选描述更窄、危害有部分缓解。核实为真:utilization.py:86/92 零容量时整列 utilization=None;report_engine.py:386-388 的归零发生在 DegradationCollector(390 行)创建之前,collector 只收 bad_time_row(utilization.py:47),report_degradation.py:75-93 空 collector 输出 report_degraded=False、message 为空;xlsx.py:70-77+249/264 把 None 写成空白单元格。危害链可达:add_working_hours(calendar_engine.py:271-330)存储的区间可跨节假日,故全节假日报表窗口(如春节周)仍能被跨期任务 overlap 出 hours>0 的行,整列利用率静默空白且无任何降级留痕,与同函数坏时间行的降级契约不对称——这一环真实。两处修正:(1) 候选的'全零效率日'触发不可达,calendar_engine.py:140-141 强校验 efficiency<=0 直接 ValidationError,唯一可达路径是整窗 shift_hours=0 的节假日;(2) 危害被候选未提的事实削弱——零分母两端可见:Excel 每行'可用工时(小时)'列写 0.0(xlsx.py:239/248),网页上下文带 capacity_hours=0(web/routes/reports_page_support.py:319),用户并非完全无从解释,'误读为导出坏了'有部分缓解。不崩溃、不损数据,属可解释性/降级留痕契约缺口,severity=low 恰当不虚高。 | B:未登记未裁决的新问题。核对结果:(1) 账本 debt_status_2026-07-19.json 对 utilization/利用率/capacity/report_engine 零命中;同家族已立案项均为不同位置——U05(downtime_impact.py:42-44 空 machine_id,已修)、R12(gantt_critical_chain.py:84 坏时间行),不覆盖零产能分母分支。(2) 2026-07-19 full-defect-sweep D01-D19 与 debt-recheck-ultra 均无 report-aggregates/utilization 条目。(3) 历史 finding-07(2026-06-14)只立案坏 start/end 时间行并已用 record_report_bad_time_row 修复,从未裁决 cap_hours<=0 分支。(4) 代码事实以现状(含并行修复中的工作区)核实属实:utilization.py:86,92 cap_hours<=0 时整列 None;report_engine.py:387-388 归零在 :390 collector 创建之前,report_degradation.py(工作区最新版)只认 bad_time_row/missing_machine 两类事件,零产能不进 payload,report_degraded=False;xlsx.py:70/249/264 None→空白单元格。(5) 无护栏注释,唯一相邻测试(test_web_silent_fallback_contract.py:233)锁的是 viewmodel 对空串的处理,不构成该行为的裁决。(6) 零产能窗口现实可达(test_calendar_shift_hours_roundtrip.py:46 锁停工日 capacity_hours=0)。P4 专项排除不适用:属全新位置且危害链完整。轻微缓解:页面摘要有"单资源可用工时 0.0 小时"可供细心用户推断,支持 severity 维持 low(不崩溃、数据本体无损,属可解释性/降级留痕不对称缺口,与同函数 bad_time_row 必留痕的契约不一致)。

## 补核定案（workflow 核实代理 API 断流，主会话补派对抗核实，双镜头同规格）

### B11 [low] [shell-lifecycle] assets/启动_排产系统_Chrome.bat:330-337, :243-248, :382-394

**应用启动窗口期（锁已建、host/port 未写）第二次双击被判 BLOCKED_UNCERTAIN，并用『清理运行时信号』误导用户对活实例做致瘫操作**（成立，维持 low；新问题）

- 危害链逐环核实成立：窗口真实存在且被本轮 D04 锁前移修复**加宽**（锁在 entrypoint.py:222-237 create_app 之前获取，host/port 契约要到 create_app+迁移+选端口后 entrypoint.py:249-266 才发布）；第二次双击必然落 BLOCKED_UNCERTAIN——bat try_reuse_existing(:305-398) 对『锁活+同 owner+端点缺失』无任何 sleep/重试分支，bat:330-333 block_uncertain lock_active_missing_host 直达 :243-248 弹窗 pause。
- 文案与事实不符且引导危险操作：lock_active_missing_host 分支里 bat 已确认归属（:322 owner 相等才落 :330），真实状态是『同账户实例正在启动、端点尚未发布』，bat 手里信息足以输出『正在启动请稍候』并重入 :192 等待循环；但文案说『无法确认现有实例归属…请先关闭现有实例或清理运行时信号』。『关闭现有实例』对普通用户不可操作（窗口期无 UI、模板层无退出按钮、shutdown 需契约 token、assets/ 无停机 bat），只剩『清理运行时信号』可操作。
- 删信号→死锁链成立（一个时序子路径自愈不破主链）：用户动手时启动往往已完成，最像『运行时信号』的 aps_runtime.lock+aps_runtime.json 被删、留下 host/port 文件后：下次启动锁缺失+契约缺失→bat:378-387 探活成功→block_uncertain healthy_without_owner_proof(:385) 持续拒启且**无自愈**（契约/锁只在启动时写一次），官方停机链同断（launcher_stop 优雅关闭要契约 token :431/:106-112，强杀要锁/契约里的 pid :454-457），连 --runtime-stop 都失败；只剩任务管理器杀进程或重启。附加恶化变体：5 个信号全删+立即再双击→第二个 bat existing_reuse=0 直接再 start exe，Python 锁可重新独占创建→真双实例写同一 SQLite——D04 锁前移要防的场景被文案引导用户亲手拆掉。
- 登记核对四处零命中：账本无 launcher bat/启动窗口条目；D01-D19 无此项且 full-defect-sweep index.md:200 明列『桌面壳/子进程生命周期』为本轮盲区；治理台账 launcher 条目均为 Python 模块 fallback 登记；assets/ 工作区无改动。**本候选实质是 D04 修复的新暴露面**（窗口从选端口的亚秒级拉宽到整个 create_app 迁移时长），任何地方都没有登记这个后续。
- severity=low 理由：第一段（误导弹窗）常见但只是 UX 缺陷不损数据；致瘫段需用户按文案多步手删文件，重启机器可恢复，单机单用户爆炸半径有限。修复方向明确：bat 对『同 owner 锁活+端点缺失』改判『正在启动』提示等待/重入等待循环即可根治第一段。

### B12 [low] [shell-lifecycle] web/bootstrap/factory.py (atexit 退出备份) + 开发文档/开发文档.md:51,125,3282-3293

**候选『退出自动备份安全网在生产上永远不触发』降档成立（high→low）：头部危害被周期性自动备份反证击穿，残留为文档口径滞后+日常桌面流程无优雅退出**（新问题）

- 致命反证 1：atexit 并非唯一自动备份触发点——before_request 挂 SystemMaintenanceService.run_if_due（factory.py:398-408），auto_backup_enabled=yes 时按 auto_backup_interval_minutes（默认 60，system_config_service.py:153）执行真实整库备份（backup_task.py:88 mgr.backup(suffix="auto")）；job state 缺失即 due=True，首个业务请求就产生第一份 auto 备份。候选核心危害『数月后库损坏→backups 目录无任何自动备份』不成立；exit 备份丢失的最大增量损失只是最后不足 60 分钟的改动没有临关机快照。
- 反证 2：auto_backup_enabled 默认 no（system_config_service.py:151/:177），默认部署下 _run_exit_backup 本身跳过。反证 3：安装器升级/卸载走 --runtime-stop（installer/aps_win7.iss:613→entrypoint.py→POST /system/runtime/shutdown→server.shutdown()→正常退出→atexit 执行），升级前恰好产生一份 exit 备份。反证 4：『每次关机残留 lock/contract』是瞬态自愈（launcher_contracts.py:135-159 按 pid 存活+exe 匹配判活，死 pid 判非活跃后 :217/:254 清陈旧锁接管）。
- 经核实为真的环节：前端零消费 shutdown（templates/static 全量 grep 仅无关 CSS 注释命中）；壳侧脱钩（bat :175/:258，关 Chrome 不停 Flask——但 bat 头部自述 reuse healthy APS instance，Flask 常驻复用是有意设计）；全仓无 signal/SetConsoleCtrlHandler；Windows 对 --windowed 无窗口进程注销/关机直接 TerminateProcess、atexit 不执行——与 Microsoft 文档化行为一致，置信度高但未在 Win7 实机验证。
- by-design 佐证：system_config_service.py:177 开关描述明写『有人访问系统时会按间隔检查；程序正常退出时也受这个开关控制』——产品口径本就以间隔备份为主机制、退出备份为附带；docs/manual_audit_report.md:630 同口径。
- 残留真问题（low）：日常桌面流程（开机→bat→用→关 Chrome→关机）不存在任何优雅退出，exit 备份与 atexit 清理只在安装器升级/卸载时触达；`开发文档/开发文档.md:51,125,3282-3293` 仍把备份机制表述为『简化为退出时自动备份』，与现状（请求驱动间隔备份为主）文档滞后。建议：修文档口径；可评估关机场景补消息窗/handler，但鉴于间隔备份兜底+单机定位不构成 high。

## 驳回清单

1. (proc-mutex) `core/infrastructure/database.py:94-99` — get_connection 未设 busy_timeout/timeout,双进程写库冲突只以裸 OperationalError 走通用 500 呈现,无双实例提示
   驳回理由：A:代码事实属实(database.py:94-99 无 timeout/busy_timeout、无 WAL、500 兜底通用文案 error_handlers.py:114-124),但危害链断在第一环"双实例形成":web/bootstrap/entrypoint.py:222-237 把运行时锁放在 create_app 之前,第二实例触库前即 rc=13 退出,永远走不到 get_connection;且锁被占时给出的正是候选声称"完全得不到"的提示——launcher_contracts.py:210-214"系统已在当前账户运行,请直接使用现有窗口,不要重复启动"(异主 :205-209),pid 不确定态按活跃处理(launcher_contracts.py:150-152)为 fail-closed。现实双开场景产出"第二实例明确启动失败",而非"两个看似正常的窗口随机卡顿+裸500"。本候选自认寄生于候选1/2(锁被攻破),无独立可达路径,不能单独立案;若候选1/2 成立,busy_timeout 缺省可作为其修复的顺带加固项。 | B:代码事实属实(database.py:94-99 确无 timeout/busy_timeout,web/error_handlers.py:100 兜底 500,文件无未提交修复),但该发现已被审计体系逐字登记:.codestable/audits/2026-07-19-full-defect-sweep/index.md:197 在"下一轮盲区维度"第1条(进程级互斥与跨进程文件竞争)中明确点名"database.py:94-99 无 busy_timeout",同 file:line 同缺陷表述,属直接命中而非相邻形态,已编入下一轮定向扫描队列。且本候选危害链前提"候选1/2 成立形成双实例"依赖的正是已立案并行修复中的 D04+D15 entrypoint 锁序复合链与 launcher PS 解码项,按清单规则不得重报;前门修复落地后双实例前提即被封堵,本条降为纵深防御性改进。账本(debt_status_2026-07-19.json)确未单列此条,若下一轮盲区扫描启动时应随该维度一并立案,而非本轮重复上报。

2. (proc-mutex) `core/infrastructure/logging.py:105-107, 121-123` — RotatingFileHandler 双进程共写同一日志文件时 Windows 下滚动必失败:日志无界增长且错误只吐 stderr 不可见
   驳回理由：A:危害链断在前提环节：候选以"双实例各自打开同一 aps.log"为前提，但当前代码已有双开防线——web/bootstrap/entrypoint.py:222-237 在 create_app（:240，AppLogger 唯一构造点为 web/bootstrap/factory.py:304）之前先 acquire_runtime_lock，锁被占的第二实例直接 rc=13 退出，永远走不到 RotatingFileHandler 构造；锁是 O_EXCL 独占创建（launcher_contracts.py:172-176 create_fixed_file_exclusive）并带 pid 存活+exe_path 匹配的失效锁清理（:135-158），不是可绕过的摆设。此外危害机理描述有误：即便双开成立，RotatingFileHandler.emit 把 doRollover 和写入包在同一 try 里，rename PermissionError 时该记录经 handleError 丢弃而非追加，aps.log 会卡在 ~maxBytes 静默丢日志，不会"无界增长吃满磁盘"。唯一残留路径是 debug reloader 父子进程双开（entrypoint.py:240 父进程无条件 create_app），但那是源码开发模式，冻结 Win7 交付 debug 关闭，不构成现实交付场景危害。logging.py:105-110/121-126 代码事实本身属实，若未来双开防线被移除可重新评估。 | B:代码事实属实(core/infrastructure/logging.py:105-110/121-126 两处 RotatingFileHandler,Windows rename 滚动在他进程持句柄时确会 PermissionError 且只吐 stderr),但全仓生产代码仅 web/bootstrap/factory.py:304 一处实例化 AppLogger,该竞争无独立触发路径,唯一成立前提是双实例。双实例有 by-design 单实例护栏(web/bootstrap/entrypoint.py:228 _acquire_runtime_lock_before_app → launcher_contracts.py 运行时锁),唯一已知破锁路径已在 2026-07-19-full-defect-sweep 立案为 med 项(launcher_processes.py PS 解码误判锁非活跃→双实例,index.md 确认清单)并连同 entrypoint.py 锁序复合链处于任务清单明示的并行修复中、勿重报之列。候选自述"危害以双实例为前提""若候选1/2 修复此条自然消解",属已立案在修根因的下游派生后果,非独立新问题。账本 debt_status_2026-07-19.json 无 logging 条目、sweep D11/D12(日志缺上下文)与本条无关,故本条不构成重复登记,但按"派生于在修已立案项即不报"裁定 isReal=false。若后续双实例修复被裁定放弃,可再以独立加固项(如 delay=True/进程隔离日志)重新评估。

## 批评家补记（workflow 内批评家 API 断流，主会话补派，9+ 子区域抽查）

### 新增确认（批评家自核带行号证据，主代理抽查决定性锚点坐实）

### B13 [med] [report-aggregates] core/services/report/downtime_impact.py:79-92 + core/services/equipment/machine_downtime_service.py:117-133

**停机重叠段聚合不合并 + 录入侧查后插竞态，报表停机工时可被静默双计**

- 证据（主代理已抽查坐实两锚点）：`_downtime_seconds`(:79-80) 对同机多段停机直接 sum(overlap_seconds)、`_schedule_overlap`(:83-92) 双层循环累加——两处都不合并重叠区间，同机重叠 active 行把重叠跨度算两遍，downtime_hours 可超报表窗口总长且零降级留痕（同文件 :44-45 为缺 machine_id 行留痕，重叠行是同类库外异常却完全静默——自家契约内的不对称）。重叠行的现实通道：machine_downtime_service.create 的 has_overlap 检查(:118)在 `with self.tx_manager.transaction()`(:121) **之外**，deferred 事务且未用已有 begin_immediate 能力（transaction.py:237），threaded 多线程+每请求独立连接下双击双提交两个请求都通过检查都插入成功，SQLite 无区间排它约束兜底（machine_downtime_repo.py:113-127 仅 SELECT 判定），直接违反 :119 自家合同文案。
- 危害：报表停机工时/排产受影响工时静默虚高（重叠窗精确双倍），无降级无测试锁，用户以错数做产能决策。severity=med：触发需并发或库外直写、频率低，但错数静默且两缺陷互为放大。
- 查重：成立清单 10 条、debt-recheck-ultra（U05 只覆盖缺 machine_id 降级）、2026-06-09 报告（#29 只覆盖 create_by_scope N+1）均未覆盖。

### B14 [low] [frozen-packaging] validate_dist_exe.py:177-190,252-265 + web/bootstrap/static_versioning.py:84-88

**交付验收链对 static 资源零护栏，static 缺失/半缺失的离线包全绿出厂**

- 证据（主代理已抽查坐实）：validate_dist_exe.py 对 --add-data 六项载荷只断言 networkx（_assert_networkx_bundled :177，全文件无 static 断言），页面检查只看 6 个 URL 的 HTTP 200。其余数据面各有兜底（schema.sql 缺失启动即 FileNotFoundError、templates 缺失页面 500 会被抓、templates_excel 启动期按真源生成失败 raise、hidden-import 缺模块启动期急切导入即爆），**唯独 static** 缺失时 static_versioning 只 _warn_once 后回退原始 URL，页面照常 200，冷启动验收/健康检查/页面冒烟全部通过；而打包清单是手维护 bat（B07 已证明该清单会漂移）。
- 危害：static 漏打/半损的包通过官方验收出厂，离线现场整套 UI 无样式无脚本（无 CDN 可救），只能靠人眼发现。
- 查重：B07/B08 是模块打包缺口，本条是数据资源验收护栏缺口，维度内不同子区域。

### 四个提示子区域清白结论（一行制）

- **(a) SQLite busy_timeout**：审计档案中无落盘驳回裁决（full-defect-sweep index.md:197 只是下一轮提名），但代码事实支持驳回——sqlite3.connect 默认 timeout=5.0 本身就是 5 秒 busy 等待，恢复热路径已单独设 30s（backup.py:400-401）→ 不重提；驳回理由以本节为落盘出处。
- **(b) 备份写入原子性**：backup.py:340-390 `.tmp` 独占创建 + 写后 PRAGMA integrity_check 不过即弃 + os.replace 原子换名，list_backups/cleanup 均按 aps_backup_*.db 后缀过滤，半成品 .tmp 不会被当合法备份 → 无问题。
- **(c) 模块级 dict/list 累积**：全量扫 core/web/data 模块级可变容器（plugins _STATE 整体替换、签名缓存有界、关键链缓存 LRU、静态版本缓存按文件数有界、AppLogger 防重复 handler、token 注册表 TTL+上限即 B09）→ 未发现新的无界累积。
- **(d) 数据文件打包完整性**：11 个路由引用的 Excel 模板名与 templates_excel/ 全对上且运行时按真源启动生成不读打包目录；migrations 全静态导入无动态发现风险；唯一零护栏项 static 已升格为 B14。

### 主动不立案（留档防重报）

- backup.py 维护锁自愈读后删 TOCTOU（:194-206，与 B02 同款模式）：源码层面存在，但 Win7 交付平台上锁持有者全程持打开 fd（:280,:294），msvcrt 打开的文件默认无 FILE_SHARE_DELETE，误删活锁会 PermissionError 并被 :207-208 捕获按"窗口仍活跃"处理——平台语义挡住危害，宁缺毋滥不立案（未来支持非 Windows 部署需重评）。

## 处置

- **终局账：14 条 = 1 high（备份轮换修剪无保底 B01）/ 8 med（proc-mutex 三条 B02-B04、restore 无完整性校验 B05、bat 锁活性判定 B06、frozen 打包两条 B07-B08、停机重叠双计 B13）/ 5 low（op-update token B09、报表利用率静默 None B10、启动窗口期误判 B11、退出备份文档口径 B12、static 验收零护栏 B14）/ 驳回 2**。
- 特别提示：**B07/B08 两条 frozen-packaging 意味着当前 HEAD 打出的 Win7 包 exe 启动即死**（hidden-import 缺 scheduler_week_plan_print + lazy __getattr__ 动态导入不进包，相互独立、修一个仍死于另一个），虽 validate_dist_exe.py 出厂验收可拦截，但属确定性交付阻断，建议最优先修并补 bat↔registrar 对账 gate。
- **用户拍板（2026-07-20）：全修**（同时另开排产算法核心 10 维度扫描，见 `.codestable/audits/` 后续档案）。组织：6 个并行修复工兵——WP-A 备份三连（B01+B04+B05）/ WP-B 锁双连（B02+B03）/ WP-C bat 双连（B06+B11）/ WP-D 打包三连（B07+B08+B14）/ WP-E 报表双连（B10+B13）/ WP-F token（B09）；B12（文档口径）由主代理当场修复（开发文档.md 第 51 行历史决策补演进注记、1.3 节历史引文注记、16.1 节退出备份触达边界说明）。
- 账本（debt_status json）与全景图待修复批次收口后按 fixed 一并登记；处置结果与门禁结论见下节。

## 处置结果（2026-07-20 当日，14 条全部修复）

组织方式：一个修复 workflow 六个并行工兵（各领互不相交文件集，6/6 交卷零 blocker），B12 主代理自修；每包局部测试全绿后集成收口，最后跑 `.venv` 日常全量门禁。

| 条目 | 修法 | 测试锁定 |
|---|---|---|
| B01 (high) | 两条修剪路径加 MIN_KEEP_BACKUPS=3 数量保底（before_restore/pre_migration/exit 快照同受保护）；当轮自动备份失败跳过本轮清理并留痕；maybe_run_auto_backup 失败留痕带上下文+traceback；手动清理同保底 | tests/calendar_maintenance/test_backup_cleanup_min_keep_floor.py（5 例） |
| B02 (med) | _remove_stale_runtime_lock 守卫式删除：删除前重读逐字段比对仍是判定为陈旧的那份 payload，不一致放弃删除重判 | tests/app_runtime/test_runtime_lock_stale_toctou_guard.py（4 例含端到端交错） |
| B03 (med) | 方案乙+丙：壳锁不动（bat/stop 合同零改动），acquire 在解析后 DB 同路径追加 db-scope 排他锁 `<db>.lock` 作同库双开真互斥；resolve_runtime_db_path 与 factory DATABASE_PATH 同源防漂移合同 | tests/app_runtime/test_runtime_db_scope_lock.py（7 例） |
| B04 (med) | 三死区闭合：ts 坏→锁文件 mtime 兜底 age；超龄锁 pid 缺失/不可解析按可疑允许自愈；pid 判存活不自愈时 warning 留痕含人工删锁指引；未超龄/读异常仍 fail-closed | tests/calendar_maintenance/test_maintenance_lock_self_heal_deadzones.py（6 例） |
| B05 (med) | _copy_db_file 恢复前对源跑 PRAGMA integrity_check（复用创建侧实现，新增 BackupIntegrityError），覆盖主恢复与 _auto_rollback 两路径；失败 ok=False 中文原因主库零改动 | tests/migration_db/test_restore_integrity_check_contract.py（3 例） |
| B06 (med) | bat :lock_is_active 追加映像名组合比对（findstr /I 引号 CSV），不匹配按陈旧锁+审计日志 lock_pid_image_mismatch=stale | tests/app_runtime/test_launcher_bat_contract.py（10 条文本合同含编码合同） |
| B07 (med) | bat 两分支补 scheduler_week_plan_print（16 项对齐 registrar） | tests/gate_meta/test_frozen_bundle_contract.py，并已纳入 QUALITY_GATE_GUARD_TESTS |
| B08 (med) | 静态锚方案：新建 core/services/scheduler/_frozen_import_anchor.py 静态镜像 _EXPORTS 13+config 2 模块，factory 静态 import 并挂 _PYINSTALLER_IMPORT_ANCHORS；lazy 本体不动源码态零变化 | 同上对账 gate：_EXPORTS 全覆盖/锚可达/导入冒烟/防 typo，漏同步判红 |
| B13 (med) | 聚合前同机区间归并+record_report_downtime_overlap_merged 留痕（code 进稳定册）；has_overlap 移入 begin_immediate 事务串行化写者（双线程实测只成一条） | test_downtime_impact_overlap_merge_degradation.py（4）+ test_machine_downtime_create_overlap_race.py（2） |
| B09 (low) | token 幂等签发（by_value 反向索引复用未过期 token，临期改签），淘汰先清过期再 LRU | tests/web_pages/test_public_token_registry_idempotent_issue.py（10 例） |
| B10 (low) | 零产能窗口 record_report_zero_capacity_window 留痕+可读提示，None 语义保留 | tests/scheduler_analysis/test_utilization_zero_capacity_degradation.py（4 例） |
| B11 (low) | 『同 owner+锁活+端点缺失』改判启动窗口期重入等待循环；全部 block 文案删『清理运行时信号』改安全指引 | test_launcher_bat_contract.py（全文无危险指引由文本合同锁定） |
| B12 (low) | 开发文档.md 三处口径修正（历史决策演进注记/1.3 引文注记/16.1 退出备份触达边界定案） | 文档修正，主代理完成 |
| B14 (low) | validate_dist_exe 加 static 锚点断言（存在非空 exit 7）+ 页面冒烟 GET 静态 URL 200 非空（exit 6） | tests/app_runtime/test_validate_dist_static_payload.py（6 例） |

结构性附带（门禁强制的两次拆分）：backup.py 578→419 行（维护窗锁拆出 core/infrastructure/maintenance_lock.py，oversize 白名单达成退出条件已移出）；launcher_contracts.py 465→279 行（运行时锁生命周期拆出 web/bootstrap/launcher_runtime_lock.py，旧口兼容再导出）。

集成协调项收口：静默回退台账两条 launcher 锁条目随拆分迁移（handler 指纹/context hash 逐字未变）+26 条行号批量对齐（sync_debt_ledger check 通过，silent_fallback_count=69）；a3 模块计数 pin 785/1508（+3 生产模块 maintenance_lock/launcher_runtime_lock/_frozen_import_anchor，+12 测试模块）+ with_tests 动态导入 44→45（对账测试锚导入冒烟 by design）；test_frozen_bundle_contract.py 纳入 QUALITY_GATE_GUARD_TESTS；ruff 全绿。

遗留跟进项（未修，登记防丢）：core/infrastructure/migration_backup.py restore_db_file_from_backup（shutil.copy2 路径）的恢复侧 integrity 复检未同步补（B05 交叉项，复查台账 R32 时交叉引用）；B07/B08 修复效果的最终 ground-truth 需 Windows 打包机实跑 build_win7_onedir.bat + validate_dist_exe.py（macOS 无法实跑 PyInstaller 冻结，源码态可判定的漂移已全部被对账 gate 锁死）。

账本登记：14 条以 B01-B14 编号、bucket=BB、状态 fixed 登入 debt_status_2026-07-19.json（账本终态 122 条 = 55 fixed / 3 in_progress / 63 planned / 1 n_a）；债务/项目全景图已重生（122 卡）。

门禁：`.venv/bin/python scripts/run_daily_quality_gate.py` 于 14 条修复+协调项收口+账本登记后全量复跑，**`[daily-fast-gate] passed` 全绿**（2026-07-20 05:20 前后，含 ruff check full、pyright、py38 语法扫描、架构适应度、静默回退台账对齐、a3 边界、registry 合同、2166+301+5 pytest）。中途一次失败为 registry 合同要求 QUALITY_GATE_GUARD_TESTS 成员必须 git 跟踪——test_frozen_bundle_contract.py 已 git add 入 index（未提交）后通过。工作区仍有本批未提交改动，按约定不声称 clean-worktree proof。

## 运行档案

- Workflow：runId wf_13b6f4d3-563（首跑 find:proc-mutex API 断流后 resume 续跑）；journal 见会话目录 subagents/workflows/wf_13b6f4d3-563/journal.jsonl。
- 用量：39 agents / 2.99M subagent tokens / 663 tool uses / 47.6 分钟；另主会话补派 3 个核实/批评家代理（2 条 shell-lifecycle 补核 + 批评家补位）。
