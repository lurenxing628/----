# 工作台形状规范实施记录

## 当前结果

- 用户要求先保存再修改。2026-09-07 19:33:32 已完整保存前端目录及三个根目录工作台回归测试，共 442 个文件、29,860,323 字节，逐文件 SHA-256 校验一致。
- 通用规则为：页面/表格/统计/行内展开 0px，普通控件/任务条/状态标签 4px，真正浮层 8px。按用途分类，不改业务计算和事件，不改全局 DS 或 bundle。
- 用户追加反馈后，报表左右改为同一网格行等高：标题不拉伸，真实表格与四条资源记录填满数据区。保留原三条超期记录，不填假行，不把行数写进布局。1080px 以下堆叠时恢复自然高度。
- 用户明确要求产能链原版更好看，因此仅恢复上方主线图的原圆角/边框/轻阴影/悬停视觉。95 条主线 CSS 规则与保存版相同，`plana-logic.js` 最终与保存版逐字节一致。保留小卡片 28px 图标、12px padding、13px 名称、换行及 1280px 纵排；下方统计、表格和维护弹窗不回退。

## 修改位置

均位于 `前端设计/ui_kits/workbench/`，除新增测试和本记录：

- `workbench-ui.js`、`workbench-ui.css`：共用半径令牌、普通按钮/Meter 的局部适配、body 下表格筛选浮层规则。原组件继续处理业务 props 和回调。
- `index.html`：页壳控件、普通页面分区、报表等高布局和窄屏恢复、资源版本；`app.jsx` 仅统一启动标识外形。
- `ReportsScreen.jsx`、`DelayScreen.jsx`、`CalibScreen.jsx`、`AnalysisScreen.jsx`、`BasicDataScreen.jsx`：同级分区、普通按钮、原数据条接入，保留方案卡和图表结构。分析联动表只处理外壳，不套普通行规则。
- `BaseShared.jsx`、`BaseBatches.jsx`、`basedata.css`：表单、向导、弹窗、内层容器；保留所有 `wb-table-frame` 标记与危险确认。
- `plana.css`：原生基础资料按用途统一，并按用户最新反馈恢复主线图局部视觉例外。
- `GanttBoard.jsx`、`GanttScreen.jsx`、`gantt-board.css`、`field-gantt.css`、`field-reporting.css`、`dashboard-workbench.css`：图表框架、控件、标签及浮层；时间条长度/位置、计划虚线、停机斜纹、业务填色、选中与有意淡化语义保留。
- `detail-drawer.js`、`datepicker/aps-datepicker.css`：仅局部 CSS 形状，日期 JS 不变。名为 drawer 的现有详情组件实际是居中浮层，保持原定位并使用 8px，而非擅自改成贴边抽屉。

## 例外

- 圆点、原生单选、真实开关、滚动条和微型图例保持功能形状；等价 `0.25rem` 的 DS Badge 保持原 4px。
- 用户指定主线图例外保留保存版 8/10/11/12/13/14/16px 及局部胶囊形状，不向普通页面推广。原版微型 2/3px 标识保持。
- 焦点、选中、错误提示环不是浮卡阴影；它们保留。选链之外的 `opacity:0.24`/灰度保留，不计入正常态对比度达标声明。

## 已完成验证

- 原入口共享交互、形状与文字：1419 项断言，1084 个文字样本最低 4.76:1。
- 报表/业务页新圆角及等高 CSS 合同：506 项；旧业务回归：154 项。
- 原生业务：2383 项；用户指定主线例外后的新圆角合同：子代理带保存版对照 6865 项，主代理最终重跑未传额外对照参数为 6863 项，均通过。
- 基础表单/日期/详情新合同：249 项；批次旧业务：170 项。
- 图表形状合同：1039 项；时间条检查 5631 项，220 个文字样本最低 4.744:1、192 个图形/边界样本最低 4.344:1。
- 现场甘特原测试补齐 `workbench-ui.css` 令牌源，未删除或降低原断言；完整 `--host` 回归 247 项通过。
- 报工 74、行内数量 69、更正 62、原记录上下文 63、布局 41、工具栏及 XLSX 导出往返 76、真实 XLSX 导入 44、首页 149 项通过。自动完工 9 个、导入模型 23 个单元测试通过。
- 工作台顶层 16 个 JS 语法、24 个 JSX 转译、7 个 CSS 加日期 CSS 解析通过。

最终合并轮次再次通过：共享 1419 项、原生圆角 6863 项、基础表单/保存版对照 249 项、图表形状 1039 项、时间条 5631 项、报工对比度 5736 项。报表等高补充检查后的业务圆角测试 506 项、现场甘特完整宿主 247 项通过。442 份保存文件全部复验一致，保存时暂存差异 SHA-256 未变；23 份既有变化文件的差异空白检查通过，21 个变化或本轮曾改变的资源引用使用 `20260907-radius-system-2`，避免恢复原版 JS 后仍读到中间缓存。

## 并行与验证边界

真实创建 4 个子代理，分别负责原生基础资料、React 业务页、图表/报工、表单/日期/详情。原生代理在用户新反馈后复用一次，实际仍为 4 个，不冒充第 5 个；全部已完成并关闭，无创建失败。

浏览器工具此前明确拒绝本地文件访问，没有以其他浏览器、HTTP、复制页或 CDP 绕过。依据是用户截图、真实入口源码、JSDOM/CSS 合同及真实 XLSX；没有新的浏览器像素或真实视口对齐证明。已有 React `fill-opacity` 警告保留。

仅前端原型及定向测试，没有生产后端、数据迁移或新依赖；未运行会写范围外产物的后端整仓门禁，不构成 clean-worktree proof。部分旧导入/导出本来就是示例操作，没有声称已接通后台。

本轮未提交、未清理。`前端设计/` 仍受 Git 忽略；新增根目录测试和本记录未跟踪。原有暂存文件及其他改动保留。文件快照不包含浏览器内存中的临时报工数据；刷新仍重置示例会话。

## 保存点与复跑

保存点：`/Users/lurenxing/.codex/visualizations/2026/09/07/01a079ea-544f-73f0-aef0-d63b087bb017/workbench-before-radius-2026-09-07-193332/`，含 `README.md` 与 `snapshot.json`。

测试使用现有本地依赖：`NODE_PATH=/Users/lurenxing/.codex/visualizations/2026/09/07/01a079ea-544f-73f0-aef0-d63b087bb017/.qa-dom/node_modules node <测试路径>`。新增测试为根目录 `tests/workbench-native-radius.cjs`、`tests/workbench-base-radius.cjs`、`tests/workbench-chart-radius.cjs` 以及工作台内 `tests/workbench-business-radius.cjs`。
