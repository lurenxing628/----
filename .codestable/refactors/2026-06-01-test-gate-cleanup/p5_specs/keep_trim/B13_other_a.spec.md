# KEEP_TRIM Spec — Batch B13_other_a

Phase P5.2 KEEP_TRIM. Branch `cleanup/p3-main-style-to-pytest`. Generated by recon analyst.

These three files mix REAL behavioral contracts with BRITTLE cosmetic/source-text snapshots.
The implementation agent applies the deletions below using the verbatim UNIQUE ANCHOR strings.
Never delete an anchor whose removal would empty a kept function or break a fixture/helper/import.

> **Cross-batch coupling note (read first).** `tests/regression_page_manual_registry.py` is a
> **file-PATH member of the quality-gate required-test / py38-scan / source-file set** in:
> - `tools/quality_gate_shared.py:146` (QUALITY_GATE_REQUIRED_TESTS path list)
> - `tools/quality_gate_shared.py:812` + `:823` (py38 syntax-scan arg list)
> - `tests/test_run_quality_gate.py:706`, `tests/regression_quality_gate_registry_split_scope_contract.py:35`,
>   `tests/regression_aps_three_gap_docs_quality_gate.py:98` (path-membership snapshots)
>
> ALL of these reference the file **by path only**, never by function nodeid or assertion content.
> Trimming assertions *inside* the file keeps the path intact → these references do NOT break.
> BUT it means this file is itself a doc-consistency / registry GATE in the quality pipeline, so
> trim is deliberately conservative (risk=mid) and ALL structural-integrity asserts are KEPT.
>
> No test FUNCTION in any of the three files is referenced by nodeid anywhere in `tools/` or `tests/`
> (grep of every funcname returned zero external hits). So whole-function deletions, where proposed,
> dangle nothing. (Spec proposes NO whole-function deletions — see per-file sections.)

---

## File 1: tests/regression_lazy_select_orphan_option.py

- **Current line count:** 110
- **TSV verdict:** `mid` — "real orphan-option + source case-insensitivity behavior but tail asserts exact HTML option attr strings + greps batch_detail_linkage.js source symbols" (cited stale anchor `:124`, file is only 110 lines now — re-mapped by content below).
- **Structure:** ONE test function `test_lazy_select_orphan_option(app_client, repo_root)`. No external nodeid refs (grep clean). Do NOT delete the whole function (would lose the real orphan-option render contract).

### What is REAL contract here — KEEP

The genuinely valuable, behavior-anchored part is the rendered-output contract that the orphan
fallback `<option>` is emitted at all and is force-selected/disabled, plus the source
case-insensitivity regression (`source:"INTERNAL"` must still be recognized as an internal op =>
`data-linkage-row="1"`), plus the lazy-template-exclusion invariant. These are KEEP:

- The whole `ctx = dict(...)` fixture block (lines ~21-65) — KEEP (it is the test setup; deleting
  it breaks everything).
- The `with app.test_request_context(...)` + `render_template(...)` (lines ~67-68) — KEEP.
- `assert 'data-linkage-row="1"' in html, "source 大小写不敏感回归..."` — **KEEP**. This is the
  real case-insensitivity behavioral regression (INTERNAL recognized as internal op), not cosmetic.
- The two `<template>` exclusion asserts (the `missing_mc not in m.group(1)` / `missing_op not in
  m2.group(1)` block, lines ~86-92) — **KEEP**. This is a structural invariant that encodes the bug
  reproduction condition (value absent from lazy template), and the `assert m` / `assert m2` guards
  that the templates exist. Real contract.

### BRITTLE item 1.A — exact HTML `<option>` attribute-string snapshots (TRIM the exact-string form, KEEP a behavior-anchored form)

The `exp_mc` / `exp_op` expected-string builders assert the option's **full verbatim attribute
order** (`selected disabled data-static-disabled="1" data-orphan="1"`) AND the exact
`（已删除）` suffix text. Asserting the full attribute string in exact order is brittle: re-ordering
attributes or restyling the placeholder text fails with no behavior regression. BUT the *presence of
the orphan fallback option, force-selected, marked data-orphan* IS the contract. So do NOT delete
outright — **down-anchor** to the load-bearing tokens.

This is a **trim-the-exact-string, keep-the-behavior** edit (granularity (a), inside the kept
function). Replace the two big f-string builders + their two equality-in asserts.

**ANCHOR (delete this exact block — lines ~70-82):**
```
    # 1) 首屏 fallback option 必须存在且带 data-orphan + “已删除”
    exp_mc = (
        f'<option value="{missing_mc}" selected disabled data-static-disabled="1" data-orphan="1">'
        f"{missing_mc}{deleted_suffix}"
        "</option>"
    )
    exp_op = (
        f'<option value="{missing_op}" selected disabled data-static-disabled="1" data-orphan="1">'
        f"{missing_op}{deleted_suffix}"
        "</option>"
    )
    assert exp_mc in html, "首屏缺少 machine 回退 option（或属性不匹配）"
    assert exp_op in html, "首屏缺少 operator 回退 option（或属性不匹配）"
```
**REPLACE WITH (keeps the behavioral contract — option exists, is orphan-marked, force-selected,
carries the "已删除" suffix — without pinning verbatim attribute order):**
```
    # 1) 首屏 fallback option 必须存在且带 data-orphan + 强制选中 + “已删除”后缀
    #    （只校验载荷标记，不锁死属性书写顺序，避免模板格式微调误报）
    assert f'value="{missing_mc}"' in html and f"{missing_mc}{deleted_suffix}" in html, "首屏缺少 machine 回退 option（值或“已删除”后缀）"
    assert f'value="{missing_op}"' in html and f"{missing_op}{deleted_suffix}" in html, "首屏缺少 operator 回退 option（值或“已删除”后缀）"
    assert html.count('data-orphan="1"') >= 2, "首屏回退 option 缺少 data-orphan 孤儿标记"
    assert html.count('data-static-disabled="1"') >= 2, "首屏回退 option 缺少 data-static-disabled 静态禁用标记"
```
- **Why brittle:** exact full-attribute-string-in-order snapshot of rendered HTML; only failure mode
  is attribute re-ordering / whitespace, not a behavior bug. The replacement keeps every meaningful
  signal (value present, deleted-suffix present, orphan marker present, static-disabled present).
- **Net:** ~13 lines deleted, 6 added → ~7 net lines removed. The function keeps strong assertions.
- **Conservative alternative (if implementer prefers zero-risk):** keep the block as-is. Marked
  brittle but optional. Spec recommends the replacement.

### BRITTLE item 1.B — JS source-text symbol greps on batch_detail_linkage.js (TRIM, keep the load assert)

Lines ~94-105 read `static/js/batch_detail_linkage.js` as TEXT and grep for internal symbol names
(`orphanOpt.selected = true`, `isSelectedOrphan`, `optionsLoadFailed`, `dataset.lazy`,
`dataset.orphan = "1"`). Verified these symbols exist in the JS today (greps return 1-3 hits each).
This is the canonical **JS source-text grep** brittle pattern: renaming a local variable or
refactoring the JS file fails the test with zero behavior change, and the test never executes the JS.

**KEEP** the assertion that the template actually loads the external script (that is a real wiring
contract: `assert "js/batch_detail_linkage.js" in html`). **TRIM** the file-read + 5 symbol greps.

**ANCHOR (delete this exact block — lines ~99-105, the file-open + 5 symbol asserts):**
```
    js_path = os.path.join(str(repo_root), "static", "js", "batch_detail_linkage.js")
    js = open(js_path, "r", encoding="utf-8", errors="replace").read()
    assert "orphanOpt.selected = true" in js, "缺少 orphanOpt.selected 强制选中逻辑"
    assert 'dataset.orphan = "1"' in js or 'data-orphan"' in js, "缺少 data-orphan 标记逻辑"
    assert "isSelectedOrphan" in js, "缺少 isSelectedOrphan（孤儿/已删除选中项识别）逻辑"
    assert "optionsLoadFailed" in js or "data-options-load-failed" in js, "缺少 optionsLoadFailed（懒加载失败标记）逻辑"
    assert "dataset.lazy" in js, "缺少 data-lazy 保护条件逻辑"
```
**KEEP the line immediately above it (do NOT delete):**
```
    assert "js/batch_detail_linkage.js" in html, "模板未加载 batch_detail_linkage.js"
```
- **Why brittle:** static JS-file text grep for internal identifiers; cosmetic-drift-only failure
  mode; does not exercise behavior.
- **Side effects:** after deletion, `js_path` and `js` locals vanish — they are used ONLY inside the
  deleted block (verified), so no dangling reference. `repo_root` fixture is still used? After this
  deletion `repo_root` becomes unused — that is harmless (an unused pytest fixture arg does not
  error). Leave the `repo_root` param in the signature to avoid touching the fixture wiring.
- **`import os`:** after this deletion `os` is no longer used in the file (only use was
  `os.path.join` here). Implementer MAY remove the `import os` line (line 3) to avoid an unused-import
  lint, but the quality gate for this file is not in the py38/import set in a way that fails on unused
  imports — to stay conservative, leaving `import os` is acceptable. Recommend removing it for
  cleanliness. (`import re` stays — still used by the template regexes.)
- **Net:** ~7 lines deleted (+1 optional `import os`).

### B-4 handling
None. Not a B-COMPAT B-4 red-line file.

### Risk note — File 1: **low**
The kept function retains: case-insensitivity regression, lazy-template exclusion invariant,
script-load wiring, and orphan-option presence/marker contract. Only verbatim-attribute-order and
JS-internal-symbol snapshots are trimmed. Function never ends with zero meaningful asserts.
**est_lines_removed: ~14** (7 from 1.A net, 7 from 1.B; +1 if `import os` removed).

---

## File 2: tests/regression_manual_entry_scope.py

- **Current line count:** 522
- **TSV verdict:** `mid` — "real src/page safe-url hardening (rejects evil.example) + manual md
  source-of-truth but bulk asserts exact HTML/aria/CSS class strings + greps ui_contract.css"
  (cited stale anchor `:487` → re-mapped to the CSS/HTML-grep tail at current lines 488-510).
- **Structure:** module-level `main(monkeypatch)` (lines ~199-512) does ALL the work; the single
  pytest entry `test_manual_entry_scope_contract(monkeypatch)` (line 515) just calls `main`. No
  external nodeid refs (grep clean). Do NOT delete `main` or the entry function.

### What is REAL contract here — KEEP (the bulk of the file)

This file's high-value behavioral contracts MUST be kept:
- Every `resp.status_code != 200` / redirect-status check (HTTP status contracts).
- The SSRF / safe-url hardening block: `src="http://evil.example/x"` and `src="//evil.example/x"`
  cases asserting `src=` is stripped from download/full/related hrefs and the redirect `Location`
  does not echo `evil.example` / `unknown.endpoint` (lines ~374-412, 467-486). **KEEP ALL.**
- Manual-markdown source-of-truth equality (`_assert_manual_markdown_scope`, the
  `full_fallback_text != manual_text.strip()` consistency check at ~293, the V1/V2 mirror equality)
  — KEEP (structural source-of-truth invariant).
- The download filename contract `expected_filename = quote("系统使用说明.md", ...)` (lines ~452-465)
  — KEEP (real user-facing download contract with no other anchor).
- Structural ORDERING invariants that encode a layout RULE (not a pixel): `0 <= sidebar_idx <
  manual_idx` (~248), `main_col_idx < content_idx < related_panel_idx` (~343),
  `page_material_html.count('id="content"') != 1` (~338). **KEEP** — these encode "manual entry must
  render inside sidebar / single #content / right-column order" rules; a violation is a real bug.
- The legacy-Excel-term forbidden checks (`_assert_no_legacy_excel_entry_terms`) and the
  `_assert_home_workspace_buttons` endpoint/href derivation (built from `url_for`, not hardcoded) —
  KEEP (these derive hrefs dynamically; they are contract, not snapshot).
- All helper functions (`_assert_contains`, `_extract_*`, `_build_url`, etc.) — KEEP (load-bearing).

### BRITTLE item 2.A — CSS pixel/geometry + raw CSS-selector source greps on ui_contract.css / style.css (TRIM the geometry literals + the source-text class greps; KEEP the structural placement intent via the existing HTML ordering asserts)

Lines ~488-510 read THREE static asset files as TEXT (`ui_contract.css`, `web_new_test/.../base.html`,
`web_new_test/.../style.css`) and grep for CSS selectors / pixel geometry / source fragments. This is
the canonical **CSS pixel/geometry literal** + **source-text grep** brittle pattern. The genuinely
load-bearing placement contract ("manual entry renders inside sidebar, not covering main buttons") is
ALREADY enforced behaviorally by the rendered-HTML ordering assert at ~248
(`0 <= sidebar_idx < manual_idx`) and the `sidebar-footnote`/version-footnote not-contains at ~250-252
— those are KEPT. The CSS-file greps merely re-assert the same intent against raw stylesheet text.

The clearly-brittle sub-asserts to TRIM (pixel geometry +共用规则 source-text微调):
- `_assert_contains(ui_contract_css, "position: relative;", ...)` — generic CSS literal; `position:
  relative;` appears 7× in the file; brittle and near-meaningless as a contract.
- `_assert_contains(ui_contract_css, "left: calc(100% + 12px);", ...)` — **pixel/geometry literal**
  (`12px`); pure cosmetic.
- `_assert_not_contains(v2_style_css, ".nav-item:hover, .nav-item.active", ...)` — asserts a specific
  CSS rule-grouping *string form*; restyling that merges/splits selectors fails with no behavior bug.

KEEP (still arguably a structural contract, BIAS=keep): the selector-PRESENCE asserts
`.sidebar .floating-manual-wrapper`, `.sidebar .floating-manual-btn`, `.manual-popover`,
`aria-current="page"`, `ep.startswith('scheduler.')`, the `Excel 演示` / hover / active /
`aria-current` selector-presence asserts. These encode "the modern UI keeps the manual entry in the
sidebar and supports aria-current" which is a real (if source-coupled) contract — under the KEEP bias
we do not strip them.

**ANCHOR — delete ONLY these three lines (the pixel + generic-position + rule-grouping greps):**

Delete (the generic position literal):
```
    _assert_contains(ui_contract_css, "position: relative;", "说明入口不应继续固定悬浮覆盖业务按钮")
```
Delete (the pixel-geometry literal):
```
    _assert_contains(ui_contract_css, "left: calc(100% + 12px);", "现代界面说明速览应从侧边栏向内容区展开")
```
Delete (the CSS rule-grouping source-form assert):
```
    _assert_not_contains(v2_style_css, ".nav-item:hover, .nav-item.active", "现代侧栏 hover 和 active 不应继续共用同一条规则")
```
- **KEEP context:** all surrounding `_assert_contains(ui_contract_css, ".sidebar ...")`,
  `.manual-popover`, the `v2_base_html` aria/endpoint asserts, and the `v2_style_css` `.nav-item:hover`
  / `.nav-item.active` / `aria-current` presence asserts stay.
- **Side effects:** `ui_contract_css`, `v2_base_html`, `v2_style_css` locals are STILL used by the
  kept asserts → do NOT remove the three `.read_text(...)` reads at lines ~488-490. No dangling.
- **Why brittle:** `12px` pixel literal + generic `position: relative;` + verbatim CSS rule-grouping
  string; cosmetic-drift-only failure mode.
- **Net:** 3 lines deleted.

### Whole-function deletion
NONE. `main` and the entry function are kept (they carry all the HTTP/SSRF/structural contracts).
grep for `test_manual_entry_scope_contract` → zero external nodeid refs.

### B-4 handling
None. Not a B-COMPAT B-4 red-line file.

### Risk note — File 2: **mid**
This file is a doc-source-of-truth + SSRF-hardening regression with very high real value; the brittle
surface is a thin CSS/source-grep tail. Trim is intentionally minimal (3 pixel/rule-form literals).
We deliberately do NOT trim the selector-presence or aria asserts (KEEP bias) even though they grep
source text, because they double as the modern-UI placement contract whose behavioral mirror
(`sidebar_idx < manual_idx`) we are keeping. **est_lines_removed: ~3.** Under-trim by design.

---

## File 3: tests/regression_page_manual_registry.py

- **Current line count:** 995
- **TSV verdict:** `low` — "keep registry structural integrity (endpoint validity / no-dup
  manual_id / anchors-hit-headings / fragment-expansion) but ~800 lines verbatim Chinese phrase
  snapshots + source-file greps" (cited stale anchor `:606` → file is 995 lines; the structural core
  is the `main()` body at ~444-657 and the title/anchor contracts).
- **Structure:** `main(monkeypatch)` orchestrator (~444-657) + many helper `_assert_*` functions +
  TWELVE pytest entry functions. Every funcname grepped → **zero external nodeid refs** (no
  tools/test_registry_data or contract test pins any of these). BUT the FILE PATH is a quality-gate
  required-test / py38-scan member (see top note) → keep the file present, trim only inside.

### CRITICAL: this file is the page-manual REGISTRY GATE — KEEP all structural integrity

The L3 reason explicitly flags the structural-integrity asserts as the VALUABLE core to keep. These
are NOT brittle and MUST be kept in full:
- `main()` lines ~522-606: endpoint count, invalid-endpoint set, missing-legacy set, **no-dup
  manual_id** (`len(set(values)) == len(...)`), topic/entry one-to-one, override validity,
  related-id validity / `<=4` / no-self-ref, bundle preview-section caps. **KEEP ALL.**
- `main()` lines ~574-578: `full_manual_anchor` must start with `#` AND its slug must hit a real
  heading id (`anchor[1:] in manual_heading_ids`). **KEEP** — anchor-hits-heading is a real
  structural invariant.
- The `{{fragment}}` expansion / no-unexpanded-fragment asserts (~558-560, 571-572). **KEEP** —
  structural.
- The two title equality pins that the reason calls out as registry-anchors:
  `manual_topics["system_backup"]["title"] == "备份/恢复"` and `...["system_history"]["title"] ==
  "排产历史"` (lines ~537-538). These are **manual_id→title registry anchors** (short, stable keys),
  KEEP — they are the "registry doesn't drift" guard, not freeform copy.

### What IS brittle here — large verbatim Chinese-phrase snapshot tables

The bulk of lines ~119-431 are module-level dicts/tuples of **verbatim user-facing Chinese sentence
fragments** that are asserted `in payload_text` / `in manual_text`. These are the "doc-text snapshot"
brittle pattern: any copy-editing of the manual (rewording a sentence, fixing punctuation) breaks the
test with zero behavior regression. HOWEVER — and this is decisive for the KEEP bias — many of these
encode **user-corrected SEMANTIC contracts** (the "齐套检查 does NOT auto-skip" correction, the
"留空默认值" defaults, the refusal conditions, the FORBIDDEN legacy phrasings). The forbidden-phrase
checks especially are real anti-regression guards (they catch the manual reverting to a wrong claim).

Per the KEEP bias and the META-GATE caution (this file is a quality gate), the spec **KEEPS the
semantic/forbidden contracts** and trims ONLY the title-DRIFT snapshots that are pure cosmetic copy
with a stable structural alternative already present, AND only where a deletion leaves the owning
function with other meaningful asserts.

### BRITTLE item 3.A — exact help-card-title freeform-copy pins in the four `*_TITLE_CASES` tables (TRIM the third tuple element = help_title copy; KEEP manual_id + title)

`PROCESS_PAGE_MANUAL_TITLE_CASES`, `EXCEL_PAGE_MANUAL_TITLE_CASES`,
`SCHEDULER_PAGE_MANUAL_TITLE_CASES` each map endpoint → `(manual_id, title, help_title)`. The
`_assert_*_title_contracts` helpers assert all three are EXACT-equal. Of these:
- `manual_id` equality = **registry anti-drift contract → KEEP** (a wrong manual_id is a real
  mis-wiring bug).
- `title` equality (e.g. `"零件工艺模板"`, `"工种配置"`) = short page-name registry key → **KEEP**
  (stable, load-bearing identity; not freeform copy).
- `help_title` (3rd element, e.g. `"零件工艺模板是排产计算的起点"`, `"工种配置——填写速查"`) =
  **freeform popover sentence → BRITTLE.** It is a full user-facing copy snapshot; re-wording the
  help-card title fails the test without any wiring/behavior regression, and there is no contract
  that the exact sentence is load-bearing (the manual_id+title already pin identity).

**This is a real-content edit and is the single largest brittle surface. RECOMMENDED but marked
risk=mid** because it touches the table shape used by three helpers. Two execution options:

**Option A (surgical, lower blast radius — RECOMMENDED): drop the help_title equality assert only,
leave the tables 3-tuple-shaped.** In each of the three helpers
(`_assert_excel_page_manual_title_contracts`, `_assert_process_page_manual_title_contracts`,
`_assert_scheduler_page_manual_title_contracts`) delete the help-card-title equality block while
keeping manual_id + title asserts and the legacy-term check.

Delete this exact block (it appears 3×, once per helper — `replace_all` is appropriate; the block is
character-identical across the three):
```
        help_card = current_manual.get("help_card") or {}
        assert help_card.get("title") == expected_help_title, (
            f"{endpoint} 的“本页说明”弹层标题不应漂移："
            f"期望 {expected_help_title!r}，实际 {help_card.get('title')!r}"
        )
```
KEEP, immediately above, in each helper:
```
        assert current_manual.get("manual_id") == expected_manual_id, (
            ...期望 manual_id={expected_manual_id}...
        )
        assert current_manual.get("title") == expected_title, (
            ...期望 {expected_title!r}...
        )
```
And KEEP, immediately below: `_assert_no_legacy_page_title_terms(endpoint, current_manual)`.
- After deletion, the loop var `expected_help_title` becomes unused. To avoid an unused-var lint and
  keep the table literals untouched, change each `for` unpack from
  `for endpoint, (expected_manual_id, expected_title, expected_help_title) in ...:` to
  `for endpoint, (expected_manual_id, expected_title, _expected_help_title) in ...:` (underscore
  prefix). The three TABLE dicts stay 3-tuple — **do NOT rewrite the dict literals** (that would be a
  huge brittle diff for no gain).
- **Why brittle:** the 3rd tuple element is a verbatim popover sentence (doc-text snapshot); only
  failure mode is copy editing. manual_id + title still guard registry identity.
- **Net:** 5 lines × 3 helpers = ~15 lines deleted; 3 one-token edits to the `for` lines.
- **Helpers kept non-empty:** YES — each still asserts manual_id + title + legacy-term.

> The `_assert_home_page_manual_contract` (`HOME_PAGE_MANUAL_REQUIRED_PHRASES`) and the four
> `*_TITLE_CASES` `manual_id`/`title` pins are NOT trimmed.

### BRITTLE item 3.B — the large `full_manual_phrases` verbatim doc-text snapshot in main() (KEEP — do NOT trim)

Lines ~634-655 assert ~17 verbatim manual sentences `in manual_text` plus one forbidden phrase. These
ARE doc-text snapshots and ARE the most "trimmable-looking" block. **DECISION: KEEP under the KEEP
bias + META-GATE caution.** Rationale: this file is the registry/doc-consistency GATE; many of these
phrases (`"系统不会自动跳过这些批次继续排其它批次"`, the `"用来查看、导出、恢复这些版本" not in`
forbidden check at ~655) are **user-corrected semantic contracts** — the loud-failure guard that the
manual has not regressed to a wrong operational claim. Deleting them silently removes the only
enforcement that the corrected口径 stays corrected. The cost of keeping is only cosmetic-edit
friction, which is acceptable here. **No edit.**

### BRITTLE item 3.C — the USER_CORRECTED / REFUSAL / SEMANTIC phrase tables (KEEP — do NOT trim)

`USER_CORRECTED_TOPIC_SEMANTIC_CASES`, `USER_CORRECTED_FULL_MANUAL_PHRASES`,
`PAGE_MANUAL_REFUSAL_CONDITION_CASES`, `PROCESS_USER_CORRECTED_REQUIRED_PHRASES`,
`PROCESS_USER_CORRECTED_FORBIDDEN_PHRASES`, `SCHEDULER_PAGE_MANUAL_FORBIDDEN_PHRASES`,
`READY_CHECK_REQUIRED_COPY`, `READY_CHECK_LEGACY_FILTER_COPY`, `PAGE_MANUAL_CLOSEOUT_FORBIDDEN_PHRASES`,
`EXCEL_BLANK_DEFAULT_PAGE_CONTRACTS`, the `manual_semantic_cases` block in main(), etc.
**ALL KEEP.** Although phrased as Chinese-fragment-in-text, these are **required/forbidden semantic
guards** (齐套检查 not-auto-skip correction, 留空默认值 defaults, refusal conditions, anti-legacy-叫法
ratchets). They are the differential oracle that the manual's *meaning* hasn't drifted. Trimming them
reduces enforcement of corrections the user explicitly made. Per META-GATE caution: KEEP.

### Whole-function deletion
NONE. All 12 pytest entry functions are kept. grep of every funcname → zero external nodeid refs, so
nothing dangles, but we keep them because each runs a distinct structural/semantic contract slice and
the file is a quality gate. The implementation agent must NOT delete any `_assert_*` helper (each is
still called) and must NOT delete `main`.

### B-4 handling
None. Not a B-COMPAT B-4 red-line file. (It IS a meta/quality gate — handled via META-GATE caution
above: conservative, structural-integrity preserved.)

### Risk note — File 3: **mid**
Enforcement that would be LOST if over-trimmed: registry no-dup-manual-id, anchor-hits-heading,
fragment-expansion, and (critically) the user-corrected 齐套检查/默认值/refusal/anti-legacy semantic
ratchets. The spec trims ONLY the freeform help-card-title copy pins (3.A) — pure popover-sentence
snapshots already backstopped by the manual_id+title registry pins. Everything semantic/structural is
KEPT. The file path stays present → quality-gate path-membership references intact.
**est_lines_removed: ~15** (help_title assert block ×3). Deliberate heavy under-trim on an ~995-line
file because it is a gate.

---

## Batch summary

| file | lines | trim_type | est_removed | whole-func del | b4 | risk |
|---|---|---|---|---|---|---|
| regression_lazy_select_orphan_option.py | 110 | js_source_grep + exact HTML attr snapshot (mixed) | ~14 | none | no | low |
| regression_manual_entry_scope.py | 522 | css_pixel + source greps | ~3 | none | no | mid |
| regression_page_manual_registry.py | 995 | doc_snapshot (help-title copy) — gate | ~15 | none | no | mid |

No whole-function deletions in the batch. No registry/contract nodeid coupling on any function.
File 3's path is a quality-gate member (path-only refs) → trims are inside-function, gate preserved.
Bias applied throughout: KEEP every structural invariant, every HTTP/SSRF contract, every
user-corrected semantic/forbidden ratchet; trim only verbatim attribute-order, JS-symbol greps, CSS
pixel/rule-form literals, and freeform popover-title copy that has a stable registry-key backstop.
