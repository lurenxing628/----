---
doc_type: issue-fix-note
issue: field-gantt-view-groups
date: 2026-09-08
status: fixed
tags: [prototype, gantt, grouping, critical-chain]
---

## 本次补齐当前样例关键链
- 修复范围是 `file://` 前端原型的“当前报工”v15 样例，不是正式生产接口。`FieldGanttScreen.jsx:103` 原来对当前报工直接使用空目录，现在读取并校验自己的结果；初始不再显示“关键链未接入”的占位。
- 新增 `前端设计/ui_kits/workbench/field-gantt-current-chain-data.js`。开发生成器 `tests/build-field-gantt-current-chain-data.cjs` 从原共享报工模型提取计划字段，实际调用仓库 `compute_critical_chain_from_rows` 和既有归一化函数，生成全局结果及五份逐任务结果；交付页面只加载本地 JS，不依赖 Python 或在线请求。
- 当前五道工序的批次、计划设备、计划人员各不相同。既有算法的全局结果是 `t2 / B202609-032 / 20 预加工`，结束时间 `2026-09-08 11:00:00`；逐工序均为单节点、零边。没有借其他样例的链，也没有为了画线补造工序关系。跨度标为“计划跨度 27h”，不是有效加工工时。
- `field-gantt-current-chains.js` 校验版本、生成时间、范围及完整计划字段；缺项、不同批次同名 ID、计划变化、计算失败/部分结果均清空链并明示原因。实际报工更正、换实际资源、草稿或示例时点变化不篡改原计划结果。当前独立样例的生成器拒绝多工序/共享资源新关系，避免逐任务计算静默截断依赖。
- `field-gantt-chains.js` 接受经过该适配器约束的 `computed-example` 目录，沿用现有对象关联选择；`FieldGanttChain.jsx` 标明“样例计算 · v15”，无边时说明“本样例内无前驱连线”。默认及空白处恢复当前样例全局结果，悬停具体对象切换对应结果；零边的连线开关禁用且不渲染空 SVG。`index.html` 接入新增本地资源并更新缓存版本。

## 本次验证与边界
- 主代理重跑快照 `--check` 和真实后端生成一致性 159 项、适配器/实际入口 177 项、当前报工宿主 503 项、复杂/密集宿主 492 项、hover/定位 216 项、工具栏/截止点 4940 项、浅深色样式 1150 项、工作区 257 项、原预设链选择 3228 项通过。两份 JSX、三份 JS 语法及 64 个本地入口资源检查通过。
- 主代理运行后端关键链 scope/unavailable 针对性测试，13 项通过。一个真实子代理生成快照及开发校验器，完成多时区与原模型隔离检查，已完成并关闭；其环境没有 JSDOM，实际入口 DOM 验证由主代理使用既有 QA 依赖补齐。
- 本轮未修改 `field-report-model.js`、复杂/密集样例事实或生产关键链算法；未提交/暂存。原有 200 行暂存及其他 dirty 保留。新增前端文件仍被现有 Git 忽略规则覆盖，只保存在本地。
- 正式生产计划/数据接口尚未连接，不把当前样例计算包装成生产实时结果。未跑整仓质量门禁，不构成 clean proof；本地 DOM/CSS 验证不是新的浏览器截图，未绕过浏览器访问限制。没有自动刷新用户页面。

## 首轮根因与范围
`FieldGanttScreen.jsx` 虽按资源分组，但上一版把 `lane.name` 从可见标题移除；现有五道工序又是一批次、一设备、一人员各对应一项，三种视图只改了隐藏归属和角标，主体看起来相同。现场模型没有关键链结果或工序依赖，主甘特使用另一批示例数据，不能按短 `t1` 等同名 ID 接链。

## 首轮已修改
- `前端设计/ui_kits/workbench/FieldGanttScreen.jsx`、`field-gantt.css`：增加显式设备、人员、批次分组标题；分别汇总批次数、设备数、完工工序数及当前分组的已知实报工时。批次视图突出工序和零件名称并自然排序；不改模型或已保存报工。
- `index.html` 更新这两项资源版本；`tests/field-gantt.integration.cjs` 和 `tests/field-gantt-style.cjs` 增加分组身份、跨资源归集、工序排序及文字对比度合同。
- 明示“关键链未接入 · 计划 v15”，未制造边、关键标记或借用其他批次。关键链本身尚未完成对接，不能宣称已修复。

## 首轮验证与剩余（历史）
现场完整宿主 393 项、浅深色样式 804 项、工作区合同 251 项通过；包括同人员跨两设备、同设备跨批次、逐次报工/剩余、CSV、报工保存后切页。样式文字最低 5.37:1。一个只读子代理核对全部原型的链路来源，未发现与当前五道工序匹配的数据，已完成关闭，无创建失败。

产品已有 `/scheduler/gantt/data` 的 `tasks`、`critical_chain` 合同，现场原型仍需同计划结果及准确任务映射后才能显示真正链路。没有浏览器像素证据，未绕过浏览器访问限制；保留原有 `fill-opacity` 警告。未跑整仓门禁、未暂存或提交，不构成 clean proof；已有 dirty 不变。刷新原型仍会重置示例报工。
