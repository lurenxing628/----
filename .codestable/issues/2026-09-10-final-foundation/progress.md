# 最终全站验收中的共享外壳补漏

- 授权：第二轮除 Win7 发布外的集成与终验实施；Main 拥有共享外壳、导航、构建和最终集成。
- 状态：局部实施与合同测试进行中，全站未验收通过。
- 不更改排产算法、共享数据库 schema、候选选择或正式采用语义。

## 已确认与处理

| 缺口 | 根因与处理 | 当前证据 |
| --- | --- | --- |
| 侧栏返回丢原上下文/滚动 | 原 `main.jsx` 只把调用参数写进当前 history，不保存每页上下文和 main/window 滚动；新增 `WorkbenchNavigation.js`，保留精确对象/范围及页面专用 history；坏上下文显示错误，不退回全量查询 | Node VM 15 项导航合同；纯模拟状态测试，不冒充浏览器 |
| 重新获焦不同步主题 | 原 `theme.js` 只监听 storage/pageshow；补 focus，继续同一 canonical/prototype/cookie 优先级与可见失败语义 | 主题 VM 23 检查 |
| 标准浏览器字段被资产扫描误判 | 新导航使用 `scrollX/scrollY`，本地资产分析器没有这两个原生字段；精确登记并扩拼写/未知名拒绝和真实 Chrome 109 探针 | 含导航、主题与实际 Chrome 109 原生字段的定点组 11 passed / 7.52 秒；没有放宽 project global 或加载顺序 |
| 主脚本缺失可能只留下空根节点 | 当前模板 root 为空，故障提示本身在 `main.jsx`，主脚本缺失时提示无法执行；需要独立、有限时的启动状态与渲染错误边界 | 已读模板与主脚本；待运行故障注入和修复。正式容量窗口不改产品 |
| delay 入口尚未接真实视图 | pages 有标题但 enabled_views/main 无分支。正式计划已有真实交付风险投影，候选逐批风险尚非同等能力 | D 静态确认；待接线及正式/候选边界测试，不以改标题冒充全 DELAY 通过 |

## 版本与验证

- 首批冻结导入合同提交为 `c00972784ccc129957f650836dd2a423792f7049`，与本共享外壳改动分别归档。
- 全构建 211 文件，`e09173b0bc08b022e9a365d516f555338a4a4cd2fc8f2a983ae012328abbe6cf`，包含运维域在途下载 UI；不是这些下载动作已验收通过。
- 本轮创建的第一个 Main 私有 host 在构建失败后尝试启动，因 manifest 不存在而明确退出，未创建业务 DB。该失败证据保留在 `aps-workbench-live-t34sja_l/host-errors.log`；不声称运行成功。后续使用新的私有 fixture，不绕过已有根目录复用守卫。
- 全部业务数据只用私有测试目录；原 53144 预览、生产库和原档案保持不动。
- 5000 正式窗口期间源码冻结，本文件只记录状态。最终浏览器必须用重新核验输入和输出哈希的完整 app，不能用 VM 或独立组件代替。

## 待完成

- 真实主入口 14 侧栏入口及 delay 情境入口、标题/激活态/前进后退/刷新、两种滚动面恢复。
- 当前计划胶囊须来自当前真实身份、范围及读取事实，不恢复原型固定 sample。
- 原生控件、日期和模态由对应业务验收轨实际操作并交图，Main 审视后才记录 V 通过。
- 缺资源/启动失败、网络失败、无数据、非法上下文分别验证，不统一成无限加载或默认对象。
- 1920x1080 与 1392x924 的深浅色；更新后的布局截图、原子动作证据、刷新/重启保持和最后源码绑定。
