roadmap: scheduler-global-optimizer
created: 2026-06-26

items:
  - slug: optimizer-proof-harness
    description: 建立 tiny oracle、lower bound、best-known 和不写 tracked evidence 的 benchmark check 模式
    depends_on: []
    status: done
    feature: 2026-06-26-optimizer-proof-harness
    minimal_loop: true
    notes: 最小闭环；gap 必须绑定 objective 和 metric；public benchmark 输出不能含内部 id/sample

  - slug: diagnostic-public-id-boundary-fix
    description: 修复诊断页展示 op 节点、数字 op_id 和内部样本的 public 边界问题
    depends_on: []
    status: done
    feature: 2026-06-26-diagnostic-public-id-boundary-fix
    minimal_loop: false
    notes: 已完成前置治理；public algo/graph/candidate/export/OperationLogs 已收紧为白名单摘要；diagnostics 可保留内部调试但不得进入普通页面、HTML、导出、OperationLogs public 投影；新增 trace/attempt/move/benchmark 样本前必须继续复用该投影边界

  - slug: optimizer-search-report-contract
    description: 给 optimizer 增加 stop_reason、seed、搜索统计、候选拒绝原因和 trace 合同
    depends_on: [optimizer-proof-harness, diagnostic-public-id-boundary-fix]
    status: done
    feature: 2026-06-27-optimizer-search-report-contract
    minimal_loop: false
    notes: baseline fallback、multi-start、optional OR-Tools warm-start failure、candidate_rejected、local search skipped/noop、time_budget、iteration_limit、no_improvement 都进入 OptimizationSearchReport；public 只投安全摘要，raw attempts/fingerprint 留 diagnostics；完整 candidate profile 与完整 CandidateFingerprint 仍留给 item 4/5

  - slug: optimizer-candidate-profile-contract
    description: 定义非 OR-Tools 搜索 profile，收紧配置校验和非法参数 fail-loud 合同
    depends_on: [optimizer-search-report-contract]
    status: done
    feature: 2026-06-28-optimizer-candidate-profile-contract
    minimal_loop: false
    notes: 已落地 CandidateProfile 合同(core/services/scheduler/run/optimizer_candidate_profile.py)并接入 OptimizationSearchReport.candidate_profile；configured/effective time budget 与 max_iterations 区分、迭代 [200,5000] 系统钳制如实写 system_limit_applied/system_limit_reason/iteration_limit_source；derive_iteration_limits 作为迭代上限单一真相源,local search 改为共用(零行为变化);未知 profile/repair/acceptance/neighborhood 与非法 budget 一律 fail-loud(与 strict 无关);profile_public/profile_diagnostics 白名单分层 + size guard 最小摘要保留 profile_public;seed 如实标 version 派生。item 4 初始落地时只覆盖 baseline/multi_start_local_search、repair=sgs、acceptance=improve_only 与旧 swap/insert/block 邻域；当前生产能力已由 item 6/7/8 扩展为 grasp_ig/vns_sa、六个业务邻域 + SGS 专用 sgs_dispatch_rule，以及 improve_only/threshold/record_to_record/simulated_annealing acceptance，未知值仍 ValidationError。未做 item 5 完整 CandidateFingerprint。

  - slug: distinct-candidate-fingerprint-contract
    description: 定义候选指纹、去重计数、same-fingerprint 拒绝和 improved 判定合同
    depends_on: [optimizer-candidate-profile-contract]
    status: done
    feature: 2026-06-28-distinct-candidate-fingerprint-contract
    minimal_loop: false
    notes: 已落地 CandidateFingerprint 合同(core/services/scheduler/run/optimizer_candidate_fingerprint.py);decision_fingerprint 覆盖 objective/strategy/params/dispatch/batch_order/resource/seed/mutable_scope,output_fingerprint 覆盖正式解码结果数量、工序-资源-时间签名、summary 与当前 objective score;distinct_candidates/accepted_distinct_candidates 改为按 decoded_output 去重并在 public/minimal 摘要声明口径;improved 显式要求 fingerprint_changed + score_strictly_better + acceptance_passed(acceptance=improve_only);same_as_parent/same_as_seen 记 same_fingerprint 拒绝,不算 improvement;hash 和内部输入只进 diagnostics,public/OperationLogs/size guard 不透传内部 id 或 fingerprint hash。

  - slug: grasp-ig-candidate-construction
    description: 用 GRASP 和 Iterated Greedy 生成更多可行起点，仍由现有 SGS 落位
    depends_on: [distinct-candidate-fingerprint-contract]
    status: done
    feature: 2026-06-28-grasp-ig-candidate-construction
    minimal_loop: false
    notes: 已落地 GRASP/IG 候选构造；默认 improve profile 扩展为 grasp_ig，candidate_strategy_families=multi_start/grasp/iterated_greedy；GRASP/IG 只生成批次顺序候选并通过 batch_order 派工解码，不保留不可达的 sgs 候选分支；候选接入 CandidateProfile/SearchReport/CandidateFingerprint，distinct 继续按 decoded_output 去重，same-fingerprint 不算 improved；没有真实 best acceptance event 时 acceptance_passed=false 且 improved=false；public/OperationLogs/size guard 只投安全摘要。

  - slug: business-neighborhood-registry
    description: 建立关键链、延期批次、瓶颈设备、换型块、资源替换、时间窗 repair 的邻域注册表
    depends_on: [distinct-candidate-fingerprint-contract]
    status: done
    feature: 2026-06-28-business-neighborhood-registry
    minimal_loop: false
    notes: 已落地业务邻域 registry 与 NeighborhoodMove 合同；支持 critical_chain/tardy_window/bottleneck_machine/changeover_block/resource_alternative/time_window 六个业务邻域，另有 SGS 专用 sgs_dispatch_rule 用于 dispatch_mode=sgs 时切换派工规则；旧 swap/insert/block 不在正式 registry 和 profile 白名单里；默认 improve profile 的 configured_neighborhoods 为六个业务邻域，实际执行 effective_neighborhoods 需按当前派工模式报告，SGS 时只能是 sgs_dispatch_rule，未知邻域 ValidationError fail-loud；local search 通过 registry 生成 move 后仍调用现有正式解码链，SearchReport 记录 neighborhood_moves diagnostics 与 neighborhood_summary public 小摘要；no-op 记 noop_neighbor，fallback 必写 fallback_reason；public/OperationLogs/size guard 不透传 raw move、内部 op/resource id、fingerprint hash。未做 VNS/SA acceptance，未做 ALNS。

  - slug: vns-sa-local-search-upgrade
    description: 把现有 local search 升级为 VNS 和 SA 后处理，先做轻量可复现版本
    depends_on: [grasp-ig-candidate-construction, business-neighborhood-registry]
    status: done
    feature: 2026-06-28-vns-sa-local-search-upgrade
    minimal_loop: false
    notes: 已落地 VNS/acceptance 局搜升级；新增 current/best 分离状态，非改进候选可按 threshold/record_to_record/simulated_annealing 接受为 current，但 best 只在 acceptance 通过、score 严格更优、output_fingerprint 非 parent 且非 seen 时更新；CandidateProfile 默认 improve 扩展为 vns_sa，acceptance 白名单扩为 improve_only/threshold/record_to_record/simulated_annealing，未知 acceptance 继续 ValidationError fail-loud；SearchReport 新增 acceptance_events/acceptance_summary、vns_events/vns_summary，并把 acceptance_passed 改为真实 best acceptance 事件，不再用 accepted_candidates>1 代理；public/OperationLogs/size guard 只投 acceptance/VNS 安全计数摘要，不泄漏 raw random draw、fingerprint hash、raw move 或内部 op/resource id。未做 ALNS。

  - slug: graph-ready-local-search-contract
    description: 定义工序图就绪队列派工的局部搜索合同、候选口径和 public/diagnostics 边界
    depends_on: [vns-sa-local-search-upgrade]
    status: done
    feature: 2026-06-29-scheduler-global-optimizer-items-9-13
    minimal_loop: false
    notes: 已落地图 ready候选搜索合同；生产只消费既有 graph_ready_context 和 node_metrics_by_op_id，不重跑图主链；候选只改变图优先级权重和 SGS ready 排序，正式时间仍由 SGS 解码；严格校验 schedulable_op_ids/fixed_op_ids/fixed_op_sources_by_op_id/predecessor/successor/sort_key/graph_priority_key/node_metrics，图不可用、有环、缺字段、非法 key、缺指标、重复 output fingerprint 均有 skipped/candidate_rejected 或 strict fail-loud 证据；旧 graph_ready_requires_graph_neighborhood 口径已替换为 graph_ready_uses_graph_candidate_phase

  - slug: graph-ready-priority-tuning
    description: 围绕关键路径、后续影响、下游工时和瓶颈资源做工序图权重多起点比较
    depends_on: [graph-ready-local-search-contract]
    status: done
    feature: 2026-06-29-scheduler-global-optimizer-items-9-13
    minimal_loop: false
    notes: 已落 balanced/critical_path_first/successor_fanout_first/downstream_work_first/bottleneck_relief/critical_bottleneck/fanout_downstream/bottleneck_downstream/graph_neutral 九组默认图权重；OptimizationSearchReport.candidate_profile 记录 max_weight_profiles、截断状态、权重 profile 名称和 failed_ops/objective_score/best_fingerprint_changed/runtime_ms/candidate_origin 择优顺序；CandidateFingerprint 按正式 SGS 输出去重，同输出记 same_fingerprint；FJSP 中门禁通过 benchmark_optimizer_medium_gate.py --run 覆盖

  - slug: optimizer-integration-auto-selection
    description: 把 GRASP/IG/VNS/SA 和工序图候选接回 OptimizationOutcome、候选比较和 summary
    depends_on: [diagnostic-public-id-boundary-fix, vns-sa-local-search-upgrade, graph-ready-priority-tuning]
    status: done
    feature: 2026-06-29-scheduler-global-optimizer-items-9-13
    minimal_loop: false
    notes: 已通过 OptimizerRuntime.run_graph_ready_candidates 和 run_heuristic_candidate_phases 接回 optimize_schedule；候选比较统一按 failed_ops/objective_score/best_fingerprint_changed/runtime_ms/candidate_origin，并已覆盖 GraphReady、GRASP/IG、VNS/SA 的 best 更新；重复 output_fingerprint 只记 same_fingerprint，不采纳；graph_ready profile 接入 public/profile_diagnostics 白名单，public 只展示小摘要，完整 attempts/fingerprint/图上下文仍留 diagnostics 且普通模板不直渲染；aps-three-gap-directions 的 candidate_id/source_table/plan_role 等 public 禁止字段未新增

  - slug: benchmark-ratchet-quality-gate
    description: 把 tiny oracle、非劣化阈值、稳定性统计纳入轻/中/长三层门禁
    depends_on: [optimizer-integration-auto-selection]
    status: done
    feature: 2026-06-29-scheduler-global-optimizer-items-9-13
    minimal_loop: false
    notes: 已新增 benchmark-ratchet-baseline.json、benchmark_optimizer_ratchet.py、benchmark_optimizer_medium_gate.py 和 required 测试 test_optimizer_benchmark_ratchet_gate.py；轻门禁覆盖 tiny proof + 图异常合同；当前 tracked baseline 和 actual snapshot 均为 dirty_worktree=true 时，--check-baseline 会按合同失败并返回 unbound_dirty_worktree，不能写成通过证明；中门禁实际跑通 FJSP mk01、SMTWT 250x2 模式和 SGS 大资源池，报告默认写 ignored long_gate；FJSP 原 700 行脚本已按数据镜像/装载/运行/报告拆成支撑模块，避免为了门禁硬压行数；FJSP makespan 仍只作技术参考，不证明 APS 业务目标全局最优

  - slug: long-run-tuning-evidence
    description: 建立工序图权重、不同 seed、不同基准集的长跑统计证据和归档口径
    depends_on: [benchmark-ratchet-quality-gate]
    status: done
    feature: 2026-06-29-scheduler-global-optimizer-items-9-13
    minimal_loop: false
    notes: 已新增 benchmark_optimizer_long_run.py，默认写 evidence/QualityGate/long_gate/optimizer_benchmark/ ignored 目录；长跑输出包含 10 seed、best_score、failed_ops、均值/最差值/标准差、重复候选率、候选拒绝率和 same_fingerprint 统计；本轮 --no-write 复核通过，写盘证据默认只进 ignored long_gate；显式写 tracked evidence 时仍需重新 clean proof；后续 ALNS 继续复用同一证据口径

  - slug: benchmark-reference-diagnostics-baseline
    description: 补齐 GraphReady v2 前的同目标下界、不可比诊断和换型基准准备口径
    depends_on: [long-run-tuning-evidence]
    status: done
    feature: 2026-06-29-graph-ready-v2-hardening
    minimal_loop: false
    notes: 已落地 reference_diagnostics；只补量尺和诊断，不重复 item 3/5/7/8 已有 noop/no_improvement/same_fingerprint/improved 合同；Jackson 单机抢占下界只能在同目标同指标可声明时进入 gap，否则标 not_comparable；WTSDS/Cicirello/SDST 换型基准已定义装载、评分、脱敏和默认 stdout/ignored 口径，不下载 tracked 数据；graph-ready-v2-comparison-baseline-contract 已消费本项输出

  - slug: graph-ready-v2-comparison-baseline-contract
    description: 建立 GraphReady v2 修改前的多算法同台比较基准，补齐算法名/版本/seed 维度的 ratchet key
    depends_on: [benchmark-reference-diagnostics-baseline]
    status: done
    feature: 2026-06-29-graph-ready-v2-hardening
    minimal_loop: false
    notes: 已建立同一 case/seed/time_budget/objective 下 greedy、local_search、grasp_ig、graph_ready_v1、graph_ready_v2_no_repair、graph_ready_v2_with_repair 的 compare_algorithms 基准；portfolio_all 只作为事后上界行，必须标 comparison_semantics=posthoc_upper_bound，普通胜平负必须标 not_comparable，不能伪装成同预算算法；ratchet key 包含 case_group、case_slug、algorithm_profile、algorithm_version、seed，禁止继续用只含 case 的 key 承载多算法比较；当前对比可在脏工作区以 unbound_dirty_worktree 运行，但不能包装成 clean proof；默认输出 stdout 或 ignored evidence，显式写 tracked evidence 后必须重新跑最新 HEAD 质量门禁

  - slug: graph-ready-v2-objective-feature-contract
    description: 为 GraphReady v2 定义 due pressure、slack、saveability、remaining work、processing time 和 sacrifice penalty 等目标感知特征
    depends_on: [graph-ready-v2-comparison-baseline-contract]
    status: done
    feature: 2026-06-29-graph-ready-v2-hardening
    minimal_loop: false
    notes: 已落地 due_pressure、slack_hours、remaining_work_hours、remaining_due_burden_hours、due_budget_hours、saveability、processing_time_rank、sacrifice_penalty、critical_ratio 等目标感知特征；只消费已有 graph_ready_context、node_metrics_by_op_id、批次交期和正式 SGS 可见输入，不重跑图主链；min_overdue 下表达“批次是否还可救、单位剩余工时能救多少交付风险、容量不足时谁应后置牺牲”；批次 due_date 是合法可空业务字段，空交期不再让 v2 整池静默退化成 v1 成功，而是生成 no_due_placeholder 特征并在交期型公式中后置；非空非法交期、缺必要特征、非法数字和 NaN/Inf 均 fail-loud；所有特征进入排序前做 rank/percentile 归一化，避免分钟数裸加压倒其他信号

  - slug: graph-ready-v2-bottleneck-resource-score
    description: 将 bottleneck_machine_score 升级为残余容量、分摊负荷、最小候选负荷、灵活性降权和交期门控的瓶颈资源分
    depends_on: [graph-ready-v2-objective-feature-contract]
    status: done
    feature: 2026-06-30-graph-ready-v2-production-capacity
    minimal_loop: false
    notes: 2026-06-29 第 9-13 项已提前把“候选资源分摊负荷、最小候选负荷、可选资源灵活性降权”落入生产 `bottleneck_machine_score` 基础图指标，并用轻门禁锁定 busy/light/flex 顺序；本轮补齐 GraphReady v2 残余容量特征，按交期窗口统计日历可用工时，扣除设备停机和 seed_results 中冻结/执行态/已排片段占用；新增 residual_capacity_hours/window/blocked/ratio/pressure 与 candidate_machine_count，旧 `bottleneck_machine_score` 不改写；交期压力计算区分 due_deadline_hours 与 due_budget_hours，内制工序有日历时用窗口产能(毛,未扣占用)小时，外协交付周期继续用自然小时(毛/净口径决策见 .codestable/compound/2026-06-30-due-budget-window-vs-residual/)，避免把内制净工时、外协自然日和交期自然小时裸混成同一口径；bottleneck_on/release/due_gate 结合残余容量压力、候选设备灵活性、due_pressure 和 saveability，交期不紧或不可救时不会让瓶颈分越过主交期信号

  - slug: graph-ready-v2-candidate-portfolio
    description: 建立 GraphReady v2 候选家族，覆盖目标规则、图交期混合、瓶颈交期混合、长尾牺牲和小扰动候选
    depends_on: [graph-ready-v2-bottleneck-resource-score]
    status: done
    feature: 2026-06-30-graph-ready-v2-production-capacity
    minimal_loop: false
    notes: 已保留旧九组图权重作为 v1 对照，并生成 EDD、SPT、min_slack、critical_ratio、ATC-like、saveability、sacrifice_long、graph_due_hybrid、bottleneck_due_gated、micro_perturbation 等候选；候选逻辑抽象为“保护更多可准交批次、必要时牺牲长尾”，不写死当前 4 工序样例；v2 公式不再让 ATC-like、saveability、graph_due_hybrid、bottleneck_due_gated 等大面积共享 sacrifice_penalty 首键，基准夹具锁住 10 个 v2 公式至少产生 6 种 ready 排序；小扰动只在主交期目标相同时作为平局信号，seed 派生且可复现；候选仍通过正式 SGS，因此不能绕过前后置、资源资格、冻结或执行态保护；默认 GraphReady 候选 profile 为 19 个（9 个 v1 对照 + 10 个 v2），若报告口径把 baseline 也算入 evaluated_candidates，常见展示为 20；配置上限仍为 60，超预算报告截断原因

  - slug: graph-ready-v2-elite-local-repair
    description: 对 GraphReady v2 前 K 个候选做轻量局部修补，修复接近好解但差一两步的排序
    depends_on: [graph-ready-v2-candidate-portfolio]
    status: in_progress
    feature: null
    minimal_loop: false
    notes: benchmark 支撑里已有 adjacent swap 与 single insert 的确定性 repair 脚手架，并能区分 graph_ready_v2_no_repair / graph_ready_v2_with_repair；生产 core 仍未落地，缺 top-K elite、max_neighbors_per_elite、time_budget_ms、tardy_boundary_move 和正式 SGS 接受链；本轮未把 repair 做成半成品，后续必须补到 core 后才可置为 done

  - slug: graph-ready-v2-portfolio-integration
    description: 把 GraphReady v2 作为候选来源接入统一候选池，与 greedy、local_search、GRASP/IG、GraphReady v1 共同择优
    depends_on: [graph-ready-v2-elite-local-repair]
    status: in_progress
    feature: null
    minimal_loop: false
    notes: 本轮已把 GraphReady v2 no-repair 候选池接入生产 GraphReady 正式 profile，不再依赖 benchmark 的 profiles_override/profile_summary_override；生产路径从 CandidateProfile.candidate_construction 选择 objective_aware_portfolio，经 run_graph_ready_candidates 补 v2 特征，再走正式 SGS 和统一 candidate_is_preferred/output_fingerprint 去重，胜出来源为 graph_ready_v2_generated；仍未完成“与 GRASP/IG、VNS/SA 同一生产候选池并行择优”和 graph_ready_v2_repaired 生产来源，因此保持 in_progress；public 只展示候选族计数、胜出来源和中文摘要，完整 attempts、指纹、内部节点/工序/资源 id 只能进 diagnostics

  - slug: graph-ready-v2-comparative-ratchet-gate
    description: 建立 GraphReady v2 修改后的轻/中/长对比门禁，和当前基准及其他算法做胜平负统计
    depends_on: [graph-ready-v2-portfolio-integration]
    status: in_progress
    feature: null
    minimal_loop: false
    notes: 本轮已跑 benchmark_optimizer_ratchet.py --check-baseline、benchmark_optimizer_compare_algorithms.py --check-baseline --allow-dirty-proof --no-write、benchmark_optimizer_long_run.py --seeds 10 --no-write、benchmark_optimizer_medium_gate.py --run；v2_no_repair 在 GraphReady 10 seed 基准中对 greedy/local_search 为 10/0/0，对 grasp_ig 为 5/5/0，对 graph_ready_v1 为 10/0/0，均值 primary_delta 分别为 -2.0/-2.0/-0.6/-1.0；portfolio_all 是事后上界，只能报告差距、来源和 not_comparable，旧 0/10/0 不能作为同预算普通胜负；但本轮证据为 dirty_worktree/unbound_dirty_worktree，且长跑脚本仍是旧 graph_ready 长跑，未建立最新 HEAD clean comparative ratchet baseline，因此保持 in_progress；缺算法、缺指标、目标不可比或 dirty proof 冒充 clean proof 时必须失败或标 not_comparable

  - slug: alns-graph-ready-bridge-contract
    description: 定义工序图闭环产物如何喂给自适应大邻域拆修搜索
    depends_on: [graph-ready-v2-comparative-ratchet-gate]
    status: planned
    feature: null
    minimal_loop: false
    notes: 只消费第 9-13 项和 GraphReady v2 已校验的图摘要、目标感知 ready rank、瓶颈资源分和多算法 ratchet baseline，不重新跑另一套图主链；桥接结果只能影响 destroy scope/repair priority，不能直接写可变工序时间；拆修搜索后续必须复用同一自动选择和 benchmark 矩阵

  - slug: alns-partial-repair-contract
    description: 定义自适应大邻域拆修搜索的局部拆修 allowed-op、payload validation 和执行态保护复用合同
    depends_on: [alns-graph-ready-bridge-contract]
    status: planned
    feature: null
    minimal_loop: false
    notes: 局部 repair 结果必须复用正式排产守门链，不能只靠拆修搜索内部校验

  - slug: alns-state-operators-core
    description: 建立自适应大邻域拆修搜索 state、destroy/repair operator 接口、operator reward 和权重更新
    depends_on: [alns-partial-repair-contract]
    status: planned
    feature: null
    minimal_loop: false
    notes: 先做纯 Python 外壳，不引入 alns/job-shop-lib 包、NumPy/Matplotlib 或重依赖；ALNSOperatorResult 字段必须在本项启动时定稿，非法 score/repair 输出/seed_results 必须 fail-loud

  - slug: alns-sgs-repair-adapter
    description: 把拆修搜索 repair 接到现有 SGS，确保 repair 后完整校验并返回候选结果
    depends_on: [alns-state-operators-core]
    status: planned
    feature: null
    minimal_loop: false
    notes: repair 失败必须明确 rejected/failed；seed_results 只能放保护/执行态固定片段，mutable operations 必须由 SGS 重新落位

  - slug: alns-domain-neighborhood-operators
    description: 定义 ALNS 领域邻域算子：联合机器-排序、关键块 N5 和移动瓶颈候选
    depends_on: [alns-sgs-repair-adapter]
    status: planned
    feature: null
    minimal_loop: false
    notes: 只生成资源 override、batch_order 或 repair request，不直接写工序时间；机器选择必须先确认业务资源资格真实可选，缺资格数据一律 candidate_rejected/fail-loud；关键块 N5 和移动瓶颈只作为 destroy scope、repair priority 或 operator 候选，仍复用正式 SGS 解码和 CandidateFingerprint

  - slug: alns-setup-aware-neighborhoods
    description: 在换型基准量尺到位后定义 setup-aware 拆修邻域
    depends_on: [alns-domain-neighborhood-operators, benchmark-reference-diagnostics-baseline]
    status: planned
    feature: null
    minimal_loop: false
    notes: 等换型基准、不可比诊断和 ALNS repair 链到位后再做；只优化换型相关候选作用域和排序/资源决策，不把 setup 收益写成缺证据的 objective improvement；无 SDST/WTSDS 等可比样例时只能跳过或标 not_comparable

  - slug: alns-selection-acceptance-trace
    description: 实现拆修搜索 operator 选择、threshold/Record-to-Record Travel/SA acceptance、segment 权重更新和 trace 输出
    depends_on: [alns-sgs-repair-adapter]
    status: planned
    feature: null
    minimal_loop: false
    notes: operator reward 只影响后续选择概率，不能直接覆盖 objective score
