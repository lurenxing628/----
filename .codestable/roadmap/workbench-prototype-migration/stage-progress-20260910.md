# 工作台移植阶段进度

后续状态：用户追加授权的第一轮已知补漏已完成，最新结果见 [第一轮补漏记录](round1-completion-20260910.md)。下文保留上一阶段冻结事实，不把历史检查结果改写成新结果。

- 日期：2026-09-10。
- 状态：本阶段收尾完成，按用户要求停止；本阶段子代理全部结束，不启动下一批。
- 总体结论：主要工作区已经接入本地后端，并有真实页面操作证据；整个移植目标仍未完成，尚不具备最终发布条件。
- 未提交或暂存本轮改动。当前是多人已有大量改动的工作区，不是 clean-worktree proof。

## 已完成的阶段成果

| 范围 | 本阶段结果 |
| --- | --- |
| 计划与试调 | 合法零工时保留为时间点，不补占用时长、不自动完工；真实候选采用、试调保存/再采用与重启读取已经验证 |
| 分件工序 | 原单件编号、单件目标量、整批量和前置工序贯通候选、正式计划、试调、现场记录及实际甘特；旧计划缺数量证据仍显示未知 |
| 物料与工艺 | 调整详情布局、数量单位、工艺分页和无结果状态；保存后保留分页；有效合并外协组显示真实组周期，不误报成员缺项 |
| 复盘与报表 | 统一连续底色，改进长文字、表格与滚动位置；真实下载按原查询范围核对 |
| 页面细节 | 预检改为“尚未生成排产结果”；时间点说明准确；风险计数 0 不再警示着色；删除过时的永久禁用采用占位按钮 |
| 后端与启动 | 修复资源查询类型配对与路由越层、试调快照对象校验、请求生命周期类型；明确路由注册入口；共享工时保护和模板来源职责归位 |
| 测试基础 | 本批 11 个新增测试文件全部登记；验证缓存区分真正影响行为的环境输入，不把未运行浏览器的结果复用为已运行 |

代码仍保留原型的配色、导航和控件体系，没有为这些修复引入新的运行时、云服务或外部页面资源。

## 实际验证

- 主线完整页面：手输 B1 工时 0，选择批次、预检、运行四个真实候选，输入采用原因和声明人后生成正式 v1。工序保存、运行受理和正式采用三个原请求各提交一次。
- 同一私有测试库正常退出、备份、重启后，原正式版本、任务、工序和回执引用保持；仅正常启动日志及对应序列增加。最终查看过程 77 张表完全不变。
- 最终前端构建 `67229ad0b232830132f049095e8c8c8b80ba29296df7b3668a128af1ebec5c78`，210 个文件，目标 `chrome109`。最终实例捕获的 1300 个已加载源码文件与冻结资源哈希复核无变化；这不等于跟踪了所有后续动态加载模块。
- 主线最终检查：50 项资源/页面合同测试、2 项实际甘特浏览器测试通过。FD 最终联合验证为 1417 项登记/缓存测试、248 项回归/消费者测试通过，均无跳过。这些集合有重叠，不相加成独立总数。
- 分件整页五例覆盖 1920x1080 / 1392x924、深浅色及超长编号；现场分件下游再覆盖四组合真实页面。使用的是 macOS Chromium 109.0.5414.46，不是 Win7 真机。
- 同资源密集排产：200 工序完整四候选 payload 字节一致，cProfile 33.140 秒降至 3.661 秒；1000 工序 24.201 秒、四候选完整。5000 同资源最终用例未通过本阶段验收。
- 全仓仅收集了 14313 项测试，不是执行了 14313 项。最终登记为 553 个 required 文件、84 个 supplemental 文件、33 个 required 分组。

## 尚未完成

1. 完整质量门禁仍未通过：最近一次产品类型检查为 81 errors / 15 warnings，相比阶段初始 230 errors 减少 149 条；剩余项没有用 ignore、改配置或刷新基线掩盖。
2. 架构检查仍有 4 项失败，以及剩余目录循环和未解析动态导入。旧 copy/template/protect 三项失败在修改前源码快照也能复现，本阶段没有扩大修复范围。
3. 280 个 required 文件尚未被 Git 跟踪，对应 tracked 契约仍为真实失败。没有为过门禁擅自暂存用户工作区。
4. 现场既有十列 XLSX 无法区分同名分件时仍明确拒绝；尚未完成该格式的全范围兼容验收。实际甘特 CSV 将原“目标数量”明确为“执行目标数量”，并在末尾追加计划数量依据列，外部按旧表头处理的脚本需注意。
5. 全能力最终统一验收、5000 同资源容量、最终离线包和 Win7 真机验证未完成。
6. 旧 UI 尚未下线；最终旧选项差异仍须随最终交付确认。原源码备份已核验保留，不因新页面上线删除原业务数据、历史或隐藏参数。

## 现场与证据

- 联调预览：`http://127.0.0.1:53144/workbench`。这是隔离测试数据库，保留服务供查看，不是生产库；不再后台推进开发任务。
- 最终实例 PID：73298；schema 31；独立根 `/private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-workbench-live-l0tgvgp8`。
- 主线最终证据：`output/workbench-migration/verification/point-main-20260910/stage-final-preview.json`；同目录有逐表快照、构建清单、测试 XML 和 FD 最终交接。
- 主线截图：`output/playwright/main-integrated-20260910/`，最终三张为 `page-2026-09-10T08-34-41-113Z.png`、`page-2026-09-10T08-37-29-060Z.png`、`page-2026-09-10T08-39-23-674Z.png`。
- 静态剩余问题及分域交接：`.codestable/issues/2026-09-10-fe-static-integration/`；现场分件：`.codestable/issues/2026-09-10-fb-piece-downstream/`。
- 原源码归档 SHA256：`5cf4ecc190bcafc11fb80bdccb40733e0e93624d86d8c2fc45854a224b900b3b`；唯一既有暂存补丁 SHA256：`952a9e734f0c0d73d6c780090ac53a2854134b3523f0a552d3fe362f4f908b7d`，均未变。

下一阶段应先处理明确剩余门禁与文件兼容问题，再做最终容量和 Win7 包验收；本阶段不继续执行这些待办。
