# 集成、终验与旧界面退役

- 开始日期：2026-09-10；本次实现快照：2026-09-11，G08归档前共16批源码提交，最新 `02434d6f785ed8b8cdc8745512d22756c49eacd9`。
- 本轮授权：完成源码归档与完整门禁、最新预览与全站验收、5000 同资源容量终验、旧 UI 退役；按依赖和合适颗粒度作本地提交。
- 排除：Win7 打包、真机验收、最终发布。没有推送或 PR 授权。
- 原工作区的既有改动保留；完整 clean proof 必须来自最终已提交 HEAD 的隔离干净 checkout，不冒充原工作区干净。
- 本文件仅保存实现快照、验收入口与既有证据指针，不随每轮实测改写HEAD。最终19步门禁、完整344、5000及预览结果统一写入 `output/workbench-migration/verification/round2-20260910/final-result.json`；尚未执行，不记最终PASS/done。
- H与G已进入PRIVATE并正常提交；原树G的156个工作文件及旧生成资产仍刻意保留，待必要门禁通过后由Main按既有清单切换，不因index/HEAD已附接而提前覆盖原工作字节。

## 剩余必交项锁定

2026-09-11按用户最新指令收敛：先集中修完，再统一最终验证。不新增非阻断工作，不重新开启广泛审计，不为补齐文档或截图数量反复跑同版矩阵。

| 编号 | 必交项 | 退出条件 |
| --- | --- | --- |
| L1 | G05已发现回归 | 首次24失败均有修后通过证据；最后C/F两个完整文件在新构建上3项通过，110.58秒。额外10项opt-in全部通过，首次失败和跳过记录保留；各分组定点证据不是最终同HEAD全门禁，不重跑中间整套216文件 |
| L2 | 已授权产品缺口 | ANA六动作、Actual到Field空批次边界、批次返回上下文、延期资源重叠明细和原试调查看偏好均已实施并归档；相关定点合同通过。原试调五项偏好和新增明细的实页脚本已收齐，复用原8个节点等待统一终验；不再增加产品功能 |
| L3 | 必要门禁接线 | 精确测试登记、已确认会阻断门禁的helper命名问题及原有导入/类型合同完成；不扩债务基线，不顺带清理历史测试类型问题 |
| L4 | 提交与原数据保护 | 按已批准颗粒度本地提交，保留原工作文件与独立台账；旧界面退役前完成真实业务数据的只读一致性备份，不用测试库冒充 |
| L5 | 完整入口与旧界面退役 | 已承诺能力的必需B/K/P/V证据按真实范围闭合，复用已验固定源证据；按既有25路径接线和124旧资产精确清单完成退役及必要回归，不追加装饰或非阻断体验优化 |
| L6 | 统一最终验证与交付 | 产品、测试、资产全部冻结并提交后，完成最终HEAD干净检出的必要质量门禁、完整344项页面验证、独占5000容量验证及最新私有预览；保留全部失败与修复记录 |

- 修复阶段只跑针对性回归；不再为每个中间HEAD重复整套集成测试，也不把定点通过称为最终门禁通过。
- 最终验证只接受真实阻断必交项的问题回到修复队列；对应修复完成后重跑受影响验证及维持最终HEAD证明必需的门禁，不另外发起“再找一遍问题”。
- 新发现的非阻断建议只保留简短记录，不进入本轮实施队列。Win7打包、真机验收、发布及远端推送仍排除。
- L5原有缺口具体锁定为：`RUN-007.no-result`、`GANTT-002.conflict-track`、`DELAY-004.conflicts`、`TRIAL-007.stale-conflict`、`TRIAL-011.export-failure`，以及原约定的试调分组/基线/仅变更/搜索/结果页签恢复。定点判断只发现延期冲突明细和试调查看状态两组需补产品代码；其余分支与58项P缺证合并成有限共享验证，不重新审查已过141项。

## 当前状态

| 工作 | 状态 | 已有证据 / 下一步 |
| --- | --- | --- |
| 退役前源码归档 | passed | `output/workbench-migration/baselines/pre-retirement-LqHg4q`；5384 文件逐项解压、大小、mode、SHA-256 与文件集合核对通过；归档 SHA-256 `8e0bb6b9f56132745d4095ea906a91d15a8f0b56eee47de32d239b4043feecf5`。不含业务数据库 |
| 来源核对与分批提交 | implementation_snapshot_ready | G08归档前已作16批本地提交，最新 `02434d6f785ed8b8cdc8745512d22756c49eacd9`。H的36改名/11入口登记及G的25产品、124删除、7测试已在PRIVATE正常提交；Main回执记录PRIVATE干净、original与PRIVATE的HEAD已附接一致。原工作字节仍保留，G156和旧资产待门禁后切换；不等于最终完整门禁通过 |
| 实际业务数据保护 | passed | `output/workbench-migration/baselines/real-data-pre-retirement-irvKCt/manifest.json`，SHA `f88a75efdc4b93adef9140502d2758d1c5b9785e1bd07bb7470ce0c0fc60e73d`。实际仓库原库及Excel业务文件、已有备份、日志共40文件原样备份；34表13747行和schema经SQLite Backup API副本逐表核对，integrity为ok、外键违规0。原文件SHA/大小/mode/inode/mtime未变，未启动应用或打开原库；不覆盖外部机器的数据根 |
| 当前完整构建与全站动作验收 | in_progress | 产品补缺后单次统一构建为`47ced597296282600fcb7a2ae8015791257290584b2552a475104f140dfed411`，manifest为`3b7f658b8be43cd3683df01819f36c0c64e567d2f0c440affa7c5a5ba823d53a`；318输入、226产物加manifest逐项核对。退役与门禁接线已入PRIVATE；最终344及领域补缺实页等待本次G08归档后的统一窗口，不重复中间矩阵 |
| 5000 同资源四候选容量终验 | passed_on_frozen_snapshot | 窗口 `r2-final-5000-20260910-v1`：100 批各 50 道工序，4 候选各 5000 条，共 20000 持久明细；受理到完整终态 122.728285375 秒，低于原 180 秒目标；结果和原引用跨重启相同。当前证明绑定 e091 构建及该时刻源码，不冒充最终 HEAD |
| 旧页面及资产退役 | committed_in_private_original_switch_pending | G的25产品postimage、124旧文件删除和7份配对测试已按精确回执合入PRIVATE，正常提交 `02434d6f`。原树G156及旧生成资产、旧53144仍未切换；只有必要门禁通过后，Main才执行已授权的原树切换。既有源验证与定点证据保留，不冒充最终集成/压力门禁通过 |
| 最终 HEAD 完整质量门禁 | pending | 提交后在隔离 checkout 运行，不改阈值、跳过测试或扩大原债务基线 |
| 架构、需求与 roadmap 回写 | implementation_snapshot_for_G08 | 保留实际实现、验收入口和既有证据指针；本次只作必要归档。最终实测状态由固定 `output/workbench-migration/verification/round2-20260910/final-result.json` 记录，不为每次结果更新而改写tracked说明。冻结的206能力族矩阵不冒充原子动作通过表 |
| 最新私有预览 | pending | 最终构建和当前服务代码绑定；原 53144 预览暂保持不动 |
| Win7 包、真机、最终发布 | excluded | 用户明确排除第 4 项；不标 done |

## 证据规则

- 206 是能力族数量，不是原子动作数量；原矩阵作为规划基线保留，真实验收索引另存。
- 浏览器证据区分实际键盘输入、`fill`、点击、截图、刷新、进程重启；后端通过不能冒充全部浏览器动作通过。
- 测试与各域交接存在重叠，不相加虚增总数。中间失败保留，修正后重跑原集合。
- 默认无路径参数的 Pyright 包含 tests，其 2011 个历史测试类型错误不等于产品类型门禁失败；完整门禁使用 `pyrightconfig.gate.json` 和独立 tools 配置。
- 备份恢复演练仅使用当前任务拥有的私有目录。代码回滚与数据库恢复分开，新业务数据不得用旧库覆盖。

## 在途补漏

- B的E sealed06完整副本已完成252/252：212规范定位加40启动故障，24个原页面保持浏览器与端口并换新PID。6,532份源码、224份含manifest资产均独立复制核对，产品与资产零漂移，原75张业务表保留。结果SHA `476f1774a9d8f92e0568f97e2253b715da8670d22d4abeb1c64c44e7371dd70b`；浏览器结果SHA `6abee5ab7f117d4624a43af0f8c76102d6f5d272f4d67046400d9abb85546f7a`，Main均已重算。该252不含旧92项首屏/交互/故障/重启组，最终源码必须使用`groups all`完整344项，不能将252称最终全站通过。
- G05 固定源计划为 `output/workbench-migration/verification/round2-20260910/G05-main-source-plan.json`，SHA `73045ad0ba333ecdbb269a3cbb2078b9d84fbe00f1ef2297eabc0b7483b37d9b`。638份sealed源、97份精确测试overlay、224份生成资产；前后来源无变化，组合后的隔离checkout与315个构建输入全部匹配。与成员小名单相比，仅E新增derive测试CLI从sealed基础转为精确overlay；不修改产品或原工作树。D/C后续增量不得反写此固定源证据。
- Field、Calibration、Reports、Review 的重新进入恢复定向9项在sealed06通过，详见 `acceptance-execution/07-read-view-sealed-proof.md`；不代表69族全部动作或最终HEAD通过。Main现已实际查看原16加补充11张图，三张未知引用错误已补拍到视口，按可见范围签核`round2-main-v-E-readview.json`；原未拍到错误的三张图不改写成错误可见证据。
- ANA-001/003原有六动作已补齐，只读模型、服务、两条GET、候选页面及必要测试共24文件已精确合并；Main已登记两个前端叶子的真实构建顺序。46项pytest为新增三文件16项加原有30项，不是46个新增测试；组件另有228检查、24下载。禁止改引擎、受理事实、schema或写命令。最终HEAD绑定仍待统一验证；详见`acceptance-planning/ana-minimum-implementation.md`。
- 首次24失败的定点修复已集中合入：Main首8项相关25通过，H四项通过，E五项及附加边界合计37通过，F前三项相关19通过，C配额/资源两项通过。原C工艺拒绝文案错误及F加载旧Batch静态的失败均保留；随后两个原浏览器完整文件在统一新构建上3项通过，110.58秒。各分组证据不相加冒充最终同HEAD门禁。
- 系统与值班台新增26张Main定点V及后续27张独立补图已完成，精确范围见`round2-main-v-F-delta.json`与`round2-main-v-F-supplement.json`；后者含20张新增状态、6张旧矩阵补图和1张正常备份详情。共同布局不冒充不同写动作执行，日志实际告警不冒充零告警，未扩大为全部228动作通过。H先前B/C登记小增量27项通过；285候选的一处CLI差异已集中对齐，36改名和11新入口登记现已一次合入PRIVATE并正常提交 `5b451aa8`；74处I001仅作导入整理，逐文件导入bindings/aliases和非导入AST一致。最终核算仍待统一结果，不追加中间矩阵。
- 历史规范定位252项首次为216通过、36失败：32项为合法空trial history的探针误判，另4项为报表跨新PID使用旧snapshot_ref返回409。旧结果原样保留。修正后上述新252/252已实际通过：四个报表页面GET200，原计划、批次、日期、查询和标题不变且不发送旧token。此处保留首次失败，不将首次216改成252，也不覆盖后续最终344要求。
- 试调/报表/校准首次142文件的46个selector为332 passed/11 failed/46 errors：46个错误来自旧测试时钟patch路径与已拆出的读时钟模块失配；精确同步已有helper的一行后，校准4整文件含规模测试85 passed。8个失败是Node编译依赖尚在G05，保留测试并延期到完整集成；另3个历史迁移fixture残留新版trigger，原完整dirty树同3项也失败。H随后仅修两份测试代码，并纳入3份真实历史DDL；Main独立核验与Git历史字节一致，旧业务行和外键断言保留，新增9项历史catalog/拒绝覆盖保护。最终145文件的49个selector提交前399 passed，提交后同HEAD干净检出399 passed/122.25秒；前述失败不覆盖，完整宿主与Node目标仍须G05复验。
- `WBP-DETAIL-008` 的原型入口可达性现已有 Main 独立范围决定；现行工艺列宽8张图也已完成定点V。详见 [Main 范围与看图记录](round2-main-decisions.md)。原206族规划不重写、不把不适用项加进生产通过数。
- 系统领域使用固定 F13-source9，同版补齐7个Python叶、查询模型两行事件筛选和7个前端叶。初124文件中的10项Ruff提示来自后续本地测试依赖尚未归档，同文件在完整副本Ruff通过；没有改导入分类或加忽略。实际逆向依赖闭包12份测试/辅助文件留G05，112文件与22个纯域测试文件完成提交；前后两次277 passed，提交后52.28秒，3条warning是record_property与JUnit xunit2的报告格式兼容提示。原38个测试文件的最终执行要求仍保留，最新导航和页面恢复修复是后续G05增量。
- 共享导航已新增范围与滚动 history 保持、坏上下文可见拒绝、页面专用 history 元数据保留，以及显式规范URL与服务端boot的精确绑定。`pages._host`在读取资产前校验所有外层查询与版本化定位，非法字段/重复参数返回带恢复入口的400页面且`no-store`。Node纯导航合同现为25个检查；最新导航parser、真实factory HTML、Node包装及既有入口合计42 passed，不能代替最终完整浏览器验收。原表单flash现传入新页面并转义呈现，错误入口也显示原操作结果，不丢失已提交状态。
- 资产依赖检查补登记真实浏览器 `scrollX/scrollY`，未知和拼写错误仍拒绝；含实际 Chromium 109 的定点组 11 passed / 7.52 秒。
- 211 文件完整构建已成功，正式容量窗口冻结 build ID `e09173b0bc08b022e9a365d516f555338a4a4cd2fc8f2a983ae012328abbe6cf`，输出为私有 fixture 的 `full-build/static/workbench`，未替换全局 static 或原预览。
- 容量结果：`/tmp/aps-final-capacity-B.bk32eh/formal-5000-r2-v1/result.json`。1472 产品输入前后哈希一致，132 次元数据观察无漂移；窗口已经结束，三个运行进程正常退出，原锁释放。纯引擎 119.754998 秒，worker 总计 121.342296 秒；观察开销计入实测，不扣除。
- 同时执行的资源读取最慢 3.349389 秒，不能据此称所有请求即时返回。worker 峰值 RSS 453558272 bytes；整段初始/重启验证进程分别 604520448/672530432 bytes，包含完整证据读取开销，不当作纯算法内存。后续最终源码与构建收口后重新核对证明适用范围。
- `delay`入口、独立启动watchdog、工作区错误边界及备份只读下载已接线。备份下载已取得真实SQLite与负测证据，最终整页矩阵仍待通过。候选完整交付风险由规划域沿冻结受理事实接入，不能只拿摘要冒充逐批完成。
- 资源/工艺恢复校验的双向依赖已抽成`ProcessReadView.js`纯叶；未放宽构建分析器。新增值班台API叶逐项登记后，当前一版完整构建为217文件/309输入，build `4c374975b2b6a19b1c604e954413e1559991558f58bc7de1ae56aa527ca8c6c0`，仅输出至Main私有目录，值班台剩余13项仍在集成。
- 同层React key重复导致甘特搜索`CAT-B`只留最后字符及重复DOM的问题已由规划域修复。原失败图保留；修后`64b6bfb181a1c4a04c2f3b218de1f8ad9c4a56b306fc3be19e0213899b0bfe77`快照完成92/92：56首屏、16故障、4同页新PID重启、16交互。四种组合共20次逐键均为`C/CA/CAT/CAT-/CAT-B`，输入节点持续连接并保持焦点，甘特DOM始终一个。108截图、156正常生产响应，普通页面错误及外部请求均为0；不以fill替代逐键验证。
- 上述基础快照根为`/private/tmp/aps-final-foundation-B.xYPahs/aps-workbench-live-2y19rdw0`；host 77281与77652均正常退出、释放锁，全部原业务行保留，仅追加正常plugins/load启动审计，前后产品源码/已加载Python/资产未变化。该证明为`unchanged_snapshot_not_final_HEAD`，不能覆盖后续页面定位、日期弹层及值班台功能增量。早先探针采集失败结果独立保留。
- 工艺与主数据第2页恢复现先按相同筛选取得新只读快照，再请求原页；实际甘特的水平恢复等待真实板宽，未保留过期token或退回默认范围。共享日期弹层现按合同在外部滚动/窗口resize时关闭并归还焦点，弹层内部时间列滚动继续保留；各域真实新构建复验在途。
- 共享列筛选真实失败已定位到列表loading变短引发文档自动夹位，而不是锚点重建、禁用或scope变化。`/private/tmp/aps-workbench-live-8fk_6dmz/final-master-controls.json`记录600.5至192.5、同节点/同identity/未禁用且无外部wheel或点击。`ResourceTableFilter.jsx`现只保留超出新滚动上限造成的精确夹位并重定位，后续真实滚动仍关闭。Chrome109四组合组件64项通过，28截图、零错误/外部请求，源hash不变；`aps-resource-table-header-ltg31ic0/table-header-result.json`为组件示例夹具，不冒充完整业务入口。
- `ResourceControls.jsx`现沿现有可见弹窗栈锁住html/body滚动，嵌套关闭不提前解锁，隐藏但已挂载的窗口不锁背景；最后关闭精确恢复原inline overflow及priority，不覆盖其他样式。Chrome109四组合原焦点/嵌套及新增样式恢复合同76项通过，结果`aps-modal-focus-4_78elrw/modal-focus-result.json`，pytest 1 passed / 12.98秒；这是模拟adapter组件证据，完整app的遮罩wheel、内部滚动、位置及焦点由C继续验证。
- 规划域真实发现预检往返丢选择、正式甘特仅变更/展开收起、任务前后工序与详情试调入口、试调打印主题恢复等缺口。D-P001至004均属冻结能力范围，已经授权本域修复；预检票据必须失效、各类任务身份不可混用、不直接改正式计划。原146动作分母和失败证据保留，不用部分通过替代终验。
- 上述共享控件首轮修复未作为最终通过：完整页面后来复现列表加载结束后的第二次自动滚动，以及新增批次按钮先禁用再挂载弹窗导致焦点丢失。第二次修复在菜单期间临时关闭祖先scroll anchoring并跟随真实布局重定位，结束后恢复原inline值/priority；Modal在首次render而非DOM提交后的effect保存原焦点。当前产品SHA分别为`b6d6f18afaec9147b9d13422efcd9d4c622ce47aefbb308c3febea746537eb42`和`1bed5a0d7e1afd6164064683e73d3998def9720c99e03ead1e49a360d40f6ac1`。Chrome109组件64/80项分别通过；独立完整源码副本的28场景浏览器部分26通过、2失败，原筛选和批次焦点四组合已通过，余下为宽屏工艺表格的列宽分配合同，C继续修复。原失败和后续源码漂移分别保留。
- 静态集成预检发现旧导航层SQL/Repository越层、一个新增目录级导入环及3个函数复杂度超限，已交G/F按职责修复，不扩基线或忽略。产品/工具类型检查与文件大小检查已分别通过，但采样不覆盖之后所有新增代码。防回潮脚本的git-diff视野不含untracked，H正在用候选全范围补查真实helper/CLI误命名为测试的情况，先形成精确改名与引用计划，不伪造测试函数过门禁。
- 主数据隔离归档修正了领域叶、执行域消费者与宿主公共导入面的提交顺序；此前9个/2个收集错误及1907个同源setup错误保留，不能当作业务断言失败。另有当前原树真实复现的v22/v20迁移测试遗漏后续元数据期望，已精确补齐对象及新表/来源映射校验；没有修改历史SQL或产品schema。跨执行/校准/host的41个测试依赖闭包放在集成批次，人员资格排产测试与helper放在执行批次；文件内容和最终登记不删除，不将缺失本地模块伪格式化为第三方导入来过hook。
- 产品原15条懒导出warnings已用TYPE_CHECKING真实声明消除，未改运行时动态导出或配置。H新跑产品1164files 0errors/0warnings及216项联合回归；仍不是最终HEAD完整门禁。
- 容量CLI已延迟加载实际执行器，`--help`与错误参数不加载factory；3项新合同通过。真实2批各3工序的小档经HTTP生成4个候选共24明细，跨进程一致、SQLite完整性与锁释放通过，明确`formal_capacity_passed=false`。含tests导入扫描已无新增环，基线不变。

## 已提交批次

以下为G08归档前16批源码提交的实现快照与验证入口。前十批有提交后隔离干净检出的定点证明，后续收口批次保留定点/正常hook事实，不追加中间整套重跑；全部结果不能相加冒充最终完整门禁。G08及最终实测的HEAD和结果以固定final-result.json为准，不反复改写本表：

| 提交 | 内容 | 定点结果 |
| --- | --- | --- |
| `c0097278` | 冻结导入与打包收集合同，4文件 | 6 passed |
| `5a9ac29c` | 运行锁、备份完整性、维护与请求基础，69文件 | 24测试文件，220 passed |
| `8c89efdf` | 资源边界、执行事实、完整批次指标及既有排产修复，140文件 | 51测试文件，913 passed |
| `2bc48516` | 物料数值及工作簿解析/CLI诊断，40文件 | 6测试文件，224 passed |
| `f454b3bf` | 停机/交付证据、关键链与报告调用边界，44文件 | 18测试文件，198 passed |
| `830a58e6` | 工作台schema、永久身份、读取/编辑上下文及原子回执，163文件 | 14测试文件，419 passed；bound日志记录前后同HEAD且干净 |
| `4b418d17` | 主数据、工艺、日历、资源与批次基础，362文件 | 17个独立域测试文件，1666 passed；提交后51.78秒，前后同HEAD且干净。跨域及factory API测试仍须后续完整接线验证 |
| `21134f73` | 执行事实、正式计划与可复现排产运行基础，311文件 | 13个重点测试文件，236 passed；提交后13.88秒，前后同HEAD且干净。首次69文件为1446 passed/11 failed，不能把首次结果称为全组通过 |
| `3f26925e` | 试调、报表、校准及真实历史迁移夹具，145文件 | 26个整文件加3个混合文件内23个函数前缀，共49个selector，399 passed；提交后122.25秒，前后同HEAD且干净。含1000/10000任务范围保留，非最终5000容量证明 |
| `0cab59b6` | 值班台、外协与系统维护领域，112文件 | 22个独立测试文件，277 passed / 3 warnings；提交后52.28秒，前后同HEAD且干净。宿主恢复和浏览器链不冒充已由该组验证 |
| `7df50ce5` | 完整工作台、受管宿主、领域增量及匹配静态产物，969文件 | 首次24失败均有定点修后证据；新构建最后2文件3 passed / 110.58秒。首hook一个导入分组空行I001已仅补空行且AST不变，再正常hook提交；未另跑该HEAD整套216 |
| `2e373043` | 既定门禁基础与当前独立台账，30文件 | 精确当前29份及唯一RauBC6覆写源逐文件核对；正常hook通过。36改名和11入口登记尚不在该commit |
| `3cd33eb5` | 已锁定规划失败和查看恢复实页脚本，7文件 | 原8个pytest节点不扩矩阵，保留原4分支断言并加7项明确L5证据；正常hook通过，实页尚未执行 |
| `70207858` | 验收CLI缺项、真实资源锚点和两条只读路由清单，6文件 | 两CJS语法/CLI help通过；原锚点6项通过0.72秒，FE03完整6项通过4.88秒；原158路由全部字段/顺序保留，仅加已实现2条GET。正常hook通过，不是Win7打包或最终门禁 |
| `5b451aa8` | H的36个helper改名及11入口登记 | 已有before/after采集保持15729项，原证明保留；74文件仅I001导入整理，bindings/aliases与非导入AST逐项相等，失败hook未删除。正常提交，不等于最终19步门禁 |
| `02434d6f` | G的25产品postimage、124旧文件删除及7配对测试 | 既有G独立候选验证已按精确回执核对；两处I001仅改导入分组且AST等价后正常提交。PRIVATE已含退役实现，original G156和旧生成资产待门禁后切换；最终完整验证尚未执行 |

执行基础这批的311文件中，21个仍在补功能的耦合文件先采用已核对的退役前基础归档，其余290个采用实际SHA相同的固定当前副本；新甘特、预检返回、关键链和这些路径的后续增量明确留G05，不覆盖原工作文件。3个R1测试文件仅按Ruff拆分导入别名，导入绑定和其余AST已核对不变。上述首次11个失败来自尚未集成的完整宿主：10个计划日期API节点未注册而返回404，1个候选完整app测试在旧宿主首次请求初始化配置。当前完整原树同11节点复测11 passed/24 deselected/14.64秒，这是dirty诊断证据，不是该commit的clean证明。两份混合测试文件及全部断言保留，G05后仍要整文件重跑。50万排程明细的大库用例包含在首次真实通过项内，不用小档替代。

冻结导入及四个既有业务组使用原`source-RauBC6/restore-check`同版内容，不能将已有dirty修复称为迁移原创。中间缺依赖、分组错误及首次hook失败均保留日志；全局或跨域测试归至真实依赖齐全的批次，没有删除最终登记。G03三个原样文件的尾部空行检查提示如实保留，历史DDL不为消提示而重写。附接提交时，仅更新该commit的Git树条目，逐项核对原工作文件和其它index条目未变，没有使用`reset --hard`或覆盖原工作区。
