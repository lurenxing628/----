# Main 范围决定与定点看图

日期：2026-09-10。仅记录下列有明确证据的决定，不代表全站终验或最终 HEAD 门禁通过。

## WBP-DETAIL-008

- 决定：`not_applicable_confirmed_unreachable`。保留这 1 个能力族及其独立记录，不删除 ID，不计入生产功能通过数；原 206 族规划 JSON 保持不变。
- 范围：冻结的原始 `前端设计/` 中 14 个工作区加 `trial-sample.html` 共 15 个入口。仅确认旧统一详情内 7 条 batch、3 条 plan 和未知编号 fallback 没有真实 UI 可达路径；不外推到任意历史页面或后续版本。
- 证据：[C 可达性报告](acceptance-master/detail008-reachability.md)。既核查实际加载 bundle 和被当前 JSX 替代的调用点，也真实点击现行入口及全部可达关联边；没有直接调用 `APSDetail.open` 冒充 UI。
- Main 已独立读取原始 `reachability.json`：15 入口、29 个直接记录、31 个可达闭包记录、21 条实际关联边、进入目标类别的边为 0、手动调用详情 API 为 0；源码变化为 0，规划未变，浏览器和临时服务已关闭。
- Main 已查看 `current-batch-B202605-018.png`、`drawer-op_int.png`、`current-plan-operation.png`，确认当前批次表单、现行统一工种详情和当前甘特内嵌工序详情是不同界面，不能混作旧详情的覆盖证据。
- 原始结果 SHA-256：`6fe1a0fd819e42ef1029858e049082d9c58d9cf6346307925a9d1c11594d93e0`；AST 调用证据：`687ab8469fbc90edd8a5c0b9a300f4cdcd0562ae03ffa9c4007a28c220718386`。Main 已重新计算，两者均与交接一致。
- 保留原 bundle 的 1 条启动时 root 尚未出现的历史内部错误；该错误未修复，不将它省略为原型完全无错误。此项没有运行生产 B/K/V/P，不新增无入口样例 UI，不借此删减其他 205 族。

## 工艺列宽定点 V

- 决定：本次 8 张指定图的布局检查通过。仅适用于 `/private/tmp/aps-workbench-live-jb7nijn7/screenshots/` 的 `controls-{1920x1080,1392x924}-{light,dark}-process-resize-{minimum,repeated-and-canceled}.png`。
- Main 已逐张查看：最小宽度时内容受列边界约束，重复拖动和取消后列头及行内容对齐；未见邻列内容交叠、工具栏按钮溢出或页面横向溢出。最小列头文字会被收窄，这不等于文字始终完整可见；该项不替代控件名称、键盘或读屏合同验证。
- 对应产品：`ProcessWorkspace.jsx` SHA-256 `df4b057c2946043dc22d320509981826987867309e04231537d357734227654e`；完整构建 `66b45a266409ca4d996bade37149f4044180fa5bbe9c8fb289e5912f9f87c79c`。
- C 的实际 app 28/28 场景及新增 44 个列宽合同是独立行为证据；Main 本次没有重新操作这些场景，也不将 8 张图扩大成 599 个领域动作、50 个共享动作或最终全站 V 通过。
- 结果与早先失败、业务行保留、重启、源绑定明细见 [共享控件记录](acceptance-master/shared-control-blockers.md)。原源码归档提交与先前失败结果均不改写。

## 系统与值班台定点 V

- Main 已逐张查看 F `F13-copy-matrix4-pytest` 的 28 张图：资源与批次时间轴、候选比较表、候选摘要弹窗、检修与原计划叠层、八项系统配置、恢复确认、刷新后只读维护页，各含 1920/1392 与深浅色四组合。
- 这些可见画面布局通过：时间轴层次可区分，表格与工具栏无错叠，弹窗和长引用在容器内换行；只读维护页明确要求重启软件，没有用“刷新”冒充重新开放业务。精确图片路径、大小、尺寸及 SHA 在 [28 图索引](round2-main-v-F-matrix4.json)。
- 对应构建为 `1417717b6ecfdc897da2af9b5d0302a13f3617ab1ad37b696343df8c9d758c46`，manifest SHA `2bc9704cab900f87760f1d9168597a7b566ef1cf8c77e5cf0a83263e6b336439`；F 的固定 source10 共 3634 文件，聚合 `1f9acb1bbdebd53696be4f696b1da3e14f0051515f2927139bae46a5b5906d0f`。
- 这些是滚动位置下的视口截图，不是全页所有内容。F 的14项实际联验与源守卫单独记账；Main 没有重演其恢复操作。新增导航确认及 Dashboard 恢复修复不在这28图证明内，不能称228动作或最终全站V全部完成。

## 恢复与导航增量定点 V

- 2026-09-11：Main 实际查看 F `F13-final-delta1` 的 26 张代表截图，并重新核对全部 SHA、字节数及 PNG 尺寸，详见 [26 图索引](round2-main-v-F-delta.json)。这些图片对应固定 source18 和构建 `5b4327b9c68a2f7e69c0df01f34df2c2087fd0c9968418e93a7d11150866c480`。
- 已看场景：重启后的值班台处置历史；系统备份与日志原二页的详情；同名文件内容已换、旧选择无稳定身份的明确提示；不能定位时的导航确认、未自动选计划的甘特总览、返回原处置。除两种失效提示仅有1392浅色图外，其余上述代表状态均涵盖1392/1920和深浅色。
- 这些可见画面布局通过：表格、原引用、详情、提示和确认按钮未见错叠或横向溢出；不能定位的总览明确未选择计划，原处置仍显示风险未知，没有凭新计划宣称旧风险消失。
- 本记录没有把55张增量图全部标成已看，也没有将26图扩大为228动作全V通过。旧校验错误、重开原因框、报工跳转等尚缺独立可见证据的适用项继续补齐；B/K/P及最终HEAD证明仍单独核对。

## 甘特来源定位定点 V

- 2026-09-11：Main 实际查看 D P003 的20张整页图，以及同一四组合的4张真实视口图；精确路径、SHA、尺寸和两个动作的范围见 [24图索引](round2-main-v-D-p003.json)。对应固定构建 `9f54333a9b8abe65cddfa918920607536199439739d4ea0603ef84a18d60a8dd`，不是后续ANA补全版本。
- `WBP-GANTT-003.predecessors`、`WBP-GANTT-004.trial-link` 的可见布局通过：三个分件前序可区分，切片外关系有明确重读定位按钮，返回完整计划后原工序有选中状态；试调页显示原正式基线、完整13道安排与目标分件，刷新后仍能看到该来源和目标。
- 两种宽度及深浅色均未见目标关系区、试调侧栏、表格与工具栏错叠或页面横向溢出。四张视口图单独核对了固定顶栏位置；长整页截图中fixed元素的截取位置不冒充实际视口布局。悬停提示临时遮住的内容不称始终可见。
- 仅签上述两个动作的V，不将20张图的其他可见页签、146动作、ANA四指标或独立B/K/P证据自动扩为通过。

## 系统与值班台补充 V

- 2026-09-11：Main 实际查看20张新增状态图、6张原矩阵补图和第23号正常备份详情增量图，共27张；逐图重新核对 SHA、字节数及尺寸。精确范围见 [27图索引](round2-main-v-F-supplement.json)，SHA-256 为 `dae28137a499cc19605f716dd519c67a2668fb6ab58dc35763827b55b60c5a58`。原28图和26图记录不改写。
- 可见主体布局通过：处置与外协校验失败、重开原因、目的页面、确认框、创建备份回执、处置与外协已确认回执，未见文字错叠或主体溢出。新增20图对应固定 source18/build16；旧6图的精确原来源按索引路径保留。
- 严格保留边界：未看到的折叠或视口外内容不签；管理样例的跳过规则不等于真实跳过事件；创建备份不冒充删除备份；跟进中处置回执不冒充关闭或重开执行；新增外协回执不冒充更正执行。共用布局只作为布局证据。
- 第16张运行日志图确实存在静态资源版本读取告警，本次只确认非紧凑行高和长日志换行，不声称运行时零告警。此记录不替代最终源码、旧界面退役后或全站验收。

## G05 首次集成与定点修正

- 固定G05首次完整216文件集成测试：2,178通过、24失败、10跳过，耗时2,124.33秒。`G05-precommit-1.json`、`.xml`、`.log`原样保留，不覆盖失败现场；该结果不是最终质量门禁通过。
- Main保存失败对应的完整tracked源码：`/private/tmp/aps-G05-failure-baseline-NEJgZ8/source`，4,898文件、103,665,475字节，manifest SHA-256 `ac34f31052f6593d4c4127f33fa4d027d4d14ac034a401d48dfaae48f03db859`。排除Git元数据、虚拟环境和ignored业务数据；复制前后逐文件及Git状态均不变。
- 第一次精确修正仅替换3个测试文件并前移2份既有事实JSON；产品未改、原959项方案未改写。单独回执 `G05-known-fixes-overlay-1.json` SHA-256 `dadf0df5dcc8c828c449b83125c646d398c89b50accae1a2debd0a5c568dab11`。
- 修正保留152条原路由全部字段和顺序，仅增加已实现的6条只读路由；加载真实`PlanProcessOrder`并保留缺失及顺序错误反例；批次组件加载真实`WorkbenchPageContext`；两份事实JSON按E06原字节供现有测试读取。
- 四个相关完整测试文件复跑25项全部通过，27.66秒，运行前后HEAD、暂存及未暂存内容不变。其余失败和10项opt-in跳过仍须分别关闭；当前为暂存态局部验证，不能称clean-worktree proof。

## 执行与分析首批 V

- 2026-09-11：Main 实际查看原16张恢复/失效截图，以及11张补充和代表截图，共27张。精确可见范围见 [执行首批V](round2-main-v-E-readview.json)，SHA-256 `4fd2866e947e651b8e5227cb63094a50ed8f6be0b1a3b8710f68a2c1793b9db0`。
- Main重新逐文件核对sealed03与sealed06各6,532份源码的SHA、大小和mode，差异只有两份E测试探针，产品相同；同时重新核对223份构建payload。完整build ID为`86bae55877b7574a7dbccad0a61c2f4b714a3105500908d614b531ae31f90a2e`。
- 可见布局通过，补拍的报表/复盘未知工序及校准未知模板错误框均已实际看到；没有替换同名对象。原三张未拍到错误框的图片仍仅签表格布局。活动快照失效与重新进入恢复分开记账。
- 原任务草稿、手动甘特视口、全局四节点链、校准列宽、原零件详情、采用锁定回执、复盘趋势与资源图各只签对应可见表面。未展开列菜单、对象关联链模式、hover、导入保护、未看到的视口外内容，不据此补签。
- 1392深色复盘恢复图单独可见中位数与P90；1392浅色趋势图没有这两项，不将两帧混作同一完整动作。27张代表图不等于69能力族全部动作通过，K/P和最终HEAD仍独立验证。

## 本域共享控件适用范围

2026-09-11，Main关闭C已经提出的四项范围歧义，不增加产品功能，也不改写原50动作或历史报告。

| 原动作 | Main决定 | 直接依据 |
| --- | --- | --- |
| `WBP-SH-006-C05` 批次列值计数 | 不适用于本域批次自有表头；不记为计数显示通过 | 冻结能力`WBP-SH-006.prototype_behavior`明确区分批次自有表头。导入原型`frontend/workbench/prototype/ui_kits/workbench/BaseBatches.jsx:336`只列值，与当前`frontend/workbench/app/BatchTable.jsx:16`一致；出现次数是通用`frontend/workbench/prototype/components/data/Table.jsx:477`的合同，不能扩大为批次原有功能 |
| `WBP-SH-008-C10` / `C11` / `C13` | 不适用于当前批次交期/齐套日期及日历批量开始/结束字段未配置的`min/max/readOnly`分支；不记为这些分支实页通过 | `frontend/workbench/app/BatchControls.jsx:16`仅对number设置min，日期没有min/max/readOnly；`frontend/workbench/app/CalendarRangeDialog.jsx:60`、`:62`同样未配置这些属性。C固定四组合实际DOM证据也为min/max空、readOnly=false，没有向业务字段注入限制 |

- 这只是上述已点名业务字段的适用范围决定，不把`WBP-SH-006/008`整族排除，也不取消通用组件的计数、日期边界和只读合同。未来实际配置这些属性的字段必须按其真实分支验收。
- 原50动作及四个ID保留，四项按本域入口记`not_applicable`，不计入passed分子；原`shared-actions.json`及`shared-scope-decisions.md`中的pending是当时的历史观察，最终索引同时引用本决定，不回写原测试日志为通过。
- 范围证据：`acceptance-master/shared-scope-decisions.md`及其固定报告`/private/tmp/aps-workbench-live-7kvfmq7f/final-master-remaining_controls.json`，SHA `5990fccc6dbc3d8b6d50a8ee20be5b601d87edbcae53dc3277de42cec4c448a4`。本次没有重跑或新增截图；通用组件与其他46项的B/K/V/P仍沿各自真实证据核对。
- 原`remaining_controls`的16通过/4失败和修后仅一个场景4/4仍分别保留，不宣称末版整模式20/20；重启保留仅允许原记录以外一条正常启动审计及相应序列，不称整库完全不变。
