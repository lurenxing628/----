# 总体方案定点审查处理

2026-09-09，主代理请既有验证代理对总体方案及共享合同作一次只读审查。代理没有修改源码/数据库或运行产品测试。本记录只说明草案合同修订，不构成实施完成或用户批准。

| 发现 | 主代理处理 | 最終仍需验证 |
| --- | --- | --- |
| 普通DB提交、202受理、文件恢复不能都称同一事务 | 合同第2/3节分别定义Accepted、普通事务回执、异步两阶段持久状态及库外维护journal；受理不显示完成，恢复后不能因回执随库消失误判未执行 | 崩溃、重复请求、客户端断开、恢复中断与启动核对的真实用例 |
| task_ref跨版本粒度含糊 | 明确operation_ref是实际工序实例；task_ref绑定具体plan_ref排程；原报工保留recorded_against_task_ref/plan_ref，新当前计划通过服务端实例映射读取同一事实 | 新采用版本后历史关联、原基线、同序号重建和分件数据不串 |
| 旧完工与缺数量/工时混为同一状态 | ExecutionProjection拆execution_state、completion_basis、data_quality；旧合法完成事实可同时资料不完整，仍阻止重排，数量未知不补0 | 旧事件与新增逐次记录不重复计数，完成状态不被迁移抹掉 |
| 值班台关闭凭据输入不全 | 补target_status、remark、completed_at、completion_evidence、evidence_reference_text；关闭必填、未来时间校验、已关闭只读、独立重开并保留旧凭据 | 按WBP-DASH-003/004/005逐字段输入、拒绝、保存、刷新和历史读回 |

主代理已核对样板`dashboard-model.js`的关闭/重开校验，修订后不以“已处理”二字替代具体结果。未另启动重复全库审查；实现完成后仍按各能力的既定测试与完整验收收口。
