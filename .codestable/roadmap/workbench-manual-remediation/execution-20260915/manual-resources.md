# 5003 资源族与删除图标手动验收

## 主代理追加：目录、工作日历与父表单恢复（22:16—22:44）

- 全部通过 CUA IAB 点击/填写操作独立5003，未用API/SQL写业务数据。
- 工作日历：9月30由默认8小时改为单独6小时，刷新回读08:00—14:00及备注；批量将9月19—20日由休息改为4小时，预览明确2天前后差异并保存。UI备份 `aps_backup_20260915_221803_manual_d9f7c6564f77.db` 后，单日清除9月30、批量清除19—20，刷新均恢复原默认（8小时/休/休）。
- 设备组：新增 `MANUAL-GROUP-0915`，更名“手动验收设备组-更新”；绑定数铣一号机后列表设备组数1、名字正确，目录关联设备1时垃圾桶删除禁用；解绑后关联0。
- 班次档：新增 `MANUAL-SHIFT-0915`，周期起点9月15、两日轮换。第1天22:00—06:00工作，第2天休息；保存后重新打开，跨夜起止及休息规则正确。绑定数铣师傅后列表回读，目录关联人员1时删除禁用。
- 班次更名返回父表单后，旧版本要求额外接受新数据且刷新后保存仍可能提交旧版本，实际复现 `stale_write`。修复见 `2026-09-15-resource-parent-context`；加载最终构建 `270587be…` 后，重新更名为“手动验收两日班-复验”，返回父表单直接更新最新依据；改班次为未选、再点刷新仍保留未选，直接保存成功，无额外“已核对，继续编辑”。
- UI备份 `aps_backup_20260915_224200_manual_7cfc8ba535ee.db`（完整性通过）保留两个测试目录记录后，分别打开无引用的班次、设备组删除预览，确认对象、关联0和逐日规则，再用垃圾桶删除。两次反馈均明确“删除已完成”。人员原设备权限仍在，原姓名与状态保留。
- 原样回导的文件专项由5001并行轨验证，不能把下方20:49旧记录的未覆盖说明当成本次最终状态。

## 结论与环境

2026-09-15 20:49–21:10，通过 CUA 原生 Google Chrome 逐次点击、填写、选择、保存、刷新完成下列验证。六类资源的新建、修改、回读及单项删除通过；人员“可操作设备”新入口完成真实保存和刷新回读；六类资源及真实/样例备份的删除均目视为垃圾桶。

本子代理没有浏览器 tab provider（`cua.getState()` 的 browsers 为空；iab/chrome createBrowserTab 均不可用），因此使用 `cua.getApp("com.google.Chrome")` 的原生 AX 控件。Chrome 经主代理协调由本轨独占，完成后已交给 utilization_impl 的 5005 任务。本轨只操作 5003，未访问或修改 5000 正式实例。

- URL：`http://127.0.0.1:5003/workbench?view=process`，系统备份使用同端口 `view=system`。
- 服务 PID：`91706`，exec session：`89906`，仍保留运行。
- 隔离根：`/private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-manual-resources-20260915-494scls2`。
- 数据库：上述根目录的 `db/aps.db`。
- 环境记录：[manual-resources-env.json](manual-resources-env.json)。初始数据只由备份 `aps_backup_20260915_183234_manual_5aec6fc5e1a9.db` 使用 SQLite Backup API 完整复制；没有脚本 seed，没有 API/SQL 创建业务测试记录。
- 启动使用隔离 `manual_app.py` 调用 `app_main("default", anchor_file=__file__)`；启动日志显示 SchemaVersion 31→32，并生成迁移前保护副本。
- 未执行全门禁、质量门禁或自动业务测试。本轨没有修改产品代码、生成静态资源或提交文件。

## 逐动作结果

| 编号 | 对象/动作 | UI 输入与结果 | 结果 |
|---|---|---|---|
| R01 | 新建物料 | `MANUAL-0915-MAT-R`；手动物料验证；M6；件；库存100；启用；备注5003独立实例手动验收。页面显示保存完成及刷新完成。 | 通过 |
| R02 | 物料详情/修改 | 详情读到库存100件；改名称“手动物料验证-已修改”、规格M8；保存后整页刷新，列表仍为M8/100件。 | 通过 |
| R03 | 物料搜索 | 搜索完整编号，只返回该物料。 | 通过 |
| R04 | 物料删除取消 | 打开删除确认，未勾选时确认按钮禁用；取消后记录仍在。 | 通过 |
| R05 | 新建自制工种 | `MANUAL-0915-IN-R`；手动自制工种；产能备注“资源资格手动验收”。详情关联设备/人员初始均为0。 | 通过 |
| R06 | 修改自制工种 | 产能备注改为“资源资格手动验收-已修改”，保存并整页刷新后列表回读一致。 | 通过 |
| R07 | 新建设备 | `MANUAL-0915-EQ-R`；手动测试设备；选择刚建自制工种；状态可用，设备组未选。 | 通过 |
| R08 | 设备详情/修改 | 详情读到绑定“手动自制工种”；名称改“手动测试设备-已修改”，保存并整页刷新后保留。 | 通过 |
| R09 | 新建人员 | `MANUAL-0915-OP-R`；手动测试人员；勾选手动自制工种；在岗；班次未选。 | 通过 |
| R10 | 人员设备授权入口 | 从人员详情点击“编辑可操作设备”，选择 `MANUAL-0915-EQ-R`，添加关联，设“熟练”和“主操：是”。 | 通过 |
| R11 | 人员授权预览/确认 | 预览列出新增关联的确切设备；点击“确认保存设备关联”，显示“已重新读取人员资料，设备关联已保存”。 | 通过 |
| R12 | 人员授权刷新回读 | 关闭并整页刷新，再开人员详情，显示测试设备编号、修改后的设备名称及“主操”。 | 通过 |
| R13 | 编辑人员不清空授权 | 改姓名“手动测试人员-已修改”；保存完成后的回读区域仍显示设备及主操；整页刷新后人员列表显示新姓名。 | 通过 |
| R14 | 新建外协工种 | `MANUAL-0915-OUT-R`；手动外协工种；默认周期规则“分别设置”；备注“外协资源手动验收”。 | 通过 |
| R15 | 修改外协工种 | 备注改为“外协资源手动验收-已修改”，保存并整页刷新后列表保留。 | 通过 |
| R16 | 新建供应商 | `MANUAL-0915-SUP-R`；手动测试供应商；勾选手动外协工种；默认周期2天；启用。 | 通过 |
| R17 | 供应商必填验证 | 编辑周期时原生 AX setValue 未保留数值，保存显示“请填写大于0的默认周期”并定位该输入；没有提交空周期。改用键盘输入3后提示消失。 | 通过；不把工具填写差异认定产品缺陷 |
| R18 | 修改供应商 | 名称改“手动测试供应商-已修改”、周期3天；保存并整页刷新，列表显示新名称及3.0。 | 通过 |
| R19 | 删除前创建备份 | 系统→备份恢复→新增备份→确认新增；显示“备份已新增并通过完整性检查”；生成下节列出的备份。确认结果后才进行删除。 | 通过 |
| R20 | 删除供应商 | 删除确认精确显示测试供应商；勾选关联核对后确认；整页刷新供应商总数2→1。 | 通过 |
| R21 | 删除外协工种 | 先删除供应商后删除测试外协工种；整页刷新总数2→1。 | 通过 |
| R22 | 删除人员 | 删除测试人员及其测试关系；整页刷新人员总数4→3。 | 通过 |
| R23 | 删除设备 | 人员删除后删除测试设备；整页刷新设备总数4→3。 | 通过 |
| R24 | 删除自制工种 | 测试设备和人员删除后删除工种；整页刷新总数4→3、未关联设备数归0。 | 通过 |
| R25 | 删除物料 | 勾选确认后删除测试物料；整页刷新物料总数1→0。 | 通过 |
| R26 | 已有资料引用保护 | 既有 QA 工种、设备、人员、供应商的删除按钮禁用且显示被其他资料引用；没有通过脚本绕开禁用。 | 通过（UI禁止入口；未模拟服务端恶意提交） |
| R27 | 人员状态筛选 | 全部状态3条→选择停用0条，显示“当前筛选没有匹配项”→清除筛选恢复3条。 | 通过 |
| R28 | 每页条数 | 人员每页20项→50项，控件回读50项。资源数据不足一页，不冒充资源跨页翻页通过。 | 通过 |
| R29 | 样例分页 | 系统管理→管理样例→备份恢复，点击下一页，1/3→2/3，范围第1–10条→第11–20条。 | 通过 |
| R30 | 资源族切换与详情 | 六类产能链入口逐一进入；各类新建/详情/编辑/关闭已真实使用。 | 通过 |
| R31 | 六类资源删除图标 | 物料实际测试行、其余五类列表的单行和批量删除均通过CUA截图目视看到垃圾桶；文字“删除”/“批量删除”可见。 | 通过 |
| R32 | 系统样例删除图标 | 管理样例备份恢复中的禁用“删除备份”仍显示垃圾桶；禁用无真实写入。 | 通过 |
| R33 | 真实备份删除图标 | 打开本轨新建备份详情，“删除备份”显示垃圾桶；保留该备份作为恢复点，没有真的删除它。 | 图标通过；备份删除动作未执行 |

## 数据保留与备份文件

删除前由 UI 创建的完整测试恢复点：

`/private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-manual-resources-20260915-494scls2/backups/aps_backup_20260915_210246_manual_35dda0faaac1.db`

- 实际文件大小：16,457,728字节，与系统详情显示一致。
- SHA-256：`1c3586f061104f267ddf8e95a420366cfb9bca049e1d92250819f98cbd3e07a7`。
- UI建备份前六类测试记录及人员设备关系均已保存，因此本恢复点保留本轮测试现场；文件核对只读，不用于脚本恢复或篡改业务。
- 最后UI回读：物料0、自制工种3、外协工种1、设备3、人员3、供应商1，与起步这些资源数量一致；删除产生的操作审计和已创建备份保留。
- 本轨未从浏览器下载导出文件；上述文件是产品在隔离实例备份目录生成的备份，不能称为浏览器下载成功。

## 限制与发现

1. 删除完成的结果文案仍是“保存已完成”。真实删除通过刷新数量和记录消失确认，建议改为“删除已完成”；已经报告主代理。本轨未改代码。
2. 资源列表的引用禁用说明逐行完整展开，会使三行记录占较大高度；文字可读、垃圾桶可见，仍属于可继续收紧的布局项。未把它作为业务失败。
3. 截图证据由本任务 CUA 的 `getScreenshot()` 直接返回，未生成本地PNG文件；不提供虚构截图路径。画面包含原生Chrome窗口及5003地址。全站工艺/批次动作由其他手动轨负责，本记录不代替那些页面验收。
4. 未在本轨验证资源批量导入/导出、列筛选组合、多页资源数据、设备组/班次目录CRUD、设备授权移除后实际排产、真正备份删除、恢复并重启。资源量不足一页时只核验每页数量与边界禁用；真实跨页使用管理样例验证。
5. 未读取生产数据库、未写5000实例。当前资源数量回到基线不等于整库逐行无变化，也不等于清空审计。本轨仅声称表中动作通过，不声称“全部功能已通过”或 clean-worktree proof。
