# 正向手动功能组补验进度

这是对当前可达控件声明与本轮manual记录的覆盖核对，不是产品审计或新测试。旧`acceptance-master/actions.json`只有基础资料/批次58族599个动作，其他入口参考当前live声明；不把599个文本动作当作全部功能分母，不把旧原型记录传播为本轮通过。

本表记录最初发现的7组缺项及当前进度，更新到2026-09-16 00:04后的主代理记录。G3/G4/G5/G7已闭环，G1草稿放弃已闭环、正向采用仍在完成；G2/G6已收到执行成功回报，详细记录待执行代理归档。总验收仍等试调正向采用和最终5000激活/数据证明，不重开其他已通过组。来源相对仓库根，`app/`指`frontend/workbench/app/`。

| 组 | 当前可达入口与定位 | 最初缺项的区别 | 当前结果与证据 |
|---|---|---|---|
| G1 试调生命周期 | `app/TrialWorkspace.jsx:123`放弃草稿；已保存方案的正式采用入口 | 候选采用v15不等于试调采用；关闭弹窗不等于归档持久草稿 | **部分完成**：5001独立临时草稿确认放弃→刷新仍为已放弃、历史可打开、保存/采用禁用，原两方案及v14不变。5005正向采用发现真实执行固定冲突，响应级别和执行固定合同已修，正向手动继续，见[试调记录](manual-trial-adoption.md)。 |
| G2 现场报工文件输入 | `app/FieldFiles.jsx:25`确认导入 | 报表导出不执行报工导入事务 | **已回报执行成功，等归档**：5002代理完成有效报工导入。18行预检曾超出矮屏，业务通过键盘确认成功；弹窗限高/滚动已修，最终界面回验及文件细节以代理记录为准。 |
| G3 物料库存专用动作 | `app/ResourceForms.jsx:186`调整库存，`ResourceWorkspace.jsx:294`接线 | 新增库存与stockOnly入口不同 | **完成**：RESTORE库存7→12，保存刷新为12，其他字段保留。见[补充手动记录](manual-final-functional-supplement.md)。 |
| G4 批次维护写入 | `app/BatchWorkspace.jsx:152/154`批改/删所选；`BatchDetail.jsx:43`补充资料 | 模板生成/复制/原文件回导不等于批量patch、集合删除或直接资源指定 | **完成**：BA/BB批改为急件/9月22/备注；BA自制M01配OP02被拒，改OP01并单件0.2→0.25成功；外协2→3天保存；备份后批删BA四道工序及BB空批，刷新为0。见补充手动记录。 |
| G5 基础资料/工艺批量删除 | `app/ResourceWorkspace.jsx:95`、`ProcessWorkspace.jsx:158` | 单项删除/垃圾桶目视不等于批量事务 | **完成**：MA/MB两物料及PA/PB两工艺分别预览、确认批删、刷新0；原RESTORE物料库存12及原4工艺保留。见补充手动记录。 |
| G6 系统维护与交付 | 维护配置保存；备份下载/删除；日志CSV/诊断ZIP/诊断文件 | 创建/恢复备份不能代替配置和文件交付 | **已回报执行成功，等归档**：5002系统操作由代理完成；自写日志使导出stale已修，CSV与ZIP内容一致成功回报。专用备份已创建/下载核验并到删除预览，但用户明确要求保留，最终删除不执行且不再算阻塞，见下节。 |
| G7 各工作区独立查询/导出 | 总览、候选、计划/实际甘特、试调的独立结果入口 | 报表文件不证明这些入口的身份/范围 | **完成**：总览OP01关联M01并进入维护，筛设备导出3行CSV；实际甘特18×47 CSV排除撤销BG1；计划甘特18×32 XLSX及交付风险定位；候选6项比较与2×30 CSV；保存试调1×22对比CSV和2任务原始JSON，前后延后72小时一致。见补充手动记录，全部真实文件有SHA。 |

## 用户确定保留的备份

5002备份`aps_backup_20260915_231723_manual_d6ade12f3efb.db`已创建、下载并核验，已打开删除预览。用户明确回复“保留，记录为未执行”，因此最终删除记录为**用户要求保留，未执行**。此项从待执行范围排除，不构成欠验阻塞，不再询问或删除备份/完整下载副本。

## 明确不增列的项目

- 单项资源CRUD、人员设备授权新增/编辑保留、工艺三步/零值/51道跨页、批次新增/修改/复制/模板生成、文件原样回导、候选排产/采用、报工新增/更正/撤销、外协单道/合并/回厂、校准、处置跟进/关闭/重开、全局日历和班次/设备组维护、恢复后原标签重新排产，均已有组合手动证据，不重开。
- `Process`旧“资料不完整停止刷新”和逐行核对已删除；旧原型“重置样板”、管理样例禁用按钮不是正式业务写入口。
- 当前没有独立个人专属班表维护入口；停机区域当前live声明只读登记依据，没有找到新增/编辑停机按钮，不凭原型/旧路由补造功能。
- 批次导入overwrite/append/replace同属一个可达导入向导，当前已实际验证overwrite原文件回导；其余模式保留为语义覆盖边界，不拆成额外三组。CSV/XLSX、尺寸、分页档位、键盘/鼠标及错误值组合也不拆成独立功能组。
- 除明确对外文件交付外，不把每个只读筛选、hover、折叠、排序都计为新功能组；必要导航可与上表事务和下载一起完成。

最初缺证据的7组经补验发现了实际产品问题，各修复和专项见实施盘点。已完成G3–G5/G7及G1放弃不重复执行；G2/G6成功细节归档后引用，最终等5005试调正向采用及5000最新激活保留证明再收口。总验收继续in-progress；本清单只改文档，没有运行测试或门禁。
