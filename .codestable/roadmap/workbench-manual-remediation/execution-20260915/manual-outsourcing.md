# B2 外协手动验收环境与交接

日期：2026-09-15。当前状态：**主代理已通过真实 IAB 完成单道登记、刷新回读、回厂更正与历史保留；跨批次合并限制通过。随后通过页面创建同批次两道外协，合并发出成功分支也已完成并刷新回读。**

## 隔离环境

- 入口：[5002 独立工作台](http://127.0.0.1:5002/workbench)。请从工作台实际导航进入外协登记。
- 监听 PID：`91606`；命令执行会话：`77527`。服务保持运行供主代理核验。
- 隔离根目录：`/private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-manual-outsourcing-20260915-1lytmyfn`。
- 测试库：`/private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-manual-outsourcing-20260915-1lytmyfn/db/aps.db`。
- 复制来源：`/Users/lurenxing/GitHub/----/backups/aps_backup_20260915_183234_manual_5aec6fc5e1a9.db`。
- 复制方法：来源库以 `mode=ro` 打开，通过 SQLite Backup API 复制到 `tempfile.mkdtemp(...).resolve()` 下的数据库；未 seed 或修改任何业务行。
- 启动日志确认应用自行完成 `SchemaVersion 31 -> 32`，并在隔离目录保留迁移前备份：`backups/aps_backup_20260915_204937_before_migrate_v31_to_v32.db`。
- 未启动全门禁；未 build/static；未进入 `127.0.0.1:5000`，未迁移正式库。

隔离目录 `manual_app.py`：

```python
import sys
sys.path.insert(0, '/Users/lurenxing/GitHub/----')
from web.bootstrap.entrypoint import app_main
app_main('default', anchor_file=__file__)
```

启动命令（从仓库目录执行）：

```bash
APS_ENV=production \
APS_SHARED_DATA_ROOT=/private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-manual-outsourcing-20260915-1lytmyfn \
APS_HOST=127.0.0.1 APS_PORT=5002 \
.venv/bin/python -B /private/var/folders/cz/6c_ysj195sbcytsy44zttdbc0000gn/T/aps-manual-outsourcing-20260915-1lytmyfn/manual_app.py
```

## 本子代理实际做过的界面动作

1. `mcp__cua_repl` 的浏览器清单为空；创建 `iab` 和 `chrome` 浏览器标签均返回 `Browser is not available`。
2. 改用 `cua.getApp('com.google.Chrome')` 原生界面。新建独立标签，地址栏输入 `http://127.0.0.1:5002/workbench?view=outsourcing`，显示“这个页面不存在，页面没有打开。请从侧栏重新进入。”**该 URL 不是有效的主工作台视图，不能用它判外协功能故障。**
3. 尝试点“打开工作台”时，元素引用失效。刷新 AX 发现当前 Chrome 前台已是另一验收代理的 `5003/workbench?view=process`；没有继续点击或填写。
4. 已报告原生 Chrome 焦点冲突。主代理明确原生 Chrome 由 `shared_controls_impl` 独占，本代理停止原生操作，5002 改由主代理独立 IAB 接管。

**本轮没有完成任何外协业务创建、保存、刷新、更正或合并登记，没有手动通过项。**上述操作只有工具返回的 AX 证据，尚无本次业务截图。服务和隔离库保留，供后续逐动作追加结果。

## 主代理最短手动用例序列

全部通过实际界面的点击与填写执行；不得以 API、SQL 或 Playwright 脚本代替业务动作。

| 步骤 | 界面动作 | 应核对的结果 | 当前结果 |
|---|---|---|---|
| 1 | 打开 5002 工作台，从实际导航进入外协登记，点“新增外协登记”。 | 当前旧外协工序数量、可选状态、批次/供应商清晰；本次基线预期有 10 道，必须以页面回读确认。 | 未执行 |
| 2 | 保持“单工序”，选一条未登记工序；记下工序、批次和供应商编号。 | 选中的是该行真实来源，无单独“旧数据确认”勾选。 | 未执行 |
| 3 | 填实际发出 `2026-09-15 09:00`、计划回厂 `2026-09-16 09:00`、状态“在途”、经办人“手动验收-B2”、原因“B2隔离环境单道外协登记”；点“预检核对”。 | 预览中的批次、图号、供应商、单道成员与步骤 2 相符；出现正常确认保存，无额外核对复选框。 | 未执行 |
| 4 | 点“确认保存外协登记”，看到完成结果后点“完成”。 | 结果明确完成；列表增加一条，日期/状态正确；原工序入口改为已有登记。 | 未执行 |
| 5 | 刷新列表或浏览器，重新打开刚才登记的“详情”。 | 同一登记回读成功，没有来源变化错误；工序与图号一致。 | 未执行 |
| 6 | 点“核实 / 更正登记”，实际回厂填 `2026-09-15 10:00`、状态“已回厂”，经办人与更正原因填写后预检并确认保存。 | 保存后状态、实际回厂时间正确；刷新详情仍可查看，更正历史增加一条，未要求重新绑定批次。 | 未执行 |
| 7 | 再点新增，选“合并发出”，检查能否找到同一批次、供应商、分件的至少两道未登记外协工序。 | 有符合条件的成员则完成预检→保存→刷新，核对全部成员一致；没有则如实记录“基线没有可合并成员”，不编造数据或用跨批次拼组。 | 未执行 |
| 8 | 在合并选择界面尝试选不同批次或供应商的第二成员。 | 界面明确不能合并；不提交无效混组。完成后截图保留列表、详情/历史和成员情况。 | 未执行 |

步骤中的时间以实际执行当天页面允许范围为准；如果执行日期已变化，填写真实不晚于当前时间的实际发出/回厂时间，不通过改时钟满足校验。

## 主代理追加的实际业务验收（21:56—21:59）

- 从值班台“外协回厂”进入：共 10 道未登记工序、来源缺口 0。
- 新增选择 BROWSER-0510-A_20，图号 QA-20260509-P01，供应商 QA外协表处理厂；实际发出 2026-09-15 09:00、计划回厂 2026-09-16 09:00、在途、经办人 手动验收-B2。预检正常展示真实批次和图号，没有额外旧数据复选框。点击确认后明确显示“外协登记已完成”。
- 点击完成并刷新浏览器：列表保存 1 条、待回厂 1、未登记 9。详情仍是同一工序及来源。
- 从详情“核实 / 更正登记”填写实际回厂 2026-09-15 10:00、已回厂和原因；预检展示前后值，确认完成。再次刷新并打开详情，已回厂 1、待回厂 0，并同时保留第 1 次在途和第 2 次已回厂历史。
- 新增选择合并发出，选 TST-0510-C01_20 后其余批次成员均禁用，清楚显示“不同批次 / 供应商”。BROWSER-0510-A_20 已登记不能重复选择。基线其余外协工序各属不同批次，没有可合并的第二成员；未伪造成功。
- 取消合并草稿出现未保存内容提示，点“放弃未保存内容并继续”回到已有登记；截图已在 CUA 工具输出保留。所有业务动作均为页面点击和填写，未通过 API/SQL 写业务数据。

## 原交接时的未覆盖说明（现以追加结果为准）

### 追加：合并发出成功分支（22:33—22:39）

- 基线没有同批次两道外协，因此全部通过真实UI新建零件 `MANUAL-OUT-P0915`、路线 `5QA表处理10QA表处理`；路线预检识别2道，保存路线、一次保存归属、保存各2天外协周期，工艺就绪。
- UI新增批次 `MANUAL-OUT-B0915`、数量2、交期9月20、齐套9月15；预览模板更新新增2道外协，确认生成。未通过SQL或API准备样本。
- 外协新增选择合并发出，选该批次 `_05` 与 `_10`；预览显示同一批次、图号、供应商、合并2道，填写实际发出9月15日11:00、计划回厂9月18日11:00、经办人“手动验收-B2-合并”。
- 确认保存后列表显示两成员合并行；浏览器刷新再打开详情，两个工序、图号、计划回厂和第1次历史均正确保留。

### 追加：值班台处置与重开（22:56—22:57）

- 从本次合并外协条目进入登记处置，页面正确表示“当前无风险”（尚未到计划回厂时间）。记录责任人“手动验收计划员”、期限9月18、行动为核对合并登记与回厂日期，保存为跟进中。
- 编辑该处置为已关闭，填写核查完成时间、具体核查结果、凭据说明；回执明确处置和历史已保存。这里关闭的是登记核查处置，没有把在途外协改成已回厂。
- 查看历史保留待分析→跟进中、跟进中→已关闭两条；再用“独立重开”填写继续跟踪实际回厂的原因，保存后状态跟进中。刷新浏览器，第三条已关闭→跟进中及前两条完成历史都可读。

本次所有业务手动步骤均待主代理执行。没有验证真实页面的 10 道可选择、单道创建、更正回读、合并登记、计数更新、历史展示，也没有验证浏览器刷新后的保留情况。此前 pytest/Chromium109 结果在独立修复记录中，不能替代此处结果。
